// cjs require begin








import * as fan from './fan.js'

import * as sys from './sys.js'
// cjs require end
const js = (typeof window !== 'undefined') ? window : global;
class CompilerErr extends sys.Err {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return CompilerErr.type$; }

  #level = null;

  level() { return this.#level; }

  __level(it) { if (it === undefined) return this.#level; else this.#level = it; }

  #file = null;

  file() { return this.#file; }

  __file(it) { if (it === undefined) return this.#file; else this.#file = it; }

  #line = null;

  line() { return this.#line; }

  __line(it) { if (it === undefined) return this.#line; else this.#line = it; }

  #col = null;

  col() { return this.#col; }

  __col(it) { if (it === undefined) return this.#col; else this.#col = it; }

  static make(msg,loc,cause,level) {
    const $self = new CompilerErr();
    CompilerErr.make$($self,msg,loc,cause,level);
    return $self;
  }

  static make$($self,msg,loc,cause,level) {
    if (cause === undefined) cause = null;
    if (level === undefined) level = sys.LogLevel.err();
    sys.Err.make$($self, msg, cause);
    $self.#level = level;
    if (loc != null) {
      $self.#file = loc.file();
      $self.#line = loc.line();
      $self.#col = loc.col();
    }
    ;
    return;
  }

  loc() {
    return Loc.make(sys.ObjUtil.coerce(((this$) => { let $_u0 = this$.#file; if ($_u0 != null) return $_u0; return "Unknown"; })(this), sys.Str.type$), this.#line, this.#col);
  }

  isErr() {
    return this.#level === sys.LogLevel.err();
  }

  isWarn() {
    return this.#level === sys.LogLevel.warn();
  }

}

class CompilerLog extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#level = sys.LogLevel.info();
    this.#indentation = 0;
    this.#out = null;
    return;
  }

  typeof() { return CompilerLog.type$; }

  #level = null;

  level(it) {
    if (it === undefined) {
      return this.#level;
    }
    else {
      this.#level = it;
      return;
    }
  }

  #indentation = 0;

  indentation(it) {
    if (it === undefined) {
      return this.#indentation;
    }
    else {
      this.#indentation = it;
      return;
    }
  }

  #out = null;

  out(it) {
    if (it === undefined) {
      return this.#out;
    }
    else {
      this.#out = it;
      return;
    }
  }

  static make(out) {
    const $self = new CompilerLog();
    CompilerLog.make$($self,out);
    return $self;
  }

  static make$($self,out) {
    if (out === undefined) out = sys.Env.cur().out();
    ;
    $self.#out = out;
    return;
  }

  isDebug() {
    return sys.ObjUtil.compareLE(this.#level, sys.LogLevel.debug());
  }

  indent() {
    ((this$) => { let $_u1 = this$.#indentation;this$.#indentation = sys.Int.increment(this$.#indentation); return $_u1; })(this);
    return;
  }

  unindent() {
    ((this$) => { let $_u2 = this$.#indentation;this$.#indentation = sys.Int.decrement(this$.#indentation); return $_u2; })(this);
    if (sys.ObjUtil.compareLT(this.#indentation, 0)) {
      this.#indentation = 0;
    }
    ;
    return;
  }

  err(msg,err) {
    if (err === undefined) err = null;
    this.log(sys.LogRec.make(sys.DateTime.now(), sys.LogLevel.err(), "compiler", msg, err));
    return;
  }

  warn(msg,err) {
    if (err === undefined) err = null;
    this.log(sys.LogRec.make(sys.DateTime.now(), sys.LogLevel.warn(), "compiler", msg, err));
    return;
  }

  info(msg,err) {
    if (err === undefined) err = null;
    this.log(sys.LogRec.make(sys.DateTime.now(), sys.LogLevel.info(), "compiler", msg, err));
    return;
  }

  debug(msg,err) {
    if (err === undefined) err = null;
    this.log(sys.LogRec.make(sys.DateTime.now(), sys.LogLevel.debug(), "compiler", msg, err));
    return;
  }

  log(rec) {
    if (sys.ObjUtil.compareLT(rec.level(), this.#level)) {
      return;
    }
    ;
    if (sys.ObjUtil.compareGE(rec.level(), sys.LogLevel.warn())) {
      this.print(sys.Str.upper(rec.level().toStr())).print(": ");
    }
    else {
      this.print(sys.Str.spaces(sys.Int.mult(this.#indentation, 2)));
    }
    ;
    this.printLine(rec.msg());
    if (rec.err() != null) {
      if (this.isDebug()) {
        rec.err().trace(sys.ObjUtil.coerce(this.#out, sys.OutStream.type$));
      }
      else {
        this.print(sys.Str.spaces(sys.Int.plus(sys.Int.mult(this.#indentation, 2), 4))).printLine(rec.err());
      }
      ;
    }
    ;
    return;
  }

  compilerErr(err) {
    if (sys.ObjUtil.compareLT(err.level(), this.#level)) {
      return;
    }
    ;
    let loc = err.loc().toLocStr();
    if (err.isWarn()) {
      this.printLine(sys.Str.plus(sys.Str.plus(sys.Str.plus("", loc), ": WARN "), err.msg()));
    }
    else {
      this.printLine(sys.Str.plus(sys.Str.plus(sys.Str.plus("", loc), ": "), err.msg()));
    }
    ;
    if (this.isDebug()) {
      err.trace(sys.ObjUtil.coerce(this.#out, sys.OutStream.type$));
    }
    ;
    return;
  }

  print(s) {
    this.#out.print(s);
    return this;
  }

  printLine(s) {
    if (s === undefined) s = "";
    sys.ObjUtil.coerce(this.#out.printLine(s), sys.OutStream.type$.toNullable()).flush();
    return this;
  }

}

class Loc extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#file = "Unknown";
    return;
  }

  typeof() { return Loc.type$; }

  #file = null;

  file(it) {
    if (it === undefined) {
      return this.#file;
    }
    else {
      this.#file = it;
      return;
    }
  }

  #line = null;

  line(it) {
    if (it === undefined) {
      return this.#line;
    }
    else {
      this.#line = it;
      return;
    }
  }

  #col = null;

  col(it) {
    if (it === undefined) {
      return this.#col;
    }
    else {
      this.#col = it;
      return;
    }
  }

  static make(file,line,col) {
    const $self = new Loc();
    Loc.make$($self,file,line,col);
    return $self;
  }

  static make$($self,file,line,col) {
    if (line === undefined) line = null;
    if (col === undefined) col = null;
    ;
    $self.#file = file;
    $self.#line = line;
    $self.#col = col;
    return;
  }

  static makeFile(file,line,col) {
    const $self = new Loc();
    Loc.makeFile$($self,file,line,col);
    return $self;
  }

  static makeFile$($self,file,line,col) {
    if (line === undefined) line = null;
    if (col === undefined) col = null;
    ;
    if (file != null) {
      let osPath = file.osPath();
      if (osPath != null) {
        $self.#file = sys.ObjUtil.coerce(osPath, sys.Str.type$);
      }
      else {
        $self.#file = file.pathStr();
      }
      ;
    }
    ;
    $self.#line = line;
    $self.#col = col;
    return;
  }

  static makeUninit() {
    const $self = new Loc();
    Loc.makeUninit$($self);
    return $self;
  }

  static makeUninit$($self) {
    ;
    return;
  }

  filename() {
    let f = this.#file;
    let slash = sys.Str.indexr(f, "/");
    if (slash == null) {
      (slash = sys.Str.indexr(f, "\\"));
    }
    ;
    if (slash != null) {
      (f = sys.Str.getRange(f, sys.Range.make(sys.Int.plus(sys.ObjUtil.coerce(slash, sys.Int.type$), 1), -1)));
    }
    ;
    return f;
  }

  fileUri() {
    try {
      return sys.File.os(this.#file).uri().toStr();
    }
    catch ($_u3) {
      return this.#file;
    }
    ;
  }

  hash() {
    let hash = sys.Str.hash(this.#file);
    if (this.#line != null) {
      (hash = sys.Int.xor(hash, sys.Int.hash(sys.ObjUtil.coerce(this.#line, sys.Int.type$))));
    }
    ;
    if (this.#col != null) {
      (hash = sys.Int.xor(hash, sys.Int.hash(sys.ObjUtil.coerce(this.#col, sys.Int.type$))));
    }
    ;
    return hash;
  }

  equals(that) {
    let x = sys.ObjUtil.as(that, Loc.type$);
    if (x == null) {
      return false;
    }
    ;
    return (sys.ObjUtil.equals(this.#file, x.#file) && sys.ObjUtil.equals(this.#line, x.#line) && sys.ObjUtil.equals(this.#col, x.#col));
  }

  compare(that) {
    let x = sys.ObjUtil.coerce(that, Loc.type$);
    if (sys.ObjUtil.compareNE(this.#file, x.#file)) {
      return sys.ObjUtil.compare(this.#file, x.#file);
    }
    ;
    if (sys.ObjUtil.compareNE(this.#line, x.#line)) {
      return sys.ObjUtil.compare(this.#line, x.#line);
    }
    ;
    return sys.ObjUtil.compare(this.#col, x.#col);
  }

  toStr() {
    return this.toLocStr();
  }

  toLocStr() {
    let s = sys.StrBuf.make();
    s.add(this.#file);
    if (this.#line != null) {
      s.add("(").add(sys.ObjUtil.coerce(this.#line, sys.Obj.type$.toNullable()));
      if (this.#col != null) {
        s.add(",").add(sys.ObjUtil.coerce(this.#col, sys.Obj.type$.toNullable()));
      }
      ;
      s.add(")");
    }
    ;
    return s.toStr();
  }

}

class Compiler extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#isSys = false;
    return;
  }

  typeof() { return Compiler.type$; }

  #input = null;

  input(it) {
    if (it === undefined) {
      return this.#input;
    }
    else {
      this.#input = it;
      return;
    }
  }

  #log = null;

  log(it) {
    if (it === undefined) {
      return this.#log;
    }
    else {
      this.#log = it;
      return;
    }
  }

  #errs = null;

  errs(it) {
    if (it === undefined) {
      return this.#errs;
    }
    else {
      this.#errs = it;
      return;
    }
  }

  #warns = null;

  warns(it) {
    if (it === undefined) {
      return this.#warns;
    }
    else {
      this.#warns = it;
      return;
    }
  }

  #depends = null;

  depends(it) {
    if (it === undefined) {
      return this.#depends;
    }
    else {
      this.#depends = it;
      return;
    }
  }

  #ns = null;

  ns(it) {
    if (it === undefined) {
      return this.#ns;
    }
    else {
      this.#ns = it;
      return;
    }
  }

  #pod = null;

  pod(it) {
    if (it === undefined) {
      return this.#pod;
    }
    else {
      this.#pod = it;
      return;
    }
  }

  #isSys = false;

  isSys(it) {
    if (it === undefined) {
      return this.#isSys;
    }
    else {
      this.#isSys = it;
      return;
    }
  }

  #srcFiles = null;

  srcFiles(it) {
    if (it === undefined) {
      return this.#srcFiles;
    }
    else {
      this.#srcFiles = it;
      return;
    }
  }

  #resFiles = null;

  resFiles(it) {
    if (it === undefined) {
      return this.#resFiles;
    }
    else {
      this.#resFiles = it;
      return;
    }
  }

  #jsFiles = null;

  jsFiles(it) {
    if (it === undefined) {
      return this.#jsFiles;
    }
    else {
      this.#jsFiles = it;
      return;
    }
  }

  #jsPropsFiles = null;

  jsPropsFiles(it) {
    if (it === undefined) {
      return this.#jsPropsFiles;
    }
    else {
      this.#jsPropsFiles = it;
      return;
    }
  }

  #types = null;

  types(it) {
    if (it === undefined) {
      return this.#types;
    }
    else {
      this.#types = it;
      return;
    }
  }

  #closures = null;

  closures(it) {
    if (it === undefined) {
      return this.#closures;
    }
    else {
      this.#closures = it;
      return;
    }
  }

  #wrappers = null;

  wrappers(it) {
    if (it === undefined) {
      return this.#wrappers;
    }
    else {
      this.#wrappers = it;
      return;
    }
  }

  #jsPod = null;

  jsPod(it) {
    if (it === undefined) {
      return this.#jsPod;
    }
    else {
      this.#jsPod = it;
      return;
    }
  }

  #js = null;

  js(it) {
    if (it === undefined) {
      return this.#js;
    }
    else {
      this.#js = it;
      return;
    }
  }

  #jsSourceMap = null;

  jsSourceMap(it) {
    if (it === undefined) {
      return this.#jsSourceMap;
    }
    else {
      this.#jsSourceMap = it;
      return;
    }
  }

  #cjs = null;

  cjs(it) {
    if (it === undefined) {
      return this.#cjs;
    }
    else {
      this.#cjs = it;
      return;
    }
  }

  #cjsSourceMap = null;

  cjsSourceMap(it) {
    if (it === undefined) {
      return this.#cjsSourceMap;
    }
    else {
      this.#cjsSourceMap = it;
      return;
    }
  }

  #esm = null;

  esm(it) {
    if (it === undefined) {
      return this.#esm;
    }
    else {
      this.#esm = it;
      return;
    }
  }

  #tsDecl = null;

  tsDecl(it) {
    if (it === undefined) {
      return this.#tsDecl;
    }
    else {
      this.#tsDecl = it;
      return;
    }
  }

  #localeDefs = null;

  localeDefs(it) {
    if (it === undefined) {
      return this.#localeDefs;
    }
    else {
      this.#localeDefs = it;
      return;
    }
  }

  #localeProps = null;

  localeProps(it) {
    if (it === undefined) {
      return this.#localeProps;
    }
    else {
      this.#localeProps = it;
      return;
    }
  }

  #fpod = null;

  fpod(it) {
    if (it === undefined) {
      return this.#fpod;
    }
    else {
      this.#fpod = it;
      return;
    }
  }

  #output = null;

  output(it) {
    if (it === undefined) {
      return this.#output;
    }
    else {
      this.#output = it;
      return;
    }
  }

  static make(input) {
    const $self = new Compiler();
    Compiler.make$($self,input);
    return $self;
  }

  static make$($self,input) {
    ;
    if (sys.ObjUtil.coerce(input.log(), sys.Obj.type$.toNullable()) == null) {
      throw sys.ArgErr.make("CompilerInput.log is null");
    }
    ;
    $self.#input = input;
    $self.#log = input.log();
    $self.#errs = sys.List.make(CompilerErr.type$);
    $self.#warns = sys.List.make(CompilerErr.type$);
    $self.#depends = sys.List.make(CDepend.type$);
    $self.#wrappers = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::CField"));
    $self.#localeDefs = sys.List.make(LocaleLiteralExpr.type$);
    return;
  }

  compile() {
    this.#log.info(sys.Str.plus(sys.Str.plus("Compile [", this.#input.podName()), "]"));
    this.#log.indent();
    try {
      this.frontend();
      this.backend();
    }
    catch ($_u4) {
      $_u4 = sys.Err.make($_u4);
      if ($_u4 instanceof CompilerErr) {
        let e = $_u4;
        ;
        if (this.#errs.isEmpty()) {
          CompilerSupport.make(this).errReport(e);
        }
        ;
        throw e;
      }
      else {
        throw $_u4;
      }
    }
    finally {
      this.cleanup();
    }
    ;
    this.#log.unindent();
    return sys.ObjUtil.coerce(this.#output, CompilerOutput.type$);
  }

  frontend() {
    InitInput.make(this).run();
    Tokenize.make(this).run();
    ResolveDepends.make(this).run();
    ScanForUsingsAndTypes.make(this).run();
    ResolveImports.make(this).run();
    Parse.make(this).run();
    OrderByInheritance.make(this).run();
    CheckInheritance.make(this).run();
    Inherit.make(this).run();
    DefaultCtor.make(this).run();
    InitEnum.make(this).run();
    InitFacet.make(this).run();
    InitClosures.make(this).run();
    Normalize.make(this).run();
    ResolveExpr.make(this).run();
    CheckErrors.make(this).run();
    CheckParamDefs.make(this).run();
    LocaleProps.make(this).run();
    CompileJs.make(this).run();
    ClosureVars.make(this).run();
    ClosureToImmutable.make(this).run();
    ConstChecks.make(this).run();
    return;
  }

  backend() {
    Assemble.make(this).run();
    GenerateOutput.make(this).run();
    return;
  }

  cleanup() {
    try {
      this.#ns.cleanup();
    }
    catch ($_u5) {
      $_u5 = sys.Err.make($_u5);
      if ($_u5 instanceof sys.Err) {
        let e = $_u5;
        ;
        e.trace();
      }
      else {
        throw $_u5;
      }
    }
    ;
    return;
  }

}

class CompilerSupport extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#suppressErr = false;
    return;
  }

  typeof() { return CompilerSupport.type$; }

  #c = null;

  // private field reflection only
  __c(it) { if (it === undefined) return this.#c; else this.#c = it; }

  #suppressErr = false;

  suppressErr(it) {
    if (it === undefined) {
      return this.#suppressErr;
    }
    else {
      this.#suppressErr = it;
      return;
    }
  }

  static make(compiler) {
    const $self = new CompilerSupport();
    CompilerSupport.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    ;
    $self.#c = compiler;
    return;
  }

  compiler() {
    return this.#c;
  }

  ns() {
    return sys.ObjUtil.coerce(this.#c.ns(), CNamespace.type$);
  }

  pod() {
    return sys.ObjUtil.coerce(this.#c.pod(), PodDef.type$);
  }

  units() {
    return this.#c.pod().units();
  }

  syntheticsUnit() {
    return sys.ObjUtil.coerce(this.#c.pod().units().first(), CompilationUnit.type$);
  }

  types() {
    return sys.ObjUtil.coerce(this.#c.types(), sys.Type.find("compiler::TypeDef[]"));
  }

  log() {
    return this.#c.log();
  }

  addTypeDef(t) {
    t.unit().types().add(t);
    this.pod().typeDefs().set(t.name(), t);
    this.#c.types().add(t);
    return;
  }

  removeTypeDef(t) {
    t.unit().types().removeSame(t);
    this.pod().typeDefs().remove(t.name());
    this.#c.types().removeSame(t);
    return;
  }

  needParameterizedCollectionCoerce(t) {
    return (this.compiler().input().coerceParameterizedCollectionTypes() && t.isParameterized() && (t.isList() || t.isMap()));
  }

  err(msg,loc) {
    if (loc === undefined) loc = null;
    if (this.#suppressErr) {
      throw SuppressedErr.make();
    }
    ;
    return this.errReport(CompilerErr.make(msg, loc));
  }

  warn(msg,loc) {
    if (loc === undefined) loc = null;
    if (this.#suppressErr) {
      throw SuppressedErr.make();
    }
    ;
    return this.errReport(CompilerErr.make(msg, loc, null, sys.LogLevel.warn()));
  }

  errReport(e) {
    this.#c.log().compilerErr(e);
    if (e.isWarn()) {
      this.#c.warns().add(e);
    }
    else {
      this.#c.errs().add(e);
    }
    ;
    return e;
  }

  bombIfErr() {
    if (!this.#c.errs().isEmpty()) {
      throw sys.ObjUtil.coerce(this.#c.errs().first(), sys.Err.type$);
    }
    ;
    return;
  }

}

class Visitor {
  constructor() {
    const this$ = this;
  }

  typeof() { return Visitor.type$; }

  walk(c,depth) {
    const this$ = this;
    c.types().each((def) => {
      def.walk(this$, depth);
      return;
    });
    return;
  }

  enterUnit(unit) {
    return;
  }

  exitUnit(unit) {
    return;
  }

  enterTypeDef(def) {
    return;
  }

  exitTypeDef(def) {
    return;
  }

  visitTypeDef(def) {
    return;
  }

  enterFieldDef(def) {
    return;
  }

  exitFieldDef(def) {
    return;
  }

  visitFieldDef(def) {
    return;
  }

  enterMethodDef(def) {
    return;
  }

  exitMethodDef(def) {
    return;
  }

  visitMethodDef(def) {
    return;
  }

  enterBlock(block) {
    return;
  }

  exitBlock(block) {
    return;
  }

  visitBlock(block) {
    return;
  }

  enterStmt(stmt) {
    return;
  }

  exitStmt(stmt) {
    return;
  }

  visitStmt(stmt) {
    return null;
  }

  enterFinally(stmt) {
    return;
  }

  exitFinally(stmt) {
    return;
  }

  visitExpr(expr) {
    return expr;
  }

}

class CompilerStep extends CompilerSupport {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return CompilerStep.type$; }

  visitMethodDef() { return Visitor.prototype.visitMethodDef.apply(this, arguments); }

  visitTypeDef() { return Visitor.prototype.visitTypeDef.apply(this, arguments); }

  enterBlock() { return Visitor.prototype.enterBlock.apply(this, arguments); }

  visitBlock() { return Visitor.prototype.visitBlock.apply(this, arguments); }

  visitFieldDef() { return Visitor.prototype.visitFieldDef.apply(this, arguments); }

  exitBlock() { return Visitor.prototype.exitBlock.apply(this, arguments); }

  exitFieldDef() { return Visitor.prototype.exitFieldDef.apply(this, arguments); }

  enterFinally() { return Visitor.prototype.enterFinally.apply(this, arguments); }

  enterStmt() { return Visitor.prototype.enterStmt.apply(this, arguments); }

  exitFinally() { return Visitor.prototype.exitFinally.apply(this, arguments); }

  visitExpr() { return Visitor.prototype.visitExpr.apply(this, arguments); }

  enterFieldDef() { return Visitor.prototype.enterFieldDef.apply(this, arguments); }

  visitStmt() { return Visitor.prototype.visitStmt.apply(this, arguments); }

  walk() { return Visitor.prototype.walk.apply(this, arguments); }

  exitStmt() { return Visitor.prototype.exitStmt.apply(this, arguments); }

  #curType = null;

  curType(it) {
    if (it === undefined) {
      return this.#curType;
    }
    else {
      this.#curType = it;
      return;
    }
  }

  #curMethod = null;

  curMethod(it) {
    if (it === undefined) {
      return this.#curMethod;
    }
    else {
      this.#curMethod = it;
      return;
    }
  }

  #curUnit = null;

  curUnit(it) {
    if (it === undefined) {
      return this.#curUnit;
    }
    else {
      this.#curUnit = it;
      return;
    }
  }

  static make(compiler) {
    const $self = new CompilerStep();
    CompilerStep.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerSupport.make$($self, compiler);
    return;
  }

  inStatic() {
    return (this.#curMethod == null || this.#curMethod.isStatic());
  }

  enterUnit(unit) {
    this.#curUnit = unit;
    return;
  }

  exitUnit(unit) {
    this.#curUnit = null;
    return;
  }

  enterTypeDef(def) {
    this.#curType = def;
    return;
  }

  exitTypeDef(def) {
    this.#curType = null;
    return;
  }

  enterMethodDef(def) {
    this.#curMethod = def;
    return;
  }

  exitMethodDef(def) {
    this.#curMethod = null;
    return;
  }

}

class CompilerInput extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#inputLoc = Loc.make("CompilerInput");
    this.#isScript = false;
    this.#forceJs = false;
    this.#jsReflectClosures = false;
    this.#depends = sys.List.make(sys.Depend.type$);
    this.#ns = ReflectNamespace.make();
    this.#meta = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("sys::Str"));
    this.#index = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("sys::Obj"));
    this.#output = null;
    this.#log = CompilerLog.make();
    this.#outDir = sys.Env.cur().workDir().plus(sys.Uri.fromStr("lib/fan/"));
    this.#includeDoc = false;
    this.#includeSrc = false;
    this.#isTest = false;
    this.#fcodeDump = false;
    this.#mode = null;
    this.#docTests = false;
    return;
  }

  typeof() { return CompilerInput.type$; }

  #inputLoc = null;

  inputLoc(it) {
    if (it === undefined) {
      return this.#inputLoc;
    }
    else {
      this.#inputLoc = it;
      return;
    }
  }

  #podName = null;

  podName(it) {
    if (it === undefined) {
      return this.#podName;
    }
    else {
      this.#podName = it;
      return;
    }
  }

  #isScript = false;

  isScript(it) {
    if (it === undefined) {
      return this.#isScript;
    }
    else {
      this.#isScript = it;
      return;
    }
  }

  #forceJs = false;

  forceJs(it) {
    if (it === undefined) {
      return this.#forceJs;
    }
    else {
      this.#forceJs = it;
      return;
    }
  }

  #jsReflectClosures = false;

  jsReflectClosures(it) {
    if (it === undefined) {
      return this.#jsReflectClosures;
    }
    else {
      this.#jsReflectClosures = it;
      return;
    }
  }

  #version = null;

  version(it) {
    if (it === undefined) {
      return this.#version;
    }
    else {
      this.#version = it;
      return;
    }
  }

  #summary = null;

  summary(it) {
    if (it === undefined) {
      return this.#summary;
    }
    else {
      this.#summary = it;
      return;
    }
  }

  #depends = null;

  depends(it) {
    if (it === undefined) {
      return this.#depends;
    }
    else {
      this.#depends = it;
      return;
    }
  }

  #ns = null;

  ns(it) {
    if (it === undefined) {
      return this.#ns;
    }
    else {
      this.#ns = it;
      return;
    }
  }

  #meta = null;

  meta(it) {
    if (it === undefined) {
      return this.#meta;
    }
    else {
      this.#meta = it;
      return;
    }
  }

  #index = null;

  index(it) {
    if (it === undefined) {
      return this.#index;
    }
    else {
      this.#index = it;
      return;
    }
  }

  #output = null;

  output(it) {
    if (it === undefined) {
      return this.#output;
    }
    else {
      this.#output = it;
      return;
    }
  }

  #log = null;

  log(it) {
    if (it === undefined) {
      return this.#log;
    }
    else {
      this.#log = it;
      return;
    }
  }

  #outDir = null;

  outDir(it) {
    if (it === undefined) {
      return this.#outDir;
    }
    else {
      this.#outDir = it;
      return;
    }
  }

  #includeDoc = false;

  includeDoc(it) {
    if (it === undefined) {
      return this.#includeDoc;
    }
    else {
      this.#includeDoc = it;
      return;
    }
  }

  #includeSrc = false;

  includeSrc(it) {
    if (it === undefined) {
      return this.#includeSrc;
    }
    else {
      this.#includeSrc = it;
      return;
    }
  }

  #isTest = false;

  isTest(it) {
    if (it === undefined) {
      return this.#isTest;
    }
    else {
      this.#isTest = it;
      return;
    }
  }

  #fcodeDump = false;

  fcodeDump(it) {
    if (it === undefined) {
      return this.#fcodeDump;
    }
    else {
      this.#fcodeDump = it;
      return;
    }
  }

  #mode = null;

  mode(it) {
    if (it === undefined) {
      return this.#mode;
    }
    else {
      this.#mode = it;
      return;
    }
  }

  #docTests = false;

  docTests(it) {
    if (it === undefined) {
      return this.#docTests;
    }
    else {
      this.#docTests = it;
      return;
    }
  }

  #wrapperPerParameterizedCollectionType = false;

  wrapperPerParameterizedCollectionType(it) {
    if (it === undefined) {
      return this.#wrapperPerParameterizedCollectionType;
    }
    else {
      this.#wrapperPerParameterizedCollectionType = it;
      return;
    }
  }

  #coerceParameterizedCollectionTypes = false;

  coerceParameterizedCollectionTypes(it) {
    if (it === undefined) {
      return this.#coerceParameterizedCollectionTypes;
    }
    else {
      this.#coerceParameterizedCollectionTypes = it;
      return;
    }
  }

  #baseDir = null;

  baseDir(it) {
    if (it === undefined) {
      return this.#baseDir;
    }
    else {
      this.#baseDir = it;
      return;
    }
  }

  #srcFiles = null;

  srcFiles(it) {
    if (it === undefined) {
      return this.#srcFiles;
    }
    else {
      this.#srcFiles = it;
      return;
    }
  }

  #resFiles = null;

  resFiles(it) {
    if (it === undefined) {
      return this.#resFiles;
    }
    else {
      this.#resFiles = it;
      return;
    }
  }

  #jsFiles = null;

  jsFiles(it) {
    if (it === undefined) {
      return this.#jsFiles;
    }
    else {
      this.#jsFiles = it;
      return;
    }
  }

  #jsPropsFiles = null;

  jsPropsFiles(it) {
    if (it === undefined) {
      return this.#jsPropsFiles;
    }
    else {
      this.#jsPropsFiles = it;
      return;
    }
  }

  #srcStr = null;

  srcStr(it) {
    if (it === undefined) {
      return this.#srcStr;
    }
    else {
      this.#srcStr = it;
      return;
    }
  }

  #srcStrLoc = null;

  srcStrLoc(it) {
    if (it === undefined) {
      return this.#srcStrLoc;
    }
    else {
      this.#srcStrLoc = it;
      return;
    }
  }

  validate() {
    this.validateReqField("podName");
    this.validateReqField("version");
    this.validateReqField("summary");
    this.validateReqField("output");
    this.validateReqField("outDir");
    this.validateReqField("mode");
    let $_u6 = this.#mode;
    if (sys.ObjUtil.equals($_u6, CompilerInputMode.file())) {
      this.validateReqField("baseDir");
    }
    else if (sys.ObjUtil.equals($_u6, CompilerInputMode.str())) {
      this.validateReqField("srcStr");
      this.validateReqField("srcStrLoc");
    }
    ;
    return;
  }

  validateReqField(field) {
    let val = sys.ObjUtil.typeof(this).field(field).get(this);
    if (val == null) {
      throw sys.ArgErr.make(sys.Str.plus(sys.Str.plus("CompilerInput.", field), " not set"), null);
    }
    ;
    return;
  }

  static make() {
    const $self = new CompilerInput();
    CompilerInput.make$($self);
    return $self;
  }

  static make$($self) {
    ;
    return;
  }

}

class CompilerInputMode extends sys.Enum {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return CompilerInputMode.type$; }

  static file() { return CompilerInputMode.vals().get(0); }

  static str() { return CompilerInputMode.vals().get(1); }

  static #vals = undefined;

  static make($ordinal,$name) {
    const $self = new CompilerInputMode();
    CompilerInputMode.make$($self,$ordinal,$name);
    return $self;
  }

  static make$($self,$ordinal,$name) {
    sys.Enum.make$($self, $ordinal, $name);
    return;
  }

  static fromStr(name$, checked=true) {
    return sys.Enum.doFromStr(CompilerInputMode.type$, CompilerInputMode.vals(), name$, checked);
  }

  static vals() {
    if (CompilerInputMode.#vals == null) {
      CompilerInputMode.#vals = sys.List.make(CompilerInputMode.type$, [
        CompilerInputMode.make(0, "file", ),
        CompilerInputMode.make(1, "str", ),
      ]).toImmutable();
    }
    return CompilerInputMode.#vals;
  }

  static static$init() {
    const $_u7 = CompilerInputMode.vals();
    if (true) {
    }
    ;
    return;
  }

}

class SuppressedErr extends sys.Err {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return SuppressedErr.type$; }

  static make() {
    const $self = new SuppressedErr();
    SuppressedErr.make$($self);
    return $self;
  }

  static make$($self) {
    sys.Err.make$($self, "", null);
    return;
  }

}

class CompilerOutput extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return CompilerOutput.type$; }

  #mode = null;

  mode(it) {
    if (it === undefined) {
      return this.#mode;
    }
    else {
      this.#mode = it;
      return;
    }
  }

  #transientPod = null;

  transientPod(it) {
    if (it === undefined) {
      return this.#transientPod;
    }
    else {
      this.#transientPod = it;
      return;
    }
  }

  #podFile = null;

  podFile(it) {
    if (it === undefined) {
      return this.#podFile;
    }
    else {
      this.#podFile = it;
      return;
    }
  }

  #js = null;

  js(it) {
    if (it === undefined) {
      return this.#js;
    }
    else {
      this.#js = it;
      return;
    }
  }

  #cjs = null;

  cjs(it) {
    if (it === undefined) {
      return this.#cjs;
    }
    else {
      this.#cjs = it;
      return;
    }
  }

  #esm = null;

  esm(it) {
    if (it === undefined) {
      return this.#esm;
    }
    else {
      this.#esm = it;
      return;
    }
  }

  static make() {
    const $self = new CompilerOutput();
    CompilerOutput.make$($self);
    return $self;
  }

  static make$($self) {
    return;
  }

}

class CompilerOutputMode extends sys.Enum {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return CompilerOutputMode.type$; }

  static transientPod() { return CompilerOutputMode.vals().get(0); }

  static podFile() { return CompilerOutputMode.vals().get(1); }

  static js() { return CompilerOutputMode.vals().get(2); }

  static #vals = undefined;

  static make($ordinal,$name) {
    const $self = new CompilerOutputMode();
    CompilerOutputMode.make$($self,$ordinal,$name);
    return $self;
  }

  static make$($self,$ordinal,$name) {
    sys.Enum.make$($self, $ordinal, $name);
    return;
  }

  static fromStr(name$, checked=true) {
    return sys.Enum.doFromStr(CompilerOutputMode.type$, CompilerOutputMode.vals(), name$, checked);
  }

  static vals() {
    if (CompilerOutputMode.#vals == null) {
      CompilerOutputMode.#vals = sys.List.make(CompilerOutputMode.type$, [
        CompilerOutputMode.make(0, "transientPod", ),
        CompilerOutputMode.make(1, "podFile", ),
        CompilerOutputMode.make(2, "js", ),
      ]).toImmutable();
    }
    return CompilerOutputMode.#vals;
  }

  static static$init() {
    const $_u8 = CompilerOutputMode.vals();
    if (true) {
    }
    ;
    return;
  }

}

class Main extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Main.type$; }

  static compileScript(podName,file,options) {
    if (options === undefined) options = null;
    let input = CompilerInput.make();
    input.podName(podName);
    input.summary("script");
    input.version(sys.Version.fromStr("0"));
    input.log().level(sys.LogLevel.warn());
    input.includeDoc(true);
    input.isScript(true);
    input.srcStr(file.readAllStr());
    input.srcStrLoc(Loc.makeFile(file));
    input.mode(CompilerInputMode.str());
    input.output(CompilerOutputMode.transientPod());
    if (options != null) {
      let log = options.get("log");
      if (log != null) {
        input.log(sys.ObjUtil.coerce(log, CompilerLog.type$));
      }
      ;
      let logOut = options.get("logOut");
      if (logOut != null) {
        input.log(CompilerLog.make(sys.ObjUtil.coerce(logOut, sys.OutStream.type$)));
      }
      ;
      let logLevel = options.get("logLevel");
      if (logLevel != null) {
        input.log().level(sys.ObjUtil.coerce(logLevel, sys.LogLevel.type$));
      }
      ;
      let fcodeDump = options.get("fcodeDump");
      if (sys.ObjUtil.equals(fcodeDump, true)) {
        input.fcodeDump(true);
      }
      ;
    }
    ;
    return sys.ObjUtil.coerce(Compiler.make(input).compile().transientPod(), sys.Pod.type$);
  }

  static compileScriptToJs(podName,file,options) {
    if (options === undefined) options = null;
    let input = CompilerInput.make();
    input.podName(podName);
    input.summary("script");
    input.version(sys.Version.fromStr("0"));
    input.log().level(sys.LogLevel.warn());
    input.isScript(true);
    input.srcStr(file.readAllStr());
    input.srcStrLoc(Loc.makeFile(file));
    input.mode(CompilerInputMode.str());
    input.output(CompilerOutputMode.js());
    if (options != null) {
      let log = options.get("log");
      if (log != null) {
        input.log(sys.ObjUtil.coerce(log, CompilerLog.type$));
      }
      ;
      let logOut = options.get("logOut");
      if (logOut != null) {
        input.log(CompilerLog.make(sys.ObjUtil.coerce(logOut, sys.OutStream.type$)));
      }
      ;
      let logLevel = options.get("logLevel");
      if (logLevel != null) {
        input.log().level(sys.ObjUtil.coerce(logLevel, sys.LogLevel.type$));
      }
      ;
      let fcodeDump = options.get("fcodeDump");
      if (sys.ObjUtil.equals(fcodeDump, true)) {
        input.fcodeDump(true);
      }
      ;
    }
    ;
    let output = Compiler.make(input).compile();
    let $_u9 = options.get("output");
    if (sys.ObjUtil.equals($_u9, "legacy")) {
      return sys.ObjUtil.coerce(output.js(), sys.Str.type$);
    }
    else if (sys.ObjUtil.equals($_u9, "esm")) {
      return sys.ObjUtil.coerce(output.esm(), sys.Str.type$);
    }
    else {
      return sys.ObjUtil.coerce(output.cjs(), sys.Str.type$);
    }
    ;
  }

  static make() {
    const $self = new Main();
    Main.make$($self);
    return $self;
  }

  static make$($self) {
    return;
  }

}

class AttrAsm extends CompilerSupport {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return AttrAsm.type$; }

  #fpod = null;

  fpod(it) {
    if (it === undefined) {
      return this.#fpod;
    }
    else {
      this.#fpod = it;
      return;
    }
  }

  #attrs = null;

  attrs(it) {
    if (it === undefined) {
      return this.#attrs;
    }
    else {
      this.#attrs = it;
      return;
    }
  }

  static make(compiler,fpod) {
    const $self = new AttrAsm();
    AttrAsm.make$($self,compiler,fpod);
    return $self;
  }

  static make$($self,compiler,fpod) {
    CompilerSupport.make$($self, compiler);
    $self.#fpod = fpod;
    $self.#attrs = sys.List.make(FAttr.type$);
    return;
  }

  sourceFile(source) {
    if (source == null) {
      return;
    }
    ;
    this.utf(FConst.SourceFileAttr(), sys.ObjUtil.coerce(source, sys.Str.type$));
    return;
  }

  lineNumber(line) {
    if ((line == null || sys.ObjUtil.equals(line, 0))) {
      return;
    }
    ;
    this.u2(FConst.LineNumberAttr(), sys.ObjUtil.coerce(line, sys.Int.type$));
    return;
  }

  facets(facets) {
    const this$ = this;
    if ((facets == null || facets.isEmpty())) {
      return;
    }
    ;
    let buf = sys.Buf.make();
    buf.writeI2(facets.size());
    facets.each((f) => {
      buf.writeI2(this$.#fpod.addTypeRef(f.type()));
      try {
        buf.writeUtf(f.serialize());
      }
      catch ($_u10) {
        $_u10 = sys.Err.make($_u10);
        if ($_u10 instanceof CompilerErr) {
          let e = $_u10;
          ;
          this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Facet value is not serializable: '", f.type()), "' ("), e.msg()), ")"), f.loc());
        }
        else {
          throw $_u10;
        }
      }
      ;
      this$.add(FConst.FacetsAttr(), sys.ObjUtil.coerce(buf, sys.Buf.type$));
      return;
    });
    return;
  }

  enumOrdinal(ordinal) {
    this.u2(FConst.EnumOrdinalAttr(), ordinal);
    return;
  }

  utf(name,data) {
    let buf = sys.Buf.make();
    buf.writeUtf(data);
    this.add(name, sys.ObjUtil.coerce(buf, sys.Buf.type$));
    return;
  }

  u2(name,data) {
    let buf = sys.Buf.make();
    buf.writeI2(data);
    this.add(name, sys.ObjUtil.coerce(buf, sys.Buf.type$));
    return;
  }

  add(name,data) {
    let a = FAttr.make();
    a.name(this.#fpod.addName(name));
    a.data(data);
    this.#attrs.add(a);
    return;
  }

}

class FConst {
  constructor() {
    const this$ = this;
  }

  typeof() { return FConst.type$; }

  static #FCodeVersion = undefined;

  static FCodeVersion() {
    if (FConst.#FCodeVersion === undefined) {
      FConst.static$init();
      if (FConst.#FCodeVersion === undefined) FConst.#FCodeVersion = null;
    }
    return FConst.#FCodeVersion;
  }

  static #Abstract = undefined;

  static Abstract() {
    if (FConst.#Abstract === undefined) {
      FConst.static$init();
      if (FConst.#Abstract === undefined) FConst.#Abstract = 0;
    }
    return FConst.#Abstract;
  }

  static #Const = undefined;

  static Const() {
    if (FConst.#Const === undefined) {
      FConst.static$init();
      if (FConst.#Const === undefined) FConst.#Const = 0;
    }
    return FConst.#Const;
  }

  static #Ctor = undefined;

  static Ctor() {
    if (FConst.#Ctor === undefined) {
      FConst.static$init();
      if (FConst.#Ctor === undefined) FConst.#Ctor = 0;
    }
    return FConst.#Ctor;
  }

  static #Enum = undefined;

  static Enum() {
    if (FConst.#Enum === undefined) {
      FConst.static$init();
      if (FConst.#Enum === undefined) FConst.#Enum = 0;
    }
    return FConst.#Enum;
  }

  static #Facet = undefined;

  static Facet() {
    if (FConst.#Facet === undefined) {
      FConst.static$init();
      if (FConst.#Facet === undefined) FConst.#Facet = 0;
    }
    return FConst.#Facet;
  }

  static #Final = undefined;

  static Final() {
    if (FConst.#Final === undefined) {
      FConst.static$init();
      if (FConst.#Final === undefined) FConst.#Final = 0;
    }
    return FConst.#Final;
  }

  static #Getter = undefined;

  static Getter() {
    if (FConst.#Getter === undefined) {
      FConst.static$init();
      if (FConst.#Getter === undefined) FConst.#Getter = 0;
    }
    return FConst.#Getter;
  }

  static #Internal = undefined;

  static Internal() {
    if (FConst.#Internal === undefined) {
      FConst.static$init();
      if (FConst.#Internal === undefined) FConst.#Internal = 0;
    }
    return FConst.#Internal;
  }

  static #Mixin = undefined;

  static Mixin() {
    if (FConst.#Mixin === undefined) {
      FConst.static$init();
      if (FConst.#Mixin === undefined) FConst.#Mixin = 0;
    }
    return FConst.#Mixin;
  }

  static #Native = undefined;

  static Native() {
    if (FConst.#Native === undefined) {
      FConst.static$init();
      if (FConst.#Native === undefined) FConst.#Native = 0;
    }
    return FConst.#Native;
  }

  static #Override = undefined;

  static Override() {
    if (FConst.#Override === undefined) {
      FConst.static$init();
      if (FConst.#Override === undefined) FConst.#Override = 0;
    }
    return FConst.#Override;
  }

  static #Private = undefined;

  static Private() {
    if (FConst.#Private === undefined) {
      FConst.static$init();
      if (FConst.#Private === undefined) FConst.#Private = 0;
    }
    return FConst.#Private;
  }

  static #Protected = undefined;

  static Protected() {
    if (FConst.#Protected === undefined) {
      FConst.static$init();
      if (FConst.#Protected === undefined) FConst.#Protected = 0;
    }
    return FConst.#Protected;
  }

  static #Public = undefined;

  static Public() {
    if (FConst.#Public === undefined) {
      FConst.static$init();
      if (FConst.#Public === undefined) FConst.#Public = 0;
    }
    return FConst.#Public;
  }

  static #Setter = undefined;

  static Setter() {
    if (FConst.#Setter === undefined) {
      FConst.static$init();
      if (FConst.#Setter === undefined) FConst.#Setter = 0;
    }
    return FConst.#Setter;
  }

  static #Static = undefined;

  static Static() {
    if (FConst.#Static === undefined) {
      FConst.static$init();
      if (FConst.#Static === undefined) FConst.#Static = 0;
    }
    return FConst.#Static;
  }

  static #Storage = undefined;

  static Storage() {
    if (FConst.#Storage === undefined) {
      FConst.static$init();
      if (FConst.#Storage === undefined) FConst.#Storage = 0;
    }
    return FConst.#Storage;
  }

  static #Synthetic = undefined;

  static Synthetic() {
    if (FConst.#Synthetic === undefined) {
      FConst.static$init();
      if (FConst.#Synthetic === undefined) FConst.#Synthetic = 0;
    }
    return FConst.#Synthetic;
  }

  static #Virtual = undefined;

  static Virtual() {
    if (FConst.#Virtual === undefined) {
      FConst.static$init();
      if (FConst.#Virtual === undefined) FConst.#Virtual = 0;
    }
    return FConst.#Virtual;
  }

  static #Once = undefined;

  static Once() {
    if (FConst.#Once === undefined) {
      FConst.static$init();
      if (FConst.#Once === undefined) FConst.#Once = 0;
    }
    return FConst.#Once;
  }

  static #FlagsMask = undefined;

  static FlagsMask() {
    if (FConst.#FlagsMask === undefined) {
      FConst.static$init();
      if (FConst.#FlagsMask === undefined) FConst.#FlagsMask = 0;
    }
    return FConst.#FlagsMask;
  }

  static #Param = undefined;

  static Param() {
    if (FConst.#Param === undefined) {
      FConst.static$init();
      if (FConst.#Param === undefined) FConst.#Param = 0;
    }
    return FConst.#Param;
  }

  static #ErrTableAttr = undefined;

  static ErrTableAttr() {
    if (FConst.#ErrTableAttr === undefined) {
      FConst.static$init();
      if (FConst.#ErrTableAttr === undefined) FConst.#ErrTableAttr = null;
    }
    return FConst.#ErrTableAttr;
  }

  static #FacetsAttr = undefined;

  static FacetsAttr() {
    if (FConst.#FacetsAttr === undefined) {
      FConst.static$init();
      if (FConst.#FacetsAttr === undefined) FConst.#FacetsAttr = null;
    }
    return FConst.#FacetsAttr;
  }

  static #LineNumberAttr = undefined;

  static LineNumberAttr() {
    if (FConst.#LineNumberAttr === undefined) {
      FConst.static$init();
      if (FConst.#LineNumberAttr === undefined) FConst.#LineNumberAttr = null;
    }
    return FConst.#LineNumberAttr;
  }

  static #LineNumbersAttr = undefined;

  static LineNumbersAttr() {
    if (FConst.#LineNumbersAttr === undefined) {
      FConst.static$init();
      if (FConst.#LineNumbersAttr === undefined) FConst.#LineNumbersAttr = null;
    }
    return FConst.#LineNumbersAttr;
  }

  static #SourceFileAttr = undefined;

  static SourceFileAttr() {
    if (FConst.#SourceFileAttr === undefined) {
      FConst.static$init();
      if (FConst.#SourceFileAttr === undefined) FConst.#SourceFileAttr = null;
    }
    return FConst.#SourceFileAttr;
  }

  static #ParamDefaultAttr = undefined;

  static ParamDefaultAttr() {
    if (FConst.#ParamDefaultAttr === undefined) {
      FConst.static$init();
      if (FConst.#ParamDefaultAttr === undefined) FConst.#ParamDefaultAttr = null;
    }
    return FConst.#ParamDefaultAttr;
  }

  static #EnumOrdinalAttr = undefined;

  static EnumOrdinalAttr() {
    if (FConst.#EnumOrdinalAttr === undefined) {
      FConst.static$init();
      if (FConst.#EnumOrdinalAttr === undefined) FConst.#EnumOrdinalAttr = null;
    }
    return FConst.#EnumOrdinalAttr;
  }

  static static$init() {
    FConst.#FCodeVersion = "1.0.51";
    FConst.#Abstract = 1;
    FConst.#Const = 2;
    FConst.#Ctor = 4;
    FConst.#Enum = 8;
    FConst.#Facet = 16;
    FConst.#Final = 32;
    FConst.#Getter = 64;
    FConst.#Internal = 128;
    FConst.#Mixin = 256;
    FConst.#Native = 512;
    FConst.#Override = 1024;
    FConst.#Private = 2048;
    FConst.#Protected = 4096;
    FConst.#Public = 8192;
    FConst.#Setter = 16384;
    FConst.#Static = 32768;
    FConst.#Storage = 65536;
    FConst.#Synthetic = 131072;
    FConst.#Virtual = 262144;
    FConst.#Once = 524288;
    FConst.#FlagsMask = 1048575;
    FConst.#Param = 1;
    FConst.#ErrTableAttr = "ErrTable";
    FConst.#FacetsAttr = "Facets";
    FConst.#LineNumberAttr = "LineNumber";
    FConst.#LineNumbersAttr = "LineNumbers";
    FConst.#SourceFileAttr = "SourceFile";
    FConst.#ParamDefaultAttr = "ParamDefault";
    FConst.#EnumOrdinalAttr = "EnumOrdinal";
    return;
  }

}

class Assembler extends CompilerSupport {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Assembler.type$; }

  static FCodeVersion() { return FConst.FCodeVersion(); }

  static Enum() { return FConst.Enum(); }

  static LineNumberAttr() { return FConst.LineNumberAttr(); }

  static Synthetic() { return FConst.Synthetic(); }

  static LineNumbersAttr() { return FConst.LineNumbersAttr(); }

  static Once() { return FConst.Once(); }

  static Getter() { return FConst.Getter(); }

  static Internal() { return FConst.Internal(); }

  static SourceFileAttr() { return FConst.SourceFileAttr(); }

  static ErrTableAttr() { return FConst.ErrTableAttr(); }

  static Mixin() { return FConst.Mixin(); }

  static Native() { return FConst.Native(); }

  static Ctor() { return FConst.Ctor(); }

  static Private() { return FConst.Private(); }

  static Final() { return FConst.Final(); }

  static Override() { return FConst.Override(); }

  static FlagsMask() { return FConst.FlagsMask(); }

  static Facet() { return FConst.Facet(); }

  static FacetsAttr() { return FConst.FacetsAttr(); }

  static Protected() { return FConst.Protected(); }

  static Abstract() { return FConst.Abstract(); }

  static Const() { return FConst.Const(); }

  static Virtual() { return FConst.Virtual(); }

  static Public() { return FConst.Public(); }

  static Storage() { return FConst.Storage(); }

  static Setter() { return FConst.Setter(); }

  static Param() { return FConst.Param(); }

  static EnumOrdinalAttr() { return FConst.EnumOrdinalAttr(); }

  static Static() { return FConst.Static(); }

  static ParamDefaultAttr() { return FConst.ParamDefaultAttr(); }

  #fpod = null;

  // private field reflection only
  __fpod(it) { if (it === undefined) return this.#fpod; else this.#fpod = it; }

  #omitCode = false;

  // private field reflection only
  __omitCode(it) { if (it === undefined) return this.#omitCode; else this.#omitCode = it; }

  static make(compiler) {
    const $self = new Assembler();
    Assembler.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerSupport.make$($self, compiler);
    return;
  }

  assemblePod() {
    const this$ = this;
    this.#fpod = FPod.make(this.ns(), this.pod().name(), null);
    this.#fpod.name(sys.ObjUtil.coerce(this.compiler().input().podName(), sys.Str.type$));
    this.#fpod.version(sys.ObjUtil.coerce(this.compiler().input().version(), sys.Version.type$));
    this.#fpod.depends(this.compiler().depends());
    this.#fpod.meta(this.assembleMeta());
    this.#fpod.index(this.pod().index());
    this.#fpod.ftypes(sys.List.make(FType.type$));
    this.types().each((t) => {
      this$.#fpod.ftypes().add(this$.assembleType(t));
      return;
    });
    return sys.ObjUtil.coerce(this.#fpod, FPod.type$);
  }

  assemblePodNoCode() {
    this.#omitCode = true;
    return this.assemblePod();
  }

  assembleMeta() {
    let meta = this.pod().meta();
    meta.set("pod.fcode", sys.Bool.toStr(!this.types().isEmpty()));
    meta.set("pod.js", sys.Bool.toStr((this.compiler().cjs() != null || this.compiler().isSys())));
    return meta;
  }

  assembleType(def) {
    const this$ = this;
    let t = FType.make(sys.ObjUtil.coerce(this.#fpod, FPod.type$));
    t.hollow(false);
    t.flags(def.flags());
    t.self(this.typeRef(def));
    t.fbase(((this$) => { if (def.base() == null) return -1; return this$.typeRef(sys.ObjUtil.coerce(def.base(), CType.type$)); })(this));
    t.fmixins(sys.ObjUtil.coerce(def.mixins().map((m) => {
      return this$.typeRef(m);
    }, sys.Int.type$), sys.Type.find("sys::Int[]?")));
    t.ffields(sys.ObjUtil.coerce(def.fieldDefs().map((f) => {
      return this$.assembleField(t, f);
    }, FField.type$), sys.Type.find("compiler::FField[]?")));
    t.fmethods(sys.ObjUtil.coerce(def.methodDefs().map((m) => {
      return this$.assembleMethod(t, m);
    }, FMethod.type$), sys.Type.find("compiler::FMethod[]?")));
    let attrs = AttrAsm.make(this.compiler(), sys.ObjUtil.coerce(this.#fpod, FPod.type$));
    if (sys.ObjUtil.equals(this.compiler().input().mode(), CompilerInputMode.str())) {
      attrs.sourceFile(def.loc().fileUri());
    }
    else {
      attrs.sourceFile(def.loc().filename());
    }
    ;
    attrs.lineNumber(def.loc().line());
    attrs.facets(def.facets());
    t.fattrs(attrs.attrs());
    return t;
  }

  assembleField(fparent,def) {
    let f = FField.make(fparent);
    f.nameIndex(this.name(def.name()));
    f.flags(def.flags());
    f.typeRef(this.typeRef(def.type()));
    let attrs = AttrAsm.make(this.compiler(), sys.ObjUtil.coerce(this.#fpod, FPod.type$));
    attrs.lineNumber(def.loc().line());
    attrs.facets(def.facets());
    if (def.enumDef() != null) {
      attrs.enumOrdinal(def.enumDef().ordinal());
    }
    ;
    f.fattrs(attrs.attrs());
    return f;
  }

  assembleMethod(fparent,def) {
    const this$ = this;
    let attrs = AttrAsm.make(this.compiler(), sys.ObjUtil.coerce(this.#fpod, FPod.type$));
    let m = FMethod.make(fparent);
    m.nameIndex(this.name(def.name()));
    m.flags(def.flags());
    m.ret(this.typeRef(def.returns()));
    m.inheritedRet(this.typeRef(def.inheritedReturns()));
    m.paramCount(def.params().size());
    m.localCount(sys.Int.minus(def.vars().size(), def.params().size()));
    m.vars(sys.ObjUtil.coerce(def.vars().map((v) => {
      let f = FMethodVar.make(m);
      f.nameIndex(this$.name(v.name()));
      f.typeRef(this$.typeRef(sys.ObjUtil.coerce(((this$) => { let $_u12 = ((this$) => { let $_u13=v.paramDef(); return ($_u13==null) ? null : $_u13.type(); })(this$); if ($_u12 != null) return $_u12; return v.ctype(); })(this$), CType.type$)));
      f.flags(v.flags());
      if (v.paramDef() != null) {
        f.defNameIndex(this$.name(FConst.ParamDefaultAttr()));
        f.def(this$.assembleExpr(v.paramDef().def()));
      }
      ;
      return f;
    }, FMethodVar.type$), sys.Type.find("compiler::FMethodVar[]?")));
    m.code(this.assembleCode(def, attrs));
    attrs.lineNumber(def.loc().line());
    attrs.facets(def.facets());
    m.fattrs(attrs.attrs());
    return m;
  }

  typeRef(type) {
    return this.#fpod.addTypeRef(type);
  }

  name(val) {
    return this.#fpod.addName(val);
  }

  assembleCode(def,attrs) {
    if (this.#omitCode) {
      return null;
    }
    ;
    let block = def.code();
    if (block == null) {
      return null;
    }
    ;
    let asm = CodeAsm.make(this.compiler(), def.loc(), sys.ObjUtil.coerce(this.#fpod, FPod.type$), def);
    if (def.ctorChain() != null) {
      asm.expr(sys.ObjUtil.coerce(def.ctorChain(), Expr.type$));
    }
    ;
    asm.block(sys.ObjUtil.coerce(block, Block.type$));
    if (sys.ObjUtil.compareGT(asm.errCount(), 0)) {
      attrs.add(FConst.ErrTableAttr(), asm.finishErrTable());
    }
    ;
    if (sys.ObjUtil.compareGT(asm.lineCount(), 0)) {
      attrs.add(FConst.LineNumbersAttr(), asm.finishLines());
    }
    ;
    return asm.finishCode();
  }

  assembleExpr(expr) {
    if (expr == null) {
      return null;
    }
    ;
    let asm = CodeAsm.make(this.compiler(), expr.loc(), sys.ObjUtil.coerce(this.#fpod, FPod.type$), null);
    asm.expr(sys.ObjUtil.coerce(expr, Expr.type$));
    return asm.finishCode();
  }

}

class CodeAsm extends CompilerSupport {
  constructor() {
    super();
    const this$ = this;
    this.#lastLine = -1;
    this.#lastOffset = -1;
    return;
  }

  typeof() { return CodeAsm.type$; }

  #loc = null;

  loc(it) {
    if (it === undefined) {
      return this.#loc;
    }
    else {
      this.#loc = it;
      return;
    }
  }

  #fpod = null;

  fpod(it) {
    if (it === undefined) {
      return this.#fpod;
    }
    else {
      this.#fpod = it;
      return;
    }
  }

  #curMethod = null;

  curMethod(it) {
    if (it === undefined) {
      return this.#curMethod;
    }
    else {
      this.#curMethod = it;
      return;
    }
  }

  #code = null;

  code(it) {
    if (it === undefined) {
      return this.#code;
    }
    else {
      this.#code = it;
      return;
    }
  }

  #errTable = null;

  errTable(it) {
    if (it === undefined) {
      return this.#errTable;
    }
    else {
      this.#errTable = it;
      return;
    }
  }

  #errCount = 0;

  errCount(it) {
    if (it === undefined) {
      return this.#errCount;
    }
    else {
      this.#errCount = it;
      return;
    }
  }

  #lines = null;

  lines(it) {
    if (it === undefined) {
      return this.#lines;
    }
    else {
      this.#lines = it;
      return;
    }
  }

  #lineCount = 0;

  lineCount(it) {
    if (it === undefined) {
      return this.#lineCount;
    }
    else {
      this.#lineCount = it;
      return;
    }
  }

  #lastLine = 0;

  lastLine(it) {
    if (it === undefined) {
      return this.#lastLine;
    }
    else {
      this.#lastLine = it;
      return;
    }
  }

  #lastOffset = 0;

  lastOffset(it) {
    if (it === undefined) {
      return this.#lastOffset;
    }
    else {
      this.#lastOffset = it;
      return;
    }
  }

  #loopStack = null;

  loopStack(it) {
    if (it === undefined) {
      return this.#loopStack;
    }
    else {
      this.#loopStack = it;
      return;
    }
  }

  #protectedRegions = null;

  protectedRegions(it) {
    if (it === undefined) {
      return this.#protectedRegions;
    }
    else {
      this.#protectedRegions = it;
      return;
    }
  }

  #leavesToReturn = null;

  leavesToReturn(it) {
    if (it === undefined) {
      return this.#leavesToReturn;
    }
    else {
      this.#leavesToReturn = it;
      return;
    }
  }

  #returnLocal = null;

  returnLocal(it) {
    if (it === undefined) {
      return this.#returnLocal;
    }
    else {
      this.#returnLocal = it;
      return;
    }
  }

  static make(compiler,loc,fpod,curMethod) {
    const $self = new CodeAsm();
    CodeAsm.make$($self,compiler,loc,fpod,curMethod);
    return $self;
  }

  static make$($self,compiler,loc,fpod,curMethod) {
    CompilerSupport.make$($self, compiler);
    ;
    $self.#loc = loc;
    $self.#fpod = fpod;
    $self.#curMethod = curMethod;
    $self.#code = sys.ObjUtil.coerce(sys.Buf.make(), sys.Buf.type$);
    $self.#errTable = sys.ObjUtil.coerce(sys.Buf.make(), sys.Buf.type$);
    $self.#errTable.writeI2(-1);
    $self.#errCount = 0;
    $self.#lines = sys.ObjUtil.coerce(sys.Buf.make(), sys.Buf.type$);
    $self.#lines.writeI2(-1);
    $self.#lineCount = 0;
    $self.#loopStack = sys.List.make(Loop.type$);
    return;
  }

  block(block) {
    const this$ = this;
    block.stmts().each((s) => {
      this$.stmt(s);
      return;
    });
    return;
  }

  stmt(stmt) {
    let $_u14 = stmt.id();
    if (sys.ObjUtil.equals($_u14, StmtId.nop())) {
      return;
    }
    else if (sys.ObjUtil.equals($_u14, StmtId.expr())) {
      this.expr(sys.ObjUtil.coerce(stmt, ExprStmt.type$).expr());
    }
    else if (sys.ObjUtil.equals($_u14, StmtId.localDef())) {
      this.localVarDefStmt(sys.ObjUtil.coerce(stmt, LocalDefStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u14, StmtId.ifStmt())) {
      this.ifStmt(sys.ObjUtil.coerce(stmt, IfStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u14, StmtId.returnStmt())) {
      this.returnStmt(sys.ObjUtil.coerce(stmt, ReturnStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u14, StmtId.throwStmt())) {
      this.throwStmt(sys.ObjUtil.coerce(stmt, ThrowStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u14, StmtId.forStmt())) {
      this.forStmt(sys.ObjUtil.coerce(stmt, ForStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u14, StmtId.whileStmt())) {
      this.whileStmt(sys.ObjUtil.coerce(stmt, WhileStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u14, StmtId.breakStmt())) {
      this.breakOrContinueStmt(stmt);
    }
    else if (sys.ObjUtil.equals($_u14, StmtId.continueStmt())) {
      this.breakOrContinueStmt(stmt);
    }
    else if (sys.ObjUtil.equals($_u14, StmtId.switchStmt())) {
      this.switchStmt(sys.ObjUtil.coerce(stmt, SwitchStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u14, StmtId.tryStmt())) {
      this.tryStmt(sys.ObjUtil.coerce(stmt, TryStmt.type$));
    }
    else {
      throw sys.Err.make(stmt.id().toStr());
    }
    ;
    return;
  }

  ifStmt(stmt) {
    const this$ = this;
    let endLabel = -1;
    let c = Cond.make();
    if (sys.ObjUtil.equals(stmt.condition().id(), ExprId.trueLiteral())) {
      this.block(stmt.trueBlock());
      return;
    }
    ;
    if (sys.ObjUtil.equals(stmt.condition().id(), ExprId.falseLiteral())) {
      if (stmt.falseBlock() != null) {
        this.block(sys.ObjUtil.coerce(stmt.falseBlock(), Block.type$));
      }
      ;
      return;
    }
    ;
    if (sys.ObjUtil.is(stmt.condition(), CondExpr.type$)) {
      this.cond(sys.ObjUtil.coerce(stmt.condition(), CondExpr.type$), c);
    }
    else {
      this.expr(stmt.condition());
      c.jumpFalses().add(sys.ObjUtil.coerce(this.jump(FOp.JumpFalse()), sys.Int.type$.toNullable()));
    }
    ;
    c.jumpTrues().each((pos) => {
      this$.backpatch(pos);
      return;
    });
    this.block(stmt.trueBlock());
    if ((!stmt.trueBlock().isExit() && stmt.falseBlock() != null)) {
      (endLabel = this.jump(FOp.Jump()));
    }
    ;
    c.jumpFalses().each((pos) => {
      this$.backpatch(pos);
      return;
    });
    if (stmt.falseBlock() != null) {
      this.block(sys.ObjUtil.coerce(stmt.falseBlock(), Block.type$));
    }
    ;
    if (sys.ObjUtil.compareNE(endLabel, -1)) {
      this.backpatch(endLabel);
    }
    ;
    return;
  }

  returnStmt(stmt) {
    const this$ = this;
    let exprOnStack = false;
    if (stmt.expr() != null) {
      this.expr(sys.ObjUtil.coerce(stmt.expr(), Expr.type$));
      (exprOnStack = !stmt.expr().ctype().isVoid());
      let inVoidMethod = (this.#curMethod != null && this.#curMethod.returns().isVoid());
      if ((inVoidMethod && exprOnStack)) {
        this.opType(FOp.Pop(), sys.ObjUtil.coerce(stmt.expr().ctype(), CType.type$));
        (exprOnStack = false);
      }
      ;
    }
    ;
    if (this.inProtectedRegion()) {
      if (exprOnStack) {
        this.#returnLocal = stmt.leaveVar();
        this.op(FOp.StoreVar(), sys.ObjUtil.coerce(this.#returnLocal.register(), sys.Int.type$.toNullable()));
      }
      ;
      this.#protectedRegions.eachr((region) => {
        if (region.hasFinally()) {
          region.jumpFinallys().add(sys.ObjUtil.coerce(this$.jump(FOp.JumpFinally()), sys.Int.type$.toNullable()));
        }
        ;
        return;
      });
      if (this.#leavesToReturn == null) {
        this.#leavesToReturn = sys.List.make(sys.Int.type$);
      }
      ;
      this.#leavesToReturn.add(sys.ObjUtil.coerce(this.jump(FOp.Leave()), sys.Int.type$.toNullable()));
      return;
    }
    ;
    this.op(FOp.Return());
    return;
  }

  throwStmt(stmt) {
    this.throwOp(stmt.exception());
    return;
  }

  throwOp(exception) {
    this.expr(exception);
    this.op(FOp.Throw());
    return;
  }

  localVarDefStmt(stmt) {
    if (stmt.isCatchVar()) {
      let var$ = stmt.var();
      this.op(FOp.CatchErrStart(), sys.ObjUtil.coerce(this.#fpod.addTypeRef(sys.ObjUtil.coerce(stmt.ctype(), CType.type$)), sys.Int.type$.toNullable()));
      if (var$.isWrapped()) {
        let wrapCtor = this.#fpod.addMethodRef(sys.ObjUtil.coerce(var$.wrapField().parent().method("make"), CMethod.type$), sys.ObjUtil.coerce(1, sys.Int.type$.toNullable()));
        this.op(FOp.CallNew(), sys.ObjUtil.coerce(wrapCtor, sys.Int.type$.toNullable()));
      }
      ;
      this.op(FOp.StoreVar(), sys.ObjUtil.coerce(var$.register(), sys.Int.type$.toNullable()));
    }
    else {
      if (stmt.init() != null) {
        this.expr(sys.ObjUtil.coerce(stmt.init(), Expr.type$));
      }
      ;
    }
    ;
    return;
  }

  whileStmt(stmt) {
    const this$ = this;
    let loop = Loop.make(stmt);
    this.#loopStack.push(loop);
    let continueLabel = this.mark();
    this.expr(stmt.condition());
    let breakJump = this.jump(FOp.JumpFalse());
    this.block(stmt.block());
    this.jump(FOp.Jump(), continueLabel);
    let breakLabel = this.mark();
    this.backpatch(breakJump);
    loop.continues().each((pos) => {
      this$.backpatch(pos, continueLabel);
      return;
    });
    loop.breaks().each((pos) => {
      this$.backpatch(pos, breakLabel);
      return;
    });
    this.#loopStack.pop();
    return;
  }

  forStmt(stmt) {
    const this$ = this;
    let breakJump = -1;
    let loop = Loop.make(stmt);
    this.#loopStack.push(loop);
    if (stmt.init() != null) {
      this.stmt(sys.ObjUtil.coerce(stmt.init(), Stmt.type$));
    }
    ;
    let condLabel = this.mark();
    if (stmt.condition() != null) {
      this.expr(sys.ObjUtil.coerce(stmt.condition(), Expr.type$));
      (breakJump = this.jump(FOp.JumpFalse()));
    }
    ;
    this.block(sys.ObjUtil.coerce(stmt.block(), Block.type$));
    let updateLabel = this.mark();
    if (stmt.update() != null) {
      this.expr(sys.ObjUtil.coerce(stmt.update(), Expr.type$));
    }
    ;
    this.jump(FOp.Jump(), condLabel);
    let endLabel = this.mark();
    if (sys.ObjUtil.compareNE(breakJump, -1)) {
      this.backpatch(breakJump, endLabel);
    }
    ;
    loop.continues().each((pos) => {
      this$.backpatch(pos, updateLabel);
      return;
    });
    loop.breaks().each((pos) => {
      this$.backpatch(pos, endLabel);
      return;
    });
    this.#loopStack.pop();
    return;
  }

  breakOrContinueStmt(stmt) {
    const this$ = this;
    let loop = this.#loopStack.peek();
    if (loop.stmt() !== sys.ObjUtil.trap(stmt,"loop", sys.List.make(sys.Obj.type$.toNullable(), []))) {
      throw this.err("Internal compiler error", stmt.loc());
    }
    ;
    let toBackpatch = null;
    if (!loop.protectedRegions().isEmpty()) {
      loop.protectedRegions().eachr((region) => {
        if (region.hasFinally()) {
          region.jumpFinallys().add(sys.ObjUtil.coerce(this$.jump(FOp.JumpFinally()), sys.Int.type$.toNullable()));
        }
        ;
        return;
      });
      (toBackpatch = sys.ObjUtil.coerce(this.jump(FOp.Leave()), sys.Int.type$.toNullable()));
    }
    else {
      (toBackpatch = sys.ObjUtil.coerce(this.jump(FOp.Jump()), sys.Int.type$.toNullable()));
    }
    ;
    if (stmt.id() === StmtId.breakStmt()) {
      loop.breaks().add(sys.ObjUtil.coerce(sys.ObjUtil.coerce(toBackpatch, sys.Int.type$), sys.Int.type$.toNullable()));
    }
    else {
      loop.continues().add(sys.ObjUtil.coerce(sys.ObjUtil.coerce(toBackpatch, sys.Int.type$), sys.Int.type$.toNullable()));
    }
    ;
    return;
  }

  switchStmt(stmt) {
    let minMax = this.computeTableRange(stmt);
    if (minMax != null) {
      this.tableSwitchStmt(stmt, minMax.get(0), minMax.get(1));
    }
    else {
      this.equalsSwitchStmt(stmt);
    }
    ;
    return;
  }

  computeTableRange(stmt) {
    const this$ = this;
    let ctype = stmt.condition().ctype();
    if ((!ctype.isInt() && !ctype.isEnum())) {
      return null;
    }
    ;
    let min = 2147483647;
    let max = -2147483648;
    let count = 0;
    try {
      stmt.cases().each((c) => {
        for (let i = 0; sys.ObjUtil.compareLT(i, c.cases().size()); i = sys.Int.increment(i)) {
          ((this$) => { let $_u15 = count;count = sys.Int.increment(count); return $_u15; })(this$);
          let expr = c.cases().get(i);
          let literal = expr.asTableSwitchCase();
          if (literal == null) {
            throw CompilerErr.make("return null", c.loc());
          }
          ;
          if (sys.ObjUtil.compareLT(literal, min)) {
            (min = sys.ObjUtil.coerce(literal, sys.Int.type$));
          }
          ;
          if (sys.ObjUtil.compareGT(literal, max)) {
            (max = sys.ObjUtil.coerce(literal, sys.Int.type$));
          }
          ;
        }
        ;
        return;
      });
    }
    catch ($_u16) {
      $_u16 = sys.Err.make($_u16);
      if ($_u16 instanceof CompilerErr) {
        let e = $_u16;
        ;
        return null;
      }
      else {
        throw $_u16;
      }
    }
    ;
    if (sys.ObjUtil.equals(count, 0)) {
      return null;
    }
    ;
    let delta = sys.Int.minus(max, min);
    if ((ctype.isEnum() || sys.ObjUtil.compareLT(delta, 32) || sys.ObjUtil.compareGT(sys.Int.mult(count, 32), delta))) {
      return sys.List.make(sys.Int.type$, [sys.ObjUtil.coerce(min, sys.Int.type$.toNullable()), sys.ObjUtil.coerce(max, sys.Int.type$.toNullable())]);
    }
    else {
      return null;
    }
    ;
  }

  tableSwitchStmt(stmt,min,max) {
    const this$ = this;
    stmt.isTableswitch(true);
    let conditionType = stmt.condition().ctype();
    let isEnum = conditionType.isEnum();
    this.expr(stmt.condition());
    if ((conditionType.isInt() && conditionType.isNullable())) {
      this.coerceOp(sys.ObjUtil.coerce(conditionType, CType.type$), sys.ObjUtil.coerce(this.ns().intType(), CType.type$));
    }
    else {
      if (isEnum) {
        this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().enumOrdinal(), CMethod.type$)), sys.Int.type$.toNullable()));
      }
      ;
    }
    ;
    if (sys.ObjUtil.compareNE(min, 0)) {
      this.op(FOp.LoadInt(), sys.ObjUtil.coerce(this.#fpod.ints().add(sys.ObjUtil.coerce(sys.Int.negate(min), sys.Obj.type$)), sys.Int.type$.toNullable()));
      this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().intPlus(), CMethod.type$)), sys.Int.type$.toNullable()));
    }
    ;
    let count = sys.Int.plus(sys.Int.minus(max, min), 1);
    let jumps = sys.List.make(Case.type$.toNullable());
    jumps.size(count);
    stmt.cases().each((c) => {
      for (let i = 0; sys.ObjUtil.compareLT(i, c.cases().size()); i = sys.Int.increment(i)) {
        let expr = c.cases().get(i);
        let literal = expr.asTableSwitchCase();
        let offset = sys.Int.minus(sys.ObjUtil.coerce(literal, sys.Int.type$), min);
        jumps.set(offset, c);
      }
      ;
      return;
    });
    this.op(FOp.Switch());
    this.#code.writeI2(count);
    let jumpStart = this.#code.size();
    let fill = sys.Int.mult(count, 2);
    sys.Int.times(fill, () => {
      this$.#code.write(255);
      return;
    });
    let defaultStart = this.mark();
    let defaultEnd = this.switchBlock(stmt.defaultBlock());
    let caseEnds = sys.List.make(sys.Int.type$.toNullable());
    caseEnds.size(stmt.cases().size());
    stmt.cases().each((c,i) => {
      c.startOffset(this$.#code.size());
      caseEnds.set(i, sys.ObjUtil.coerce(sys.ObjUtil.coerce(this$.switchBlock(c.block()), sys.Int.type$.toNullable()), sys.Int.type$.toNullable()));
      return;
    });
    let end = this.#code.size();
    this.#code.seek(jumpStart);
    jumps.each((c,i) => {
      if (c == null) {
        this$.#code.writeI2(defaultStart);
      }
      else {
        this$.#code.writeI2(c.startOffset());
      }
      ;
      return;
    });
    this.#code.seek(end);
    if (sys.ObjUtil.compareNE(defaultEnd, -1)) {
      this.backpatch(defaultEnd);
    }
    ;
    caseEnds.each((pos) => {
      if (sys.ObjUtil.compareNE(pos, -1)) {
        this$.backpatch(pos);
      }
      ;
      return;
    });
    return;
  }

  equalsSwitchStmt(stmt) {
    const this$ = this;
    stmt.isTableswitch(false);
    let condition = stmt.condition();
    this.expr(condition);
    let jumpPositions = sys.List.make(sys.Int.type$);
    let jumpCases = sys.List.make(Case.type$);
    stmt.cases().each((c) => {
      for (let i = 0; sys.ObjUtil.compareLT(i, c.cases().size()); i = sys.Int.increment(i)) {
        this$.opType(FOp.Dup(), sys.ObjUtil.coerce(condition.ctype(), CType.type$));
        this$.compareOp(sys.ObjUtil.coerce(stmt.condition().ctype(), sys.Obj.type$), FOp.CmpEQ(), c.cases().get(i));
        jumpPositions.add(sys.ObjUtil.coerce(this$.jump(FOp.JumpTrue()), sys.Int.type$.toNullable()));
        jumpCases.add(c);
      }
      ;
      return;
    });
    let defaultStart = this.mark();
    let defaultEnd = this.switchBlock(stmt.defaultBlock(), condition.ctype());
    let caseEnds = sys.List.make(sys.Int.type$.toNullable());
    caseEnds.size(stmt.cases().size());
    stmt.cases().each((c,i) => {
      c.startOffset(this$.#code.size());
      caseEnds.set(i, sys.ObjUtil.coerce(sys.ObjUtil.coerce(this$.switchBlock(c.block(), condition.ctype()), sys.Int.type$.toNullable()), sys.Int.type$.toNullable()));
      return;
    });
    let end = this.#code.size();
    jumpPositions.each((pos,i) => {
      this$.backpatch(pos, jumpCases.get(i).startOffset());
      return;
    });
    this.#code.seek(end);
    if (sys.ObjUtil.compareNE(defaultEnd, -1)) {
      this.backpatch(defaultEnd);
    }
    ;
    caseEnds.each((pos) => {
      if (sys.ObjUtil.compareNE(pos, -1)) {
        this$.backpatch(pos);
      }
      ;
      return;
    });
    return;
  }

  switchBlock(block,popType) {
    if (popType === undefined) popType = null;
    if (popType != null) {
      this.opType(FOp.Pop(), sys.ObjUtil.coerce(popType, CType.type$));
    }
    ;
    if (block != null) {
      this.block(sys.ObjUtil.coerce(block, Block.type$));
      if (block.isExit()) {
        return -1;
      }
      ;
    }
    ;
    return this.jump(FOp.Jump());
  }

  inProtectedRegion() {
    return (this.#protectedRegions != null && !this.#protectedRegions.isEmpty());
  }

  tryStmt(stmt) {
    const this$ = this;
    if (this.#protectedRegions == null) {
      this.#protectedRegions = sys.List.make(ProtectedRegion.type$);
    }
    ;
    let region = ProtectedRegion.make(stmt);
    this.#protectedRegions.push(region);
    if (!this.#loopStack.isEmpty()) {
      this.#loopStack.peek().protectedRegions().push(region);
    }
    ;
    let start = this.mark();
    this.block(sys.ObjUtil.coerce(stmt.block(), Block.type$));
    let end = this.mark();
    let tryDone = -1;
    let finallyStart = -1;
    if (!stmt.block().isExit()) {
      if (region.hasFinally()) {
        region.jumpFinallys().add(sys.ObjUtil.coerce(this.jump(FOp.JumpFinally()), sys.Int.type$.toNullable()));
        (end = this.mark());
      }
      ;
      (tryDone = this.jump(FOp.Leave()));
    }
    ;
    let catchDones = sys.List.make(sys.Int.type$.toNullable());
    catchDones.size(stmt.catches().size());
    stmt.catches().each((c,i) => {
      catchDones.set(i, sys.ObjUtil.coerce(sys.ObjUtil.coerce(this$.tryCatch(c, start, end, region), sys.Int.type$.toNullable()), sys.Int.type$.toNullable()));
      return;
    });
    if (region.hasFinally()) {
      this.addToErrTable(start, end, this.mark(), null);
      stmt.catches().each((c) => {
        this$.addToErrTable(c.start(), c.end(), this$.mark(), null);
        return;
      });
      region.jumpFinallys().each((pos) => {
        this$.backpatch(pos);
        return;
      });
      this.op(FOp.FinallyStart());
      this.block(sys.ObjUtil.coerce(stmt.finallyBlock(), Block.type$));
      this.op(FOp.FinallyEnd());
    }
    ;
    if (sys.ObjUtil.compareNE(tryDone, -1)) {
      this.backpatch(tryDone);
    }
    ;
    catchDones.each((pos) => {
      if (sys.ObjUtil.compareNE(pos, -1)) {
        this$.backpatch(pos);
      }
      ;
      return;
    });
    if (!this.#loopStack.isEmpty()) {
      this.#loopStack.peek().protectedRegions().pop();
    }
    ;
    this.#protectedRegions.pop();
    return;
  }

  tryCatch(c,start,end,region) {
    let handler = this.mark();
    c.start(this.mark());
    if (c.errVariable() == null) {
      this.op(FOp.CatchAllStart());
    }
    ;
    this.block(sys.ObjUtil.coerce(c.block(), Block.type$));
    let done = -1;
    if (!c.block().isExit()) {
      if (region.hasFinally()) {
        region.jumpFinallys().add(sys.ObjUtil.coerce(this.jump(FOp.JumpFinally()), sys.Int.type$.toNullable()));
      }
      ;
      (done = this.jump(FOp.Leave()));
    }
    ;
    c.end(this.mark());
    this.op(FOp.CatchEnd());
    this.addToErrTable(start, end, handler, c.errType());
    return done;
  }

  addToErrTable(start,end,handler,errType) {
    if (errType == null) {
      (errType = this.ns().errType());
    }
    ;
    ((this$) => { let $_u17 = this$.#errCount;this$.#errCount = sys.Int.increment(this$.#errCount); return $_u17; })(this);
    this.#errTable.writeI2(start);
    this.#errTable.writeI2(end);
    this.#errTable.writeI2(handler);
    this.#errTable.writeI2(this.#fpod.addTypeRef(sys.ObjUtil.coerce(errType, CType.type$)));
    return;
  }

  expr(expr) {
    this.line(expr.loc());
    let $_u18 = expr.id();
    if (sys.ObjUtil.equals($_u18, ExprId.nullLiteral())) {
      this.nullLiteral();
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.trueLiteral()) || sys.ObjUtil.equals($_u18, ExprId.falseLiteral())) {
      this.boolLiteral(sys.ObjUtil.coerce(expr, LiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.intLiteral())) {
      this.intLiteral(sys.ObjUtil.coerce(expr, LiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.floatLiteral())) {
      this.floatLiteral(sys.ObjUtil.coerce(expr, LiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.decimalLiteral())) {
      this.decimalLiteral(sys.ObjUtil.coerce(expr, LiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.strLiteral())) {
      this.strLiteral(sys.ObjUtil.coerce(expr, LiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.durationLiteral())) {
      this.durationLiteral(sys.ObjUtil.coerce(expr, LiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.uriLiteral())) {
      this.uriLiteral(sys.ObjUtil.coerce(expr, LiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.typeLiteral())) {
      this.typeLiteral(sys.ObjUtil.coerce(expr, LiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.slotLiteral())) {
      this.slotLiteral(sys.ObjUtil.coerce(expr, SlotLiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.rangeLiteral())) {
      this.rangeLiteral(sys.ObjUtil.coerce(expr, RangeLiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.listLiteral())) {
      this.listLiteral(sys.ObjUtil.coerce(expr, ListLiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.mapLiteral())) {
      this.mapLiteral(sys.ObjUtil.coerce(expr, MapLiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.boolNot())) {
      this.not(sys.ObjUtil.coerce(expr, UnaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.cmpNull())) {
      this.cmpNull(sys.ObjUtil.coerce(expr, UnaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.cmpNotNull())) {
      this.cmpNotNull(sys.ObjUtil.coerce(expr, UnaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.elvis())) {
      this.elvis(sys.ObjUtil.coerce(expr, BinaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.assign())) {
      this.assign(sys.ObjUtil.coerce(expr, BinaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.same())) {
      this.same(sys.ObjUtil.coerce(expr, BinaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.notSame())) {
      this.notSame(sys.ObjUtil.coerce(expr, BinaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.boolOr())) {
      this.or(sys.ObjUtil.coerce(expr, CondExpr.type$), null);
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.boolAnd())) {
      this.and(sys.ObjUtil.coerce(expr, CondExpr.type$), null);
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.isExpr())) {
      this.isExpr(sys.ObjUtil.coerce(expr, TypeCheckExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.isnotExpr())) {
      this.isnotExpr(sys.ObjUtil.coerce(expr, TypeCheckExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.asExpr())) {
      this.asExpr(sys.ObjUtil.coerce(expr, TypeCheckExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.localVar()) || sys.ObjUtil.equals($_u18, ExprId.thisExpr()) || sys.ObjUtil.equals($_u18, ExprId.superExpr()) || sys.ObjUtil.equals($_u18, ExprId.itExpr())) {
      this.loadLocalVar(sys.ObjUtil.coerce(expr, LocalVarExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.call()) || sys.ObjUtil.equals($_u18, ExprId.construction())) {
      this.call(sys.ObjUtil.coerce(expr, CallExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.shortcut())) {
      this.shortcut(sys.ObjUtil.coerce(expr, ShortcutExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.field())) {
      this.loadField(sys.ObjUtil.coerce(expr, FieldExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.coerce())) {
      this.coerce(sys.ObjUtil.coerce(expr, TypeCheckExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.closure())) {
      this.closure(sys.ObjUtil.coerce(expr, ClosureExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.ternary())) {
      this.ternary(sys.ObjUtil.coerce(expr, TernaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.staticTarget())) {
      return;
    }
    else if (sys.ObjUtil.equals($_u18, ExprId.throwExpr())) {
      this.throwOp(sys.ObjUtil.coerce(expr, ThrowExpr.type$).exception());
    }
    else {
      throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus("", expr.id()), " - "), expr.toStr()), expr.loc());
    }
    ;
    return;
  }

  nullLiteral() {
    this.op(FOp.LoadNull());
    return;
  }

  boolLiteral(expr) {
    if (sys.ObjUtil.equals(expr.val(), true)) {
      this.op(FOp.LoadTrue());
    }
    else {
      this.op(FOp.LoadFalse());
    }
    ;
    return;
  }

  intLiteral(expr) {
    this.op(FOp.LoadInt(), sys.ObjUtil.coerce(this.#fpod.ints().add(sys.ObjUtil.coerce(expr.val(), sys.Obj.type$)), sys.Int.type$.toNullable()));
    return;
  }

  floatLiteral(expr) {
    this.op(FOp.LoadFloat(), sys.ObjUtil.coerce(this.#fpod.floats().add(sys.ObjUtil.coerce(expr.val(), sys.Obj.type$)), sys.Int.type$.toNullable()));
    return;
  }

  decimalLiteral(expr) {
    this.op(FOp.LoadDecimal(), sys.ObjUtil.coerce(this.#fpod.decimals().add(sys.ObjUtil.coerce(expr.val(), sys.Obj.type$)), sys.Int.type$.toNullable()));
    return;
  }

  strLiteral(expr) {
    this.op(FOp.LoadStr(), sys.ObjUtil.coerce(this.#fpod.strs().add(sys.ObjUtil.coerce(expr.val(), sys.Obj.type$)), sys.Int.type$.toNullable()));
    return;
  }

  durationLiteral(expr) {
    this.op(FOp.LoadDuration(), sys.ObjUtil.coerce(this.#fpod.durations().add(sys.ObjUtil.coerce(expr.val(), sys.Obj.type$)), sys.Int.type$.toNullable()));
    return;
  }

  uriLiteral(expr) {
    this.op(FOp.LoadUri(), sys.ObjUtil.coerce(this.#fpod.uris().add(sys.ObjUtil.coerce(expr.val(), sys.Obj.type$)), sys.Int.type$.toNullable()));
    return;
  }

  typeLiteral(expr) {
    let val = sys.ObjUtil.coerce(expr.val(), CType.type$);
    this.op(FOp.LoadType(), sys.ObjUtil.coerce(this.#fpod.addTypeRef(val), sys.Int.type$.toNullable()));
    return;
  }

  slotLiteral(expr) {
    this.op(FOp.LoadType(), sys.ObjUtil.coerce(this.#fpod.addTypeRef(expr.parent()), sys.Int.type$.toNullable()));
    this.op(FOp.LoadStr(), sys.ObjUtil.coerce(this.#fpod.strs().add(expr.name()), sys.Int.type$.toNullable()));
    if (sys.ObjUtil.is(expr.slot(), CField.type$)) {
      this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().typeField(), CMethod.type$), sys.ObjUtil.coerce(1, sys.Int.type$.toNullable())), sys.Int.type$.toNullable()));
    }
    else {
      this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().typeMethod(), CMethod.type$), sys.ObjUtil.coerce(1, sys.Int.type$.toNullable())), sys.Int.type$.toNullable()));
    }
    ;
    return;
  }

  rangeLiteral(r) {
    this.expr(r.start());
    this.expr(r.end());
    if (r.exclusive()) {
      this.op(FOp.CallNew(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().rangeMakeExclusive(), CMethod.type$)), sys.Int.type$.toNullable()));
    }
    else {
      this.op(FOp.CallNew(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().rangeMakeInclusive(), CMethod.type$)), sys.Int.type$.toNullable()));
    }
    ;
    return;
  }

  listLiteral(list) {
    let t = list.ctype();
    if (sys.ObjUtil.is(t, NullableType.type$)) {
      (t = sys.ObjUtil.coerce(sys.ObjUtil.trap(t,"root", sys.List.make(sys.Obj.type$.toNullable(), [])), CType.type$.toNullable()));
    }
    ;
    let v = sys.ObjUtil.coerce(t, ListType.type$).v();
    this.op(FOp.LoadType(), sys.ObjUtil.coerce(this.#fpod.addTypeRef(v), sys.Int.type$.toNullable()));
    this.op(FOp.LoadInt(), sys.ObjUtil.coerce(this.#fpod.ints().add(sys.ObjUtil.coerce(list.vals().size(), sys.Obj.type$)), sys.Int.type$.toNullable()));
    this.op(FOp.CallNew(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().listMake(), CMethod.type$)), sys.Int.type$.toNullable()));
    let add = this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().listAdd(), CMethod.type$));
    for (let i = 0; sys.ObjUtil.compareLT(i, list.vals().size()); i = sys.Int.increment(i)) {
      this.expr(list.vals().get(i));
      this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(add, sys.Int.type$.toNullable()));
    }
    ;
    return;
  }

  mapLiteral(map) {
    this.op(FOp.LoadType(), sys.ObjUtil.coerce(this.#fpod.addTypeRef(sys.ObjUtil.coerce(map.ctype(), CType.type$)), sys.Int.type$.toNullable()));
    this.op(FOp.CallNew(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().mapMake(), CMethod.type$)), sys.Int.type$.toNullable()));
    let set$ = this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().mapSet(), CMethod.type$));
    for (let i = 0; sys.ObjUtil.compareLT(i, map.keys().size()); i = sys.Int.increment(i)) {
      this.expr(map.keys().get(i));
      this.expr(map.vals().get(i));
      this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(set$, sys.Int.type$.toNullable()));
    }
    ;
    return;
  }

  not(unary) {
    this.expr(unary.operand());
    this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().boolNot(), CMethod.type$)), sys.Int.type$.toNullable()));
    return;
  }

  cmpNull(unary) {
    this.expr(unary.operand());
    this.opType(FOp.CmpNull(), sys.ObjUtil.coerce(unary.operand().ctype(), CType.type$));
    return;
  }

  cmpNotNull(unary) {
    this.expr(unary.operand());
    this.opType(FOp.CmpNotNull(), sys.ObjUtil.coerce(unary.operand().ctype(), CType.type$));
    return;
  }

  same(binary) {
    if ((binary.lhs().id() === ExprId.nullLiteral() || binary.rhs().id() === ExprId.nullLiteral())) {
      this.err("Unexpected use of same with null literals", binary.loc());
    }
    ;
    this.expr(binary.lhs());
    this.expr(binary.rhs());
    this.op(FOp.CmpSame());
    return;
  }

  notSame(binary) {
    if ((binary.lhs().id() === ExprId.nullLiteral() || binary.rhs().id() === ExprId.nullLiteral())) {
      this.err("Unexpected use of same with null literals", binary.loc());
    }
    ;
    this.expr(binary.lhs());
    this.expr(binary.rhs());
    this.op(FOp.CmpNotSame());
    return;
  }

  cond(expr,cond) {
    let $_u19 = expr.id();
    if (sys.ObjUtil.equals($_u19, ExprId.boolOr())) {
      this.or(expr, cond);
    }
    else if (sys.ObjUtil.equals($_u19, ExprId.boolAnd())) {
      this.and(expr, cond);
    }
    else {
      throw sys.Err.make(expr.id().toStr());
    }
    ;
    return;
  }

  or(expr,cond) {
    const this$ = this;
    let topLevel = cond == null;
    if (topLevel) {
      (cond = Cond.make());
    }
    ;
    expr.operands().each((operand,i) => {
      this$.expr(operand);
      if (sys.ObjUtil.compareLT(i, sys.Int.minus(expr.operands().size(), 1))) {
        cond.jumpTrues().add(sys.ObjUtil.coerce(this$.jump(FOp.JumpTrue()), sys.Int.type$.toNullable()));
      }
      else {
        cond.jumpFalses().add(sys.ObjUtil.coerce(this$.jump(FOp.JumpFalse()), sys.Int.type$.toNullable()));
      }
      ;
      return;
    });
    if (topLevel) {
      this.condEnd(sys.ObjUtil.coerce(cond, Cond.type$));
    }
    ;
    return;
  }

  and(expr,cond) {
    const this$ = this;
    let topLevel = cond == null;
    if (topLevel) {
      (cond = Cond.make());
    }
    ;
    expr.operands().each((operand) => {
      this$.expr(operand);
      cond.jumpFalses().add(sys.ObjUtil.coerce(this$.jump(FOp.JumpFalse()), sys.Int.type$.toNullable()));
      return;
    });
    if (topLevel) {
      this.condEnd(sys.ObjUtil.coerce(cond, Cond.type$));
    }
    ;
    return;
  }

  condEnd(cond) {
    const this$ = this;
    cond.jumpTrues().each((pos) => {
      this$.backpatch(pos);
      return;
    });
    this.op(FOp.LoadTrue());
    let end = this.jump(FOp.Jump());
    cond.jumpFalses().each((pos) => {
      this$.backpatch(pos);
      return;
    });
    this.op(FOp.LoadFalse());
    this.backpatch(end);
    return;
  }

  isExpr(tc) {
    this.expr(tc.target());
    this.op(FOp.Is(), sys.ObjUtil.coerce(this.#fpod.addTypeRef(tc.check()), sys.Int.type$.toNullable()));
    return;
  }

  isnotExpr(tc) {
    this.isExpr(tc);
    this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().boolNot(), CMethod.type$)), sys.Int.type$.toNullable()));
    return;
  }

  asExpr(tc) {
    this.expr(tc.target());
    this.op(FOp.As(), sys.ObjUtil.coerce(this.#fpod.addTypeRef(tc.check()), sys.Int.type$.toNullable()));
    return;
  }

  coerce(tc) {
    this.expr(tc.target());
    this.coerceOp(sys.ObjUtil.coerce(tc.from(), CType.type$), sys.ObjUtil.coerce(tc.ctype(), CType.type$));
    if (!tc.leave()) {
      this.opType(FOp.Pop(), sys.ObjUtil.coerce(tc.ctype(), CType.type$));
    }
    ;
    return;
  }

  coerceOp(from$,to) {
    let fromRef = this.#fpod.addTypeRef(from$);
    let toRef = this.#fpod.addTypeRef(to);
    if (sys.ObjUtil.equals(fromRef, toRef)) {
      return;
    }
    ;
    this.op(FOp.Coerce());
    this.#code.writeI2(fromRef);
    this.#code.writeI2(toRef);
    return;
  }

  elvis(binary) {
    this.expr(binary.lhs());
    this.opType(FOp.Dup(), sys.ObjUtil.coerce(binary.lhs().ctype(), CType.type$));
    this.opType(FOp.CmpNull(), sys.ObjUtil.coerce(binary.lhs().ctype(), CType.type$));
    let isNullLabel = this.jump(FOp.JumpTrue());
    let endLabel = this.jump(FOp.Jump());
    this.backpatch(isNullLabel);
    this.opType(FOp.Pop(), sys.ObjUtil.coerce(binary.lhs().ctype(), CType.type$));
    this.expr(binary.rhs());
    this.backpatch(endLabel);
    return;
  }

  ternary(ternary) {
    this.expr(ternary.condition());
    let falseLabel = this.jump(FOp.JumpFalse());
    this.expr(ternary.trueExpr());
    let endLabel = this.jump(FOp.Jump());
    this.backpatch(falseLabel);
    this.expr(ternary.falseExpr());
    this.backpatch(endLabel);
    return;
  }

  closure(c) {
    this.expr(sys.ObjUtil.coerce(c.substitute(), Expr.type$));
    return;
  }

  assign(expr) {
    let $_u20 = expr.lhs().id();
    if (sys.ObjUtil.equals($_u20, ExprId.localVar())) {
      this.assignLocalVar(expr);
    }
    else if (sys.ObjUtil.equals($_u20, ExprId.field())) {
      this.assignField(expr);
    }
    else {
      throw this.err("Internal compiler error", expr.loc());
    }
    ;
    return;
  }

  loadLocalVar(var$) {
    this.op(FOp.LoadVar(), sys.ObjUtil.coerce(var$.register(), sys.Int.type$.toNullable()));
    return;
  }

  storeLocalVar(var$) {
    this.op(FOp.StoreVar(), sys.ObjUtil.coerce(var$.register(), sys.Int.type$.toNullable()));
    return;
  }

  assignLocalVar(assign) {
    this.expr(assign.rhs());
    if (assign.leave()) {
      this.opType(FOp.Dup(), sys.ObjUtil.coerce(assign.ctype(), CType.type$));
    }
    ;
    this.storeLocalVar(sys.ObjUtil.coerce(assign.lhs(), LocalVarExpr.type$));
    return;
  }

  loadField(fexpr,dupTarget) {
    if (dupTarget === undefined) dupTarget = false;
    let field = fexpr.field();
    if (fexpr.target() != null) {
      this.expr(sys.ObjUtil.coerce(fexpr.target(), Expr.type$));
      if (dupTarget) {
        this.opType(FOp.Dup(), sys.ObjUtil.coerce(fexpr.target().ctype(), CType.type$));
      }
      ;
    }
    ;
    let isNullLabel = null;
    if (fexpr.isSafe()) {
      if (fexpr.target() == null) {
        throw this.err("Compiler error field isSafe", fexpr.loc());
      }
      ;
      this.opType(FOp.Dup(), sys.ObjUtil.coerce(fexpr.ctype(), CType.type$));
      this.opType(FOp.CmpNull(), sys.ObjUtil.coerce(fexpr.ctype(), CType.type$));
      (isNullLabel = sys.ObjUtil.coerce(this.jump(FOp.JumpTrue()), sys.Int.type$.toNullable()));
    }
    ;
    if (fexpr.useAccessor()) {
      let getter = field.getter();
      let index = this.#fpod.addMethodRef(sys.ObjUtil.coerce(getter, CMethod.type$));
      if (field.parent().isMixin()) {
        if (getter.isStatic()) {
          this.op(FOp.CallMixinStatic(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
        }
        else {
          this.op(FOp.CallMixinVirtual(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
        }
        ;
      }
      else {
        if (getter.isStatic()) {
          this.op(FOp.CallStatic(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
        }
        else {
          if (sys.ObjUtil.equals(fexpr.target().id(), ExprId.superExpr())) {
            this.op(FOp.CallNonVirtual(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
          }
          else {
            this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
          }
          ;
        }
        ;
      }
      ;
      if (field.isParameterized()) {
        this.coerceOp(sys.ObjUtil.coerce(this.ns().objType(), CType.type$), field.type());
      }
      else {
        if (field.isCovariant()) {
          this.coerceOp(field.inheritedReturns(), field.type());
        }
        ;
      }
      ;
    }
    else {
      let index = this.#fpod.addFieldRef(sys.ObjUtil.coerce(field, CField.type$));
      if (field.parent().isMixin()) {
        if (field.isStatic()) {
          this.op(FOp.LoadMixinStatic(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
        }
        else {
          throw this.err("LoadMixinInstance", fexpr.loc());
        }
        ;
      }
      else {
        if (field.isStatic()) {
          this.op(FOp.LoadStatic(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
        }
        else {
          this.op(FOp.LoadInstance(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
        }
        ;
      }
      ;
    }
    ;
    if (fexpr.isSafe()) {
      if (field.type().isVal()) {
        this.coerceOp(field.type(), field.type().toNullable());
      }
      ;
      let endLabel = this.jump(FOp.Jump());
      this.backpatch(sys.ObjUtil.coerce(isNullLabel, sys.Int.type$));
      this.opType(FOp.Pop(), sys.ObjUtil.coerce(fexpr.ctype(), CType.type$));
      this.op(FOp.LoadNull());
      this.backpatch(endLabel);
    }
    ;
    return;
  }

  assignField(assign) {
    let lhs = sys.ObjUtil.coerce(assign.lhs(), FieldExpr.type$);
    let isInstanceField = !lhs.field().isStatic();
    if (lhs.target() != null) {
      this.expr(sys.ObjUtil.coerce(lhs.target(), Expr.type$));
    }
    ;
    this.expr(assign.rhs());
    if (assign.leave()) {
      this.opType(FOp.Dup(), sys.ObjUtil.coerce(assign.ctype(), CType.type$));
      if (isInstanceField) {
        this.op(FOp.StoreVar(), sys.ObjUtil.coerce(assign.tempVar().register(), sys.Int.type$.toNullable()));
      }
      ;
    }
    ;
    this.storeField(lhs);
    if ((assign.leave() && isInstanceField)) {
      this.op(FOp.LoadVar(), sys.ObjUtil.coerce(assign.tempVar().register(), sys.Int.type$.toNullable()));
    }
    ;
    return;
  }

  storeField(fexpr) {
    let field = fexpr.field();
    if (fexpr.useAccessor()) {
      let setter = field.setter();
      let index = this.#fpod.addMethodRef(sys.ObjUtil.coerce(setter, CMethod.type$));
      if (field.parent().isMixin()) {
        if (setter.isStatic()) {
          this.op(FOp.CallMixinStatic(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
        }
        else {
          this.op(FOp.CallMixinVirtual(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
        }
        ;
      }
      else {
        if (setter.isStatic()) {
          this.op(FOp.CallStatic(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
        }
        else {
          if (sys.ObjUtil.equals(fexpr.target().id(), ExprId.superExpr())) {
            this.op(FOp.CallNonVirtual(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
          }
          else {
            this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
          }
          ;
        }
        ;
      }
      ;
    }
    else {
      let index = this.#fpod.addFieldRef(sys.ObjUtil.coerce(field, CField.type$));
      if (field.parent().isMixin()) {
        if (field.isStatic()) {
          this.op(FOp.StoreMixinStatic(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
        }
        else {
          throw this.err("StoreMixinInstance", fexpr.loc());
        }
        ;
      }
      else {
        if (field.isStatic()) {
          this.op(FOp.StoreStatic(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
        }
        else {
          this.op(FOp.StoreInstance(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
        }
        ;
      }
      ;
    }
    ;
    return;
  }

  call(call,leave) {
    if (leave === undefined) leave = call.leave();
    const this$ = this;
    let method = call.method();
    let target = call.target();
    if (target != null) {
      this.expr(sys.ObjUtil.coerce(target, Expr.type$));
    }
    ;
    let isNullLabel = null;
    if (call.isSafe()) {
      if ((target == null || (target.ctype().isVal() && !target.ctype().isNullable()))) {
        throw this.err(sys.Str.plus("Compiler error call isSafe: ", call), call.loc());
      }
      ;
      this.opType(FOp.Dup(), sys.ObjUtil.coerce(target.ctype(), CType.type$));
      this.opType(FOp.CmpNull(), sys.ObjUtil.coerce(target.ctype(), CType.type$));
      (isNullLabel = sys.ObjUtil.coerce(this.jump(FOp.JumpTrue()), sys.Int.type$.toNullable()));
      if ((target.ctype().isVal() || method.parent().isVal())) {
        this.coerceOp(sys.ObjUtil.coerce(target.ctype(), CType.type$), call.method().parent());
      }
      ;
    }
    ;
    if (call.isDynamic()) {
      this.dynamicCall(call);
    }
    else {
      call.args().each((arg) => {
        this$.expr(arg);
        return;
      });
      this.invokeCall(call, leave);
    }
    ;
    if (call.isSafe()) {
      if ((method.returns().isVal() && call.leave())) {
        this.coerceOp(method.returns(), call.ctype().toNullable());
      }
      ;
      let endLabel = this.jump(FOp.Jump());
      this.backpatch(sys.ObjUtil.coerce(isNullLabel, sys.Int.type$));
      this.opType(FOp.Pop(), sys.ObjUtil.coerce(target.ctype(), CType.type$));
      if (call.leave()) {
        this.op(FOp.LoadNull());
      }
      ;
      this.backpatch(endLabel);
    }
    ;
    return;
  }

  dynamicCall(call) {
    const this$ = this;
    this.op(FOp.LoadStr(), sys.ObjUtil.coerce(this.#fpod.strs().add(sys.ObjUtil.coerce(call.name(), sys.Obj.type$)), sys.Int.type$.toNullable()));
    if (call.args().isEmpty()) {
      this.op(FOp.LoadNull());
    }
    else {
      this.op(FOp.LoadInt(), sys.ObjUtil.coerce(this.#fpod.ints().add(sys.ObjUtil.coerce(call.args().size(), sys.Obj.type$)), sys.Int.type$.toNullable()));
      this.op(FOp.CallNew(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().listMakeObj(), CMethod.type$)), sys.Int.type$.toNullable()));
      let add = this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().listAdd(), CMethod.type$));
      call.args().each((arg) => {
        this$.expr(arg);
        this$.op(FOp.CallVirtual(), sys.ObjUtil.coerce(add, sys.Int.type$.toNullable()));
        return;
      });
    }
    ;
    this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().objTrap(), CMethod.type$)), sys.Int.type$.toNullable()));
    if (!call.leave()) {
      this.opType(FOp.Pop(), sys.ObjUtil.coerce(call.ctype(), CType.type$));
    }
    ;
    return;
  }

  invokeCall(call,leave) {
    if (leave === undefined) leave = call.leave();
    let m = call.method();
    let index = this.#fpod.addMethodRef(sys.ObjUtil.coerce(m, CMethod.type$), sys.ObjUtil.coerce(call.args().size(), sys.Int.type$.toNullable()));
    if (m.parent().isMixin()) {
      if (m.isStatic()) {
        this.op(FOp.CallMixinStatic(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
      }
      else {
        if (sys.ObjUtil.equals(call.target().id(), ExprId.superExpr())) {
          this.op(FOp.CallMixinNonVirtual(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
        }
        else {
          this.op(FOp.CallMixinVirtual(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
        }
        ;
      }
      ;
    }
    else {
      if (m.isStatic()) {
        this.op(FOp.CallStatic(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
      }
      else {
        if (m.isCtor()) {
          if ((call.target() == null || sys.ObjUtil.equals(call.target().id(), ExprId.staticTarget()))) {
            this.op(FOp.CallNew(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
          }
          else {
            this.op(FOp.CallCtor(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
          }
          ;
        }
        else {
          let targetId = call.target().id();
          if ((sys.ObjUtil.equals(targetId, ExprId.superExpr()) || (sys.ObjUtil.equals(targetId, ExprId.thisExpr()) && !m.isVirtual() && !m.parent().isObj()))) {
            this.op(FOp.CallNonVirtual(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
          }
          else {
            this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(index, sys.Int.type$.toNullable()));
          }
          ;
        }
        ;
      }
      ;
    }
    ;
    if (leave) {
      if (m.isParameterized()) {
        let ret = m.generic().returns();
        if (ret.isGenericParameter()) {
          this.coerceOp(sys.ObjUtil.coerce(this.ns().objType(), CType.type$), m.returns());
        }
        ;
      }
      else {
        if (m.isCovariant()) {
          this.coerceOp(m.inheritedReturns(), m.returns());
        }
        ;
      }
      ;
    }
    ;
    if (!leave) {
      let x = ((this$) => { if (m.isParameterized()) return m.generic(); return m; })(this);
      if ((!x.returns().isVoid() || x.isInstanceCtor())) {
        this.opType(FOp.Pop(), x.returns());
      }
      ;
    }
    ;
    return;
  }

  shortcut(call) {
    let target = call.target();
    let firstArg = call.args().first();
    let $_u22 = call.opToken();
    if (sys.ObjUtil.equals($_u22, Token.eq())) {
      this.compareOp(sys.ObjUtil.coerce(target, sys.Obj.type$), FOp.CmpEQ(), sys.ObjUtil.coerce(firstArg, Expr.type$));
      return;
    }
    else if (sys.ObjUtil.equals($_u22, Token.notEq())) {
      this.compareOp(sys.ObjUtil.coerce(target, sys.Obj.type$), FOp.CmpNE(), sys.ObjUtil.coerce(firstArg, Expr.type$));
      return;
    }
    else if (sys.ObjUtil.equals($_u22, Token.cmp())) {
      this.compareOp(sys.ObjUtil.coerce(target, sys.Obj.type$), FOp.Cmp(), sys.ObjUtil.coerce(firstArg, Expr.type$));
      return;
    }
    else if (sys.ObjUtil.equals($_u22, Token.lt())) {
      this.compareOp(sys.ObjUtil.coerce(target, sys.Obj.type$), FOp.CmpLT(), sys.ObjUtil.coerce(firstArg, Expr.type$));
      return;
    }
    else if (sys.ObjUtil.equals($_u22, Token.ltEq())) {
      this.compareOp(sys.ObjUtil.coerce(target, sys.Obj.type$), FOp.CmpLE(), sys.ObjUtil.coerce(firstArg, Expr.type$));
      return;
    }
    else if (sys.ObjUtil.equals($_u22, Token.gt())) {
      this.compareOp(sys.ObjUtil.coerce(target, sys.Obj.type$), FOp.CmpGT(), sys.ObjUtil.coerce(firstArg, Expr.type$));
      return;
    }
    else if (sys.ObjUtil.equals($_u22, Token.gtEq())) {
      this.compareOp(sys.ObjUtil.coerce(target, sys.Obj.type$), FOp.CmpGE(), sys.ObjUtil.coerce(firstArg, Expr.type$));
      return;
    }
    ;
    if (call.isStrConcat()) {
      this.addStr(call, true);
      return;
    }
    ;
    if (call.isAssign()) {
      this.shortcutAssign(call);
      return;
    }
    ;
    this.call(call);
    return;
  }

  compareOp(lhs,opCode,rhs) {
    let lhsExpr = sys.ObjUtil.as(lhs, Expr.type$);
    let lhsType = ((this$) => { if (lhsExpr != null) return lhsExpr.ctype(); return sys.ObjUtil.coerce(lhs, CType.type$); })(this);
    if (lhsExpr != null) {
      this.expr(sys.ObjUtil.coerce(lhsExpr, Expr.type$));
    }
    ;
    this.expr(rhs);
    let fromRef = this.#fpod.addTypeRef(sys.ObjUtil.coerce(lhsType, CType.type$));
    let toRef = this.#fpod.addTypeRef(sys.ObjUtil.coerce(rhs.ctype(), CType.type$));
    this.op(opCode);
    this.#code.writeI2(fromRef);
    this.#code.writeI2(toRef);
    return;
  }

  shortcutAssign(c) {
    const this$ = this;
    let var$ = c.target();
    let leaveUsingTemp = false;
    let coerce = null;
    if (sys.ObjUtil.equals(var$.id(), ExprId.coerce())) {
      (coerce = sys.ObjUtil.coerce(var$, TypeCheckExpr.type$));
      (var$ = coerce.target());
    }
    ;
    let $_u24 = var$.id();
    if (sys.ObjUtil.equals($_u24, ExprId.localVar())) {
      this.loadLocalVar(sys.ObjUtil.coerce(var$, LocalVarExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u24, ExprId.field())) {
      let fexpr = sys.ObjUtil.coerce(var$, FieldExpr.type$);
      this.loadField(fexpr, true);
      (leaveUsingTemp = !fexpr.field().isStatic());
    }
    else if (sys.ObjUtil.equals($_u24, ExprId.shortcut())) {
      let index = sys.ObjUtil.coerce(c, IndexedAssignExpr.type$);
      let get$ = sys.ObjUtil.coerce(var$, ShortcutExpr.type$);
      this.expr(sys.ObjUtil.coerce(get$.target(), Expr.type$));
      this.opType(FOp.Dup(), sys.ObjUtil.coerce(get$.target().ctype(), CType.type$));
      this.op(FOp.StoreVar(), sys.ObjUtil.coerce(index.scratchA().register(), sys.Int.type$.toNullable()));
      this.expr(get$.args().get(0));
      this.opType(FOp.Dup(), sys.ObjUtil.coerce(get$.args().get(0).ctype(), CType.type$));
      this.op(FOp.StoreVar(), sys.ObjUtil.coerce(index.scratchB().register(), sys.Int.type$.toNullable()));
      this.op(FOp.LoadVar(), sys.ObjUtil.coerce(index.scratchA().register(), sys.Int.type$.toNullable()));
      this.op(FOp.LoadVar(), sys.ObjUtil.coerce(index.scratchB().register(), sys.Int.type$.toNullable()));
      this.invokeCall(get$, true);
      (leaveUsingTemp = true);
    }
    else {
      throw this.err("Internal error", var$.loc());
    }
    ;
    if (coerce != null) {
      this.coerceOp(sys.ObjUtil.coerce(var$.ctype(), CType.type$), coerce.check());
    }
    ;
    if ((c.leave() && c.isPostfixLeave())) {
      this.opType(FOp.Dup(), sys.ObjUtil.coerce(c.ctype(), CType.type$));
      if (leaveUsingTemp) {
        this.op(FOp.StoreVar(), sys.ObjUtil.coerce(c.tempVar().register(), sys.Int.type$.toNullable()));
      }
      ;
    }
    ;
    c.args().each((arg) => {
      this$.expr(arg);
      return;
    });
    this.invokeCall(c, true);
    if ((c.leave() && !c.isPostfixLeave())) {
      this.opType(FOp.Dup(), sys.ObjUtil.coerce(c.ctype(), CType.type$));
      if (leaveUsingTemp) {
        this.op(FOp.StoreVar(), sys.ObjUtil.coerce(c.tempVar().register(), sys.Int.type$.toNullable()));
      }
      ;
    }
    ;
    if (coerce != null) {
      this.coerceOp(coerce.check(), sys.ObjUtil.coerce(var$.ctype(), CType.type$));
    }
    else {
      this.coerceOp(sys.ObjUtil.coerce(c.ctype(), CType.type$), sys.ObjUtil.coerce(var$.ctype(), CType.type$));
    }
    ;
    let $_u25 = var$.id();
    if (sys.ObjUtil.equals($_u25, ExprId.localVar())) {
      this.storeLocalVar(sys.ObjUtil.coerce(var$, LocalVarExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u25, ExprId.field())) {
      this.storeField(sys.ObjUtil.coerce(var$, FieldExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u25, ExprId.shortcut())) {
      let set$ = sys.ObjUtil.coerce(sys.ObjUtil.trap(c,"setMethod", sys.List.make(sys.Obj.type$.toNullable(), [])), CMethod.type$);
      let setParam = ((this$) => { if (set$.isParameterized()) return set$.generic(); return set$; })(this).params().get(1).type();
      if ((c.ctype().isVal() && !setParam.isVal() && coerce == null)) {
        this.coerceOp(sys.ObjUtil.coerce(c.ctype(), CType.type$), setParam);
      }
      ;
      this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(set$, sys.ObjUtil.coerce(2, sys.Int.type$.toNullable())), sys.Int.type$.toNullable()));
      if (!set$.returns().isVoid()) {
        this.opType(FOp.Pop(), set$.returns());
      }
      ;
    }
    else {
      throw this.err("Internal error", var$.loc());
    }
    ;
    if ((c.leave() && leaveUsingTemp)) {
      this.op(FOp.LoadVar(), sys.ObjUtil.coerce(c.tempVar().register(), sys.Int.type$.toNullable()));
    }
    ;
    return;
  }

  addStr(expr,topLevel) {
    if (topLevel) {
      this.op(FOp.CallNew(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().strBufMake(), CMethod.type$), sys.ObjUtil.coerce(0, sys.Int.type$.toNullable())), sys.Int.type$.toNullable()));
    }
    ;
    let lhs = expr.target();
    let rhs = expr.args().first();
    let lhsShortcut = sys.ObjUtil.as(lhs, ShortcutExpr.type$);
    if ((lhsShortcut != null && lhsShortcut.isStrConcat())) {
      this.addStr(sys.ObjUtil.coerce(lhsShortcut, ShortcutExpr.type$), false);
    }
    else {
      if (!this.isEmptyStrLiteral(sys.ObjUtil.coerce(lhs, Expr.type$))) {
        this.expr(sys.ObjUtil.coerce(lhs, Expr.type$));
        if (lhs.ctype().isVal()) {
          this.coerceOp(sys.ObjUtil.coerce(lhs.ctype(), CType.type$), sys.ObjUtil.coerce(this.ns().objType(), CType.type$));
        }
        ;
        this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().strBufAdd(), CMethod.type$)), sys.Int.type$.toNullable()));
      }
      ;
    }
    ;
    if (!this.isEmptyStrLiteral(sys.ObjUtil.coerce(rhs, Expr.type$))) {
      this.expr(sys.ObjUtil.coerce(rhs, Expr.type$));
      this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().strBufAdd(), CMethod.type$)), sys.Int.type$.toNullable()));
    }
    ;
    if (topLevel) {
      this.op(FOp.CallVirtual(), sys.ObjUtil.coerce(this.#fpod.addMethodRef(sys.ObjUtil.coerce(this.ns().strBufToStr(), CMethod.type$)), sys.Int.type$.toNullable()));
    }
    ;
    return;
  }

  isEmptyStrLiteral(expr) {
    return (expr.id() === ExprId.strLiteral() && sys.ObjUtil.equals(sys.ObjUtil.trap(expr,"val", sys.List.make(sys.Obj.type$.toNullable(), [])), ""));
  }

  opType(opcode,arg) {
    this.op(opcode, sys.ObjUtil.coerce(this.#fpod.addTypeRef(arg), sys.Int.type$.toNullable()));
    return;
  }

  op(op,arg) {
    if (arg === undefined) arg = null;
    this.#code.write(op.ordinal());
    if (arg != null) {
      this.#code.writeI2(sys.ObjUtil.coerce(arg, sys.Int.type$));
    }
    ;
    return;
  }

  mark() {
    return this.#code.size();
  }

  jump(op,mark) {
    if (mark === undefined) mark = 65535;
    this.op(op, sys.ObjUtil.coerce(mark, sys.Int.type$.toNullable()));
    return sys.Int.minus(this.#code.size(), 2);
  }

  backpatch(pos,mark) {
    if (mark === undefined) mark = this.#code.size();
    let orig = this.#code.pos();
    this.#code.seek(pos).writeI2(mark);
    this.#code.seek(orig);
    return;
  }

  finishCode() {
    const this$ = this;
    if (this.#leavesToReturn != null) {
      this.#leavesToReturn.each((pos) => {
        this$.backpatch(pos);
        return;
      });
      if (this.#returnLocal != null) {
        this.op(FOp.LoadVar(), sys.ObjUtil.coerce(this.#returnLocal.register(), sys.Int.type$.toNullable()));
      }
      ;
      this.op(FOp.Return());
    }
    ;
    if (sys.ObjUtil.compareGE(this.#code.size(), 32767)) {
      throw this.err("Method too big", this.#loc);
    }
    ;
    return this.#code;
  }

  finishErrTable() {
    this.#errTable.seek(0).writeI2(this.#errCount);
    return this.#errTable;
  }

  finishLines() {
    this.#lines.seek(0).writeI2(this.#lineCount);
    return this.#lines;
  }

  line(loc) {
    let line = loc.line();
    let offset = this.#code.size();
    if ((line == null || sys.ObjUtil.equals(this.#lastLine, line) || sys.ObjUtil.equals(this.#lastOffset, offset))) {
      return;
    }
    ;
    ((this$) => { let $_u27 = this$.#lineCount;this$.#lineCount = sys.Int.increment(this$.#lineCount); return $_u27; })(this);
    this.#lines.writeI2(offset);
    this.#lines.writeI2(sys.ObjUtil.coerce(line, sys.Int.type$));
    this.#lastLine = sys.ObjUtil.coerce(line, sys.Int.type$);
    this.#lastOffset = offset;
    return;
  }

}

class Loop extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#breaks = sys.List.make(sys.Int.type$);
    this.#continues = sys.List.make(sys.Int.type$);
    this.#protectedRegions = sys.List.make(ProtectedRegion.type$);
    return;
  }

  typeof() { return Loop.type$; }

  #stmt = null;

  stmt(it) {
    if (it === undefined) {
      return this.#stmt;
    }
    else {
      this.#stmt = it;
      return;
    }
  }

  #breaks = null;

  breaks(it) {
    if (it === undefined) {
      return this.#breaks;
    }
    else {
      this.#breaks = it;
      return;
    }
  }

  #continues = null;

  continues(it) {
    if (it === undefined) {
      return this.#continues;
    }
    else {
      this.#continues = it;
      return;
    }
  }

  #protectedRegions = null;

  protectedRegions(it) {
    if (it === undefined) {
      return this.#protectedRegions;
    }
    else {
      this.#protectedRegions = it;
      return;
    }
  }

  static make(stmt) {
    const $self = new Loop();
    Loop.make$($self,stmt);
    return $self;
  }

  static make$($self,stmt) {
    ;
    $self.#stmt = stmt;
    return;
  }

}

class ProtectedRegion extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ProtectedRegion.type$; }

  #hasFinally = false;

  hasFinally(it) {
    if (it === undefined) {
      return this.#hasFinally;
    }
    else {
      this.#hasFinally = it;
      return;
    }
  }

  #jumpFinallys = null;

  jumpFinallys(it) {
    if (it === undefined) {
      return this.#jumpFinallys;
    }
    else {
      this.#jumpFinallys = it;
      return;
    }
  }

  static make(stmt) {
    const $self = new ProtectedRegion();
    ProtectedRegion.make$($self,stmt);
    return $self;
  }

  static make$($self,stmt) {
    $self.#hasFinally = stmt.finallyBlock() != null;
    if ($self.#hasFinally) {
      $self.#jumpFinallys = sys.List.make(sys.Int.type$);
    }
    ;
    return;
  }

}

class Cond extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#jumpTrues = sys.List.make(sys.Int.type$);
    this.#jumpFalses = sys.List.make(sys.Int.type$);
    return;
  }

  typeof() { return Cond.type$; }

  #jumpTrues = null;

  jumpTrues(it) {
    if (it === undefined) {
      return this.#jumpTrues;
    }
    else {
      this.#jumpTrues = it;
      return;
    }
  }

  #jumpFalses = null;

  jumpFalses(it) {
    if (it === undefined) {
      return this.#jumpFalses;
    }
    else {
      this.#jumpFalses = it;
      return;
    }
  }

  static make() {
    const $self = new Cond();
    Cond.make$($self);
    return $self;
  }

  static make$($self) {
    ;
    return;
  }

}

class Node extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Node.type$; }

  #loc = null;

  loc(it) {
    if (it === undefined) {
      return this.#loc;
    }
    else {
      this.#loc = it;
      return;
    }
  }

  static make(loc) {
    const $self = new Node();
    Node.make$($self,loc);
    return $self;
  }

  static make$($self,loc) {
    $self.#loc = loc;
    return;
  }

  dump() {
    this.print(AstWriter.make());
    return;
  }

}

class CNode {
  constructor() {
    const this$ = this;
  }

  typeof() { return CNode.type$; }

  hasFacet(qname) {
    return this.facet(qname) != null;
  }

  isNoDoc() {
    return this.hasFacet("sys::NoDoc");
  }

}

class DefNode extends Node {
  constructor() {
    super();
    const this$ = this;
    this.#flags = 0;
    return;
  }

  typeof() { return DefNode.type$; }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  #docDef = null;

  docDef(it) {
    if (it === undefined) {
      return this.#docDef;
    }
    else {
      this.#docDef = it;
      return;
    }
  }

  #flags = 0;

  flags(it) {
    if (it === undefined) {
      return this.#flags;
    }
    else {
      this.#flags = it;
      return;
    }
  }

  #facets = null;

  facets(it) {
    if (it === undefined) {
      return this.#facets;
    }
    else {
      this.#facets = it;
      return;
    }
  }

  static make(loc) {
    const $self = new DefNode();
    DefNode.make$($self,loc);
    return $self;
  }

  static make$($self,loc) {
    Node.make$($self, loc);
    ;
    return;
  }

  doc() {
    return this.#docDef;
  }

  walkFacets(v,depth) {
    const this$ = this;
    if ((this.#facets != null && sys.ObjUtil.compareGE(depth, VisitDepth.expr()))) {
      this.#facets.each((f) => {
        f.walk(v);
        return;
      });
    }
    ;
    return;
  }

  facet(qname) {
    const this$ = this;
    if (this.#facets == null) {
      return null;
    }
    ;
    return this.#facets.find((f) => {
      return sys.ObjUtil.equals(f.type().qname(), qname);
    });
  }

  addFacet(support,type,vals) {
    if (vals === undefined) vals = null;
    const this$ = this;
    if (this.#facets == null) {
      this.#facets = sys.List.make(FacetDef.type$);
    }
    ;
    let loc = this.loc();
    let f = FacetDef.make(loc, type);
    ((this$) => { let $_u28 = vals; if ($_u28 == null) return null; return vals.each((v,n) => {
      f.names().add(n);
      f.vals().add(Expr.makeForLiteral(loc, this$.ns(), v));
      return;
    }); })(this);
    this.#facets.add(f);
    return;
  }

  printFacets(out) {
    const this$ = this;
    if (this.#facets == null) {
      return;
    }
    ;
    this.#facets.each((f) => {
      f.print(out);
      return;
    });
    return;
  }

  isDocumented() {
    if (sys.ObjUtil.compareNE(sys.Int.and(this.#flags, FConst.Synthetic()), 0)) {
      return false;
    }
    ;
    if ((sys.ObjUtil.equals(sys.Int.and(this.#flags, FConst.Public()), 0) && sys.ObjUtil.equals(sys.Int.and(this.#flags, FConst.Protected()), 0))) {
      return false;
    }
    ;
    if (sys.ObjUtil.is(this, TypeDef.type$)) {
      if ((this.ns().c() != null && this.ns().c().input().docTests())) {
        return true;
      }
      ;
      let t = sys.ObjUtil.coerce(this, TypeDef.type$);
      if ((t.base() != null && t.base().fits(sys.ObjUtil.coerce(this.ns().testType(), CType.type$)))) {
        return t.isAbstract();
      }
      ;
    }
    else {
      if (sys.ObjUtil.is(this, MethodDef.type$)) {
        let m = sys.ObjUtil.coerce(this, MethodDef.type$);
        if (m.isFieldAccessor()) {
          return false;
        }
        ;
      }
      ;
    }
    ;
    return true;
  }

}

class CDoc {
  constructor() {
    const this$ = this;
  }

  typeof() { return CDoc.type$; }

  static fromStr(s) {
    return ((this$) => { if (s == null) return null; return MDoc.make(sys.ObjUtil.coerce(s, sys.Str.type$)); })(this);
  }

}

class DocDef extends Node {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return DocDef.type$; }

  #lines = null;

  lines(it) {
    if (it === undefined) {
      return this.#lines;
    }
    else {
      this.#lines = it;
      return;
    }
  }

  static make(loc,lines) {
    const $self = new DocDef();
    DocDef.make$($self,loc,lines);
    return $self;
  }

  static make$($self,loc,lines) {
    Node.make$($self, loc);
    $self.#lines = lines;
    return;
  }

  text() {
    return this.#lines.join("\n");
  }

  print(out) {
    const this$ = this;
    this.#lines.each((line) => {
      out.w("** ").w(line).nl();
      return;
    });
    return;
  }

}

class Block extends Node {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Block.type$; }

  #stmts = null;

  stmts(it) {
    if (it === undefined) {
      return this.#stmts;
    }
    else {
      this.#stmts = it;
      return;
    }
  }

  static make(loc) {
    const $self = new Block();
    Block.make$($self,loc);
    return $self;
  }

  static make$($self,loc) {
    Node.make$($self, loc);
    $self.#stmts = sys.List.make(Stmt.type$);
    return;
  }

  isEmpty() {
    return this.#stmts.isEmpty();
  }

  size() {
    return this.#stmts.size();
  }

  isExit() {
    if (this.#stmts.isEmpty()) {
      return false;
    }
    ;
    return this.#stmts.last().isExit();
  }

  isDefiniteAssign(f) {
    const this$ = this;
    return this.#stmts.any((s) => {
      return s.isDefiniteAssign(f);
    });
  }

  add(stmt) {
    this.#stmts.add(stmt);
    return;
  }

  addAll(stmts) {
    this.#stmts.addAll(stmts);
    return;
  }

  walkExpr(closure) {
    this.walk(ExprVisitor.make(closure), VisitDepth.expr());
    return;
  }

  walk(v,depth) {
    const this$ = this;
    v.enterBlock(this);
    let copy = sys.List.make(Stmt.type$);
    copy.capacity(this.#stmts.size());
    this.#stmts.each((stmt) => {
      let r = stmt.walk(v, depth);
      if (r == null) {
        copy.add(stmt);
      }
      else {
        copy.addAll(sys.ObjUtil.coerce(r, sys.Type.find("compiler::Stmt[]")));
      }
      ;
      return;
    });
    this.#stmts = copy;
    v.visitBlock(this);
    v.exitBlock(this);
    return;
  }

  print(out) {
    this.printOpt(out);
    return;
  }

  printOpt(out,braces) {
    if (braces === undefined) braces = true;
    const this$ = this;
    if (braces) {
      out.w("{").nl();
    }
    ;
    out.indent();
    this.#stmts.each((stmt) => {
      stmt.print(out);
      return;
    });
    out.unindent();
    if (braces) {
      out.w("}").nl();
    }
    ;
    return;
  }

}

class Expr extends Node {
  constructor() {
    super();
    const this$ = this;
    this.#leave = true;
    return;
  }

  typeof() { return Expr.type$; }

  #id = null;

  id() { return this.#id; }

  __id(it) { if (it === undefined) return this.#id; else this.#id = it; }

  #ctype = null;

  ctype(it) {
    if (it === undefined) {
      return this.#ctype;
    }
    else {
      this.#ctype = it;
      return;
    }
  }

  #leave = false;

  leave(it) {
    if (it === undefined) {
      return this.#leave;
    }
    else {
      this.#leave = it;
      return;
    }
  }

  static make(loc,id) {
    const $self = new Expr();
    Expr.make$($self,loc,id);
    return $self;
  }

  static make$($self,loc,id) {
    Node.make$($self, loc);
    ;
    $self.#id = id;
    return;
  }

  asTableSwitchCase() {
    return null;
  }

  toTypeStr() {
    if (sys.ObjUtil.equals(this.#id, ExprId.nullLiteral())) {
      return "null";
    }
    ;
    return this.#ctype.toStr();
  }

  assignTarget() {
    return null;
  }

  isAssignable() {
    return false;
  }

  isCond() {
    return false;
  }

  isStmt() {
    return false;
  }

  synthetic() {
    return false;
  }

  isDefiniteAssign(f) {
    return false;
  }

  isAlwaysNullable() {
    return false;
  }

  asCall() {
    return null;
  }

  assignRequiresTempVar() {
    return false;
  }

  sameVarAs(that) {
    return false;
  }

  static ctypes(exprs) {
    const this$ = this;
    return sys.ObjUtil.coerce(exprs.map((e) => {
      return sys.ObjUtil.coerce(e.#ctype, CType.type$);
    }, CType.type$), sys.Type.find("compiler::CType[]"));
  }

  static commonType(ns,exprs) {
    const this$ = this;
    let hasNull = false;
    (exprs = exprs.exclude((e) => {
      if (e.#id !== ExprId.nullLiteral()) {
        return false;
      }
      ;
      (hasNull = true);
      return true;
    }));
    let t = CType.common(ns, Expr.ctypes(exprs));
    if (hasNull) {
      (t = t.toNullable());
    }
    ;
    return t;
  }

  toStmt() {
    return ExprStmt.make(this);
  }

  serialize() {
    throw CompilerErr.make(sys.Str.plus(sys.Str.plus("'", this.#id), "' not serializable"), this.loc());
  }

  static makeForLiteral(loc,ns,val) {
    const this$ = this;
    let $_u30 = sys.ObjUtil.typeof(val);
    if (sys.ObjUtil.equals($_u30, sys.Bool.type$)) {
      return ((this$) => { if (sys.ObjUtil.equals(val, true)) return LiteralExpr.make(loc, ExprId.trueLiteral(), sys.ObjUtil.coerce(ns.boolType(), CType.type$), sys.ObjUtil.coerce(true, sys.Obj.type$.toNullable())); return LiteralExpr.make(loc, ExprId.falseLiteral(), sys.ObjUtil.coerce(ns.boolType(), CType.type$), sys.ObjUtil.coerce(false, sys.Obj.type$.toNullable())); })(this);
    }
    else if (sys.ObjUtil.equals($_u30, sys.Str.type$)) {
      return LiteralExpr.make(loc, ExprId.strLiteral(), sys.ObjUtil.coerce(ns.strType(), CType.type$), val);
    }
    else if (sys.ObjUtil.equals($_u30, sys.DateTime.type$)) {
      return sys.ObjUtil.coerce(sys.ObjUtil.with(CallExpr.make(loc, null, "fromStr", ExprId.construction()), (it) => {
        it.method(sys.ObjUtil.coerce(ns.resolveSlot("sys::DateTime.fromStr"), CMethod.type$.toNullable()));
        it.#ctype = it.method().parent();
        it.args(sys.List.make(Expr.type$, [Expr.makeForLiteral(loc, ns, sys.ObjUtil.toStr(val))]));
        return;
      }), CallExpr.type$);
    }
    else {
      throw sys.Err.make(sys.Str.plus("Unsupported literal type ", sys.ObjUtil.typeof(val)));
    }
    ;
  }

  noLeave() {
    let result = this;
    if (result.#id === ExprId.coerce()) {
      let coerce = sys.ObjUtil.coerce(result, TypeCheckExpr.type$);
      if (coerce.synthetic()) {
        (result = coerce.target());
      }
      ;
    }
    ;
    result.#leave = false;
    return result;
  }

  toDocStr() {
    try {
      if (sys.ObjUtil.is(this, LiteralExpr.type$)) {
        let s = this.serialize();
        if (sys.ObjUtil.compareGT(sys.Str.size(s), 40)) {
          (s = "...");
        }
        ;
        return s;
      }
      ;
      if (sys.ObjUtil.is(this, TypeCheckExpr.type$)) {
        return sys.ObjUtil.coerce(this, TypeCheckExpr.type$).target().toDocStr();
      }
      ;
      let slot = null;
      if (sys.ObjUtil.is(this, CallExpr.type$)) {
        (slot = sys.ObjUtil.coerce(this, CallExpr.type$).method());
      }
      else {
        if (sys.ObjUtil.is(this, FieldExpr.type$)) {
          (slot = sys.ObjUtil.coerce(this, FieldExpr.type$).field());
        }
        ;
      }
      ;
      if ((slot != null && (slot.isPrivate() || slot.isInternal()))) {
        return null;
      }
      ;
      let s = this.toStr();
      if ((sys.ObjUtil.equals(sys.Str.get(s, 0), 40) && sys.ObjUtil.equals(sys.Str.get(s, -1), 41))) {
        (s = sys.Str.getRange(s, sys.Range.make(1, -2)));
      }
      ;
      if (sys.Str.contains(s, "=")) {
        (s = sys.Str.trim(sys.Str.getRange(s, sys.Range.make(sys.Int.plus(sys.ObjUtil.coerce(sys.Str.index(s, "="), sys.Int.type$), 1), -1))));
      }
      ;
      if ((sys.ObjUtil.equals(sys.Str.get(s, 0), 40) && sys.ObjUtil.equals(sys.Str.get(s, -1), 41) && !sys.Str.endsWith(s, "()"))) {
        (s = sys.Str.getRange(s, sys.Range.make(1, -2)));
      }
      ;
      (s = sys.Str.replace(s, ".@", "."));
      (s = sys.Str.replace(s, ".?(", "("));
      while (true) {
        let qcolon = sys.Str.index(s, "::");
        if (qcolon == null) {
          break;
        }
        ;
        let i = sys.Int.minus(sys.ObjUtil.coerce(qcolon, sys.Int.type$), 1);
        for (; sys.ObjUtil.compareGE(i, 0); i = sys.Int.decrement(i)) {
          if ((!sys.Int.isAlphaNum(sys.Str.get(s, i)) && sys.ObjUtil.compareNE(sys.Str.get(s, i), 95))) {
            break;
          }
          ;
        }
        ;
        (s = ((this$) => { if (sys.ObjUtil.compareLT(i, 0)) return sys.Str.getRange(s, sys.Range.make(sys.Int.plus(sys.ObjUtil.coerce(qcolon, sys.Int.type$), 2), -1)); return sys.Str.plus(sys.Str.getRange(s, sys.Range.make(0, i)), sys.Str.getRange(s, sys.Range.make(sys.Int.plus(sys.ObjUtil.coerce(qcolon, sys.Int.type$), 2), -1))); })(this));
      }
      ;
      if (sys.ObjUtil.compareGT(sys.Str.size(s), 40)) {
        (s = "...");
      }
      ;
      return s;
    }
    catch ($_u33) {
      $_u33 = sys.Err.make($_u33);
      if ($_u33 instanceof sys.Err) {
        let e = $_u33;
        ;
        e.trace();
        return this.toStr();
      }
      else {
        throw $_u33;
      }
    }
    ;
  }

  walk(v) {
    this.walkChildren(v);
    return v.visitExpr(this);
  }

  walkChildren(v) {
    return;
  }

  static walkExpr(v,expr) {
    if (expr == null) {
      return null;
    }
    ;
    return expr.walk(v);
  }

  static walkExprs(v,exprs) {
    for (let i = 0; sys.ObjUtil.compareLT(i, exprs.size()); i = sys.Int.increment(i)) {
      let expr = exprs.get(i);
      if (expr != null) {
        let replace = expr.walk(v);
        if (expr !== replace) {
          exprs.set(i, replace);
        }
        ;
      }
      ;
    }
    ;
    return exprs;
  }

  print(out) {
    out.w(this.toStr());
    return;
  }

}

class LiteralExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return LiteralExpr.type$; }

  #val = null;

  val(it) {
    if (it === undefined) {
      return this.#val;
    }
    else {
      this.#val = it;
      return;
    }
  }

  static make(loc,id,ctype,val) {
    const $self = new LiteralExpr();
    LiteralExpr.make$($self,loc,id,ctype,val);
    return $self;
  }

  static make$($self,loc,id,ctype,val) {
    Expr.make$($self, loc, id);
    $self.ctype(ctype);
    $self.#val = val;
    if ((val == null && !ctype.isNullable())) {
      throw sys.Err.make("null literal must typed as nullable!");
    }
    ;
    return;
  }

  static makeNull(loc,ns) {
    const $self = new LiteralExpr();
    LiteralExpr.makeNull$($self,loc,ns);
    return $self;
  }

  static makeNull$($self,loc,ns) {
    LiteralExpr.make$($self, loc, ExprId.nullLiteral(), ns.objType().toNullable(), null);
    return;
  }

  static makeTrue(loc,ns) {
    const $self = new LiteralExpr();
    LiteralExpr.makeTrue$($self,loc,ns);
    return $self;
  }

  static makeTrue$($self,loc,ns) {
    LiteralExpr.make$($self, loc, ExprId.trueLiteral(), sys.ObjUtil.coerce(ns.boolType(), CType.type$), sys.ObjUtil.coerce(true, sys.Obj.type$.toNullable()));
    return;
  }

  static makeFalse(loc,ns) {
    const $self = new LiteralExpr();
    LiteralExpr.makeFalse$($self,loc,ns);
    return $self;
  }

  static makeFalse$($self,loc,ns) {
    LiteralExpr.make$($self, loc, ExprId.falseLiteral(), sys.ObjUtil.coerce(ns.boolType(), CType.type$), sys.ObjUtil.coerce(false, sys.Obj.type$.toNullable()));
    return;
  }

  static makeStr(loc,ns,val) {
    const $self = new LiteralExpr();
    LiteralExpr.makeStr$($self,loc,ns,val);
    return $self;
  }

  static makeStr$($self,loc,ns,val) {
    LiteralExpr.make$($self, loc, ExprId.strLiteral(), sys.ObjUtil.coerce(ns.strType(), CType.type$), val);
    return;
  }

  static makeDefaultLiteral(loc,ns,ctype) {
    if (!ctype.isNullable()) {
      if (ctype.isBool()) {
        return LiteralExpr.make(loc, ExprId.falseLiteral(), ctype, sys.ObjUtil.coerce(false, sys.Obj.type$.toNullable()));
      }
      ;
      if (ctype.isInt()) {
        return LiteralExpr.make(loc, ExprId.intLiteral(), ctype, sys.ObjUtil.coerce(0, sys.Obj.type$.toNullable()));
      }
      ;
      if (ctype.isFloat()) {
        return LiteralExpr.make(loc, ExprId.floatLiteral(), ctype, sys.ObjUtil.coerce(sys.Float.make(0.0), sys.Obj.type$.toNullable()));
      }
      ;
    }
    ;
    return LiteralExpr.makeNull(loc, ns);
  }

  isAlwaysNullable() {
    return this.id() === ExprId.nullLiteral();
  }

  asTableSwitchCase() {
    return sys.ObjUtil.as(this.#val, sys.Int.type$);
  }

  serialize() {
    let $_u34 = this.id();
    if (sys.ObjUtil.equals($_u34, ExprId.nullLiteral())) {
      return "null";
    }
    else if (sys.ObjUtil.equals($_u34, ExprId.falseLiteral())) {
      return "false";
    }
    else if (sys.ObjUtil.equals($_u34, ExprId.trueLiteral())) {
      return "true";
    }
    else if (sys.ObjUtil.equals($_u34, ExprId.intLiteral())) {
      return sys.ObjUtil.toStr(this.#val);
    }
    else if (sys.ObjUtil.equals($_u34, ExprId.floatLiteral())) {
      return sys.Str.plus(sys.ObjUtil.toStr(this.#val), "f");
    }
    else if (sys.ObjUtil.equals($_u34, ExprId.decimalLiteral())) {
      return sys.Str.plus(sys.ObjUtil.toStr(this.#val), "d");
    }
    else if (sys.ObjUtil.equals($_u34, ExprId.strLiteral())) {
      return sys.Str.toCode(sys.ObjUtil.toStr(this.#val));
    }
    else if (sys.ObjUtil.equals($_u34, ExprId.uriLiteral())) {
      return sys.Str.toCode(sys.ObjUtil.toStr(this.#val), sys.ObjUtil.coerce(96, sys.Int.type$.toNullable()));
    }
    else if (sys.ObjUtil.equals($_u34, ExprId.typeLiteral())) {
      return sys.Str.plus(sys.Str.plus("", sys.ObjUtil.trap(this.#val,"signature", sys.List.make(sys.Obj.type$.toNullable(), []))), "#");
    }
    else if (sys.ObjUtil.equals($_u34, ExprId.durationLiteral())) {
      return sys.ObjUtil.toStr(this.#val);
    }
    else {
      return Expr.prototype.serialize.call(this);
    }
    ;
  }

  toStr() {
    let $_u35 = this.id();
    if (sys.ObjUtil.equals($_u35, ExprId.nullLiteral())) {
      return "null";
    }
    else if (sys.ObjUtil.equals($_u35, ExprId.strLiteral())) {
      return sys.Str.plus(sys.Str.plus("\"", sys.Str.replace(sys.ObjUtil.toStr(this.#val), "\n", "\\n")), "\"");
    }
    else if (sys.ObjUtil.equals($_u35, ExprId.typeLiteral())) {
      return sys.Str.plus(sys.Str.plus("", this.#val), "#");
    }
    else if (sys.ObjUtil.equals($_u35, ExprId.uriLiteral())) {
      return sys.Str.plus(sys.Str.plus("`", this.#val), "`");
    }
    else {
      return sys.ObjUtil.toStr(this.#val);
    }
    ;
  }

}

class LocaleLiteralExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return LocaleLiteralExpr.type$; }

  #pattern = null;

  pattern(it) {
    if (it === undefined) {
      return this.#pattern;
    }
    else {
      this.#pattern = it;
      return;
    }
  }

  #key = null;

  key(it) {
    if (it === undefined) {
      return this.#key;
    }
    else {
      this.#key = it;
      return;
    }
  }

  #podName = null;

  podName(it) {
    if (it === undefined) {
      return this.#podName;
    }
    else {
      this.#podName = it;
      return;
    }
  }

  #def = null;

  def(it) {
    if (it === undefined) {
      return this.#def;
    }
    else {
      this.#def = it;
      return;
    }
  }

  static make(loc,pattern) {
    const $self = new LocaleLiteralExpr();
    LocaleLiteralExpr.make$($self,loc,pattern);
    return $self;
  }

  static make$($self,loc,pattern) {
    Expr.make$($self, loc, ExprId.localeLiteral());
    $self.#pattern = pattern;
    $self.#key = pattern;
    let eq = sys.Str.index(pattern, "=");
    if (eq != null) {
      $self.#key = sys.Str.getRange(pattern, sys.Range.make(0, sys.ObjUtil.coerce(eq, sys.Int.type$), true));
      $self.#def = sys.Str.getRange(pattern, sys.Range.make(sys.Int.plus(sys.ObjUtil.coerce(eq, sys.Int.type$), 1), -1));
    }
    ;
    let colons = sys.Str.index($self.#key, "::");
    if (colons != null) {
      $self.#podName = sys.Str.getRange($self.#key, sys.Range.make(0, sys.ObjUtil.coerce(colons, sys.Int.type$), true));
      $self.#key = sys.Str.getRange($self.#key, sys.Range.make(sys.Int.plus(sys.ObjUtil.coerce(colons, sys.Int.type$), 2), -1));
    }
    ;
    return;
  }

  toStr() {
    return sys.Str.plus(sys.Str.plus("<", this.#pattern), ">");
  }

}

class SlotLiteralExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return SlotLiteralExpr.type$; }

  #parent = null;

  parent(it) {
    if (it === undefined) {
      return this.#parent;
    }
    else {
      this.#parent = it;
      return;
    }
  }

  #name = null;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #slot = null;

  slot(it) {
    if (it === undefined) {
      return this.#slot;
    }
    else {
      this.#slot = it;
      return;
    }
  }

  static make(loc,parent,name) {
    const $self = new SlotLiteralExpr();
    SlotLiteralExpr.make$($self,loc,parent,name);
    return $self;
  }

  static make$($self,loc,parent,name) {
    Expr.make$($self, loc, ExprId.slotLiteral());
    $self.#parent = parent;
    $self.#name = name;
    return;
  }

  serialize() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#parent.signature()), "#"), this.#name);
  }

  toStr() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#parent.signature()), "#"), this.#name);
  }

}

class RangeLiteralExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return RangeLiteralExpr.type$; }

  #start = null;

  start(it) {
    if (it === undefined) {
      return this.#start;
    }
    else {
      this.#start = it;
      return;
    }
  }

  #end = null;

  end(it) {
    if (it === undefined) {
      return this.#end;
    }
    else {
      this.#end = it;
      return;
    }
  }

  #exclusive = false;

  exclusive(it) {
    if (it === undefined) {
      return this.#exclusive;
    }
    else {
      this.#exclusive = it;
      return;
    }
  }

  static make(loc,ctype,start,end,exclusive) {
    const $self = new RangeLiteralExpr();
    RangeLiteralExpr.make$($self,loc,ctype,start,end,exclusive);
    return $self;
  }

  static make$($self,loc,ctype,start,end,exclusive) {
    Expr.make$($self, loc, ExprId.rangeLiteral());
    $self.ctype(ctype);
    $self.#start = start;
    $self.#end = end;
    $self.#exclusive = exclusive;
    return;
  }

  walkChildren(v) {
    this.#start = this.#start.walk(v);
    this.#end = this.#end.walk(v);
    return;
  }

  toStr() {
    if (this.#exclusive) {
      return sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#start), "..."), this.#end);
    }
    else {
      return sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#start), ".."), this.#end);
    }
    ;
  }

}

class ListLiteralExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
    this.#vals = sys.List.make(Expr.type$);
    return;
  }

  typeof() { return ListLiteralExpr.type$; }

  #explicitType = null;

  explicitType(it) {
    if (it === undefined) {
      return this.#explicitType;
    }
    else {
      this.#explicitType = it;
      return;
    }
  }

  #vals = null;

  vals(it) {
    if (it === undefined) {
      return this.#vals;
    }
    else {
      this.#vals = it;
      return;
    }
  }

  static make(loc,explicitType) {
    const $self = new ListLiteralExpr();
    ListLiteralExpr.make$($self,loc,explicitType);
    return $self;
  }

  static make$($self,loc,explicitType) {
    if (explicitType === undefined) explicitType = null;
    Expr.make$($self, loc, ExprId.listLiteral());
    ;
    $self.#explicitType = explicitType;
    return;
  }

  static makeFor(loc,ctype,vals) {
    const $self = new ListLiteralExpr();
    ListLiteralExpr.makeFor$($self,loc,ctype,vals);
    return $self;
  }

  static makeFor$($self,loc,ctype,vals) {
    Expr.make$($self, loc, ExprId.listLiteral());
    ;
    $self.ctype(ctype);
    $self.#vals = vals;
    return;
  }

  walkChildren(v) {
    this.#vals = Expr.walkExprs(v, this.#vals);
    return;
  }

  serialize() {
    const this$ = this;
    return this.format((e) => {
      return e.serialize();
    });
  }

  toStr() {
    const this$ = this;
    return this.format((e) => {
      return e.toStr();
    });
  }

  format(f) {
    const this$ = this;
    let s = sys.StrBuf.make();
    if (this.#explicitType != null) {
      s.add(this.#explicitType.v());
    }
    ;
    s.add("[");
    if (this.#vals.isEmpty()) {
      s.add(",");
    }
    else {
      this.#vals.each((v,i) => {
        if (sys.ObjUtil.compareGT(i, 0)) {
          s.add(",");
        }
        ;
        s.add(sys.Func.call(f, v));
        return;
      });
    }
    ;
    s.add("]");
    return s.toStr();
  }

}

class MapLiteralExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
    this.#keys = sys.List.make(Expr.type$);
    this.#vals = sys.List.make(Expr.type$);
    return;
  }

  typeof() { return MapLiteralExpr.type$; }

  #explicitType = null;

  explicitType(it) {
    if (it === undefined) {
      return this.#explicitType;
    }
    else {
      this.#explicitType = it;
      return;
    }
  }

  #keys = null;

  keys(it) {
    if (it === undefined) {
      return this.#keys;
    }
    else {
      this.#keys = it;
      return;
    }
  }

  #vals = null;

  vals(it) {
    if (it === undefined) {
      return this.#vals;
    }
    else {
      this.#vals = it;
      return;
    }
  }

  static make(loc,explicitType) {
    const $self = new MapLiteralExpr();
    MapLiteralExpr.make$($self,loc,explicitType);
    return $self;
  }

  static make$($self,loc,explicitType) {
    if (explicitType === undefined) explicitType = null;
    Expr.make$($self, loc, ExprId.mapLiteral());
    ;
    $self.#explicitType = explicitType;
    return;
  }

  walkChildren(v) {
    this.#keys = Expr.walkExprs(v, this.#keys);
    this.#vals = Expr.walkExprs(v, this.#vals);
    return;
  }

  serialize() {
    const this$ = this;
    return this.format((e) => {
      return e.serialize();
    });
  }

  toStr() {
    const this$ = this;
    return this.format((e) => {
      return e.toStr();
    });
  }

  format(f) {
    const this$ = this;
    let s = sys.StrBuf.make();
    if (this.#explicitType != null) {
      s.add(this.#explicitType);
    }
    ;
    s.add("[");
    if (this.#vals.isEmpty()) {
      s.add(":");
    }
    else {
      sys.Int.times(this.#keys.size(), (i) => {
        if (sys.ObjUtil.compareGT(i, 0)) {
          s.add(",");
        }
        ;
        s.add(sys.Func.call(f, this$.#keys.get(i))).add(":").add(sys.Func.call(f, this$.#vals.get(i)));
        return;
      });
    }
    ;
    s.add("]");
    return s.toStr();
  }

}

class UnaryExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return UnaryExpr.type$; }

  #opToken = null;

  opToken(it) {
    if (it === undefined) {
      return this.#opToken;
    }
    else {
      this.#opToken = it;
      return;
    }
  }

  #operand = null;

  operand(it) {
    if (it === undefined) {
      return this.#operand;
    }
    else {
      this.#operand = it;
      return;
    }
  }

  static make(loc,id,opToken,operand) {
    const $self = new UnaryExpr();
    UnaryExpr.make$($self,loc,id,opToken,operand);
    return $self;
  }

  static make$($self,loc,id,opToken,operand) {
    Expr.make$($self, loc, id);
    $self.#opToken = opToken;
    $self.#operand = operand;
    return;
  }

  walkChildren(v) {
    this.#operand = this.#operand.walk(v);
    return;
  }

  toStr() {
    if (sys.ObjUtil.equals(this.id(), ExprId.cmpNull())) {
      return sys.Str.plus(this.#operand.toStr(), " == null");
    }
    else {
      if (sys.ObjUtil.equals(this.id(), ExprId.cmpNotNull())) {
        return sys.Str.plus(this.#operand.toStr(), " != null");
      }
      else {
        return sys.Str.plus(this.#opToken.toStr(), this.#operand.toStr());
      }
      ;
    }
    ;
  }

}

class BinaryExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return BinaryExpr.type$; }

  #opToken = null;

  opToken(it) {
    if (it === undefined) {
      return this.#opToken;
    }
    else {
      this.#opToken = it;
      return;
    }
  }

  #lhs = null;

  lhs(it) {
    if (it === undefined) {
      return this.#lhs;
    }
    else {
      this.#lhs = it;
      return;
    }
  }

  #rhs = null;

  rhs(it) {
    if (it === undefined) {
      return this.#rhs;
    }
    else {
      this.#rhs = it;
      return;
    }
  }

  #tempVar = null;

  tempVar(it) {
    if (it === undefined) {
      return this.#tempVar;
    }
    else {
      this.#tempVar = it;
      return;
    }
  }

  static make(lhs,opToken,rhs) {
    const $self = new BinaryExpr();
    BinaryExpr.make$($self,lhs,opToken,rhs);
    return $self;
  }

  static make$($self,lhs,opToken,rhs) {
    Expr.make$($self, lhs.loc(), opToken.toExprId());
    $self.#lhs = lhs;
    $self.#opToken = opToken;
    $self.#rhs = rhs;
    return;
  }

  static makeAssign(lhs,rhs,leave) {
    const $self = new BinaryExpr();
    BinaryExpr.makeAssign$($self,lhs,rhs,leave);
    return $self;
  }

  static makeAssign$($self,lhs,rhs,leave) {
    if (leave === undefined) leave = false;
    BinaryExpr.make$($self, lhs, Token.assign(), rhs);
    $self.ctype(lhs.ctype());
    $self.leave(leave);
    return;
  }

  assignTarget() {
    return ((this$) => { if (this$.id() === ExprId.assign()) return this$.#lhs; return null; })(this);
  }

  isStmt() {
    return this.id() === ExprId.assign();
  }

  isDefiniteAssign(f) {
    if ((this.id() === ExprId.assign() && sys.Func.call(f, this.#lhs))) {
      return true;
    }
    ;
    return this.#rhs.isDefiniteAssign(f);
  }

  walkChildren(v) {
    this.#lhs = this.#lhs.walk(v);
    this.#rhs = this.#rhs.walk(v);
    return;
  }

  serialize() {
    if (this.id() === ExprId.assign()) {
      return sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#lhs.serialize()), "="), this.#rhs.serialize());
    }
    else {
      return Expr.prototype.serialize.call(this);
    }
    ;
  }

  toStr() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("(", this.#lhs), " "), this.#opToken), " "), this.#rhs), ")");
  }

}

class CondExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return CondExpr.type$; }

  #opToken = null;

  opToken(it) {
    if (it === undefined) {
      return this.#opToken;
    }
    else {
      this.#opToken = it;
      return;
    }
  }

  #operands = null;

  operands(it) {
    if (it === undefined) {
      return this.#operands;
    }
    else {
      this.#operands = it;
      return;
    }
  }

  static make(first,opToken) {
    const $self = new CondExpr();
    CondExpr.make$($self,first,opToken);
    return $self;
  }

  static make$($self,first,opToken) {
    Expr.make$($self, first.loc(), opToken.toExprId());
    $self.#opToken = opToken;
    $self.#operands = sys.List.make(Expr.type$, [first]);
    return;
  }

  isCond() {
    return true;
  }

  walkChildren(v) {
    this.#operands = Expr.walkExprs(v, this.#operands);
    return;
  }

  toStr() {
    return this.#operands.join(sys.Str.plus(sys.Str.plus(" ", this.#opToken), " "));
  }

}

class NameExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return NameExpr.type$; }

  #target = null;

  target(it) {
    if (it === undefined) {
      return this.#target;
    }
    else {
      this.#target = it;
      return;
    }
  }

  #name = null;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #isSafe = false;

  isSafe(it) {
    if (it === undefined) {
      return this.#isSafe;
    }
    else {
      this.#isSafe = it;
      return;
    }
  }

  static make(loc,id,target,name) {
    const $self = new NameExpr();
    NameExpr.make$($self,loc,id,target,name);
    return $self;
  }

  static make$($self,loc,id,target,name) {
    Expr.make$($self, loc, id);
    $self.#target = target;
    $self.#name = name;
    $self.#isSafe = false;
    return;
  }

  isAlwaysNullable() {
    return this.#isSafe;
  }

  walkChildren(v) {
    this.#target = Expr.walkExpr(v, this.#target);
    return;
  }

  toStr() {
    if (this.#target != null) {
      return sys.Str.plus(sys.Str.plus(this.#target.toStr(), ((this$) => { if (this$.#isSafe) return "?."; return "."; })(this)), this.#name);
    }
    else {
      return sys.ObjUtil.coerce(this.#name, sys.Str.type$);
    }
    ;
  }

}

class UnknownVarExpr extends NameExpr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return UnknownVarExpr.type$; }

  static make(loc,target,name,id) {
    const $self = new UnknownVarExpr();
    UnknownVarExpr.make$($self,loc,target,name,id);
    return $self;
  }

  static make$($self,loc,target,name,id) {
    if (id === undefined) id = ExprId.unknownVar();
    NameExpr.make$($self, loc, id, target, name);
    return;
  }

}

class CallExpr extends NameExpr {
  constructor() {
    super();
    const this$ = this;
    this.#synthetic = false;
    return;
  }

  typeof() { return CallExpr.type$; }

  #args = null;

  args(it) {
    if (it === undefined) {
      return this.#args;
    }
    else {
      this.#args = it;
      return;
    }
  }

  #isDynamic = false;

  isDynamic(it) {
    if (it === undefined) {
      return this.#isDynamic;
    }
    else {
      this.#isDynamic = it;
      return;
    }
  }

  #isCtorChain = false;

  isCtorChain(it) {
    if (it === undefined) {
      return this.#isCtorChain;
    }
    else {
      this.#isCtorChain = it;
      return;
    }
  }

  #noParens = false;

  noParens(it) {
    if (it === undefined) {
      return this.#noParens;
    }
    else {
      this.#noParens = it;
      return;
    }
  }

  #isCallOp = false;

  isCallOp(it) {
    if (it === undefined) {
      return this.#isCallOp;
    }
    else {
      this.#isCallOp = it;
      return;
    }
  }

  #isItAdd = false;

  isItAdd(it) {
    if (it === undefined) {
      return this.#isItAdd;
    }
    else {
      this.#isItAdd = it;
      return;
    }
  }

  #method = null;

  method(it) {
    if (it === undefined) {
      return this.#method;
    }
    else {
      this.#method = it;
      return;
    }
  }

  #synthetic = false;

  synthetic(it) {
    if (it === undefined) {
      return this.#synthetic;
    }
    else {
      this.#synthetic = it;
      return;
    }
  }

  static make(loc,target,name,id) {
    const $self = new CallExpr();
    CallExpr.make$($self,loc,target,name,id);
    return $self;
  }

  static make$($self,loc,target,name,id) {
    if (target === undefined) target = null;
    if (name === undefined) name = null;
    if (id === undefined) id = ExprId.call();
    NameExpr.make$($self, loc, id, target, name);
    ;
    $self.#args = sys.List.make(Expr.type$);
    $self.#isDynamic = false;
    $self.isSafe(false);
    $self.#isCtorChain = false;
    return;
  }

  static makeWithMethod(loc,target,method,args) {
    const $self = new CallExpr();
    CallExpr.makeWithMethod$($self,loc,target,method,args);
    return $self;
  }

  static makeWithMethod$($self,loc,target,method,args) {
    if (args === undefined) args = null;
    CallExpr.make$($self, loc, target, method.name(), ExprId.call());
    $self.#method = method;
    $self.ctype(((this$) => { if (method.isCtor()) return method.parent(); return method.returns(); })($self));
    if (args != null) {
      $self.#args = sys.ObjUtil.coerce(args, sys.Type.find("compiler::Expr[]"));
    }
    ;
    return;
  }

  toStr() {
    return this.toCallStr(true);
  }

  isDefiniteAssign(f) {
    const this$ = this;
    if ((this.target() != null && this.target().isDefiniteAssign(f))) {
      return true;
    }
    ;
    return this.#args.any((arg) => {
      return arg.isDefiniteAssign(f);
    });
  }

  isStmt() {
    if (this.#method.isCtor()) {
      return false;
    }
    ;
    if ((sys.ObjUtil.equals(this.#method.name(), "with") && sys.ObjUtil.is(this.target(), CallExpr.type$) && sys.ObjUtil.coerce(this.target(), CallExpr.type$).#method.isCtor())) {
      return false;
    }
    ;
    return true;
  }

  asCall() {
    return this;
  }

  isCompare() {
    return false;
  }

  targetx() {
    if (this.target() != null) {
      return sys.ObjUtil.coerce(this.target(), Expr.type$);
    }
    ;
    if ((this.#method.isStatic() || this.#method.isCtor())) {
      return StaticTargetExpr.make(this.loc(), this.#method.parent());
    }
    ;
    throw sys.Err.make(sys.Str.plus("Call missing target: ", this));
  }

  walkChildren(v) {
    this.target(Expr.walkExpr(v, this.target()));
    this.#args = Expr.walkExprs(v, this.#args);
    return;
  }

  serialize() {
    const this$ = this;
    if ((sys.ObjUtil.compareNE(this.id(), ExprId.construction()) || sys.ObjUtil.compareNE(this.#method.name(), "fromStr"))) {
      return NameExpr.prototype.serialize.call(this);
    }
    ;
    let argSer = this.#args.join(",", (e) => {
      return e.serialize();
    });
    return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#method.parent()), "("), argSer), ")");
  }

  print(out) {
    out.w(this.toCallStr(false));
    if ((sys.ObjUtil.compareGT(this.#args.size(), 0) && sys.ObjUtil.is(this.#args.last(), ClosureExpr.type$))) {
      this.#args.last().print(out);
    }
    ;
    return;
  }

  toCallStr(isToStr) {
    let s = sys.StrBuf.make();
    if (this.target() != null) {
      s.add(this.target()).add(((this$) => { if (this$.isSafe()) return "?"; return ""; })(this)).add(((this$) => { if (this$.#isDynamic) return "->"; return "."; })(this));
    }
    else {
      if ((this.#method != null && (this.#method.isStatic() || this.#method.isCtor()))) {
        s.add(this.#method.parent().qname()).add(".");
      }
      ;
    }
    ;
    s.add(this.name()).add("(");
    if (sys.ObjUtil.is(this.#args.last(), ClosureExpr.type$)) {
      s.add(this.#args.getRange(sys.Range.make(0, -2)).join(", ")).add(") ");
      if (isToStr) {
        s.add(this.#args.last());
      }
      ;
    }
    else {
      s.add(this.#args.join(", ")).add(")");
    }
    ;
    return s.toStr();
  }

}

class ShortcutExpr extends CallExpr {
  constructor() {
    super();
    const this$ = this;
    this.#isPostfixLeave = false;
    return;
  }

  typeof() { return ShortcutExpr.type$; }

  #op = null;

  op(it) {
    if (it === undefined) {
      return this.#op;
    }
    else {
      this.#op = it;
      return;
    }
  }

  #opToken = null;

  opToken(it) {
    if (it === undefined) {
      return this.#opToken;
    }
    else {
      this.#opToken = it;
      return;
    }
  }

  #isPostfixLeave = false;

  isPostfixLeave(it) {
    if (it === undefined) {
      return this.#isPostfixLeave;
    }
    else {
      this.#isPostfixLeave = it;
      return;
    }
  }

  #tempVar = null;

  tempVar(it) {
    if (it === undefined) {
      return this.#tempVar;
    }
    else {
      this.#tempVar = it;
      return;
    }
  }

  static makeUnary(loc,opToken,operand) {
    const $self = new ShortcutExpr();
    ShortcutExpr.makeUnary$($self,loc,opToken,operand);
    return $self;
  }

  static makeUnary$($self,loc,opToken,operand) {
    CallExpr.make$($self, loc, null, null, ExprId.shortcut());
    ;
    $self.#op = opToken.toShortcutOp(1);
    $self.#opToken = opToken;
    $self.name($self.#op.methodName());
    $self.target(operand);
    return;
  }

  static makeBinary(lhs,opToken,rhs) {
    const $self = new ShortcutExpr();
    ShortcutExpr.makeBinary$($self,lhs,opToken,rhs);
    return $self;
  }

  static makeBinary$($self,lhs,opToken,rhs) {
    CallExpr.make$($self, lhs.loc(), null, null, ExprId.shortcut());
    ;
    $self.#op = opToken.toShortcutOp(2);
    $self.#opToken = opToken;
    $self.name($self.#op.methodName());
    $self.target(lhs);
    $self.args().add(rhs);
    return;
  }

  static makeGet(loc,target,index) {
    const $self = new ShortcutExpr();
    ShortcutExpr.makeGet$($self,loc,target,index);
    return $self;
  }

  static makeGet$($self,loc,target,index) {
    CallExpr.make$($self, loc, null, null, ExprId.shortcut());
    ;
    $self.#op = ShortcutOp.get();
    $self.#opToken = Token.lbracket();
    $self.name($self.#op.methodName());
    $self.target(target);
    $self.args().add(index);
    return;
  }

  static makeFrom(from$) {
    const $self = new ShortcutExpr();
    ShortcutExpr.makeFrom$($self,from$);
    return $self;
  }

  static makeFrom$($self,from$) {
    CallExpr.make$($self, from$.loc(), null, null, ExprId.shortcut());
    ;
    $self.#op = from$.#op;
    $self.#opToken = from$.#opToken;
    $self.name(from$.name());
    $self.target(from$.target());
    $self.args(from$.args());
    $self.#isPostfixLeave = from$.#isPostfixLeave;
    return;
  }

  assignRequiresTempVar() {
    return this.isAssignable();
  }

  assignTarget() {
    return ((this$) => { if (this$.isAssign()) return this$.target(); return null; })(this);
  }

  isAssignable() {
    return this.#op === ShortcutOp.get();
  }

  isCompare() {
    return (this.#op === ShortcutOp.eq() || this.#op === ShortcutOp.cmp());
  }

  isStmt() {
    return (this.isAssign() || this.#op === ShortcutOp.set());
  }

  isAssign() {
    return (this.#opToken.isAssign() || this.#opToken.isIncrementOrDecrement());
  }

  isStrConcat() {
    return (sys.ObjUtil.equals(this.#opToken, Token.plus()) && sys.ObjUtil.equals(this.args().size(), 1) && this.target().ctype().isStr());
  }

  toStr() {
    if (sys.ObjUtil.equals(this.#op, ShortcutOp.get())) {
      return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.target()), "["), this.args().first()), "]");
    }
    ;
    if (sys.ObjUtil.equals(this.#op, ShortcutOp.increment())) {
      return ((this$) => { if (this$.#isPostfixLeave) return sys.Str.plus(sys.Str.plus("", this$.target()), "++"); return sys.Str.plus("++", this$.target()); })(this);
    }
    ;
    if (sys.ObjUtil.equals(this.#op, ShortcutOp.decrement())) {
      return ((this$) => { if (this$.#isPostfixLeave) return sys.Str.plus(sys.Str.plus("", this$.target()), "--"); return sys.Str.plus("--", this$.target()); })(this);
    }
    ;
    if (this.isAssign()) {
      return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.target()), " "), this.#opToken), " "), this.args().first());
    }
    ;
    if (sys.ObjUtil.equals(this.#op.degree(), 1)) {
      return sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#opToken), ""), this.target());
    }
    ;
    if (sys.ObjUtil.equals(this.#op.degree(), 2)) {
      return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("(", this.target()), " "), this.#opToken), " "), this.args().first()), ")");
    }
    ;
    return CallExpr.prototype.toStr.call(this);
  }

  print(out) {
    out.w(this.toStr());
    return;
  }

}

class IndexedAssignExpr extends ShortcutExpr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return IndexedAssignExpr.type$; }

  #scratchA = null;

  scratchA(it) {
    if (it === undefined) {
      return this.#scratchA;
    }
    else {
      this.#scratchA = it;
      return;
    }
  }

  #scratchB = null;

  scratchB(it) {
    if (it === undefined) {
      return this.#scratchB;
    }
    else {
      this.#scratchB = it;
      return;
    }
  }

  #setMethod = null;

  setMethod(it) {
    if (it === undefined) {
      return this.#setMethod;
    }
    else {
      this.#setMethod = it;
      return;
    }
  }

  static makeFrom(from$) {
    const $self = new IndexedAssignExpr();
    IndexedAssignExpr.makeFrom$($self,from$);
    return $self;
  }

  static makeFrom$($self,from$) {
    ShortcutExpr.makeFrom$($self, from$);
    return;
  }

}

class FieldExpr extends NameExpr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FieldExpr.type$; }

  #field = null;

  field(it) {
    if (it === undefined) {
      return this.#field;
    }
    else {
      this.#field = it;
      return;
    }
  }

  #useAccessor = false;

  useAccessor(it) {
    if (it === undefined) {
      return this.#useAccessor;
    }
    else {
      this.#useAccessor = it;
      return;
    }
  }

  static make(loc,target,field,useAccessor) {
    const $self = new FieldExpr();
    FieldExpr.make$($self,loc,target,field,useAccessor);
    return $self;
  }

  static make$($self,loc,target,field,useAccessor) {
    if (target === undefined) target = null;
    if (field === undefined) field = null;
    if (useAccessor === undefined) useAccessor = true;
    NameExpr.make$($self, loc, ExprId.field(), target, null);
    $self.#useAccessor = useAccessor;
    $self.isSafe(false);
    if (field != null) {
      $self.name(field.name());
      $self.#field = field;
      $self.ctype(field.type());
    }
    ;
    return;
  }

  isAssignable() {
    return true;
  }

  assignRequiresTempVar() {
    return !this.#field.isStatic();
  }

  sameVarAs(that) {
    let x = sys.ObjUtil.as(that, FieldExpr.type$);
    if (x == null) {
      return false;
    }
    ;
    return (sys.ObjUtil.equals(this.#field, x.#field) && this.target() != null && x.target() != null && this.target().sameVarAs(sys.ObjUtil.coerce(x.target(), Expr.type$)));
  }

  asTableSwitchCase() {
    if ((this.#field.isStatic() && this.#field.parent().isEnum() && this.ctype().isEnum())) {
      let $_u44 = sys.ObjUtil.typeof(this.#field);
      if (sys.ObjUtil.equals($_u44, ReflectField.type$)) {
        let ifield = sys.ObjUtil.as(this.#field, ReflectField.type$);
        return sys.ObjUtil.coerce(sys.ObjUtil.coerce(ifield.f().get(), sys.Enum.type$).ordinal(), sys.Int.type$.toNullable());
      }
      else if (sys.ObjUtil.equals($_u44, FieldDef.type$)) {
        let fieldDef = sys.ObjUtil.as(this.#field, FieldDef.type$);
        let enumDef = fieldDef.parentDef().enumDef(this.#field.name());
        if (enumDef != null) {
          return sys.ObjUtil.coerce(enumDef.ordinal(), sys.Int.type$.toNullable());
        }
        ;
      }
      else if (sys.ObjUtil.equals($_u44, FField.type$)) {
        let ffield = sys.ObjUtil.as(this.#field, FField.type$);
        let attr = ffield.attr(FConst.EnumOrdinalAttr());
        if (attr != null) {
          return sys.ObjUtil.coerce(attr.u2(), sys.Int.type$.toNullable());
        }
        ;
      }
      else {
        throw sys.Err.make(sys.Str.plus(sys.Str.plus(sys.Str.plus("Invalid field for tableswitch: ", sys.ObjUtil.typeof(this.#field)), " "), this.loc().toLocStr()));
      }
      ;
    }
    ;
    return null;
  }

  serialize() {
    if (this.#field.isStatic()) {
      if (this.#field.parent().isFloat()) {
        let $_u45 = this.name();
        if (sys.ObjUtil.equals($_u45, "nan")) {
          return "sys::Float(\"NaN\")";
        }
        else if (sys.ObjUtil.equals($_u45, "posInf")) {
          return "sys::Float(\"INF\")";
        }
        else if (sys.ObjUtil.equals($_u45, "negInf")) {
          return "sys::Float(\"-INF\")";
        }
        ;
      }
      ;
      if (this.#field.isEnum()) {
        return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#field.parent().qname()), "(\""), this.name()), "\")");
      }
      ;
    }
    ;
    return NameExpr.prototype.serialize.call(this);
  }

  toStr() {
    let s = sys.StrBuf.make();
    if (this.target() != null) {
      s.add(this.target()).add(".");
    }
    ;
    if (this.isSafe()) {
      s.add("?");
    }
    ;
    if (!this.#useAccessor) {
      s.add("@");
    }
    ;
    s.add(this.name());
    return s.toStr();
  }

}

class LocalVarExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
    this.#unwrap = true;
    return;
  }

  typeof() { return LocalVarExpr.type$; }

  #var = null;

  var(it) {
    if (it === undefined) {
      return this.#var;
    }
    else {
      this.#var = it;
      return;
    }
  }

  #unwrap = false;

  unwrap(it) {
    if (it === undefined) {
      return this.#unwrap;
    }
    else {
      this.#unwrap = it;
      return;
    }
  }

  static make(loc,var$,id) {
    const $self = new LocalVarExpr();
    LocalVarExpr.make$($self,loc,var$,id);
    return $self;
  }

  static make$($self,loc,var$,id) {
    if (id === undefined) id = ExprId.localVar();
    Expr.make$($self, loc, id);
    ;
    if (var$ != null) {
      $self.#var = var$;
      $self.ctype(var$.ctype());
    }
    ;
    return;
  }

  static makeNoUnwrap(loc,var$) {
    let self$ = LocalVarExpr.make(loc, var$, ExprId.localVar());
    self$.#unwrap = false;
    return self$;
  }

  isAssignable() {
    return true;
  }

  assignRequiresTempVar() {
    return this.#var.usedInClosure();
  }

  sameVarAs(that) {
    let x = sys.ObjUtil.as(that, LocalVarExpr.type$);
    if (x == null) {
      return false;
    }
    ;
    if (sys.ObjUtil.compareNE(((this$) => { let $_u46=this$.#var; return ($_u46==null) ? null : $_u46.usedInClosure(); })(this), ((this$) => { let $_u47=((this$) => { let $_u48=x; return ($_u48==null) ? null : $_u48.#var; })(this$); return ($_u47==null) ? null : $_u47.usedInClosure(); })(this))) {
      return false;
    }
    ;
    return sys.ObjUtil.equals(this.register(), x.register());
  }

  register() {
    return this.#var.register();
  }

  name() {
    return this.#var.name();
  }

  toStr() {
    if (this.#var == null) {
      return "???";
    }
    ;
    return this.#var.name();
  }

}

class ThisExpr extends LocalVarExpr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ThisExpr.type$; }

  static make(loc,ctype) {
    const $self = new ThisExpr();
    ThisExpr.make$($self,loc,ctype);
    return $self;
  }

  static make$($self,loc,ctype) {
    if (ctype === undefined) ctype = null;
    LocalVarExpr.make$($self, loc, null, ExprId.thisExpr());
    $self.ctype(ctype);
    return;
  }

  isAssignable() {
    return false;
  }

  register() {
    return 0;
  }

  toStr() {
    return "this";
  }

}

class SuperExpr extends LocalVarExpr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return SuperExpr.type$; }

  #explicitType = null;

  explicitType(it) {
    if (it === undefined) {
      return this.#explicitType;
    }
    else {
      this.#explicitType = it;
      return;
    }
  }

  static make(loc,explicitType) {
    const $self = new SuperExpr();
    SuperExpr.make$($self,loc,explicitType);
    return $self;
  }

  static make$($self,loc,explicitType) {
    if (explicitType === undefined) explicitType = null;
    LocalVarExpr.make$($self, loc, null, ExprId.superExpr());
    $self.#explicitType = explicitType;
    return;
  }

  isAssignable() {
    return false;
  }

  register() {
    return 0;
  }

  toStr() {
    if (this.#explicitType != null) {
      return sys.Str.plus(sys.Str.plus("", this.#explicitType), ".super");
    }
    else {
      return "super";
    }
    ;
  }

}

class ItExpr extends LocalVarExpr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ItExpr.type$; }

  static make(loc,ctype) {
    const $self = new ItExpr();
    ItExpr.make$($self,loc,ctype);
    return $self;
  }

  static make$($self,loc,ctype) {
    if (ctype === undefined) ctype = null;
    LocalVarExpr.make$($self, loc, null, ExprId.itExpr());
    $self.ctype(ctype);
    return;
  }

  isAssignable() {
    return false;
  }

  register() {
    return 1;
  }

  name() {
    return "it";
  }

  toStr() {
    return "it";
  }

}

class StaticTargetExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return StaticTargetExpr.type$; }

  static make(loc,ctype) {
    const $self = new StaticTargetExpr();
    StaticTargetExpr.make$($self,loc,ctype);
    return $self;
  }

  static make$($self,loc,ctype) {
    Expr.make$($self, loc, ExprId.staticTarget());
    $self.ctype(ctype);
    return;
  }

  sameVarAs(that) {
    return (that.id() === ExprId.staticTarget() && sys.ObjUtil.equals(this.ctype(), that.ctype()));
  }

  toStr() {
    return this.ctype().signature();
  }

}

class TypeCheckExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
    this.#synthetic = false;
    return;
  }

  typeof() { return TypeCheckExpr.type$; }

  #from = null;

  from(it) {
    if (it === undefined) {
      return ((this$) => { let $_u49 = this$.#from; if ($_u49 != null) return $_u49; return this$.#target.ctype(); })(this);
    }
    else {
      this.#from = it;
      return;
    }
  }

  #target = null;

  target(it) {
    if (it === undefined) {
      return this.#target;
    }
    else {
      this.#target = it;
      return;
    }
  }

  #check = null;

  check(it) {
    if (it === undefined) {
      return this.#check;
    }
    else {
      this.#check = it;
      return;
    }
  }

  #synthetic = false;

  synthetic(it) {
    if (it === undefined) {
      return this.#synthetic;
    }
    else {
      this.#synthetic = it;
      return;
    }
  }

  static make(loc,id,target,check) {
    const $self = new TypeCheckExpr();
    TypeCheckExpr.make$($self,loc,id,target,check);
    return $self;
  }

  static make$($self,loc,id,target,check) {
    Expr.make$($self, loc, id);
    ;
    $self.#target = target;
    $self.#check = check;
    $self.ctype(check);
    return;
  }

  static coerce(target,to) {
    const $self = new TypeCheckExpr();
    TypeCheckExpr.coerce$($self,target,to);
    return $self;
  }

  static coerce$($self,target,to) {
    Expr.make$($self, target.loc(), ExprId.coerce());
    ;
    if (to.isGenericParameter()) {
      (to = to.raw());
    }
    ;
    $self.#target = target;
    $self.from(target.ctype());
    $self.#check = to;
    $self.ctype(to);
    $self.synthetic(true);
    return;
  }

  walkChildren(v) {
    this.#target = sys.ObjUtil.coerce(Expr.walkExpr(v, this.#target), Expr.type$);
    return;
  }

  isStmt() {
    return (this.id() === ExprId.coerce() && this.#target.isStmt());
  }

  isAlwaysNullable() {
    return this.id() === ExprId.asExpr();
  }

  isDefiniteAssign(f) {
    return this.#target.isDefiniteAssign(f);
  }

  serialize() {
    if (sys.ObjUtil.equals(this.id(), ExprId.coerce())) {
      return this.#target.serialize();
    }
    else {
      return Expr.prototype.serialize.call(this);
    }
    ;
  }

  opStr() {
    let $_u50 = this.id();
    if (sys.ObjUtil.equals($_u50, ExprId.isExpr())) {
      return "is";
    }
    else if (sys.ObjUtil.equals($_u50, ExprId.isnotExpr())) {
      return "isnot";
    }
    else if (sys.ObjUtil.equals($_u50, ExprId.asExpr())) {
      return "as";
    }
    else {
      throw sys.Err.make(this.id().toStr());
    }
    ;
  }

  toStr() {
    let $_u51 = this.id();
    if (sys.ObjUtil.equals($_u51, ExprId.isExpr())) {
      return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("(", this.#target), " is "), this.#check), ")");
    }
    else if (sys.ObjUtil.equals($_u51, ExprId.isnotExpr())) {
      return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("(", this.#target), " isnot "), this.#check), ")");
    }
    else if (sys.ObjUtil.equals($_u51, ExprId.asExpr())) {
      return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("(", this.#target), " as "), this.#check), ")");
    }
    else if (sys.ObjUtil.equals($_u51, ExprId.coerce())) {
      return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("((", this.#check), ")"), this.#target), ")");
    }
    else {
      throw sys.Err.make(this.id().toStr());
    }
    ;
  }

}

class TernaryExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return TernaryExpr.type$; }

  #condition = null;

  condition(it) {
    if (it === undefined) {
      return this.#condition;
    }
    else {
      this.#condition = it;
      return;
    }
  }

  #trueExpr = null;

  trueExpr(it) {
    if (it === undefined) {
      return this.#trueExpr;
    }
    else {
      this.#trueExpr = it;
      return;
    }
  }

  #falseExpr = null;

  falseExpr(it) {
    if (it === undefined) {
      return this.#falseExpr;
    }
    else {
      this.#falseExpr = it;
      return;
    }
  }

  static make(condition,trueExpr,falseExpr) {
    const $self = new TernaryExpr();
    TernaryExpr.make$($self,condition,trueExpr,falseExpr);
    return $self;
  }

  static make$($self,condition,trueExpr,falseExpr) {
    Expr.make$($self, condition.loc(), ExprId.ternary());
    $self.#condition = condition;
    $self.#trueExpr = trueExpr;
    $self.#falseExpr = falseExpr;
    return;
  }

  walkChildren(v) {
    this.#condition = this.#condition.walk(v);
    this.#trueExpr = this.#trueExpr.walk(v);
    this.#falseExpr = this.#falseExpr.walk(v);
    return;
  }

  toStr() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#condition), " ? "), this.#trueExpr), " : "), this.#falseExpr);
  }

}

class ComplexLiteral extends Expr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ComplexLiteral.type$; }

  #names = null;

  names(it) {
    if (it === undefined) {
      return this.#names;
    }
    else {
      this.#names = it;
      return;
    }
  }

  #vals = null;

  vals(it) {
    if (it === undefined) {
      return this.#vals;
    }
    else {
      this.#vals = it;
      return;
    }
  }

  static make(loc,ctype) {
    const $self = new ComplexLiteral();
    ComplexLiteral.make$($self,loc,ctype);
    return $self;
  }

  static make$($self,loc,ctype) {
    Expr.make$($self, loc, ExprId.complexLiteral());
    $self.ctype(ctype);
    $self.#names = sys.List.make(sys.Str.type$);
    $self.#vals = sys.List.make(Expr.type$);
    return;
  }

  walkChildren(v) {
    this.#vals = Expr.walkExprs(v, this.#vals);
    return;
  }

  toStr() {
    const this$ = this;
    return this.doToStr((expr) => {
      return expr.toStr();
    });
  }

  serialize() {
    const this$ = this;
    return this.doToStr((expr) => {
      return expr.serialize();
    });
  }

  doToStr(f) {
    const this$ = this;
    let s = sys.StrBuf.make();
    s.add(sys.Str.plus(sys.Str.plus("", this.ctype()), " {"));
    this.#names.each((n,i) => {
      s.add(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", n), " = "), sys.Func.call(f, this$.#vals.get(i))), ";"));
      return;
    });
    s.add("}");
    return s.toStr();
  }

}

class ClosureExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
    return;
  }

  typeof() { return ClosureExpr.type$; }

  #enclosingType = null;

  enclosingType(it) {
    if (it === undefined) {
      return this.#enclosingType;
    }
    else {
      this.#enclosingType = it;
      return;
    }
  }

  #enclosingSlot = null;

  enclosingSlot(it) {
    if (it === undefined) {
      return this.#enclosingSlot;
    }
    else {
      this.#enclosingSlot = it;
      return;
    }
  }

  #enclosingClosure = null;

  enclosingClosure(it) {
    if (it === undefined) {
      return this.#enclosingClosure;
    }
    else {
      this.#enclosingClosure = it;
      return;
    }
  }

  #signature = null;

  signature(it) {
    if (it === undefined) {
      return this.#signature;
    }
    else {
      this.#signature = it;
      return;
    }
  }

  #code = null;

  code(it) {
    if (it === undefined) {
      return this.#code;
    }
    else {
      this.#code = it;
      return;
    }
  }

  #name = null;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #isItBlock = false;

  isItBlock(it) {
    if (it === undefined) {
      return this.#isItBlock;
    }
    else {
      this.#isItBlock = it;
      return;
    }
  }

  #substitute = null;

  substitute(it) {
    if (it === undefined) {
      return this.#substitute;
    }
    else {
      this.#substitute = it;
      return;
    }
  }

  #cls = null;

  cls(it) {
    if (it === undefined) {
      return this.#cls;
    }
    else {
      this.#cls = it;
      return;
    }
  }

  #call = null;

  call(it) {
    if (it === undefined) {
      return this.#call;
    }
    else {
      this.#call = it;
      return;
    }
  }

  #doCall = null;

  doCall(it) {
    if (it === undefined) {
      return this.#doCall;
    }
    else {
      this.#doCall = it;
      return;
    }
  }

  #enclosingVars = null;

  enclosingVars(it) {
    if (it === undefined) {
      return this.#enclosingVars;
    }
    else {
      this.#enclosingVars = it;
      return;
    }
  }

  #setsConst = false;

  setsConst(it) {
    if (it === undefined) {
      return this.#setsConst;
    }
    else {
      this.#setsConst = it;
      return;
    }
  }

  #itType = null;

  itType(it) {
    if (it === undefined) {
      return this.#itType;
    }
    else {
      this.#itType = it;
      return;
    }
  }

  #outerThisField$Store = undefined;

  // private field reflection only
  __outerThisField$Store(it) { if (it === undefined) return this.#outerThisField$Store; else this.#outerThisField$Store = it; }

  static make(loc,enclosingType,enclosingSlot,enclosingClosure,signature,name) {
    const $self = new ClosureExpr();
    ClosureExpr.make$($self,loc,enclosingType,enclosingSlot,enclosingClosure,signature,name);
    return $self;
  }

  static make$($self,loc,enclosingType,enclosingSlot,enclosingClosure,signature,name) {
    Expr.make$($self, loc, ExprId.closure());
    ;
    $self.ctype(signature);
    $self.#enclosingType = enclosingType;
    $self.#enclosingSlot = enclosingSlot;
    $self.#enclosingClosure = enclosingClosure;
    $self.#signature = signature;
    $self.#name = name;
    return;
  }

  outerThisField() {
    if (this.#outerThisField$Store === undefined) {
      this.#outerThisField$Store = this.outerThisField$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#outerThisField$Store, CField.type$);
  }

  toStr() {
    return sys.Str.plus(sys.Str.plus("", this.#signature), " { ... }");
  }

  print(out) {
    out.w(this.#signature.toStr());
    if (this.#substitute != null) {
      out.w(" { substitute: ");
      this.#substitute.print(out);
      out.w(" }").nl();
    }
    else {
      out.nl();
      this.#code.print(out);
    }
    ;
    return;
  }

  isDefiniteAssign(f) {
    if (this.#doCall == null) {
      return false;
    }
    ;
    return this.#doCall.code().isDefiniteAssign(f);
  }

  toWith(target) {
    if (target.ctype() != null) {
      this.setInferredSignature(FuncType.makeItBlock(sys.ObjUtil.coerce(target.ctype(), CType.type$)));
    }
    ;
    let x = CallExpr.makeWithMethod(this.loc(), target, sys.ObjUtil.coerce(this.#enclosingType.ns().objWith(), CMethod.type$), sys.List.make(Expr.type$, [this]));
    if (target.ctype() == null) {
      return x;
    }
    ;
    return TypeCheckExpr.coerce(x, sys.ObjUtil.coerce(target.ctype(), CType.type$));
  }

  setInferredSignature(t) {
    const this$ = this;
    if ((!this.#signature.inferredSignature() || this.#cls == null)) {
      return;
    }
    ;
    (t = t.toArity(sys.ObjUtil.coerce(this.#cls.base(), FuncType.type$).arity()));
    (t = this.#signature.mostSpecific(t));
    if (t.usesThis()) {
      throw sys.Err.make(sys.Str.plus("Inferring signature with un-parameterized this type: ", t));
    }
    ;
    this.#signature = t;
    this.ctype(t);
    if (this.#doCall != null) {
      this.#doCall.paramDefs().each((p,i) => {
        if (sys.ObjUtil.compareLT(i, this$.#signature.params().size())) {
          p.type(this$.#signature.params().get(i));
        }
        ;
        return;
      });
      if ((this.#doCall.returns().isVoid() && !t.returns().isVoid())) {
        this.#doCall.returns(t.returns());
        this.collapseExprAndReturn(sys.ObjUtil.coerce(this.#doCall, MethodDef.type$));
        this.collapseExprAndReturn(sys.ObjUtil.coerce(this.#call, MethodDef.type$));
      }
      ;
    }
    ;
    if (this.#isItBlock) {
      this.#itType = t.params().first();
    }
    ;
    this.#cls.base(t);
    return;
  }

  collapseExprAndReturn(m) {
    let code = m.code().stmts();
    if (sys.ObjUtil.compareNE(code.size(), 2)) {
      return;
    }
    ;
    if (code.get(0).id() !== StmtId.expr()) {
      return;
    }
    ;
    if (code.get(1).id() !== StmtId.returnStmt()) {
      return;
    }
    ;
    if (!sys.ObjUtil.coerce(code.last(), ReturnStmt.type$).isSynthetic()) {
      return;
    }
    ;
    let expr = sys.ObjUtil.coerce(code.first(), ExprStmt.type$).expr();
    code.set(0, ReturnStmt.makeSynthetic(expr.loc(), expr));
    code.removeAt(1);
    return;
  }

  outerThisField$Once() {
    if (this.#enclosingSlot.isStatic()) {
      throw sys.Err.make(sys.Str.plus("Internal error: ", this.loc().toLocStr()));
    }
    ;
    return ClosureVars.makeOuterThisField(this);
  }

}

class DslExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return DslExpr.type$; }

  #anchorType = null;

  anchorType(it) {
    if (it === undefined) {
      return this.#anchorType;
    }
    else {
      this.#anchorType = it;
      return;
    }
  }

  #src = null;

  src(it) {
    if (it === undefined) {
      return this.#src;
    }
    else {
      this.#src = it;
      return;
    }
  }

  #srcLoc = null;

  srcLoc(it) {
    if (it === undefined) {
      return this.#srcLoc;
    }
    else {
      this.#srcLoc = it;
      return;
    }
  }

  #leadingTabs = 0;

  leadingTabs(it) {
    if (it === undefined) {
      return this.#leadingTabs;
    }
    else {
      this.#leadingTabs = it;
      return;
    }
  }

  #leadingSpaces = 0;

  leadingSpaces(it) {
    if (it === undefined) {
      return this.#leadingSpaces;
    }
    else {
      this.#leadingSpaces = it;
      return;
    }
  }

  static make(loc,anchorType,srcLoc,src) {
    const $self = new DslExpr();
    DslExpr.make$($self,loc,anchorType,srcLoc,src);
    return $self;
  }

  static make$($self,loc,anchorType,srcLoc,src) {
    Expr.make$($self, loc, ExprId.dsl());
    $self.#anchorType = anchorType;
    $self.#src = src;
    $self.#srcLoc = srcLoc;
    return;
  }

  toStr() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#anchorType), " <|"), this.#src), "|>");
  }

  print(out) {
    out.w(this.toStr());
    return;
  }

}

class ThrowExpr extends Expr {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ThrowExpr.type$; }

  #exception = null;

  exception(it) {
    if (it === undefined) {
      return this.#exception;
    }
    else {
      this.#exception = it;
      return;
    }
  }

  static make(loc,exception) {
    const $self = new ThrowExpr();
    ThrowExpr.make$($self,loc,exception);
    return $self;
  }

  static make$($self,loc,exception) {
    Expr.make$($self, loc, ExprId.throwExpr());
    $self.#exception = exception;
    return;
  }

  walkChildren(v) {
    this.#exception = this.#exception.walk(v);
    return;
  }

  toStr() {
    return sys.Str.plus("throw ", this.#exception);
  }

}

class ExprId extends sys.Enum {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ExprId.type$; }

  static nullLiteral() { return ExprId.vals().get(0); }

  static trueLiteral() { return ExprId.vals().get(1); }

  static falseLiteral() { return ExprId.vals().get(2); }

  static intLiteral() { return ExprId.vals().get(3); }

  static floatLiteral() { return ExprId.vals().get(4); }

  static decimalLiteral() { return ExprId.vals().get(5); }

  static strLiteral() { return ExprId.vals().get(6); }

  static durationLiteral() { return ExprId.vals().get(7); }

  static uriLiteral() { return ExprId.vals().get(8); }

  static typeLiteral() { return ExprId.vals().get(9); }

  static localeLiteral() { return ExprId.vals().get(10); }

  static slotLiteral() { return ExprId.vals().get(11); }

  static rangeLiteral() { return ExprId.vals().get(12); }

  static listLiteral() { return ExprId.vals().get(13); }

  static mapLiteral() { return ExprId.vals().get(14); }

  static boolNot() { return ExprId.vals().get(15); }

  static cmpNull() { return ExprId.vals().get(16); }

  static cmpNotNull() { return ExprId.vals().get(17); }

  static elvis() { return ExprId.vals().get(18); }

  static assign() { return ExprId.vals().get(19); }

  static same() { return ExprId.vals().get(20); }

  static notSame() { return ExprId.vals().get(21); }

  static boolOr() { return ExprId.vals().get(22); }

  static boolAnd() { return ExprId.vals().get(23); }

  static isExpr() { return ExprId.vals().get(24); }

  static isnotExpr() { return ExprId.vals().get(25); }

  static asExpr() { return ExprId.vals().get(26); }

  static coerce() { return ExprId.vals().get(27); }

  static call() { return ExprId.vals().get(28); }

  static construction() { return ExprId.vals().get(29); }

  static shortcut() { return ExprId.vals().get(30); }

  static field() { return ExprId.vals().get(31); }

  static localVar() { return ExprId.vals().get(32); }

  static thisExpr() { return ExprId.vals().get(33); }

  static superExpr() { return ExprId.vals().get(34); }

  static itExpr() { return ExprId.vals().get(35); }

  static staticTarget() { return ExprId.vals().get(36); }

  static unknownVar() { return ExprId.vals().get(37); }

  static storage() { return ExprId.vals().get(38); }

  static ternary() { return ExprId.vals().get(39); }

  static complexLiteral() { return ExprId.vals().get(40); }

  static closure() { return ExprId.vals().get(41); }

  static dsl() { return ExprId.vals().get(42); }

  static throwExpr() { return ExprId.vals().get(43); }

  static #vals = undefined;

  static make($ordinal,$name) {
    const $self = new ExprId();
    ExprId.make$($self,$ordinal,$name);
    return $self;
  }

  static make$($self,$ordinal,$name) {
    sys.Enum.make$($self, $ordinal, $name);
    return;
  }

  static fromStr(name$, checked=true) {
    return sys.Enum.doFromStr(ExprId.type$, ExprId.vals(), name$, checked);
  }

  static vals() {
    if (ExprId.#vals == null) {
      ExprId.#vals = sys.List.make(ExprId.type$, [
        ExprId.make(0, "nullLiteral", ),
        ExprId.make(1, "trueLiteral", ),
        ExprId.make(2, "falseLiteral", ),
        ExprId.make(3, "intLiteral", ),
        ExprId.make(4, "floatLiteral", ),
        ExprId.make(5, "decimalLiteral", ),
        ExprId.make(6, "strLiteral", ),
        ExprId.make(7, "durationLiteral", ),
        ExprId.make(8, "uriLiteral", ),
        ExprId.make(9, "typeLiteral", ),
        ExprId.make(10, "localeLiteral", ),
        ExprId.make(11, "slotLiteral", ),
        ExprId.make(12, "rangeLiteral", ),
        ExprId.make(13, "listLiteral", ),
        ExprId.make(14, "mapLiteral", ),
        ExprId.make(15, "boolNot", ),
        ExprId.make(16, "cmpNull", ),
        ExprId.make(17, "cmpNotNull", ),
        ExprId.make(18, "elvis", ),
        ExprId.make(19, "assign", ),
        ExprId.make(20, "same", ),
        ExprId.make(21, "notSame", ),
        ExprId.make(22, "boolOr", ),
        ExprId.make(23, "boolAnd", ),
        ExprId.make(24, "isExpr", ),
        ExprId.make(25, "isnotExpr", ),
        ExprId.make(26, "asExpr", ),
        ExprId.make(27, "coerce", ),
        ExprId.make(28, "call", ),
        ExprId.make(29, "construction", ),
        ExprId.make(30, "shortcut", ),
        ExprId.make(31, "field", ),
        ExprId.make(32, "localVar", ),
        ExprId.make(33, "thisExpr", ),
        ExprId.make(34, "superExpr", ),
        ExprId.make(35, "itExpr", ),
        ExprId.make(36, "staticTarget", ),
        ExprId.make(37, "unknownVar", ),
        ExprId.make(38, "storage", ),
        ExprId.make(39, "ternary", ),
        ExprId.make(40, "complexLiteral", ),
        ExprId.make(41, "closure", ),
        ExprId.make(42, "dsl", ),
        ExprId.make(43, "throwExpr", ),
      ]).toImmutable();
    }
    return ExprId.#vals;
  }

  static static$init() {
    const $_u52 = ExprId.vals();
    if (true) {
    }
    ;
    return;
  }

}

class ShortcutOp extends sys.Enum {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ShortcutOp.type$; }

  static plus() { return ShortcutOp.vals().get(0); }

  static minus() { return ShortcutOp.vals().get(1); }

  static mult() { return ShortcutOp.vals().get(2); }

  static div() { return ShortcutOp.vals().get(3); }

  static mod() { return ShortcutOp.vals().get(4); }

  static negate() { return ShortcutOp.vals().get(5); }

  static increment() { return ShortcutOp.vals().get(6); }

  static decrement() { return ShortcutOp.vals().get(7); }

  static eq() { return ShortcutOp.vals().get(8); }

  static cmp() { return ShortcutOp.vals().get(9); }

  static get() { return ShortcutOp.vals().get(10); }

  static set() { return ShortcutOp.vals().get(11); }

  static add() { return ShortcutOp.vals().get(12); }

  static #vals = undefined;

  static #prefixes = undefined;

  static prefixes() {
    if (ShortcutOp.#prefixes === undefined) {
      ShortcutOp.static$init();
      if (ShortcutOp.#prefixes === undefined) ShortcutOp.#prefixes = null;
    }
    return ShortcutOp.#prefixes;
  }

  #degree = 0;

  degree() { return this.#degree; }

  __degree(it) { if (it === undefined) return this.#degree; else this.#degree = it; }

  #methodName = null;

  methodName() { return this.#methodName; }

  __methodName(it) { if (it === undefined) return this.#methodName; else this.#methodName = it; }

  #isOperator = false;

  isOperator() { return this.#isOperator; }

  __isOperator(it) { if (it === undefined) return this.#isOperator; else this.#isOperator = it; }

  #symbol = null;

  symbol() { return this.#symbol; }

  __symbol(it) { if (it === undefined) return this.#symbol; else this.#symbol = it; }

  static make($ordinal,$name,degree,symbol,methodName) {
    const $self = new ShortcutOp();
    ShortcutOp.make$($self,$ordinal,$name,degree,symbol,methodName);
    return $self;
  }

  static make$($self,$ordinal,$name,degree,symbol,methodName) {
    if (methodName === undefined) methodName = null;
    sys.Enum.make$($self, $ordinal, $name);
    $self.#degree = degree;
    $self.#symbol = symbol;
    $self.#methodName = sys.ObjUtil.coerce(((this$) => { if (methodName == null) return this$.name(); return methodName; })($self), sys.Str.type$);
    $self.#isOperator = methodName == null;
    return;
  }

  static fromPrefix(prefix) {
    return ShortcutOp.prefixes().get(prefix);
  }

  formatErr(lhs,rhs) {
    if (this === ShortcutOp.get()) {
      return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", lhs), " [ "), rhs), " ]");
    }
    ;
    if (this === ShortcutOp.set()) {
      return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", lhs), " [ "), rhs), " ]=");
    }
    ;
    return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", lhs), " "), this.#symbol), " "), rhs);
  }

  static fromStr(name$, checked=true) {
    return sys.Enum.doFromStr(ShortcutOp.type$, ShortcutOp.vals(), name$, checked);
  }

  static vals() {
    if (ShortcutOp.#vals == null) {
      ShortcutOp.#vals = sys.List.make(ShortcutOp.type$, [
        ShortcutOp.make(0, "plus", 2, "+"),
        ShortcutOp.make(1, "minus", 2, "-"),
        ShortcutOp.make(2, "mult", 2, "*"),
        ShortcutOp.make(3, "div", 2, "/"),
        ShortcutOp.make(4, "mod", 2, "%"),
        ShortcutOp.make(5, "negate", 1, "-"),
        ShortcutOp.make(6, "increment", 1, "++"),
        ShortcutOp.make(7, "decrement", 1, "--"),
        ShortcutOp.make(8, "eq", 2, "==", "equals"),
        ShortcutOp.make(9, "cmp", 2, "<=>", "compare"),
        ShortcutOp.make(10, "get", 2, "[]"),
        ShortcutOp.make(11, "set", 3, "[]="),
        ShortcutOp.make(12, "add", 2, ","),
      ]).toImmutable();
    }
    return ShortcutOp.#vals;
  }

  static static$init() {
    const $_u54 = ShortcutOp.vals();
    if (true) {
    }
    ;
    if (true) {
      let m = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::ShortcutOp"));
      ShortcutOp.#vals.each((val) => {
        m.set(val.#methodName, val);
        return;
      });
      ShortcutOp.#prefixes = sys.ObjUtil.coerce(sys.ObjUtil.toImmutable(m), sys.Type.find("[sys::Str:compiler::ShortcutOp]"));
    }
    ;
    return;
  }

}

class CompilationUnit extends Node {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return CompilationUnit.type$; }

  #pod = null;

  pod(it) {
    if (it === undefined) {
      return this.#pod;
    }
    else {
      this.#pod = it;
      return;
    }
  }

  #tokens = null;

  tokens(it) {
    if (it === undefined) {
      return this.#tokens;
    }
    else {
      this.#tokens = it;
      return;
    }
  }

  #usings = null;

  usings(it) {
    if (it === undefined) {
      return this.#usings;
    }
    else {
      this.#usings = it;
      return;
    }
  }

  #types = null;

  types(it) {
    if (it === undefined) {
      return this.#types;
    }
    else {
      this.#types = it;
      return;
    }
  }

  #importedTypes = null;

  importedTypes(it) {
    if (it === undefined) {
      return this.#importedTypes;
    }
    else {
      this.#importedTypes = it;
      return;
    }
  }

  static make(loc,pod) {
    const $self = new CompilationUnit();
    CompilationUnit.make$($self,loc,pod);
    return $self;
  }

  static make$($self,loc,pod) {
    Node.make$($self, loc);
    $self.#pod = pod;
    $self.#usings = sys.List.make(Using.type$);
    $self.#types = sys.List.make(TypeDef.type$);
    return;
  }

  print(out) {
    const this$ = this;
    out.nl();
    this.#usings.each((u) => {
      u.print(out);
      return;
    });
    this.#types.each((t) => {
      t.print(out);
      return;
    });
    return;
  }

  toStr() {
    return this.loc().toStr();
  }

}

class CFacet {
  constructor() {
    const this$ = this;
  }

  typeof() { return CFacet.type$; }

}

class FacetDef extends Node {
  constructor() {
    super();
    const this$ = this;
    this.#names = sys.List.make(sys.Str.type$);
    this.#vals = sys.List.make(Expr.type$);
    return;
  }

  typeof() { return FacetDef.type$; }

  #type = null;

  type(it) {
    if (it === undefined) {
      return this.#type;
    }
    else {
      this.#type = it;
      return;
    }
  }

  #names = null;

  names(it) {
    if (it === undefined) {
      return this.#names;
    }
    else {
      this.#names = it;
      return;
    }
  }

  #vals = null;

  vals(it) {
    if (it === undefined) {
      return this.#vals;
    }
    else {
      this.#vals = it;
      return;
    }
  }

  static make(loc,type) {
    const $self = new FacetDef();
    FacetDef.make$($self,loc,type);
    return $self;
  }

  static make$($self,loc,type) {
    Node.make$($self, loc);
    ;
    $self.#type = type;
    return;
  }

  qname() {
    return this.#type.qname();
  }

  get(name) {
    let i = this.#names.index(name);
    if (i == null) {
      return null;
    }
    ;
    let literal = sys.ObjUtil.as(this.#vals.get(sys.ObjUtil.coerce(i, sys.Int.type$)), LiteralExpr.type$);
    if (literal == null) {
      return null;
    }
    ;
    return literal.val();
  }

  serialize() {
    const this$ = this;
    if (this.#names.isEmpty()) {
      return "";
    }
    ;
    let s = sys.StrBuf.make();
    if (this.#type.isForeign()) {
      s.addChar(91);
      this.#names.each((n,i) => {
        s.add(sys.Str.toCode(n)).addChar(58).add(this$.#vals.get(i).serialize()).addChar(44);
        return;
      });
      s.addChar(93);
    }
    else {
      s.add(this.#type.qname()).addChar(123);
      this.#names.each((n,i) => {
        s.add(n).addChar(61).add(this$.#vals.get(i).serialize()).addChar(59);
        return;
      });
      s.addChar(125);
    }
    ;
    return s.toStr();
  }

  walk(v) {
    this.#vals = Expr.walkExprs(v, this.#vals);
    return;
  }

  toStr() {
    return ((this$) => { if (this$.#names.isEmpty()) return sys.Str.plus("@", this$.#type); return sys.Str.plus("@", this$.serialize()); })(this);
  }

  print(out) {
    out.w(this.toStr()).nl();
    return;
  }

}

class Stmt extends Node {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Stmt.type$; }

  #id = null;

  id() { return this.#id; }

  __id(it) { if (it === undefined) return this.#id; else this.#id = it; }

  static make(loc,id) {
    const $self = new Stmt();
    Stmt.make$($self,loc,id);
    return $self;
  }

  static make$($self,loc,id) {
    Node.make$($self, loc);
    $self.#id = id;
    return;
  }

  isOnceFieldInit() {
    return false;
  }

  walk(v,depth) {
    v.enterStmt(this);
    this.walkChildren(v, depth);
    let r = v.visitStmt(this);
    v.exitStmt(this);
    return r;
  }

  walkChildren(v,depth) {
    return;
  }

  static walkExpr(v,depth,expr) {
    if ((depth === VisitDepth.expr() && expr != null)) {
      return expr.walk(v);
    }
    else {
      return expr;
    }
    ;
  }

}

class NopStmt extends Stmt {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return NopStmt.type$; }

  static make(loc) {
    const $self = new NopStmt();
    NopStmt.make$($self,loc);
    return $self;
  }

  static make$($self,loc) {
    Stmt.make$($self, loc, StmtId.nop());
    return;
  }

  isExit() {
    return false;
  }

  isDefiniteAssign(f) {
    return false;
  }

  print(out) {
    out.w("nop").nl();
    return;
  }

}

class ExprStmt extends Stmt {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ExprStmt.type$; }

  #expr = null;

  expr(it) {
    if (it === undefined) {
      return this.#expr;
    }
    else {
      this.#expr = it;
      return;
    }
  }

  static make(expr) {
    const $self = new ExprStmt();
    ExprStmt.make$($self,expr);
    return $self;
  }

  static make$($self,expr) {
    Stmt.make$($self, expr.loc(), StmtId.expr());
    $self.#expr = expr;
    return;
  }

  isExit() {
    return false;
  }

  isDefiniteAssign(f) {
    return this.#expr.isDefiniteAssign(f);
  }

  isOnceFieldInit() {
    if (this.#expr.id() !== ExprId.assign()) {
      return false;
    }
    ;
    let x = sys.ObjUtil.as(sys.ObjUtil.coerce(this.#expr, BinaryExpr.type$).lhs(), FieldExpr.type$);
    if (x == null) {
      return false;
    }
    ;
    return x.field().isOnce();
  }

  walkChildren(v,depth) {
    this.#expr = sys.ObjUtil.coerce(Stmt.walkExpr(v, depth, this.#expr), Expr.type$);
    return;
  }

  toStr() {
    return this.#expr.toStr();
  }

  print(out) {
    this.printOpt(out);
    return;
  }

  printOpt(out,nl) {
    if (nl === undefined) nl = true;
    this.#expr.print(out);
    if (nl) {
      out.nl();
    }
    ;
    return;
  }

}

class LocalDefStmt extends Stmt {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return LocalDefStmt.type$; }

  #ctype = null;

  ctype(it) {
    if (it === undefined) {
      return this.#ctype;
    }
    else {
      this.#ctype = it;
      return;
    }
  }

  #name = null;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #init = null;

  init(it) {
    if (it === undefined) {
      return this.#init;
    }
    else {
      this.#init = it;
      return;
    }
  }

  #isCatchVar = false;

  isCatchVar(it) {
    if (it === undefined) {
      return this.#isCatchVar;
    }
    else {
      this.#isCatchVar = it;
      return;
    }
  }

  #var = null;

  var(it) {
    if (it === undefined) {
      return this.#var;
    }
    else {
      this.#var = it;
      return;
    }
  }

  static make(loc,ctype,name) {
    const $self = new LocalDefStmt();
    LocalDefStmt.make$($self,loc,ctype,name);
    return $self;
  }

  static make$($self,loc,ctype,name) {
    if (ctype === undefined) ctype = null;
    if (name === undefined) name = "";
    Stmt.make$($self, loc, StmtId.localDef());
    $self.#ctype = ctype;
    $self.#name = name;
    return;
  }

  static makeAssign(init) {
    const $self = new LocalDefStmt();
    LocalDefStmt.makeAssign$($self,init);
    return $self;
  }

  static makeAssign$($self,init) {
    Stmt.make$($self, init.loc(), StmtId.localDef());
    let var$ = sys.ObjUtil.coerce(init.lhs(), LocalVarExpr.type$);
    $self.#ctype = var$.ctype();
    $self.#name = var$.name();
    $self.#init = init;
    return;
  }

  isExit() {
    return false;
  }

  isDefiniteAssign(f) {
    if (this.#init != null) {
      return this.#init.isDefiniteAssign(f);
    }
    ;
    return false;
  }

  walkChildren(v,depth) {
    this.#init = Stmt.walkExpr(v, depth, this.#init);
    return;
  }

  static makeCatchVar(c) {
    const $self = new LocalDefStmt();
    LocalDefStmt.makeCatchVar$($self,c);
    return $self;
  }

  static makeCatchVar$($self,c) {
    Stmt.make$($self, c.loc(), StmtId.localDef());
    $self.#ctype = c.errType();
    $self.#name = sys.ObjUtil.coerce(c.errVariable(), sys.Str.type$);
    $self.#isCatchVar = true;
    return;
  }

  toStr() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#ctype), " "), this.#name), " ("), this.#var), ")");
  }

  print(out) {
    this.printOpt(out);
    return;
  }

  printOpt(out,nl) {
    if (nl === undefined) nl = true;
    if (this.#ctype != null) {
      out.w(sys.Str.plus(sys.Str.plus("", this.#ctype), " "));
    }
    ;
    out.w(this.#name);
    if (this.#init != null) {
      out.w(sys.Str.plus(" init: ", this.#init));
    }
    ;
    if (nl) {
      out.nl();
    }
    ;
    return;
  }

  initVal() {
    return ((this$) => { if (this$.#init == null) return null; return sys.ObjUtil.coerce(this$.#init, BinaryExpr.type$).rhs(); })(this);
  }

}

class IfStmt extends Stmt {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return IfStmt.type$; }

  #condition = null;

  condition(it) {
    if (it === undefined) {
      return this.#condition;
    }
    else {
      this.#condition = it;
      return;
    }
  }

  #trueBlock = null;

  trueBlock(it) {
    if (it === undefined) {
      return this.#trueBlock;
    }
    else {
      this.#trueBlock = it;
      return;
    }
  }

  #falseBlock = null;

  falseBlock(it) {
    if (it === undefined) {
      return this.#falseBlock;
    }
    else {
      this.#falseBlock = it;
      return;
    }
  }

  static make(loc,condition,trueBlock) {
    const $self = new IfStmt();
    IfStmt.make$($self,loc,condition,trueBlock);
    return $self;
  }

  static make$($self,loc,condition,trueBlock) {
    Stmt.make$($self, loc, StmtId.ifStmt());
    $self.#condition = condition;
    $self.#trueBlock = trueBlock;
    return;
  }

  isExit() {
    if (this.#falseBlock == null) {
      return false;
    }
    ;
    return (this.#trueBlock.isExit() && this.#falseBlock.isExit());
  }

  isDefiniteAssign(f) {
    if (!this.#trueBlock.isDefiniteAssign(f)) {
      return false;
    }
    ;
    if (this.#condition.id() === ExprId.trueLiteral()) {
      return true;
    }
    ;
    if (this.#falseBlock == null) {
      return false;
    }
    ;
    return this.#falseBlock.isDefiniteAssign(f);
  }

  walkChildren(v,depth) {
    this.#condition = sys.ObjUtil.coerce(Stmt.walkExpr(v, depth, this.#condition), Expr.type$);
    this.#trueBlock.walk(v, depth);
    if (this.#falseBlock != null) {
      this.#falseBlock.walk(v, depth);
    }
    ;
    return;
  }

  print(out) {
    out.w(sys.Str.plus(sys.Str.plus("if (", this.#condition), ")")).nl();
    this.#trueBlock.print(out);
    if (this.#falseBlock != null) {
      out.w("else").nl();
      this.#falseBlock.print(out);
    }
    ;
    return;
  }

}

class ReturnStmt extends Stmt {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ReturnStmt.type$; }

  #expr = null;

  expr(it) {
    if (it === undefined) {
      return this.#expr;
    }
    else {
      this.#expr = it;
      return;
    }
  }

  #leaveVar = null;

  leaveVar(it) {
    if (it === undefined) {
      return this.#leaveVar;
    }
    else {
      this.#leaveVar = it;
      return;
    }
  }

  #isSynthetic = false;

  isSynthetic(it) {
    if (it === undefined) {
      return this.#isSynthetic;
    }
    else {
      this.#isSynthetic = it;
      return;
    }
  }

  static make(loc,expr) {
    const $self = new ReturnStmt();
    ReturnStmt.make$($self,loc,expr);
    return $self;
  }

  static make$($self,loc,expr) {
    if (expr === undefined) expr = null;
    Stmt.make$($self, loc, StmtId.returnStmt());
    $self.#expr = expr;
    return;
  }

  static makeSynthetic(loc,expr) {
    if (expr === undefined) expr = null;
    let stmt = ReturnStmt.make(loc, expr);
    stmt.#isSynthetic = true;
    return stmt;
  }

  isExit() {
    return true;
  }

  isDefiniteAssign(f) {
    if (this.#expr == null) {
      return false;
    }
    ;
    return this.#expr.isDefiniteAssign(f);
  }

  walkChildren(v,depth) {
    this.#expr = Stmt.walkExpr(v, depth, this.#expr);
    return;
  }

  toStr() {
    return ((this$) => { if (this$.#expr != null) return sys.Str.plus("return ", this$.#expr); return "return"; })(this);
  }

  print(out) {
    out.w("return");
    if (this.#expr != null) {
      out.w(sys.Str.plus(" ", this.#expr));
    }
    ;
    out.nl();
    return;
  }

}

class ThrowStmt extends Stmt {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ThrowStmt.type$; }

  #exception = null;

  exception(it) {
    if (it === undefined) {
      return this.#exception;
    }
    else {
      this.#exception = it;
      return;
    }
  }

  static make(loc,exception) {
    const $self = new ThrowStmt();
    ThrowStmt.make$($self,loc,exception);
    return $self;
  }

  static make$($self,loc,exception) {
    Stmt.make$($self, loc, StmtId.throwStmt());
    $self.#exception = exception;
    return;
  }

  isExit() {
    return true;
  }

  isDefiniteAssign(f) {
    return true;
  }

  walkChildren(v,depth) {
    this.#exception = sys.ObjUtil.coerce(Stmt.walkExpr(v, depth, this.#exception), Expr.type$);
    return;
  }

  print(out) {
    out.w(sys.Str.plus("throw ", this.#exception)).nl();
    return;
  }

}

class ForStmt extends Stmt {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ForStmt.type$; }

  #init = null;

  init(it) {
    if (it === undefined) {
      return this.#init;
    }
    else {
      this.#init = it;
      return;
    }
  }

  #condition = null;

  condition(it) {
    if (it === undefined) {
      return this.#condition;
    }
    else {
      this.#condition = it;
      return;
    }
  }

  #update = null;

  update(it) {
    if (it === undefined) {
      return this.#update;
    }
    else {
      this.#update = it;
      return;
    }
  }

  #block = null;

  block(it) {
    if (it === undefined) {
      return this.#block;
    }
    else {
      this.#block = it;
      return;
    }
  }

  static make(loc) {
    const $self = new ForStmt();
    ForStmt.make$($self,loc);
    return $self;
  }

  static make$($self,loc) {
    Stmt.make$($self, loc, StmtId.forStmt());
    return;
  }

  isExit() {
    return false;
  }

  isDefiniteAssign(f) {
    if (this.#condition.isDefiniteAssign(f)) {
      return true;
    }
    ;
    if ((this.#init != null && this.#init.isDefiniteAssign(f))) {
      return true;
    }
    ;
    return false;
  }

  walkChildren(v,depth) {
    if (this.#init != null) {
      this.#init.walk(v, depth);
    }
    ;
    this.#condition = Stmt.walkExpr(v, depth, this.#condition);
    this.#update = Stmt.walkExpr(v, depth, this.#update);
    this.#block.walk(v, depth);
    return;
  }

  print(out) {
    out.w("for (");
    if (this.#init != null) {
      sys.ObjUtil.trap(this.#init,"printOpt", sys.List.make(sys.Obj.type$.toNullable(), [out, sys.ObjUtil.coerce(false, sys.Bool.type$.toNullable())]));
    }
    ;
    out.w("; ");
    if (this.#condition != null) {
      this.#condition.print(out);
    }
    ;
    out.w("; ");
    if (this.#update != null) {
      this.#update.print(out);
    }
    ;
    out.w(")").nl();
    this.#block.print(out);
    return;
  }

}

class WhileStmt extends Stmt {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return WhileStmt.type$; }

  #condition = null;

  condition(it) {
    if (it === undefined) {
      return this.#condition;
    }
    else {
      this.#condition = it;
      return;
    }
  }

  #block = null;

  block(it) {
    if (it === undefined) {
      return this.#block;
    }
    else {
      this.#block = it;
      return;
    }
  }

  static make(loc,condition,block) {
    const $self = new WhileStmt();
    WhileStmt.make$($self,loc,condition,block);
    return $self;
  }

  static make$($self,loc,condition,block) {
    Stmt.make$($self, loc, StmtId.whileStmt());
    $self.#condition = condition;
    $self.#block = block;
    return;
  }

  isExit() {
    return false;
  }

  isDefiniteAssign(f) {
    return this.#condition.isDefiniteAssign(f);
  }

  walkChildren(v,depth) {
    this.#condition = sys.ObjUtil.coerce(Stmt.walkExpr(v, depth, this.#condition), Expr.type$);
    this.#block.walk(v, depth);
    return;
  }

  print(out) {
    out.w(sys.Str.plus(sys.Str.plus("while (", this.#condition), ")")).nl();
    this.#block.print(out);
    return;
  }

}

class BreakStmt extends Stmt {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return BreakStmt.type$; }

  #loop = null;

  loop(it) {
    if (it === undefined) {
      return this.#loop;
    }
    else {
      this.#loop = it;
      return;
    }
  }

  static make(loc) {
    const $self = new BreakStmt();
    BreakStmt.make$($self,loc);
    return $self;
  }

  static make$($self,loc) {
    Stmt.make$($self, loc, StmtId.breakStmt());
    return;
  }

  isExit() {
    return false;
  }

  isDefiniteAssign(f) {
    return false;
  }

  print(out) {
    out.w("break").nl();
    return;
  }

}

class ContinueStmt extends Stmt {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ContinueStmt.type$; }

  #loop = null;

  loop(it) {
    if (it === undefined) {
      return this.#loop;
    }
    else {
      this.#loop = it;
      return;
    }
  }

  static make(loc) {
    const $self = new ContinueStmt();
    ContinueStmt.make$($self,loc);
    return $self;
  }

  static make$($self,loc) {
    Stmt.make$($self, loc, StmtId.continueStmt());
    return;
  }

  isExit() {
    return false;
  }

  isDefiniteAssign(f) {
    return false;
  }

  print(out) {
    out.w("continue").nl();
    return;
  }

}

class TryStmt extends Stmt {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return TryStmt.type$; }

  #exception = null;

  exception(it) {
    if (it === undefined) {
      return this.#exception;
    }
    else {
      this.#exception = it;
      return;
    }
  }

  #block = null;

  block(it) {
    if (it === undefined) {
      return this.#block;
    }
    else {
      this.#block = it;
      return;
    }
  }

  #catches = null;

  catches(it) {
    if (it === undefined) {
      return this.#catches;
    }
    else {
      this.#catches = it;
      return;
    }
  }

  #finallyBlock = null;

  finallyBlock(it) {
    if (it === undefined) {
      return this.#finallyBlock;
    }
    else {
      this.#finallyBlock = it;
      return;
    }
  }

  static make(loc) {
    const $self = new TryStmt();
    TryStmt.make$($self,loc);
    return $self;
  }

  static make$($self,loc) {
    Stmt.make$($self, loc, StmtId.tryStmt());
    $self.#catches = sys.List.make(Catch.type$);
    return;
  }

  isExit() {
    const this$ = this;
    if (!this.#block.isExit()) {
      return false;
    }
    ;
    return this.#catches.all((c) => {
      return c.block().isExit();
    });
  }

  isDefiniteAssign(f) {
    const this$ = this;
    if ((this.#finallyBlock != null && this.#finallyBlock.isDefiniteAssign(f))) {
      return true;
    }
    ;
    if (!this.#block.isDefiniteAssign(f)) {
      return false;
    }
    ;
    return this.#catches.all((c) => {
      return c.isDefiniteAssign(f);
    });
  }

  walkChildren(v,depth) {
    const this$ = this;
    this.#block.walk(v, depth);
    this.#catches.each((c) => {
      c.block().walk(v, depth);
      return;
    });
    if (this.#finallyBlock != null) {
      v.enterFinally(this);
      this.#finallyBlock.walk(v, depth);
      v.exitFinally(this);
    }
    ;
    return;
  }

  print(out) {
    const this$ = this;
    out.w("try").nl();
    this.#block.print(out);
    this.#catches.each((c) => {
      c.print(out);
      return;
    });
    if (this.#finallyBlock != null) {
      out.w("finally").nl();
      this.#finallyBlock.print(out);
    }
    ;
    return;
  }

}

class Catch extends Node {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Catch.type$; }

  #errType = null;

  errType(it) {
    if (it === undefined) {
      return this.#errType;
    }
    else {
      this.#errType = it;
      return;
    }
  }

  #errVariable = null;

  errVariable(it) {
    if (it === undefined) {
      return this.#errVariable;
    }
    else {
      this.#errVariable = it;
      return;
    }
  }

  #block = null;

  block(it) {
    if (it === undefined) {
      return this.#block;
    }
    else {
      this.#block = it;
      return;
    }
  }

  #start = 0;

  start(it) {
    if (it === undefined) {
      return this.#start;
    }
    else {
      this.#start = it;
      return;
    }
  }

  #end = 0;

  end(it) {
    if (it === undefined) {
      return this.#end;
    }
    else {
      this.#end = it;
      return;
    }
  }

  static make(loc) {
    const $self = new Catch();
    Catch.make$($self,loc);
    return $self;
  }

  static make$($self,loc) {
    Node.make$($self, loc);
    return;
  }

  isDefiniteAssign(f) {
    if (((this$) => { let $_u58=this$.#block.stmts().last(); return ($_u58==null) ? null : $_u58.id(); })(this) === StmtId.throwStmt()) {
      return true;
    }
    ;
    return this.#block.isDefiniteAssign(f);
  }

  print(out) {
    out.w("catch");
    if (this.#errType != null) {
      out.w(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("(", this.#errType), " "), this.#errVariable), ")"));
    }
    ;
    out.nl();
    this.#block.print(out);
    return;
  }

}

class SwitchStmt extends Stmt {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return SwitchStmt.type$; }

  #condition = null;

  condition(it) {
    if (it === undefined) {
      return this.#condition;
    }
    else {
      this.#condition = it;
      return;
    }
  }

  #cases = null;

  cases(it) {
    if (it === undefined) {
      return this.#cases;
    }
    else {
      this.#cases = it;
      return;
    }
  }

  #defaultBlock = null;

  defaultBlock(it) {
    if (it === undefined) {
      return this.#defaultBlock;
    }
    else {
      this.#defaultBlock = it;
      return;
    }
  }

  #isTableswitch = false;

  isTableswitch(it) {
    if (it === undefined) {
      return this.#isTableswitch;
    }
    else {
      this.#isTableswitch = it;
      return;
    }
  }

  static make(loc,condition) {
    const $self = new SwitchStmt();
    SwitchStmt.make$($self,loc,condition);
    return $self;
  }

  static make$($self,loc,condition) {
    Stmt.make$($self, loc, StmtId.switchStmt());
    $self.#condition = condition;
    $self.#cases = sys.List.make(Case.type$);
    return;
  }

  isDefiniteAssign(f) {
    const this$ = this;
    if (this.#defaultBlock == null) {
      return false;
    }
    ;
    if (!this.#defaultBlock.isDefiniteAssign(f)) {
      return false;
    }
    ;
    return this.#cases.all((c) => {
      return c.block().isDefiniteAssign(f);
    });
  }

  isExit() {
    const this$ = this;
    if (this.#defaultBlock == null) {
      return false;
    }
    ;
    if (!this.#defaultBlock.isExit()) {
      return false;
    }
    ;
    return this.#cases.all((c) => {
      return c.block().isExit();
    });
  }

  walkChildren(v,depth) {
    const this$ = this;
    this.#condition = sys.ObjUtil.coerce(Stmt.walkExpr(v, depth, this.#condition), Expr.type$);
    this.#cases.each((c) => {
      c.walk(v, depth);
      return;
    });
    if (this.#defaultBlock != null) {
      this.#defaultBlock.walk(v, depth);
    }
    ;
    return;
  }

  print(out) {
    const this$ = this;
    out.w(sys.Str.plus(sys.Str.plus("switch (", this.#condition), ")")).nl();
    out.w("{").nl();
    out.indent();
    this.#cases.each((c) => {
      c.print(out);
      return;
    });
    if (this.#defaultBlock != null) {
      out.w("default:").nl();
      out.indent();
      this.#defaultBlock.printOpt(out, false);
      out.unindent();
    }
    ;
    out.unindent();
    out.w("}").nl();
    return;
  }

}

class Case extends Node {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Case.type$; }

  #cases = null;

  cases(it) {
    if (it === undefined) {
      return this.#cases;
    }
    else {
      this.#cases = it;
      return;
    }
  }

  #block = null;

  block(it) {
    if (it === undefined) {
      return this.#block;
    }
    else {
      this.#block = it;
      return;
    }
  }

  #startOffset = 0;

  startOffset(it) {
    if (it === undefined) {
      return this.#startOffset;
    }
    else {
      this.#startOffset = it;
      return;
    }
  }

  static make(loc) {
    const $self = new Case();
    Case.make$($self,loc);
    return $self;
  }

  static make$($self,loc) {
    Node.make$($self, loc);
    $self.#cases = sys.List.make(Expr.type$);
    return;
  }

  walk(v,depth) {
    if (depth === VisitDepth.expr()) {
      this.#cases = Expr.walkExprs(v, this.#cases);
    }
    ;
    this.#block.walk(v, depth);
    return;
  }

  print(out) {
    const this$ = this;
    this.#cases.each((c) => {
      out.w(sys.Str.plus(sys.Str.plus("case ", c), ":")).nl();
      return;
    });
    out.indent();
    if (this.#block != null) {
      this.#block.printOpt(out, false);
    }
    ;
    out.unindent();
    return;
  }

}

class StmtId extends sys.Enum {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return StmtId.type$; }

  static nop() { return StmtId.vals().get(0); }

  static expr() { return StmtId.vals().get(1); }

  static localDef() { return StmtId.vals().get(2); }

  static ifStmt() { return StmtId.vals().get(3); }

  static returnStmt() { return StmtId.vals().get(4); }

  static throwStmt() { return StmtId.vals().get(5); }

  static forStmt() { return StmtId.vals().get(6); }

  static whileStmt() { return StmtId.vals().get(7); }

  static breakStmt() { return StmtId.vals().get(8); }

  static continueStmt() { return StmtId.vals().get(9); }

  static tryStmt() { return StmtId.vals().get(10); }

  static switchStmt() { return StmtId.vals().get(11); }

  static #vals = undefined;

  static make($ordinal,$name) {
    const $self = new StmtId();
    StmtId.make$($self,$ordinal,$name);
    return $self;
  }

  static make$($self,$ordinal,$name) {
    sys.Enum.make$($self, $ordinal, $name);
    return;
  }

  static fromStr(name$, checked=true) {
    return sys.Enum.doFromStr(StmtId.type$, StmtId.vals(), name$, checked);
  }

  static vals() {
    if (StmtId.#vals == null) {
      StmtId.#vals = sys.List.make(StmtId.type$, [
        StmtId.make(0, "nop", ),
        StmtId.make(1, "expr", ),
        StmtId.make(2, "localDef", ),
        StmtId.make(3, "ifStmt", ),
        StmtId.make(4, "returnStmt", ),
        StmtId.make(5, "throwStmt", ),
        StmtId.make(6, "forStmt", ),
        StmtId.make(7, "whileStmt", ),
        StmtId.make(8, "breakStmt", ),
        StmtId.make(9, "continueStmt", ),
        StmtId.make(10, "tryStmt", ),
        StmtId.make(11, "switchStmt", ),
      ]).toImmutable();
    }
    return StmtId.#vals;
  }

  static static$init() {
    const $_u59 = StmtId.vals();
    if (true) {
    }
    ;
    return;
  }

}

class CType {
  constructor() {
    const this$ = this;
  }

  typeof() { return CType.type$; }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  toStr() {
    return this.signature();
  }

  deref() {
    return this;
  }

  static isValType(qname) {
    let $_u60 = qname;
    if (sys.ObjUtil.equals($_u60, "sys::Bool") || sys.ObjUtil.equals($_u60, "sys::Int") || sys.ObjUtil.equals($_u60, "sys::Float")) {
      return true;
    }
    else {
      return false;
    }
    ;
  }

  toNonNullable() {
    return this;
  }

  isForeign() {
    return false;
  }

  foreignInheritance() {
    const this$ = this;
    if (this.base() == null) {
      return null;
    }
    ;
    if (this.base().isForeign()) {
      return this.base();
    }
    ;
    let m = this.mixins().find((t) => {
      return t.isForeign();
    });
    if (m != null) {
      return m;
    }
    ;
    return this.base().foreignInheritance();
  }

  bridge() {
    return this.pod().bridge();
  }

  inferredAs() {
    return this;
  }

  isSupported() {
    return true;
  }

  raw() {
    if (!this.isGenericParameter()) {
      return this;
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Str.size(this.name()), 1)) {
      return this;
    }
    ;
    let raw = this.ns().objType();
    let $_u61 = sys.Str.get(this.name(), 0);
    if (sys.ObjUtil.equals($_u61, 76)) {
      (raw = this.ns().listType());
    }
    else if (sys.ObjUtil.equals($_u61, 77)) {
      (raw = this.ns().mapType());
    }
    ;
    if (this.isNullable()) {
      (raw = raw.toNullable());
    }
    ;
    return sys.ObjUtil.coerce(raw, CType.type$);
  }

  parameterizeThis(thisType) {
    return this;
  }

  hash() {
    return sys.Str.hash(this.signature());
  }

  equals(t) {
    if (this === t) {
      return true;
    }
    ;
    let that = sys.ObjUtil.as(t, CType.type$);
    if (that == null) {
      return false;
    }
    ;
    return sys.ObjUtil.equals(this.signature(), that.signature());
  }

  fits(t) {
    (t = t.toNonNullable());
    if (t.isObj()) {
      return true;
    }
    ;
    if (sys.ObjUtil.equals(this, t)) {
      return true;
    }
    ;
    if ((this.base() != null && this.base().fits(t))) {
      return true;
    }
    ;
    for (let i = 0; sys.ObjUtil.compareLT(i, this.mixins().size()); i = sys.Int.increment(i)) {
      if (this.mixins().get(i).fits(t)) {
        return true;
      }
      ;
    }
    ;
    if ((sys.ObjUtil.equals(sys.Str.size(t.name()), 1) && sys.ObjUtil.equals(t.pod().name(), "sys"))) {
      return true;
    }
    ;
    return false;
  }

  fitsAny(types) {
    const this$ = this;
    return types.any((t) => {
      return this$.fits(t);
    });
  }

  static common(ns,types) {
    const this$ = this;
    if (sys.ObjUtil.equals(types.size(), 2)) {
      if (types.get(0).isNothing()) {
        return types.get(1);
      }
      ;
      if (types.get(1).isNothing()) {
        return types.get(0);
      }
      ;
    }
    ;
    if (sys.ObjUtil.equals(types.size(), 0)) {
      return ns.objType().toNullable();
    }
    ;
    if (sys.ObjUtil.equals(types.size(), 1)) {
      return sys.ObjUtil.coerce(types.first(), CType.type$);
    }
    ;
    let mixins = false;
    let nullable = false;
    let parameterized = false;
    (types = types.dup());
    types.each((t,i) => {
      if (t.isParameterized()) {
        (parameterized = true);
      }
      ;
      if (t.isNullable()) {
        (nullable = true);
      }
      ;
      if (t.isMixin()) {
        (mixins = true);
      }
      ;
      types.set(i, t.toNonNullable());
      return;
    });
    let best = null;
    if (parameterized) {
      (best = CType.commonParameterized(ns, types));
    }
    else {
      if (mixins) {
        (best = CType.commonMixin(ns, types));
      }
      else {
        (best = CType.commonClass(ns, types));
      }
      ;
    }
    ;
    return sys.ObjUtil.coerce(((this$) => { if (nullable) return best.toNullable(); return best; })(this), CType.type$);
  }

  static commonClass(ns,types) {
    let best = types.get(0);
    for (let i = 1; sys.ObjUtil.compareLT(i, types.size()); i = sys.Int.increment(i)) {
      let t = types.get(i);
      while (!t.fits(best)) {
        let bestBase = best.base();
        if (bestBase == null) {
          return sys.ObjUtil.coerce(ns.objType(), CType.type$);
        }
        ;
        (best = sys.ObjUtil.coerce(bestBase, CType.type$));
      }
      ;
    }
    ;
    return best;
  }

  static commonMixin(ns,types) {
    const this$ = this;
    let first = types.get(0);
    let allSame = types.all((t) => {
      return sys.ObjUtil.equals(t, first);
    });
    return sys.ObjUtil.coerce(((this$) => { if (allSame) return first; return ns.objType(); })(this), CType.type$);
  }

  static commonParameterized(ns,types) {
    const this$ = this;
    let allList = true;
    let allMap = true;
    let allFunc = true;
    types.each((t) => {
      (allList = (allList && sys.ObjUtil.is(t, ListType.type$)));
      (allMap = (allMap && sys.ObjUtil.is(t, MapType.type$)));
      (allFunc = (allFunc && sys.ObjUtil.is(t, FuncType.type$)));
      return;
    });
    if (allList) {
      return CType.commonList(ns, sys.ObjUtil.coerce(types, sys.Type.find("compiler::ListType[]")));
    }
    ;
    if (allMap) {
      return CType.commonExact(ns, types, sys.ObjUtil.coerce(ns.mapType(), CType.type$));
    }
    ;
    if (allFunc) {
      return CType.commonExact(ns, types, sys.ObjUtil.coerce(ns.funcType(), CType.type$));
    }
    ;
    return sys.ObjUtil.coerce(ns.objType(), CType.type$);
  }

  static commonList(ns,types) {
    const this$ = this;
    let vTypes = types.map((t) => {
      return t.v();
    }, CType.type$);
    return CType.common(ns, sys.ObjUtil.coerce(vTypes, sys.Type.find("compiler::CType[]"))).toListOf();
  }

  static commonExact(ns,types,fallback) {
    const this$ = this;
    let first = types.get(0);
    let exact = types.all((t) => {
      return sys.ObjUtil.equals(first, t);
    });
    return ((this$) => { if (exact) return first; return fallback; })(this);
  }

  isClass() {
    return (!this.isMixin() && !this.isEnum());
  }

  isMixin() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Mixin()), 0);
  }

  isEnum() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Enum()), 0);
  }

  isFacet() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Facet()), 0);
  }

  isAbstract() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Abstract()), 0);
  }

  isConst() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Const()), 0);
  }

  isFinal() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Final()), 0);
  }

  isPublic() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Public()), 0);
  }

  isInternal() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Internal()), 0);
  }

  isSynthetic() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Synthetic()), 0);
  }

  isNative() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Native()), 0);
  }

  isObj() {
    return sys.ObjUtil.equals(this.qname(), "sys::Obj");
  }

  isBool() {
    return sys.ObjUtil.equals(this.qname(), "sys::Bool");
  }

  isInt() {
    return sys.ObjUtil.equals(this.qname(), "sys::Int");
  }

  isFloat() {
    return sys.ObjUtil.equals(this.qname(), "sys::Float");
  }

  isDecimal() {
    return sys.ObjUtil.equals(this.qname(), "sys::Decimal");
  }

  isRange() {
    return sys.ObjUtil.equals(this.qname(), "sys::Range");
  }

  isStr() {
    return sys.ObjUtil.equals(this.qname(), "sys::Str");
  }

  isNum() {
    return sys.ObjUtil.equals(this.qname(), "sys::Num");
  }

  isThis() {
    return sys.ObjUtil.equals(this.qname(), "sys::This");
  }

  isType() {
    return sys.ObjUtil.equals(this.qname(), "sys::Type");
  }

  isVoid() {
    return sys.ObjUtil.equals(this.qname(), "sys::Void");
  }

  isBuf() {
    return sys.ObjUtil.equals(this.qname(), "sys::Buf");
  }

  isList() {
    return this.fits(sys.ObjUtil.coerce(this.ns().listType(), CType.type$));
  }

  isMap() {
    return this.fits(sys.ObjUtil.coerce(this.ns().mapType(), CType.type$));
  }

  isFunc() {
    return this.fits(sys.ObjUtil.coerce(this.ns().funcType(), CType.type$));
  }

  isNothing() {
    return this === this.ns().nothingType();
  }

  isValid() {
    return (!this.isVoid() && !this.isThis());
  }

  isConstFieldType() {
    if (this.isConst()) {
      return true;
    }
    ;
    let t = this.deref().toNonNullable();
    if ((t.isObj() || t.isList() || t.isMap() || t.isBuf() || t.isFunc())) {
      return true;
    }
    ;
    return false;
  }

  hasSlot(name) {
    return this.slots().containsKey(name);
  }

  slot(name) {
    return this.slots().get(name);
  }

  field(name) {
    return sys.ObjUtil.as(this.slot(name), CField.type$);
  }

  method(name) {
    return sys.ObjUtil.as(this.slot(name), CMethod.type$);
  }

  fields() {
    return sys.ObjUtil.coerce(this.slots().vals().findType(CField.type$), sys.Type.find("compiler::CField[]"));
  }

  methods() {
    return sys.ObjUtil.coerce(this.slots().vals().findType(CMethod.type$), sys.Type.find("compiler::CMethod[]"));
  }

  ctors() {
    const this$ = this;
    return sys.ObjUtil.coerce(this.slots().vals().findAll((s) => {
      return s.isCtor();
    }), sys.Type.find("compiler::CMethod[]"));
  }

  instanceCtors() {
    const this$ = this;
    return sys.ObjUtil.coerce(this.slots().vals().findAll((s) => {
      return s.isInstanceCtor();
    }), sys.Type.find("compiler::CMethod[]"));
  }

}

class TypeDef extends DefNode {
  constructor() {
    super();
    const this$ = this;
    this.#baseSpecified = true;
    return;
  }

  typeof() { return TypeDef.type$; }

  isNum() { return CType.prototype.isNum.apply(this, arguments); }

  inferredAs() { return CType.prototype.inferredAs.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CType.prototype.isSynthetic.apply(this, arguments); }

  fitsAny() { return CType.prototype.fitsAny.apply(this, arguments); }

  isNative() { return CType.prototype.isNative.apply(this, arguments); }

  isConstFieldType() { return CType.prototype.isConstFieldType.apply(this, arguments); }

  isObj() { return CType.prototype.isObj.apply(this, arguments); }

  isBool() { return CType.prototype.isBool.apply(this, arguments); }

  isBuf() { return CType.prototype.isBuf.apply(this, arguments); }

  isInt() { return CType.prototype.isInt.apply(this, arguments); }

  isDecimal() { return CType.prototype.isDecimal.apply(this, arguments); }

  isFacet() { return CType.prototype.isFacet.apply(this, arguments); }

  isNothing() { return CType.prototype.isNothing.apply(this, arguments); }

  toStr() { return CType.prototype.toStr.apply(this, arguments); }

  method() { return CType.prototype.method.apply(this, arguments); }

  foreignInheritance() { return CType.prototype.foreignInheritance.apply(this, arguments); }

  raw() { return CType.prototype.raw.apply(this, arguments); }

  isSupported() { return CType.prototype.isSupported.apply(this, arguments); }

  isFunc() { return CType.prototype.isFunc.apply(this, arguments); }

  fits() { return CType.prototype.fits.apply(this, arguments); }

  isType() { return CType.prototype.isType.apply(this, arguments); }

  isMap() { return CType.prototype.isMap.apply(this, arguments); }

  isInternal() { return CType.prototype.isInternal.apply(this, arguments); }

  hasSlot() { return CType.prototype.hasSlot.apply(this, arguments); }

  field() { return CType.prototype.field.apply(this, arguments); }

  bridge() { return CType.prototype.bridge.apply(this, arguments); }

  fields() { return CType.prototype.fields.apply(this, arguments); }

  hash() { return CType.prototype.hash.apply(this, arguments); }

  toNonNullable() { return CType.prototype.toNonNullable.apply(this, arguments); }

  isThis() { return CType.prototype.isThis.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  deref() { return CType.prototype.deref.apply(this, arguments); }

  instanceCtors() { return CType.prototype.instanceCtors.apply(this, arguments); }

  isForeign() { return CType.prototype.isForeign.apply(this, arguments); }

  methods() { return CType.prototype.methods.apply(this, arguments); }

  slot() { return CType.prototype.slot.apply(this, arguments); }

  isList() { return CType.prototype.isList.apply(this, arguments); }

  parameterizeThis() { return CType.prototype.parameterizeThis.apply(this, arguments); }

  isConst() { return CType.prototype.isConst.apply(this, arguments); }

  isPublic() { return CType.prototype.isPublic.apply(this, arguments); }

  isStr() { return CType.prototype.isStr.apply(this, arguments); }

  isFinal() { return CType.prototype.isFinal.apply(this, arguments); }

  isClass() { return CType.prototype.isClass.apply(this, arguments); }

  isValid() { return CType.prototype.isValid.apply(this, arguments); }

  isEnum() { return CType.prototype.isEnum.apply(this, arguments); }

  isFloat() { return CType.prototype.isFloat.apply(this, arguments); }

  isMixin() { return CType.prototype.isMixin.apply(this, arguments); }

  isAbstract() { return CType.prototype.isAbstract.apply(this, arguments); }

  isRange() { return CType.prototype.isRange.apply(this, arguments); }

  isVoid() { return CType.prototype.isVoid.apply(this, arguments); }

  ctors() { return CType.prototype.ctors.apply(this, arguments); }

  equals() { return CType.prototype.equals.apply(this, arguments); }

  #ns = null;

  ns(it) {
    if (it === undefined) {
      return this.#ns;
    }
    else {
      this.#ns = it;
      return;
    }
  }

  #unit = null;

  unit(it) {
    if (it === undefined) {
      return this.#unit;
    }
    else {
      this.#unit = it;
      return;
    }
  }

  #podDef = null;

  podDef(it) {
    if (it === undefined) {
      return this.#podDef;
    }
    else {
      this.#podDef = it;
      return;
    }
  }

  #pod = null;

  pod(it) {
    if (it === undefined) {
      return this.#pod;
    }
    else {
      this.#pod = it;
      return;
    }
  }

  #name = null;

  name() { return this.#name; }

  __name(it) { if (it === undefined) return this.#name; else this.#name = it; }

  #qname = null;

  qname() { return this.#qname; }

  __qname(it) { if (it === undefined) return this.#qname; else this.#qname = it; }

  #isVal = false;

  isVal() { return this.#isVal; }

  __isVal(it) { if (it === undefined) return this.#isVal; else this.#isVal = it; }

  #baseSpecified = false;

  baseSpecified(it) {
    if (it === undefined) {
      return this.#baseSpecified;
    }
    else {
      this.#baseSpecified = it;
      return;
    }
  }

  #base = null;

  base(it) {
    if (it === undefined) {
      return this.#base;
    }
    else {
      this.#base = it;
      return;
    }
  }

  #mixins = null;

  mixins(it) {
    if (it === undefined) {
      return this.#mixins;
    }
    else {
      this.#mixins = it;
      return;
    }
  }

  #enumDefs = null;

  enumDefs(it) {
    if (it === undefined) {
      return this.#enumDefs;
    }
    else {
      this.#enumDefs = it;
      return;
    }
  }

  #closures = null;

  closures(it) {
    if (it === undefined) {
      return this.#closures;
    }
    else {
      this.#closures = it;
      return;
    }
  }

  #closure = null;

  closure(it) {
    if (it === undefined) {
      return this.#closure;
    }
    else {
      this.#closure = it;
      return;
    }
  }

  #slotMap = null;

  // private field reflection only
  __slotMap(it) { if (it === undefined) return this.#slotMap; else this.#slotMap = it; }

  #slotDefMap = null;

  // private field reflection only
  __slotDefMap(it) { if (it === undefined) return this.#slotDefMap; else this.#slotDefMap = it; }

  #slotDefList = null;

  // private field reflection only
  __slotDefList(it) { if (it === undefined) return this.#slotDefList; else this.#slotDefList = it; }

  #indexedFacets = null;

  indexedFacets(it) {
    if (it === undefined) {
      return this.#indexedFacets;
    }
    else {
      this.#indexedFacets = it;
      return;
    }
  }

  #toNullable$Store = undefined;

  // private field reflection only
  __toNullable$Store(it) { if (it === undefined) return this.#toNullable$Store; else this.#toNullable$Store = it; }

  #toListOf$Store = undefined;

  // private field reflection only
  __toListOf$Store(it) { if (it === undefined) return this.#toListOf$Store; else this.#toListOf$Store = it; }

  #hasNativePeer$Store = undefined;

  // private field reflection only
  __hasNativePeer$Store(it) { if (it === undefined) return this.#hasNativePeer$Store; else this.#hasNativePeer$Store = it; }

  #hasItBlockCtor$Store = undefined;

  // private field reflection only
  __hasItBlockCtor$Store(it) { if (it === undefined) return this.#hasItBlockCtor$Store; else this.#hasItBlockCtor$Store = it; }

  #operators$Store = undefined;

  // private field reflection only
  __operators$Store(it) { if (it === undefined) return this.#operators$Store; else this.#operators$Store = it; }

  static make(ns,loc,unit,name,flags) {
    const $self = new TypeDef();
    TypeDef.make$($self,ns,loc,unit,name,flags);
    return $self;
  }

  static make$($self,ns,loc,unit,name,flags) {
    if (flags === undefined) flags = 0;
    DefNode.make$($self, loc);
    ;
    $self.ns(ns);
    $self.pod(unit.pod());
    $self.#podDef = sys.ObjUtil.coerce(unit.pod(), PodDef.type$);
    $self.#unit = unit;
    $self.#name = name;
    $self.#qname = sys.Str.plus(sys.Str.plus($self.pod().name(), "::"), name);
    $self.flags(flags);
    $self.#isVal = CType.isValType($self.#qname);
    $self.mixins(sys.List.make(CType.type$));
    $self.#enumDefs = sys.List.make(EnumDef.type$);
    $self.#slotMap = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::CSlot"));
    $self.#slotDefMap = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::SlotDef"));
    $self.#slotDefList = sys.List.make(SlotDef.type$);
    $self.#closures = sys.List.make(ClosureExpr.type$);
    return;
  }

  signature() {
    return this.#qname;
  }

  isNullable() {
    return false;
  }

  toNullable() {
    if (this.#toNullable$Store === undefined) {
      this.#toNullable$Store = this.toNullable$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#toNullable$Store, CType.type$);
  }

  isGeneric() {
    return false;
  }

  isParameterized() {
    return false;
  }

  isGenericParameter() {
    return false;
  }

  toListOf() {
    if (this.#toListOf$Store === undefined) {
      this.#toListOf$Store = this.toListOf$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#toListOf$Store, CType.type$);
  }

  isClosure() {
    return this.#closure != null;
  }

  hasNativePeer() {
    if (this.#hasNativePeer$Store === undefined) {
      this.#hasNativePeer$Store = sys.ObjUtil.coerce(this.hasNativePeer$Once(), sys.Obj.type$.toNullable());
    }
    ;
    return sys.ObjUtil.coerce(this.#hasNativePeer$Store, sys.Bool.type$);
  }

  hasItBlockCtor() {
    if (this.#hasItBlockCtor$Store === undefined) {
      this.#hasItBlockCtor$Store = sys.ObjUtil.coerce(this.hasItBlockCtor$Once(), sys.Obj.type$.toNullable());
    }
    ;
    return sys.ObjUtil.coerce(this.#hasItBlockCtor$Store, sys.Bool.type$);
  }

  slots() {
    return this.#slotMap;
  }

  operators() {
    if (this.#operators$Store === undefined) {
      this.#operators$Store = this.operators$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#operators$Store, COperators.type$);
  }

  addSlot(s,slotDefIndex) {
    if (slotDefIndex === undefined) slotDefIndex = null;
    let m = sys.ObjUtil.as(s, MethodDef.type$);
    if (m != null) {
      if (m.isStaticInit()) {
        this.#slotDefList.add(sys.ObjUtil.coerce(m, SlotDef.type$));
        return;
      }
      ;
      if (m.isFieldAccessor()) {
        this.#slotDefList.add(sys.ObjUtil.coerce(m, SlotDef.type$));
        return;
      }
      ;
    }
    ;
    let name = s.name();
    if (this.hasSlot(name)) {
      throw sys.Err.make(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Internal error: duplicate slot ", name), " ["), this.loc().toLocStr()), "]"));
    }
    ;
    this.#slotMap.set(name, s);
    let def = sys.ObjUtil.as(s, SlotDef.type$);
    if ((def != null && def.parent() === this)) {
      this.#slotDefMap.set(name, sys.ObjUtil.coerce(def, SlotDef.type$));
      if (slotDefIndex == null) {
        this.#slotDefList.add(sys.ObjUtil.coerce(def, SlotDef.type$));
      }
      else {
        this.#slotDefList.insert(sys.ObjUtil.coerce(slotDefIndex, sys.Int.type$), sys.ObjUtil.coerce(def, SlotDef.type$));
      }
      ;
      if (sys.ObjUtil.is(s, FieldDef.type$)) {
        let f = sys.ObjUtil.coerce(s, FieldDef.type$);
        if (f.get() != null) {
          this.addSlot(sys.ObjUtil.coerce(f.get(), CSlot.type$));
        }
        ;
        if (f.set() != null) {
          this.addSlot(sys.ObjUtil.coerce(f.set(), CSlot.type$));
        }
        ;
      }
      ;
    }
    ;
    return;
  }

  replaceSlot(oldSlot,newSlot) {
    if (sys.ObjUtil.compareNE(oldSlot.name(), newSlot.name())) {
      throw sys.Err.make(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Internal error: not same names: ", oldSlot), " != "), newSlot), " ["), this.loc().toLocStr()), "]"));
    }
    ;
    if (this.#slotMap.get(oldSlot.name()) !== oldSlot) {
      throw sys.Err.make(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Internal error: old slot not mapped: ", oldSlot), " ["), this.loc().toLocStr()), "]"));
    }
    ;
    let name = oldSlot.name();
    this.#slotMap.set(name, newSlot);
    let oldDef = sys.ObjUtil.as(oldSlot, SlotDef.type$);
    if ((oldDef != null && oldDef.parent() === this)) {
      this.#slotDefMap.set(name, sys.ObjUtil.coerce(oldDef, SlotDef.type$));
      this.#slotDefList.remove(sys.ObjUtil.coerce(oldDef, SlotDef.type$));
    }
    ;
    let newDef = sys.ObjUtil.as(newSlot, SlotDef.type$);
    if ((newDef != null && newDef.parent() === this)) {
      this.#slotDefMap.set(name, sys.ObjUtil.coerce(newDef, SlotDef.type$));
      this.#slotDefList.add(sys.ObjUtil.coerce(newDef, SlotDef.type$));
    }
    ;
    return;
  }

  staticInit() {
    return sys.ObjUtil.coerce(this.#slotDefMap.get("static\$init"), MethodDef.type$.toNullable());
  }

  normalizeStaticInits(m) {
    const this$ = this;
    this.#slotDefList = this.#slotDefList.exclude((s) => {
      return MethodDef.isNameStaticInit(s.name());
    });
    this.#closures.each((c) => {
      if ((sys.ObjUtil.is(c.enclosingSlot(), MethodDef.type$) && sys.ObjUtil.coerce(c.enclosingSlot(), MethodDef.type$).isStaticInit())) {
        c.enclosingSlot(m);
      }
      ;
      return;
    });
    this.#slotMap.set(m.name(), m);
    this.#slotDefMap.set(m.name(), m);
    this.#slotDefList.add(m);
    return;
  }

  hasSlotDef(name) {
    return this.#slotDefMap.containsKey(name);
  }

  slotDef(name) {
    return this.#slotDefMap.get(name);
  }

  fieldDef(name) {
    return sys.ObjUtil.coerce(this.#slotDefMap.get(name), FieldDef.type$);
  }

  methodDef(name) {
    return sys.ObjUtil.coerce(this.#slotDefMap.get(name), MethodDef.type$);
  }

  slotDefs() {
    return this.#slotDefList;
  }

  fieldDefs() {
    return sys.ObjUtil.coerce(this.#slotDefList.findType(FieldDef.type$), sys.Type.find("compiler::FieldDef[]"));
  }

  staticFieldDefs() {
    const this$ = this;
    return this.fieldDefs().findAll((f) => {
      return f.isStatic();
    });
  }

  instanceFieldDefs() {
    const this$ = this;
    return this.fieldDefs().findAll((f) => {
      return !f.isStatic();
    });
  }

  methodDefs() {
    return sys.ObjUtil.coerce(this.#slotDefList.findType(MethodDef.type$), sys.Type.find("compiler::MethodDef[]"));
  }

  ctorDefs() {
    const this$ = this;
    return this.methodDefs().findAll((m) => {
      return m.isCtor();
    });
  }

  enumDef(name) {
    const this$ = this;
    return this.#enumDefs.find((def) => {
      return sys.ObjUtil.equals(def.name(), name);
    });
  }

  walk(v,depth) {
    const this$ = this;
    v.enterUnit(this.#unit);
    v.enterTypeDef(this);
    this.walkFacets(v, depth);
    if (sys.ObjUtil.compareGE(depth, VisitDepth.slotDef())) {
      this.slotDefs().each((slot) => {
        slot.walk(v, depth);
        return;
      });
    }
    ;
    v.visitTypeDef(this);
    v.exitTypeDef(this);
    v.exitUnit(this.#unit);
    return;
  }

  print(out) {
    const this$ = this;
    out.nl();
    this.printFacets(out);
    if (this.isMixin()) {
      out.w(sys.Str.plus("mixin ", this.#qname)).nl();
    }
    else {
      if (this.isEnum()) {
        out.w(sys.Str.plus("enum ", this.#qname)).nl();
      }
      else {
        out.w(sys.Str.plus("class ", this.#qname)).nl();
      }
      ;
    }
    ;
    if ((this.base() != null || !this.mixins().isEmpty())) {
      out.w(" : ");
      if (this.base() != null) {
        out.w(sys.Str.plus(" ", this.base()));
      }
      ;
      if (!this.mixins().isEmpty()) {
        out.w(", ").w(this.mixins().join(", ")).nl();
      }
      ;
    }
    ;
    out.w("{").nl();
    out.indent();
    this.#enumDefs.each((e) => {
      e.print(out);
      return;
    });
    this.slotDefs().each((s) => {
      s.print(out);
      return;
    });
    out.unindent();
    out.w("}").nl();
    return;
  }

  toNullable$Once() {
    return NullableType.make(this);
  }

  toListOf$Once() {
    return ListType.make(this);
  }

  hasNativePeer$Once() {
    const this$ = this;
    return this.slotDefs().any((x) => {
      return x.isNative();
    });
  }

  hasItBlockCtor$Once() {
    const this$ = this;
    return this.slotDefs().any((x) => {
      return (sys.ObjUtil.is(x, MethodDef.type$) && sys.ObjUtil.coerce(x, MethodDef.type$).isItBlockCtor());
    });
  }

  operators$Once() {
    return COperators.make(this);
  }

}

class Using extends Node {
  constructor() {
    super();
    const this$ = this;
    this.#podName = "?";
    return;
  }

  typeof() { return Using.type$; }

  #podName = null;

  podName(it) {
    if (it === undefined) {
      return this.#podName;
    }
    else {
      this.#podName = it;
      return;
    }
  }

  #typeName = null;

  typeName(it) {
    if (it === undefined) {
      return this.#typeName;
    }
    else {
      this.#typeName = it;
      return;
    }
  }

  #asName = null;

  asName(it) {
    if (it === undefined) {
      return this.#asName;
    }
    else {
      this.#asName = it;
      return;
    }
  }

  #resolvedPod = null;

  resolvedPod(it) {
    if (it === undefined) {
      return this.#resolvedPod;
    }
    else {
      this.#resolvedPod = it;
      return;
    }
  }

  #resolvedType = null;

  resolvedType(it) {
    if (it === undefined) {
      return this.#resolvedType;
    }
    else {
      this.#resolvedType = it;
      return;
    }
  }

  static make(loc) {
    const $self = new Using();
    Using.make$($self,loc);
    return $self;
  }

  static make$($self,loc) {
    Node.make$($self, loc);
    ;
    return;
  }

  isPod() {
    return this.#typeName == null;
  }

  print(out) {
    out.w(this.toStr()).nl();
    return;
  }

  toStr() {
    let s = sys.Str.plus("using ", this.#podName);
    if (this.#typeName != null) {
      s = sys.Str.plus(s, sys.Str.plus("::", this.#typeName));
    }
    ;
    if (this.#asName != null) {
      s = sys.Str.plus(s, sys.Str.plus(" as ", this.#asName));
    }
    ;
    return s;
  }

}

class CSlot {
  constructor() {
    const this$ = this;
  }

  typeof() { return CSlot.type$; }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  ns() {
    return this.parent().ns();
  }

  toStr() {
    return this.signature();
  }

  isAbstract() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Abstract()), 0);
  }

  isAccessor() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), sys.Int.or(FConst.Getter(), FConst.Setter())), 0);
  }

  isConst() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Const()), 0);
  }

  isCtor() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Ctor()), 0);
  }

  isEnum() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Enum()), 0);
  }

  isGetter() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Getter()), 0);
  }

  isInternal() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Internal()), 0);
  }

  isNative() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Native()), 0);
  }

  isOverride() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Override()), 0);
  }

  isPrivate() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Private()), 0);
  }

  isProtected() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Protected()), 0);
  }

  isPublic() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Public()), 0);
  }

  isSetter() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Setter()), 0);
  }

  isStatic() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Static()), 0);
  }

  isStorage() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Storage()), 0);
  }

  isSynthetic() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Synthetic()), 0);
  }

  isVirtual() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Virtual()), 0);
  }

  isOnce() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.flags(), FConst.Once()), 0);
  }

  isInstanceCtor() {
    return (this.isCtor() && !this.isStatic());
  }

  isStaticCtor() {
    return (this.isCtor() && this.isStatic());
  }

  isForeign() {
    return false;
  }

  usesForeign() {
    return this.usesBridge() != null;
  }

  bridge() {
    return this.parent().pod().bridge();
  }

  isVisibleTo(curType) {
    if (sys.ObjUtil.equals(this.parent(), curType)) {
      return true;
    }
    ;
    if (this.isPrivate()) {
      return false;
    }
    ;
    if (this.isInternal()) {
      return sys.ObjUtil.equals(this.parent().pod(), curType.pod());
    }
    ;
    if (this.isProtected()) {
      return (sys.ObjUtil.equals(this.parent().pod(), curType.pod()) || curType.fits(this.parent()));
    }
    ;
    return true;
  }

}

class SlotDef extends DefNode {
  constructor() {
    super();
    const this$ = this;
    this.#name = "?";
    this.#overridden = false;
    return;
  }

  typeof() { return SlotDef.type$; }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  #parentDef = null;

  parentDef(it) {
    if (it === undefined) {
      return this.#parentDef;
    }
    else {
      this.#parentDef = it;
      return;
    }
  }

  #name = null;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #overridden = false;

  overridden(it) {
    if (it === undefined) {
      return this.#overridden;
    }
    else {
      this.#overridden = it;
      return;
    }
  }

  static make(loc,parentDef) {
    const $self = new SlotDef();
    SlotDef.make$($self,loc,parentDef);
    return $self;
  }

  static make$($self,loc,parentDef) {
    DefNode.make$($self, loc);
    ;
    $self.#parentDef = parentDef;
    return;
  }

  ns() {
    return this.parent().ns();
  }

  parent() {
    return this.#parentDef;
  }

  qname() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.parent().qname()), "."), this.name());
  }

}

class VisitDepth extends sys.Enum {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return VisitDepth.type$; }

  static typeDef() { return VisitDepth.vals().get(0); }

  static slotDef() { return VisitDepth.vals().get(1); }

  static stmt() { return VisitDepth.vals().get(2); }

  static expr() { return VisitDepth.vals().get(3); }

  static #vals = undefined;

  static make($ordinal,$name) {
    const $self = new VisitDepth();
    VisitDepth.make$($self,$ordinal,$name);
    return $self;
  }

  static make$($self,$ordinal,$name) {
    sys.Enum.make$($self, $ordinal, $name);
    return;
  }

  static fromStr(name$, checked=true) {
    return sys.Enum.doFromStr(VisitDepth.type$, VisitDepth.vals(), name$, checked);
  }

  static vals() {
    if (VisitDepth.#vals == null) {
      VisitDepth.#vals = sys.List.make(VisitDepth.type$, [
        VisitDepth.make(0, "typeDef", ),
        VisitDepth.make(1, "slotDef", ),
        VisitDepth.make(2, "stmt", ),
        VisitDepth.make(3, "expr", ),
      ]).toImmutable();
    }
    return VisitDepth.#vals;
  }

  static static$init() {
    const $_u65 = VisitDepth.vals();
    if (true) {
    }
    ;
    return;
  }

}

class ExprVisitor extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ExprVisitor.type$; }

  visitMethodDef() { return Visitor.prototype.visitMethodDef.apply(this, arguments); }

  visitTypeDef() { return Visitor.prototype.visitTypeDef.apply(this, arguments); }

  enterBlock() { return Visitor.prototype.enterBlock.apply(this, arguments); }

  visitBlock() { return Visitor.prototype.visitBlock.apply(this, arguments); }

  visitFieldDef() { return Visitor.prototype.visitFieldDef.apply(this, arguments); }

  exitTypeDef() { return Visitor.prototype.exitTypeDef.apply(this, arguments); }

  exitBlock() { return Visitor.prototype.exitBlock.apply(this, arguments); }

  exitFieldDef() { return Visitor.prototype.exitFieldDef.apply(this, arguments); }

  enterFinally() { return Visitor.prototype.enterFinally.apply(this, arguments); }

  enterMethodDef() { return Visitor.prototype.enterMethodDef.apply(this, arguments); }

  enterStmt() { return Visitor.prototype.enterStmt.apply(this, arguments); }

  exitFinally() { return Visitor.prototype.exitFinally.apply(this, arguments); }

  enterUnit() { return Visitor.prototype.enterUnit.apply(this, arguments); }

  exitMethodDef() { return Visitor.prototype.exitMethodDef.apply(this, arguments); }

  enterTypeDef() { return Visitor.prototype.enterTypeDef.apply(this, arguments); }

  enterFieldDef() { return Visitor.prototype.enterFieldDef.apply(this, arguments); }

  exitUnit() { return Visitor.prototype.exitUnit.apply(this, arguments); }

  visitStmt() { return Visitor.prototype.visitStmt.apply(this, arguments); }

  walk() { return Visitor.prototype.walk.apply(this, arguments); }

  exitStmt() { return Visitor.prototype.exitStmt.apply(this, arguments); }

  #func = null;

  func(it) {
    if (it === undefined) {
      return this.#func;
    }
    else {
      this.#func = it;
      return;
    }
  }

  static make(func) {
    const $self = new ExprVisitor();
    ExprVisitor.make$($self,func);
    return $self;
  }

  static make$($self,func) {
    $self.#func = func;
    return;
  }

  visitExpr(expr) {
    return sys.ObjUtil.coerce(sys.Func.call(this.#func, expr), Expr.type$);
  }

}

class CField {
  constructor() {
    const this$ = this;
  }

  typeof() { return CField.type$; }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  ns() { return CSlot.prototype.ns.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  fieldType() {
    return this.type();
  }

  isCovariant() {
    return (this.isOverride() && sys.ObjUtil.compareNE(this.type(), this.inheritedReturns()));
  }

  isGeneric() {
    return this.type().isGenericParameter();
  }

  isParameterized() {
    return false;
  }

  usesBridge() {
    if (this.bridge() != null) {
      return this.bridge();
    }
    ;
    return this.type().bridge();
  }

}

class FieldDef extends SlotDef {
  constructor() {
    super();
    const this$ = this;
    this.#walkInit = true;
    return;
  }

  typeof() { return FieldDef.type$; }

  isParameterized() { return CField.prototype.isParameterized.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  fieldType() { return CField.prototype.fieldType.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  usesBridge() { return CField.prototype.usesBridge.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isGeneric() { return CField.prototype.isGeneric.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isCovariant() { return CField.prototype.isCovariant.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  #type = null;

  type(it) {
    if (it === undefined) {
      return this.#type;
    }
    else {
      this.#type = it;
      return;
    }
  }

  #field = null;

  field(it) {
    if (it === undefined) {
      return this.#field;
    }
    else {
      this.#field = it;
      return;
    }
  }

  #init = null;

  init(it) {
    if (it === undefined) {
      return this.#init;
    }
    else {
      this.#init = it;
      return;
    }
  }

  #walkInit = false;

  walkInit(it) {
    if (it === undefined) {
      return this.#walkInit;
    }
    else {
      this.#walkInit = it;
      return;
    }
  }

  #get = null;

  get(it) {
    if (it === undefined) {
      return this.#get;
    }
    else {
      this.#get = it;
      return;
    }
  }

  #set = null;

  set(it) {
    if (it === undefined) {
      return this.#set;
    }
    else {
      this.#set = it;
      return;
    }
  }

  #concreteBase = null;

  concreteBase(it) {
    if (it === undefined) {
      return this.#concreteBase;
    }
    else {
      this.#concreteBase = it;
      return;
    }
  }

  #inheritedRet = null;

  inheritedRet(it) {
    if (it === undefined) {
      return this.#inheritedRet;
    }
    else {
      this.#inheritedRet = it;
      return;
    }
  }

  #requiresNullCheck = false;

  requiresNullCheck(it) {
    if (it === undefined) {
      return this.#requiresNullCheck;
    }
    else {
      this.#requiresNullCheck = it;
      return;
    }
  }

  #enumDef = null;

  enumDef(it) {
    if (it === undefined) {
      return this.#enumDef;
    }
    else {
      this.#enumDef = it;
      return;
    }
  }

  #closureInfo = null;

  closureInfo(it) {
    if (it === undefined) {
      return this.#closureInfo;
    }
    else {
      this.#closureInfo = it;
      return;
    }
  }

  static make(loc,parent,name,flags) {
    const $self = new FieldDef();
    FieldDef.make$($self,loc,parent,name,flags);
    return $self;
  }

  static make$($self,loc,parent,name,flags) {
    if (name === undefined) name = "?";
    if (flags === undefined) flags = 0;
    SlotDef.make$($self, loc, parent);
    ;
    $self.name(name);
    $self.flags(flags);
    $self.type(sys.ObjUtil.coerce(parent.ns().errType(), CType.type$));
    return;
  }

  hasGet() {
    return (this.#get != null && !this.#get.isSynthetic());
  }

  hasSet() {
    return (this.#set != null && !this.#set.isSynthetic());
  }

  makeAccessorExpr(loc,useAccessor) {
    let target = null;
    if (this.isStatic()) {
      (target = StaticTargetExpr.make(loc, this.parent()));
    }
    else {
      (target = ThisExpr.make(loc));
    }
    ;
    return FieldExpr.make(loc, target, this, useAccessor);
  }

  signature() {
    return this.qname();
  }

  getter() {
    return this.#get;
  }

  setter() {
    return this.#set;
  }

  inheritedReturns() {
    if (this.#inheritedRet != null) {
      return sys.ObjUtil.coerce(this.#inheritedRet, CType.type$);
    }
    else {
      return this.type();
    }
    ;
  }

  walk(v,depth) {
    v.enterFieldDef(this);
    this.walkFacets(v, depth);
    if ((sys.ObjUtil.compareGE(depth, VisitDepth.expr()) && this.#init != null && this.#walkInit)) {
      this.#init = this.#init.walk(v);
    }
    ;
    v.visitFieldDef(this);
    v.exitFieldDef(this);
    return;
  }

  print(out) {
    this.printFacets(out);
    out.flags(this.flags());
    out.w(this.type()).w(" ");
    out.w(this.name());
    if (this.#init != null) {
      out.w(" := ");
      this.#init.print(out);
    }
    ;
    out.nl().nl();
    return;
  }

}

class CPod {
  constructor() {
    const this$ = this;
  }

  typeof() { return CPod.type$; }

  hasJs() {
    return (sys.ObjUtil.equals(this.meta().get("pod.js"), "true") || sys.ObjUtil.equals(this.name(), "sys"));
  }

  isForeign() {
    return false;
  }

  bridge() {
    return null;
  }

  hash() {
    return sys.Str.hash(this.name());
  }

  equals(t) {
    if (this === t) {
      return true;
    }
    ;
    let that = sys.ObjUtil.as(t, CPod.type$);
    if (that == null) {
      return false;
    }
    ;
    return sys.ObjUtil.equals(this.name(), that.name());
  }

  toStr() {
    return this.name();
  }

}

class PodDef extends Node {
  constructor() {
    super();
    const this$ = this;
    this.#meta = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("sys::Str"));
    this.#index = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("sys::Obj"));
    return;
  }

  typeof() { return PodDef.type$; }

  toStr() { return CPod.prototype.toStr.apply(this, arguments); }

  hasJs() { return CPod.prototype.hasJs.apply(this, arguments); }

  isForeign() { return CPod.prototype.isForeign.apply(this, arguments); }

  equals() { return CPod.prototype.equals.apply(this, arguments); }

  bridge() { return CPod.prototype.bridge.apply(this, arguments); }

  hash() { return CPod.prototype.hash.apply(this, arguments); }

  #ns = null;

  ns(it) {
    if (it === undefined) {
      return this.#ns;
    }
    else {
      this.#ns = it;
      return;
    }
  }

  #name = null;

  name() { return this.#name; }

  __name(it) { if (it === undefined) return this.#name; else this.#name = it; }

  #meta = null;

  meta(it) {
    if (it === undefined) {
      return this.#meta;
    }
    else {
      this.#meta = it;
      return;
    }
  }

  #index = null;

  index(it) {
    if (it === undefined) {
      return this.#index;
    }
    else {
      this.#index = it;
      return;
    }
  }

  #units = null;

  units(it) {
    if (it === undefined) {
      return this.#units;
    }
    else {
      this.#units = it;
      return;
    }
  }

  #typeDefs = null;

  typeDefs(it) {
    if (it === undefined) {
      return this.#typeDefs;
    }
    else {
      this.#typeDefs = it;
      return;
    }
  }

  static make(ns,loc,name) {
    const $self = new PodDef();
    PodDef.make$($self,ns,loc,name);
    return $self;
  }

  static make$($self,ns,loc,name) {
    Node.make$($self, loc);
    ;
    $self.ns(ns);
    $self.#name = name;
    $self.#units = sys.List.make(CompilationUnit.type$);
    return;
  }

  version() {
    throw sys.UnsupportedErr.make("PodDef.version");
  }

  depends() {
    return this.ns().compiler().depends();
  }

  file() {
    throw sys.UnsupportedErr.make();
  }

  resolveType(name,checked) {
    let t = this.#typeDefs.get(name);
    if (t != null) {
      return t;
    }
    ;
    if (checked) {
      throw sys.UnknownTypeErr.make(sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#name), "::"), name));
    }
    ;
    return null;
  }

  types() {
    return this.#typeDefs.vals();
  }

  print(out) {
    const this$ = this;
    out.nl();
    out.w("======================================").nl();
    out.w(sys.Str.plus("pod ", this.#name)).nl();
    out.w("======================================").nl();
    this.#units.each((unit) => {
      unit.print(out);
      return;
    });
    out.nl();
    return;
  }

}

class CParam {
  constructor() {
    const this$ = this;
  }

  typeof() { return CParam.type$; }

  paramType() {
    return this.type();
  }

}

class ParamDef extends Node {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ParamDef.type$; }

  paramType() { return CParam.prototype.paramType.apply(this, arguments); }

  #type = null;

  type(it) {
    if (it === undefined) {
      return this.#type;
    }
    else {
      this.#type = it;
      return;
    }
  }

  #name = null;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #def = null;

  def(it) {
    if (it === undefined) {
      return this.#def;
    }
    else {
      this.#def = it;
      return;
    }
  }

  static make(loc,type,name,def) {
    const $self = new ParamDef();
    ParamDef.make$($self,loc,type,name,def);
    return $self;
  }

  static make$($self,loc,type,name,def) {
    if (def === undefined) def = null;
    Node.make$($self, loc);
    $self.type(type);
    $self.name(name);
    $self.#def = def;
    return;
  }

  hasDefault() {
    return this.#def != null;
  }

  isAssign() {
    return (this.#def != null && this.#def.id() === ExprId.assign());
  }

  toStr() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.type()), " "), this.name());
  }

  print(out) {
    out.w(this.type()).w(" ").w(this.name());
    if (this.#def != null) {
      out.w(" := ");
      this.#def.print(out);
    }
    ;
    return;
  }

}

class CMethod {
  constructor() {
    const this$ = this;
  }

  typeof() { return CMethod.type$; }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  ns() { return CSlot.prototype.ns.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  returnType() {
    return this.returns();
  }

  inheritedReturnType() {
    return this.inheritedReturns();
  }

  isCovariant() {
    return (this.isOverride() && !this.returns().isThis() && sys.ObjUtil.compareNE(this.returns(), this.inheritedReturns()));
  }

  usesBridge() {
    const this$ = this;
    if (this.bridge() != null) {
      return this.bridge();
    }
    ;
    if (this.returns().bridge() != null) {
      return this.returns().bridge();
    }
    ;
    return sys.ObjUtil.coerce(this.params().eachWhile((p) => {
      return p.type().bridge();
    }), CBridge.type$.toNullable());
  }

  isGeneric() {
    return false;
  }

  isParameterized() {
    return false;
  }

  generic() {
    return null;
  }

  static calcGeneric(m) {
    const this$ = this;
    if (!m.parent().isGeneric()) {
      return false;
    }
    ;
    let isGeneric = m.returns().isGenericParameter();
    m.params().each((p) => {
      (isGeneric = (isGeneric || p.type().isGenericParameter()));
      return;
    });
    return isGeneric;
  }

  nameAndParamTypesToStr() {
    const this$ = this;
    return sys.Str.plus(sys.Str.plus(sys.Str.plus(this.name(), "("), this.params().join(", ", (p) => {
      return p.type().inferredAs().signature();
    })), ")");
  }

  hasSameParams(that) {
    let a = this.params();
    let b = that.params();
    if (sys.ObjUtil.compareNE(a.size(), b.size())) {
      return false;
    }
    ;
    for (let i = 0; sys.ObjUtil.compareLT(i, a.size()); i = sys.Int.increment(i)) {
      if (sys.ObjUtil.compareNE(a.get(i).type(), b.get(i).type())) {
        return false;
      }
      ;
    }
    ;
    return true;
  }

}

class MethodDef extends SlotDef {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return MethodDef.type$; }

  isParameterized() { return CMethod.prototype.isParameterized.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  hasSameParams() { return CMethod.prototype.hasSameParams.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  inheritedReturnType() { return CMethod.prototype.inheritedReturnType.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  nameAndParamTypesToStr() { return CMethod.prototype.nameAndParamTypesToStr.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  returnType() { return CMethod.prototype.returnType.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  usesBridge() { return CMethod.prototype.usesBridge.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isGeneric() { return CMethod.prototype.isGeneric.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  generic() { return CMethod.prototype.generic.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isCovariant() { return CMethod.prototype.isCovariant.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  #transpileTempCount = 0;

  // private field reflection only
  __transpileTempCount(it) { if (it === undefined) return this.#transpileTempCount; else this.#transpileTempCount = it; }

  #returns = null;

  returns(it) {
    if (it === undefined) {
      return this.#returns;
    }
    else {
      this.#returns = it;
      return;
    }
  }

  #ret = null;

  ret(it) {
    if (it === undefined) {
      return this.returns();
    }
    else {
      this.returns(it);
      return;
    }
  }

  #inheritedRet = null;

  inheritedRet(it) {
    if (it === undefined) {
      return this.#inheritedRet;
    }
    else {
      this.#inheritedRet = it;
      return;
    }
  }

  #paramDefs = null;

  paramDefs(it) {
    if (it === undefined) {
      return this.#paramDefs;
    }
    else {
      this.#paramDefs = it;
      return;
    }
  }

  #code = null;

  code(it) {
    if (it === undefined) {
      return this.#code;
    }
    else {
      this.#code = it;
      return;
    }
  }

  #ctorChain = null;

  ctorChain(it) {
    if (it === undefined) {
      return this.#ctorChain;
    }
    else {
      this.#ctorChain = it;
      return;
    }
  }

  #vars = null;

  vars(it) {
    if (it === undefined) {
      return this.#vars;
    }
    else {
      this.#vars = it;
      return;
    }
  }

  #accessorFor = null;

  accessorFor(it) {
    if (it === undefined) {
      return this.#accessorFor;
    }
    else {
      this.#accessorFor = it;
      return;
    }
  }

  #usesCvars = false;

  usesCvars(it) {
    if (it === undefined) {
      return this.#usesCvars;
    }
    else {
      this.#usesCvars = it;
      return;
    }
  }

  static makeStaticInit(loc,parent,block) {
    let def = MethodDef.make(loc, parent);
    def.name("static\$init");
    def.flags(sys.Int.plus(sys.Int.plus(FConst.Private(), FConst.Static()), FConst.Synthetic()));
    def.returns(sys.ObjUtil.coerce(parent.ns().voidType(), CType.type$));
    def.#code = block;
    return def;
  }

  static makeInstanceInit(loc,parent,block) {
    let def = MethodDef.make(loc, parent);
    def.name(sys.Str.plus(sys.Str.plus(sys.Str.plus("instance\$init\$", parent.pod().name()), "\$"), parent.name()));
    def.flags(sys.Int.plus(FConst.Private(), FConst.Synthetic()));
    def.returns(sys.ObjUtil.coerce(parent.ns().voidType(), CType.type$));
    def.#code = block;
    return def;
  }

  static make(loc,parent,name,flags) {
    const $self = new MethodDef();
    MethodDef.make$($self,loc,parent,name,flags);
    return $self;
  }

  static make$($self,loc,parent,name,flags) {
    if (name === undefined) name = "?";
    if (flags === undefined) flags = 0;
    SlotDef.make$($self, loc, parent);
    $self.name(name);
    $self.flags(flags);
    $self.returns(sys.ObjUtil.coerce(parent.ns().error(), CType.type$));
    $self.#paramDefs = sys.List.make(ParamDef.type$);
    $self.#vars = sys.List.make(MethodVar.type$);
    return;
  }

  isStaticInit() {
    return sys.ObjUtil.equals(this.name(), "static\$init");
  }

  static isNameStaticInit(name) {
    return sys.ObjUtil.equals(name, "static\$init");
  }

  isInstanceInit() {
    return sys.Str.startsWith(this.name(), "instance\$init\$");
  }

  static isNameInstanceInit(name) {
    return sys.Str.startsWith(name, "instance\$init\$");
  }

  isFieldAccessor() {
    return this.#accessorFor != null;
  }

  isFieldSetter() {
    return (this.#accessorFor != null && sys.ObjUtil.equals(this.#paramDefs.size(), 1));
  }

  isItBlockCtor() {
    if ((!this.isCtor() || this.params().isEmpty())) {
      return false;
    }
    ;
    let lastArg = sys.ObjUtil.as(this.params().last().type().deref().toNonNullable(), FuncType.type$);
    if ((lastArg == null || sys.ObjUtil.compareNE(lastArg.params().size(), 1))) {
      return false;
    }
    ;
    return true;
  }

  addLocalVarForDef(def,scope) {
    let var$ = this.addLocalVar(sys.ObjUtil.coerce(def.ctype(), CType.type$), def.name(), scope);
    var$.isCatchVar(def.isCatchVar());
    return var$;
  }

  addLocalVar(ctype,name,scope) {
    let reg = this.#vars.size();
    if (!this.isStatic()) {
      ((this$) => { let $_u66 = reg;reg = sys.Int.increment(reg); return $_u66; })(this);
    }
    ;
    if (name == null) {
      (name = sys.Str.plus("\$temp", sys.ObjUtil.coerce(reg, sys.Obj.type$.toNullable())));
    }
    ;
    let var$ = MethodVar.make(this, reg, ctype, sys.ObjUtil.coerce(name, sys.Str.type$), 0, scope);
    this.#vars.add(var$);
    return var$;
  }

  getOrAddShadowVar(binding,scope) {
    const this$ = this;
    let name = binding.name();
    let dup = this.#vars.find((v) => {
      return sys.ObjUtil.equals(v.name(), name);
    });
    if (dup != null) {
      return sys.ObjUtil.coerce(dup, MethodVar.type$);
    }
    ;
    let shadow = this.addLocalVar(binding.ctype(), name, scope);
    shadow.usedInClosure(true);
    shadow.shadows(binding);
    return shadow;
  }

  addParamVar(ctype,name) {
    if ((sys.ObjUtil.compareGT(this.#vars.size(), 0) && !this.#vars.get(-1).isParam())) {
      throw sys.Err.make(sys.Str.plus("Add param with locals ", this.qname()));
    }
    ;
    let param = ParamDef.make(this.loc(), ctype, name);
    this.params().add(param);
    let var$ = MethodVar.makeForParam(this, this.params().size(), param, ctype);
    this.#vars.add(var$);
    return var$;
  }

  transpileTempVar() {
    let n = sys.Str.plus("_temp", sys.ObjUtil.coerce(this.#transpileTempCount, sys.Obj.type$.toNullable()));
    ((this$) => { let $_u67 = this$.#transpileTempCount;this$.#transpileTempCount = sys.Int.increment(this$.#transpileTempCount); return $_u67; })(this);
    return n;
  }

  signature() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus(this.qname(), "("), this.params().join(",")), ")");
  }

  inheritedReturns() {
    if (this.#inheritedRet != null) {
      return sys.ObjUtil.coerce(this.#inheritedRet, CType.type$);
    }
    else {
      return this.returns();
    }
    ;
  }

  params() {
    return this.#paramDefs;
  }

  walk(v,depth) {
    const this$ = this;
    v.enterMethodDef(this);
    this.walkFacets(v, depth);
    if (sys.ObjUtil.compareGE(depth, VisitDepth.stmt())) {
      if (sys.ObjUtil.compareGE(depth, VisitDepth.expr())) {
        if (this.#ctorChain != null) {
          this.#ctorChain = sys.ObjUtil.coerce(this.#ctorChain.walk(v), CallExpr.type$);
        }
        ;
        this.#paramDefs.each((p) => {
          if (p.def() != null) {
            p.def(p.def().walk(v));
          }
          ;
          return;
        });
      }
      ;
      if (this.#code != null) {
        this.#code.walk(v, depth);
      }
      ;
    }
    ;
    v.visitMethodDef(this);
    v.exitMethodDef(this);
    return;
  }

  print(out) {
    const this$ = this;
    this.printFacets(out);
    out.flags(this.flags()).w(this.returns()).w(" ").w(this.name()).w("(");
    this.#paramDefs.each((p,i) => {
      if (sys.ObjUtil.compareGT(i, 0)) {
        out.w(", ");
      }
      ;
      p.print(out);
      return;
    });
    out.w(")").nl();
    if (this.#ctorChain != null) {
      out.w(" : ");
      this.#ctorChain.print(out);
      out.nl();
    }
    ;
    if (this.#code != null) {
      this.#code.print(out);
    }
    ;
    out.nl();
    return;
  }

}

class TypeRef extends Node {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return TypeRef.type$; }

  isNum() { return CType.prototype.isNum.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CType.prototype.isSynthetic.apply(this, arguments); }

  fitsAny() { return CType.prototype.fitsAny.apply(this, arguments); }

  isNative() { return CType.prototype.isNative.apply(this, arguments); }

  isConstFieldType() { return CType.prototype.isConstFieldType.apply(this, arguments); }

  isObj() { return CType.prototype.isObj.apply(this, arguments); }

  isBool() { return CType.prototype.isBool.apply(this, arguments); }

  isBuf() { return CType.prototype.isBuf.apply(this, arguments); }

  isInt() { return CType.prototype.isInt.apply(this, arguments); }

  isDecimal() { return CType.prototype.isDecimal.apply(this, arguments); }

  isFacet() { return CType.prototype.isFacet.apply(this, arguments); }

  isNothing() { return CType.prototype.isNothing.apply(this, arguments); }

  toStr() { return CType.prototype.toStr.apply(this, arguments); }

  foreignInheritance() { return CType.prototype.foreignInheritance.apply(this, arguments); }

  raw() { return CType.prototype.raw.apply(this, arguments); }

  isSupported() { return CType.prototype.isSupported.apply(this, arguments); }

  isFunc() { return CType.prototype.isFunc.apply(this, arguments); }

  isType() { return CType.prototype.isType.apply(this, arguments); }

  isMap() { return CType.prototype.isMap.apply(this, arguments); }

  isInternal() { return CType.prototype.isInternal.apply(this, arguments); }

  hasSlot() { return CType.prototype.hasSlot.apply(this, arguments); }

  bridge() { return CType.prototype.bridge.apply(this, arguments); }

  fields() { return CType.prototype.fields.apply(this, arguments); }

  hash() { return CType.prototype.hash.apply(this, arguments); }

  isThis() { return CType.prototype.isThis.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  instanceCtors() { return CType.prototype.instanceCtors.apply(this, arguments); }

  methods() { return CType.prototype.methods.apply(this, arguments); }

  isList() { return CType.prototype.isList.apply(this, arguments); }

  isConst() { return CType.prototype.isConst.apply(this, arguments); }

  isPublic() { return CType.prototype.isPublic.apply(this, arguments); }

  isStr() { return CType.prototype.isStr.apply(this, arguments); }

  isFinal() { return CType.prototype.isFinal.apply(this, arguments); }

  isClass() { return CType.prototype.isClass.apply(this, arguments); }

  isEnum() { return CType.prototype.isEnum.apply(this, arguments); }

  isFloat() { return CType.prototype.isFloat.apply(this, arguments); }

  isMixin() { return CType.prototype.isMixin.apply(this, arguments); }

  isAbstract() { return CType.prototype.isAbstract.apply(this, arguments); }

  isRange() { return CType.prototype.isRange.apply(this, arguments); }

  isVoid() { return CType.prototype.isVoid.apply(this, arguments); }

  ctors() { return CType.prototype.ctors.apply(this, arguments); }

  equals() { return CType.prototype.equals.apply(this, arguments); }

  #t = null;

  t() {
    return this.#t;
  }

  static make(loc,t) {
    const $self = new TypeRef();
    TypeRef.make$($self,loc,t);
    return $self;
  }

  static make$($self,loc,t) {
    Node.make$($self, loc);
    $self.#t = t;
    return;
  }

  ns() {
    return this.#t.ns();
  }

  doc() {
    return this.#t.doc();
  }

  pod() {
    return this.#t.pod();
  }

  name() {
    return this.#t.name();
  }

  qname() {
    return this.#t.qname();
  }

  signature() {
    return this.#t.signature();
  }

  deref() {
    return this.#t;
  }

  isForeign() {
    return this.#t.isForeign();
  }

  flags() {
    return this.#t.flags();
  }

  facet(qname) {
    return this.#t.facet(qname);
  }

  base() {
    return this.#t.base();
  }

  mixins() {
    return this.#t.mixins();
  }

  fits(that) {
    return this.#t.fits(that);
  }

  isValid() {
    return this.#t.isValid();
  }

  isVal() {
    return this.#t.isVal();
  }

  isNullable() {
    return this.#t.isNullable();
  }

  toNullable() {
    return this.#t.toNullable();
  }

  toNonNullable() {
    return this.#t.toNonNullable();
  }

  inferredAs() {
    return this.#t.inferredAs();
  }

  isGeneric() {
    return this.#t.isGeneric();
  }

  isParameterized() {
    return this.#t.isParameterized();
  }

  isGenericParameter() {
    return this.#t.isGenericParameter();
  }

  parameterizeThis(thisType) {
    return this.#t.parameterizeThis(thisType);
  }

  toListOf() {
    return this.#t.toListOf();
  }

  slot(name) {
    return this.#t.slot(name);
  }

  field(name) {
    return this.#t.field(name);
  }

  method(name) {
    return this.#t.method(name);
  }

  slots() {
    return this.#t.slots();
  }

  operators() {
    return this.#t.operators();
  }

  print(out) {
    out.w(this.#t.toStr());
    return;
  }

}

class AstWriter extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#indentation = 0;
    this.#needIndent = false;
    return;
  }

  typeof() { return AstWriter.type$; }

  #out = null;

  out(it) {
    if (it === undefined) {
      return this.#out;
    }
    else {
      this.#out = it;
      return;
    }
  }

  #indentation = 0;

  indentation(it) {
    if (it === undefined) {
      return this.#indentation;
    }
    else {
      this.#indentation = it;
      return;
    }
  }

  #needIndent = false;

  needIndent(it) {
    if (it === undefined) {
      return this.#needIndent;
    }
    else {
      this.#needIndent = it;
      return;
    }
  }

  static make(out) {
    const $self = new AstWriter();
    AstWriter.make$($self,out);
    return $self;
  }

  static make$($self,out) {
    if (out === undefined) out = sys.Env.cur().out();
    ;
    $self.#out = out;
    return;
  }

  w(o) {
    if (this.#needIndent) {
      this.#out.writeChars(sys.Str.spaces(sys.Int.mult(this.#indentation, 2)));
      this.#needIndent = false;
    }
    ;
    this.#out.writeChars(sys.ObjUtil.toStr(o));
    return this;
  }

  nl() {
    this.w("\n");
    this.#needIndent = true;
    this.#out.flush();
    return this;
  }

  indent() {
    ((this$) => { let $_u68 = this$.#indentation;this$.#indentation = sys.Int.increment(this$.#indentation); return $_u68; })(this);
    return this;
  }

  unindent() {
    ((this$) => { let $_u69 = this$.#indentation;this$.#indentation = sys.Int.decrement(this$.#indentation); return $_u69; })(this);
    if (sys.ObjUtil.compareLT(this.#indentation, 0)) {
      this.#indentation = 0;
    }
    ;
    return this;
  }

  flags(flags) {
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Public()), 0)) {
      this.w("public ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Protected()), 0)) {
      this.w("protected ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Private()), 0)) {
      this.w("private ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Internal()), 0)) {
      this.w("internal ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Native()), 0)) {
      this.w("native ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Enum()), 0)) {
      this.w("enum ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Mixin()), 0)) {
      this.w("mixin ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Final()), 0)) {
      this.w("final ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Ctor()), 0)) {
      this.w("new ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Override()), 0)) {
      this.w("override ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0)) {
      this.w("abstract ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Static()), 0)) {
      this.w("static ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Storage()), 0)) {
      this.w("storage ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Virtual()), 0)) {
      this.w("virtual ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Once()), 0)) {
      this.w("once ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Synthetic()), 0)) {
      this.w("synthetic ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Getter()), 0)) {
      this.w("getter ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Setter()), 0)) {
      this.w("setter ");
    }
    ;
    return this;
  }

  static flagsToStr(flags) {
    let s = sys.StrBuf.make();
    let w = AstWriter.make(s.out()).flags(flags);
    return s.toStr();
  }

}

class MethodVar extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return MethodVar.type$; }

  #method = null;

  method(it) {
    if (it === undefined) {
      return this.#method;
    }
    else {
      this.#method = it;
      return;
    }
  }

  #register = 0;

  register(it) {
    if (it === undefined) {
      return this.#register;
    }
    else {
      this.#register = it;
      return;
    }
  }

  #ctype = null;

  ctype(it) {
    if (it === undefined) {
      return this.#ctype;
    }
    else {
      this.#ctype = it;
      return;
    }
  }

  #name = null;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #flags = 0;

  flags(it) {
    if (it === undefined) {
      return this.#flags;
    }
    else {
      this.#flags = it;
      return;
    }
  }

  #isCatchVar = false;

  isCatchVar(it) {
    if (it === undefined) {
      return this.#isCatchVar;
    }
    else {
      this.#isCatchVar = it;
      return;
    }
  }

  #scope = null;

  scope(it) {
    if (it === undefined) {
      return this.#scope;
    }
    else {
      this.#scope = it;
      return;
    }
  }

  #paramDef = null;

  paramDef(it) {
    if (it === undefined) {
      return this.#paramDef;
    }
    else {
      this.#paramDef = it;
      return;
    }
  }

  #usedInClosure = false;

  usedInClosure(it) {
    if (it === undefined) {
      return this.#usedInClosure;
    }
    else {
      this.#usedInClosure = it;
      return;
    }
  }

  #shadows = null;

  shadows(it) {
    if (it === undefined) {
      return this.#shadows;
    }
    else {
      this.#shadows = it;
      return;
    }
  }

  #wrapField = null;

  wrapField(it) {
    if (it === undefined) {
      return this.#wrapField;
    }
    else {
      this.#wrapField = it;
      return;
    }
  }

  #isReassigned = false;

  isReassigned(it) {
    if (it === undefined) {
      return this.#isReassigned;
    }
    else {
      this.#isReassigned = it;
      return;
    }
  }

  #paramWrapper = null;

  paramWrapper(it) {
    if (it === undefined) {
      return this.#paramWrapper;
    }
    else {
      this.#paramWrapper = it;
      return;
    }
  }

  static make(method,register,ctype,name,flags,scope) {
    const $self = new MethodVar();
    MethodVar.make$($self,method,register,ctype,name,flags,scope);
    return $self;
  }

  static make$($self,method,register,ctype,name,flags,scope) {
    if (flags === undefined) flags = 0;
    if (scope === undefined) scope = null;
    $self.#method = method;
    $self.#register = register;
    $self.#ctype = ctype;
    $self.#name = name;
    $self.#flags = flags;
    $self.#scope = scope;
    return;
  }

  static makeForParam(method,register,p,paramType) {
    const $self = new MethodVar();
    MethodVar.makeForParam$($self,method,register,p,paramType);
    return $self;
  }

  static makeForParam$($self,method,register,p,paramType) {
    MethodVar.make$($self, method, register, paramType, p.name(), FConst.Param(), null);
    $self.#paramDef = p;
    return;
  }

  isParam() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.#flags, FConst.Param()), 0);
  }

  isWrapped() {
    return this.#wrapField != null;
  }

  toStr() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", sys.ObjUtil.coerce(this.#register, sys.Obj.type$.toNullable())), "  "), this.#name), ": "), this.#ctype);
  }

  reassigned() {
    this.#isReassigned = true;
    if (this.#shadows != null) {
      this.#shadows.reassigned();
    }
    ;
    return;
  }

}

class EnumDef extends Node {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return EnumDef.type$; }

  #doc = null;

  doc(it) {
    if (it === undefined) {
      return this.#doc;
    }
    else {
      this.#doc = it;
      return;
    }
  }

  #facets = null;

  facets(it) {
    if (it === undefined) {
      return this.#facets;
    }
    else {
      this.#facets = it;
      return;
    }
  }

  #ordinal = 0;

  ordinal(it) {
    if (it === undefined) {
      return this.#ordinal;
    }
    else {
      this.#ordinal = it;
      return;
    }
  }

  #name = null;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #ctorArgs = null;

  ctorArgs(it) {
    if (it === undefined) {
      return this.#ctorArgs;
    }
    else {
      this.#ctorArgs = it;
      return;
    }
  }

  static make(loc,doc,facets,name,ordinal) {
    const $self = new EnumDef();
    EnumDef.make$($self,loc,doc,facets,name,ordinal);
    return $self;
  }

  static make$($self,loc,doc,facets,name,ordinal) {
    Node.make$($self, loc);
    $self.#doc = doc;
    $self.#facets = facets;
    $self.#name = name;
    $self.#ordinal = ordinal;
    $self.#ctorArgs = sys.List.make(Expr.type$);
    return;
  }

  toStr() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus("", sys.ObjUtil.coerce(this.#ordinal, sys.Obj.type$.toNullable())), ":"), this.#name);
  }

  print(out) {
    out.w(this.#name);
    if (!this.#ctorArgs.isEmpty()) {
      out.w("(").w(this.#ctorArgs.join(", ")).w(")");
    }
    ;
    out.w("  // ").w(sys.ObjUtil.coerce(this.#ordinal, sys.Obj.type$)).nl();
    return;
  }

}

class DslPlugin extends CompilerSupport {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return DslPlugin.type$; }

  static find(c,loc,anchorType) {
    let qname = anchorType.qname();
    let $_u70 = qname;
    if (sys.ObjUtil.equals($_u70, "sys::Str")) {
      return StrDslPlugin.make(c.compiler());
    }
    else if (sys.ObjUtil.equals($_u70, "sys::Regex")) {
      return RegexDslPlugin.make(c.compiler());
    }
    ;
    let t = sys.Env.cur().index(sys.Str.plus("compiler.dsl.", qname));
    if (sys.ObjUtil.compareGT(t.size(), 1)) {
      c.err(sys.Str.plus(sys.Str.plus(sys.Str.plus("Multiple DSL plugins registered for '", qname), "': "), t), loc);
      return null;
    }
    ;
    if (sys.ObjUtil.equals(t.size(), 0)) {
      c.err(sys.Str.plus(sys.Str.plus("No DSL plugin is registered for '", qname), "'"), loc);
      return null;
    }
    ;
    try {
      return sys.ObjUtil.coerce(sys.Type.find(sys.ObjUtil.coerce(t.first(), sys.Str.type$)).make(sys.List.make(Compiler.type$, [c.compiler()])), DslPlugin.type$.toNullable());
    }
    catch ($_u71) {
      $_u71 = sys.Err.make($_u71);
      if ($_u71 instanceof sys.Err) {
        let e = $_u71;
        ;
        e.trace();
        c.errReport(CompilerErr.make(sys.Str.plus(sys.Str.plus("Cannot construct DSL plugin '", t.first()), "'"), loc, e));
        return null;
      }
      else {
        throw $_u71;
      }
    }
    ;
  }

  static make(c) {
    const $self = new DslPlugin();
    DslPlugin.make$($self,c);
    return $self;
  }

  static make$($self,c) {
    CompilerSupport.make$($self, c);
    return;
  }

  normalizeSrc(dsl) {
    let lines = sys.Str.splitLines(dsl.src());
    if (sys.ObjUtil.equals(lines.size(), 1)) {
      return dsl.src();
    }
    ;
    let s = sys.StrBuf.make();
    s.add(lines.get(0));
    for (let i = 1; sys.ObjUtil.compareLT(i, lines.size()); i = sys.Int.increment(i)) {
      s.addChar(10);
      let line = lines.get(i);
      let numTabs = dsl.leadingTabs();
      let numSpaces = dsl.leadingSpaces();
      let j = 0;
      for (; sys.ObjUtil.compareLT(j, sys.Str.size(line)); j = sys.Int.increment(j)) {
        let ch = sys.Str.get(line, j);
        if ((sys.ObjUtil.equals(ch, 9) || sys.ObjUtil.equals(ch, 32))) {
          if (sys.ObjUtil.equals(ch, 9)) {
            numTabs = sys.Int.decrement(numTabs);
          }
          else {
            numSpaces = sys.Int.decrement(numSpaces);
          }
          ;
          if ((sys.ObjUtil.equals(numTabs, 0) && sys.ObjUtil.equals(numSpaces, 0))) {
            break;
          }
          ;
          continue;
        }
        ;
        let loc = Loc.make(dsl.srcLoc().file(), sys.ObjUtil.coerce(sys.Int.plus(sys.ObjUtil.coerce(dsl.srcLoc().line(), sys.Int.type$), i), sys.Int.type$.toNullable()), sys.ObjUtil.coerce(sys.Int.plus(j, 1), sys.Int.type$.toNullable()));
        if (sys.ObjUtil.equals(dsl.leadingTabs(), 0)) {
          throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Leading space in ", dsl.anchorType().name()), " DSL must be "), sys.ObjUtil.coerce(dsl.leadingSpaces(), sys.Obj.type$.toNullable())), " spaces"), loc);
        }
        else {
          throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Leading space in ", dsl.anchorType().name()), " DSL must be "), sys.ObjUtil.coerce(dsl.leadingTabs(), sys.Obj.type$.toNullable())), " tabs and "), sys.ObjUtil.coerce(dsl.leadingSpaces(), sys.Obj.type$.toNullable())), " spaces"), loc);
        }
        ;
      }
      ;
      if (sys.ObjUtil.compareLT(j, sys.Str.size(line))) {
        s.add(sys.Str.getRange(line, sys.Range.make(sys.Int.plus(j, 1), -1)));
      }
      ;
    }
    ;
    return s.toStr();
  }

}

class RegexDslPlugin extends DslPlugin {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return RegexDslPlugin.type$; }

  static make(c) {
    const $self = new RegexDslPlugin();
    RegexDslPlugin.make$($self,c);
    return $self;
  }

  static make$($self,c) {
    DslPlugin.make$($self, c);
    return;
  }

  compile(dsl) {
    let regexType = this.ns().resolveType("sys::Regex");
    let fromStr = regexType.method("fromStr");
    let args = sys.List.make(Expr.type$, [Expr.makeForLiteral(dsl.loc(), this.ns(), dsl.src())]);
    return CallExpr.makeWithMethod(dsl.loc(), null, sys.ObjUtil.coerce(fromStr, CMethod.type$), args);
  }

}

class StrDslPlugin extends DslPlugin {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return StrDslPlugin.type$; }

  static make(c) {
    const $self = new StrDslPlugin();
    StrDslPlugin.make$($self,c);
    return $self;
  }

  static make$($self,c) {
    DslPlugin.make$($self, c);
    return;
  }

  compile(dsl) {
    return Expr.makeForLiteral(dsl.loc(), this.ns(), this.normalizeSrc(dsl));
  }

}

class FUtil extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FUtil.type$; }

  static FCodeVersion() { return FConst.FCodeVersion(); }

  static Enum() { return FConst.Enum(); }

  static LineNumberAttr() { return FConst.LineNumberAttr(); }

  static Synthetic() { return FConst.Synthetic(); }

  static LineNumbersAttr() { return FConst.LineNumbersAttr(); }

  static Once() { return FConst.Once(); }

  static Getter() { return FConst.Getter(); }

  static Internal() { return FConst.Internal(); }

  static SourceFileAttr() { return FConst.SourceFileAttr(); }

  static ErrTableAttr() { return FConst.ErrTableAttr(); }

  static Mixin() { return FConst.Mixin(); }

  static Native() { return FConst.Native(); }

  static Ctor() { return FConst.Ctor(); }

  static Private() { return FConst.Private(); }

  static Final() { return FConst.Final(); }

  static Override() { return FConst.Override(); }

  static FlagsMask() { return FConst.FlagsMask(); }

  static Facet() { return FConst.Facet(); }

  static FacetsAttr() { return FConst.FacetsAttr(); }

  static Protected() { return FConst.Protected(); }

  static Abstract() { return FConst.Abstract(); }

  static Const() { return FConst.Const(); }

  static Virtual() { return FConst.Virtual(); }

  static Public() { return FConst.Public(); }

  static Storage() { return FConst.Storage(); }

  static Setter() { return FConst.Setter(); }

  static Param() { return FConst.Param(); }

  static EnumOrdinalAttr() { return FConst.EnumOrdinalAttr(); }

  static Static() { return FConst.Static(); }

  static ParamDefaultAttr() { return FConst.ParamDefaultAttr(); }

  static writeBuf(out,buf) {
    if (buf == null) {
      out.writeI2(0);
    }
    else {
      out.writeI2(buf.size());
      out.writeBuf(sys.ObjUtil.coerce(sys.ObjUtil.coerce(buf.seek(0), sys.Buf.type$.toNullable()), sys.Buf.type$));
    }
    ;
    return;
  }

  static readBuf(in$) {
    let size = in$.readU2();
    if (sys.ObjUtil.equals(size, 0)) {
      return null;
    }
    ;
    return in$.readBufFully(null, size);
  }

  static writeAttrs(out,fattrs) {
    const this$ = this;
    if (fattrs == null) {
      out.writeI2(0);
    }
    else {
      out.writeI2(fattrs.size());
      fattrs.each((a) => {
        a.write(out);
        return;
      });
    }
    ;
    return;
  }

  static readAttrs(in$) {
    const this$ = this;
    let size = in$.readU2();
    if (sys.ObjUtil.equals(size, 0)) {
      return sys.ObjUtil.coerce(FAttr.type$.emptyList(), sys.Type.find("compiler::FAttr[]"));
    }
    ;
    let fattrs = sys.List.make(FAttr.type$);
    fattrs.capacity(size);
    sys.Int.times(size, (it) => {
      fattrs.add(FAttr.make().read(in$));
      return;
    });
    return fattrs;
  }

  static writeDoc(out,key,doc) {
    const this$ = this;
    if (doc == null) {
      return;
    }
    ;
    out.print(key).print("\n").print("  ");
    sys.Str.each(doc, (ch) => {
      if (sys.ObjUtil.equals(ch, 13)) {
        throw sys.ArgErr.make();
      }
      ;
      out.writeChar(ch);
      if (sys.ObjUtil.equals(ch, 10)) {
        out.print("  ");
      }
      ;
      return;
    });
    out.print("\n\n");
    return;
  }

  static make() {
    const $self = new FUtil();
    FUtil.make$($self);
    return $self;
  }

  static make$($self) {
    return;
  }

}

class FPod extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FPod.type$; }

  toStr() { return CPod.prototype.toStr.apply(this, arguments); }

  hasJs() { return CPod.prototype.hasJs.apply(this, arguments); }

  isForeign() { return CPod.prototype.isForeign.apply(this, arguments); }

  equals() { return CPod.prototype.equals.apply(this, arguments); }

  bridge() { return CPod.prototype.bridge.apply(this, arguments); }

  hash() { return CPod.prototype.hash.apply(this, arguments); }

  static FCodeVersion() { return FConst.FCodeVersion(); }

  static Enum() { return FConst.Enum(); }

  static LineNumberAttr() { return FConst.LineNumberAttr(); }

  static Synthetic() { return FConst.Synthetic(); }

  static LineNumbersAttr() { return FConst.LineNumbersAttr(); }

  static Once() { return FConst.Once(); }

  static Getter() { return FConst.Getter(); }

  static Internal() { return FConst.Internal(); }

  static SourceFileAttr() { return FConst.SourceFileAttr(); }

  static ErrTableAttr() { return FConst.ErrTableAttr(); }

  static Mixin() { return FConst.Mixin(); }

  static Native() { return FConst.Native(); }

  static Ctor() { return FConst.Ctor(); }

  static Private() { return FConst.Private(); }

  static Final() { return FConst.Final(); }

  static Override() { return FConst.Override(); }

  static FlagsMask() { return FConst.FlagsMask(); }

  static Facet() { return FConst.Facet(); }

  static FacetsAttr() { return FConst.FacetsAttr(); }

  static Protected() { return FConst.Protected(); }

  static Abstract() { return FConst.Abstract(); }

  static Const() { return FConst.Const(); }

  static Virtual() { return FConst.Virtual(); }

  static Public() { return FConst.Public(); }

  static Storage() { return FConst.Storage(); }

  static Setter() { return FConst.Setter(); }

  static Param() { return FConst.Param(); }

  static EnumOrdinalAttr() { return FConst.EnumOrdinalAttr(); }

  static Static() { return FConst.Static(); }

  static ParamDefaultAttr() { return FConst.ParamDefaultAttr(); }

  #ns = null;

  ns(it) {
    if (it === undefined) {
      return this.#ns;
    }
    else {
      this.#ns = it;
      return;
    }
  }

  #name = null;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #version = null;

  version(it) {
    if (it === undefined) {
      return this.#version;
    }
    else {
      this.#version = it;
      return;
    }
  }

  #depends = null;

  depends(it) {
    if (it === undefined) {
      return this.#depends;
    }
    else {
      this.#depends = it;
      return;
    }
  }

  #meta = null;

  meta(it) {
    if (it === undefined) {
      return this.#meta;
    }
    else {
      this.#meta = it;
      return;
    }
  }

  #index = null;

  index(it) {
    if (it === undefined) {
      return this.#index;
    }
    else {
      this.#index = it;
      return;
    }
  }

  #zip = null;

  zip(it) {
    if (it === undefined) {
      return this.#zip;
    }
    else {
      this.#zip = it;
      return;
    }
  }

  #ftypes = null;

  ftypes(it) {
    if (it === undefined) {
      return this.#ftypes;
    }
    else {
      this.#ftypes = it;
      return;
    }
  }

  #names = null;

  names(it) {
    if (it === undefined) {
      return this.#names;
    }
    else {
      this.#names = it;
      return;
    }
  }

  #typeRefs = null;

  typeRefs(it) {
    if (it === undefined) {
      return this.#typeRefs;
    }
    else {
      this.#typeRefs = it;
      return;
    }
  }

  #fieldRefs = null;

  fieldRefs(it) {
    if (it === undefined) {
      return this.#fieldRefs;
    }
    else {
      this.#fieldRefs = it;
      return;
    }
  }

  #methodRefs = null;

  methodRefs(it) {
    if (it === undefined) {
      return this.#methodRefs;
    }
    else {
      this.#methodRefs = it;
      return;
    }
  }

  #ints = null;

  ints(it) {
    if (it === undefined) {
      return this.#ints;
    }
    else {
      this.#ints = it;
      return;
    }
  }

  #floats = null;

  floats(it) {
    if (it === undefined) {
      return this.#floats;
    }
    else {
      this.#floats = it;
      return;
    }
  }

  #decimals = null;

  decimals(it) {
    if (it === undefined) {
      return this.#decimals;
    }
    else {
      this.#decimals = it;
      return;
    }
  }

  #strs = null;

  strs(it) {
    if (it === undefined) {
      return this.#strs;
    }
    else {
      this.#strs = it;
      return;
    }
  }

  #durations = null;

  durations(it) {
    if (it === undefined) {
      return this.#durations;
    }
    else {
      this.#durations = it;
      return;
    }
  }

  #uris = null;

  uris(it) {
    if (it === undefined) {
      return this.#uris;
    }
    else {
      this.#uris = it;
      return;
    }
  }

  #ftypesByName = null;

  ftypesByName(it) {
    if (it === undefined) {
      return this.#ftypesByName;
    }
    else {
      this.#ftypesByName = it;
      return;
    }
  }

  #onFile = null;

  onFile(it) {
    if (it === undefined) {
      return this.#onFile;
    }
    else {
      this.#onFile = it;
      return;
    }
  }

  static make(ns,podName,zip) {
    const $self = new FPod();
    FPod.make$($self,ns,podName,zip);
    return $self;
  }

  static make$($self,ns,podName,zip) {
    $self.ns(ns);
    $self.version(sys.Version.defVal());
    $self.depends(sys.List.make(CDepend.type$));
    $self.name(podName);
    $self.#zip = zip;
    $self.#names = FTable.makeStrs($self);
    $self.#typeRefs = FTable.makeTypeRefs($self);
    $self.#fieldRefs = FTable.makeFieldRefs($self);
    $self.#methodRefs = FTable.makeMethodRefs($self);
    $self.#ints = FTable.makeInts($self);
    $self.#floats = FTable.makeFloats($self);
    $self.#decimals = FTable.makeDecimals($self);
    $self.#strs = FTable.makeStrs($self);
    $self.#durations = FTable.makeDurations($self);
    $self.#uris = FTable.makeStrs($self);
    $self.meta(sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("sys::Str")));
    $self.#index = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("sys::Str"));
    return;
  }

  file() {
    return sys.ObjUtil.coerce(this.#zip.file(), sys.File.type$);
  }

  resolveType(name,checked) {
    let t = this.#ftypesByName.get(name);
    if (t != null) {
      return t;
    }
    ;
    if (checked) {
      throw sys.UnknownTypeErr.make(sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.name()), "::"), name));
    }
    ;
    return null;
  }

  types() {
    return sys.ObjUtil.coerce(this.#ftypes, sys.Type.find("compiler::CType[]"));
  }

  toType(index) {
    if (sys.ObjUtil.equals(index, 65535)) {
      return null;
    }
    ;
    let r = this.typeRef(index);
    let sig = ((this$) => { if (r.isGenericInstance()) return r.sig(); return sys.Str.plus(sys.Str.plus(sys.Str.plus(this$.n(r.podName()), "::"), this$.n(r.typeName())), r.sig()); })(this);
    return this.ns().resolveType(sig);
  }

  resolveTypes(indexes) {
    const this$ = this;
    return sys.ObjUtil.coerce(indexes.map((index) => {
      return sys.ObjUtil.coerce(this$.toType(index), CType.type$);
    }, CType.type$), sys.Type.find("compiler::CType[]"));
  }

  n(index) {
    return sys.ObjUtil.coerce(this.#names.get(index), sys.Str.type$);
  }

  typeRef(index) {
    return sys.ObjUtil.coerce(this.#typeRefs.get(index), FTypeRef.type$);
  }

  fieldRef(index) {
    return sys.ObjUtil.coerce(this.#fieldRefs.get(index), FFieldRef.type$);
  }

  methodRef(index) {
    return sys.ObjUtil.coerce(this.#methodRefs.get(index), FMethodRef.type$);
  }

  integer(index) {
    return sys.ObjUtil.coerce(this.#ints.get(index), sys.Int.type$);
  }

  float(index) {
    return sys.ObjUtil.coerce(this.#floats.get(index), sys.Float.type$);
  }

  decimal(index) {
    return sys.ObjUtil.coerce(this.#decimals.get(index), sys.Decimal.type$);
  }

  str(index) {
    return sys.ObjUtil.coerce(this.#strs.get(index), sys.Str.type$);
  }

  duration(index) {
    return sys.ObjUtil.coerce(this.#durations.get(index), sys.Duration.type$);
  }

  uri(index) {
    return sys.ObjUtil.coerce(this.#uris.get(index), sys.Str.type$);
  }

  typeRefStr(index) {
    return this.typeRef(index).format(this);
  }

  fieldRefStr(index) {
    return this.fieldRef(index).format(this);
  }

  methodRefStr(index) {
    return this.methodRef(index).format(this);
  }

  addName(val) {
    return this.#names.add(val);
  }

  addTypeRef(t) {
    let p = this.addName(t.pod().name());
    let n = this.addName(t.name());
    let sig = "";
    if (t.isParameterized()) {
      (sig = t.signature());
    }
    else {
      if (t.isNullable()) {
        (sig = "?");
      }
      ;
    }
    ;
    return this.#typeRefs.add(FTypeRef.make(p, n, sig));
  }

  addFieldRef(field) {
    let p = this.addTypeRef(field.parent());
    let n = this.addName(field.name());
    let t = this.addTypeRef(field.type());
    return this.#fieldRefs.add(FFieldRef.make(p, n, t));
  }

  addMethodRef(method,argCount) {
    if (argCount === undefined) argCount = null;
    const this$ = this;
    if (method.isParameterized()) {
      (method = sys.ObjUtil.coerce(method.generic(), CMethod.type$));
    }
    ;
    let p = this.addTypeRef(method.parent());
    let n = this.addName(method.name());
    let r = this.addTypeRef(method.inheritedReturns().raw());
    let params = sys.ObjUtil.coerce(method.params().map((x) => {
      return this$.addTypeRef(x.type().raw());
    }, sys.Int.type$), sys.Type.find("sys::Int[]"));
    if ((argCount != null && sys.ObjUtil.compareLT(argCount, params.size()))) {
      (params = params.getRange(sys.Range.make(0, sys.ObjUtil.coerce(argCount, sys.Int.type$), true)));
    }
    ;
    return this.#methodRefs.add(FMethodRef.make(p, n, r, params));
  }

  dump(out) {
    if (out === undefined) out = sys.Env.cur().out();
    let p = FPrinter.make(this, out);
    p.showCode(true);
    p.ftypes();
    return;
  }

  read() {
    const this$ = this;
    this.meta(this.in(sys.Uri.fromStr("/meta.props")).readProps());
    this.name(sys.ObjUtil.coerce(((this$) => { let $_u73 = this$.meta().get("pod.name"); if ($_u73 != null) return $_u73; throw sys.IOErr.make("Missing meta pod.name"); })(this), sys.Str.type$));
    this.version(sys.ObjUtil.coerce(sys.Version.fromStr(sys.ObjUtil.coerce(this.meta().get("pod.version"), sys.Str.type$)), sys.Version.type$));
    let d = this.meta().get("pod.depends");
    this.depends(sys.ObjUtil.coerce(((this$) => { if (sys.Str.isEmpty(d)) return sys.List.make(CDepend.type$); return sys.Str.split(d, sys.ObjUtil.coerce(59, sys.Int.type$.toNullable())).map((s) => {
      return CDepend.fromStr(s);
    }, CDepend.type$); })(this), sys.Type.find("compiler::CDepend[]")));
    this.#names.read(this.in(sys.Uri.fromStr("/fcode/names.def")));
    this.#typeRefs.read(this.in(sys.Uri.fromStr("/fcode/typeRefs.def")));
    this.#fieldRefs.read(this.in(sys.Uri.fromStr("/fcode/fieldRefs.def")));
    this.#methodRefs.read(this.in(sys.Uri.fromStr("/fcode/methodRefs.def")));
    this.#ints.read(this.in(sys.Uri.fromStr("/fcode/ints.def")));
    this.#floats.read(this.in(sys.Uri.fromStr("/fcode/floats.def")));
    this.#decimals.read(this.in(sys.Uri.fromStr("/fcode/decimals.def")));
    this.#strs.read(this.in(sys.Uri.fromStr("/fcode/strs.def")));
    this.#durations.read(this.in(sys.Uri.fromStr("/fcode/durations.def")));
    this.#uris.read(this.in(sys.Uri.fromStr("/fcode/uris.def")));
    let in$ = this.in(sys.Uri.fromStr("/fcode/types.def"));
    this.#ftypes = sys.List.make(FType.type$);
    this.#ftypesByName = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::FType"));
    if (in$ != null) {
      sys.Int.times(in$.readU2(), (it) => {
        let ftype = FType.make(this$).readMeta(sys.ObjUtil.coerce(in$, sys.InStream.type$));
        this$.#ftypes.add(ftype);
        this$.#ftypesByName.set(ftype.name(), ftype);
        this$.ns().typeCache().set(ftype.qname(), ftype);
        return;
      });
      in$.close();
    }
    ;
    return;
  }

  readFully() {
    const this$ = this;
    this.#ftypes.each((t) => {
      t.read();
      return;
    });
    return;
  }

  write(zip) {
    if (zip === undefined) zip = sys.ObjUtil.coerce(this.#zip, sys.Zip.type$);
    const this$ = this;
    this.doWrite((uri) => {
      return zip.writeNext(uri);
    });
    return;
  }

  doWrite(onFile) {
    const this$ = this;
    this.#onFile = onFile;
    this.writeMeta(sys.Uri.fromStr("/meta.props"));
    this.writeIndex(sys.Uri.fromStr("/index.props"));
    if (!this.#names.isEmpty()) {
      this.#names.write(this.out(sys.Uri.fromStr("/fcode/names.def")));
    }
    ;
    if (!this.#typeRefs.isEmpty()) {
      this.#typeRefs.write(this.out(sys.Uri.fromStr("/fcode/typeRefs.def")));
    }
    ;
    if (!this.#fieldRefs.isEmpty()) {
      this.#fieldRefs.write(this.out(sys.Uri.fromStr("/fcode/fieldRefs.def")));
    }
    ;
    if (!this.#methodRefs.isEmpty()) {
      this.#methodRefs.write(this.out(sys.Uri.fromStr("/fcode/methodRefs.def")));
    }
    ;
    if (!this.#ints.isEmpty()) {
      this.#ints.write(this.out(sys.Uri.fromStr("/fcode/ints.def")));
    }
    ;
    if (!this.#floats.isEmpty()) {
      this.#floats.write(this.out(sys.Uri.fromStr("/fcode/floats.def")));
    }
    ;
    if (!this.#decimals.isEmpty()) {
      this.#decimals.write(this.out(sys.Uri.fromStr("/fcode/decimals.def")));
    }
    ;
    if (!this.#strs.isEmpty()) {
      this.#strs.write(this.out(sys.Uri.fromStr("/fcode/strs.def")));
    }
    ;
    if (!this.#durations.isEmpty()) {
      this.#durations.write(this.out(sys.Uri.fromStr("/fcode/durations.def")));
    }
    ;
    if (!this.#uris.isEmpty()) {
      this.#uris.write(this.out(sys.Uri.fromStr("/fcode/uris.def")));
    }
    ;
    if (!this.#ftypes.isEmpty()) {
      let out = this.out(sys.Uri.fromStr("/fcode/types.def"));
      out.writeI2(this.#ftypes.size());
      this.#ftypes.each((t) => {
        t.writeMeta(out);
        return;
      });
      out.close();
    }
    ;
    this.#ftypes.each((t) => {
      t.write();
      return;
    });
    this.#onFile = null;
    return;
  }

  writeMeta(uri) {
    let out = this.out(uri);
    out.writeProps(this.meta());
    out.close();
    return;
  }

  writeIndex(uri) {
    const this$ = this;
    if (this.#index.isEmpty()) {
      return;
    }
    ;
    let out = this.out(uri);
    this.#index.each((v,n) => {
      if (sys.ObjUtil.is(v, sys.Str.type$)) {
        this$.prop(out, n, sys.ObjUtil.coerce(v, sys.Str.type$));
      }
      else {
        sys.ObjUtil.coerce(v, sys.Type.find("sys::Str[]")).each((vi) => {
          this$.prop(out, n, vi);
          return;
        });
      }
      ;
      return;
    });
    out.close();
    return;
  }

  prop(out,n,v) {
    out.print(sys.Str.toCode(n, null, true)).print("=").print(sys.Str.toCode(v, null, true)).print("\n");
    return;
  }

  in(uri) {
    let file = this.#zip.contents().get(uri);
    if (file == null) {
      return null;
    }
    ;
    return file.in();
  }

  out(uri) {
    return sys.Func.call(this.#onFile, uri);
  }

}

class FTypeRef extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FTypeRef.type$; }

  #podName = 0;

  podName() { return this.#podName; }

  __podName(it) { if (it === undefined) return this.#podName; else this.#podName = it; }

  #typeName = 0;

  typeName() { return this.#typeName; }

  __typeName(it) { if (it === undefined) return this.#typeName; else this.#typeName = it; }

  #sig = null;

  sig() { return this.#sig; }

  __sig(it) { if (it === undefined) return this.#sig; else this.#sig = it; }

  #hashcode = 0;

  hashcode() { return this.#hashcode; }

  __hashcode(it) { if (it === undefined) return this.#hashcode; else this.#hashcode = it; }

  static make(podName,typeName,sig) {
    const $self = new FTypeRef();
    FTypeRef.make$($self,podName,typeName,sig);
    return $self;
  }

  static make$($self,podName,typeName,sig) {
    $self.#podName = podName;
    $self.#typeName = typeName;
    $self.#sig = sig;
    $self.#hashcode = sys.Int.xor(sys.Int.xor(sys.Int.shiftl(podName, 17), typeName), sys.Str.hash(sig));
    return;
  }

  hash() {
    return this.#hashcode;
  }

  equals(obj) {
    let x = sys.ObjUtil.coerce(obj, FTypeRef.type$);
    return (sys.ObjUtil.equals(this.#podName, x.#podName) && sys.ObjUtil.equals(this.#typeName, x.#typeName) && sys.ObjUtil.equals(this.#sig, x.#sig));
  }

  isGenericInstance() {
    return sys.ObjUtil.compareGT(sys.Str.size(this.#sig), 1);
  }

  signature(pod) {
    if (this.isGenericInstance()) {
      return this.#sig;
    }
    ;
    return sys.Str.plus(sys.Str.plus(sys.Str.plus(pod.n(this.#podName), "::"), pod.n(this.#typeName)), this.#sig);
  }

  format(pod) {
    return this.signature(pod);
  }

  write(out) {
    out.writeI2(this.#podName);
    out.writeI2(this.#typeName);
    out.writeUtf(this.#sig);
    return;
  }

  static read(in$) {
    return FTypeRef.make(in$.readU2(), in$.readU2(), in$.readUtf());
  }

}

class FFieldRef extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FFieldRef.type$; }

  #parent = 0;

  parent() { return this.#parent; }

  __parent(it) { if (it === undefined) return this.#parent; else this.#parent = it; }

  #name = 0;

  name() { return this.#name; }

  __name(it) { if (it === undefined) return this.#name; else this.#name = it; }

  #typeRef = 0;

  typeRef() { return this.#typeRef; }

  __typeRef(it) { if (it === undefined) return this.#typeRef; else this.#typeRef = it; }

  #hashcode = 0;

  hashcode() { return this.#hashcode; }

  __hashcode(it) { if (it === undefined) return this.#hashcode; else this.#hashcode = it; }

  static make(parent,name,typeRef) {
    const $self = new FFieldRef();
    FFieldRef.make$($self,parent,name,typeRef);
    return $self;
  }

  static make$($self,parent,name,typeRef) {
    $self.#parent = parent;
    $self.#name = name;
    $self.#typeRef = typeRef;
    $self.#hashcode = sys.Int.xor(sys.Int.xor(sys.Int.shiftl(parent, 23), sys.Int.shiftl(name, 7)), typeRef);
    return;
  }

  hash() {
    return this.#hashcode;
  }

  equals(obj) {
    let x = sys.ObjUtil.coerce(obj, FFieldRef.type$);
    return (sys.ObjUtil.equals(this.#parent, x.#parent) && sys.ObjUtil.equals(this.#name, x.#name) && sys.ObjUtil.equals(this.#typeRef, x.#typeRef));
  }

  format(pod) {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(pod.typeRefStr(this.#parent), "."), pod.names().get(this.#name)), " -> "), pod.typeRefStr(this.#typeRef));
  }

  write(out) {
    out.writeI2(this.#parent);
    out.writeI2(this.#name);
    out.writeI2(this.#typeRef);
    return;
  }

  static read(in$) {
    return FFieldRef.make(in$.readU2(), in$.readU2(), in$.readU2());
  }

}

class FMethodRef extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FMethodRef.type$; }

  #parent = 0;

  parent() { return this.#parent; }

  __parent(it) { if (it === undefined) return this.#parent; else this.#parent = it; }

  #name = 0;

  name() { return this.#name; }

  __name(it) { if (it === undefined) return this.#name; else this.#name = it; }

  #ret = 0;

  ret() { return this.#ret; }

  __ret(it) { if (it === undefined) return this.#ret; else this.#ret = it; }

  #params = null;

  params() { return this.#params; }

  __params(it) { if (it === undefined) return this.#params; else this.#params = it; }

  #hashcode = 0;

  hashcode() { return this.#hashcode; }

  __hashcode(it) { if (it === undefined) return this.#hashcode; else this.#hashcode = it; }

  static make(parent,name,ret,params) {
    const $self = new FMethodRef();
    FMethodRef.make$($self,parent,name,ret,params);
    return $self;
  }

  static make$($self,parent,name,ret,params) {
    $self.#parent = parent;
    $self.#name = name;
    $self.#ret = ret;
    $self.#params = sys.ObjUtil.coerce(sys.ObjUtil.toImmutable(params), sys.Type.find("sys::Int[]"));
    $self.#hashcode = sys.Int.xor(sys.Int.xor(sys.Int.shiftl(parent, 23), sys.Int.shiftl(name, 7)), ret);
    return;
  }

  hash() {
    return this.#hashcode;
  }

  equals(obj) {
    let x = sys.ObjUtil.coerce(obj, FMethodRef.type$);
    return (sys.ObjUtil.equals(this.#parent, x.#parent) && sys.ObjUtil.equals(this.#name, x.#name) && sys.ObjUtil.equals(this.#ret, x.#ret) && sys.ObjUtil.equals(this.#params, x.#params));
  }

  format(pod) {
    const this$ = this;
    let s = sys.Str.plus(sys.Str.plus(sys.Str.plus(pod.typeRefStr(this.#parent), "."), pod.names().get(this.#name)), "(");
    this.#params.each((p,i) => {
      if (sys.ObjUtil.compareGT(i, 0)) {
        s = sys.Str.plus(s, ", ");
      }
      ;
      s = sys.Str.plus(s, pod.typeRefStr(p));
      return;
    });
    s = sys.Str.plus(s, sys.Str.plus(") -> ", pod.typeRefStr(this.#ret)));
    return s;
  }

  write(out) {
    const this$ = this;
    out.writeI2(this.#parent);
    out.writeI2(this.#name);
    out.writeI2(this.#ret);
    out.write(this.#params.size());
    this.#params.each((param) => {
      out.writeI2(param);
      return;
    });
    return;
  }

  static read(in$) {
    const this$ = this;
    let parent = in$.readU2();
    let name = in$.readU2();
    let ret = in$.readU2();
    let p = sys.List.make(sys.Int.type$);
    sys.Int.times(in$.readU1(), (it) => {
      p.add(sys.ObjUtil.coerce(in$.readU2(), sys.Int.type$.toNullable()));
      return;
    });
    return FMethodRef.make(parent, name, ret, p);
  }

}

class FSlot extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FSlot.type$; }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  ns() { return CSlot.prototype.ns.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  static FCodeVersion() { return FConst.FCodeVersion(); }

  static Enum() { return FConst.Enum(); }

  static LineNumberAttr() { return FConst.LineNumberAttr(); }

  static Synthetic() { return FConst.Synthetic(); }

  static LineNumbersAttr() { return FConst.LineNumbersAttr(); }

  static Once() { return FConst.Once(); }

  static Getter() { return FConst.Getter(); }

  static Internal() { return FConst.Internal(); }

  static SourceFileAttr() { return FConst.SourceFileAttr(); }

  static ErrTableAttr() { return FConst.ErrTableAttr(); }

  static Mixin() { return FConst.Mixin(); }

  static Native() { return FConst.Native(); }

  static Ctor() { return FConst.Ctor(); }

  static Private() { return FConst.Private(); }

  static Final() { return FConst.Final(); }

  static Override() { return FConst.Override(); }

  static FlagsMask() { return FConst.FlagsMask(); }

  static Facet() { return FConst.Facet(); }

  static FacetsAttr() { return FConst.FacetsAttr(); }

  static Protected() { return FConst.Protected(); }

  static Abstract() { return FConst.Abstract(); }

  static Const() { return FConst.Const(); }

  static Virtual() { return FConst.Virtual(); }

  static Public() { return FConst.Public(); }

  static Storage() { return FConst.Storage(); }

  static Setter() { return FConst.Setter(); }

  static Param() { return FConst.Param(); }

  static EnumOrdinalAttr() { return FConst.EnumOrdinalAttr(); }

  static Static() { return FConst.Static(); }

  static ParamDefaultAttr() { return FConst.ParamDefaultAttr(); }

  #fparent = null;

  fparent(it) {
    if (it === undefined) {
      return this.#fparent;
    }
    else {
      this.#fparent = it;
      return;
    }
  }

  #flags = 0;

  flags(it) {
    if (it === undefined) {
      return this.#flags;
    }
    else {
      this.#flags = it;
      return;
    }
  }

  #nameIndex = 0;

  nameIndex(it) {
    if (it === undefined) {
      return this.#nameIndex;
    }
    else {
      this.#nameIndex = it;
      return;
    }
  }

  #fattrs = null;

  fattrs(it) {
    if (it === undefined) {
      return this.#fattrs;
    }
    else {
      this.#fattrs = it;
      return;
    }
  }

  #ffacets = null;

  ffacets(it) {
    if (it === undefined) {
      return this.#ffacets;
    }
    else {
      this.#ffacets = it;
      return;
    }
  }

  static make(fparent) {
    const $self = new FSlot();
    FSlot.make$($self,fparent);
    return $self;
  }

  static make$($self,fparent) {
    $self.#fparent = fparent;
    return;
  }

  pod() {
    return this.#fparent.pod();
  }

  parent() {
    return this.#fparent;
  }

  name() {
    return this.#fparent.fpod().n(this.#nameIndex);
  }

  qname() {
    return sys.Str.plus(sys.Str.plus(this.#fparent.qname(), "."), this.name());
  }

  doc() {
    return null;
  }

  attr(name) {
    const this$ = this;
    return this.#fattrs.find((a) => {
      return sys.ObjUtil.equals(this$.#fparent.pod().n(a.name()), name);
    });
  }

  facet(qname) {
    const this$ = this;
    if (this.#ffacets == null) {
      this.#ffacets = FFacet.decode(this.#fparent.pod(), this.attr(FConst.FacetsAttr()));
    }
    ;
    return this.#ffacets.find((f) => {
      return sys.ObjUtil.equals(f.qname(), qname);
    });
  }

  writeCommon(out) {
    out.writeI2(this.#nameIndex);
    out.writeI4(sys.Int.and(this.flags(), FConst.FlagsMask()));
    return;
  }

  readCommon(in$) {
    this.#nameIndex = in$.readU2();
    this.flags(in$.readU4());
    return;
  }

}

class FField extends FSlot {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FField.type$; }

  isParameterized() { return CField.prototype.isParameterized.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  fieldType() { return CField.prototype.fieldType.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  ns() { return CSlot.prototype.ns.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  usesBridge() { return CField.prototype.usesBridge.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isGeneric() { return CField.prototype.isGeneric.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isCovariant() { return CField.prototype.isCovariant.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  #typeRef = 0;

  typeRef(it) {
    if (it === undefined) {
      return this.#typeRef;
    }
    else {
      this.#typeRef = it;
      return;
    }
  }

  #getter = null;

  getter(it) {
    if (it === undefined) {
      return this.#getter;
    }
    else {
      this.#getter = it;
      return;
    }
  }

  #setter = null;

  setter(it) {
    if (it === undefined) {
      return this.#setter;
    }
    else {
      this.#setter = it;
      return;
    }
  }

  static make(fparent) {
    const $self = new FField();
    FField.make$($self,fparent);
    return $self;
  }

  static make$($self,fparent) {
    FSlot.make$($self, fparent);
    return;
  }

  write(out) {
    FSlot.prototype.writeCommon.call(this, out);
    out.writeI2(this.#typeRef);
    FUtil.writeAttrs(out, this.fattrs());
    return;
  }

  read(in$) {
    FSlot.prototype.readCommon.call(this, in$);
    this.#typeRef = in$.readU2();
    this.fattrs(FUtil.readAttrs(in$));
    return this;
  }

  signature() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.type()), " "), this.name());
  }

  type() {
    return sys.ObjUtil.coerce(this.fparent().fpod().toType(this.#typeRef), CType.type$);
  }

  inheritedReturns() {
    if ((!this.isOverride() || this.getter() == null)) {
      return this.type();
    }
    else {
      return this.getter().inheritedReturns();
    }
    ;
  }

}

class FPrinter extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#showIndex = false;
    this.#showCode = true;
    this.#showLines = false;
    return;
  }

  typeof() { return FPrinter.type$; }

  static FCodeVersion() { return FConst.FCodeVersion(); }

  static Enum() { return FConst.Enum(); }

  static LineNumberAttr() { return FConst.LineNumberAttr(); }

  static Synthetic() { return FConst.Synthetic(); }

  static LineNumbersAttr() { return FConst.LineNumbersAttr(); }

  static Once() { return FConst.Once(); }

  static Getter() { return FConst.Getter(); }

  static Internal() { return FConst.Internal(); }

  static SourceFileAttr() { return FConst.SourceFileAttr(); }

  static ErrTableAttr() { return FConst.ErrTableAttr(); }

  static Mixin() { return FConst.Mixin(); }

  static Native() { return FConst.Native(); }

  static Ctor() { return FConst.Ctor(); }

  static Private() { return FConst.Private(); }

  static Final() { return FConst.Final(); }

  static Override() { return FConst.Override(); }

  static FlagsMask() { return FConst.FlagsMask(); }

  static Facet() { return FConst.Facet(); }

  static FacetsAttr() { return FConst.FacetsAttr(); }

  static Protected() { return FConst.Protected(); }

  static Abstract() { return FConst.Abstract(); }

  static Const() { return FConst.Const(); }

  static Virtual() { return FConst.Virtual(); }

  static Public() { return FConst.Public(); }

  static Storage() { return FConst.Storage(); }

  static Setter() { return FConst.Setter(); }

  static Param() { return FConst.Param(); }

  static EnumOrdinalAttr() { return FConst.EnumOrdinalAttr(); }

  static Static() { return FConst.Static(); }

  static ParamDefaultAttr() { return FConst.ParamDefaultAttr(); }

  #pod = null;

  pod(it) {
    if (it === undefined) {
      return this.#pod;
    }
    else {
      this.#pod = it;
      return;
    }
  }

  #out = null;

  out(it) {
    if (it === undefined) {
      return this.#out;
    }
    else {
      this.#out = it;
      return;
    }
  }

  #showIndex = false;

  showIndex(it) {
    if (it === undefined) {
      return this.#showIndex;
    }
    else {
      this.#showIndex = it;
      return;
    }
  }

  #showCode = false;

  showCode(it) {
    if (it === undefined) {
      return this.#showCode;
    }
    else {
      this.#showCode = it;
      return;
    }
  }

  #showLines = false;

  showLines(it) {
    if (it === undefined) {
      return this.#showLines;
    }
    else {
      this.#showLines = it;
      return;
    }
  }

  static make(pod,out) {
    const $self = new FPrinter();
    FPrinter.make$($self,pod,out);
    return $self;
  }

  static make$($self,pod,out) {
    if (out === undefined) out = sys.Env.cur().out();
    ;
    $self.#pod = pod;
    $self.#out = out;
    return;
  }

  all() {
    this.tables();
    this.ftypes();
    this.#out.flush();
    return;
  }

  tables() {
    this.printLine("##### Tables #####");
    this.table("--- names ---", this.#pod.names());
    this.table("--- typeRefs ---", this.#pod.typeRefs());
    this.table("--- fieldRefs ---", this.#pod.fieldRefs());
    this.table("--- methodRefs ---", this.#pod.methodRefs());
    this.table("--- ints ---", this.#pod.ints());
    this.table("--- floats ---", this.#pod.floats());
    this.table("--- strs ---", this.#pod.strs());
    this.table("--- durations ---", this.#pod.durations());
    this.table("--- uris ---", this.#pod.uris());
    this.#out.flush();
    return;
  }

  table(title,table) {
    const this$ = this;
    this.printLine(title);
    table.table().each((obj,index) => {
      let m = sys.ObjUtil.typeof(obj).method("format", false);
      let s = ((this$) => { if (m != null) return m.callList(sys.List.make(sys.Obj.type$, [obj, this$.#pod])); return sys.ObjUtil.toStr(obj); })(this$);
      this$.printLine(sys.Str.plus(sys.Str.plus(sys.Str.plus("  [", sys.ObjUtil.coerce(index, sys.Obj.type$.toNullable())), "]  "), s));
      return;
    });
    return;
  }

  ftypes() {
    const this$ = this;
    this.printLine("##### Types #####");
    this.#pod.ftypes().each((t) => {
      this$.ftype(t);
      return;
    });
    this.#out.flush();
    return;
  }

  ftype(t) {
    const this$ = this;
    this.printLine(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("--", this.typeRef(t.self())), " : "), this.typeRef(t.fbase())), "--"));
    if (!t.fmixins().isEmpty()) {
      this.printLine(sys.Str.plus("  mixin ", t.fmixins().join(", ", (m) => {
        return this$.typeRef(m);
      })));
    }
    ;
    this.attrs(t.fattrs());
    this.printLine();
    t.ffields().each((f) => {
      this$.field(f);
      return;
    });
    t.fmethods().each((m) => {
      this$.method(m);
      return;
    });
    this.#out.flush();
    return;
  }

  slot(s) {
    if (sys.ObjUtil.is(s, FField.type$)) {
      this.field(sys.ObjUtil.coerce(s, FField.type$));
    }
    else {
      this.method(sys.ObjUtil.coerce(s, FMethod.type$));
    }
    ;
    this.#out.flush();
    return;
  }

  field(f) {
    this.printLine(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("  ", this.name(f.nameIndex())), " -> "), this.typeRef(f.typeRef())), " ["), this.flags(f.flags())), "]"));
    this.attrs(f.fattrs());
    this.printLine();
    return;
  }

  method(m) {
    const this$ = this;
    this.print(sys.Str.plus(sys.Str.plus("  ", this.name(m.nameIndex())), " ("));
    this.print(m.fparams().join(", ", (p) => {
      return sys.Str.plus(sys.Str.plus(this$.typeRef(p.typeRef()), " "), this$.name(p.nameIndex()));
    }));
    this.print(sys.Str.plus(") -> ", this.typeRef(m.ret())));
    if (sys.ObjUtil.compareNE(m.ret(), m.inheritedRet())) {
      this.print(sys.Str.plus(sys.Str.plus(" {", this.typeRef(m.inheritedRet())), "}"));
    }
    ;
    this.printLine(sys.Str.plus(sys.Str.plus(" [", this.flags(m.flags())), "]"));
    m.vars().each((v,reg) => {
      let role = ((this$) => { if (v.isParam()) return "Param"; return "Local"; })(this$);
      if (sys.ObjUtil.equals(sys.Int.and(m.flags(), FConst.Static()), 0)) {
        ((this$) => { let $_u77 = reg;reg = sys.Int.increment(reg); return $_u77; })(this$);
      }
      ;
      this$.printLine(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("    [", role), " "), sys.ObjUtil.coerce(reg, sys.Obj.type$.toNullable())), "] "), this$.#pod.n(v.nameIndex())), " -> "), this$.typeRef(v.typeRef())));
      if (v.def() != null) {
        this$.code(sys.ObjUtil.coerce(v.def(), sys.Buf.type$));
      }
      ;
      return;
    });
    if (m.code() != null) {
      this.printLine("    [Code]");
      this.code(sys.ObjUtil.coerce(m.code(), sys.Buf.type$));
    }
    ;
    this.attrs(m.fattrs());
    this.printLine();
    return;
  }

  code(code) {
    if (!this.#showCode) {
      return;
    }
    ;
    this.#out.flush();
    let codePrinter = FCodePrinter.make(this.#pod, this.#out);
    codePrinter.showIndex(this.#showIndex);
    codePrinter.code(code);
    return;
  }

  attrs(attrs) {
    const this$ = this;
    if (attrs == null) {
      return;
    }
    ;
    attrs.each((a) => {
      this$.attr(a);
      return;
    });
    return;
  }

  attr(attr) {
    let name = this.name(attr.name());
    if ((sys.ObjUtil.equals(name, FConst.LineNumbersAttr()) && !this.#showLines)) {
      return;
    }
    ;
    this.printLine(sys.Str.plus(sys.Str.plus(sys.Str.plus("    [", name), "] size="), sys.ObjUtil.coerce(attr.data().size(), sys.Obj.type$.toNullable())));
    if (sys.ObjUtil.equals(name, FConst.SourceFileAttr())) {
      this.sourceFileAttr(attr);
    }
    ;
    if (sys.ObjUtil.equals(name, FConst.ErrTableAttr())) {
      this.errTableAttr(attr);
    }
    ;
    if (sys.ObjUtil.equals(name, FConst.LineNumberAttr())) {
      this.lineNumberAttr(attr);
    }
    ;
    if (sys.ObjUtil.equals(name, FConst.LineNumbersAttr())) {
      this.lineNumbersAttr(attr);
    }
    ;
    if (sys.ObjUtil.equals(name, FConst.FacetsAttr())) {
      this.facetsAttr(attr);
    }
    ;
    if (sys.ObjUtil.equals(name, FConst.EnumOrdinalAttr())) {
      this.enumOrdinalAttr(attr);
    }
    ;
    return;
  }

  sourceFileAttr(attr) {
    this.printLine(sys.Str.plus("       ", attr.utf()));
    return;
  }

  lineNumberAttr(attr) {
    this.printLine(sys.Str.plus("       ", sys.ObjUtil.coerce(attr.u2(), sys.Obj.type$.toNullable())));
    return;
  }

  facetsAttr(attr) {
    const this$ = this;
    let buf = attr.data();
    buf.seek(0);
    sys.Int.times(buf.readU2(), (it) => {
      let type = this$.#pod.typeRef(buf.readU2());
      let val = buf.readUtf();
      this$.printLine(sys.Str.plus("       @", ((this$) => { if (sys.Str.isEmpty(val)) return type; return val; })(this$)));
      return;
    });
    return;
  }

  enumOrdinalAttr(attr) {
    this.printLine(sys.Str.plus("       ", sys.ObjUtil.coerce(attr.u2(), sys.Obj.type$.toNullable())));
    return;
  }

  errTableAttr(attr) {
    const this$ = this;
    let buf = attr.data();
    buf.seek(0);
    sys.Int.times(buf.readU2(), (it) => {
      let start = buf.readU2();
      let end = buf.readU2();
      let handler = buf.readU2();
      let tr = buf.readU2();
      this$.printLine(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("      ", sys.ObjUtil.coerce(start, sys.Obj.type$.toNullable())), "  to "), sys.ObjUtil.coerce(end, sys.Obj.type$.toNullable())), " -> "), sys.ObjUtil.coerce(handler, sys.Obj.type$.toNullable())), "  "), this$.typeRef(tr)));
      return;
    });
    return;
  }

  lineNumbersAttr(attr) {
    const this$ = this;
    let buf = attr.data();
    buf.seek(0);
    sys.Int.times(buf.readU2(), (it) => {
      let pc = buf.readU2();
      let line = buf.readU2();
      this$.printLine(sys.Str.plus(sys.Str.plus(sys.Str.plus("       ", sys.ObjUtil.coerce(pc, sys.Obj.type$.toNullable())), ": "), sys.ObjUtil.coerce(line, sys.Obj.type$.toNullable())));
      return;
    });
    return;
  }

  typeRef(i) {
    if (sys.ObjUtil.equals(i, 65535)) {
      return "null";
    }
    ;
    return sys.Str.plus(this.#pod.typeRefStr(i), this.index(i));
  }

  name(i) {
    return sys.Str.plus(this.#pod.n(i), this.index(i));
  }

  flags(flags) {
    let s = sys.StrBuf.make();
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0)) {
      s.add("abstract ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Const()), 0)) {
      s.add("const ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Ctor()), 0)) {
      s.add("ctor ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Enum()), 0)) {
      s.add("enum ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Final()), 0)) {
      s.add("final ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Getter()), 0)) {
      s.add("getter ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Internal()), 0)) {
      s.add("internal ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Mixin()), 0)) {
      s.add("mixin ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Native()), 0)) {
      s.add("native ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Override()), 0)) {
      s.add("override ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Private()), 0)) {
      s.add("private ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Protected()), 0)) {
      s.add("protected ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Public()), 0)) {
      s.add("public ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Setter()), 0)) {
      s.add("setter ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Static()), 0)) {
      s.add("static ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Storage()), 0)) {
      s.add("storage ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Synthetic()), 0)) {
      s.add("synthetic ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Virtual()), 0)) {
      s.add("virtual ");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Once()), 0)) {
      s.add("once ");
    }
    ;
    return sys.Str.getRange(s.toStr(), sys.Range.make(0, -2));
  }

  index(index) {
    if (this.#showIndex) {
      return sys.Str.plus(sys.Str.plus("[", sys.ObjUtil.coerce(index, sys.Obj.type$.toNullable())), "]");
    }
    ;
    return "";
  }

  print(obj) {
    this.#out.print(obj);
    return this;
  }

  printLine(obj) {
    if (obj === undefined) obj = "";
    this.#out.printLine(obj);
    return this;
  }

}

class FMethod extends FSlot {
  constructor() {
    super();
    const this$ = this;
    this.#maxStack = 16;
    return;
  }

  typeof() { return FMethod.type$; }

  isParameterized() { return CMethod.prototype.isParameterized.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  hasSameParams() { return CMethod.prototype.hasSameParams.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  inheritedReturnType() { return CMethod.prototype.inheritedReturnType.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  nameAndParamTypesToStr() { return CMethod.prototype.nameAndParamTypesToStr.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  returnType() { return CMethod.prototype.returnType.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  ns() { return CSlot.prototype.ns.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  usesBridge() { return CMethod.prototype.usesBridge.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  generic() { return CMethod.prototype.generic.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isCovariant() { return CMethod.prototype.isCovariant.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  #ret = 0;

  ret(it) {
    if (it === undefined) {
      return this.#ret;
    }
    else {
      this.#ret = it;
      return;
    }
  }

  #inheritedRet = 0;

  inheritedRet(it) {
    if (it === undefined) {
      return this.#inheritedRet;
    }
    else {
      this.#inheritedRet = it;
      return;
    }
  }

  #vars = null;

  vars(it) {
    if (it === undefined) {
      return this.#vars;
    }
    else {
      this.#vars = it;
      return;
    }
  }

  #paramCount = 0;

  paramCount(it) {
    if (it === undefined) {
      return this.#paramCount;
    }
    else {
      this.#paramCount = it;
      return;
    }
  }

  #localCount = 0;

  localCount(it) {
    if (it === undefined) {
      return this.#localCount;
    }
    else {
      this.#localCount = it;
      return;
    }
  }

  #code = null;

  code(it) {
    if (it === undefined) {
      return this.#code;
    }
    else {
      this.#code = it;
      return;
    }
  }

  #maxStack = 0;

  maxStack(it) {
    if (it === undefined) {
      return this.#maxStack;
    }
    else {
      this.#maxStack = it;
      return;
    }
  }

  #isGeneric$Store = undefined;

  // private field reflection only
  __isGeneric$Store(it) { if (it === undefined) return this.#isGeneric$Store; else this.#isGeneric$Store = it; }

  static make(fparent) {
    const $self = new FMethod();
    FMethod.make$($self,fparent);
    return $self;
  }

  static make$($self,fparent) {
    FSlot.make$($self, fparent);
    ;
    return;
  }

  fparams() {
    const this$ = this;
    return this.#vars.findAll((v) => {
      return v.isParam();
    });
  }

  returns() {
    return sys.ObjUtil.coerce(this.fparent().fpod().toType(this.#ret), CType.type$);
  }

  params() {
    return this.fparams();
  }

  signature() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.returns()), " "), this.name()), "("), this.params().join(",")), ")");
  }

  isGeneric() {
    if (this.#isGeneric$Store === undefined) {
      this.#isGeneric$Store = sys.ObjUtil.coerce(this.isGeneric$Once(), sys.Obj.type$.toNullable());
    }
    ;
    return sys.ObjUtil.coerce(this.#isGeneric$Store, sys.Bool.type$);
  }

  inheritedReturns() {
    return sys.ObjUtil.coerce(this.fparent().fpod().toType(this.#inheritedRet), CType.type$);
  }

  write(out) {
    const this$ = this;
    FSlot.prototype.writeCommon.call(this, out);
    out.writeI2(this.#ret);
    out.writeI2(this.#inheritedRet);
    out.write(this.#maxStack);
    out.write(this.#paramCount);
    out.write(this.#localCount);
    this.#vars.each((var$) => {
      var$.write(out);
      return;
    });
    FUtil.writeBuf(out, this.#code);
    FUtil.writeAttrs(out, this.fattrs());
    return;
  }

  read(in$) {
    const this$ = this;
    FSlot.prototype.readCommon.call(this, in$);
    this.#ret = in$.readU2();
    this.#inheritedRet = in$.readU2();
    this.#maxStack = in$.readU1();
    this.#paramCount = in$.readU1();
    this.#localCount = in$.readU1();
    this.#vars = sys.List.make(FMethodVar.type$);
    sys.Int.times(sys.Int.plus(this.#paramCount, this.#localCount), (it) => {
      this$.#vars.add(FMethodVar.make(this$).read(in$));
      return;
    });
    this.#code = FUtil.readBuf(in$);
    this.fattrs(FUtil.readAttrs(in$));
    return this;
  }

  dump() {
    let p = FPrinter.make(this.fparent().fpod());
    p.showCode(true);
    p.method(this);
    return;
  }

  isGeneric$Once() {
    return CMethod.calcGeneric(this);
  }

}

class FMethodVar extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FMethodVar.type$; }

  static FCodeVersion() { return FConst.FCodeVersion(); }

  static Enum() { return FConst.Enum(); }

  static LineNumberAttr() { return FConst.LineNumberAttr(); }

  static Synthetic() { return FConst.Synthetic(); }

  static LineNumbersAttr() { return FConst.LineNumbersAttr(); }

  static Once() { return FConst.Once(); }

  static Getter() { return FConst.Getter(); }

  static Internal() { return FConst.Internal(); }

  static SourceFileAttr() { return FConst.SourceFileAttr(); }

  static ErrTableAttr() { return FConst.ErrTableAttr(); }

  static Mixin() { return FConst.Mixin(); }

  static Native() { return FConst.Native(); }

  static Ctor() { return FConst.Ctor(); }

  static Private() { return FConst.Private(); }

  static Final() { return FConst.Final(); }

  static Override() { return FConst.Override(); }

  static FlagsMask() { return FConst.FlagsMask(); }

  static Facet() { return FConst.Facet(); }

  static FacetsAttr() { return FConst.FacetsAttr(); }

  static Protected() { return FConst.Protected(); }

  static Abstract() { return FConst.Abstract(); }

  static Const() { return FConst.Const(); }

  static Virtual() { return FConst.Virtual(); }

  static Public() { return FConst.Public(); }

  static Storage() { return FConst.Storage(); }

  static Setter() { return FConst.Setter(); }

  static Param() { return FConst.Param(); }

  static EnumOrdinalAttr() { return FConst.EnumOrdinalAttr(); }

  static Static() { return FConst.Static(); }

  static ParamDefaultAttr() { return FConst.ParamDefaultAttr(); }

  paramType() { return CParam.prototype.paramType.apply(this, arguments); }

  #fmethod = null;

  fmethod(it) {
    if (it === undefined) {
      return this.#fmethod;
    }
    else {
      this.#fmethod = it;
      return;
    }
  }

  #nameIndex = 0;

  nameIndex(it) {
    if (it === undefined) {
      return this.#nameIndex;
    }
    else {
      this.#nameIndex = it;
      return;
    }
  }

  #typeRef = 0;

  typeRef(it) {
    if (it === undefined) {
      return this.#typeRef;
    }
    else {
      this.#typeRef = it;
      return;
    }
  }

  #flags = 0;

  flags(it) {
    if (it === undefined) {
      return this.#flags;
    }
    else {
      this.#flags = it;
      return;
    }
  }

  #defNameIndex = 0;

  defNameIndex(it) {
    if (it === undefined) {
      return this.#defNameIndex;
    }
    else {
      this.#defNameIndex = it;
      return;
    }
  }

  #def = null;

  def(it) {
    if (it === undefined) {
      return this.#def;
    }
    else {
      this.#def = it;
      return;
    }
  }

  static make(fmethod) {
    const $self = new FMethodVar();
    FMethodVar.make$($self,fmethod);
    return $self;
  }

  static make$($self,fmethod) {
    $self.#fmethod = fmethod;
    return;
  }

  name() {
    return this.fpod().n(this.#nameIndex);
  }

  type() {
    return sys.ObjUtil.coerce(this.fpod().toType(this.#typeRef), CType.type$);
  }

  hasDefault() {
    return this.#def != null;
  }

  toStr() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.type()), " "), this.name());
  }

  isParam() {
    return sys.ObjUtil.compareNE(sys.Int.and(this.#flags, FConst.Param()), 0);
  }

  write(out) {
    out.writeI2(this.#nameIndex);
    out.writeI2(this.#typeRef);
    out.write(this.#flags);
    if (this.#def == null) {
      out.writeI2(0);
    }
    else {
      out.writeI2(1);
      out.writeI2(this.#defNameIndex);
      FUtil.writeBuf(out, this.#def);
    }
    ;
    return;
  }

  read(in$) {
    const this$ = this;
    this.#nameIndex = in$.readU2();
    this.#typeRef = in$.readU2();
    this.#flags = in$.readU1();
    sys.Int.times(in$.readU2(), (it) => {
      let attrNameIndex = in$.readU2();
      let attrBuf = FUtil.readBuf(in$);
      if (sys.ObjUtil.equals(this$.#fmethod.pod().n(attrNameIndex), FConst.ParamDefaultAttr())) {
        this$.#defNameIndex = attrNameIndex;
        this$.#def = attrBuf;
      }
      ;
      return;
    });
    return this;
  }

  fpod() {
    return this.#fmethod.fparent().fpod();
  }

}

class FAttr extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FAttr.type$; }

  static FCodeVersion() { return FConst.FCodeVersion(); }

  static Enum() { return FConst.Enum(); }

  static LineNumberAttr() { return FConst.LineNumberAttr(); }

  static Synthetic() { return FConst.Synthetic(); }

  static LineNumbersAttr() { return FConst.LineNumbersAttr(); }

  static Once() { return FConst.Once(); }

  static Getter() { return FConst.Getter(); }

  static Internal() { return FConst.Internal(); }

  static SourceFileAttr() { return FConst.SourceFileAttr(); }

  static ErrTableAttr() { return FConst.ErrTableAttr(); }

  static Mixin() { return FConst.Mixin(); }

  static Native() { return FConst.Native(); }

  static Ctor() { return FConst.Ctor(); }

  static Private() { return FConst.Private(); }

  static Final() { return FConst.Final(); }

  static Override() { return FConst.Override(); }

  static FlagsMask() { return FConst.FlagsMask(); }

  static Facet() { return FConst.Facet(); }

  static FacetsAttr() { return FConst.FacetsAttr(); }

  static Protected() { return FConst.Protected(); }

  static Abstract() { return FConst.Abstract(); }

  static Const() { return FConst.Const(); }

  static Virtual() { return FConst.Virtual(); }

  static Public() { return FConst.Public(); }

  static Storage() { return FConst.Storage(); }

  static Setter() { return FConst.Setter(); }

  static Param() { return FConst.Param(); }

  static EnumOrdinalAttr() { return FConst.EnumOrdinalAttr(); }

  static Static() { return FConst.Static(); }

  static ParamDefaultAttr() { return FConst.ParamDefaultAttr(); }

  #name = 0;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #data = null;

  data(it) {
    if (it === undefined) {
      return this.#data;
    }
    else {
      this.#data = it;
      return;
    }
  }

  utf() {
    return sys.ObjUtil.coerce(this.#data.seek(0), sys.Buf.type$.toNullable()).readUtf();
  }

  u2() {
    return sys.ObjUtil.coerce(this.#data.seek(0), sys.Buf.type$.toNullable()).readU2();
  }

  write(out) {
    out.writeI2(this.#name);
    FUtil.writeBuf(out, this.#data);
    return;
  }

  read(in$) {
    this.#name = in$.readU2();
    this.#data = FUtil.readBuf(in$);
    return this;
  }

  static make() {
    const $self = new FAttr();
    FAttr.make$($self);
    return $self;
  }

  static make$($self) {
    return;
  }

}

class FFacet extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FFacet.type$; }

  #qname = null;

  qname() { return this.#qname; }

  __qname(it) { if (it === undefined) return this.#qname; else this.#qname = it; }

  #val = null;

  val() { return this.#val; }

  __val(it) { if (it === undefined) return this.#val; else this.#val = it; }

  static decode(fpod,attr) {
    const this$ = this;
    if (attr == null) {
      return sys.ObjUtil.coerce(FFacet.type$.emptyList(), sys.Type.find("compiler::FFacet[]"));
    }
    ;
    let num = sys.ObjUtil.coerce(attr.data().seek(0), sys.Buf.type$.toNullable()).readU2();
    let ffacets = sys.List.make(FFacet.type$);
    ffacets.capacity(num);
    sys.Int.times(num, (it) => {
      let qname = fpod.typeRef(attr.data().readU2()).signature(fpod);
      let val = attr.data().readUtf();
      ffacets.add(FFacet.make(qname, val));
      return;
    });
    return ffacets;
  }

  static make(qn,v) {
    const $self = new FFacet();
    FFacet.make$($self,qn,v);
    return $self;
  }

  static make$($self,qn,v) {
    $self.#qname = qn;
    $self.#val = v;
    return;
  }

  toStr() {
    return ((this$) => { if (sys.Str.isEmpty(this$.#val)) return this$.#qname; return this$.#val; })(this);
  }

  get(name) {
    try {
      let s = sys.Int.plus(sys.Int.plus(sys.ObjUtil.coerce(sys.Str.index(this.#val, name), sys.Int.type$), sys.Str.size(name)), 1);
      let e = ((this$) => { let $_u80 = sys.Str.index(this$.#val, ";", sys.Int.plus(s, 1)); if ($_u80 != null) return $_u80; return sys.ObjUtil.coerce(sys.Int.minus(sys.Str.size(this$.#val), 1), sys.Int.type$.toNullable()); })(this);
      let v = sys.Str.getRange(this.#val, sys.Range.make(s, sys.ObjUtil.coerce(e, sys.Int.type$), true));
      return sys.Str.in(v).readObj();
    }
    catch ($_u81) {
    }
    ;
    return null;
  }

}

class FCodePrinter extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FCodePrinter.type$; }

  static FCodeVersion() { return FConst.FCodeVersion(); }

  static Enum() { return FConst.Enum(); }

  static LineNumberAttr() { return FConst.LineNumberAttr(); }

  static Synthetic() { return FConst.Synthetic(); }

  static LineNumbersAttr() { return FConst.LineNumbersAttr(); }

  static Once() { return FConst.Once(); }

  static Getter() { return FConst.Getter(); }

  static Internal() { return FConst.Internal(); }

  static SourceFileAttr() { return FConst.SourceFileAttr(); }

  static ErrTableAttr() { return FConst.ErrTableAttr(); }

  static Mixin() { return FConst.Mixin(); }

  static Native() { return FConst.Native(); }

  static Ctor() { return FConst.Ctor(); }

  static Private() { return FConst.Private(); }

  static Final() { return FConst.Final(); }

  static Override() { return FConst.Override(); }

  static FlagsMask() { return FConst.FlagsMask(); }

  static Facet() { return FConst.Facet(); }

  static FacetsAttr() { return FConst.FacetsAttr(); }

  static Protected() { return FConst.Protected(); }

  static Abstract() { return FConst.Abstract(); }

  static Const() { return FConst.Const(); }

  static Virtual() { return FConst.Virtual(); }

  static Public() { return FConst.Public(); }

  static Storage() { return FConst.Storage(); }

  static Setter() { return FConst.Setter(); }

  static Param() { return FConst.Param(); }

  static EnumOrdinalAttr() { return FConst.EnumOrdinalAttr(); }

  static Static() { return FConst.Static(); }

  static ParamDefaultAttr() { return FConst.ParamDefaultAttr(); }

  #pod = null;

  pod(it) {
    if (it === undefined) {
      return this.#pod;
    }
    else {
      this.#pod = it;
      return;
    }
  }

  #out = null;

  out(it) {
    if (it === undefined) {
      return this.#out;
    }
    else {
      this.#out = it;
      return;
    }
  }

  #showIndex = false;

  showIndex(it) {
    if (it === undefined) {
      return this.#showIndex;
    }
    else {
      this.#showIndex = it;
      return;
    }
  }

  #buf = null;

  buf(it) {
    if (it === undefined) {
      return this.#buf;
    }
    else {
      this.#buf = it;
      return;
    }
  }

  static make(pod,out) {
    const $self = new FCodePrinter();
    FCodePrinter.make$($self,pod,out);
    return $self;
  }

  static make$($self,pod,out) {
    if (out === undefined) out = sys.Env.cur().out();
    $self.#pod = pod;
    $self.#out = out;
    return;
  }

  code(buf) {
    try {
      this.#buf = buf.seek(0);
      while (true) {
        let op = buf.read();
        if (op == null) {
          break;
        }
        ;
        this.op(FOp.vals().get(sys.ObjUtil.coerce(op, sys.Int.type$)));
      }
      ;
    }
    catch ($_u82) {
      $_u82 = sys.Err.make($_u82);
      if ($_u82 instanceof sys.Err) {
        let e = $_u82;
        ;
        e.trace();
      }
      else {
        throw $_u82;
      }
    }
    ;
    this.#out.flush();
    this.#buf = null;
    return;
  }

  op(op) {
    this.print(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("    ", sys.Str.justr(sys.Int.toStr(sys.Int.minus(this.#buf.pos(), 1)), 3)), ": "), sys.Str.justl(op.name(), 19)), " "));
    if (sys.ObjUtil.equals(op, FOp.Switch())) {
      this.printSwitch();
    }
    else {
      let $_u83 = op.arg();
      if (sys.ObjUtil.equals($_u83, FOpArg.None())) {
        this.print();
      }
      else if (sys.ObjUtil.equals($_u83, FOpArg.Int())) {
        let i = this.#buf.readU2();
        this.print(sys.Str.plus(sys.Int.toStr(this.#pod.integer(i)), this.index(i)));
      }
      else if (sys.ObjUtil.equals($_u83, FOpArg.Float())) {
        let i = this.#buf.readU2();
        this.print(sys.Str.plus(sys.Float.toStr(this.#pod.float(i)), this.index(i)));
      }
      else if (sys.ObjUtil.equals($_u83, FOpArg.Decimal())) {
        let i = this.#buf.readU2();
        this.print(sys.Str.plus(sys.Decimal.toStr(this.#pod.decimal(i)), this.index(i)));
      }
      else if (sys.ObjUtil.equals($_u83, FOpArg.Str())) {
        let i = this.#buf.readU2();
        this.print(sys.Str.plus(sys.Str.toStr(this.#pod.str(i)), this.index(i)));
      }
      else if (sys.ObjUtil.equals($_u83, FOpArg.Duration())) {
        let i = this.#buf.readU2();
        this.print(sys.Str.plus(this.#pod.duration(i).toStr(), this.index(i)));
      }
      else if (sys.ObjUtil.equals($_u83, FOpArg.Uri())) {
        let i = this.#buf.readU2();
        this.print(sys.Str.plus(sys.Str.toStr(this.#pod.uri(i)), this.index(i)));
      }
      else if (sys.ObjUtil.equals($_u83, FOpArg.Register())) {
        let i = this.#buf.readU2();
        this.print(sys.ObjUtil.coerce(i, sys.Obj.type$));
      }
      else if (sys.ObjUtil.equals($_u83, FOpArg.TypeRef())) {
        let i = this.#buf.readU2();
        this.print(sys.Str.plus(this.#pod.typeRefStr(i), this.index(i)));
      }
      else if (sys.ObjUtil.equals($_u83, FOpArg.FieldRef())) {
        let i = this.#buf.readU2();
        this.print(sys.Str.plus(this.#pod.fieldRefStr(i), this.index(i)));
      }
      else if (sys.ObjUtil.equals($_u83, FOpArg.MethodRef())) {
        let i = this.#buf.readU2();
        this.print(sys.Str.plus(this.#pod.methodRefStr(i), this.index(i)));
      }
      else if (sys.ObjUtil.equals($_u83, FOpArg.Jump())) {
        let i = this.#buf.readU2();
        this.print(sys.ObjUtil.coerce(i, sys.Obj.type$));
      }
      else if (sys.ObjUtil.equals($_u83, FOpArg.TypePair())) {
        let i1 = this.#buf.readU2();
        let i2 = this.#buf.readU2();
        this.print(sys.Str.plus(this.#pod.typeRefStr(i1), this.index(i1)));
        if (sys.ObjUtil.equals(op, FOp.Coerce())) {
          this.print(" => ");
        }
        else {
          this.print(" <=> ");
        }
        ;
        this.print(sys.Str.plus(this.#pod.typeRefStr(i2), this.index(i2)));
      }
      else {
        throw sys.Err.make(op.arg().toStr());
      }
      ;
    }
    ;
    this.printLine();
    return;
  }

  printSwitch() {
    const this$ = this;
    sys.Int.times(this.#buf.readU2(), (i) => {
      this$.printLine();
      this$.print(sys.Str.plus(sys.Str.plus(sys.Str.plus("          ", sys.ObjUtil.coerce(i, sys.Obj.type$.toNullable())), " -> "), sys.ObjUtil.coerce(this$.#buf.readU2(), sys.Obj.type$.toNullable())));
      return;
    });
    return;
  }

  index(index) {
    if (this.#showIndex) {
      return sys.Str.plus(sys.Str.plus("[", sys.ObjUtil.coerce(index, sys.Obj.type$.toNullable())), "]");
    }
    ;
    return "";
  }

  print(obj) {
    if (obj === undefined) obj = "";
    this.#out.print(obj);
    return this;
  }

  printLine(obj) {
    if (obj === undefined) obj = "";
    this.#out.printLine(obj);
    return this;
  }

}

class FOp extends sys.Enum {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FOp.type$; }

  static Nop() { return FOp.vals().get(0); }

  static LoadNull() { return FOp.vals().get(1); }

  static LoadFalse() { return FOp.vals().get(2); }

  static LoadTrue() { return FOp.vals().get(3); }

  static LoadInt() { return FOp.vals().get(4); }

  static LoadFloat() { return FOp.vals().get(5); }

  static LoadDecimal() { return FOp.vals().get(6); }

  static LoadStr() { return FOp.vals().get(7); }

  static LoadDuration() { return FOp.vals().get(8); }

  static LoadType() { return FOp.vals().get(9); }

  static LoadUri() { return FOp.vals().get(10); }

  static LoadVar() { return FOp.vals().get(11); }

  static StoreVar() { return FOp.vals().get(12); }

  static LoadInstance() { return FOp.vals().get(13); }

  static StoreInstance() { return FOp.vals().get(14); }

  static LoadStatic() { return FOp.vals().get(15); }

  static StoreStatic() { return FOp.vals().get(16); }

  static LoadMixinStatic() { return FOp.vals().get(17); }

  static StoreMixinStatic() { return FOp.vals().get(18); }

  static CallNew() { return FOp.vals().get(19); }

  static CallCtor() { return FOp.vals().get(20); }

  static CallStatic() { return FOp.vals().get(21); }

  static CallVirtual() { return FOp.vals().get(22); }

  static CallNonVirtual() { return FOp.vals().get(23); }

  static CallMixinStatic() { return FOp.vals().get(24); }

  static CallMixinVirtual() { return FOp.vals().get(25); }

  static CallMixinNonVirtual() { return FOp.vals().get(26); }

  static Jump() { return FOp.vals().get(27); }

  static JumpTrue() { return FOp.vals().get(28); }

  static JumpFalse() { return FOp.vals().get(29); }

  static CmpEQ() { return FOp.vals().get(30); }

  static CmpNE() { return FOp.vals().get(31); }

  static Cmp() { return FOp.vals().get(32); }

  static CmpLE() { return FOp.vals().get(33); }

  static CmpLT() { return FOp.vals().get(34); }

  static CmpGT() { return FOp.vals().get(35); }

  static CmpGE() { return FOp.vals().get(36); }

  static CmpSame() { return FOp.vals().get(37); }

  static CmpNotSame() { return FOp.vals().get(38); }

  static CmpNull() { return FOp.vals().get(39); }

  static CmpNotNull() { return FOp.vals().get(40); }

  static Return() { return FOp.vals().get(41); }

  static Pop() { return FOp.vals().get(42); }

  static Dup() { return FOp.vals().get(43); }

  static Is() { return FOp.vals().get(44); }

  static As() { return FOp.vals().get(45); }

  static Coerce() { return FOp.vals().get(46); }

  static Switch() { return FOp.vals().get(47); }

  static Throw() { return FOp.vals().get(48); }

  static Leave() { return FOp.vals().get(49); }

  static JumpFinally() { return FOp.vals().get(50); }

  static CatchAllStart() { return FOp.vals().get(51); }

  static CatchErrStart() { return FOp.vals().get(52); }

  static CatchEnd() { return FOp.vals().get(53); }

  static FinallyStart() { return FOp.vals().get(54); }

  static FinallyEnd() { return FOp.vals().get(55); }

  static #vals = undefined;

  #arg = null;

  arg() { return this.#arg; }

  __arg(it) { if (it === undefined) return this.#arg; else this.#arg = it; }

  static make($ordinal,$name,arg) {
    const $self = new FOp();
    FOp.make$($self,$ordinal,$name,arg);
    return $self;
  }

  static make$($self,$ordinal,$name,arg) {
    if (arg === undefined) arg = FOpArg.None();
    sys.Enum.make$($self, $ordinal, $name);
    $self.#arg = arg;
    return;
  }

  static fromStr(name$, checked=true) {
    return sys.Enum.doFromStr(FOp.type$, FOp.vals(), name$, checked);
  }

  static vals() {
    if (FOp.#vals == null) {
      FOp.#vals = sys.List.make(FOp.type$, [
        FOp.make(0, "Nop", ),
        FOp.make(1, "LoadNull", ),
        FOp.make(2, "LoadFalse", ),
        FOp.make(3, "LoadTrue", ),
        FOp.make(4, "LoadInt", Int),
        FOp.make(5, "LoadFloat", Float),
        FOp.make(6, "LoadDecimal", Decimal),
        FOp.make(7, "LoadStr", Str),
        FOp.make(8, "LoadDuration", Duration),
        FOp.make(9, "LoadType", TypeRef),
        FOp.make(10, "LoadUri", Uri),
        FOp.make(11, "LoadVar", Register),
        FOp.make(12, "StoreVar", Register),
        FOp.make(13, "LoadInstance", FieldRef),
        FOp.make(14, "StoreInstance", FieldRef),
        FOp.make(15, "LoadStatic", FieldRef),
        FOp.make(16, "StoreStatic", FieldRef),
        FOp.make(17, "LoadMixinStatic", FieldRef),
        FOp.make(18, "StoreMixinStatic", FieldRef),
        FOp.make(19, "CallNew", MethodRef),
        FOp.make(20, "CallCtor", MethodRef),
        FOp.make(21, "CallStatic", MethodRef),
        FOp.make(22, "CallVirtual", MethodRef),
        FOp.make(23, "CallNonVirtual", MethodRef),
        FOp.make(24, "CallMixinStatic", MethodRef),
        FOp.make(25, "CallMixinVirtual", MethodRef),
        FOp.make(26, "CallMixinNonVirtual", MethodRef),
        FOp.make(27, "Jump", Jump),
        FOp.make(28, "JumpTrue", Jump),
        FOp.make(29, "JumpFalse", Jump),
        FOp.make(30, "CmpEQ", TypePair),
        FOp.make(31, "CmpNE", TypePair),
        FOp.make(32, "Cmp", TypePair),
        FOp.make(33, "CmpLE", TypePair),
        FOp.make(34, "CmpLT", TypePair),
        FOp.make(35, "CmpGT", TypePair),
        FOp.make(36, "CmpGE", TypePair),
        FOp.make(37, "CmpSame", ),
        FOp.make(38, "CmpNotSame", ),
        FOp.make(39, "CmpNull", TypeRef),
        FOp.make(40, "CmpNotNull", TypeRef),
        FOp.make(41, "Return", ),
        FOp.make(42, "Pop", TypeRef),
        FOp.make(43, "Dup", TypeRef),
        FOp.make(44, "Is", TypeRef),
        FOp.make(45, "As", TypeRef),
        FOp.make(46, "Coerce", TypePair),
        FOp.make(47, "Switch", ),
        FOp.make(48, "Throw", ),
        FOp.make(49, "Leave", Jump),
        FOp.make(50, "JumpFinally", Jump),
        FOp.make(51, "CatchAllStart", ),
        FOp.make(52, "CatchErrStart", TypeRef),
        FOp.make(53, "CatchEnd", ),
        FOp.make(54, "FinallyStart", ),
        FOp.make(55, "FinallyEnd", ),
      ]).toImmutable();
    }
    return FOp.#vals;
  }

  static static$init() {
    const $_u84 = FOp.vals();
    if (true) {
    }
    ;
    return;
  }

}

class FOpArg extends sys.Enum {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FOpArg.type$; }

  static None() { return FOpArg.vals().get(0); }

  static Int() { return FOpArg.vals().get(1); }

  static Float() { return FOpArg.vals().get(2); }

  static Decimal() { return FOpArg.vals().get(3); }

  static Str() { return FOpArg.vals().get(4); }

  static Duration() { return FOpArg.vals().get(5); }

  static Uri() { return FOpArg.vals().get(6); }

  static Register() { return FOpArg.vals().get(7); }

  static TypeRef() { return FOpArg.vals().get(8); }

  static FieldRef() { return FOpArg.vals().get(9); }

  static MethodRef() { return FOpArg.vals().get(10); }

  static Jump() { return FOpArg.vals().get(11); }

  static TypePair() { return FOpArg.vals().get(12); }

  static #vals = undefined;

  static make($ordinal,$name) {
    const $self = new FOpArg();
    FOpArg.make$($self,$ordinal,$name);
    return $self;
  }

  static make$($self,$ordinal,$name) {
    sys.Enum.make$($self, $ordinal, $name);
    return;
  }

  static fromStr(name$, checked=true) {
    return sys.Enum.doFromStr(FOpArg.type$, FOpArg.vals(), name$, checked);
  }

  static vals() {
    if (FOpArg.#vals == null) {
      FOpArg.#vals = sys.List.make(FOpArg.type$, [
        FOpArg.make(0, "None", ),
        FOpArg.make(1, "Int", ),
        FOpArg.make(2, "Float", ),
        FOpArg.make(3, "Decimal", ),
        FOpArg.make(4, "Str", ),
        FOpArg.make(5, "Duration", ),
        FOpArg.make(6, "Uri", ),
        FOpArg.make(7, "Register", ),
        FOpArg.make(8, "TypeRef", ),
        FOpArg.make(9, "FieldRef", ),
        FOpArg.make(10, "MethodRef", ),
        FOpArg.make(11, "Jump", ),
        FOpArg.make(12, "TypePair", ),
      ]).toImmutable();
    }
    return FOpArg.#vals;
  }

  static static$init() {
    const $_u85 = FOpArg.vals();
    if (true) {
    }
    ;
    return;
  }

}

class FTable extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FTable.type$; }

  #pod = null;

  pod(it) {
    if (it === undefined) {
      return this.#pod;
    }
    else {
      this.#pod = it;
      return;
    }
  }

  #table = null;

  table(it) {
    if (it === undefined) {
      return this.#table;
    }
    else {
      this.#table = it;
      return;
    }
  }

  #reverse = null;

  reverse(it) {
    if (it === undefined) {
      return this.#reverse;
    }
    else {
      this.#reverse = it;
      return;
    }
  }

  #writer = null;

  writer(it) {
    if (it === undefined) {
      return this.#writer;
    }
    else {
      this.#writer = it;
      return;
    }
  }

  #reader = null;

  reader(it) {
    if (it === undefined) {
      return this.#reader;
    }
    else {
      this.#reader = it;
      return;
    }
  }

  static makeStrs(pod) {
    const this$ = this;
    return FTable.make(pod, (out,obj) => {
      out.writeUtf(sys.ObjUtil.coerce(obj, sys.Str.type$));
      return;
    }, (in$) => {
      return sys.Str.intern(in$.readUtf());
    });
  }

  static makeTypeRefs(pod) {
    const this$ = this;
    return FTable.make(pod, (out,obj) => {
      sys.ObjUtil.coerce(obj, FTypeRef.type$).write(out);
      return;
    }, (in$) => {
      return FTypeRef.read(in$);
    });
  }

  static makeFieldRefs(pod) {
    const this$ = this;
    return FTable.make(pod, (out,obj) => {
      sys.ObjUtil.coerce(obj, FFieldRef.type$).write(out);
      return;
    }, (in$) => {
      return FFieldRef.read(in$);
    });
  }

  static makeMethodRefs(pod) {
    const this$ = this;
    return FTable.make(pod, (out,obj) => {
      sys.ObjUtil.coerce(obj, FMethodRef.type$).write(out);
      return;
    }, (in$) => {
      return FMethodRef.read(in$);
    });
  }

  static makeInts(pod) {
    const this$ = this;
    return FTable.make(pod, (out,obj) => {
      out.writeI8(sys.ObjUtil.coerce(obj, sys.Int.type$));
      return;
    }, (in$) => {
      return sys.ObjUtil.coerce(in$.readS8(), sys.Obj.type$);
    });
  }

  static makeFloats(pod) {
    const this$ = this;
    return FTable.make(pod, (out,obj) => {
      out.writeF8(sys.ObjUtil.coerce(obj, sys.Float.type$));
      return;
    }, (in$) => {
      return sys.ObjUtil.coerce(in$.readF8(), sys.Obj.type$);
    });
  }

  static makeDecimals(pod) {
    const this$ = this;
    return FTable.make(pod, (out,obj) => {
      out.writeDecimal(sys.ObjUtil.coerce(obj, sys.Decimal.type$));
      return;
    }, (in$) => {
      return in$.readDecimal();
    });
  }

  static makeDurations(pod) {
    const this$ = this;
    return FTable.make(pod, (out,obj) => {
      out.writeI8(sys.ObjUtil.coerce(obj, sys.Duration.type$).ticks());
      return;
    }, (in$) => {
      return sys.ObjUtil.coerce(sys.Duration.make(in$.readS8()), sys.Obj.type$);
    });
  }

  static make(pod,writer,reader) {
    const $self = new FTable();
    FTable.make$($self,pod,writer,reader);
    return $self;
  }

  static make$($self,pod,writer,reader) {
    $self.#pod = pod;
    $self.#writer = writer;
    $self.#reader = reader;
    $self.#table = sys.List.make(sys.Obj.type$.toNullable());
    $self.#reverse = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Obj"), sys.Type.find("sys::Int"));
    return;
  }

  isEmpty() {
    return this.#table.isEmpty();
  }

  get(index) {
    return this.#table.get(index);
  }

  add(val) {
    let index = this.#reverse.get(val);
    if (index == null) {
      (index = sys.ObjUtil.coerce(this.#table.size(), sys.Int.type$.toNullable()));
      this.#table.add(val);
      this.#reverse.set(val, sys.ObjUtil.coerce(sys.ObjUtil.coerce(index, sys.Int.type$), sys.Int.type$.toNullable()));
    }
    ;
    return sys.ObjUtil.coerce(index, sys.Int.type$);
  }

  read(in$) {
    const this$ = this;
    this.#table = sys.List.make(sys.Obj.type$.toNullable());
    if (in$ == null) {
      return this;
    }
    ;
    sys.Int.times(in$.readU2(), (it) => {
      this$.#table.add(sys.Func.call(this$.#reader, sys.ObjUtil.coerce(in$, sys.InStream.type$)));
      return;
    });
    in$.close();
    return this;
  }

  write(out) {
    const this$ = this;
    out.writeI2(this.#table.size());
    this.#table.each((obj) => {
      sys.Func.call(this$.#writer, out, obj);
      return;
    });
    out.close();
    return;
  }

}

class CNamespace extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#bridgeCache = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::CBridge"));
    this.#podCache = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::CPod"));
    this.#typeCache = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::CType"));
    return;
  }

  typeof() { return CNamespace.type$; }

  #bridgeCache = null;

  // private field reflection only
  __bridgeCache(it) { if (it === undefined) return this.#bridgeCache; else this.#bridgeCache = it; }

  #podCache = null;

  // private field reflection only
  __podCache(it) { if (it === undefined) return this.#podCache; else this.#podCache = it; }

  #typeCache = null;

  typeCache(it) {
    if (it === undefined) {
      return this.#typeCache;
    }
    else {
      this.#typeCache = it;
      return;
    }
  }

  #c = null;

  c(it) {
    if (it === undefined) {
      return this.#c;
    }
    else {
      this.#c = it;
      return;
    }
  }

  #depends = null;

  depends(it) {
    if (it === undefined) {
      return this.#depends;
    }
    else {
      this.#depends = it;
      return;
    }
  }

  #sysPod = null;

  sysPod() {
    return this.#sysPod;
  }

  #genericParams = null;

  genericParams() {
    return this.#genericParams;
  }

  #error = null;

  error() {
    return this.#error;
  }

  #nothingType = null;

  nothingType() {
    return this.#nothingType;
  }

  #itBlockType = null;

  itBlockType() {
    return this.#itBlockType;
  }

  #objType = null;

  objType() {
    return this.#objType;
  }

  #boolType = null;

  boolType() {
    return this.#boolType;
  }

  #enumType = null;

  enumType() {
    return this.#enumType;
  }

  #facetType = null;

  facetType() {
    return this.#facetType;
  }

  #intType = null;

  intType() {
    return this.#intType;
  }

  #floatType = null;

  floatType() {
    return this.#floatType;
  }

  #decimalType = null;

  decimalType() {
    return this.#decimalType;
  }

  #strType = null;

  strType() {
    return this.#strType;
  }

  #strBufType = null;

  strBufType() {
    return this.#strBufType;
  }

  #durationType = null;

  durationType() {
    return this.#durationType;
  }

  #listType = null;

  listType() {
    return this.#listType;
  }

  #mapType = null;

  mapType() {
    return this.#mapType;
  }

  #funcType = null;

  funcType() {
    return this.#funcType;
  }

  #errType = null;

  errType() {
    return this.#errType;
  }

  #podType = null;

  podType() {
    return this.#podType;
  }

  #typeType = null;

  typeType() {
    return this.#typeType;
  }

  #slotType = null;

  slotType() {
    return this.#slotType;
  }

  #fieldType = null;

  fieldType() {
    return this.#fieldType;
  }

  #methodType = null;

  methodType() {
    return this.#methodType;
  }

  #rangeType = null;

  rangeType() {
    return this.#rangeType;
  }

  #testType = null;

  testType() {
    return this.#testType;
  }

  #uriType = null;

  uriType() {
    return this.#uriType;
  }

  #voidType = null;

  voidType() {
    return this.#voidType;
  }

  #fieldNotSetErrType = null;

  fieldNotSetErrType() {
    return this.#fieldNotSetErrType;
  }

  #notImmutableErrType = null;

  notImmutableErrType() {
    return this.#notImmutableErrType;
  }

  #objTrap = null;

  objTrap() {
    return this.#objTrap;
  }

  #objWith = null;

  objWith() {
    return this.#objWith;
  }

  #objToImmutable = null;

  objToImmutable() {
    return this.#objToImmutable;
  }

  #boolNot = null;

  boolNot() {
    return this.#boolNot;
  }

  #intIncrement = null;

  intIncrement() {
    return this.#intIncrement;
  }

  #intDecrement = null;

  intDecrement() {
    return this.#intDecrement;
  }

  #intPlus = null;

  intPlus() {
    return this.#intPlus;
  }

  #floatPlus = null;

  floatPlus() {
    return this.#floatPlus;
  }

  #floatMinus = null;

  floatMinus() {
    return this.#floatMinus;
  }

  #strPlus = null;

  strPlus() {
    return this.#strPlus;
  }

  #strBufMake = null;

  strBufMake() {
    return this.#strBufMake;
  }

  #strBufAdd = null;

  strBufAdd() {
    return this.#strBufAdd;
  }

  #strBufToStr = null;

  strBufToStr() {
    return this.#strBufToStr;
  }

  #listMake = null;

  listMake() {
    return this.#listMake;
  }

  #listMakeObj = null;

  listMakeObj() {
    return this.#listMakeObj;
  }

  #listAdd = null;

  listAdd() {
    return this.#listAdd;
  }

  #mapMake = null;

  mapMake() {
    return this.#mapMake;
  }

  #mapSet = null;

  mapSet() {
    return this.#mapSet;
  }

  #enumOrdinal = null;

  enumOrdinal() {
    return this.#enumOrdinal;
  }

  #funcBind = null;

  funcBind() {
    return this.#funcBind;
  }

  #rangeMakeInclusive = null;

  rangeMakeInclusive() {
    return this.#rangeMakeInclusive;
  }

  #rangeMakeExclusive = null;

  rangeMakeExclusive() {
    return this.#rangeMakeExclusive;
  }

  #slotFindMethod = null;

  slotFindMethod() {
    return this.#slotFindMethod;
  }

  #slotFindFunc = null;

  slotFindFunc() {
    return this.#slotFindFunc;
  }

  #podFind = null;

  podFind() {
    return this.#podFind;
  }

  #podLocale = null;

  podLocale() {
    return this.#podLocale;
  }

  #typePod = null;

  typePod() {
    return this.#typePod;
  }

  #typeField = null;

  typeField() {
    return this.#typeField;
  }

  #typeMethod = null;

  typeMethod() {
    return this.#typeMethod;
  }

  #funcEnterCtor = null;

  funcEnterCtor() {
    return this.#funcEnterCtor;
  }

  #funcExitCtor = null;

  funcExitCtor() {
    return this.#funcExitCtor;
  }

  #funcCheckInCtor = null;

  funcCheckInCtor() {
    return this.#funcCheckInCtor;
  }

  #funcCall = null;

  funcCall() {
    return this.#funcCall;
  }

  #fieldNotSetErrMake = null;

  fieldNotSetErrMake() {
    return this.#fieldNotSetErrMake;
  }

  #notImmutableErrMake = null;

  notImmutableErrMake() {
    return this.#notImmutableErrMake;
  }

  init() {
    this.#sysPod = this.resolvePod("sys", null);
    this.#error = GenericParameterType.make(this, "Error");
    this.#nothingType = GenericParameterType.make(this, "Nothing");
    this.#genericParams = sys.Map.__fromLiteral(["A","B","C","D","E","F","G","H","K","L","M","R","V"], [this.genericParam("A"),this.genericParam("B"),this.genericParam("C"),this.genericParam("D"),this.genericParam("E"),this.genericParam("F"),this.genericParam("G"),this.genericParam("H"),this.genericParam("K"),this.genericParam("L"),this.genericParam("M"),this.genericParam("R"),this.genericParam("V")], sys.Type.find("sys::Str"), sys.Type.find("compiler::CType")).ro();
    this.#objType = this.sysType("Obj");
    this.#boolType = this.sysType("Bool");
    this.#enumType = this.sysType("Enum");
    this.#facetType = this.sysType("Facet");
    this.#intType = this.sysType("Int");
    this.#floatType = this.sysType("Float");
    this.#decimalType = this.sysType("Decimal");
    this.#strType = this.sysType("Str");
    this.#strBufType = this.sysType("StrBuf");
    this.#durationType = this.sysType("Duration");
    this.#listType = this.sysType("List");
    this.#mapType = this.sysType("Map");
    this.#funcType = this.sysType("Func");
    this.#errType = this.sysType("Err");
    this.#podType = this.sysType("Pod");
    this.#typeType = this.sysType("Type");
    this.#slotType = this.sysType("Slot");
    this.#fieldType = this.sysType("Field");
    this.#methodType = this.sysType("Method");
    this.#rangeType = this.sysType("Range");
    this.#testType = this.sysType("Test");
    this.#uriType = this.sysType("Uri");
    this.#voidType = this.sysType("Void");
    this.#fieldNotSetErrType = this.sysType("FieldNotSetErr");
    this.#notImmutableErrType = this.sysType("NotImmutableErr");
    this.#objTrap = this.sysMethod(sys.ObjUtil.coerce(this.#objType, CType.type$), "trap");
    this.#objWith = this.sysMethod(sys.ObjUtil.coerce(this.#objType, CType.type$), "with");
    this.#objToImmutable = this.sysMethod(sys.ObjUtil.coerce(this.#objType, CType.type$), "toImmutable");
    this.#boolNot = this.sysMethod(sys.ObjUtil.coerce(this.#boolType, CType.type$), "not");
    this.#intIncrement = this.sysMethod(sys.ObjUtil.coerce(this.#intType, CType.type$), "increment");
    this.#intDecrement = this.sysMethod(sys.ObjUtil.coerce(this.#intType, CType.type$), "decrement");
    this.#intPlus = this.sysMethod(sys.ObjUtil.coerce(this.#intType, CType.type$), "plus");
    this.#floatPlus = this.sysMethod(sys.ObjUtil.coerce(this.#floatType, CType.type$), "plus");
    this.#floatMinus = this.sysMethod(sys.ObjUtil.coerce(this.#floatType, CType.type$), "minus");
    this.#strPlus = this.sysMethod(sys.ObjUtil.coerce(this.#strType, CType.type$), "plus");
    this.#strBufMake = this.sysMethod(sys.ObjUtil.coerce(this.#strBufType, CType.type$), "make");
    this.#strBufAdd = this.sysMethod(sys.ObjUtil.coerce(this.#strBufType, CType.type$), "add");
    this.#strBufToStr = this.sysMethod(sys.ObjUtil.coerce(this.#strBufType, CType.type$), "toStr");
    this.#listMake = this.sysMethod(sys.ObjUtil.coerce(this.#listType, CType.type$), "make");
    this.#listMakeObj = this.sysMethod(sys.ObjUtil.coerce(this.#listType, CType.type$), "makeObj");
    this.#listAdd = this.sysMethod(sys.ObjUtil.coerce(this.#listType, CType.type$), "add");
    this.#mapMake = this.sysMethod(sys.ObjUtil.coerce(this.#mapType, CType.type$), "make");
    this.#mapSet = this.sysMethod(sys.ObjUtil.coerce(this.#mapType, CType.type$), "set");
    this.#enumOrdinal = this.sysMethod(sys.ObjUtil.coerce(this.#enumType, CType.type$), "ordinal");
    this.#funcBind = this.sysMethod(sys.ObjUtil.coerce(this.#funcType, CType.type$), "bind");
    this.#rangeMakeInclusive = this.sysMethod(sys.ObjUtil.coerce(this.#rangeType, CType.type$), "makeInclusive");
    this.#rangeMakeExclusive = this.sysMethod(sys.ObjUtil.coerce(this.#rangeType, CType.type$), "makeExclusive");
    this.#slotFindMethod = this.sysMethod(sys.ObjUtil.coerce(this.#slotType, CType.type$), "findMethod");
    this.#slotFindFunc = this.sysMethod(sys.ObjUtil.coerce(this.#slotType, CType.type$), "findFunc");
    this.#podFind = this.sysMethod(sys.ObjUtil.coerce(this.#podType, CType.type$), "find");
    this.#podLocale = this.sysMethod(sys.ObjUtil.coerce(this.#podType, CType.type$), "locale");
    this.#typePod = this.sysMethod(sys.ObjUtil.coerce(this.#typeType, CType.type$), "pod");
    this.#typeField = this.sysMethod(sys.ObjUtil.coerce(this.#typeType, CType.type$), "field");
    this.#typeMethod = this.sysMethod(sys.ObjUtil.coerce(this.#typeType, CType.type$), "method");
    this.#funcCall = this.sysMethod(sys.ObjUtil.coerce(this.#funcType, CType.type$), "call");
    this.#fieldNotSetErrMake = this.sysMethod(sys.ObjUtil.coerce(this.#fieldNotSetErrType, CType.type$), "make");
    this.#notImmutableErrMake = this.sysMethod(sys.ObjUtil.coerce(this.#notImmutableErrType, CType.type$), "make");
    let mockFlags = sys.Int.plus(FConst.Public(), FConst.Virtual());
    this.#funcEnterCtor = MockMethod.make(sys.ObjUtil.coerce(this.#funcType, CType.type$), "enterCtor", mockFlags, sys.ObjUtil.coerce(this.#voidType, CType.type$), sys.List.make(CType.type$.toNullable(), [this.#objType]));
    this.#funcExitCtor = MockMethod.make(sys.ObjUtil.coerce(this.#funcType, CType.type$), "exitCtor", mockFlags, sys.ObjUtil.coerce(this.#voidType, CType.type$), sys.List.make(CType.type$));
    this.#funcCheckInCtor = MockMethod.make(sys.ObjUtil.coerce(this.#funcType, CType.type$), "checkInCtor", mockFlags, sys.ObjUtil.coerce(this.#voidType, CType.type$), sys.List.make(CType.type$.toNullable(), [this.#objType]));
    this.#itBlockType = FuncType.makeItBlock(this.#objType.toNullable());
    this.#itBlockType.inferredSignature(true);
    return;
  }

  genericParam(name) {
    let t = GenericParameterType.make(this, name);
    let n = t.toNullable();
    this.#typeCache.set(t.signature(), t);
    this.#typeCache.set(n.signature(), n);
    return t;
  }

  sysType(name) {
    return sys.ObjUtil.coerce(this.#sysPod.resolveType(name, true), CType.type$);
  }

  sysMethod(t,name) {
    let m = t.method(name);
    if (m == null) {
      throw sys.Err.make(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Cannot resolve '", t.qname()), "."), name), "' method in namespace"));
    }
    ;
    return sys.ObjUtil.coerce(m, CMethod.type$);
  }

  cleanup() {
    const this$ = this;
    this.#bridgeCache.each((bridge) => {
      try {
        bridge.cleanup();
      }
      catch ($_u86) {
        $_u86 = sys.Err.make($_u86);
        if ($_u86 instanceof sys.Err) {
          let e = $_u86;
          ;
          e.trace();
        }
        else {
          throw $_u86;
        }
      }
      ;
      return;
    });
    return;
  }

  resolveBridge(name,loc) {
    let bridge = this.#bridgeCache.get(name);
    if (bridge != null) {
      return sys.ObjUtil.coerce(bridge, CBridge.type$);
    }
    ;
    (bridge = this.findBridge(this.compiler(), name, loc));
    this.#bridgeCache.set(name, sys.ObjUtil.coerce(bridge, CBridge.type$));
    return sys.ObjUtil.coerce(bridge, CBridge.type$);
  }

  findBridge(compiler,name,loc) {
    let t = sys.Env.cur().index(sys.Str.plus("compiler.bridge.", name));
    if (sys.ObjUtil.compareGT(t.size(), 1)) {
      throw CompilerErr.make(sys.Str.plus(sys.Str.plus(sys.Str.plus("Multiple FFI bridges available for '", name), "': "), t), loc);
    }
    ;
    if (sys.ObjUtil.equals(t.size(), 0)) {
      throw CompilerErr.make(sys.Str.plus(sys.Str.plus("No FFI bridge available for '", name), "'"), loc);
    }
    ;
    try {
      return sys.ObjUtil.coerce(sys.Type.find(sys.ObjUtil.coerce(t.first(), sys.Str.type$)).make(sys.List.make(Compiler.type$, [compiler])), CBridge.type$);
    }
    catch ($_u87) {
      $_u87 = sys.Err.make($_u87);
      if ($_u87 instanceof sys.Err) {
        let e = $_u87;
        ;
        throw CompilerErr.make(sys.Str.plus(sys.Str.plus("Cannot construct FFI bridge '", t.first()), "'"), loc, e);
      }
      else {
        throw $_u87;
      }
    }
    ;
  }

  resolvePod(podName,loc) {
    let pod = this.#podCache.get(podName);
    if (pod != null) {
      return sys.ObjUtil.coerce(pod, CPod.type$);
    }
    ;
    if (sys.ObjUtil.equals(sys.Str.get(podName, 0), 91)) {
      let sep = sys.Str.index(podName, "]");
      let ffi = sys.Str.getRange(podName, sys.Range.make(1, sys.ObjUtil.coerce(sep, sys.Int.type$), true));
      let package$ = sys.Str.getRange(podName, sys.Range.make(sys.Int.plus(sys.ObjUtil.coerce(sep, sys.Int.type$), 1), -1));
      (pod = this.resolveBridge(ffi, loc).resolvePod(package$, loc));
    }
    else {
      (pod = this.findPod(podName));
      if (pod == null) {
        throw CompilerErr.make(sys.Str.plus(sys.Str.plus("Pod not found '", podName), "'"), loc);
      }
      ;
    }
    ;
    this.#podCache.set(podName, sys.ObjUtil.coerce(pod, CPod.type$));
    return sys.ObjUtil.coerce(pod, CPod.type$);
  }

  resolveType(sig) {
    let t = this.#typeCache.get(sig);
    if (t != null) {
      return sys.ObjUtil.coerce(t, CType.type$);
    }
    ;
    (t = TypeParser.resolve(this, sig));
    this.#typeCache.set(sig, sys.ObjUtil.coerce(t, CType.type$));
    return sys.ObjUtil.coerce(t, CType.type$);
  }

  resolveSlot(qname) {
    let dot = sys.Str.indexr(qname, ".");
    let slot = this.resolveType(sys.Str.getRange(qname, sys.Range.make(0, sys.ObjUtil.coerce(dot, sys.Int.type$), true))).slot(sys.Str.getRange(qname, sys.Range.make(sys.Int.plus(sys.ObjUtil.coerce(dot, sys.Int.type$), 1), -1)));
    if (slot == null) {
      throw sys.Err.make(sys.Str.plus("Cannot resolve slot: ", qname));
    }
    ;
    return sys.ObjUtil.coerce(slot, CSlot.type$);
  }

  genericParameter(id) {
    let t = this.#genericParams.get(id);
    if (t == null) {
      throw sys.UnknownTypeErr.make(sys.Str.plus("sys::", id));
    }
    ;
    return sys.ObjUtil.coerce(t, CType.type$);
  }

  compiler() {
    return sys.ObjUtil.coerce(((this$) => { let $_u88 = this$.#c; if ($_u88 != null) return $_u88; throw sys.Err.make("Compiler not associated with CNamespace"); })(this), Compiler.type$);
  }

  flattenAndOrderByDepends(pods) {
    return this.orderByDepends(this.flattenDepends(pods));
  }

  flattenDepends(pods) {
    const this$ = this;
    let acc = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::CPod"));
    pods.each((pod) => {
      this$.doFlattenDepends(acc, pod);
      return;
    });
    return acc.vals();
  }

  doFlattenDepends(acc,pod) {
    const this$ = this;
    if (acc.containsKey(pod.name())) {
      return;
    }
    ;
    acc.set(pod.name(), pod);
    pod.depends().each((depend) => {
      this$.doFlattenDepends(acc, this$.resolvePod(depend.name(), null));
      return;
    });
    return;
  }

  orderByDepends(pods) {
    let left = pods.dup().sort();
    let ordered = sys.List.make(CPod.type$);
    while (!left.isEmpty()) {
      let i = 0;
      for ((i = 0); sys.ObjUtil.compareLT(i, left.size()); i = sys.Int.increment(i)) {
        if (this.noDependsInLeft(left, left.get(i))) {
          break;
        }
        ;
      }
      ;
      ordered.add(left.removeAt(i));
    }
    ;
    return ordered;
  }

  noDependsInLeft(left,p) {
    let depends = p.depends();
    for (let i = 0; sys.ObjUtil.compareLT(i, depends.size()); i = sys.Int.increment(i)) {
      let d = depends.get(i);
      for (let j = 0; sys.ObjUtil.compareLT(j, left.size()); j = sys.Int.increment(j)) {
        if (sys.ObjUtil.equals(d.name(), left.get(j).name())) {
          return false;
        }
        ;
      }
      ;
    }
    ;
    return true;
  }

  static make() {
    const $self = new CNamespace();
    CNamespace.make$($self);
    return $self;
  }

  static make$($self) {
    ;
    return;
  }

}

class FPodNamespace extends CNamespace {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FPodNamespace.type$; }

  #dir = null;

  dir() { return this.#dir; }

  __dir(it) { if (it === undefined) return this.#dir; else this.#dir = it; }

  static make(dir) {
    const $self = new FPodNamespace();
    FPodNamespace.make$($self,dir);
    return $self;
  }

  static make$($self,dir) {
    CNamespace.make$($self);
    $self.#dir = dir;
    $self.init();
    return;
  }

  findPod(podName) {
    let file = null;
    try {
      if (this.#dir != null) {
        (file = this.#dir.plus(sys.Str.toUri(sys.Str.plus(sys.Str.plus("", podName), ".pod"))));
      }
      else {
        (file = sys.Env.cur().findPodFile(podName));
      }
      ;
    }
    catch ($_u89) {
      return null;
    }
    ;
    if (!file.exists()) {
      return null;
    }
    ;
    let fpod = FPod.make(this, podName, sys.Zip.open(sys.ObjUtil.coerce(file, sys.File.type$)));
    fpod.read();
    return fpod;
  }

}

class Fanp extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#showTables = false;
    this.#showCode = false;
    this.#showLines = false;
    this.#showIndex = false;
    return;
  }

  typeof() { return Fanp.type$; }

  #compiler = null;

  compiler(it) {
    if (it === undefined) {
      return this.#compiler;
    }
    else {
      this.#compiler = it;
      return;
    }
  }

  #showTables = false;

  showTables(it) {
    if (it === undefined) {
      return this.#showTables;
    }
    else {
      this.#showTables = it;
      return;
    }
  }

  #showCode = false;

  showCode(it) {
    if (it === undefined) {
      return this.#showCode;
    }
    else {
      this.#showCode = it;
      return;
    }
  }

  #showLines = false;

  showLines(it) {
    if (it === undefined) {
      return this.#showLines;
    }
    else {
      this.#showLines = it;
      return;
    }
  }

  #showIndex = false;

  showIndex(it) {
    if (it === undefined) {
      return this.#showIndex;
    }
    else {
      this.#showIndex = it;
      return;
    }
  }

  #file = null;

  file(it) {
    if (it === undefined) {
      return this.#file;
    }
    else {
      this.#file = it;
      return;
    }
  }

  execute(target) {
    let colon = sys.Str.index(target, ":");
    let dot = sys.Str.index(target, ".");
    if (colon == null) {
      this.printPod(sys.ObjUtil.coerce(sys.Pod.find(target), sys.Pod.type$));
    }
    else {
      if (sys.ObjUtil.compareLT(dot, 0)) {
        this.printType(sys.ObjUtil.coerce(sys.Type.find(target), sys.Type.type$));
      }
      else {
        this.printSlot(sys.ObjUtil.coerce(sys.Slot.find(target), sys.Slot.type$));
      }
      ;
    }
    ;
    return;
  }

  executeFile(target) {
    const this$ = this;
    let scriptFile = sys.File.os(sys.ObjUtil.coerce(this.#file, sys.Str.type$));
    let input = sys.ObjUtil.coerce(sys.ObjUtil.with(CompilerInput.make(), (it) => {
      it.podName("script");
      it.summary("script");
      it.version(sys.Version.fromStr("0"));
      it.log().level(sys.LogLevel.warn());
      it.includeDoc(true);
      it.isScript(true);
      it.srcStr(scriptFile.readAllStr());
      it.srcStrLoc(Loc.makeFile(scriptFile));
      it.mode(CompilerInputMode.str());
      it.output(CompilerOutputMode.transientPod());
      return;
    }), CompilerInput.type$);
    this.#compiler = Compiler.make(input);
    this.#compiler.compile();
    let pod = this.#compiler.output().transientPod();
    if (target == null) {
      this.printPod(sys.ObjUtil.coerce(pod, sys.Pod.type$));
      return;
    }
    ;
    let dot = sys.Str.index(target, ".");
    if (sys.ObjUtil.compareLT(dot, 0)) {
      this.printType(sys.ObjUtil.coerce(pod.type(sys.ObjUtil.coerce(target, sys.Str.type$)), sys.Type.type$));
    }
    else {
      let typeName = sys.Str.getRange(target, sys.Range.make(0, sys.ObjUtil.coerce(dot, sys.Int.type$), true));
      let slotName = sys.Str.getRange(target, sys.Range.make(sys.Int.plus(sys.ObjUtil.coerce(dot, sys.Int.type$), 1), -1));
      this.printSlot(sys.ObjUtil.coerce(pod.type(typeName).slot(slotName), sys.Slot.type$));
    }
    ;
    return;
  }

  printPod(pod) {
    let p = this.printer(pod);
    if (this.#showTables) {
      p.tables();
      return;
    }
    ;
    p.ftypes();
    return;
  }

  printType(t) {
    let p = this.printer(sys.ObjUtil.coerce(t.pod(), sys.Pod.type$));
    if (this.#showTables) {
      p.tables();
      return;
    }
    ;
    let ftype = this.ftype(p.pod(), t.name());
    p.ftype(ftype);
    return;
  }

  printSlot(slot) {
    let p = this.printer(sys.ObjUtil.coerce(slot.parent().pod(), sys.Pod.type$));
    if (this.#showTables) {
      p.tables();
      return;
    }
    ;
    let fslot = this.fslot(this.ftype(p.pod(), slot.parent().name()), slot.name());
    p.slot(fslot);
    return;
  }

  printer(pod) {
    let printer = FPrinter.make(this.fpod(pod.name()));
    printer.showCode(this.#showCode);
    printer.showLines(this.#showLines);
    printer.showIndex(this.#showIndex);
    return printer;
  }

  fpod(podName) {
    if (this.#file == null) {
      let ns = FPodNamespace.make(null);
      let fpod = sys.ObjUtil.coerce(ns.resolvePod(podName, null), FPod.type$);
      fpod.readFully();
      return fpod;
    }
    else {
      return sys.ObjUtil.coerce(this.#compiler.fpod(), FPod.type$);
    }
    ;
  }

  ftype(pod,typeName) {
    const this$ = this;
    let ftype = pod.ftypes().find((ft) => {
      let r = pod.typeRef(ft.self());
      return sys.ObjUtil.equals(typeName, pod.n(r.typeName()));
    });
    if (ftype == null) {
      throw sys.UnknownTypeErr.make(sys.Str.plus(sys.Str.plus(pod.name(), "::"), typeName));
    }
    ;
    return sys.ObjUtil.coerce(ftype, FType.type$);
  }

  fslot(ftype,slotName) {
    const this$ = this;
    let slot = null;
    (slot = ftype.ffields().find((s) => {
      return sys.ObjUtil.equals(slotName, ftype.fpod().n(s.nameIndex()));
    }));
    if (slot != null) {
      return sys.ObjUtil.coerce(slot, FSlot.type$);
    }
    ;
    (slot = ftype.fmethods().find((s) => {
      return sys.ObjUtil.equals(slotName, ftype.fpod().n(s.nameIndex()));
    }));
    if (slot != null) {
      return sys.ObjUtil.coerce(slot, FSlot.type$);
    }
    ;
    throw sys.UnknownSlotErr.make(slotName);
  }

  run(args) {
    if (args.isEmpty()) {
      this.help();
      return;
    }
    ;
    let target = null;
    for (let i = 0; sys.ObjUtil.compareLT(i, args.size()); i = sys.Int.increment(i)) {
      let a = args.get(i);
      if (sys.Str.isEmpty(a)) {
        return;
      }
      ;
      if ((sys.ObjUtil.equals(a, "-help") || sys.ObjUtil.equals(a, "-h") || sys.ObjUtil.equals(a, "-?"))) {
        this.help();
        return;
      }
      else {
        if (sys.ObjUtil.equals(a, "-t")) {
          this.#showTables = true;
        }
        else {
          if (sys.ObjUtil.equals(a, "-c")) {
            this.#showCode = true;
          }
          else {
            if (sys.ObjUtil.equals(a, "-l")) {
              this.#showLines = true;
            }
            else {
              if (sys.ObjUtil.equals(a, "-i")) {
                this.#showIndex = true;
              }
              else {
                if (sys.ObjUtil.equals(a, "-f")) {
                  i = sys.Int.plus(i, 1);
                  this.#file = args.get(i);
                }
                else {
                  if (sys.ObjUtil.equals(sys.Str.get(a, 0), 45)) {
                    sys.ObjUtil.echo(sys.Str.plus("WARNING: Unknown option ", a));
                  }
                  else {
                    (target = a);
                  }
                  ;
                }
                ;
              }
              ;
            }
            ;
          }
          ;
        }
        ;
      }
      ;
    }
    ;
    if ((target == null && this.#file == null)) {
      this.help();
      return;
    }
    ;
    if (this.#file == null) {
      this.execute(sys.ObjUtil.coerce(target, sys.Str.type$));
    }
    else {
      this.executeFile(target);
    }
    ;
    return;
  }

  help() {
    sys.ObjUtil.echo("Fantom Disassembler");
    sys.ObjUtil.echo("Usage:");
    sys.ObjUtil.echo("  fanp [options] <pod>");
    sys.ObjUtil.echo("  fanp [options] <pod>::<type>");
    sys.ObjUtil.echo("  fanp [options] <pod>::<type>.<method>");
    sys.ObjUtil.echo("Options:");
    sys.ObjUtil.echo("  -help, -h, -?  print usage help");
    sys.ObjUtil.echo("  -t             print constant pool tables");
    sys.ObjUtil.echo("  -c             print code buffers");
    sys.ObjUtil.echo("  -l             print line number table");
    sys.ObjUtil.echo("  -i             print table indexes in code");
    sys.ObjUtil.echo("  -f <file>      disassemble from script file");
    return;
  }

  static main() {
    Fanp.make().run(sys.Env.cur().args());
    return;
  }

  static make() {
    const $self = new Fanp();
    Fanp.make$($self);
    return $self;
  }

  static make$($self) {
    ;
    return;
  }

}

class FType extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#hollow = true;
    return;
  }

  typeof() { return FType.type$; }

  isNum() { return CType.prototype.isNum.apply(this, arguments); }

  inferredAs() { return CType.prototype.inferredAs.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CType.prototype.isSynthetic.apply(this, arguments); }

  fitsAny() { return CType.prototype.fitsAny.apply(this, arguments); }

  isNative() { return CType.prototype.isNative.apply(this, arguments); }

  isConstFieldType() { return CType.prototype.isConstFieldType.apply(this, arguments); }

  isObj() { return CType.prototype.isObj.apply(this, arguments); }

  isBool() { return CType.prototype.isBool.apply(this, arguments); }

  isBuf() { return CType.prototype.isBuf.apply(this, arguments); }

  isInt() { return CType.prototype.isInt.apply(this, arguments); }

  isDecimal() { return CType.prototype.isDecimal.apply(this, arguments); }

  isFacet() { return CType.prototype.isFacet.apply(this, arguments); }

  isNothing() { return CType.prototype.isNothing.apply(this, arguments); }

  toStr() { return CType.prototype.toStr.apply(this, arguments); }

  method() { return CType.prototype.method.apply(this, arguments); }

  foreignInheritance() { return CType.prototype.foreignInheritance.apply(this, arguments); }

  raw() { return CType.prototype.raw.apply(this, arguments); }

  isSupported() { return CType.prototype.isSupported.apply(this, arguments); }

  isFunc() { return CType.prototype.isFunc.apply(this, arguments); }

  fits() { return CType.prototype.fits.apply(this, arguments); }

  isType() { return CType.prototype.isType.apply(this, arguments); }

  isMap() { return CType.prototype.isMap.apply(this, arguments); }

  isInternal() { return CType.prototype.isInternal.apply(this, arguments); }

  hasSlot() { return CType.prototype.hasSlot.apply(this, arguments); }

  field() { return CType.prototype.field.apply(this, arguments); }

  bridge() { return CType.prototype.bridge.apply(this, arguments); }

  fields() { return CType.prototype.fields.apply(this, arguments); }

  hash() { return CType.prototype.hash.apply(this, arguments); }

  toNonNullable() { return CType.prototype.toNonNullable.apply(this, arguments); }

  isThis() { return CType.prototype.isThis.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  deref() { return CType.prototype.deref.apply(this, arguments); }

  instanceCtors() { return CType.prototype.instanceCtors.apply(this, arguments); }

  isForeign() { return CType.prototype.isForeign.apply(this, arguments); }

  methods() { return CType.prototype.methods.apply(this, arguments); }

  slot() { return CType.prototype.slot.apply(this, arguments); }

  isList() { return CType.prototype.isList.apply(this, arguments); }

  parameterizeThis() { return CType.prototype.parameterizeThis.apply(this, arguments); }

  isConst() { return CType.prototype.isConst.apply(this, arguments); }

  isPublic() { return CType.prototype.isPublic.apply(this, arguments); }

  isStr() { return CType.prototype.isStr.apply(this, arguments); }

  isFinal() { return CType.prototype.isFinal.apply(this, arguments); }

  isClass() { return CType.prototype.isClass.apply(this, arguments); }

  isValid() { return CType.prototype.isValid.apply(this, arguments); }

  isEnum() { return CType.prototype.isEnum.apply(this, arguments); }

  isFloat() { return CType.prototype.isFloat.apply(this, arguments); }

  isMixin() { return CType.prototype.isMixin.apply(this, arguments); }

  isAbstract() { return CType.prototype.isAbstract.apply(this, arguments); }

  isRange() { return CType.prototype.isRange.apply(this, arguments); }

  isVoid() { return CType.prototype.isVoid.apply(this, arguments); }

  ctors() { return CType.prototype.ctors.apply(this, arguments); }

  equals() { return CType.prototype.equals.apply(this, arguments); }

  #base = null;

  base(it) {
    if (it === undefined) {
      if (this.#base == null) {
        this.#base = this.#fpod.toType(this.#fbase);
      }
      ;
      return this.#base;
    }
    else {
      this.#base = it;
      return;
    }
  }

  #slotsCached = null;

  // private field reflection only
  __slotsCached(it) { if (it === undefined) return this.#slotsCached; else this.#slotsCached = it; }

  #flags = 0;

  flags(it) {
    if (it === undefined) {
      return this.#flags;
    }
    else {
      this.#flags = it;
      return;
    }
  }

  #hollow = false;

  hollow(it) {
    if (it === undefined) {
      return this.#hollow;
    }
    else {
      this.#hollow = it;
      return;
    }
  }

  #fpod = null;

  fpod(it) {
    if (it === undefined) {
      return this.#fpod;
    }
    else {
      this.#fpod = it;
      return;
    }
  }

  #self = 0;

  self(it) {
    if (it === undefined) {
      return this.#self;
    }
    else {
      this.#self = it;
      return;
    }
  }

  #fbase = 0;

  fbase(it) {
    if (it === undefined) {
      return this.#fbase;
    }
    else {
      this.#fbase = it;
      return;
    }
  }

  #fmixins = null;

  fmixins(it) {
    if (it === undefined) {
      return this.#fmixins;
    }
    else {
      this.#fmixins = it;
      return;
    }
  }

  #ffields = null;

  ffields(it) {
    if (it === undefined) {
      return this.#ffields;
    }
    else {
      this.#ffields = it;
      return;
    }
  }

  #fmethods = null;

  fmethods(it) {
    if (it === undefined) {
      return this.#fmethods;
    }
    else {
      this.#fmethods = it;
      return;
    }
  }

  #fattrs = null;

  fattrs(it) {
    if (it === undefined) {
      return this.#fattrs;
    }
    else {
      this.#fattrs = it;
      return;
    }
  }

  #ffacets = null;

  ffacets(it) {
    if (it === undefined) {
      return this.#ffacets;
    }
    else {
      this.#ffacets = it;
      return;
    }
  }

  #name$Store = undefined;

  // private field reflection only
  __name$Store(it) { if (it === undefined) return this.#name$Store; else this.#name$Store = it; }

  #qname$Store = undefined;

  // private field reflection only
  __qname$Store(it) { if (it === undefined) return this.#qname$Store; else this.#qname$Store = it; }

  #mixins$Store = undefined;

  // private field reflection only
  __mixins$Store(it) { if (it === undefined) return this.#mixins$Store; else this.#mixins$Store = it; }

  #isVal$Store = undefined;

  // private field reflection only
  __isVal$Store(it) { if (it === undefined) return this.#isVal$Store; else this.#isVal$Store = it; }

  #operators$Store = undefined;

  // private field reflection only
  __operators$Store(it) { if (it === undefined) return this.#operators$Store; else this.#operators$Store = it; }

  #toNullable$Store = undefined;

  // private field reflection only
  __toNullable$Store(it) { if (it === undefined) return this.#toNullable$Store; else this.#toNullable$Store = it; }

  #toListOf$Store = undefined;

  // private field reflection only
  __toListOf$Store(it) { if (it === undefined) return this.#toListOf$Store; else this.#toListOf$Store = it; }

  static make(fpod) {
    const $self = new FType();
    FType.make$($self,fpod);
    return $self;
  }

  static make$($self,fpod) {
    ;
    $self.#fpod = fpod;
    $self.#fattrs = sys.List.make(FAttr.type$);
    return;
  }

  ns() {
    return this.#fpod.ns();
  }

  pod() {
    return this.#fpod;
  }

  name() {
    if (this.#name$Store === undefined) {
      this.#name$Store = this.name$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#name$Store, sys.Str.type$);
  }

  qname() {
    if (this.#qname$Store === undefined) {
      this.#qname$Store = this.qname$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#qname$Store, sys.Str.type$);
  }

  signature() {
    return this.qname();
  }

  doc() {
    return null;
  }

  attr(name) {
    const this$ = this;
    return this.#fattrs.find((a) => {
      return sys.ObjUtil.equals(this$.#fpod.n(a.name()), name);
    });
  }

  mixins() {
    if (this.#mixins$Store === undefined) {
      this.#mixins$Store = this.mixins$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#mixins$Store, sys.Type.find("compiler::CType[]"));
  }

  isVal() {
    if (this.#isVal$Store === undefined) {
      this.#isVal$Store = sys.ObjUtil.coerce(this.isVal$Once(), sys.Obj.type$.toNullable());
    }
    ;
    return sys.ObjUtil.coerce(this.#isVal$Store, sys.Bool.type$);
  }

  facet(qname) {
    const this$ = this;
    if (this.#ffacets == null) {
      this.reflect();
    }
    ;
    return this.#ffacets.find((f) => {
      return sys.ObjUtil.equals(f.qname(), qname);
    });
  }

  slots() {
    if (this.#slotsCached == null) {
      this.reflect();
    }
    ;
    return sys.ObjUtil.coerce(this.#slotsCached, sys.Type.find("[sys::Str:compiler::CSlot]"));
  }

  operators() {
    if (this.#operators$Store === undefined) {
      this.#operators$Store = this.operators$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#operators$Store, COperators.type$);
  }

  isNullable() {
    return false;
  }

  toNullable() {
    if (this.#toNullable$Store === undefined) {
      this.#toNullable$Store = this.toNullable$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#toNullable$Store, CType.type$);
  }

  isGeneric() {
    return (sys.ObjUtil.equals(this.#fpod.name(), "sys") && (sys.ObjUtil.equals(this.name(), "List") || sys.ObjUtil.equals(this.name(), "Map") || sys.ObjUtil.equals(this.name(), "Func")));
  }

  isParameterized() {
    return false;
  }

  isGenericParameter() {
    return (sys.ObjUtil.equals(this.#fpod.name(), "sys") && sys.ObjUtil.equals(sys.Str.size(this.name()), 1));
  }

  toListOf() {
    if (this.#toListOf$Store === undefined) {
      this.#toListOf$Store = this.toListOf$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#toListOf$Store, CType.type$);
  }

  reflect() {
    const this$ = this;
    this.read();
    this.#slotsCached = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::CSlot"));
    this.#ffields.each((f) => {
      this$.slots().set(f.name(), f);
      return;
    });
    this.#fmethods.each((m) => {
      let f = sys.ObjUtil.coerce(this$.slots().get(m.name()), FField.type$.toNullable());
      if (f != null) {
        if (sys.ObjUtil.compareNE(sys.Int.and(m.flags(), FConst.Getter()), 0)) {
          f.getter(m);
        }
        else {
          if (sys.ObjUtil.compareNE(sys.Int.and(m.flags(), FConst.Setter()), 0)) {
            f.setter(m);
          }
          else {
            throw sys.Err.make(sys.Str.plus(sys.Str.plus(sys.Str.plus("Conflicting slots: ", f), " and "), m));
          }
          ;
        }
        ;
      }
      else {
        this$.#slotsCached.set(m.name(), m);
      }
      ;
      return;
    });
    if (this.base() != null) {
      this.inherit(sys.ObjUtil.coerce(this.base(), CType.type$));
    }
    ;
    this.mixins().each((t) => {
      this$.inherit(t);
      return;
    });
    return;
  }

  inherit(t) {
    const this$ = this;
    t.slots().each((newSlot) => {
      if (this$.#slotsCached.get(newSlot.name()) != null) {
        return;
      }
      ;
      if ((newSlot.isCtor() || newSlot.isPrivate() || (newSlot.isInternal() && sys.ObjUtil.compareNE(newSlot.parent().pod(), t.pod())))) {
        return;
      }
      ;
      this$.#slotsCached.set(newSlot.name(), newSlot);
      return;
    });
    return;
  }

  writeMeta(out) {
    const this$ = this;
    out.writeI2(this.#self);
    out.writeI2(this.#fbase);
    out.writeI2(this.#fmixins.size());
    this.#fmixins.each((m) => {
      out.writeI2(m);
      return;
    });
    out.writeI4(sys.Int.and(this.flags(), FConst.FlagsMask()));
    return;
  }

  readMeta(in$) {
    const this$ = this;
    this.#self = in$.readU2();
    this.#fbase = in$.readU2();
    this.#fmixins = sys.List.make(sys.Int.type$);
    sys.Int.times(in$.readU2(), (it) => {
      this$.#fmixins.add(sys.ObjUtil.coerce(in$.readU2(), sys.Int.type$.toNullable()));
      return;
    });
    this.flags(in$.readU4());
    return this;
  }

  uri() {
    return sys.ObjUtil.coerce(sys.Uri.fromStr(sys.Str.plus(sys.Str.plus("/fcode/", this.#fpod.n(this.#fpod.typeRef(this.#self).typeName())), ".fcode")), sys.Uri.type$);
  }

  write() {
    const this$ = this;
    let out = this.#fpod.out(this.uri());
    out.writeI2(this.#ffields.size());
    this.#ffields.each((f) => {
      f.write(out);
      return;
    });
    out.writeI2(this.#fmethods.size());
    this.#fmethods.each((m) => {
      m.write(out);
      return;
    });
    out.writeI2(this.#fattrs.size());
    this.#fattrs.each((a) => {
      a.write(out);
      return;
    });
    out.close();
    return;
  }

  read() {
    const this$ = this;
    let in$ = this.#fpod.in(this.uri());
    this.#ffields = sys.List.make(FField.type$);
    sys.Int.times(in$.readU2(), (it) => {
      this$.#ffields.add(FField.make(this$).read(sys.ObjUtil.coerce(in$, sys.InStream.type$)));
      return;
    });
    this.#fmethods = sys.List.make(FMethod.type$);
    sys.Int.times(in$.readU2(), (it) => {
      this$.#fmethods.add(FMethod.make(this$).read(sys.ObjUtil.coerce(in$, sys.InStream.type$)));
      return;
    });
    this.#fattrs = sys.List.make(FAttr.type$);
    sys.Int.times(in$.readU2(), (it) => {
      this$.#fattrs.add(FAttr.make().read(sys.ObjUtil.coerce(in$, sys.InStream.type$)));
      return;
    });
    this.#ffacets = FFacet.decode(this.#fpod, this.attr(FConst.FacetsAttr()));
    in$.close();
    return;
  }

  name$Once() {
    return this.#fpod.n(this.#fpod.typeRef(this.#self).typeName());
  }

  qname$Once() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#fpod.name()), "::"), this.name());
  }

  mixins$Once() {
    return this.#fpod.resolveTypes(sys.ObjUtil.coerce(this.#fmixins, sys.Type.find("sys::Int[]")));
  }

  isVal$Once() {
    return CType.isValType(this.qname());
  }

  operators$Once() {
    return COperators.make(this);
  }

  toNullable$Once() {
    return NullableType.make(this);
  }

  toListOf$Once() {
    return ListType.make(this);
  }

}

class COperators extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#byPrefix = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::CMethod[]"));
    return;
  }

  typeof() { return COperators.type$; }

  static #prefixes = undefined;

  static prefixes() {
    if (COperators.#prefixes === undefined) {
      COperators.static$init();
      if (COperators.#prefixes === undefined) COperators.#prefixes = null;
    }
    return COperators.#prefixes;
  }

  static #exacts = undefined;

  static exacts() {
    if (COperators.#exacts === undefined) {
      COperators.static$init();
      if (COperators.#exacts === undefined) COperators.#exacts = null;
    }
    return COperators.#exacts;
  }

  #parent = null;

  parent() {
    return this.#parent;
  }

  #byPrefix = null;

  // private field reflection only
  __byPrefix(it) { if (it === undefined) return this.#byPrefix; else this.#byPrefix = it; }

  static make(parent) {
    const $self = new COperators();
    COperators.make$($self,parent);
    return $self;
  }

  static make$($self,parent) {
    const this$ = $self;
    ;
    $self.#parent = parent;
    parent.slots().each((slot) => {
      if ((sys.ObjUtil.is(slot, CMethod.type$) && slot.hasFacet("sys::Operator"))) {
        let prefix = COperators.toPrefix(slot.name());
        if (prefix == null) {
          sys.ObjUtil.echo(sys.Str.plus("WARN: operator method has invalid perfix: ", slot.qname()));
        }
        else {
          let acc = this$.#byPrefix.get(sys.ObjUtil.coerce(prefix, sys.Str.type$));
          if (acc == null) {
            this$.#byPrefix.set(sys.ObjUtil.coerce(prefix, sys.Str.type$), sys.ObjUtil.coerce((acc = sys.List.make(CMethod.type$)), sys.Type.find("compiler::CMethod[]")));
          }
          ;
          acc.add(sys.ObjUtil.coerce(slot, CMethod.type$));
        }
        ;
      }
      ;
      return;
    });
    return;
  }

  static toPrefix(methodName) {
    const this$ = this;
    return ((this$) => { let $_u90 = COperators.exacts().get(methodName); if ($_u90 != null) return $_u90; return COperators.prefixes().find((p) => {
      return sys.Str.startsWith(methodName, p);
    }); })(this);
  }

  find(prefix) {
    return sys.ObjUtil.coerce(((this$) => { let $_u91 = this$.#byPrefix.get(prefix); if ($_u91 != null) return $_u91; return CMethod.type$.emptyList(); })(this), sys.Type.find("compiler::CMethod[]"));
  }

  static static$init() {
    COperators.#prefixes = sys.ObjUtil.coerce(sys.ObjUtil.toImmutable(sys.List.make(sys.Str.type$, ["get", "plus", "minus", "mult", "div", "mod"])), sys.Type.find("sys::Str[]"));
    COperators.#exacts = sys.ObjUtil.coerce(sys.ObjUtil.toImmutable(sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("sys::Str")).setList(COperators.#prefixes).setList(sys.List.make(sys.Str.type$, ["set", "negate", "increment", "decrement", "add"]))), sys.Type.find("[sys::Str:sys::Str]"));
    return;
  }

}

class ReflectSlot extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    return;
  }

  typeof() { return ReflectSlot.type$; }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  ns() { return CSlot.prototype.ns.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  #flags$Store = undefined;

  // private field reflection only
  __flags$Store(it) { if (it === undefined) return this.#flags$Store; else this.#flags$Store = it; }

  doc() {
    return CDoc.fromStr(this.slot().doc());
  }

  name() {
    return this.slot().name();
  }

  qname() {
    return this.slot().qname();
  }

  signature() {
    return this.slot().signature();
  }

  flags() {
    if (this.#flags$Store === undefined) {
      this.#flags$Store = sys.ObjUtil.coerce(this.flags$Once(), sys.Obj.type$.toNullable());
    }
    ;
    return sys.ObjUtil.coerce(this.#flags$Store, sys.Int.type$);
  }

  facet(qname) {
    try {
      return ReflectFacet.map(sys.ObjUtil.coerce(this.ns(), ReflectNamespace.type$), this.slot().facet(sys.ObjUtil.coerce(sys.Type.find(qname), sys.Type.type$), false));
    }
    catch ($_u92) {
      $_u92 = sys.Err.make($_u92);
      if ($_u92 instanceof sys.Err) {
        let e = $_u92;
        ;
        e.trace();
      }
      else {
        throw $_u92;
      }
    }
    ;
    return null;
  }

  static make() {
    const $self = new ReflectSlot();
    ReflectSlot.make$($self);
    return $self;
  }

  static make$($self) {
    ;
    return;
  }

  flags$Once() {
    return sys.ObjUtil.coerce(sys.ObjUtil.trap(this.slot(),"flags", sys.List.make(sys.Obj.type$.toNullable(), [])), sys.Int.type$);
  }

}

class ReflectField extends ReflectSlot {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ReflectField.type$; }

  isParameterized() { return CField.prototype.isParameterized.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  fieldType() { return CField.prototype.fieldType.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  usesBridge() { return CField.prototype.usesBridge.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isGeneric() { return CField.prototype.isGeneric.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isCovariant() { return CField.prototype.isCovariant.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  #ns = null;

  ns(it) {
    if (it === undefined) {
      return this.#ns;
    }
    else {
      this.#ns = it;
      return;
    }
  }

  #parent = null;

  parent(it) {
    if (it === undefined) {
      return this.#parent;
    }
    else {
      this.#parent = it;
      return;
    }
  }

  #type = null;

  type() {
    return this.#type;
  }

  #getter = null;

  getter() {
    return this.#getter;
  }

  #setter = null;

  setter() {
    return this.#setter;
  }

  #f = null;

  f() {
    return this.#f;
  }

  static make(ns,parent,f) {
    const $self = new ReflectField();
    ReflectField.make$($self,ns,parent,f);
    return $self;
  }

  static make$($self,ns,parent,f) {
    ReflectSlot.make$($self);
    $self.ns(ns);
    $self.parent(parent);
    $self.#f = f;
    $self.#type = sys.ObjUtil.coerce(ns.importType(f.type()), CType.type$);
    let get$ = sys.ObjUtil.coerce(sys.ObjUtil.trap(f,"getter", sys.List.make(sys.Obj.type$.toNullable(), [])), sys.Method.type$.toNullable());
    if (get$ != null) {
      $self.#getter = ns.importMethod(sys.ObjUtil.coerce(get$, sys.Method.type$));
    }
    ;
    let set$ = sys.ObjUtil.coerce(sys.ObjUtil.trap(f,"setter", sys.List.make(sys.Obj.type$.toNullable(), [])), sys.Method.type$.toNullable());
    if (set$ != null) {
      $self.#setter = ns.importMethod(sys.ObjUtil.coerce(set$, sys.Method.type$));
    }
    ;
    return;
  }

  slot() {
    return this.#f;
  }

  inheritedReturns() {
    if ((!this.isOverride() || this.#getter == null)) {
      return this.#type;
    }
    else {
      return this.#getter.inheritedReturns();
    }
    ;
  }

}

class ReflectMethod extends ReflectSlot {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ReflectMethod.type$; }

  isParameterized() { return CMethod.prototype.isParameterized.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  hasSameParams() { return CMethod.prototype.hasSameParams.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  inheritedReturnType() { return CMethod.prototype.inheritedReturnType.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  nameAndParamTypesToStr() { return CMethod.prototype.nameAndParamTypesToStr.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  returnType() { return CMethod.prototype.returnType.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  usesBridge() { return CMethod.prototype.usesBridge.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  generic() { return CMethod.prototype.generic.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isCovariant() { return CMethod.prototype.isCovariant.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  #ns = null;

  ns(it) {
    if (it === undefined) {
      return this.#ns;
    }
    else {
      this.#ns = it;
      return;
    }
  }

  #parent = null;

  parent(it) {
    if (it === undefined) {
      return this.#parent;
    }
    else {
      this.#parent = it;
      return;
    }
  }

  #returns = null;

  returns() {
    return this.#returns;
  }

  #params = null;

  params() {
    return this.#params;
  }

  #isGeneric = false;

  isGeneric() {
    return this.#isGeneric;
  }

  #m = null;

  m() {
    return this.#m;
  }

  static make(ns,parent,m) {
    const $self = new ReflectMethod();
    ReflectMethod.make$($self,ns,parent,m);
    return $self;
  }

  static make$($self,ns,parent,m) {
    const this$ = $self;
    ReflectSlot.make$($self);
    $self.ns(ns);
    $self.parent(parent);
    $self.#m = m;
    $self.#returns = sys.ObjUtil.coerce(ns.importType(m.returns()), CType.type$);
    $self.#params = sys.ObjUtil.coerce(m.params().map((p) => {
      return ReflectParam.make(ns, p);
    }, CParam.type$), sys.Type.find("compiler::CParam[]"));
    $self.#isGeneric = CMethod.calcGeneric($self);
    return;
  }

  slot() {
    return this.#m;
  }

  inheritedReturns() {
    if ((this.isOverride() || this.#returns.isThis())) {
      return sys.ObjUtil.coerce(this.ns().importType(sys.ObjUtil.coerce(sys.ObjUtil.trap(this.#m,"inheritedReturns", sys.List.make(sys.Obj.type$.toNullable(), [])), sys.Type.type$)), CType.type$);
    }
    else {
      return this.#returns;
    }
    ;
  }

}

class ReflectParam extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ReflectParam.type$; }

  paramType() { return CParam.prototype.paramType.apply(this, arguments); }

  #type = null;

  type() {
    return this.#type;
  }

  #p = null;

  p() {
    return this.#p;
  }

  static make(ns,p) {
    const $self = new ReflectParam();
    ReflectParam.make$($self,ns,p);
    return $self;
  }

  static make$($self,ns,p) {
    $self.#p = p;
    $self.#type = sys.ObjUtil.coerce(ns.importType(p.type()), CType.type$);
    return;
  }

  name() {
    return this.#p.name();
  }

  hasDefault() {
    return this.#p.hasDefault();
  }

}

class ReflectFacet extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ReflectFacet.type$; }

  #f = null;

  f(it) {
    if (it === undefined) {
      return this.#f;
    }
    else {
      this.#f = it;
      return;
    }
  }

  static map(ns,f) {
    if (f == null) {
      return null;
    }
    ;
    return ReflectFacet.make(sys.ObjUtil.coerce(f, sys.Facet.type$));
  }

  static make(f) {
    const $self = new ReflectFacet();
    ReflectFacet.make$($self,f);
    return $self;
  }

  static make$($self,f) {
    $self.#f = f;
    return;
  }

  qname() {
    return sys.ObjUtil.typeof(this.#f).qname();
  }

  get(name) {
    return ((this$) => { let $_u93 = sys.ObjUtil.typeof(this$.#f).field(name, false); if ($_u93 == null) return null; return sys.ObjUtil.typeof(this$.#f).field(name, false).get(this$.#f); })(this);
  }

  toStr() {
    return sys.ObjUtil.toStr(this.#f);
  }

}

class ReflectPod extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#typeMap = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::ReflectType"));
    this.#loadedAllTypes = false;
    return;
  }

  typeof() { return ReflectPod.type$; }

  toStr() { return CPod.prototype.toStr.apply(this, arguments); }

  hasJs() { return CPod.prototype.hasJs.apply(this, arguments); }

  isForeign() { return CPod.prototype.isForeign.apply(this, arguments); }

  equals() { return CPod.prototype.equals.apply(this, arguments); }

  bridge() { return CPod.prototype.bridge.apply(this, arguments); }

  hash() { return CPod.prototype.hash.apply(this, arguments); }

  #ns = null;

  ns() {
    return this.#ns;
  }

  #pod = null;

  pod() {
    return this.#pod;
  }

  #typeMap = null;

  // private field reflection only
  __typeMap(it) { if (it === undefined) return this.#typeMap; else this.#typeMap = it; }

  #loadedAllTypes = false;

  // private field reflection only
  __loadedAllTypes(it) { if (it === undefined) return this.#loadedAllTypes; else this.#loadedAllTypes = it; }

  #depends$Store = undefined;

  // private field reflection only
  __depends$Store(it) { if (it === undefined) return this.#depends$Store; else this.#depends$Store = it; }

  #file$Store = undefined;

  // private field reflection only
  __file$Store(it) { if (it === undefined) return this.#file$Store; else this.#file$Store = it; }

  static make(ns,pod) {
    const $self = new ReflectPod();
    ReflectPod.make$($self,ns,pod);
    return $self;
  }

  static make$($self,ns,pod) {
    ;
    $self.#ns = ns;
    $self.#pod = pod;
    return;
  }

  name() {
    return this.#pod.name();
  }

  version() {
    return this.#pod.version();
  }

  depends() {
    if (this.#depends$Store === undefined) {
      this.#depends$Store = this.depends$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#depends$Store, sys.Type.find("compiler::CDepend[]"));
  }

  file() {
    if (this.#file$Store === undefined) {
      this.#file$Store = this.file$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#file$Store, sys.File.type$);
  }

  meta() {
    return this.#pod.meta();
  }

  types() {
    const this$ = this;
    if (!this.#loadedAllTypes) {
      this.#loadedAllTypes = true;
      this.#pod.types().each((t) => {
        this$.resolveType(t.name(), true);
        return;
      });
    }
    ;
    return this.#typeMap.vals();
  }

  resolveType(typeName,checked) {
    let rt = this.#typeMap.get(typeName);
    if (rt != null) {
      return rt;
    }
    ;
    let t = this.#pod.type(typeName, checked);
    if (t == null) {
      return null;
    }
    ;
    (rt = ReflectType.make(this.#ns, sys.ObjUtil.coerce(t, sys.Type.type$)));
    this.#typeMap.set(typeName, sys.ObjUtil.coerce(rt, ReflectType.type$));
    this.#ns.typeCache().set(t.signature(), sys.ObjUtil.coerce(rt, CType.type$));
    return rt;
  }

  depends$Once() {
    return CDepend.makeList(this.#pod.depends());
  }

  file$Once() {
    return sys.ObjUtil.coerce(sys.Env.cur().findPodFile(this.name()), sys.File.type$);
  }

}

class ReflectType extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    this.#slotMap = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::CSlot"));
    this.#slotsLoaded = false;
    return;
  }

  typeof() { return ReflectType.type$; }

  isNum() { return CType.prototype.isNum.apply(this, arguments); }

  inferredAs() { return CType.prototype.inferredAs.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CType.prototype.isSynthetic.apply(this, arguments); }

  fitsAny() { return CType.prototype.fitsAny.apply(this, arguments); }

  isNative() { return CType.prototype.isNative.apply(this, arguments); }

  isConstFieldType() { return CType.prototype.isConstFieldType.apply(this, arguments); }

  isObj() { return CType.prototype.isObj.apply(this, arguments); }

  isBool() { return CType.prototype.isBool.apply(this, arguments); }

  isBuf() { return CType.prototype.isBuf.apply(this, arguments); }

  isInt() { return CType.prototype.isInt.apply(this, arguments); }

  isDecimal() { return CType.prototype.isDecimal.apply(this, arguments); }

  isFacet() { return CType.prototype.isFacet.apply(this, arguments); }

  isNothing() { return CType.prototype.isNothing.apply(this, arguments); }

  toStr() { return CType.prototype.toStr.apply(this, arguments); }

  method() { return CType.prototype.method.apply(this, arguments); }

  foreignInheritance() { return CType.prototype.foreignInheritance.apply(this, arguments); }

  raw() { return CType.prototype.raw.apply(this, arguments); }

  isSupported() { return CType.prototype.isSupported.apply(this, arguments); }

  isFunc() { return CType.prototype.isFunc.apply(this, arguments); }

  fits() { return CType.prototype.fits.apply(this, arguments); }

  isType() { return CType.prototype.isType.apply(this, arguments); }

  isMap() { return CType.prototype.isMap.apply(this, arguments); }

  isInternal() { return CType.prototype.isInternal.apply(this, arguments); }

  hasSlot() { return CType.prototype.hasSlot.apply(this, arguments); }

  field() { return CType.prototype.field.apply(this, arguments); }

  bridge() { return CType.prototype.bridge.apply(this, arguments); }

  fields() { return CType.prototype.fields.apply(this, arguments); }

  hash() { return CType.prototype.hash.apply(this, arguments); }

  toNonNullable() { return CType.prototype.toNonNullable.apply(this, arguments); }

  isThis() { return CType.prototype.isThis.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  deref() { return CType.prototype.deref.apply(this, arguments); }

  instanceCtors() { return CType.prototype.instanceCtors.apply(this, arguments); }

  isForeign() { return CType.prototype.isForeign.apply(this, arguments); }

  methods() { return CType.prototype.methods.apply(this, arguments); }

  isList() { return CType.prototype.isList.apply(this, arguments); }

  parameterizeThis() { return CType.prototype.parameterizeThis.apply(this, arguments); }

  isConst() { return CType.prototype.isConst.apply(this, arguments); }

  isPublic() { return CType.prototype.isPublic.apply(this, arguments); }

  isStr() { return CType.prototype.isStr.apply(this, arguments); }

  isFinal() { return CType.prototype.isFinal.apply(this, arguments); }

  isClass() { return CType.prototype.isClass.apply(this, arguments); }

  isValid() { return CType.prototype.isValid.apply(this, arguments); }

  isEnum() { return CType.prototype.isEnum.apply(this, arguments); }

  isFloat() { return CType.prototype.isFloat.apply(this, arguments); }

  isMixin() { return CType.prototype.isMixin.apply(this, arguments); }

  isAbstract() { return CType.prototype.isAbstract.apply(this, arguments); }

  isRange() { return CType.prototype.isRange.apply(this, arguments); }

  isVoid() { return CType.prototype.isVoid.apply(this, arguments); }

  ctors() { return CType.prototype.ctors.apply(this, arguments); }

  equals() { return CType.prototype.equals.apply(this, arguments); }

  #isVal = false;

  isVal() { return this.#isVal; }

  __isVal(it) { if (it === undefined) return this.#isVal; else this.#isVal = it; }

  #t = null;

  t() { return this.#t; }

  __t(it) { if (it === undefined) return this.#t; else this.#t = it; }

  #pod = null;

  pod() {
    return this.#pod;
  }

  #base = null;

  base() {
    return this.#base;
  }

  #mixins = null;

  mixins() {
    return this.#mixins;
  }

  #slotMap = null;

  // private field reflection only
  __slotMap(it) { if (it === undefined) return this.#slotMap; else this.#slotMap = it; }

  #slotsLoaded = false;

  // private field reflection only
  __slotsLoaded(it) { if (it === undefined) return this.#slotsLoaded; else this.#slotsLoaded = it; }

  #toNullable$Store = undefined;

  // private field reflection only
  __toNullable$Store(it) { if (it === undefined) return this.#toNullable$Store; else this.#toNullable$Store = it; }

  #toListOf$Store = undefined;

  // private field reflection only
  __toListOf$Store(it) { if (it === undefined) return this.#toListOf$Store; else this.#toListOf$Store = it; }

  #operators$Store = undefined;

  // private field reflection only
  __operators$Store(it) { if (it === undefined) return this.#operators$Store; else this.#operators$Store = it; }

  static make(ns,t) {
    const $self = new ReflectType();
    ReflectType.make$($self,ns,t);
    return $self;
  }

  static make$($self,ns,t) {
    ;
    $self.#pod = ns.importPod(sys.ObjUtil.coerce(t.pod(), sys.Pod.type$));
    $self.#t = t;
    $self.#base = ns.importType(t.base());
    $self.#mixins = ns.importTypes(t.mixins());
    $self.#isVal = t.isVal();
    return;
  }

  ns() {
    return this.#pod.ns();
  }

  doc() {
    return CDoc.fromStr(this.#t.doc());
  }

  name() {
    return this.#t.name();
  }

  qname() {
    return this.#t.qname();
  }

  signature() {
    return this.#t.signature();
  }

  flags() {
    return sys.ObjUtil.coerce(sys.ObjUtil.trap(this.#t,"flags", sys.List.make(sys.Obj.type$.toNullable(), [])), sys.Int.type$);
  }

  isNullable() {
    return false;
  }

  toNullable() {
    if (this.#toNullable$Store === undefined) {
      this.#toNullable$Store = this.toNullable$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#toNullable$Store, CType.type$);
  }

  isGeneric() {
    return this.#t.isGeneric();
  }

  isParameterized() {
    return !this.#t.params().isEmpty();
  }

  isGenericParameter() {
    return (this.#pod === this.ns().sysPod() && sys.ObjUtil.equals(sys.Str.size(this.name()), 1));
  }

  toListOf() {
    if (this.#toListOf$Store === undefined) {
      this.#toListOf$Store = this.toListOf$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#toListOf$Store, CType.type$);
  }

  facet(qname) {
    try {
      return ReflectFacet.map(this.ns(), this.#t.facet(sys.ObjUtil.coerce(sys.Type.find(qname), sys.Type.type$), false));
    }
    catch ($_u94) {
      $_u94 = sys.Err.make($_u94);
      if ($_u94 instanceof sys.Err) {
        let e = $_u94;
        ;
        e.trace();
      }
      else {
        throw $_u94;
      }
    }
    ;
    return null;
  }

  slots() {
    const this$ = this;
    if (!this.#slotsLoaded) {
      this.#slotsLoaded = true;
      if (!this.isGenericParameter()) {
        this.#t.slots().each((s) => {
          if (this$.#slotMap.get(s.name()) == null) {
            this$.#slotMap.set(s.name(), this$.ns().importSlot(s));
          }
          ;
          return;
        });
      }
      ;
    }
    ;
    return this.#slotMap;
  }

  operators() {
    if (this.#operators$Store === undefined) {
      this.#operators$Store = this.operators$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#operators$Store, COperators.type$);
  }

  slot(name) {
    let cs = this.#slotMap.get(name);
    if (cs == null) {
      let s = this.#t.slot(name, false);
      if (s != null) {
        this.#slotMap.set(name, sys.ObjUtil.coerce((cs = this.ns().importSlot(sys.ObjUtil.coerce(s, sys.Slot.type$))), CSlot.type$));
      }
      ;
    }
    ;
    return cs;
  }

  toNullable$Once() {
    return NullableType.make(this);
  }

  toListOf$Once() {
    return ListType.make(this);
  }

  operators$Once() {
    return COperators.make(this);
  }

}

class MarkerFacet extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return MarkerFacet.type$; }

  #qname = null;

  qname() { return this.#qname; }

  __qname(it) { if (it === undefined) return this.#qname; else this.#qname = it; }

  static make(qname) {
    const $self = new MarkerFacet();
    MarkerFacet.make$($self,qname);
    return $self;
  }

  static make$($self,qname) {
    $self.#qname = qname;
    return;
  }

  get(name) {
    return null;
  }

}

class ReflectNamespace extends CNamespace {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ReflectNamespace.type$; }

  #pods = null;

  // private field reflection only
  __pods(it) { if (it === undefined) return this.#pods; else this.#pods = it; }

  static make() {
    const $self = new ReflectNamespace();
    ReflectNamespace.make$($self);
    return $self;
  }

  static make$($self) {
    CNamespace.make$($self);
    $self.#pods = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::ReflectPod"));
    $self.init();
    return;
  }

  findPod(podName) {
    let pod = sys.Pod.find(podName, false);
    if (pod == null) {
      return null;
    }
    ;
    return ReflectPod.make(this, sys.ObjUtil.coerce(pod, sys.Pod.type$));
  }

  importPod(pod) {
    return sys.ObjUtil.coerce(this.resolvePod(pod.name(), null), ReflectPod.type$);
  }

  importType(t) {
    if (t == null) {
      return null;
    }
    ;
    return this.resolveType(t.signature());
  }

  importTypes(t) {
    const this$ = this;
    return sys.ObjUtil.coerce(t.map((x) => {
      return sys.ObjUtil.coerce(this$.importType(x), CType.type$);
    }, CType.type$), sys.Type.find("compiler::CType[]"));
  }

  importSlot(slot) {
    if (sys.ObjUtil.is(slot, sys.Method.type$)) {
      return this.importMethod(sys.ObjUtil.coerce(slot, sys.Method.type$));
    }
    else {
      return this.importField(sys.ObjUtil.coerce(slot, sys.Field.type$));
    }
    ;
  }

  importField(f) {
    return ReflectField.make(this, sys.ObjUtil.coerce(this.importType(f.parent()), CType.type$), f);
  }

  importMethod(m) {
    return ReflectMethod.make(this, sys.ObjUtil.coerce(this.importType(m.parent()), CType.type$), m);
  }

}

class CBridge extends CompilerSupport {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return CBridge.type$; }

  static make(c) {
    const $self = new CBridge();
    CBridge.make$($self,c);
    return $self;
  }

  static make$($self,c) {
    CompilerSupport.make$($self, c);
    return;
  }

  coerce(expr,expected,onErr) {
    return Coercer.make(this.compiler()).doCoerce(expr, expected, onErr);
  }

  cleanup() {
    return;
  }

}

class NullableType extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    return;
  }

  typeof() { return NullableType.type$; }

  isNum() { return CType.prototype.isNum.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CType.prototype.isSynthetic.apply(this, arguments); }

  fitsAny() { return CType.prototype.fitsAny.apply(this, arguments); }

  isNative() { return CType.prototype.isNative.apply(this, arguments); }

  isConstFieldType() { return CType.prototype.isConstFieldType.apply(this, arguments); }

  isObj() { return CType.prototype.isObj.apply(this, arguments); }

  isBool() { return CType.prototype.isBool.apply(this, arguments); }

  isBuf() { return CType.prototype.isBuf.apply(this, arguments); }

  isInt() { return CType.prototype.isInt.apply(this, arguments); }

  isDecimal() { return CType.prototype.isDecimal.apply(this, arguments); }

  isFacet() { return CType.prototype.isFacet.apply(this, arguments); }

  isNothing() { return CType.prototype.isNothing.apply(this, arguments); }

  toStr() { return CType.prototype.toStr.apply(this, arguments); }

  method() { return CType.prototype.method.apply(this, arguments); }

  foreignInheritance() { return CType.prototype.foreignInheritance.apply(this, arguments); }

  raw() { return CType.prototype.raw.apply(this, arguments); }

  isFunc() { return CType.prototype.isFunc.apply(this, arguments); }

  isType() { return CType.prototype.isType.apply(this, arguments); }

  isMap() { return CType.prototype.isMap.apply(this, arguments); }

  isInternal() { return CType.prototype.isInternal.apply(this, arguments); }

  hasSlot() { return CType.prototype.hasSlot.apply(this, arguments); }

  field() { return CType.prototype.field.apply(this, arguments); }

  bridge() { return CType.prototype.bridge.apply(this, arguments); }

  fields() { return CType.prototype.fields.apply(this, arguments); }

  hash() { return CType.prototype.hash.apply(this, arguments); }

  isThis() { return CType.prototype.isThis.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  deref() { return CType.prototype.deref.apply(this, arguments); }

  instanceCtors() { return CType.prototype.instanceCtors.apply(this, arguments); }

  methods() { return CType.prototype.methods.apply(this, arguments); }

  slot() { return CType.prototype.slot.apply(this, arguments); }

  isList() { return CType.prototype.isList.apply(this, arguments); }

  isConst() { return CType.prototype.isConst.apply(this, arguments); }

  isPublic() { return CType.prototype.isPublic.apply(this, arguments); }

  isStr() { return CType.prototype.isStr.apply(this, arguments); }

  isFinal() { return CType.prototype.isFinal.apply(this, arguments); }

  isClass() { return CType.prototype.isClass.apply(this, arguments); }

  isEnum() { return CType.prototype.isEnum.apply(this, arguments); }

  isFloat() { return CType.prototype.isFloat.apply(this, arguments); }

  isMixin() { return CType.prototype.isMixin.apply(this, arguments); }

  isAbstract() { return CType.prototype.isAbstract.apply(this, arguments); }

  isRange() { return CType.prototype.isRange.apply(this, arguments); }

  isVoid() { return CType.prototype.isVoid.apply(this, arguments); }

  ctors() { return CType.prototype.ctors.apply(this, arguments); }

  equals() { return CType.prototype.equals.apply(this, arguments); }

  #signature = null;

  signature(it) {
    if (it === undefined) {
      return this.#signature;
    }
    else {
      this.#signature = it;
      return;
    }
  }

  #root = null;

  root() {
    return this.#root;
  }

  #toListOf$Store = undefined;

  // private field reflection only
  __toListOf$Store(it) { if (it === undefined) return this.#toListOf$Store; else this.#toListOf$Store = it; }

  static make(root) {
    const $self = new NullableType();
    NullableType.make$($self,root);
    return $self;
  }

  static make$($self,root) {
    ;
    if (root.isNullable()) {
      throw sys.Err.make(sys.Str.plus(sys.Str.plus("Cannot wrap ", root), " as NullableType"));
    }
    ;
    $self.#root = root;
    $self.signature(sys.Str.plus(root.signature(), "?"));
    return;
  }

  ns() {
    return this.#root.ns();
  }

  doc() {
    return this.#root.doc();
  }

  pod() {
    return this.#root.pod();
  }

  name() {
    return this.#root.name();
  }

  qname() {
    return this.#root.qname();
  }

  flags() {
    return this.#root.flags();
  }

  isVal() {
    return this.#root.isVal();
  }

  facet(qname) {
    return this.#root.facet(qname);
  }

  isNullable() {
    return true;
  }

  toNullable() {
    return this;
  }

  toNonNullable() {
    return this.#root;
  }

  isGeneric() {
    return this.#root.isGeneric();
  }

  isParameterized() {
    return this.#root.isParameterized();
  }

  isGenericParameter() {
    return this.#root.isGenericParameter();
  }

  parameterizeThis(thisType) {
    return this.#root.parameterizeThis(thisType).toNullable();
  }

  isForeign() {
    return this.#root.isForeign();
  }

  isSupported() {
    return this.#root.isSupported();
  }

  inferredAs() {
    let x = this.#root.inferredAs();
    if (x === this.#root) {
      return this;
    }
    ;
    return x.toNullable();
  }

  toListOf() {
    if (this.#toListOf$Store === undefined) {
      this.#toListOf$Store = this.toListOf$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#toListOf$Store, CType.type$);
  }

  base() {
    return this.#root.base();
  }

  mixins() {
    return this.#root.mixins();
  }

  fits(t) {
    return this.#root.fits(t);
  }

  slots() {
    return this.#root.slots();
  }

  operators() {
    return this.#root.operators();
  }

  isValid() {
    return this.#root.isValid();
  }

  toListOf$Once() {
    return ListType.make(this);
  }

}

class MDoc extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return MDoc.type$; }

  #text = null;

  text() { return this.#text; }

  __text(it) { if (it === undefined) return this.#text; else this.#text = it; }

  static make(text) {
    const $self = new MDoc();
    MDoc.make$($self,text);
    return $self;
  }

  static make$($self,text) {
    $self.#text = text;
    return;
  }

}

class GenericType extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    return;
  }

  typeof() { return GenericType.type$; }

  isNum() { return CType.prototype.isNum.apply(this, arguments); }

  inferredAs() { return CType.prototype.inferredAs.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CType.prototype.isSynthetic.apply(this, arguments); }

  fitsAny() { return CType.prototype.fitsAny.apply(this, arguments); }

  isNative() { return CType.prototype.isNative.apply(this, arguments); }

  isConstFieldType() { return CType.prototype.isConstFieldType.apply(this, arguments); }

  isObj() { return CType.prototype.isObj.apply(this, arguments); }

  isBool() { return CType.prototype.isBool.apply(this, arguments); }

  isBuf() { return CType.prototype.isBuf.apply(this, arguments); }

  isInt() { return CType.prototype.isInt.apply(this, arguments); }

  isDecimal() { return CType.prototype.isDecimal.apply(this, arguments); }

  isFacet() { return CType.prototype.isFacet.apply(this, arguments); }

  isNothing() { return CType.prototype.isNothing.apply(this, arguments); }

  toStr() { return CType.prototype.toStr.apply(this, arguments); }

  method() { return CType.prototype.method.apply(this, arguments); }

  foreignInheritance() { return CType.prototype.foreignInheritance.apply(this, arguments); }

  raw() { return CType.prototype.raw.apply(this, arguments); }

  isSupported() { return CType.prototype.isSupported.apply(this, arguments); }

  isFunc() { return CType.prototype.isFunc.apply(this, arguments); }

  fits() { return CType.prototype.fits.apply(this, arguments); }

  isType() { return CType.prototype.isType.apply(this, arguments); }

  isMap() { return CType.prototype.isMap.apply(this, arguments); }

  isInternal() { return CType.prototype.isInternal.apply(this, arguments); }

  hasSlot() { return CType.prototype.hasSlot.apply(this, arguments); }

  field() { return CType.prototype.field.apply(this, arguments); }

  bridge() { return CType.prototype.bridge.apply(this, arguments); }

  fields() { return CType.prototype.fields.apply(this, arguments); }

  hash() { return CType.prototype.hash.apply(this, arguments); }

  toNonNullable() { return CType.prototype.toNonNullable.apply(this, arguments); }

  isThis() { return CType.prototype.isThis.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  deref() { return CType.prototype.deref.apply(this, arguments); }

  instanceCtors() { return CType.prototype.instanceCtors.apply(this, arguments); }

  isForeign() { return CType.prototype.isForeign.apply(this, arguments); }

  methods() { return CType.prototype.methods.apply(this, arguments); }

  slot() { return CType.prototype.slot.apply(this, arguments); }

  isList() { return CType.prototype.isList.apply(this, arguments); }

  parameterizeThis() { return CType.prototype.parameterizeThis.apply(this, arguments); }

  isConst() { return CType.prototype.isConst.apply(this, arguments); }

  isPublic() { return CType.prototype.isPublic.apply(this, arguments); }

  isStr() { return CType.prototype.isStr.apply(this, arguments); }

  isFinal() { return CType.prototype.isFinal.apply(this, arguments); }

  isClass() { return CType.prototype.isClass.apply(this, arguments); }

  isValid() { return CType.prototype.isValid.apply(this, arguments); }

  isEnum() { return CType.prototype.isEnum.apply(this, arguments); }

  isFloat() { return CType.prototype.isFloat.apply(this, arguments); }

  isMixin() { return CType.prototype.isMixin.apply(this, arguments); }

  isAbstract() { return CType.prototype.isAbstract.apply(this, arguments); }

  isRange() { return CType.prototype.isRange.apply(this, arguments); }

  isVoid() { return CType.prototype.isVoid.apply(this, arguments); }

  ctors() { return CType.prototype.ctors.apply(this, arguments); }

  equals() { return CType.prototype.equals.apply(this, arguments); }

  #base = null;

  base() {
    return this.#base;
  }

  #toNullable$Store = undefined;

  // private field reflection only
  __toNullable$Store(it) { if (it === undefined) return this.#toNullable$Store; else this.#toNullable$Store = it; }

  #toListOf$Store = undefined;

  // private field reflection only
  __toListOf$Store(it) { if (it === undefined) return this.#toListOf$Store; else this.#toListOf$Store = it; }

  #slots$Store = undefined;

  // private field reflection only
  __slots$Store(it) { if (it === undefined) return this.#slots$Store; else this.#slots$Store = it; }

  #operators$Store = undefined;

  // private field reflection only
  __operators$Store(it) { if (it === undefined) return this.#operators$Store; else this.#operators$Store = it; }

  static make(base) {
    const $self = new GenericType();
    GenericType.make$($self,base);
    return $self;
  }

  static make$($self,base) {
    ;
    $self.#base = base;
    return;
  }

  ns() {
    return this.#base.ns();
  }

  doc() {
    return this.#base.doc();
  }

  pod() {
    return sys.ObjUtil.coerce(this.ns().sysPod(), CPod.type$);
  }

  name() {
    return this.#base.name();
  }

  qname() {
    return this.#base.qname();
  }

  isVal() {
    return false;
  }

  isNullable() {
    return false;
  }

  toNullable() {
    if (this.#toNullable$Store === undefined) {
      this.#toNullable$Store = this.toNullable$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#toNullable$Store, CType.type$);
  }

  isGeneric() {
    return false;
  }

  isParameterized() {
    return true;
  }

  toListOf() {
    if (this.#toListOf$Store === undefined) {
      this.#toListOf$Store = this.toListOf$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#toListOf$Store, CType.type$);
  }

  mixins() {
    return sys.List.make(CType.type$);
  }

  facet(qname) {
    return this.#base.facet(qname);
  }

  slots() {
    if (this.#slots$Store === undefined) {
      this.#slots$Store = this.slots$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#slots$Store, sys.Type.find("[sys::Str:compiler::CSlot]"));
  }

  operators() {
    if (this.#operators$Store === undefined) {
      this.#operators$Store = this.operators$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#operators$Store, COperators.type$);
  }

  parameterizeSlots() {
    const this$ = this;
    return sys.ObjUtil.coerce(this.#base.slots().map((slot) => {
      return this$.parameterizeSlot(slot);
    }, CSlot.type$), sys.Type.find("[sys::Str:compiler::CSlot]"));
  }

  parameterizeSlot(slot) {
    if (sys.ObjUtil.is(slot, CMethod.type$)) {
      let m = sys.ObjUtil.coerce(slot, CMethod.type$);
      if (!m.isGeneric()) {
        return slot;
      }
      ;
      return ParameterizedMethod.make(this, m);
    }
    else {
      let f = sys.ObjUtil.coerce(slot, CField.type$);
      if (!f.type().isGenericParameter()) {
        return slot;
      }
      ;
      return ParameterizedField.make(this, f);
    }
    ;
  }

  parameterize(t) {
    if (!t.isGenericParameter()) {
      return t;
    }
    ;
    let nullable = t.isNullable();
    let nn = t.toNonNullable();
    if (sys.ObjUtil.is(nn, ListType.type$)) {
      (t = this.parameterizeListType(sys.ObjUtil.coerce(nn, ListType.type$)));
    }
    else {
      if (sys.ObjUtil.is(nn, MapType.type$)) {
        (t = this.parameterizeMapType(sys.ObjUtil.coerce(nn, MapType.type$)));
      }
      else {
        if (sys.ObjUtil.is(nn, FuncType.type$)) {
          (t = this.parameterizeFuncType(sys.ObjUtil.coerce(nn, FuncType.type$)));
        }
        else {
          if (sys.ObjUtil.compareNE(sys.Str.size(nn.name()), 1)) {
            throw sys.Err.make(sys.Str.plus("Cannot parameterize ", t.qname()));
          }
          ;
          (t = this.doParameterize(sys.Str.get(nn.name(), 0)));
        }
        ;
      }
      ;
    }
    ;
    return ((this$) => { if (nullable) return t.toNullable(); return t; })(this);
  }

  parameterizeListType(t) {
    return this.parameterize(t.v()).toListOf();
  }

  parameterizeMapType(t) {
    return MapType.make(this.parameterize(t.k()), this.parameterize(t.v()));
  }

  parameterizeFuncType(t) {
    const this$ = this;
    let params = sys.ObjUtil.coerce(t.params().map((p) => {
      return this$.parameterize(p);
    }, CType.type$), sys.Type.find("compiler::CType[]"));
    let ret = this.parameterize(t.returns());
    return FuncType.make(params, t.names(), ret);
  }

  toNullable$Once() {
    return NullableType.make(this);
  }

  toListOf$Once() {
    return ListType.make(this);
  }

  slots$Once() {
    return this.parameterizeSlots();
  }

  operators$Once() {
    return COperators.make(this);
  }

}

class ListType extends GenericType {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ListType.type$; }

  #v = null;

  v() {
    return this.#v;
  }

  #signature = null;

  signature() { return this.#signature; }

  __signature(it) { if (it === undefined) return this.#signature; else this.#signature = it; }

  static make(v) {
    const $self = new ListType();
    ListType.make$($self,v);
    return $self;
  }

  static make$($self,v) {
    GenericType.make$($self, sys.ObjUtil.coerce(v.ns().listType(), CType.type$));
    $self.#v = v;
    $self.#signature = sys.Str.plus(sys.Str.plus("", v.signature()), "[]");
    return;
  }

  isGenericParameter() {
    return this.#v.isGenericParameter();
  }

  flags() {
    return ((this$) => { if (this$.#v.isPublic()) return FConst.Public(); return FConst.Internal(); })(this);
  }

  fits(t) {
    (t = t.toNonNullable());
    if (sys.ObjUtil.equals(this, t)) {
      return true;
    }
    ;
    if (sys.ObjUtil.equals(t.signature(), "sys::List")) {
      return true;
    }
    ;
    if (t.isObj()) {
      return true;
    }
    ;
    if ((sys.ObjUtil.equals(sys.Str.size(t.name()), 1) && sys.ObjUtil.equals(t.pod().name(), "sys"))) {
      return true;
    }
    ;
    let that = sys.ObjUtil.as(t.deref(), ListType.type$);
    if (that == null) {
      return false;
    }
    ;
    return this.#v.fits(that.#v);
  }

  doParameterize(ch) {
    let $_u97 = ch;
    if (sys.ObjUtil.equals($_u97, 86)) {
      return this.#v;
    }
    else if (sys.ObjUtil.equals($_u97, 76)) {
      return this;
    }
    else {
      throw sys.Err.make(sys.Int.toChar(ch));
    }
    ;
  }

  isValid() {
    return this.#v.isValid();
  }

}

class MapType extends GenericType {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return MapType.type$; }

  #k = null;

  k() {
    return this.#k;
  }

  #v = null;

  v() {
    return this.#v;
  }

  #signature = null;

  signature() { return this.#signature; }

  __signature(it) { if (it === undefined) return this.#signature; else this.#signature = it; }

  static make(k,v) {
    const $self = new MapType();
    MapType.make$($self,k,v);
    return $self;
  }

  static make$($self,k,v) {
    GenericType.make$($self, sys.ObjUtil.coerce(k.ns().mapType(), CType.type$));
    $self.#k = k;
    $self.#v = v;
    $self.#signature = sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("[", k.signature()), ":"), v.signature()), "]");
    return;
  }

  flags() {
    return ((this$) => { if ((this$.#k.isPublic() && this$.#v.isPublic())) return FConst.Public(); return FConst.Internal(); })(this);
  }

  isGenericParameter() {
    return (this.#k.isGenericParameter() || this.#v.isGenericParameter());
  }

  fits(t) {
    (t = t.toNonNullable());
    if (sys.ObjUtil.equals(this, t)) {
      return true;
    }
    ;
    if (sys.ObjUtil.equals(t.signature(), "sys::Map")) {
      return true;
    }
    ;
    if (t.isObj()) {
      return true;
    }
    ;
    if ((sys.ObjUtil.equals(sys.Str.size(t.name()), 1) && sys.ObjUtil.equals(t.pod().name(), "sys"))) {
      return true;
    }
    ;
    let that = sys.ObjUtil.as(t.deref(), MapType.type$);
    if (that == null) {
      return false;
    }
    ;
    return (this.#k.fits(that.#k) && this.#v.fits(that.#v));
  }

  doParameterize(ch) {
    let $_u99 = ch;
    if (sys.ObjUtil.equals($_u99, 75)) {
      return this.#k;
    }
    else if (sys.ObjUtil.equals($_u99, 86)) {
      return this.#v;
    }
    else if (sys.ObjUtil.equals($_u99, 77)) {
      return this;
    }
    else {
      throw sys.Err.make(sys.Int.toChar(ch));
    }
    ;
  }

  isValid() {
    return (this.#k.isValid() && this.#v.isValid());
  }

}

class FuncType extends GenericType {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return FuncType.type$; }

  #params = null;

  params() {
    return this.#params;
  }

  #names = null;

  names() {
    return this.#names;
  }

  #returns = null;

  returns() {
    return this.#returns;
  }

  #unnamed = false;

  unnamed(it) {
    if (it === undefined) {
      return this.#unnamed;
    }
    else {
      this.#unnamed = it;
      return;
    }
  }

  #signature = null;

  signature() { return this.#signature; }

  __signature(it) { if (it === undefined) return this.#signature; else this.#signature = it; }

  #isGenericParameter = false;

  isGenericParameter() { return this.#isGenericParameter; }

  __isGenericParameter(it) { if (it === undefined) return this.#isGenericParameter; else this.#isGenericParameter = it; }

  #inferredSignature = false;

  inferredSignature(it) {
    if (it === undefined) {
      return this.#inferredSignature;
    }
    else {
      this.#inferredSignature = it;
      return;
    }
  }

  static make(params,names,returns) {
    const $self = new FuncType();
    FuncType.make$($self,params,names,returns);
    return $self;
  }

  static make$($self,params,names,returns) {
    const this$ = $self;
    GenericType.make$($self, sys.ObjUtil.coerce(returns.ns().funcType(), CType.type$));
    $self.#params = params;
    $self.#names = names;
    $self.#returns = returns;
    $self.#isGenericParameter = returns.isGenericParameter();
    let s = sys.StrBuf.make().add("|");
    params.each((p,i) => {
      this$.#isGenericParameter = sys.Bool.or(this$.#isGenericParameter, p.isGenericParameter());
      if (sys.ObjUtil.compareGT(i, 0)) {
        s.add(",");
      }
      ;
      s.add(p.signature());
      return;
    });
    s.add("->").add(returns.signature()).add("|");
    $self.#signature = s.toStr();
    return;
  }

  static makeItBlock(itType) {
    const $self = new FuncType();
    FuncType.makeItBlock$($self,itType);
    return $self;
  }

  static makeItBlock$($self,itType) {
    FuncType.make$($self, sys.List.make(CType.type$, [itType]), sys.List.make(sys.Str.type$, ["it"]), sys.ObjUtil.coerce(itType.ns().voidType(), CType.type$));
    if (itType.isThis()) {
      throw sys.Err.make(sys.Str.plus("Invalid it-block func signature: ", $self));
    }
    ;
    return;
  }

  flags() {
    const this$ = this;
    let allPublic = (this.#returns.isPublic() && this.#params.all((p) => {
      return p.isPublic();
    }));
    return ((this$) => { if (allPublic) return FConst.Public(); return FConst.Internal(); })(this);
  }

  fits(t) {
    (t = t.toNonNullable());
    if (sys.ObjUtil.equals(this, t)) {
      return true;
    }
    ;
    if (sys.ObjUtil.equals(t.signature(), "sys::Func")) {
      return true;
    }
    ;
    if (t.isObj()) {
      return true;
    }
    ;
    if ((sys.ObjUtil.equals(sys.Str.size(t.name()), 1) && sys.ObjUtil.equals(t.pod().name(), "sys"))) {
      return true;
    }
    ;
    let that = sys.ObjUtil.as(t.deref(), FuncType.type$);
    if (that == null) {
      return false;
    }
    ;
    if ((!that.#returns.isVoid() && !this.#returns.fits(that.#returns))) {
      return false;
    }
    ;
    if (sys.ObjUtil.compareGT(this.#params.size(), that.#params.size())) {
      return false;
    }
    ;
    for (let i = 0; sys.ObjUtil.compareLT(i, this.#params.size()); i = sys.Int.increment(i)) {
      if (!that.#params.get(i).fits(this.#params.get(i))) {
        return false;
      }
      ;
    }
    ;
    return true;
  }

  arity() {
    return this.#params.size();
  }

  toArity(num) {
    if (sys.ObjUtil.equals(num, this.#params.size())) {
      return this;
    }
    ;
    if (sys.ObjUtil.compareGT(num, this.#params.size())) {
      throw sys.Err.make(sys.Str.plus("Cannot increase arity ", this));
    }
    ;
    return FuncType.make(this.#params.getRange(sys.Range.make(0, num, true)), this.#names.getRange(sys.Range.make(0, num, true)), this.#returns);
  }

  mostSpecific(b) {
    const this$ = this;
    let a = this;
    if (sys.ObjUtil.compareNE(a.arity(), b.arity())) {
      throw sys.Err.make(sys.Str.plus(sys.Str.plus(sys.Str.plus("Different arities: ", a), " / "), b));
    }
    ;
    let params = a.#params.map((p,i) => {
      return FuncType.toMostSpecific(p, b.#params.get(i));
    }, sys.Obj.type$.toNullable());
    let returns = FuncType.toMostSpecific(a.#returns, b.#returns);
    return FuncType.make(sys.ObjUtil.coerce(params, sys.Type.find("compiler::CType[]")), b.#names, returns);
  }

  static toMostSpecific(a,b) {
    if (b.isGenericParameter()) {
      return a;
    }
    ;
    if ((a.isObj() || a.isVoid() || a.isGenericParameter())) {
      return b;
    }
    ;
    return a;
  }

  toParamDefs(loc) {
    let p = sys.List.make(ParamDef.type$);
    p.capacity(this.#params.size());
    for (let i = 0; sys.ObjUtil.compareLT(i, this.#params.size()); i = sys.Int.increment(i)) {
      p.add(ParamDef.make(loc, this.#params.get(i), this.#names.get(i)));
    }
    ;
    return p;
  }

  doParameterize(ch) {
    if ((sys.ObjUtil.compareLE(65, ch) && sys.ObjUtil.compareLE(ch, 72))) {
      let index = sys.Int.minus(ch, 65);
      if (sys.ObjUtil.compareLT(index, this.#params.size())) {
        return this.#params.get(index);
      }
      ;
      return sys.ObjUtil.coerce(this.ns().objType(), CType.type$);
    }
    ;
    let $_u101 = ch;
    if (sys.ObjUtil.equals($_u101, 82)) {
      return this.#returns;
    }
    else {
      throw sys.Err.make(sys.Int.toChar(ch));
    }
    ;
  }

  usesThis() {
    const this$ = this;
    return (this.#returns.isThis() || this.#params.any((p) => {
      return p.isThis();
    }));
  }

  parameterizeThis(thisType) {
    const this$ = this;
    if (!this.usesThis()) {
      return this;
    }
    ;
    let f = (t) => {
      return ((this$) => { if (t.isThis()) return thisType; return t; })(this$);
    };
    return FuncType.make(sys.ObjUtil.coerce(this.#params.map(f), sys.Type.find("compiler::CType[]")), this.#names, sys.Func.call(f, this.#returns));
  }

  isValid() {
    const this$ = this;
    return ((this.#returns.isVoid() || this.#returns.isValid()) && this.#params.all((p) => {
      return p.isValid();
    }));
  }

  ret() {
    return this.#returns;
  }

}

class GenericParameterType extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
    return;
  }

  typeof() { return GenericParameterType.type$; }

  isNum() { return CType.prototype.isNum.apply(this, arguments); }

  inferredAs() { return CType.prototype.inferredAs.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CType.prototype.isSynthetic.apply(this, arguments); }

  fitsAny() { return CType.prototype.fitsAny.apply(this, arguments); }

  isNative() { return CType.prototype.isNative.apply(this, arguments); }

  isConstFieldType() { return CType.prototype.isConstFieldType.apply(this, arguments); }

  isObj() { return CType.prototype.isObj.apply(this, arguments); }

  isBool() { return CType.prototype.isBool.apply(this, arguments); }

  isBuf() { return CType.prototype.isBuf.apply(this, arguments); }

  isInt() { return CType.prototype.isInt.apply(this, arguments); }

  isDecimal() { return CType.prototype.isDecimal.apply(this, arguments); }

  isFacet() { return CType.prototype.isFacet.apply(this, arguments); }

  isNothing() { return CType.prototype.isNothing.apply(this, arguments); }

  toStr() { return CType.prototype.toStr.apply(this, arguments); }

  method() { return CType.prototype.method.apply(this, arguments); }

  foreignInheritance() { return CType.prototype.foreignInheritance.apply(this, arguments); }

  raw() { return CType.prototype.raw.apply(this, arguments); }

  isSupported() { return CType.prototype.isSupported.apply(this, arguments); }

  isFunc() { return CType.prototype.isFunc.apply(this, arguments); }

  fits() { return CType.prototype.fits.apply(this, arguments); }

  isType() { return CType.prototype.isType.apply(this, arguments); }

  isMap() { return CType.prototype.isMap.apply(this, arguments); }

  isInternal() { return CType.prototype.isInternal.apply(this, arguments); }

  hasSlot() { return CType.prototype.hasSlot.apply(this, arguments); }

  field() { return CType.prototype.field.apply(this, arguments); }

  bridge() { return CType.prototype.bridge.apply(this, arguments); }

  fields() { return CType.prototype.fields.apply(this, arguments); }

  hash() { return CType.prototype.hash.apply(this, arguments); }

  toNonNullable() { return CType.prototype.toNonNullable.apply(this, arguments); }

  isThis() { return CType.prototype.isThis.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  deref() { return CType.prototype.deref.apply(this, arguments); }

  instanceCtors() { return CType.prototype.instanceCtors.apply(this, arguments); }

  isForeign() { return CType.prototype.isForeign.apply(this, arguments); }

  methods() { return CType.prototype.methods.apply(this, arguments); }

  slot() { return CType.prototype.slot.apply(this, arguments); }

  isList() { return CType.prototype.isList.apply(this, arguments); }

  parameterizeThis() { return CType.prototype.parameterizeThis.apply(this, arguments); }

  isConst() { return CType.prototype.isConst.apply(this, arguments); }

  isPublic() { return CType.prototype.isPublic.apply(this, arguments); }

  isStr() { return CType.prototype.isStr.apply(this, arguments); }

  isFinal() { return CType.prototype.isFinal.apply(this, arguments); }

  isClass() { return CType.prototype.isClass.apply(this, arguments); }

  isValid() { return CType.prototype.isValid.apply(this, arguments); }

  isEnum() { return CType.prototype.isEnum.apply(this, arguments); }

  isFloat() { return CType.prototype.isFloat.apply(this, arguments); }

  isMixin() { return CType.prototype.isMixin.apply(this, arguments); }

  isAbstract() { return CType.prototype.isAbstract.apply(this, arguments); }

  isRange() { return CType.prototype.isRange.apply(this, arguments); }

  isVoid() { return CType.prototype.isVoid.apply(this, arguments); }

  ctors() { return CType.prototype.ctors.apply(this, arguments); }

  equals() { return CType.prototype.equals.apply(this, arguments); }

  #ns = null;

  ns(it) {
    if (it === undefined) {
      return this.#ns;
    }
    else {
      this.#ns = it;
      return;
    }
  }

  #name = null;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #qname = null;

  qname(it) {
    if (it === undefined) {
      return this.#qname;
    }
    else {
      this.#qname = it;
      return;
    }
  }

  #toNullable$Store = undefined;

  // private field reflection only
  __toNullable$Store(it) { if (it === undefined) return this.#toNullable$Store; else this.#toNullable$Store = it; }

  #toListOf$Store = undefined;

  // private field reflection only
  __toListOf$Store(it) { if (it === undefined) return this.#toListOf$Store; else this.#toListOf$Store = it; }

  static make(ns,name) {
    const $self = new GenericParameterType();
    GenericParameterType.make$($self,ns,name);
    return $self;
  }

  static make$($self,ns,name) {
    ;
    $self.ns(ns);
    $self.name(name);
    $self.qname(sys.Str.plus("sys::", name));
    return;
  }

  pod() {
    return sys.ObjUtil.coerce(this.ns().sysPod(), CPod.type$);
  }

  doc() {
    return null;
  }

  signature() {
    return this.qname();
  }

  flags() {
    return FConst.Public();
  }

  isVal() {
    return false;
  }

  isNullable() {
    return false;
  }

  toNullable() {
    if (this.#toNullable$Store === undefined) {
      this.#toNullable$Store = this.toNullable$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#toNullable$Store, CType.type$);
  }

  isGeneric() {
    return false;
  }

  isParameterized() {
    return false;
  }

  isGenericParameter() {
    return true;
  }

  toListOf() {
    if (this.#toListOf$Store === undefined) {
      this.#toListOf$Store = this.toListOf$Once();
    }
    ;
    return sys.ObjUtil.coerce(this.#toListOf$Store, CType.type$);
  }

  base() {
    return this.ns().objType();
  }

  mixins() {
    return sys.List.make(CType.type$);
  }

  facet(qname) {
    return null;
  }

  slots() {
    throw sys.UnsupportedErr.make();
  }

  operators() {
    return this.ns().objType().operators();
  }

  toNullable$Once() {
    return NullableType.make(this);
  }

  toListOf$Once() {
    return ListType.make(this);
  }

}

class ParameterizedField extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ParameterizedField.type$; }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  fieldType() { return CField.prototype.fieldType.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  ns() { return CSlot.prototype.ns.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  usesBridge() { return CField.prototype.usesBridge.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isGeneric() { return CField.prototype.isGeneric.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isCovariant() { return CField.prototype.isCovariant.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  #type = null;

  type(it) {
    if (it === undefined) {
      return this.#type;
    }
    else {
      this.#type = it;
      return;
    }
  }

  #getter = null;

  getter(it) {
    if (it === undefined) {
      return this.#getter;
    }
    else {
      this.#getter = it;
      return;
    }
  }

  #setter = null;

  setter(it) {
    if (it === undefined) {
      return this.#setter;
    }
    else {
      this.#setter = it;
      return;
    }
  }

  #parent = null;

  parent() {
    return this.#parent;
  }

  #generic = null;

  generic() {
    return this.#generic;
  }

  static make(parent,generic) {
    const $self = new ParameterizedField();
    ParameterizedField.make$($self,parent,generic);
    return $self;
  }

  static make$($self,parent,generic) {
    $self.#parent = parent;
    $self.#generic = generic;
    $self.type(parent.parameterize(generic.type()));
    $self.getter(ParameterizedMethod.make(parent, sys.ObjUtil.coerce(generic.getter(), CMethod.type$)));
    $self.setter(ParameterizedMethod.make(parent, sys.ObjUtil.coerce(generic.setter(), CMethod.type$)));
    return;
  }

  doc() {
    return this.#generic.doc();
  }

  name() {
    return this.#generic.name();
  }

  qname() {
    return this.#generic.qname();
  }

  signature() {
    return this.#generic.signature();
  }

  flags() {
    return this.#generic.flags();
  }

  facet(qname) {
    return this.#generic.facet(qname);
  }

  inheritedReturns() {
    return this.type();
  }

  isParameterized() {
    return true;
  }

}

class ParameterizedMethod extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ParameterizedMethod.type$; }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  hasSameParams() { return CMethod.prototype.hasSameParams.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  inheritedReturnType() { return CMethod.prototype.inheritedReturnType.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  nameAndParamTypesToStr() { return CMethod.prototype.nameAndParamTypesToStr.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  returnType() { return CMethod.prototype.returnType.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  ns() { return CSlot.prototype.ns.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  usesBridge() { return CMethod.prototype.usesBridge.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isGeneric() { return CMethod.prototype.isGeneric.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isCovariant() { return CMethod.prototype.isCovariant.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  #parent = null;

  parent() {
    return this.#parent;
  }

  #signature = null;

  signature() {
    return this.#signature;
  }

  #generic = null;

  generic() {
    return this.#generic;
  }

  #returns = null;

  returns() {
    return this.#returns;
  }

  #params = null;

  params() {
    return this.#params;
  }

  static make(parent,generic) {
    const $self = new ParameterizedMethod();
    ParameterizedMethod.make$($self,parent,generic);
    return $self;
  }

  static make$($self,parent,generic) {
    const this$ = $self;
    $self.#parent = parent;
    $self.#generic = generic;
    $self.#returns = parent.parameterize(generic.returns());
    $self.#params = sys.ObjUtil.coerce(generic.params().map((p) => {
      if (!p.type().isGenericParameter()) {
        return p;
      }
      else {
        return ParameterizedMethodParam.make(parent, p);
      }
      ;
    }, CParam.type$), sys.Type.find("compiler::CParam[]"));
    $self.#signature = sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", $self.#returns), " "), $self.name()), "("), $self.#params.join(", ")), ")");
    return;
  }

  doc() {
    return this.#generic.doc();
  }

  name() {
    return this.#generic.name();
  }

  qname() {
    return this.#generic.qname();
  }

  flags() {
    return this.#generic.flags();
  }

  facet(qname) {
    return this.#generic.facet(qname);
  }

  isParameterized() {
    return true;
  }

  inheritedReturns() {
    return this.#generic.inheritedReturns();
  }

}

class ParameterizedMethodParam extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ParameterizedMethodParam.type$; }

  paramType() { return CParam.prototype.paramType.apply(this, arguments); }

  #type = null;

  type() {
    return this.#type;
  }

  #generic = null;

  generic() {
    return this.#generic;
  }

  static make(parent,generic) {
    const $self = new ParameterizedMethodParam();
    ParameterizedMethodParam.make$($self,parent,generic);
    return $self;
  }

  static make$($self,parent,generic) {
    $self.#generic = generic;
    $self.#type = parent.parameterize(generic.type());
    return;
  }

  name() {
    return this.#generic.name();
  }

  hasDefault() {
    return this.#generic.hasDefault();
  }

  toStr() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#type), " "), this.name());
  }

}

class MockSlot extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return MockSlot.type$; }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  ns() { return CSlot.prototype.ns.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  #parent = null;

  parent(it) {
    if (it === undefined) {
      return this.#parent;
    }
    else {
      this.#parent = it;
      return;
    }
  }

  #name = null;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #flags = 0;

  flags(it) {
    if (it === undefined) {
      return this.#flags;
    }
    else {
      this.#flags = it;
      return;
    }
  }

  static make(parent,name,flags) {
    const $self = new MockSlot();
    MockSlot.make$($self,parent,name,flags);
    return $self;
  }

  static make$($self,parent,name,flags) {
    $self.parent(parent);
    $self.name(name);
    $self.flags(flags);
    return;
  }

  qname() {
    return sys.Str.plus(sys.Str.plus(this.parent().qname(), "."), this.name());
  }

  signature() {
    return this.qname();
  }

  doc() {
    return null;
  }

  facet(qname) {
    return null;
  }

}

class MockField extends MockSlot {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return MockField.type$; }

  isParameterized() { return CField.prototype.isParameterized.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  fieldType() { return CField.prototype.fieldType.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  ns() { return CSlot.prototype.ns.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  usesBridge() { return CField.prototype.usesBridge.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isGeneric() { return CField.prototype.isGeneric.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isCovariant() { return CField.prototype.isCovariant.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  #type = null;

  type(it) {
    if (it === undefined) {
      return this.#type;
    }
    else {
      this.#type = it;
      return;
    }
  }

  static make(parent,name,flags,of$) {
    const $self = new MockField();
    MockField.make$($self,parent,name,flags,of$);
    return $self;
  }

  static make$($self,parent,name,flags,of$) {
    MockSlot.make$($self, parent, name, flags);
    $self.type(of$);
    return;
  }

  getter() {
    return null;
  }

  setter() {
    return null;
  }

  inheritedReturns() {
    return this.type();
  }

}

class MockMethod extends MockSlot {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return MockMethod.type$; }

  isParameterized() { return CMethod.prototype.isParameterized.apply(this, arguments); }

  isNoDoc() { return CNode.prototype.isNoDoc.apply(this, arguments); }

  isSynthetic() { return CSlot.prototype.isSynthetic.apply(this, arguments); }

  isInstanceCtor() { return CSlot.prototype.isInstanceCtor.apply(this, arguments); }

  isNative() { return CSlot.prototype.isNative.apply(this, arguments); }

  hasSameParams() { return CMethod.prototype.hasSameParams.apply(this, arguments); }

  usesForeign() { return CSlot.prototype.usesForeign.apply(this, arguments); }

  inheritedReturnType() { return CMethod.prototype.inheritedReturnType.apply(this, arguments); }

  toStr() { return CSlot.prototype.toStr.apply(this, arguments); }

  isInternal() { return CSlot.prototype.isInternal.apply(this, arguments); }

  nameAndParamTypesToStr() { return CMethod.prototype.nameAndParamTypesToStr.apply(this, arguments); }

  isVirtual() { return CSlot.prototype.isVirtual.apply(this, arguments); }

  bridge() { return CSlot.prototype.bridge.apply(this, arguments); }

  isStaticCtor() { return CSlot.prototype.isStaticCtor.apply(this, arguments); }

  returnType() { return CMethod.prototype.returnType.apply(this, arguments); }

  hasFacet() { return CNode.prototype.hasFacet.apply(this, arguments); }

  isStatic() { return CSlot.prototype.isStatic.apply(this, arguments); }

  ns() { return CSlot.prototype.ns.apply(this, arguments); }

  isForeign() { return CSlot.prototype.isForeign.apply(this, arguments); }

  isPrivate() { return CSlot.prototype.isPrivate.apply(this, arguments); }

  isStorage() { return CSlot.prototype.isStorage.apply(this, arguments); }

  isProtected() { return CSlot.prototype.isProtected.apply(this, arguments); }

  isAccessor() { return CSlot.prototype.isAccessor.apply(this, arguments); }

  isConst() { return CSlot.prototype.isConst.apply(this, arguments); }

  isVisibleTo() { return CSlot.prototype.isVisibleTo.apply(this, arguments); }

  isOverride() { return CSlot.prototype.isOverride.apply(this, arguments); }

  isPublic() { return CSlot.prototype.isPublic.apply(this, arguments); }

  usesBridge() { return CMethod.prototype.usesBridge.apply(this, arguments); }

  isGetter() { return CSlot.prototype.isGetter.apply(this, arguments); }

  isGeneric() { return CMethod.prototype.isGeneric.apply(this, arguments); }

  isEnum() { return CSlot.prototype.isEnum.apply(this, arguments); }

  isAbstract() { return CSlot.prototype.isAbstract.apply(this, arguments); }

  isOnce() { return CSlot.prototype.isOnce.apply(this, arguments); }

  generic() { return CMethod.prototype.generic.apply(this, arguments); }

  isSetter() { return CSlot.prototype.isSetter.apply(this, arguments); }

  isCovariant() { return CMethod.prototype.isCovariant.apply(this, arguments); }

  isCtor() { return CSlot.prototype.isCtor.apply(this, arguments); }

  #returns = null;

  returns(it) {
    if (it === undefined) {
      return this.#returns;
    }
    else {
      this.#returns = it;
      return;
    }
  }

  #params = null;

  params(it) {
    if (it === undefined) {
      return this.#params;
    }
    else {
      this.#params = it;
      return;
    }
  }

  static make(parent,name,flags,ret,params) {
    const $self = new MockMethod();
    MockMethod.make$($self,parent,name,flags,ret,params);
    return $self;
  }

  static make$($self,parent,name,flags,ret,params) {
    const this$ = $self;
    MockSlot.make$($self, parent, name, flags);
    $self.parent(parent);
    $self.returns(ret);
    $self.params(sys.ObjUtil.coerce(params.map((p,i) => {
      return MockParam.make(sys.Str.plus("p", sys.ObjUtil.coerce(i, sys.Obj.type$.toNullable())), p);
    }, CParam.type$), sys.Type.find("compiler::CParam[]")));
    return;
  }

  inheritedReturns() {
    return this.returns();
  }

}

class MockParam extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return MockParam.type$; }

  paramType() { return CParam.prototype.paramType.apply(this, arguments); }

  #name = null;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #type = null;

  type(it) {
    if (it === undefined) {
      return this.#type;
    }
    else {
      this.#type = it;
      return;
    }
  }

  static make(name,of$) {
    const $self = new MockParam();
    MockParam.make$($self,name,of$);
    return $self;
  }

  static make$($self,name,of$) {
    $self.name(name);
    $self.type(of$);
    return;
  }

  hasDefault() {
    return false;
  }

}

class TypeParser extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return TypeParser.type$; }

  #ns = null;

  // private field reflection only
  __ns(it) { if (it === undefined) return this.#ns; else this.#ns = it; }

  #sig = null;

  // private field reflection only
  __sig(it) { if (it === undefined) return this.#sig; else this.#sig = it; }

  #len = 0;

  // private field reflection only
  __len(it) { if (it === undefined) return this.#len; else this.#len = it; }

  #pos = 0;

  // private field reflection only
  __pos(it) { if (it === undefined) return this.#pos; else this.#pos = it; }

  #cur = 0;

  // private field reflection only
  __cur(it) { if (it === undefined) return this.#cur; else this.#cur = it; }

  #peek = 0;

  // private field reflection only
  __peek(it) { if (it === undefined) return this.#peek; else this.#peek = it; }

  static resolve(ns,sig) {
    let last = sys.Str.get(sig, -1);
    if (sys.ObjUtil.equals(last, 63)) {
      return TypeParser.resolve(ns, sys.Str.getRange(sig, sys.Range.make(0, -2))).toNullable();
    }
    ;
    if ((sys.ObjUtil.compareNE(last, 93) && sys.ObjUtil.compareNE(last, 124))) {
      let colon = sys.Str.index(sig, "::");
      let podName = sys.Str.getRange(sig, sys.Range.make(0, sys.ObjUtil.coerce(colon, sys.Int.type$), true));
      let typeName = sys.Str.getRange(sig, sys.Range.make(sys.Int.plus(sys.ObjUtil.coerce(colon, sys.Int.type$), 2), -1));
      return sys.ObjUtil.coerce(ns.resolvePod(podName, null).resolveType(typeName, true), CType.type$);
    }
    ;
    return TypeParser.make(ns, sig).loadTop();
  }

  static make(ns,sig) {
    const $self = new TypeParser();
    TypeParser.make$($self,ns,sig);
    return $self;
  }

  static make$($self,ns,sig) {
    $self.#ns = ns;
    $self.#sig = sig;
    $self.#len = sys.Str.size(sig);
    $self.#pos = 0;
    $self.#cur = sys.Str.get(sig, $self.#pos);
    $self.#peek = sys.Str.get(sig, sys.Int.plus($self.#pos, 1));
    return;
  }

  loadTop() {
    let t = this.loadAny();
    if (sys.ObjUtil.compareNE(this.#cur, 0)) {
      throw this.err();
    }
    ;
    return t;
  }

  loadAny() {
    let t = null;
    if (sys.ObjUtil.equals(this.#cur, 124)) {
      (t = this.loadFunc());
    }
    else {
      if (sys.ObjUtil.equals(this.#cur, 91)) {
        let ffi = true;
        for (let i = sys.Int.plus(this.#pos, 1); sys.ObjUtil.compareLT(i, this.#len); i = sys.Int.increment(i)) {
          let ch = sys.Str.get(this.#sig, i);
          if (TypeParser.isIdChar(ch)) {
            continue;
          }
          ;
          (ffi = sys.ObjUtil.equals(ch, 93));
          break;
        }
        ;
        if (ffi) {
          (t = this.loadFFI());
        }
        else {
          (t = this.loadMap());
        }
        ;
      }
      else {
        (t = this.loadBasic());
      }
      ;
    }
    ;
    if (sys.ObjUtil.equals(this.#cur, 63)) {
      this.consume(sys.ObjUtil.coerce(63, sys.Int.type$.toNullable()));
      (t = t.toNullable());
    }
    ;
    while (sys.ObjUtil.equals(this.#cur, 91)) {
      this.consume(sys.ObjUtil.coerce(91, sys.Int.type$.toNullable()));
      this.consume(sys.ObjUtil.coerce(93, sys.Int.type$.toNullable()));
      (t = t.toListOf());
      if (sys.ObjUtil.equals(this.#cur, 63)) {
        this.consume(sys.ObjUtil.coerce(63, sys.Int.type$.toNullable()));
        (t = t.toNullable());
      }
      ;
    }
    ;
    return sys.ObjUtil.coerce(t, CType.type$);
  }

  loadMap() {
    this.consume(sys.ObjUtil.coerce(91, sys.Int.type$.toNullable()));
    let key = this.loadAny();
    this.consume(sys.ObjUtil.coerce(58, sys.Int.type$.toNullable()));
    let val = this.loadAny();
    this.consume(sys.ObjUtil.coerce(93, sys.Int.type$.toNullable()));
    return MapType.make(key, val);
  }

  loadFunc() {
    this.consume(sys.ObjUtil.coerce(124, sys.Int.type$.toNullable()));
    let params = sys.List.make(CType.type$);
    let names = sys.List.make(sys.Str.type$);
    if (sys.ObjUtil.compareNE(this.#cur, 45)) {
      while (true) {
        params.add(this.loadAny());
        names.add(sys.Int.toChar(sys.Int.plus(97, names.size())));
        if (sys.ObjUtil.equals(this.#cur, 45)) {
          break;
        }
        ;
        this.consume(sys.ObjUtil.coerce(44, sys.Int.type$.toNullable()));
      }
      ;
    }
    ;
    this.consume(sys.ObjUtil.coerce(45, sys.Int.type$.toNullable()));
    this.consume(sys.ObjUtil.coerce(62, sys.Int.type$.toNullable()));
    let ret = this.loadAny();
    this.consume(sys.ObjUtil.coerce(124, sys.Int.type$.toNullable()));
    return FuncType.make(params, names, ret);
  }

  loadFFI() {
    let start = this.#pos;
    while ((sys.ObjUtil.compareNE(this.#cur, 58) || sys.ObjUtil.compareNE(this.#peek, 58))) {
      this.consume();
    }
    ;
    this.consume(sys.ObjUtil.coerce(58, sys.Int.type$.toNullable()));
    this.consume(sys.ObjUtil.coerce(58, sys.Int.type$.toNullable()));
    while (sys.ObjUtil.equals(this.#cur, 91)) {
      this.consume();
    }
    ;
    while (TypeParser.isIdChar(this.#cur)) {
      this.consume();
    }
    ;
    let qname = sys.Str.getRange(this.#sig, sys.Range.make(start, this.#pos, true));
    return this.#ns.resolveType(qname);
  }

  loadBasic() {
    let start = this.#pos;
    while ((sys.ObjUtil.compareNE(this.#cur, 58) || sys.ObjUtil.compareNE(this.#peek, 58))) {
      this.consume();
    }
    ;
    this.consume(sys.ObjUtil.coerce(58, sys.Int.type$.toNullable()));
    this.consume(sys.ObjUtil.coerce(58, sys.Int.type$.toNullable()));
    while (TypeParser.isIdChar(this.#cur)) {
      this.consume();
    }
    ;
    let qname = sys.Str.getRange(this.#sig, sys.Range.make(start, this.#pos, true));
    return this.#ns.resolveType(qname);
  }

  consume(expected) {
    if (expected === undefined) expected = null;
    if ((expected != null && sys.ObjUtil.compareNE(this.#cur, expected))) {
      throw this.err();
    }
    ;
    this.#cur = this.#peek;
    ((this$) => { let $_u103 = this$.#pos;this$.#pos = sys.Int.increment(this$.#pos); return $_u103; })(this);
    this.#peek = ((this$) => { if (sys.ObjUtil.compareLT(sys.Int.plus(this$.#pos, 1), this$.#len)) return sys.Str.get(this$.#sig, sys.Int.plus(this$.#pos, 1)); return 0; })(this);
    return;
  }

  static isIdChar(ch) {
    return (sys.Int.isAlphaNum(ch) || sys.ObjUtil.equals(ch, 95));
  }

  err() {
    return sys.ArgErr.make(sys.Str.plus(sys.Str.plus("Invalid type signature '", this.#sig), "'"));
  }

}

class CDepend extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return CDepend.type$; }

  #depend = null;

  depend() { return this.#depend; }

  __depend(it) { if (it === undefined) return this.#depend; else this.#depend = it; }

  #pod = null;

  pod(it) {
    if (it === undefined) {
      return this.#pod;
    }
    else {
      this.#pod = it;
      return;
    }
  }

  static fromStr(s) {
    return CDepend.make(sys.ObjUtil.coerce(sys.Depend.fromStr(s), sys.Depend.type$), null);
  }

  static make(d,p) {
    const $self = new CDepend();
    CDepend.make$($self,d,p);
    return $self;
  }

  static make$($self,d,p) {
    $self.#depend = d;
    $self.#pod = p;
    return;
  }

  static makeList(d) {
    const this$ = this;
    return sys.ObjUtil.coerce(d.map((x) => {
      return CDepend.make(x, null);
    }, CDepend.type$), sys.Type.find("compiler::CDepend[]"));
  }

  name() {
    return this.#depend.name();
  }

  toStr() {
    return this.#depend.toStr();
  }

}

class Tokenizer extends CompilerSupport {
  constructor() {
    super();
    const this$ = this;
    this.#line = 1;
    this.#col = 1;
    return;
  }

  typeof() { return Tokenizer.type$; }

  #buf = null;

  // private field reflection only
  __buf(it) { if (it === undefined) return this.#buf; else this.#buf = it; }

  #pos = 0;

  // private field reflection only
  __pos(it) { if (it === undefined) return this.#pos; else this.#pos = it; }

  #isDoc = false;

  // private field reflection only
  __isDoc(it) { if (it === undefined) return this.#isDoc; else this.#isDoc = it; }

  #filename = null;

  // private field reflection only
  __filename(it) { if (it === undefined) return this.#filename; else this.#filename = it; }

  #line = 0;

  // private field reflection only
  __line(it) { if (it === undefined) return this.#line; else this.#line = it; }

  #col = 0;

  // private field reflection only
  __col(it) { if (it === undefined) return this.#col; else this.#col = it; }

  #curLine = 0;

  // private field reflection only
  __curLine(it) { if (it === undefined) return this.#curLine; else this.#curLine = it; }

  #cur = 0;

  // private field reflection only
  __cur(it) { if (it === undefined) return this.#cur; else this.#cur = it; }

  #peek = 0;

  // private field reflection only
  __peek(it) { if (it === undefined) return this.#peek; else this.#peek = it; }

  #lastLine = 0;

  // private field reflection only
  __lastLine(it) { if (it === undefined) return this.#lastLine; else this.#lastLine = it; }

  #posOfLine = 0;

  // private field reflection only
  __posOfLine(it) { if (it === undefined) return this.#posOfLine; else this.#posOfLine = it; }

  #tokens = null;

  // private field reflection only
  __tokens(it) { if (it === undefined) return this.#tokens; else this.#tokens = it; }

  #inStrLiteral = false;

  // private field reflection only
  __inStrLiteral(it) { if (it === undefined) return this.#inStrLiteral; else this.#inStrLiteral = it; }

  #whitespace = false;

  // private field reflection only
  __whitespace(it) { if (it === undefined) return this.#whitespace; else this.#whitespace = it; }

  static make(compiler,loc,buf,isDoc) {
    const $self = new Tokenizer();
    Tokenizer.make$($self,compiler,loc,buf,isDoc);
    return $self;
  }

  static make$($self,compiler,loc,buf,isDoc) {
    CompilerSupport.make$($self, compiler);
    ;
    $self.#buf = buf;
    $self.#filename = loc.file();
    $self.#isDoc = isDoc;
    $self.#tokens = sys.List.make(TokenVal.type$);
    $self.#inStrLiteral = false;
    $self.#posOfLine = 0;
    $self.#whitespace = false;
    $self.#cur = ((this$) => { let $_u105 = 32; this$.#peek = $_u105; return $_u105; })($self);
    if (sys.ObjUtil.compareGT(sys.Str.size(buf), 0)) {
      $self.#cur = sys.Str.get(buf, 0);
    }
    ;
    if (sys.ObjUtil.compareGT(sys.Str.size(buf), 1)) {
      $self.#peek = sys.Str.get(buf, 1);
    }
    ;
    $self.#pos = 0;
    if (sys.ObjUtil.equals($self.#cur, 35)) {
      while (true) {
        if (sys.ObjUtil.equals($self.#cur, 10)) {
          $self.consume();
          break;
        }
        ;
        if (sys.ObjUtil.equals($self.#cur, 0)) {
          break;
        }
        ;
        $self.consume();
      }
      ;
    }
    ;
    return;
  }

  tokenize() {
    while (true) {
      let tok = this.next();
      this.#tokens.add(sys.ObjUtil.coerce(tok, TokenVal.type$));
      if (tok.kind() === Token.eof()) {
        break;
      }
      ;
    }
    ;
    return this.#tokens;
  }

  next() {
    while (true) {
      this.#curLine = this.#line;
      let col = this.#col;
      let tok = this.find();
      if (tok == null) {
        continue;
      }
      ;
      tok.file(this.#filename);
      tok.line(sys.ObjUtil.coerce(this.#curLine, sys.Int.type$.toNullable()));
      tok.col(sys.ObjUtil.coerce(col, sys.Int.type$.toNullable()));
      tok.newline(sys.ObjUtil.compareLT(this.#lastLine, this.#line));
      tok.whitespace(this.#whitespace);
      this.#lastLine = this.#line;
      this.#whitespace = false;
      return tok;
    }
    ;
    return null;
  }

  find() {
    if (sys.Int.isSpace(this.#cur)) {
      this.consume();
      this.#whitespace = true;
      return null;
    }
    ;
    if (Tokenizer.isIdentifierStart(this.#cur)) {
      return this.word();
    }
    ;
    if (sys.Int.isDigit(this.#cur)) {
      return this.number();
    }
    ;
    if ((sys.ObjUtil.equals(this.#cur, 46) && sys.Int.isDigit(this.#peek))) {
      return this.number();
    }
    ;
    if ((sys.ObjUtil.equals(this.#cur, 34) && sys.ObjUtil.equals(this.#peek, 34) && sys.ObjUtil.equals(this.peekPeek(), 34))) {
      return this.quoted(Quoted.triple());
    }
    ;
    if (sys.ObjUtil.equals(this.#cur, 34)) {
      return this.quoted(Quoted.normal());
    }
    ;
    if (sys.ObjUtil.equals(this.#cur, 96)) {
      return this.quoted(Quoted.uri());
    }
    ;
    if (sys.ObjUtil.equals(this.#cur, 39)) {
      return this.ch();
    }
    ;
    if ((sys.ObjUtil.equals(this.#cur, 42) && sys.ObjUtil.equals(this.#peek, 42))) {
      return this.docComment();
    }
    ;
    if ((sys.ObjUtil.equals(this.#cur, 47) && sys.ObjUtil.equals(this.#peek, 47))) {
      return this.skipCommentSL();
    }
    ;
    if ((sys.ObjUtil.equals(this.#cur, 47) && sys.ObjUtil.equals(this.#peek, 42))) {
      return this.skipCommentML();
    }
    ;
    if ((sys.ObjUtil.equals(this.#cur, 60) && sys.ObjUtil.equals(this.#peek, 124))) {
      return this.dsl();
    }
    ;
    return this.symbol();
  }

  word() {
    let start = this.#pos;
    while ((sys.Int.isAlphaNum(this.#cur) || sys.ObjUtil.equals(this.#cur, 95))) {
      this.consume();
    }
    ;
    let word = sys.Str.getRange(this.#buf, sys.Range.make(start, this.#pos, true));
    let keyword = Token.keywords().get(word);
    if (keyword != null) {
      return TokenVal.make(sys.ObjUtil.coerce(keyword, Token.type$));
    }
    ;
    return TokenVal.make(Token.identifier(), word);
  }

  static isIdentifierStart(c) {
    return (sys.Int.isAlpha(c) || sys.ObjUtil.equals(c, 95));
  }

  number() {
    if (sys.ObjUtil.equals(this.#cur, 48)) {
      if (sys.ObjUtil.equals(this.#peek, 120)) {
        return this.hexInt();
      }
      ;
      if (sys.ObjUtil.equals(this.#peek, 98)) {
        return this.binaryInt();
      }
      ;
    }
    ;
    let start = this.#pos;
    let dot = false;
    let exp = false;
    while ((sys.Int.isDigit(this.#cur) || sys.ObjUtil.equals(this.#cur, 95))) {
      this.consume();
    }
    ;
    if ((sys.ObjUtil.equals(this.#cur, 46) && sys.Int.isDigit(this.#peek))) {
      (dot = true);
      this.consume();
      while ((sys.Int.isDigit(this.#cur) || sys.ObjUtil.equals(this.#cur, 95))) {
        this.consume();
      }
      ;
    }
    ;
    if ((sys.ObjUtil.equals(this.#cur, 101) || sys.ObjUtil.equals(this.#cur, 69))) {
      this.consume();
      (exp = true);
      if ((sys.ObjUtil.equals(this.#cur, 45) || sys.ObjUtil.equals(this.#cur, 43))) {
        this.consume();
      }
      ;
      if (!sys.Int.isDigit(this.#cur)) {
        throw this.err("Expected exponent digits");
      }
      ;
      while ((sys.Int.isDigit(this.#cur) || sys.ObjUtil.equals(this.#cur, 95))) {
        this.consume();
      }
      ;
    }
    ;
    let str = sys.Str.replace(sys.Str.getRange(this.#buf, sys.Range.make(start, this.#pos, true)), "_", "");
    let floatSuffix = false;
    let decimalSuffix = false;
    let dur = null;
    if ((sys.Int.isLower(this.#cur) && sys.Int.isLower(this.#peek))) {
      if ((sys.ObjUtil.equals(this.#cur, 110) && sys.ObjUtil.equals(this.#peek, 115))) {
        this.consume();
        this.consume();
        (dur = sys.ObjUtil.coerce(1, sys.Int.type$.toNullable()));
      }
      ;
      if ((sys.ObjUtil.equals(this.#cur, 109) && sys.ObjUtil.equals(this.#peek, 115))) {
        this.consume();
        this.consume();
        (dur = sys.ObjUtil.coerce(1000000, sys.Int.type$.toNullable()));
      }
      ;
      if ((sys.ObjUtil.equals(this.#cur, 115) && sys.ObjUtil.equals(this.#peek, 101))) {
        this.consume();
        this.consume();
        if (sys.ObjUtil.compareNE(this.#cur, 99)) {
          throw this.err("Expected 'sec' in Duration literal");
        }
        ;
        this.consume();
        (dur = sys.ObjUtil.coerce(1000000000, sys.Int.type$.toNullable()));
      }
      ;
      if ((sys.ObjUtil.equals(this.#cur, 109) && sys.ObjUtil.equals(this.#peek, 105))) {
        this.consume();
        this.consume();
        if (sys.ObjUtil.compareNE(this.#cur, 110)) {
          throw this.err("Expected 'min' in Duration literal");
        }
        ;
        this.consume();
        (dur = sys.ObjUtil.coerce(60000000000, sys.Int.type$.toNullable()));
      }
      ;
      if ((sys.ObjUtil.equals(this.#cur, 104) && sys.ObjUtil.equals(this.#peek, 114))) {
        this.consume();
        this.consume();
        (dur = sys.ObjUtil.coerce(3600000000000, sys.Int.type$.toNullable()));
      }
      ;
      if ((sys.ObjUtil.equals(this.#cur, 100) && sys.ObjUtil.equals(this.#peek, 97))) {
        this.consume();
        this.consume();
        if (sys.ObjUtil.compareNE(this.#cur, 121)) {
          throw this.err("Expected 'day' in Duration literal");
        }
        ;
        this.consume();
        (dur = sys.ObjUtil.coerce(86400000000000, sys.Int.type$.toNullable()));
      }
      ;
    }
    else {
      if ((sys.ObjUtil.equals(this.#cur, 102) || sys.ObjUtil.equals(this.#cur, 70))) {
        this.consume();
        (floatSuffix = true);
      }
      else {
        if ((sys.ObjUtil.equals(this.#cur, 100) || sys.ObjUtil.equals(this.#cur, 68))) {
          this.consume();
          (decimalSuffix = true);
        }
        ;
      }
      ;
    }
    ;
    try {
      if (floatSuffix) {
        let num = sys.Float.fromStr(str);
        return TokenVal.make(Token.floatLiteral(), sys.ObjUtil.coerce(num, sys.Obj.type$.toNullable()));
      }
      ;
      if ((decimalSuffix || dot || exp)) {
        let num = sys.Decimal.fromStr(str);
        if (dur != null) {
          return TokenVal.make(Token.durationLiteral(), sys.Duration.make(sys.Decimal.toInt(sys.Decimal.mult(num, sys.Num.toDecimal(sys.ObjUtil.coerce(dur, sys.Num.type$))))));
        }
        else {
          if (!decimalSuffix) {
            this.err("Float/Decimal literal must have f/d suffix");
          }
          ;
          return TokenVal.make(Token.decimalLiteral(), num);
        }
        ;
      }
      ;
      let num = sys.Int.fromStr(str);
      if (dur != null) {
        return TokenVal.make(Token.durationLiteral(), sys.Duration.make(sys.Int.mult(sys.ObjUtil.coerce(num, sys.Int.type$), sys.ObjUtil.coerce(dur, sys.Int.type$))));
      }
      else {
        return TokenVal.make(Token.intLiteral(), sys.ObjUtil.coerce(num, sys.Obj.type$.toNullable()));
      }
      ;
    }
    catch ($_u106) {
      $_u106 = sys.Err.make($_u106);
      if ($_u106 instanceof sys.ParseErr) {
        let e = $_u106;
        ;
        throw this.err(sys.Str.plus(sys.Str.plus("Invalid numeric literal '", str), "'"));
      }
      else {
        throw $_u106;
      }
    }
    ;
  }

  hexInt() {
    this.consume();
    this.consume();
    let val = sys.Int.fromDigit(this.#cur, 16);
    if (val == null) {
      throw this.err("Expecting hex number");
    }
    ;
    this.consume();
    let nibCount = 1;
    while (true) {
      let nib = sys.Int.fromDigit(this.#cur, 16);
      if (nib == null) {
        if (sys.ObjUtil.equals(this.#cur, 95)) {
          this.consume();
          continue;
        }
        ;
        break;
      }
      ;
      ((this$) => { let $_u107 = nibCount;nibCount = sys.Int.increment(nibCount); return $_u107; })(this);
      if (sys.ObjUtil.compareGT(nibCount, 16)) {
        throw this.err("Hex literal too big");
      }
      ;
      (val = sys.ObjUtil.coerce(sys.Int.plus(sys.Int.shiftl(sys.ObjUtil.coerce(val, sys.Int.type$), 4), sys.ObjUtil.coerce(nib, sys.Int.type$)), sys.Int.type$.toNullable()));
      this.consume();
    }
    ;
    return TokenVal.make(Token.intLiteral(), sys.ObjUtil.coerce(val, sys.Obj.type$.toNullable()));
  }

  binaryInt() {
    this.consume();
    this.consume();
    let val = sys.Int.fromDigit(this.#cur, 2);
    if (val == null) {
      throw this.err("Expecting binary digit");
    }
    ;
    this.consume();
    let bitCount = 1;
    while (true) {
      let bit = sys.Int.fromDigit(this.#cur, 2);
      if (bit == null) {
        if (sys.ObjUtil.equals(this.#cur, 95)) {
          this.consume();
          continue;
        }
        ;
        break;
      }
      ;
      ((this$) => { let $_u108 = bitCount;bitCount = sys.Int.increment(bitCount); return $_u108; })(this);
      if (sys.ObjUtil.compareGT(bitCount, 64)) {
        throw this.err("Binary literal too big");
      }
      ;
      (val = sys.ObjUtil.coerce(sys.Int.plus(sys.Int.shiftl(sys.ObjUtil.coerce(val, sys.Int.type$), 1), sys.ObjUtil.coerce(bit, sys.Int.type$)), sys.Int.type$.toNullable()));
      this.consume();
    }
    ;
    return TokenVal.make(Token.intLiteral(), sys.ObjUtil.coerce(val, sys.Obj.type$.toNullable()));
  }

  quoted(q) {
    this.#inStrLiteral = true;
    try {
      let line = this.#line;
      let col = this.#col;
      this.consume();
      if (q.isTriple()) {
        this.consume();
        this.consume();
      }
      ;
      let openLine = this.#posOfLine;
      let openPos = this.#pos;
      let multiLineOk = true;
      let s = sys.StrBuf.make();
      let interpolated = false;
      while (true) {
        if (sys.ObjUtil.equals(this.#cur, 0)) {
          throw this.err(sys.Str.plus("Unexpected end of ", q));
        }
        ;
        if (this.endOfQuoted(q)) {
          break;
        }
        ;
        if (sys.ObjUtil.equals(this.#cur, 10)) {
          if (!q.multiLine()) {
            throw this.err(sys.Str.plus("Unexpected end of ", q));
          }
          ;
          s.addChar(this.#cur);
          this.consume();
          if (multiLineOk) {
            (multiLineOk = this.skipStrWs(openLine, openPos));
          }
          ;
          continue;
        }
        ;
        if (sys.ObjUtil.equals(this.#cur, 36)) {
          if (!interpolated) {
            (interpolated = true);
            this.#tokens.add(this.makeVirtualToken(line, col, Token.lparenSynthetic(), null));
          }
          ;
          if (!this.interpolation(line, col, s.toStr(), q)) {
            (line = this.#line);
            (col = sys.Int.minus(this.#col, 1));
            this.#tokens.add(this.makeVirtualToken(line, col, Token.rparen(), null));
            if (q.isUri()) {
              this.#tokens.add(this.makeVirtualToken(line, col, Token.dot()));
              this.#tokens.add(this.makeVirtualToken(line, col, Token.identifier(), "toUri"));
            }
            ;
            return null;
          }
          ;
          (line = this.#line);
          (col = this.#col);
          s.clear();
        }
        else {
          if (sys.ObjUtil.equals(this.#cur, 92)) {
            if (q.isUri()) {
              let $_u109 = this.#peek;
              if (sys.ObjUtil.equals($_u109, 58) || sys.ObjUtil.equals($_u109, 47) || sys.ObjUtil.equals($_u109, 63) || sys.ObjUtil.equals($_u109, 35) || sys.ObjUtil.equals($_u109, 91) || sys.ObjUtil.equals($_u109, 93) || sys.ObjUtil.equals($_u109, 64) || sys.ObjUtil.equals($_u109, 92) || sys.ObjUtil.equals($_u109, 38) || sys.ObjUtil.equals($_u109, 61) || sys.ObjUtil.equals($_u109, 59)) {
                s.addChar(this.#cur).addChar(this.#peek);
                this.consume();
                this.consume();
              }
              else {
                s.addChar(this.escape());
              }
              ;
            }
            else {
              s.addChar(this.escape());
            }
            ;
          }
          else {
            s.addChar(this.#cur);
            this.consume();
          }
          ;
        }
        ;
      }
      ;
      if (interpolated) {
        this.#tokens.add(this.makeVirtualToken(line, col, Token.strLiteral(), s.toStr()));
        (line = this.#line);
        (col = sys.Int.minus(this.#col, 1));
        this.#tokens.add(this.makeVirtualToken(line, col, Token.rparen(), null));
        if (q.isUri()) {
          this.#tokens.add(this.makeVirtualToken(line, col, Token.dot(), null));
          this.#tokens.add(this.makeVirtualToken(line, col, Token.identifier(), "toUri"));
        }
        ;
        return null;
      }
      else {
        if (q.isUri()) {
          return TokenVal.make(Token.uriLiteral(), s.toStr());
        }
        else {
          return TokenVal.make(Token.strLiteral(), s.toStr());
        }
        ;
      }
      ;
    }
    finally {
      this.#inStrLiteral = false;
    }
    ;
  }

  skipStrWs(openLine,openPos) {
    for (let i = openLine; sys.ObjUtil.compareLT(i, openPos); i = sys.Int.increment(i)) {
      let a = sys.Str.get(this.#buf, i);
      if (((sys.ObjUtil.equals(a, 9) && sys.ObjUtil.compareNE(this.#cur, 9)) || (sys.ObjUtil.compareNE(a, 9) && sys.ObjUtil.compareNE(this.#cur, 32)))) {
        if (sys.ObjUtil.equals(this.#cur, 10)) {
          return true;
        }
        ;
        let numTabs = 0;
        let numSpaces = 0;
        for (let j = openLine; sys.ObjUtil.compareLT(j, openPos); j = sys.Int.increment(j)) {
          if (sys.ObjUtil.equals(sys.Str.get(this.#buf, j), 9)) {
            numTabs = sys.Int.increment(numTabs);
          }
          else {
            numSpaces = sys.Int.increment(numSpaces);
          }
          ;
        }
        ;
        if (sys.ObjUtil.equals(numTabs, 0)) {
          this.err(sys.Str.plus(sys.Str.plus("Leading space in multi-line Str must be ", sys.ObjUtil.coerce(numSpaces, sys.Obj.type$.toNullable())), " spaces"));
        }
        else {
          this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Leading space in multi-line Str must be ", sys.ObjUtil.coerce(numTabs, sys.Obj.type$.toNullable())), " tabs and "), sys.ObjUtil.coerce(numSpaces, sys.Obj.type$.toNullable())), " spaces"));
        }
        ;
        return false;
      }
      ;
      this.consume();
    }
    ;
    return true;
  }

  interpolation(line,col,s,q) {
    this.consume();
    this.#tokens.add(this.makeVirtualToken(line, col, Token.strLiteral(), s));
    (line = this.#line);
    (col = this.#col);
    this.#tokens.add(this.makeVirtualToken(line, col, Token.plus()));
    if (sys.ObjUtil.equals(this.#cur, 123)) {
      (line = this.#line);
      (col = this.#col);
      this.#tokens.add(this.makeVirtualToken(line, col, Token.lparenSynthetic()));
      this.consume();
      while (true) {
        if ((this.endOfQuoted(q) || sys.ObjUtil.equals(this.#cur, 0))) {
          throw this.err(sys.Str.plus(sys.Str.plus("Unexpected end of ", q), ", missing }"));
        }
        ;
        let tok = this.next();
        if (tok.kind() === Token.strLiteral()) {
          throw this.err("Cannot nest Str literal within interpolation", tok);
        }
        ;
        if (tok.kind() === Token.uriLiteral()) {
          throw this.err("Cannot nest Uri literal within interpolation", tok);
        }
        ;
        if (tok.kind() === Token.rbrace()) {
          break;
        }
        ;
        this.#tokens.add(sys.ObjUtil.coerce(tok, TokenVal.type$));
      }
      ;
      (line = this.#line);
      (col = this.#col);
      this.#tokens.add(this.makeVirtualToken(line, col, Token.rparen()));
    }
    else {
      if (sys.ObjUtil.equals(this.#cur, 60)) {
        (line = this.#line);
        (col = this.#col);
        this.#tokens.add(this.makeVirtualToken(line, col, Token.lparenSynthetic()));
        this.consume();
        let buf = sys.StrBuf.make();
        while (true) {
          if ((this.endOfQuoted(q) || sys.ObjUtil.equals(this.#cur, 0))) {
            throw this.err(sys.Str.plus(sys.Str.plus("Unexpected end of ", q), ", missing >"));
          }
          ;
          if (sys.ObjUtil.equals(this.#cur, 10)) {
            throw this.err("Unexpected newline, missing >");
          }
          ;
          if (sys.ObjUtil.equals(this.#cur, 62)) {
            break;
          }
          ;
          buf.addChar(this.#cur);
          this.consume();
        }
        ;
        this.consume();
        let tok = TokenVal.make(Token.localeLiteral(), buf.toStr());
        tok.file(this.#filename);
        tok.line(sys.ObjUtil.coerce(line, sys.Int.type$.toNullable()));
        tok.col(sys.ObjUtil.coerce(col, sys.Int.type$.toNullable()));
        this.#tokens.add(tok);
        (line = this.#line);
        (col = this.#col);
        this.#tokens.add(this.makeVirtualToken(line, col, Token.rparen()));
      }
      else {
        let tok = this.next();
        if ((tok.kind() !== Token.identifier() && tok.kind() !== Token.thisKeyword() && tok.kind() !== Token.superKeyword() && tok.kind() !== Token.itKeyword())) {
          throw this.err("Expected identifier after \$");
        }
        ;
        this.#tokens.add(sys.ObjUtil.coerce(tok, TokenVal.type$));
        while (true) {
          if (sys.ObjUtil.compareNE(this.#cur, 46)) {
            break;
          }
          ;
          if (!Tokenizer.isIdentifierStart(this.#peek)) {
            throw this.err("Expected identifier after dot");
          }
          ;
          this.#tokens.add(sys.ObjUtil.coerce(this.next(), TokenVal.type$));
          (tok = this.next());
          this.#tokens.add(sys.ObjUtil.coerce(tok, TokenVal.type$));
        }
        ;
      }
      ;
    }
    ;
    if (this.endOfQuoted(q)) {
      return false;
    }
    ;
    (line = this.#line);
    (col = this.#col);
    this.#tokens.add(this.makeVirtualToken(line, col, Token.plus()));
    return true;
  }

  endOfQuoted(q) {
    let $_u110 = q;
    if (sys.ObjUtil.equals($_u110, Quoted.normal())) {
      if (sys.ObjUtil.compareNE(this.#cur, 34)) {
        return false;
      }
      ;
      this.consume();
      return true;
    }
    else if (sys.ObjUtil.equals($_u110, Quoted.triple())) {
      if ((sys.ObjUtil.compareNE(this.#cur, 34) || sys.ObjUtil.compareNE(this.#peek, 34) || sys.ObjUtil.compareNE(this.peekPeek(), 34))) {
        return false;
      }
      ;
      this.consume();
      this.consume();
      this.consume();
      return true;
    }
    else if (sys.ObjUtil.equals($_u110, Quoted.uri())) {
      if (sys.ObjUtil.compareNE(this.#cur, 96)) {
        return false;
      }
      ;
      this.consume();
      return true;
    }
    else {
      throw sys.Err.make(q.toStr());
    }
    ;
  }

  makeVirtualToken(line,col,kind,value) {
    if (value === undefined) value = null;
    let tok = TokenVal.make(kind, value);
    tok.file(this.#filename);
    tok.line(sys.ObjUtil.coerce(line, sys.Int.type$.toNullable()));
    tok.col(sys.ObjUtil.coerce(col, sys.Int.type$.toNullable()));
    return tok;
  }

  ch() {
    this.consume();
    let c = -1;
    if (sys.ObjUtil.equals(this.#cur, 92)) {
      (c = this.escape());
    }
    else {
      (c = this.#cur);
      this.consume();
    }
    ;
    if (sys.ObjUtil.compareNE(this.#cur, 39)) {
      throw this.err("Expecting ' close of char literal");
    }
    ;
    this.consume();
    return TokenVal.make(Token.intLiteral(), sys.ObjUtil.coerce(c, sys.Obj.type$.toNullable()));
  }

  escape() {
    if (sys.ObjUtil.compareNE(this.#cur, 92)) {
      throw this.err("Internal error");
    }
    ;
    this.consume();
    let $_u111 = this.#cur;
    if (sys.ObjUtil.equals($_u111, 98)) {
      this.consume();
      return 8;
    }
    else if (sys.ObjUtil.equals($_u111, 102)) {
      this.consume();
      return 12;
    }
    else if (sys.ObjUtil.equals($_u111, 110)) {
      this.consume();
      return 10;
    }
    else if (sys.ObjUtil.equals($_u111, 114)) {
      this.consume();
      return 13;
    }
    else if (sys.ObjUtil.equals($_u111, 116)) {
      this.consume();
      return 9;
    }
    else if (sys.ObjUtil.equals($_u111, 34)) {
      this.consume();
      return 34;
    }
    else if (sys.ObjUtil.equals($_u111, 36)) {
      this.consume();
      return 36;
    }
    else if (sys.ObjUtil.equals($_u111, 39)) {
      this.consume();
      return 39;
    }
    else if (sys.ObjUtil.equals($_u111, 96)) {
      this.consume();
      return 96;
    }
    else if (sys.ObjUtil.equals($_u111, 92)) {
      this.consume();
      return 92;
    }
    ;
    if (sys.ObjUtil.equals(this.#cur, 117)) {
      this.consume();
      if (sys.ObjUtil.equals(this.#cur, 123)) {
        this.consume();
        let ch = 0;
        let numDigits = 0;
        while (sys.ObjUtil.compareNE(this.#cur, 125)) {
          let i = sys.Int.fromDigit(this.#cur, 16);
          ((this$) => { let $_u112 = numDigits;numDigits = sys.Int.increment(numDigits); return $_u112; })(this);
          if (i == null) {
            throw this.err("Invalid hex value for \\u{x}");
          }
          ;
          (ch = sys.Int.or(sys.Int.shiftl(ch, 4), sys.ObjUtil.coerce(i, sys.Int.type$)));
          this.consume();
        }
        ;
        if ((sys.ObjUtil.equals(numDigits, 0) || sys.ObjUtil.compareGT(numDigits, 6))) {
          throw this.err("Invalid number of hex digits for \\u{x}");
        }
        ;
        this.consume();
        return ch;
      }
      else {
        let n3 = sys.Int.fromDigit(this.#cur, 16);
        this.consume();
        let n2 = sys.Int.fromDigit(this.#cur, 16);
        this.consume();
        let n1 = sys.Int.fromDigit(this.#cur, 16);
        this.consume();
        let n0 = sys.Int.fromDigit(this.#cur, 16);
        this.consume();
        if ((n3 == null || n2 == null || n1 == null || n0 == null)) {
          throw this.err("Invalid hex value for \\uxxxx");
        }
        ;
        return sys.Int.or(sys.Int.or(sys.Int.or(sys.Int.shiftl(sys.ObjUtil.coerce(n3, sys.Int.type$), 12), sys.Int.shiftl(sys.ObjUtil.coerce(n2, sys.Int.type$), 8)), sys.Int.shiftl(sys.ObjUtil.coerce(n1, sys.Int.type$), 4)), sys.ObjUtil.coerce(n0, sys.Int.type$));
      }
      ;
    }
    ;
    throw this.err("Invalid escape sequence");
  }

  dsl() {
    this.consume();
    this.consume();
    let leadingTabs = 0;
    let leadingSpaces = 0;
    for (let i = this.#posOfLine; sys.ObjUtil.compareLT(i, this.#pos); i = sys.Int.increment(i)) {
      if (sys.ObjUtil.equals(sys.Str.get(this.#buf, i), 9)) {
        ((this$) => { let $_u113 = leadingTabs;leadingTabs = sys.Int.increment(leadingTabs); return $_u113; })(this);
      }
      else {
        ((this$) => { let $_u114 = leadingSpaces;leadingSpaces = sys.Int.increment(leadingSpaces); return $_u114; })(this);
      }
      ;
    }
    ;
    let s = sys.StrBuf.make();
    while (true) {
      if ((sys.ObjUtil.equals(this.#cur, 124) && sys.ObjUtil.equals(this.#peek, 62))) {
        break;
      }
      ;
      if (sys.ObjUtil.equals(this.#cur, 0)) {
        throw this.err("Unexpected end of DSL");
      }
      ;
      s.addChar(this.#cur);
      this.consume();
    }
    ;
    this.consume();
    this.consume();
    return TokenValDsl.make(Token.dsl(), s.toStr(), leadingTabs, leadingSpaces);
  }

  skipCommentSL() {
    this.consume();
    this.consume();
    while (true) {
      if (sys.ObjUtil.equals(this.#cur, 10)) {
        this.consume();
        break;
      }
      ;
      if (sys.ObjUtil.equals(this.#cur, 0)) {
        break;
      }
      ;
      this.consume();
    }
    ;
    return null;
  }

  skipCommentML() {
    this.consume();
    this.consume();
    let depth = 1;
    while (true) {
      if ((sys.ObjUtil.equals(this.#cur, 42) && sys.ObjUtil.equals(this.#peek, 47))) {
        this.consume();
        this.consume();
        ((this$) => { let $_u115 = depth;depth = sys.Int.decrement(depth); return $_u115; })(this);
        if (sys.ObjUtil.compareLE(depth, 0)) {
          break;
        }
        ;
      }
      ;
      if ((sys.ObjUtil.equals(this.#cur, 47) && sys.ObjUtil.equals(this.#peek, 42))) {
        this.consume();
        this.consume();
        ((this$) => { let $_u116 = depth;depth = sys.Int.increment(depth); return $_u116; })(this);
        continue;
      }
      ;
      if (sys.ObjUtil.equals(this.#cur, 0)) {
        break;
      }
      ;
      this.consume();
    }
    ;
    return null;
  }

  docComment() {
    if (!this.#isDoc) {
      this.skipCommentSL();
      return null;
    }
    ;
    while (sys.ObjUtil.equals(this.#cur, 42)) {
      this.consume();
    }
    ;
    if (sys.ObjUtil.equals(this.#cur, 32)) {
      this.consume();
    }
    ;
    let lines = sys.List.make(sys.Str.type$);
    let s = sys.StrBuf.make();
    while (sys.ObjUtil.compareGT(this.#cur, 0)) {
      let c = this.#cur;
      this.consume();
      if (sys.ObjUtil.compareNE(c, 10)) {
        s.addChar(c);
        continue;
      }
      ;
      let line = s.toStr();
      if ((!lines.isEmpty() || !sys.Str.isEmpty(sys.Str.trim(line)))) {
        lines.add(line);
      }
      else {
        ((this$) => { let $_u117 = this$.#curLine;this$.#curLine = sys.Int.increment(this$.#curLine); return $_u117; })(this);
      }
      ;
      s.clear();
      while ((sys.ObjUtil.equals(this.#cur, 32) || sys.ObjUtil.equals(this.#cur, 9))) {
        this.consume();
      }
      ;
      if ((sys.ObjUtil.compareNE(this.#cur, 42) || sys.ObjUtil.compareNE(this.#peek, 42))) {
        break;
      }
      ;
      while (sys.ObjUtil.equals(this.#cur, 42)) {
        this.consume();
      }
      ;
      if ((sys.ObjUtil.equals(this.#cur, 32) || sys.ObjUtil.equals(this.#cur, 9))) {
        this.consume();
      }
      ;
    }
    ;
    lines.add(s.toStr());
    while (!lines.isEmpty()) {
      if (sys.Str.isEmpty(sys.Str.trim(lines.last()))) {
        lines.removeAt(-1);
      }
      else {
        break;
      }
      ;
    }
    ;
    return TokenVal.make(Token.docComment(), lines);
  }

  symbol() {
    let c = this.#cur;
    this.consume();
    let $_u118 = c;
    if (sys.ObjUtil.equals($_u118, 13)) {
      throw this.err("Carriage return \\r not allowed in source");
    }
    else if (sys.ObjUtil.equals($_u118, 33)) {
      if (sys.ObjUtil.equals(this.#cur, 61)) {
        this.consume();
        if (sys.ObjUtil.equals(this.#cur, 61)) {
          this.consume();
          return TokenVal.make(Token.notSame());
        }
        ;
        return TokenVal.make(Token.notEq());
      }
      ;
      return TokenVal.make(Token.bang());
    }
    else if (sys.ObjUtil.equals($_u118, 35)) {
      return TokenVal.make(Token.pound());
    }
    else if (sys.ObjUtil.equals($_u118, 37)) {
      if (sys.ObjUtil.equals(this.#cur, 61)) {
        this.consume();
        return TokenVal.make(Token.assignPercent());
      }
      ;
      return TokenVal.make(Token.percent());
    }
    else if (sys.ObjUtil.equals($_u118, 38)) {
      if (sys.ObjUtil.equals(this.#cur, 38)) {
        this.consume();
        return TokenVal.make(Token.doubleAmp());
      }
      ;
      return TokenVal.make(Token.amp());
    }
    else if (sys.ObjUtil.equals($_u118, 40)) {
      return TokenVal.make(Token.lparen());
    }
    else if (sys.ObjUtil.equals($_u118, 41)) {
      return TokenVal.make(Token.rparen());
    }
    else if (sys.ObjUtil.equals($_u118, 42)) {
      if (sys.ObjUtil.equals(this.#cur, 61)) {
        this.consume();
        return TokenVal.make(Token.assignStar());
      }
      ;
      return TokenVal.make(Token.star());
    }
    else if (sys.ObjUtil.equals($_u118, 43)) {
      if (sys.ObjUtil.equals(this.#cur, 61)) {
        this.consume();
        return TokenVal.make(Token.assignPlus());
      }
      ;
      if (sys.ObjUtil.equals(this.#cur, 43)) {
        this.consume();
        return TokenVal.make(Token.increment());
      }
      ;
      return TokenVal.make(Token.plus());
    }
    else if (sys.ObjUtil.equals($_u118, 44)) {
      return TokenVal.make(Token.comma());
    }
    else if (sys.ObjUtil.equals($_u118, 45)) {
      if (sys.ObjUtil.equals(this.#cur, 62)) {
        this.consume();
        return TokenVal.make(Token.arrow());
      }
      ;
      if (sys.ObjUtil.equals(this.#cur, 45)) {
        this.consume();
        return TokenVal.make(Token.decrement());
      }
      ;
      if (sys.ObjUtil.equals(this.#cur, 61)) {
        this.consume();
        return TokenVal.make(Token.assignMinus());
      }
      ;
      return TokenVal.make(Token.minus());
    }
    else if (sys.ObjUtil.equals($_u118, 46)) {
      if (sys.ObjUtil.equals(this.#cur, 46)) {
        this.consume();
        if (sys.ObjUtil.equals(this.#cur, 60)) {
          this.consume();
          return TokenVal.make(Token.dotDotLt());
        }
        ;
        return TokenVal.make(Token.dotDot());
      }
      ;
      return TokenVal.make(Token.dot());
    }
    else if (sys.ObjUtil.equals($_u118, 47)) {
      if (sys.ObjUtil.equals(this.#cur, 61)) {
        this.consume();
        return TokenVal.make(Token.assignSlash());
      }
      ;
      return TokenVal.make(Token.slash());
    }
    else if (sys.ObjUtil.equals($_u118, 58)) {
      if (sys.ObjUtil.equals(this.#cur, 58)) {
        this.consume();
        return TokenVal.make(Token.doubleColon());
      }
      ;
      if (sys.ObjUtil.equals(this.#cur, 61)) {
        this.consume();
        return TokenVal.make(Token.defAssign());
      }
      ;
      return TokenVal.make(Token.colon());
    }
    else if (sys.ObjUtil.equals($_u118, 59)) {
      return TokenVal.make(Token.semicolon());
    }
    else if (sys.ObjUtil.equals($_u118, 60)) {
      if (sys.ObjUtil.equals(this.#cur, 61)) {
        this.consume();
        if (sys.ObjUtil.equals(this.#cur, 62)) {
          this.consume();
          return TokenVal.make(Token.cmp());
        }
        ;
        return TokenVal.make(Token.ltEq());
      }
      ;
      return TokenVal.make(Token.lt());
    }
    else if (sys.ObjUtil.equals($_u118, 61)) {
      if (sys.ObjUtil.equals(this.#cur, 61)) {
        this.consume();
        if (sys.ObjUtil.equals(this.#cur, 61)) {
          this.consume();
          return TokenVal.make(Token.same());
        }
        ;
        return TokenVal.make(Token.eq());
      }
      ;
      return TokenVal.make(Token.assign());
    }
    else if (sys.ObjUtil.equals($_u118, 62)) {
      if (sys.ObjUtil.equals(this.#cur, 61)) {
        this.consume();
        return TokenVal.make(Token.gtEq());
      }
      ;
      return TokenVal.make(Token.gt());
    }
    else if (sys.ObjUtil.equals($_u118, 63)) {
      if (sys.ObjUtil.equals(this.#cur, 58)) {
        this.consume();
        return TokenVal.make(Token.elvis());
      }
      ;
      if (sys.ObjUtil.equals(this.#cur, 46)) {
        this.consume();
        return TokenVal.make(Token.safeDot());
      }
      ;
      if ((sys.ObjUtil.equals(this.#cur, 45) && sys.ObjUtil.equals(this.#peek, 62))) {
        this.consume();
        this.consume();
        return TokenVal.make(Token.safeArrow());
      }
      ;
      return TokenVal.make(Token.question());
    }
    else if (sys.ObjUtil.equals($_u118, 64)) {
      return TokenVal.make(Token.at());
    }
    else if (sys.ObjUtil.equals($_u118, 91)) {
      return TokenVal.make(Token.lbracket());
    }
    else if (sys.ObjUtil.equals($_u118, 93)) {
      return TokenVal.make(Token.rbracket());
    }
    else if (sys.ObjUtil.equals($_u118, 94)) {
      return TokenVal.make(Token.caret());
    }
    else if (sys.ObjUtil.equals($_u118, 123)) {
      return TokenVal.make(Token.lbrace());
    }
    else if (sys.ObjUtil.equals($_u118, 124)) {
      if (sys.ObjUtil.equals(this.#cur, 124)) {
        this.consume();
        return TokenVal.make(Token.doublePipe());
      }
      ;
      return TokenVal.make(Token.pipe());
    }
    else if (sys.ObjUtil.equals($_u118, 125)) {
      return TokenVal.make(Token.rbrace());
    }
    else if (sys.ObjUtil.equals($_u118, 126)) {
      return TokenVal.make(Token.tilde());
    }
    else if (sys.ObjUtil.equals($_u118, 36)) {
      return TokenVal.make(Token.dollar());
    }
    ;
    if (sys.ObjUtil.equals(c, 0)) {
      return TokenVal.make(Token.eof());
    }
    ;
    throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Unexpected symbol: ", sys.Int.toChar(c)), " (0x"), sys.Int.toHex(c)), ")"));
  }

  err(msg,loc) {
    if (loc === undefined) loc = null;
    if (loc == null) {
      (loc = Loc.make(this.#filename, sys.ObjUtil.coerce(this.#line, sys.Int.type$.toNullable()), sys.ObjUtil.coerce(this.#col, sys.Int.type$.toNullable())));
    }
    ;
    return CompilerSupport.prototype.err.call(this, msg, loc);
  }

  peekPeek() {
    return ((this$) => { if (sys.ObjUtil.compareLT(sys.Int.plus(this$.#pos, 2), sys.Str.size(this$.#buf))) return sys.Str.get(this$.#buf, sys.Int.plus(this$.#pos, 2)); return 0; })(this);
  }

  consume() {
    if (sys.ObjUtil.equals(this.#cur, 10)) {
      ((this$) => { let $_u120 = this$.#line;this$.#line = sys.Int.increment(this$.#line); return $_u120; })(this);
      this.#col = 1;
      this.#posOfLine = sys.Int.plus(this.#pos, 1);
    }
    else {
      ((this$) => { let $_u121 = this$.#col;this$.#col = sys.Int.increment(this$.#col); return $_u121; })(this);
    }
    ;
    this.#cur = this.#peek;
    ((this$) => { let $_u122 = this$.#pos;this$.#pos = sys.Int.increment(this$.#pos); return $_u122; })(this);
    if (sys.ObjUtil.compareLT(sys.Int.plus(this.#pos, 1), sys.Str.size(this.#buf))) {
      this.#peek = sys.Str.get(this.#buf, sys.Int.plus(this.#pos, 1));
    }
    else {
      this.#peek = 0;
    }
    ;
    return;
  }

}

class Quoted extends sys.Enum {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Quoted.type$; }

  static normal() { return Quoted.vals().get(0); }

  static triple() { return Quoted.vals().get(1); }

  static uri() { return Quoted.vals().get(2); }

  static #vals = undefined;

  #toStr = null;

  toStr() { return this.#toStr; }

  __toStr(it) { if (it === undefined) return this.#toStr; else this.#toStr = it; }

  #multiLine = false;

  multiLine() { return this.#multiLine; }

  __multiLine(it) { if (it === undefined) return this.#multiLine; else this.#multiLine = it; }

  isUri() {
    return this === Quoted.uri();
  }

  isTriple() {
    return this === Quoted.triple();
  }

  static make($ordinal,$name,s,ml) {
    const $self = new Quoted();
    Quoted.make$($self,$ordinal,$name,s,ml);
    return $self;
  }

  static make$($self,$ordinal,$name,s,ml) {
    sys.Enum.make$($self, $ordinal, $name);
    $self.#toStr = s;
    $self.#multiLine = ml;
    return;
  }

  static fromStr(name$, checked=true) {
    return sys.Enum.doFromStr(Quoted.type$, Quoted.vals(), name$, checked);
  }

  static vals() {
    if (Quoted.#vals == null) {
      Quoted.#vals = sys.List.make(Quoted.type$, [
        Quoted.make(0, "normal", "Str literal", true),
        Quoted.make(1, "triple", "Str literal", true),
        Quoted.make(2, "uri", "Uri literal", false),
      ]).toImmutable();
    }
    return Quoted.#vals;
  }

  static static$init() {
    const $_u123 = Quoted.vals();
    if (true) {
    }
    ;
    return;
  }

}

class TokenVal extends Loc {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return TokenVal.type$; }

  #kind = null;

  kind(it) {
    if (it === undefined) {
      return this.#kind;
    }
    else {
      this.#kind = it;
      return;
    }
  }

  #val = null;

  val(it) {
    if (it === undefined) {
      return this.#val;
    }
    else {
      this.#val = it;
      return;
    }
  }

  #newline = false;

  newline(it) {
    if (it === undefined) {
      return this.#newline;
    }
    else {
      this.#newline = it;
      return;
    }
  }

  #whitespace = false;

  whitespace(it) {
    if (it === undefined) {
      return this.#whitespace;
    }
    else {
      this.#whitespace = it;
      return;
    }
  }

  static make(kind,val) {
    const $self = new TokenVal();
    TokenVal.make$($self,kind,val);
    return $self;
  }

  static make$($self,kind,val) {
    if (val === undefined) val = null;
    Loc.makeUninit$($self);
    $self.#kind = kind;
    $self.#val = val;
    return;
  }

  hash() {
    return sys.ObjUtil.hash(this.#kind);
  }

  equals(obj) {
    let that = sys.ObjUtil.as(obj, TokenVal.type$);
    if (that == null) {
      return false;
    }
    ;
    return (this.#kind === that.#kind && sys.ObjUtil.equals(this.#val, that.#val));
  }

  toStr() {
    if (this.#kind === Token.identifier()) {
      return sys.ObjUtil.toStr(this.#val);
    }
    ;
    return this.#kind.symbol();
  }

  toCode() {
    let $_u124 = this.#kind;
    if (sys.ObjUtil.equals($_u124, Token.identifier())) {
      return sys.ObjUtil.coerce(this.#val, sys.Str.type$);
    }
    else if (sys.ObjUtil.equals($_u124, Token.strLiteral())) {
      return sys.Str.toCode(sys.ObjUtil.coerce(this.#val, sys.Str.type$));
    }
    else if (sys.ObjUtil.equals($_u124, Token.intLiteral())) {
      return sys.Int.toCode(sys.ObjUtil.coerce(this.#val, sys.Int.type$));
    }
    else if (sys.ObjUtil.equals($_u124, Token.floatLiteral())) {
      return sys.Float.toCode(sys.ObjUtil.coerce(this.#val, sys.Float.type$));
    }
    else if (sys.ObjUtil.equals($_u124, Token.decimalLiteral())) {
      return sys.Decimal.toCode(sys.ObjUtil.coerce(this.#val, sys.Decimal.type$));
    }
    else if (sys.ObjUtil.equals($_u124, Token.durationLiteral())) {
      return sys.ObjUtil.coerce(this.#val, sys.Duration.type$).toCode();
    }
    else if (sys.ObjUtil.equals($_u124, Token.uriLiteral())) {
      return sys.Uri.fromStr(sys.ObjUtil.coerce(this.#val, sys.Str.type$)).toCode();
    }
    else if (sys.ObjUtil.equals($_u124, Token.dsl())) {
      return sys.Str.plus(sys.Str.plus("<|", this.#val), "|>");
    }
    ;
    return this.#kind.symbol();
  }

  isCallOpenParen() {
    return (this.#kind === Token.lparen() && !this.#newline);
  }

  isIndexOpenBracket() {
    return (this.#kind === Token.lbracket() && !this.#newline);
  }

}

class TokenValDsl extends TokenVal {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return TokenValDsl.type$; }

  #leadingTabs = 0;

  leadingTabs(it) {
    if (it === undefined) {
      return this.#leadingTabs;
    }
    else {
      this.#leadingTabs = it;
      return;
    }
  }

  #leadingSpaces = 0;

  leadingSpaces(it) {
    if (it === undefined) {
      return this.#leadingSpaces;
    }
    else {
      this.#leadingSpaces = it;
      return;
    }
  }

  static make(kind,src,tabs,spaces) {
    const $self = new TokenValDsl();
    TokenValDsl.make$($self,kind,src,tabs,spaces);
    return $self;
  }

  static make$($self,kind,src,tabs,spaces) {
    TokenVal.make$($self, kind, src);
    $self.#leadingTabs = tabs;
    $self.#leadingSpaces = spaces;
    return;
  }

}

class Parser extends CompilerSupport {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Parser.type$; }

  static #ProtectionMask = undefined;

  static ProtectionMask() {
    if (Parser.#ProtectionMask === undefined) {
      Parser.static$init();
      if (Parser.#ProtectionMask === undefined) Parser.#ProtectionMask = 0;
    }
    return Parser.#ProtectionMask;
  }

  #unit = null;

  // private field reflection only
  __unit(it) { if (it === undefined) return this.#unit; else this.#unit = it; }

  #tokens = null;

  // private field reflection only
  __tokens(it) { if (it === undefined) return this.#tokens; else this.#tokens = it; }

  #numTokens = 0;

  // private field reflection only
  __numTokens(it) { if (it === undefined) return this.#numTokens; else this.#numTokens = it; }

  #pos = 0;

  // private field reflection only
  __pos(it) { if (it === undefined) return this.#pos; else this.#pos = it; }

  #cur = null;

  // private field reflection only
  __cur(it) { if (it === undefined) return this.#cur; else this.#cur = it; }

  #curt = null;

  // private field reflection only
  __curt(it) { if (it === undefined) return this.#curt; else this.#curt = it; }

  #peek = null;

  // private field reflection only
  __peek(it) { if (it === undefined) return this.#peek; else this.#peek = it; }

  #peekt = null;

  // private field reflection only
  __peekt(it) { if (it === undefined) return this.#peekt; else this.#peekt = it; }

  #inFieldInit = false;

  // private field reflection only
  __inFieldInit(it) { if (it === undefined) return this.#inFieldInit; else this.#inFieldInit = it; }

  #curType = null;

  // private field reflection only
  __curType(it) { if (it === undefined) return this.#curType; else this.#curType = it; }

  #curSlot = null;

  // private field reflection only
  __curSlot(it) { if (it === undefined) return this.#curSlot; else this.#curSlot = it; }

  #curClosure = null;

  // private field reflection only
  __curClosure(it) { if (it === undefined) return this.#curClosure; else this.#curClosure = it; }

  #closureCount = null;

  // private field reflection only
  __closureCount(it) { if (it === undefined) return this.#closureCount; else this.#closureCount = it; }

  #closures = null;

  // private field reflection only
  __closures(it) { if (it === undefined) return this.#closures; else this.#closures = it; }

  static make(compiler,unit,closures) {
    const $self = new Parser();
    Parser.make$($self,compiler,unit,closures);
    return $self;
  }

  static make$($self,compiler,unit,closures) {
    CompilerSupport.make$($self, compiler);
    $self.#unit = unit;
    $self.#tokens = sys.ObjUtil.coerce(unit.tokens(), sys.Type.find("compiler::TokenVal[]"));
    $self.#numTokens = unit.tokens().size();
    $self.#closures = closures;
    $self.reset(0);
    return;
  }

  parse() {
    this.usings();
    while (this.#curt !== Token.eof()) {
      this.typeDef();
    }
    ;
    return;
  }

  usings() {
    while (sys.ObjUtil.equals(this.#curt, Token.usingKeyword())) {
      this.skipUsing();
    }
    ;
    return;
  }

  skipUsing() {
    this.consume(Token.usingKeyword());
    if (this.#curt === Token.strLiteral()) {
      this.consume();
    }
    else {
      if (this.#curt === Token.lbracket()) {
        this.consume();
        this.consumeId();
        this.consume(Token.rbracket());
      }
      ;
      this.consumeId();
      while (this.#curt === Token.dot()) {
        this.consume();
        this.consumeId();
      }
      ;
    }
    ;
    if (this.#curt === Token.doubleColon()) {
      this.consume();
      this.consumeId();
      while (this.#curt === Token.dollar()) {
        this.consume();
        if (this.#curt === Token.identifier()) {
          this.consumeId();
        }
        ;
      }
      ;
      if (this.#curt === Token.asKeyword()) {
        this.consume();
        this.consumeId();
      }
      ;
    }
    ;
    this.endOfStmt();
    return;
  }

  typeDef() {
    const this$ = this;
    let doc = this.doc();
    if (this.#curt === Token.usingKeyword()) {
      throw this.err("Cannot use ** doc comments before using statement");
    }
    ;
    if (this.#curt === Token.eof()) {
      return;
    }
    ;
    let facets = this.facets();
    let flags = this.flags(false);
    if (sys.ObjUtil.equals(sys.Int.and(flags, sys.Int.not(Parser.ProtectionMask())), 0)) {
      (flags = sys.Int.or(flags, FConst.Public()));
    }
    ;
    if (this.compiler().isSys()) {
      (flags = sys.Int.or(flags, FConst.Native()));
    }
    ;
    let loc = this.#cur;
    let isMixin = false;
    let isEnum = false;
    if (this.#curt === Token.mixinKeyword()) {
      if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0)) {
        this.err("The 'abstract' modifier is implied on mixin", loc);
      }
      ;
      if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Final()), 0)) {
        this.err("Cannot use 'final' modifier on mixin", loc);
      }
      ;
      (flags = sys.Int.or(flags, sys.Int.plus(FConst.Mixin(), FConst.Abstract())));
      (isMixin = true);
      this.consume();
    }
    else {
      if ((this.#curt === Token.identifier() && sys.ObjUtil.equals(this.#cur.val(), "enum"))) {
        if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Const()), 0)) {
          this.err("The 'const' modifier is implied on enum", loc);
        }
        ;
        if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Final()), 0)) {
          this.err("The 'final' modifier is implied on enum", loc);
        }
        ;
        if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0)) {
          this.err("Cannot use 'abstract' modifier on enum", loc);
        }
        ;
        (flags = sys.Int.or(flags, sys.Int.plus(sys.Int.plus(FConst.Enum(), FConst.Const()), FConst.Final())));
        (isEnum = true);
        this.consume();
      }
      ;
      if ((this.#curt === Token.identifier() && sys.ObjUtil.equals(this.#cur.val(), "facet"))) {
        if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Const()), 0)) {
          this.err("The 'const' modifier is implied on facet", loc);
        }
        ;
        if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Final()), 0)) {
          this.err("The 'final' modifier is implied on facet", loc);
        }
        ;
        if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0)) {
          this.err("Cannot use 'abstract' modifier on facet", loc);
        }
        ;
        (flags = sys.Int.or(flags, sys.Int.plus(sys.Int.plus(FConst.Facet(), FConst.Const()), FConst.Final())));
        this.consume();
      }
      ;
      this.consume(Token.classKeyword());
    }
    ;
    let name = this.consumeId();
    let def = this.#unit.types().find((def) => {
      return sys.ObjUtil.equals(def.name(), name);
    });
    if (def == null) {
      throw this.err("Invalid class definition", this.#cur);
    }
    ;
    def.docDef(doc);
    def.facets(facets);
    def.flags(flags);
    if (def.isFacet()) {
      def.mixins().add(sys.ObjUtil.coerce(this.ns().facetType(), CType.type$));
    }
    ;
    if (this.#curt === Token.colon()) {
      this.consume();
      let first = this.inheritType();
      if (!first.isMixin()) {
        def.base(first);
      }
      else {
        def.mixins().add(first);
      }
      ;
      while (this.#curt === Token.comma()) {
        this.consume();
        def.mixins().add(this.inheritType());
      }
      ;
    }
    ;
    if (def.base() == null) {
      def.baseSpecified(false);
      if (isEnum) {
        def.base(this.ns().enumType());
      }
      else {
        if (sys.ObjUtil.compareNE(def.qname(), "sys::Obj")) {
          def.base(this.ns().objType());
        }
        ;
      }
      ;
    }
    ;
    this.consume(Token.lbrace());
    this.#curType = def;
    this.#closureCount = sys.ObjUtil.coerce(0, sys.Int.type$.toNullable());
    if (isEnum) {
      this.enumDefs(sys.ObjUtil.coerce(def, TypeDef.type$));
    }
    ;
    while (true) {
      (doc = this.doc());
      if (this.#curt === Token.rbrace()) {
        break;
      }
      ;
      let slot = this.slotDef(sys.ObjUtil.coerce(def, TypeDef.type$), doc);
      if (def.hasSlotDef(slot.name())) {
        this.err(sys.Str.plus(sys.Str.plus("Duplicate slot name '", slot.name()), "'"), slot.loc());
      }
      else {
        def.addSlot(slot);
      }
      ;
    }
    ;
    this.#closureCount = null;
    this.#curType = null;
    this.consume(Token.rbrace());
    return;
  }

  inheritType() {
    let t = this.typeRef();
    if (sys.ObjUtil.equals(t, this.ns().facetType())) {
      this.err("Cannot inherit 'Facet' explicitly", t.loc());
    }
    ;
    if (sys.ObjUtil.equals(t, this.ns().enumType())) {
      this.err("Cannot inherit 'Enum' explicitly", t.loc());
    }
    ;
    return t;
  }

  flags(normalize) {
    if (normalize === undefined) normalize = true;
    let loc = this.#cur;
    let flags = 0;
    let protection = false;
    for (let done = false; !done; ) {
      let oldFlags = flags;
      let $_u125 = this.#curt;
      if (sys.ObjUtil.equals($_u125, Token.abstractKeyword())) {
        (flags = sys.Int.or(flags, FConst.Abstract()));
      }
      else if (sys.ObjUtil.equals($_u125, Token.constKeyword())) {
        (flags = sys.Int.or(flags, FConst.Const()));
      }
      else if (sys.ObjUtil.equals($_u125, Token.finalKeyword())) {
        (flags = sys.Int.or(flags, FConst.Final()));
      }
      else if (sys.ObjUtil.equals($_u125, Token.internalKeyword())) {
        (flags = sys.Int.or(flags, FConst.Internal()));
        (protection = true);
      }
      else if (sys.ObjUtil.equals($_u125, Token.nativeKeyword())) {
        (flags = sys.Int.or(flags, FConst.Native()));
      }
      else if (sys.ObjUtil.equals($_u125, Token.newKeyword())) {
        (flags = sys.Int.or(flags, FConst.Ctor()));
      }
      else if (sys.ObjUtil.equals($_u125, Token.onceKeyword())) {
        (flags = sys.Int.or(flags, FConst.Once()));
      }
      else if (sys.ObjUtil.equals($_u125, Token.overrideKeyword())) {
        (flags = sys.Int.or(flags, FConst.Override()));
      }
      else if (sys.ObjUtil.equals($_u125, Token.privateKeyword())) {
        (flags = sys.Int.or(flags, FConst.Private()));
        (protection = true);
      }
      else if (sys.ObjUtil.equals($_u125, Token.protectedKeyword())) {
        (flags = sys.Int.or(flags, FConst.Protected()));
        (protection = true);
      }
      else if (sys.ObjUtil.equals($_u125, Token.publicKeyword())) {
        (flags = sys.Int.or(flags, FConst.Public()));
        (protection = true);
      }
      else if (sys.ObjUtil.equals($_u125, Token.staticKeyword())) {
        (flags = sys.Int.or(flags, FConst.Static()));
      }
      else if (sys.ObjUtil.equals($_u125, Token.virtualKeyword())) {
        (flags = sys.Int.or(flags, FConst.Virtual()));
      }
      else {
        (done = true);
      }
      ;
      if (done) {
        break;
      }
      ;
      if (sys.ObjUtil.equals(oldFlags, flags)) {
        this.err("Repeated modifier");
      }
      ;
      (oldFlags = flags);
      this.consume();
    }
    ;
    if ((sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0) && sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Virtual()), 0))) {
      this.err("Abstract implies virtual", loc);
    }
    ;
    if ((sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Override()), 0) && sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Virtual()), 0))) {
      this.err("Override implies virtual", loc);
    }
    ;
    if (normalize) {
      if (!protection) {
        (flags = sys.Int.or(flags, FConst.Public()));
      }
      ;
      if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0)) {
        (flags = sys.Int.or(flags, FConst.Virtual()));
      }
      ;
      if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Override()), 0)) {
        if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Final()), 0)) {
          (flags = sys.Int.and(flags, sys.Int.not(FConst.Final())));
        }
        else {
          (flags = sys.Int.or(flags, FConst.Virtual()));
        }
        ;
      }
      ;
    }
    ;
    return flags;
  }

  enumDefs(def) {
    const this$ = this;
    let sInit = MethodDef.makeStaticInit(def.loc(), def, null);
    sInit.code(Block.make(def.loc()));
    def.addSlot(sInit);
    this.#curSlot = sInit;
    let ordinal = 0;
    def.enumDefs().add(this.enumDef(((this$) => { let $_u126 = ordinal;ordinal = sys.Int.increment(ordinal); return $_u126; })(this)));
    while (this.#curt === Token.comma()) {
      this.consume();
      let enumDef = this.enumDef(((this$) => { let $_u127 = ordinal;ordinal = sys.Int.increment(ordinal); return $_u127; })(this));
      if (def.enumDefs().any((e) => {
        return sys.ObjUtil.equals(e.name(), enumDef.name());
      })) {
        this.err(sys.Str.plus(sys.Str.plus("Duplicate enum name '", enumDef.name()), "'"), enumDef.loc());
      }
      ;
      def.enumDefs().add(enumDef);
    }
    ;
    this.endOfStmt();
    this.#curSlot = null;
    return;
  }

  enumDef(ordinal) {
    let doc = this.doc();
    let facets = this.facets();
    let def = EnumDef.make(sys.ObjUtil.coerce(this.#cur, Loc.type$), doc, facets, this.consumeId(), ordinal);
    if (this.#curt === Token.lparen()) {
      this.consume(Token.lparen());
      if (sys.ObjUtil.compareNE(this.#curt, Token.rparen())) {
        while (true) {
          def.ctorArgs().add(this.expr());
          if (this.#curt === Token.rparen()) {
            break;
          }
          ;
          this.consume(Token.comma());
        }
        ;
      }
      ;
      this.consume(Token.rparen());
    }
    ;
    return def;
  }

  slotDef(parent,doc) {
    if ((this.#curt === Token.staticKeyword() && this.#peekt === Token.lbrace())) {
      let loc = this.#cur;
      this.consume();
      let sInit = MethodDef.makeStaticInit(sys.ObjUtil.coerce(loc, Loc.type$), parent, null);
      this.#curSlot = sInit;
      sInit.code(this.block());
      this.#curSlot = null;
      return sInit;
    }
    ;
    let loc = this.#cur;
    let facets = this.facets();
    let flags = this.flags();
    if ((this.#curt === Token.identifier() && sys.ObjUtil.equals(this.#cur.val(), parent.name()) && sys.ObjUtil.equals(this.#peekt, Token.lparen()))) {
      this.err("Invalid constructor syntax - use new keyword");
      return this.methodDef(sys.ObjUtil.coerce(loc, Loc.type$), parent, doc, facets, sys.Int.or(flags, FConst.Ctor()), TypeRef.make(sys.ObjUtil.coerce(loc, Loc.type$), sys.ObjUtil.coerce(this.ns().voidType(), CType.type$)), this.consumeId());
    }
    ;
    if ((this.#curt === Token.identifier() && (this.#peekt === Token.defAssign() || this.#peekt === Token.assign()))) {
      let name = this.consumeId();
      return this.fieldDef(sys.ObjUtil.coerce(loc, Loc.type$), parent, doc, facets, flags, null, name);
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Ctor()), 0)) {
      let name = this.consumeId();
      let returns = ((this$) => { if (sys.ObjUtil.equals(sys.Int.and(flags, FConst.Static()), 0)) return TypeRef.make(sys.ObjUtil.coerce(loc, Loc.type$), sys.ObjUtil.coerce(this$.ns().voidType(), CType.type$)); return TypeRef.make(sys.ObjUtil.coerce(loc, Loc.type$), parent.toNullable()); })(this);
      return this.methodDef(sys.ObjUtil.coerce(loc, Loc.type$), parent, doc, facets, flags, returns, name);
    }
    ;
    let type = this.typeRef();
    let name = this.consumeId();
    if (this.#curt === Token.lparen()) {
      return this.methodDef(sys.ObjUtil.coerce(loc, Loc.type$), parent, doc, facets, flags, type, name);
    }
    else {
      return this.fieldDef(sys.ObjUtil.coerce(loc, Loc.type$), parent, doc, facets, flags, type, name);
    }
    ;
  }

  fieldDef(loc,parent,doc,facets,flags,type,name) {
    let field = FieldDef.make(loc, parent);
    field.docDef(doc);
    field.facets(facets);
    field.flags(flags);
    field.name(name);
    if (type != null) {
      field.type(sys.ObjUtil.coerce(type, CType.type$));
    }
    ;
    if (field.isConst()) {
      field.flags(sys.Int.or(field.flags(), FConst.Storage()));
    }
    ;
    if ((this.#curt === Token.defAssign() || this.#curt === Token.assign())) {
      if (this.#curt === Token.assign()) {
        this.err("Must use := for field initialization");
      }
      ;
      this.consume();
      this.#curSlot = field;
      this.#inFieldInit = true;
      field.init(this.expr());
      this.#inFieldInit = false;
      this.#curSlot = null;
    }
    ;
    if (type == null) {
      this.err("Type inference not supported for fields", loc);
    }
    ;
    if (!field.isConst()) {
      this.defGetAndSet(field);
    }
    ;
    if (this.#curt === Token.lbrace()) {
      this.consume(Token.lbrace());
      this.getOrSet(field);
      this.getOrSet(field);
      this.consume(Token.rbrace());
    }
    ;
    if (!field.isConst()) {
      if (field.get().code() == null) {
        this.genSyntheticGet(field);
      }
      ;
      if (field.set().code() == null) {
        this.genSyntheticSet(field);
      }
      ;
    }
    ;
    if ((field.isConst() && field.isOverride())) {
      this.defGet(field);
      this.genSyntheticGet(field);
    }
    ;
    this.endOfStmt();
    return field;
  }

  defGetAndSet(f) {
    this.defGet(f);
    this.defSet(f);
    return;
  }

  defGet(f) {
    let loc = f.loc();
    let get$ = MethodDef.make(loc, f.parentDef());
    get$.accessorFor(f);
    get$.flags(sys.Int.or(f.flags(), FConst.Getter()));
    get$.name(f.name());
    get$.returns(f.type());
    f.get(get$);
    return;
  }

  defSet(f) {
    let loc = f.loc();
    let set$ = MethodDef.make(loc, f.parentDef());
    set$.accessorFor(f);
    set$.flags(sys.Int.or(f.flags(), FConst.Setter()));
    set$.name(f.name());
    set$.returns(sys.ObjUtil.coerce(this.ns().voidType(), CType.type$));
    set$.params().add(ParamDef.make(loc, f.type(), "it"));
    f.set(set$);
    return;
  }

  genSyntheticGet(f) {
    let loc = f.loc();
    f.get().flags(sys.Int.or(f.get().flags(), FConst.Synthetic()));
    if ((!f.isAbstract() && !f.isNative())) {
      f.flags(sys.Int.or(f.flags(), FConst.Storage()));
      f.get().code(Block.make(loc));
      f.get().code().add(ReturnStmt.make(loc, f.makeAccessorExpr(loc, false)));
    }
    ;
    return;
  }

  genSyntheticSet(f) {
    let loc = f.loc();
    f.set().flags(sys.Int.or(f.set().flags(), FConst.Synthetic()));
    if ((!f.isAbstract() && !f.isNative())) {
      f.flags(sys.Int.or(f.flags(), FConst.Storage()));
      let lhs = f.makeAccessorExpr(loc, false);
      let rhs = UnknownVarExpr.make(loc, null, "it");
      f.set().code(Block.make(loc));
      f.set().code().add(BinaryExpr.makeAssign(lhs, rhs).toStmt());
      f.set().code().add(ReturnStmt.make(loc));
    }
    ;
    return;
  }

  getOrSet(f) {
    let loc = this.#cur;
    let accessorFlags = this.flags(false);
    if (this.#curt === Token.identifier()) {
      let idLoc = this.#cur;
      let id = this.consumeId();
      if (sys.ObjUtil.equals(id, "get")) {
        this.#curSlot = f.get();
      }
      else {
        this.#curSlot = f.set();
      }
      ;
      let block = null;
      if (this.#curt === Token.lbrace()) {
        (block = this.block());
      }
      else {
        this.endOfStmt();
      }
      ;
      if (f.isConst()) {
        this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Const field '", f.name()), "' cannot have "), id), "ter"), idLoc);
        return;
      }
      ;
      if (sys.ObjUtil.equals(id, "get")) {
        if (sys.ObjUtil.compareNE(accessorFlags, 0)) {
          this.err("Cannot use modifiers on field getter", loc);
        }
        ;
        f.get().code(block);
      }
      else {
        if (sys.ObjUtil.equals(id, "set")) {
          if (sys.ObjUtil.compareNE(accessorFlags, 0)) {
            if (sys.ObjUtil.compareNE(sys.Int.and(accessorFlags, Parser.ProtectionMask()), 0)) {
              this.err("Cannot use modifiers on field setter except to narrow protection", loc);
            }
            ;
            f.set().flags(sys.Int.or(sys.Int.and(f.set().flags(), Parser.ProtectionMask()), accessorFlags));
          }
          ;
          f.set().code(block);
        }
        else {
          this.err(sys.Str.plus(sys.Str.plus("Expected 'get' or 'set', not '", id), "'"), idLoc);
        }
        ;
      }
      ;
    }
    ;
    return;
  }

  methodDef(loc,parent,doc,facets,flags,ret,name) {
    const this$ = this;
    let method = MethodDef.make(loc, parent);
    method.docDef(doc);
    method.facets(facets);
    method.flags(flags);
    method.returns(ret);
    method.name(name);
    if (ret.isThis()) {
      method.inheritedRet(parent);
    }
    ;
    this.#curSlot = method;
    this.consume(Token.lparen());
    if (this.#curt !== Token.rparen()) {
      while (true) {
        let newParam = this.paramDef();
        if (method.params().any(sys.ObjUtil.coerce((p) => {
          return sys.ObjUtil.equals(p.name(), newParam.name());
        }, sys.Type.find("|compiler::CParam,sys::Int->sys::Bool|")))) {
          this.err(sys.Str.plus(sys.Str.plus("Duplicate parameter name '", newParam.name()), "'"), newParam.loc());
        }
        ;
        method.params().add(newParam);
        if (this.#curt === Token.rparen()) {
          break;
        }
        ;
        this.consume(Token.comma());
      }
      ;
    }
    ;
    this.consume(Token.rparen());
    if (parent.isNative()) {
      (flags = sys.Int.or(flags, FConst.Native()));
    }
    ;
    if ((sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0) || sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Native()), 0))) {
      if (this.#curt === Token.lbrace()) {
        this.err("Abstract and native methods cannot have method body");
        this.block();
      }
      else {
        this.endOfStmt();
      }
      ;
      return method;
    }
    ;
    if ((sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Ctor()), 0) && this.#curt === Token.colon())) {
      method.ctorChain(this.ctorChain(method));
    }
    ;
    if (sys.ObjUtil.compareNE(this.#curt, Token.lbrace())) {
      this.err("Expecting method body");
    }
    else {
      method.code(this.block());
    }
    ;
    this.#curSlot = null;
    return method;
  }

  paramDef() {
    let param = ParamDef.make(sys.ObjUtil.coerce(this.#cur, Loc.type$), this.typeRef(), this.consumeId());
    if ((this.#curt === Token.defAssign() || this.#curt === Token.assign())) {
      if (this.#curt === Token.assign()) {
        this.err("Must use := for parameter default");
      }
      ;
      this.consume();
      param.def(this.expr());
    }
    ;
    return param;
  }

  ctorChain(method) {
    this.consume(Token.colon());
    let loc = this.#cur;
    let call = CallExpr.make(sys.ObjUtil.coerce(loc, Loc.type$));
    call.isCtorChain(true);
    let $_u129 = this.#curt;
    if (sys.ObjUtil.equals($_u129, Token.superKeyword())) {
      this.consume();
      call.target(SuperExpr.make(sys.ObjUtil.coerce(loc, Loc.type$)));
    }
    else if (sys.ObjUtil.equals($_u129, Token.thisKeyword())) {
      this.consume();
      call.target(ThisExpr.make(sys.ObjUtil.coerce(loc, Loc.type$)));
    }
    else {
      throw this.err("Expecting this or super for constructor chaining", loc);
    }
    ;
    if ((call.target().id() === ExprId.superExpr() && sys.ObjUtil.compareNE(this.#curt, Token.dot()))) {
      call.name(method.name());
    }
    else {
      this.consume(Token.dot());
      call.name(this.consumeId());
    }
    ;
    this.callArgs(call, false);
    return call;
  }

  facets() {
    if (this.#curt !== Token.at()) {
      return null;
    }
    ;
    let facets = sys.List.make(FacetDef.type$);
    while (this.#curt === Token.at()) {
      let loc = this.#cur;
      this.consume();
      if (this.#curt !== Token.identifier()) {
        throw this.err("Expecting identifier");
      }
      ;
      let type = this.ctype();
      let f = FacetDef.make(sys.ObjUtil.coerce(loc, Loc.type$), type);
      if (this.#curt === Token.lbrace()) {
        this.consume(Token.lbrace());
        while (this.#curt === Token.identifier()) {
          f.names().add(this.consumeId());
          this.consume(Token.assign());
          f.vals().add(this.expr());
          this.endOfStmt();
        }
        ;
        this.consume(Token.rbrace());
      }
      ;
      facets.add(f);
    }
    ;
    return facets;
  }

  block() {
    this.verify(Token.lbrace());
    return this.stmtOrBlock();
  }

  stmtOrBlock() {
    let block = Block.make(sys.ObjUtil.coerce(this.#cur, Loc.type$));
    if (this.#curt !== Token.lbrace()) {
      block.stmts().add(this.stmt());
    }
    else {
      this.consume(Token.lbrace());
      while (sys.ObjUtil.compareNE(this.#curt, Token.rbrace())) {
        block.stmts().add(this.stmt());
      }
      ;
      this.consume(Token.rbrace());
    }
    ;
    return block;
  }

  stmt() {
    let $_u130 = this.#curt;
    if (sys.ObjUtil.equals($_u130, Token.breakKeyword())) {
      return this.breakStmt();
    }
    else if (sys.ObjUtil.equals($_u130, Token.continueKeyword())) {
      return this.continueStmt();
    }
    else if (sys.ObjUtil.equals($_u130, Token.forKeyword())) {
      return this.forStmt();
    }
    else if (sys.ObjUtil.equals($_u130, Token.ifKeyword())) {
      return this.ifStmt();
    }
    else if (sys.ObjUtil.equals($_u130, Token.returnKeyword())) {
      return this.returnStmt();
    }
    else if (sys.ObjUtil.equals($_u130, Token.switchKeyword())) {
      return this.switchStmt();
    }
    else if (sys.ObjUtil.equals($_u130, Token.throwKeyword())) {
      return this.throwStmt();
    }
    else if (sys.ObjUtil.equals($_u130, Token.tryKeyword())) {
      return this.tryStmt();
    }
    else if (sys.ObjUtil.equals($_u130, Token.whileKeyword())) {
      return this.whileStmt();
    }
    ;
    return this.exprOrLocalDefStmt(true);
  }

  exprOrLocalDefStmt(isEndOfStmt) {
    let loc = this.#cur;
    let mark = this.#pos;
    let localType = this.tryType();
    if (localType != null) {
      if (this.#curt === Token.identifier()) {
        return this.localDefStmt(sys.ObjUtil.coerce(loc, Loc.type$), localType, isEndOfStmt);
      }
      ;
      if (this.#curt === Token.defAssign()) {
        throw this.err("Expected local variable identifier");
      }
      ;
    }
    ;
    this.reset(mark);
    if ((this.#curt === Token.identifier() && this.#peekt === Token.defAssign())) {
      return this.localDefStmt(sys.ObjUtil.coerce(loc, Loc.type$), null, isEndOfStmt);
    }
    ;
    let id = ((this$) => { if (this$.#curt === Token.identifier()) return sys.ObjUtil.coerce(this$.#cur.val(), sys.Str.type$); return null; })(this);
    let e = this.expr();
    if (this.#curt === Token.comma()) {
      (e = this.itAdd(e));
    }
    ;
    let stmt = ExprStmt.make(e);
    if (!isEndOfStmt) {
      return stmt;
    }
    ;
    if (this.endOfStmt(null)) {
      return stmt;
    }
    ;
    if ((id != null && this.#curt === Token.identifier() && (this.#peekt === Token.defAssign() || this.#peekt === Token.assign()))) {
      throw this.err(sys.Str.plus(sys.Str.plus("Unknown type '", id), "' for local declaration"), loc);
    }
    else {
      if ((id == null && this.#curt === Token.defAssign())) {
        throw this.err("Left hand side of ':=' must be identifier", loc);
      }
      else {
        throw this.err("Expected expression statement", loc);
      }
      ;
    }
    ;
  }

  itAdd(e) {
    const this$ = this;
    (e = sys.ObjUtil.coerce(sys.ObjUtil.with(CallExpr.make(e.loc(), ItExpr.make(sys.ObjUtil.coerce(this.#cur, Loc.type$)), "add"), (it) => {
      it.args().add(e);
      it.isItAdd(true);
      return;
    }), CallExpr.type$));
    while (true) {
      this.consume(Token.comma());
      if ((this.#curt === Token.rbrace() || this.#curt === Token.semicolon())) {
        break;
      }
      ;
      (e = sys.ObjUtil.coerce(sys.ObjUtil.with(CallExpr.make(sys.ObjUtil.coerce(this.#cur, Loc.type$), e, "add"), (it) => {
        it.args().add(this$.expr());
        return;
      }), CallExpr.type$));
      if ((this.#curt === Token.rbrace() || this.#curt === Token.semicolon())) {
        break;
      }
      ;
    }
    ;
    return e;
  }

  localDefStmt(loc,localType,isEndOfStmt) {
    let name = this.consumeId();
    let conflict = this.#unit.importedTypes().get(name);
    if ((conflict != null && sys.ObjUtil.compareGT(conflict.size(), 0))) {
      this.err(sys.Str.plus(sys.Str.plus("Variable name conflicts with imported type '", conflict.first()), "'"), loc);
    }
    ;
    let stmt = LocalDefStmt.make(loc, localType, name);
    if ((this.#curt === Token.defAssign() || this.#curt === Token.assign())) {
      if (this.#curt === Token.assign()) {
        this.err("Must use := for declaration assignments");
      }
      ;
      this.consume();
      stmt.init(this.expr());
    }
    ;
    if (isEndOfStmt) {
      this.endOfStmt();
    }
    ;
    return stmt;
  }

  ifStmt() {
    let loc = this.#cur;
    this.consume(Token.ifKeyword());
    this.consume(Token.lparen());
    let cond = this.expr();
    this.consume(Token.rparen());
    let trueBlock = this.stmtOrBlock();
    let stmt = IfStmt.make(sys.ObjUtil.coerce(loc, Loc.type$), cond, trueBlock);
    if (this.#curt === Token.elseKeyword()) {
      this.consume(Token.elseKeyword());
      stmt.falseBlock(this.stmtOrBlock());
    }
    ;
    return stmt;
  }

  returnStmt() {
    let stmt = ReturnStmt.make(sys.ObjUtil.coerce(this.#cur, Loc.type$));
    this.consume(Token.returnKeyword());
    if (!this.endOfStmt(null)) {
      stmt.expr(this.expr());
      this.endOfStmt();
    }
    ;
    return stmt;
  }

  throwStmt() {
    let loc = this.#cur;
    this.consume(Token.throwKeyword());
    let stmt = ThrowStmt.make(sys.ObjUtil.coerce(loc, Loc.type$), this.expr());
    this.endOfStmt();
    return stmt;
  }

  whileStmt() {
    let loc = this.#cur;
    this.consume(Token.whileKeyword());
    this.consume(Token.lparen());
    let cond = this.expr();
    this.consume(Token.rparen());
    return WhileStmt.make(sys.ObjUtil.coerce(loc, Loc.type$), cond, this.stmtOrBlock());
  }

  forStmt() {
    let stmt = ForStmt.make(sys.ObjUtil.coerce(this.#cur, Loc.type$));
    this.consume(Token.forKeyword());
    this.consume(Token.lparen());
    if (this.#curt !== Token.semicolon()) {
      stmt.init(this.exprOrLocalDefStmt(false));
    }
    ;
    this.consume(Token.semicolon());
    if (sys.ObjUtil.compareNE(this.#curt, Token.semicolon())) {
      stmt.condition(this.expr());
    }
    ;
    this.consume(Token.semicolon());
    if (sys.ObjUtil.compareNE(this.#curt, Token.rparen())) {
      stmt.update(this.expr());
    }
    ;
    this.consume(Token.rparen());
    stmt.block(this.stmtOrBlock());
    return stmt;
  }

  breakStmt() {
    let stmt = BreakStmt.make(sys.ObjUtil.coerce(this.#cur, Loc.type$));
    this.consume(Token.breakKeyword());
    this.endOfStmt();
    return stmt;
  }

  continueStmt() {
    let stmt = ContinueStmt.make(sys.ObjUtil.coerce(this.#cur, Loc.type$));
    this.consume(Token.continueKeyword());
    this.endOfStmt();
    return stmt;
  }

  tryStmt() {
    let stmt = TryStmt.make(sys.ObjUtil.coerce(this.#cur, Loc.type$));
    this.consume(Token.tryKeyword());
    stmt.block(this.stmtOrBlock());
    if ((this.#curt !== Token.catchKeyword() && this.#curt !== Token.finallyKeyword())) {
      throw this.err("Expecting catch or finally block");
    }
    ;
    while (this.#curt === Token.catchKeyword()) {
      stmt.catches().add(this.tryCatch());
    }
    ;
    if (this.#curt === Token.finallyKeyword()) {
      this.consume();
      stmt.finallyBlock(this.stmtOrBlock());
    }
    ;
    return stmt;
  }

  tryCatch() {
    let c = Catch.make(sys.ObjUtil.coerce(this.#cur, Loc.type$));
    this.consume(Token.catchKeyword());
    if (this.#curt === Token.lparen()) {
      this.consume(Token.lparen());
      c.errType(this.typeRef());
      c.errVariable(this.consumeId());
      this.consume(Token.rparen());
    }
    ;
    c.block(this.stmtOrBlock());
    if (c.errVariable() != null) {
      c.block().stmts().insert(0, LocalDefStmt.makeCatchVar(c));
    }
    ;
    return c;
  }

  switchStmt() {
    let loc = this.#cur;
    this.consume(Token.switchKeyword());
    this.consume(Token.lparen());
    let stmt = SwitchStmt.make(sys.ObjUtil.coerce(loc, Loc.type$), this.expr());
    this.consume(Token.rparen());
    this.consume(Token.lbrace());
    while (sys.ObjUtil.compareNE(this.#curt, Token.rbrace())) {
      if (this.#curt === Token.caseKeyword()) {
        let c = Case.make(sys.ObjUtil.coerce(this.#cur, Loc.type$));
        while (this.#curt === Token.caseKeyword()) {
          this.consume();
          c.cases().add(this.expr());
          this.consume(Token.colon());
        }
        ;
        if (this.#curt !== Token.defaultKeyword()) {
          c.block(this.switchBlock());
          stmt.cases().add(c);
        }
        ;
      }
      else {
        if (this.#curt === Token.defaultKeyword()) {
          if (stmt.defaultBlock() != null) {
            this.err("Duplicate default blocks");
          }
          ;
          this.consume();
          this.consume(Token.colon());
          stmt.defaultBlock(this.switchBlock());
        }
        else {
          throw this.err("Expected case or default statement");
        }
        ;
      }
      ;
    }
    ;
    this.consume(Token.rbrace());
    this.endOfStmt();
    return stmt;
  }

  switchBlock() {
    let block = Block.make(sys.ObjUtil.coerce(this.#cur, Loc.type$));
    while ((this.#curt !== Token.caseKeyword() && sys.ObjUtil.compareNE(this.#curt, Token.defaultKeyword()) && this.#curt !== Token.rbrace())) {
      block.stmts().add(this.stmt());
    }
    ;
    return block;
  }

  expr() {
    return this.assignExpr();
  }

  assignExpr(expr) {
    if (expr === undefined) expr = null;
    if (expr == null) {
      (expr = this.ifExpr());
    }
    ;
    if (this.#curt.isAssign()) {
      if (this.#curt === Token.assign()) {
        return BinaryExpr.make(sys.ObjUtil.coerce(expr, Expr.type$), this.consume().kind(), this.assignExpr());
      }
      else {
        return ShortcutExpr.makeBinary(sys.ObjUtil.coerce(expr, Expr.type$), this.consume().kind(), this.assignExpr());
      }
      ;
    }
    ;
    return sys.ObjUtil.coerce(expr, Expr.type$);
  }

  ifExpr() {
    let expr = this.condOrExpr();
    if (this.#curt === Token.question()) {
      let condition = expr;
      this.consume(Token.question());
      let trueExpr = this.ifExprBody();
      if ((this.#curt === Token.defAssign() && expr.id() === ExprId.unknownVar() && trueExpr.id() === ExprId.unknownVar())) {
        throw this.err(sys.Str.plus(sys.Str.plus("Unknown type '", expr), "' for local declaration"), expr.loc());
      }
      ;
      this.consume(Token.colon());
      let falseExpr = this.ifExprBody();
      (expr = TernaryExpr.make(condition, trueExpr, falseExpr));
    }
    else {
      if (this.#curt === Token.elvis()) {
        let lhs = expr;
        this.consume();
        let rhs = this.ifExprBody();
        (expr = BinaryExpr.make(lhs, Token.elvis(), rhs));
      }
      ;
    }
    ;
    return expr;
  }

  ifExprBody() {
    if (this.#curt === Token.throwKeyword()) {
      let loc = this.#cur;
      this.consume(Token.throwKeyword());
      return ThrowExpr.make(sys.ObjUtil.coerce(loc, Loc.type$), this.expr());
    }
    else {
      return this.condOrExpr();
    }
    ;
  }

  condOrExpr() {
    let expr = this.condAndExpr();
    if (this.#curt === Token.doublePipe()) {
      let cond = CondExpr.make(expr, this.#cur.kind());
      while (this.#curt === Token.doublePipe()) {
        this.consume();
        cond.operands().add(this.condAndExpr());
      }
      ;
      (expr = cond);
    }
    ;
    return expr;
  }

  condAndExpr() {
    let expr = this.equalityExpr();
    if (this.#curt === Token.doubleAmp()) {
      let cond = CondExpr.make(expr, this.#cur.kind());
      while (this.#curt === Token.doubleAmp()) {
        this.consume();
        cond.operands().add(this.equalityExpr());
      }
      ;
      (expr = cond);
    }
    ;
    return expr;
  }

  equalityExpr() {
    let expr = this.relationalExpr();
    if ((this.#curt === Token.eq() || this.#curt === Token.notEq() || this.#curt === Token.same() || this.#curt === Token.notSame())) {
      let lhs = expr;
      let tok = this.consume().kind();
      let rhs = this.relationalExpr();
      if ((lhs.id() === ExprId.nullLiteral() || rhs.id() === ExprId.nullLiteral())) {
        let id = ((this$) => { if ((tok === Token.eq() || tok === Token.same())) return ExprId.cmpNull(); return ExprId.cmpNotNull(); })(this);
        let operand = ((this$) => { if (lhs.id() === ExprId.nullLiteral()) return rhs; return lhs; })(this);
        (expr = UnaryExpr.make(lhs.loc(), id, tok, operand));
      }
      else {
        if ((tok === Token.same() || tok === Token.notSame())) {
          (expr = BinaryExpr.make(lhs, tok, rhs));
        }
        else {
          (expr = ShortcutExpr.makeBinary(lhs, tok, rhs));
        }
        ;
      }
      ;
    }
    ;
    return expr;
  }

  relationalExpr() {
    let expr = this.rangeExpr();
    if ((this.#curt === Token.isKeyword() || this.#curt === Token.isnotKeyword() || this.#curt === Token.asKeyword() || this.#curt === Token.lt() || this.#curt === Token.ltEq() || this.#curt === Token.gt() || this.#curt === Token.gtEq() || this.#curt === Token.cmp())) {
      let $_u134 = this.#curt;
      if (sys.ObjUtil.equals($_u134, Token.isKeyword())) {
        this.consume();
        (expr = TypeCheckExpr.make(expr.loc(), ExprId.isExpr(), expr, this.ctype()));
      }
      else if (sys.ObjUtil.equals($_u134, Token.isnotKeyword())) {
        this.consume();
        (expr = TypeCheckExpr.make(expr.loc(), ExprId.isnotExpr(), expr, this.ctype()));
      }
      else if (sys.ObjUtil.equals($_u134, Token.asKeyword())) {
        this.consume();
        (expr = TypeCheckExpr.make(expr.loc(), ExprId.asExpr(), expr, this.ctype()));
      }
      else {
        (expr = ShortcutExpr.makeBinary(expr, this.consume().kind(), this.rangeExpr()));
      }
      ;
    }
    ;
    return expr;
  }

  rangeExpr() {
    let expr = this.addExpr();
    if ((this.#curt === Token.dotDot() || this.#curt === Token.dotDotLt())) {
      let start = expr;
      let exclusive = this.consume().kind() === Token.dotDotLt();
      let end = this.addExpr();
      return RangeLiteralExpr.make(expr.loc(), sys.ObjUtil.coerce(this.ns().rangeType(), CType.type$), start, end, exclusive);
    }
    ;
    return expr;
  }

  addExpr() {
    let expr = this.multExpr();
    while ((this.#curt === Token.plus() || this.#curt === Token.minus())) {
      (expr = ShortcutExpr.makeBinary(expr, this.consume().kind(), this.multExpr()));
    }
    ;
    return expr;
  }

  multExpr() {
    let expr = this.parenExpr();
    while ((this.#curt === Token.star() || this.#curt === Token.slash() || this.#curt === Token.percent())) {
      (expr = ShortcutExpr.makeBinary(expr, this.consume().kind(), this.parenExpr()));
    }
    ;
    return expr;
  }

  parenExpr() {
    if ((this.#curt !== Token.lparen() && this.#curt !== Token.lparenSynthetic())) {
      return this.unaryExpr();
    }
    ;
    let loc = this.#cur;
    this.consume();
    let mark = this.#pos;
    let castType = this.tryType();
    if (this.#curt === Token.rparen()) {
      this.consume();
      if (castType == null) {
        throw this.err("Expecting cast '(type)'");
      }
      ;
      return TypeCheckExpr.make(sys.ObjUtil.coerce(loc, Loc.type$), ExprId.coerce(), this.parenExpr(), sys.ObjUtil.coerce(castType, CType.type$));
    }
    ;
    this.reset(mark);
    let expr = this.expr();
    this.consume(Token.rparen());
    while (true) {
      let chained = this.termChainExpr(expr);
      if (chained == null) {
        break;
      }
      ;
      (expr = sys.ObjUtil.coerce(chained, Expr.type$));
    }
    ;
    return expr;
  }

  unaryExpr() {
    let loc = this.#cur;
    let tok = this.#cur;
    let tokt = this.#curt;
    if (tokt === Token.bang()) {
      this.consume();
      return UnaryExpr.make(sys.ObjUtil.coerce(loc, Loc.type$), tokt.toExprId(), sys.ObjUtil.coerce(tokt, Token.type$), this.parenExpr());
    }
    ;
    if (tokt === Token.plus()) {
      this.consume();
      return this.parenExpr();
    }
    ;
    if (tokt === Token.minus()) {
      this.consume();
      return ShortcutExpr.makeUnary(sys.ObjUtil.coerce(loc, Loc.type$), sys.ObjUtil.coerce(tokt, Token.type$), this.parenExpr());
    }
    ;
    if (tokt.isIncrementOrDecrement()) {
      this.consume();
      return ShortcutExpr.makeUnary(sys.ObjUtil.coerce(loc, Loc.type$), sys.ObjUtil.coerce(tokt, Token.type$), this.parenExpr());
    }
    ;
    let expr = this.termExpr();
    (tokt = this.#curt);
    (tok = this.#cur);
    if ((tokt.isIncrementOrDecrement() && !tok.newline())) {
      this.consume();
      let shortcut = ShortcutExpr.makeUnary(sys.ObjUtil.coerce(loc, Loc.type$), sys.ObjUtil.coerce(tokt, Token.type$), expr);
      shortcut.isPostfixLeave(true);
      return shortcut;
    }
    ;
    return expr;
  }

  termExpr(target) {
    if (target === undefined) target = null;
    if (target == null) {
      (target = this.termBaseExpr());
    }
    ;
    while (true) {
      let chained = this.termChainExpr(sys.ObjUtil.coerce(target, Expr.type$));
      if (chained == null) {
        break;
      }
      ;
      (target = chained);
    }
    ;
    return sys.ObjUtil.coerce(target, Expr.type$);
  }

  termBaseExpr() {
    let loc = this.#cur;
    let ctype = this.tryType();
    if (ctype != null) {
      return this.typeBaseExpr(sys.ObjUtil.coerce(loc, Loc.type$), sys.ObjUtil.coerce(ctype, CType.type$));
    }
    ;
    let $_u135 = this.#curt;
    if (sys.ObjUtil.equals($_u135, Token.amp())) {
      return this.idExpr(null, false, false);
    }
    else if (sys.ObjUtil.equals($_u135, Token.identifier())) {
      return this.idExpr(null, false, false);
    }
    else if (sys.ObjUtil.equals($_u135, Token.intLiteral())) {
      return LiteralExpr.make(sys.ObjUtil.coerce(loc, Loc.type$), ExprId.intLiteral(), sys.ObjUtil.coerce(this.ns().intType(), CType.type$), this.consume().val());
    }
    else if (sys.ObjUtil.equals($_u135, Token.floatLiteral())) {
      return LiteralExpr.make(sys.ObjUtil.coerce(loc, Loc.type$), ExprId.floatLiteral(), sys.ObjUtil.coerce(this.ns().floatType(), CType.type$), this.consume().val());
    }
    else if (sys.ObjUtil.equals($_u135, Token.decimalLiteral())) {
      return LiteralExpr.make(sys.ObjUtil.coerce(loc, Loc.type$), ExprId.decimalLiteral(), sys.ObjUtil.coerce(this.ns().decimalType(), CType.type$), this.consume().val());
    }
    else if (sys.ObjUtil.equals($_u135, Token.strLiteral())) {
      return LiteralExpr.make(sys.ObjUtil.coerce(loc, Loc.type$), ExprId.strLiteral(), sys.ObjUtil.coerce(this.ns().strType(), CType.type$), this.consume().val());
    }
    else if (sys.ObjUtil.equals($_u135, Token.durationLiteral())) {
      return LiteralExpr.make(sys.ObjUtil.coerce(loc, Loc.type$), ExprId.durationLiteral(), sys.ObjUtil.coerce(this.ns().durationType(), CType.type$), this.consume().val());
    }
    else if (sys.ObjUtil.equals($_u135, Token.uriLiteral())) {
      return LiteralExpr.make(sys.ObjUtil.coerce(loc, Loc.type$), ExprId.uriLiteral(), sys.ObjUtil.coerce(this.ns().uriType(), CType.type$), this.consume().val());
    }
    else if (sys.ObjUtil.equals($_u135, Token.localeLiteral())) {
      return LocaleLiteralExpr.make(sys.ObjUtil.coerce(loc, Loc.type$), sys.ObjUtil.coerce(this.consume().val(), sys.Str.type$));
    }
    else if (sys.ObjUtil.equals($_u135, Token.lbracket())) {
      return this.collectionLiteralExpr(sys.ObjUtil.coerce(loc, Loc.type$), null);
    }
    else if (sys.ObjUtil.equals($_u135, Token.falseKeyword())) {
      this.consume();
      return LiteralExpr.makeFalse(sys.ObjUtil.coerce(loc, Loc.type$), this.ns());
    }
    else if (sys.ObjUtil.equals($_u135, Token.nullKeyword())) {
      this.consume();
      return LiteralExpr.makeNull(sys.ObjUtil.coerce(loc, Loc.type$), this.ns());
    }
    else if (sys.ObjUtil.equals($_u135, Token.superKeyword())) {
      this.consume();
      if (this.#curt !== Token.dot()) {
        this.err("Expected '.' dot after 'super' keyword");
      }
      ;
      return SuperExpr.make(sys.ObjUtil.coerce(loc, Loc.type$));
    }
    else if (sys.ObjUtil.equals($_u135, Token.thisKeyword())) {
      this.consume();
      return ThisExpr.make(sys.ObjUtil.coerce(loc, Loc.type$));
    }
    else if (sys.ObjUtil.equals($_u135, Token.itKeyword())) {
      this.consume();
      return ItExpr.make(sys.ObjUtil.coerce(loc, Loc.type$));
    }
    else if (sys.ObjUtil.equals($_u135, Token.trueKeyword())) {
      this.consume();
      return LiteralExpr.makeTrue(sys.ObjUtil.coerce(loc, Loc.type$), this.ns());
    }
    else if (sys.ObjUtil.equals($_u135, Token.pound())) {
      this.consume();
      return SlotLiteralExpr.make(sys.ObjUtil.coerce(loc, Loc.type$), sys.ObjUtil.coerce(this.#curType, CType.type$), this.consumeId());
    }
    ;
    if (sys.ObjUtil.equals(this.#curt, Token.pipe())) {
      throw this.err("Invalid closure expression (check types)");
    }
    else {
      throw this.err(sys.Str.plus(sys.Str.plus("Expected expression, not '", this.#cur), "'"));
    }
    ;
  }

  typeBaseExpr(loc,ctype) {
    const this$ = this;
    if (this.#curt === Token.pound()) {
      this.consume();
      if ((this.#curt === Token.identifier() && !this.#cur.newline())) {
        return SlotLiteralExpr.make(loc, ctype, this.consumeId());
      }
      else {
        return LiteralExpr.make(loc, ExprId.typeLiteral(), sys.ObjUtil.coerce(this.ns().typeType(), CType.type$), ctype);
      }
      ;
    }
    ;
    if (sys.ObjUtil.equals(this.#curt, Token.dot())) {
      this.consume();
      if (this.#curt === Token.superKeyword()) {
        this.consume();
        if (this.#curt !== Token.dot()) {
          this.err("Expected '.' dot after 'super' keyword");
        }
        ;
        return SuperExpr.make(loc, ctype);
      }
      else {
        return this.idExpr(StaticTargetExpr.make(loc, ctype), false, false);
      }
      ;
    }
    ;
    if (sys.ObjUtil.equals(this.#curt, Token.dsl())) {
      let srcLoc = Loc.make(this.#cur.file(), this.#cur.line(), sys.ObjUtil.coerce(sys.Int.plus(sys.ObjUtil.coerce(this.#cur.col(), sys.Int.type$), 2), sys.Int.type$.toNullable()));
      let dslVal = sys.ObjUtil.as(this.#cur, TokenValDsl.type$);
      return sys.ObjUtil.coerce(sys.ObjUtil.with(DslExpr.make(loc, ctype, srcLoc, sys.ObjUtil.coerce(this.consume().val(), sys.Str.type$)), (it) => {
        it.leadingTabs(dslVal.leadingTabs());
        it.leadingSpaces(dslVal.leadingSpaces());
        return;
      }), DslExpr.type$);
    }
    ;
    if (this.#curt === Token.lbracket()) {
      return this.collectionLiteralExpr(loc, ctype);
    }
    ;
    if ((sys.ObjUtil.equals(this.#curt, Token.lbrace()) && sys.ObjUtil.is(ctype, FuncType.type$))) {
      return this.closure(loc, sys.ObjUtil.coerce(ctype, FuncType.type$));
    }
    ;
    if (sys.ObjUtil.equals(this.#curt, Token.lparen())) {
      let construction = CallExpr.make(loc, StaticTargetExpr.make(loc, ctype), "<ctor>", ExprId.construction());
      this.callArgs(construction);
      return construction;
    }
    ;
    if (sys.ObjUtil.equals(this.#curt, Token.lbrace())) {
      if (this.#curSlot == null) {
        return this.complexLiteral(loc, ctype);
      }
      ;
      let ctor = CallExpr.make(loc, StaticTargetExpr.make(loc, ctype), "make");
      let itBlock = this.tryItBlock();
      if (itBlock != null) {
        ctor.args().add(sys.ObjUtil.coerce(itBlock, Expr.type$));
      }
      ;
      return ctor;
    }
    ;
    throw this.err("Unexpected type literal", loc);
  }

  termChainExpr(target) {
    let loc = this.#cur;
    let $_u136 = this.#curt;
    if (sys.ObjUtil.equals($_u136, Token.dot())) {
      this.consume();
      return this.idExpr(target, false, false);
    }
    else if (sys.ObjUtil.equals($_u136, Token.arrow())) {
      this.consume();
      return this.idExpr(target, true, false);
    }
    else if (sys.ObjUtil.equals($_u136, Token.safeDot())) {
      this.consume();
      return this.idExpr(target, false, true);
    }
    else if (sys.ObjUtil.equals($_u136, Token.safeArrow())) {
      this.consume();
      return this.idExpr(target, true, true);
    }
    ;
    if (this.#cur.isIndexOpenBracket()) {
      return this.indexExpr(target);
    }
    ;
    if (this.#cur.isCallOpenParen()) {
      return this.callOp(target);
    }
    ;
    if (this.#curt === Token.lbrace()) {
      let itBlock = this.tryItBlock();
      if (itBlock != null) {
        return itBlock.toWith(target);
      }
      ;
    }
    ;
    return null;
  }

  idExpr(target,dynamicCall,safeCall) {
    const this$ = this;
    let loc = this.#cur;
    if (sys.ObjUtil.equals(this.#curt, Token.amp())) {
      this.consume();
      let var$ = UnknownVarExpr.make(sys.ObjUtil.coerce(loc, Loc.type$), target, this.consumeId(), ExprId.storage());
      var$.isSafe(safeCall);
      return var$;
    }
    ;
    if (this.#peek.isCallOpenParen()) {
      let call = this.callExpr(target);
      call.isDynamic(dynamicCall);
      call.isSafe(safeCall);
      return call;
    }
    ;
    let name = this.consumeId();
    let closure = this.tryClosure();
    if (closure != null) {
      let call = CallExpr.make(sys.ObjUtil.coerce(loc, Loc.type$));
      call.target(target);
      call.name(name);
      call.isDynamic(dynamicCall);
      call.isSafe(safeCall);
      call.noParens(true);
      call.args().add(sys.ObjUtil.coerce(closure, Expr.type$));
      return call;
    }
    ;
    if (dynamicCall) {
      let call = CallExpr.make(sys.ObjUtil.coerce(loc, Loc.type$));
      call.target(target);
      call.name(name);
      call.isDynamic(true);
      call.isSafe(safeCall);
      call.noParens(true);
      return call;
    }
    ;
    if (this.#curt === Token.pound()) {
      throw this.err(sys.Str.plus(sys.Str.plus("Unknown type '", name), "' for type literal"), loc);
    }
    ;
    return sys.ObjUtil.coerce(sys.ObjUtil.with(UnknownVarExpr.make(sys.ObjUtil.coerce(loc, Loc.type$), target, name), (it) => {
      it.isSafe(safeCall);
      return;
    }), UnknownVarExpr.type$);
  }

  callExpr(target) {
    let call = CallExpr.make(sys.ObjUtil.coerce(this.#cur, Loc.type$));
    call.target(target);
    call.name(this.consumeId());
    this.callArgs(call);
    return call;
  }

  callArgs(call,closureOk) {
    if (closureOk === undefined) closureOk = true;
    this.consume(Token.lparen());
    if (sys.ObjUtil.compareNE(this.#curt, Token.rparen())) {
      while (true) {
        call.args().add(this.expr());
        if (this.#curt === Token.rparen()) {
          break;
        }
        ;
        this.consume(Token.comma());
      }
      ;
    }
    ;
    this.consume(Token.rparen());
    if (closureOk) {
      let closure = this.tryClosure();
      if (closure != null) {
        call.args().add(sys.ObjUtil.coerce(closure, Expr.type$));
      }
      ;
    }
    ;
    return;
  }

  callOp(target) {
    let loc = this.#cur;
    let call = CallExpr.make(sys.ObjUtil.coerce(loc, Loc.type$));
    call.isCallOp(true);
    call.target(target);
    this.callArgs(call);
    call.name("call");
    return call;
  }

  indexExpr(target) {
    let loc = this.#cur;
    this.consume(Token.lbracket());
    if ((this.#curt === Token.comma() && target.id() === ExprId.unknownVar())) {
      throw this.err(sys.Str.plus(sys.Str.plus("Unknown type '", target), "' for list literal"), target.loc());
    }
    ;
    let expr = this.expr();
    this.consume(Token.rbracket());
    return ShortcutExpr.makeGet(sys.ObjUtil.coerce(loc, Loc.type$), target, expr);
  }

  collectionLiteralExpr(loc,explicitType) {
    if (this.#peekt === Token.comma()) {
      return this.listLiteralExpr(loc, explicitType, null);
    }
    ;
    if (this.#peekt === Token.colon()) {
      return this.mapLiteralExpr(loc, explicitType, null);
    }
    ;
    this.consume(Token.lbracket());
    if (this.#curt === Token.rbracket()) {
      this.err("Invalid list literal; use '[,]' for empty Obj[] list", loc);
      this.consume();
      return ListLiteralExpr.make(loc);
    }
    ;
    let first = this.expr();
    if (this.#curt === Token.colon()) {
      return this.mapLiteralExpr(loc, explicitType, first);
    }
    else {
      return this.listLiteralExpr(loc, explicitType, first);
    }
    ;
  }

  listLiteralExpr(loc,explicitType,first) {
    if (explicitType != null) {
      (explicitType = explicitType.toListOf());
    }
    ;
    let list = ListLiteralExpr.make(loc, sys.ObjUtil.coerce(explicitType, ListType.type$.toNullable()));
    if (first == null) {
      this.consume(Token.lbracket());
      if (this.#curt === Token.comma()) {
        this.consume();
        this.consume(Token.rbracket());
        return list;
      }
      ;
      (first = this.expr());
    }
    ;
    list.vals().add(sys.ObjUtil.coerce(first, Expr.type$));
    while (this.#curt === Token.comma()) {
      this.consume();
      if (this.#curt === Token.rbracket()) {
        break;
      }
      ;
      list.vals().add(this.expr());
    }
    ;
    this.consume(Token.rbracket());
    return list;
  }

  mapLiteralExpr(loc,explicitType,first) {
    if ((explicitType != null && !sys.ObjUtil.is(explicitType, MapType.type$))) {
      this.err(sys.Str.plus(sys.Str.plus("Invalid map type '", explicitType), "' for map literal"), loc);
      (explicitType = null);
    }
    ;
    let map = MapLiteralExpr.make(loc, sys.ObjUtil.coerce(explicitType, MapType.type$.toNullable()));
    if (first == null) {
      this.consume(Token.lbracket());
      if (this.#curt === Token.colon()) {
        this.consume();
        this.consume(Token.rbracket());
        return map;
      }
      ;
      (first = this.expr());
    }
    ;
    map.keys().add(sys.ObjUtil.coerce(first, Expr.type$));
    this.consume(Token.colon());
    map.vals().add(this.expr());
    while (this.#curt === Token.comma()) {
      this.consume();
      if (this.#curt === Token.rbracket()) {
        break;
      }
      ;
      map.keys().add(this.expr());
      this.consume(Token.colon());
      map.vals().add(this.expr());
    }
    ;
    this.consume(Token.rbracket());
    return map;
  }

  tryClosure() {
    let loc = this.#cur;
    if (this.#curt === Token.lbrace()) {
      return this.tryItBlock();
    }
    ;
    if (this.#curt !== Token.pipe()) {
      return null;
    }
    ;
    let mark = this.#pos;
    let funcType = sys.ObjUtil.as(this.tryType(), FuncType.type$);
    if (funcType == null) {
      this.reset(mark);
      return null;
    }
    ;
    if (this.#curt !== Token.lbrace()) {
      this.reset(mark);
      return null;
    }
    ;
    return this.closure(sys.ObjUtil.coerce(loc, Loc.type$), sys.ObjUtil.coerce(funcType, FuncType.type$));
  }

  tryItBlock() {
    if (this.#inFieldInit) {
      if (this.#peek.kind().isProtectionKeyword()) {
        return null;
      }
      ;
      if (this.#peek.kind() === Token.staticKeyword()) {
        return null;
      }
      ;
      if (this.#peek.kind() === Token.readonlyKeyword()) {
        return null;
      }
      ;
      if (sys.ObjUtil.equals(this.#peekt, Token.identifier())) {
        if ((sys.ObjUtil.equals(this.#peek.val(), "get") || sys.ObjUtil.equals(this.#peek.val(), "set"))) {
          return null;
        }
        ;
      }
      ;
    }
    ;
    let ib = this.closure(sys.ObjUtil.coerce(this.#cur, Loc.type$), sys.ObjUtil.coerce(this.ns().itBlockType(), FuncType.type$));
    ib.isItBlock(true);
    ib.itType(this.ns().error());
    return ib;
  }

  closure(loc,funcType) {
    if ((this.#curType == null || this.#curSlot == null)) {
      throw this.err("Unexpected closure");
    }
    ;
    let name = sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#curType.name()), "\$"), this.#curSlot.name()), "\$"), sys.ObjUtil.coerce(((this$) => { let $_u137 = this$.#closureCount;this$.#closureCount = sys.Int.increment(sys.ObjUtil.coerce(this$.#closureCount, sys.Int.type$)); return $_u137; })(this), sys.Obj.type$.toNullable()));
    if (funcType.unnamed()) {
      this.err("Closure parameters must be named", loc);
    }
    ;
    let closure = ClosureExpr.make(loc, sys.ObjUtil.coerce(this.#curType, TypeDef.type$), sys.ObjUtil.coerce(this.#curSlot, SlotDef.type$), this.#curClosure, funcType, name);
    this.#closures.add(closure);
    this.#curType.closures().add(closure);
    let oldClosure = this.#curClosure;
    this.#curClosure = closure;
    closure.code(this.block());
    this.#curClosure = oldClosure;
    return closure;
  }

  complexLiteral(loc,ctype) {
    let complex = ComplexLiteral.make(loc, ctype);
    this.consume(Token.lbrace());
    while (this.#curt !== Token.rbrace()) {
      complex.names().add(this.consumeId());
      this.consume(Token.assign());
      complex.vals().add(this.expr());
      this.endOfStmt();
    }
    ;
    this.consume(Token.rbrace());
    return complex;
  }

  typeRef() {
    let loc = sys.ObjUtil.coerce(this.#cur, Loc.type$);
    return TypeRef.make(loc, this.ctype(true));
  }

  tryType() {
    if ((this.#curt !== Token.identifier() && this.#curt !== Token.pipe() && this.#curt !== Token.lbracket())) {
      return null;
    }
    ;
    let oldSuppress = this.suppressErr();
    this.suppressErr(true);
    let mark = this.#pos;
    let type = null;
    try {
      (type = this.ctype());
    }
    catch ($_u138) {
      $_u138 = sys.Err.make($_u138);
      if ($_u138 instanceof SuppressedErr) {
        let e = $_u138;
        ;
      }
      else {
        throw $_u138;
      }
    }
    ;
    this.suppressErr(oldSuppress);
    if (type == null) {
      this.reset(mark);
    }
    ;
    return type;
  }

  ctype(isTypeRef) {
    if (isTypeRef === undefined) isTypeRef = false;
    let t = null;
    if (this.#curt === Token.identifier()) {
      (t = this.simpleType());
    }
    else {
      if (this.#curt === Token.lbracket()) {
        let loc = this.consume(Token.lbracket());
        (t = this.ctype());
        this.consume(Token.rbracket());
        if (!sys.ObjUtil.is(t, MapType.type$)) {
          this.err("Invalid map type", loc);
        }
        ;
      }
      else {
        if (this.#curt === Token.pipe()) {
          (t = this.funcType(isTypeRef));
        }
        else {
          throw this.err("Expecting type name");
        }
        ;
      }
      ;
    }
    ;
    if ((this.#curt === Token.question() && !this.#cur.whitespace())) {
      this.consume(Token.question());
      (t = t.toNullable());
      if ((this.#curt === Token.question() && !this.#cur.whitespace())) {
        throw this.err("Type cannot have multiple '?'");
      }
      ;
    }
    ;
    while ((this.#curt === Token.lbracket() && this.#peekt === Token.rbracket())) {
      this.consume(Token.lbracket());
      this.consume(Token.rbracket());
      (t = t.toListOf());
      if ((this.#curt === Token.question() && !this.#cur.whitespace())) {
        this.consume(Token.question());
        (t = t.toNullable());
      }
      ;
    }
    ;
    if ((this.#curt === Token.elvis() && !this.#cur.whitespace())) {
      throw this.err("Map type cannot have nullable key type");
    }
    ;
    if (this.#curt === Token.colon()) {
      if (t.isNullable()) {
        throw this.err("Map type cannot have nullable key type");
      }
      ;
      this.consume(Token.colon());
      let key = t;
      let val = this.ctype();
      (t = MapType.make(sys.ObjUtil.coerce(key, CType.type$), val));
    }
    ;
    if ((this.#curt === Token.question() && !this.#cur.whitespace())) {
      this.consume(Token.question());
      (t = t.toNullable());
    }
    ;
    return sys.ObjUtil.coerce(t, CType.type$);
  }

  simpleType() {
    const this$ = this;
    let loc = this.#cur;
    let id = this.consumeId();
    if (this.#curt === Token.doubleColon()) {
      this.consume();
      return sys.ObjUtil.coerce(((this$) => { let $_u139 = ResolveImports.resolveQualified(this$, id, this$.consumeId(), sys.ObjUtil.coerce(loc, Loc.type$)); if ($_u139 != null) return $_u139; return this$.ns().voidType(); })(this), CType.type$);
    }
    ;
    let types = this.#unit.importedTypes().get(id);
    if ((types == null || types.isEmpty())) {
      if ((this.compiler().isSys() && sys.ObjUtil.equals(sys.Str.size(id), 1))) {
        return this.ns().genericParameter(id);
      }
      ;
      this.err(sys.Str.plus(sys.Str.plus("Unknown type '", id), "'"), loc);
      return sys.ObjUtil.coerce(this.ns().voidType(), CType.type$);
    }
    ;
    if (sys.ObjUtil.compareGT(types.size(), 1)) {
      let publicTypes = types.exclude((t) => {
        return (t.isInternal() && sys.ObjUtil.compareNE(t.pod().name(), this$.compiler().pod().name()));
      });
      if (!publicTypes.isEmpty()) {
        (types = publicTypes);
      }
      ;
    }
    ;
    if (sys.ObjUtil.compareGT(types.size(), 1)) {
      this.errReport(CompilerErr.make(sys.Str.plus("Ambiguous type: ", types.join(", ")), loc));
    }
    ;
    return sys.ObjUtil.coerce(types.first(), CType.type$);
  }

  funcType(isTypeRef) {
    let params = sys.List.make(CType.type$);
    let names = sys.List.make(sys.Str.type$);
    let ret = this.ns().voidType();
    this.consume(Token.pipe());
    let inferred = false;
    let unnamed = sys.List.make(sys.Bool.type$, [sys.ObjUtil.coerce(false, sys.Bool.type$.toNullable())]);
    if (this.#curt !== Token.arrow()) {
      (inferred = this.funcTypeFormal(isTypeRef, params, names, unnamed));
    }
    ;
    while (this.#curt === Token.comma()) {
      this.consume();
      (inferred = sys.Bool.or(inferred, this.funcTypeFormal(isTypeRef, params, names, unnamed)));
    }
    ;
    if ((this.#curt === Token.safeArrow() && !params.isEmpty())) {
      params.set(-1, params.get(-1).toNullable());
      this.consume();
      (ret = this.ctype());
    }
    ;
    if (this.#curt === Token.arrow()) {
      this.consume();
      if ((this.#curt !== Token.pipe() || this.#cur.whitespace())) {
        (ret = this.ctype());
      }
      else {
        if (!params.isEmpty()) {
          this.errReport(CompilerErr.make("Expecting function return type", this.#cur));
        }
        ;
      }
      ;
    }
    ;
    this.consume(Token.pipe());
    let ft = FuncType.make(params, names, sys.ObjUtil.coerce(ret, CType.type$));
    ft.inferredSignature(inferred);
    ft.unnamed(sys.ObjUtil.coerce(unnamed.first(), sys.Bool.type$));
    return ft;
  }

  funcTypeFormal(isTypeRef,params,names,unnamed) {
    let t = ((this$) => { if (isTypeRef) return this$.ctype(true); return this$.tryType(); })(this);
    if (t != null) {
      params.add(sys.ObjUtil.coerce(t, CType.type$));
      if (this.#curt === Token.identifier()) {
        names.add(this.consumeId());
      }
      else {
        names.add(sys.Str.plus("_", sys.Int.toChar(sys.Int.plus(97, names.size()))));
        unnamed.set(0, sys.ObjUtil.coerce(true, sys.Bool.type$.toNullable()));
      }
      ;
      return false;
    }
    else {
      params.add(this.ns().objType().toNullable());
      names.add(this.consumeId());
      return true;
    }
    ;
  }

  doc() {
    let doc = null;
    while (this.#curt === Token.docComment()) {
      let loc = this.#cur;
      let lines = sys.ObjUtil.coerce(this.consume(Token.docComment()).val(), sys.Type.find("sys::Str[]"));
      (doc = DocDef.make(sys.ObjUtil.coerce(loc, Loc.type$), lines));
    }
    ;
    return doc;
  }

  err(msg,loc) {
    if (loc === undefined) loc = null;
    if (loc == null) {
      (loc = this.#cur);
    }
    ;
    return CompilerSupport.prototype.err.call(this, msg, loc);
  }

  consumeId() {
    if (this.#curt !== Token.identifier()) {
      throw this.err(sys.Str.plus(sys.Str.plus("Expected identifier, not '", this.#cur), "'"));
    }
    ;
    return sys.ObjUtil.coerce(this.consume().val(), sys.Str.type$);
  }

  verify(kind) {
    if (this.#curt !== kind) {
      throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Expected '", kind.symbol()), "', not '"), this.#cur), "'"));
    }
    ;
    return;
  }

  consume(kind) {
    if (kind === undefined) kind = null;
    if (kind != null) {
      this.verify(sys.ObjUtil.coerce(kind, Token.type$));
    }
    ;
    let result = this.#cur;
    let next = null;
    ((this$) => { let $_u141 = this$.#pos;this$.#pos = sys.Int.increment(this$.#pos); return $_u141; })(this);
    if (sys.ObjUtil.compareLT(sys.Int.plus(this.#pos, 1), this.#numTokens)) {
      (next = this.#tokens.get(sys.Int.plus(this.#pos, 1)));
    }
    else {
      (next = this.#tokens.get(sys.Int.minus(this.#numTokens, 1)));
    }
    ;
    this.#cur = this.#peek;
    this.#peek = next;
    this.#curt = this.#cur.kind();
    this.#peekt = this.#peek.kind();
    return sys.ObjUtil.coerce(result, TokenVal.type$);
  }

  endOfStmt(errMsg) {
    if (errMsg === undefined) errMsg = sys.Str.plus(sys.Str.plus("Expected end of statement: semicolon, newline, or end of block; not '", this.#cur), "'");
    if (this.#cur.newline()) {
      return true;
    }
    ;
    if (this.#curt === Token.semicolon()) {
      this.consume();
      return true;
    }
    ;
    if (this.#curt === Token.rbrace()) {
      return true;
    }
    ;
    if (this.#curt === Token.eof()) {
      return true;
    }
    ;
    if (errMsg == null) {
      return false;
    }
    ;
    throw this.err(sys.ObjUtil.coerce(errMsg, sys.Str.type$));
  }

  reset(pos) {
    this.#pos = pos;
    this.#cur = this.#tokens.get(pos);
    if (sys.ObjUtil.compareLT(sys.Int.plus(pos, 1), this.#numTokens)) {
      this.#peek = this.#tokens.get(sys.Int.plus(pos, 1));
    }
    else {
      this.#peek = this.#tokens.get(pos);
    }
    ;
    this.#curt = this.#cur.kind();
    this.#peekt = this.#peek.kind();
    return;
  }

  static static$init() {
    Parser.#ProtectionMask = sys.Int.not(sys.Int.or(sys.Int.or(sys.Int.or(FConst.Public(), FConst.Protected()), FConst.Private()), FConst.Internal()));
    return;
  }

}

class Token extends sys.Enum {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Token.type$; }

  static identifier() { return Token.vals().get(0); }

  static strLiteral() { return Token.vals().get(1); }

  static intLiteral() { return Token.vals().get(2); }

  static floatLiteral() { return Token.vals().get(3); }

  static decimalLiteral() { return Token.vals().get(4); }

  static durationLiteral() { return Token.vals().get(5); }

  static uriLiteral() { return Token.vals().get(6); }

  static dsl() { return Token.vals().get(7); }

  static localeLiteral() { return Token.vals().get(8); }

  static dot() { return Token.vals().get(9); }

  static semicolon() { return Token.vals().get(10); }

  static comma() { return Token.vals().get(11); }

  static colon() { return Token.vals().get(12); }

  static doubleColon() { return Token.vals().get(13); }

  static plus() { return Token.vals().get(14); }

  static minus() { return Token.vals().get(15); }

  static star() { return Token.vals().get(16); }

  static slash() { return Token.vals().get(17); }

  static percent() { return Token.vals().get(18); }

  static pound() { return Token.vals().get(19); }

  static increment() { return Token.vals().get(20); }

  static decrement() { return Token.vals().get(21); }

  static bang() { return Token.vals().get(22); }

  static question() { return Token.vals().get(23); }

  static tilde() { return Token.vals().get(24); }

  static pipe() { return Token.vals().get(25); }

  static amp() { return Token.vals().get(26); }

  static caret() { return Token.vals().get(27); }

  static at() { return Token.vals().get(28); }

  static doublePipe() { return Token.vals().get(29); }

  static doubleAmp() { return Token.vals().get(30); }

  static same() { return Token.vals().get(31); }

  static notSame() { return Token.vals().get(32); }

  static eq() { return Token.vals().get(33); }

  static notEq() { return Token.vals().get(34); }

  static cmp() { return Token.vals().get(35); }

  static lt() { return Token.vals().get(36); }

  static ltEq() { return Token.vals().get(37); }

  static gt() { return Token.vals().get(38); }

  static gtEq() { return Token.vals().get(39); }

  static lbrace() { return Token.vals().get(40); }

  static rbrace() { return Token.vals().get(41); }

  static lparen() { return Token.vals().get(42); }

  static rparen() { return Token.vals().get(43); }

  static lbracket() { return Token.vals().get(44); }

  static rbracket() { return Token.vals().get(45); }

  static dotDot() { return Token.vals().get(46); }

  static dotDotLt() { return Token.vals().get(47); }

  static defAssign() { return Token.vals().get(48); }

  static assign() { return Token.vals().get(49); }

  static assignPlus() { return Token.vals().get(50); }

  static assignMinus() { return Token.vals().get(51); }

  static assignStar() { return Token.vals().get(52); }

  static assignSlash() { return Token.vals().get(53); }

  static assignPercent() { return Token.vals().get(54); }

  static arrow() { return Token.vals().get(55); }

  static elvis() { return Token.vals().get(56); }

  static safeDot() { return Token.vals().get(57); }

  static safeArrow() { return Token.vals().get(58); }

  static docComment() { return Token.vals().get(59); }

  static dollar() { return Token.vals().get(60); }

  static lparenSynthetic() { return Token.vals().get(61); }

  static abstractKeyword() { return Token.vals().get(62); }

  static asKeyword() { return Token.vals().get(63); }

  static assertKeyword() { return Token.vals().get(64); }

  static breakKeyword() { return Token.vals().get(65); }

  static caseKeyword() { return Token.vals().get(66); }

  static catchKeyword() { return Token.vals().get(67); }

  static classKeyword() { return Token.vals().get(68); }

  static constKeyword() { return Token.vals().get(69); }

  static continueKeyword() { return Token.vals().get(70); }

  static defaultKeyword() { return Token.vals().get(71); }

  static doKeyword() { return Token.vals().get(72); }

  static elseKeyword() { return Token.vals().get(73); }

  static falseKeyword() { return Token.vals().get(74); }

  static finalKeyword() { return Token.vals().get(75); }

  static finallyKeyword() { return Token.vals().get(76); }

  static forKeyword() { return Token.vals().get(77); }

  static foreachKeyword() { return Token.vals().get(78); }

  static ifKeyword() { return Token.vals().get(79); }

  static internalKeyword() { return Token.vals().get(80); }

  static isKeyword() { return Token.vals().get(81); }

  static isnotKeyword() { return Token.vals().get(82); }

  static itKeyword() { return Token.vals().get(83); }

  static mixinKeyword() { return Token.vals().get(84); }

  static nativeKeyword() { return Token.vals().get(85); }

  static newKeyword() { return Token.vals().get(86); }

  static nullKeyword() { return Token.vals().get(87); }

  static onceKeyword() { return Token.vals().get(88); }

  static overrideKeyword() { return Token.vals().get(89); }

  static privateKeyword() { return Token.vals().get(90); }

  static protectedKeyword() { return Token.vals().get(91); }

  static publicKeyword() { return Token.vals().get(92); }

  static readonlyKeyword() { return Token.vals().get(93); }

  static returnKeyword() { return Token.vals().get(94); }

  static staticKeyword() { return Token.vals().get(95); }

  static superKeyword() { return Token.vals().get(96); }

  static switchKeyword() { return Token.vals().get(97); }

  static thisKeyword() { return Token.vals().get(98); }

  static throwKeyword() { return Token.vals().get(99); }

  static trueKeyword() { return Token.vals().get(100); }

  static tryKeyword() { return Token.vals().get(101); }

  static usingKeyword() { return Token.vals().get(102); }

  static virtualKeyword() { return Token.vals().get(103); }

  static volatileKeyword() { return Token.vals().get(104); }

  static voidKeyword() { return Token.vals().get(105); }

  static whileKeyword() { return Token.vals().get(106); }

  static eof() { return Token.vals().get(107); }

  static #vals = undefined;

  static #keywords = undefined;

  static keywords() {
    if (Token.#keywords === undefined) {
      Token.static$init();
      if (Token.#keywords === undefined) Token.#keywords = null;
    }
    return Token.#keywords;
  }

  #symbol = null;

  symbol() { return this.#symbol; }

  __symbol(it) { if (it === undefined) return this.#symbol; else this.#symbol = it; }

  #keyword = false;

  keyword() { return this.#keyword; }

  __keyword(it) { if (it === undefined) return this.#keyword; else this.#keyword = it; }

  #isAssign = false;

  isAssign() { return this.#isAssign; }

  __isAssign(it) { if (it === undefined) return this.#isAssign; else this.#isAssign = it; }

  static make($ordinal,$name,symbol) {
    const $self = new Token();
    Token.make$($self,$ordinal,$name,symbol);
    return $self;
  }

  static make$($self,$ordinal,$name,symbol) {
    if (symbol === undefined) symbol = null;
    sys.Enum.make$($self, $ordinal, $name);
    if (symbol == null) {
      if (!sys.Str.endsWith($self.name(), "Keyword")) {
        throw sys.Err.make($self.name());
      }
      ;
      $self.#symbol = sys.Str.getRange($self.name(), sys.Range.make(0, -8));
      $self.#keyword = true;
      $self.#isAssign = false;
    }
    else {
      $self.#symbol = sys.ObjUtil.coerce(symbol, sys.Str.type$);
      $self.#keyword = false;
      $self.#isAssign = sys.Str.startsWith($self.name(), "assign");
    }
    ;
    return;
  }

  toExprId() {
    let $_u142 = this;
    if (sys.ObjUtil.equals($_u142, Token.bang())) {
      return ExprId.boolNot();
    }
    else if (sys.ObjUtil.equals($_u142, Token.assign())) {
      return ExprId.assign();
    }
    else if (sys.ObjUtil.equals($_u142, Token.doubleAmp())) {
      return ExprId.boolAnd();
    }
    else if (sys.ObjUtil.equals($_u142, Token.doublePipe())) {
      return ExprId.boolOr();
    }
    else if (sys.ObjUtil.equals($_u142, Token.same())) {
      return ExprId.same();
    }
    else if (sys.ObjUtil.equals($_u142, Token.notSame())) {
      return ExprId.notSame();
    }
    else if (sys.ObjUtil.equals($_u142, Token.elvis())) {
      return ExprId.elvis();
    }
    else {
      throw sys.Err.make(this.toStr());
    }
    ;
  }

  toShortcutOp(degree) {
    let $_u143 = this;
    if (sys.ObjUtil.equals($_u143, Token.plus())) {
      return ShortcutOp.plus();
    }
    else if (sys.ObjUtil.equals($_u143, Token.minus())) {
      return ((this$) => { if (sys.ObjUtil.equals(degree, 1)) return ShortcutOp.negate(); return ShortcutOp.minus(); })(this);
    }
    else if (sys.ObjUtil.equals($_u143, Token.star())) {
      return ShortcutOp.mult();
    }
    else if (sys.ObjUtil.equals($_u143, Token.slash())) {
      return ShortcutOp.div();
    }
    else if (sys.ObjUtil.equals($_u143, Token.percent())) {
      return ShortcutOp.mod();
    }
    else if (sys.ObjUtil.equals($_u143, Token.increment())) {
      return ShortcutOp.increment();
    }
    else if (sys.ObjUtil.equals($_u143, Token.decrement())) {
      return ShortcutOp.decrement();
    }
    else if (sys.ObjUtil.equals($_u143, Token.eq())) {
      return ShortcutOp.eq();
    }
    else if (sys.ObjUtil.equals($_u143, Token.notEq())) {
      return ShortcutOp.eq();
    }
    else if (sys.ObjUtil.equals($_u143, Token.cmp())) {
      return ShortcutOp.cmp();
    }
    else if (sys.ObjUtil.equals($_u143, Token.gt())) {
      return ShortcutOp.cmp();
    }
    else if (sys.ObjUtil.equals($_u143, Token.gtEq())) {
      return ShortcutOp.cmp();
    }
    else if (sys.ObjUtil.equals($_u143, Token.lt())) {
      return ShortcutOp.cmp();
    }
    else if (sys.ObjUtil.equals($_u143, Token.ltEq())) {
      return ShortcutOp.cmp();
    }
    else if (sys.ObjUtil.equals($_u143, Token.assignPlus())) {
      return ShortcutOp.plus();
    }
    else if (sys.ObjUtil.equals($_u143, Token.assignMinus())) {
      return ShortcutOp.minus();
    }
    else if (sys.ObjUtil.equals($_u143, Token.assignStar())) {
      return ShortcutOp.mult();
    }
    else if (sys.ObjUtil.equals($_u143, Token.assignSlash())) {
      return ShortcutOp.div();
    }
    else if (sys.ObjUtil.equals($_u143, Token.assignPercent())) {
      return ShortcutOp.mod();
    }
    else {
      throw sys.Err.make(this.toStr());
    }
    ;
  }

  isProtectionKeyword() {
    return (this === Token.publicKeyword() || this === Token.protectedKeyword() || this === Token.internalKeyword() || this === Token.privateKeyword());
  }

  isIncrementOrDecrement() {
    return (this === Token.increment() || this === Token.decrement());
  }

  toStr() {
    return this.#symbol;
  }

  static main() {
    const this$ = this;
    Token.vals().each((t) => {
      sys.ObjUtil.echo(sys.Str.plus(sys.Str.plus(sys.Str.plus(t.name(), "  '"), t.#symbol), "'"));
      return;
    });
    sys.ObjUtil.echo(Token.keywords());
    return;
  }

  static fromStr(name$, checked=true) {
    return sys.Enum.doFromStr(Token.type$, Token.vals(), name$, checked);
  }

  static vals() {
    if (Token.#vals == null) {
      Token.#vals = sys.List.make(Token.type$, [
        Token.make(0, "identifier", "identifier"),
        Token.make(1, "strLiteral", "Str literal"),
        Token.make(2, "intLiteral", "Int literal"),
        Token.make(3, "floatLiteral", "Float literal"),
        Token.make(4, "decimalLiteral", "Decimal literal"),
        Token.make(5, "durationLiteral", "Duration literal"),
        Token.make(6, "uriLiteral", "Uri literal"),
        Token.make(7, "dsl", "DSL"),
        Token.make(8, "localeLiteral", "Locale literal"),
        Token.make(9, "dot", "."),
        Token.make(10, "semicolon", ";"),
        Token.make(11, "comma", ","),
        Token.make(12, "colon", ":"),
        Token.make(13, "doubleColon", "::"),
        Token.make(14, "plus", "+"),
        Token.make(15, "minus", "-"),
        Token.make(16, "star", "*"),
        Token.make(17, "slash", "/"),
        Token.make(18, "percent", "%"),
        Token.make(19, "pound", "#"),
        Token.make(20, "increment", "++"),
        Token.make(21, "decrement", "--"),
        Token.make(22, "bang", "!"),
        Token.make(23, "question", "?"),
        Token.make(24, "tilde", "~"),
        Token.make(25, "pipe", "|"),
        Token.make(26, "amp", "&"),
        Token.make(27, "caret", "^"),
        Token.make(28, "at", "@"),
        Token.make(29, "doublePipe", "||"),
        Token.make(30, "doubleAmp", "&&"),
        Token.make(31, "same", "==="),
        Token.make(32, "notSame", "!=="),
        Token.make(33, "eq", "=="),
        Token.make(34, "notEq", "!="),
        Token.make(35, "cmp", "<=>"),
        Token.make(36, "lt", "<"),
        Token.make(37, "ltEq", "<="),
        Token.make(38, "gt", ">"),
        Token.make(39, "gtEq", ">="),
        Token.make(40, "lbrace", "{"),
        Token.make(41, "rbrace", "}"),
        Token.make(42, "lparen", "("),
        Token.make(43, "rparen", ")"),
        Token.make(44, "lbracket", "["),
        Token.make(45, "rbracket", "]"),
        Token.make(46, "dotDot", ".."),
        Token.make(47, "dotDotLt", "..<"),
        Token.make(48, "defAssign", ":="),
        Token.make(49, "assign", "="),
        Token.make(50, "assignPlus", "+="),
        Token.make(51, "assignMinus", "-="),
        Token.make(52, "assignStar", "*="),
        Token.make(53, "assignSlash", "/="),
        Token.make(54, "assignPercent", "%="),
        Token.make(55, "arrow", "->"),
        Token.make(56, "elvis", "?:"),
        Token.make(57, "safeDot", "?."),
        Token.make(58, "safeArrow", "?->"),
        Token.make(59, "docComment", "**"),
        Token.make(60, "dollar", "\$"),
        Token.make(61, "lparenSynthetic", "("),
        Token.make(62, "abstractKeyword", ),
        Token.make(63, "asKeyword", ),
        Token.make(64, "assertKeyword", ),
        Token.make(65, "breakKeyword", ),
        Token.make(66, "caseKeyword", ),
        Token.make(67, "catchKeyword", ),
        Token.make(68, "classKeyword", ),
        Token.make(69, "constKeyword", ),
        Token.make(70, "continueKeyword", ),
        Token.make(71, "defaultKeyword", ),
        Token.make(72, "doKeyword", ),
        Token.make(73, "elseKeyword", ),
        Token.make(74, "falseKeyword", ),
        Token.make(75, "finalKeyword", ),
        Token.make(76, "finallyKeyword", ),
        Token.make(77, "forKeyword", ),
        Token.make(78, "foreachKeyword", ),
        Token.make(79, "ifKeyword", ),
        Token.make(80, "internalKeyword", ),
        Token.make(81, "isKeyword", ),
        Token.make(82, "isnotKeyword", ),
        Token.make(83, "itKeyword", ),
        Token.make(84, "mixinKeyword", ),
        Token.make(85, "nativeKeyword", ),
        Token.make(86, "newKeyword", ),
        Token.make(87, "nullKeyword", ),
        Token.make(88, "onceKeyword", ),
        Token.make(89, "overrideKeyword", ),
        Token.make(90, "privateKeyword", ),
        Token.make(91, "protectedKeyword", ),
        Token.make(92, "publicKeyword", ),
        Token.make(93, "readonlyKeyword", ),
        Token.make(94, "returnKeyword", ),
        Token.make(95, "staticKeyword", ),
        Token.make(96, "superKeyword", ),
        Token.make(97, "switchKeyword", ),
        Token.make(98, "thisKeyword", ),
        Token.make(99, "throwKeyword", ),
        Token.make(100, "trueKeyword", ),
        Token.make(101, "tryKeyword", ),
        Token.make(102, "usingKeyword", ),
        Token.make(103, "virtualKeyword", ),
        Token.make(104, "volatileKeyword", ),
        Token.make(105, "voidKeyword", ),
        Token.make(106, "whileKeyword", ),
        Token.make(107, "eof", "eof"),
      ]).toImmutable();
    }
    return Token.#vals;
  }

  static static$init() {
    const $_u145 = Token.vals();
    if (true) {
    }
    ;
    if (true) {
      let map = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::Token"));
      Token.#vals.each((t) => {
        if (t.#keyword) {
          map.set(t.#symbol, t);
        }
        ;
        return;
      });
      Token.#keywords = sys.ObjUtil.coerce(sys.ObjUtil.toImmutable(map), sys.Type.find("[sys::Str:compiler::Token]"));
    }
    ;
    return;
  }

}

class CompileJs extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return CompileJs.type$; }

  #hasJs = false;

  // private field reflection only
  __hasJs(it) { if (it === undefined) return this.#hasJs; else this.#hasJs = it; }

  static make(compiler) {
    const $self = new CompileJs();
    CompileJs.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    const this$ = $self;
    CompilerStep.make$($self, compiler);
    $self.#hasJs = compiler.types().any((it) => {
      return it.hasFacet("sys::Js");
    });
    return;
  }

  run() {
    this.log().info("CompileJs");
    if (this.needCompileJs()) {
      this.compile("compilerJs::CompileJsPlugin");
    }
    ;
    if (this.needCompileEs()) {
      if (sys.ObjUtil.compareNE(this.pod().name(), "sys")) {
        this.compile("compilerEs::CompileEsPlugin");
      }
      ;
    }
    ;
    return;
  }

  compile(qname) {
    let t = sys.Type.find(qname, false);
    if (t == null) {
      this.log().info(sys.Str.plus(sys.Str.plus("WARN: ", qname), " not installed"));
      return;
    }
    ;
    sys.ObjUtil.trap(t.make(sys.List.make(Compiler.type$, [this.compiler()])),"run", sys.List.make(sys.Obj.type$.toNullable(), []));
    return;
  }

  needCompileEs() {
    return (this.needCompileJs() || this.compiler().isSys());
  }

  needCompileJs() {
    if (this.compiler().input().output() === CompilerOutputMode.js()) {
      return true;
    }
    ;
    if ((this.compiler().jsFiles() != null && !this.compiler().jsFiles().isEmpty())) {
      return true;
    }
    ;
    if (this.compiler().input().forceJs()) {
      return true;
    }
    ;
    if ((this.compiler().jsPropsFiles() != null && !this.compiler().jsPropsFiles().isEmpty())) {
      return true;
    }
    ;
    return this.#hasJs;
  }

}

class InitInput extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return InitInput.type$; }

  #loc = null;

  // private field reflection only
  __loc(it) { if (it === undefined) return this.#loc; else this.#loc = it; }

  #input = null;

  // private field reflection only
  __input(it) { if (it === undefined) return this.#input; else this.#input = it; }

  static make(compiler) {
    const $self = new InitInput();
    InitInput.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    $self.#loc = compiler.input().inputLoc();
    $self.#input = compiler.input();
    return;
  }

  run() {
    this.validateInput();
    this.validatePodName();
    this.initNamespace();
    this.initPod();
    this.initDepends();
    this.initFiles();
    return;
  }

  validateInput() {
    try {
      this.#input.validate();
    }
    catch ($_u146) {
      $_u146 = sys.Err.make($_u146);
      if ($_u146 instanceof CompilerErr) {
        let err = $_u146;
        ;
        throw this.errReport(err);
      }
      else {
        throw $_u146;
      }
    }
    ;
    return;
  }

  validatePodName() {
    let msg = InitInput.isValidPodName(sys.ObjUtil.coerce(this.#input.podName(), sys.Str.type$));
    if (msg != null) {
      throw this.err(sys.ObjUtil.coerce(msg, sys.Str.type$), this.#input.inputLoc());
    }
    ;
    return;
  }

  static isValidPodName(name) {
    if (sys.Str.isEmpty(name)) {
      return "Pod name is empty";
    }
    ;
    if (!sys.Int.isAlpha(sys.Str.get(name, 0))) {
      return "Pod name must begin with alpha char";
    }
    ;
    for (let i = 0; sys.ObjUtil.compareLT(i, sys.Str.size(name)); ((this$) => { let $_u147 = i;i = sys.Int.increment(i); return $_u147; })(this)) {
      let ch = sys.Str.get(name, i);
      if ((!sys.Int.isAlphaNum(ch) && sys.ObjUtil.compareNE(ch, 95))) {
        return sys.Str.plus(sys.Str.plus("Pod name contains invalid char '", sys.Int.toChar(ch)), "'");
      }
      ;
    }
    ;
    return null;
  }

  initNamespace() {
    this.compiler().ns(this.#input.ns());
    this.#input.ns().c(this.compiler());
    return;
  }

  initPod() {
    const this$ = this;
    let ts = sys.DateTime.now().floor(sys.Duration.fromStr("1sec"));
    let meta = sys.ObjUtil.coerce(sys.ObjUtil.with(sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("sys::Str")), (it) => {
      it.ordered(true);
      return;
    }), sys.Type.find("[sys::Str:sys::Str]"));
    meta.set("pod.name", sys.ObjUtil.coerce(this.#input.podName(), sys.Str.type$));
    meta.set("pod.version", this.#input.version().toStr());
    meta.set("pod.depends", this.#input.depends().join(";"));
    meta.set("pod.summary", sys.ObjUtil.coerce(this.#input.summary(), sys.Str.type$));
    meta.set("pod.isScript", sys.Bool.toStr(this.#input.isScript()));
    meta.set("fcode.version", FConst.FCodeVersion());
    meta.set("build.host", sys.Env.cur().host());
    meta.set("build.user", sys.Env.cur().user());
    meta.set("build.ts", ts.toStr());
    meta.set("build.tsKey", ts.toLocale("YYMMDDhhmmss"));
    meta.set("build.compiler", sys.ObjUtil.typeof(this).pod().version().toStr());
    meta.set("build.platform", sys.Env.cur().platform());
    meta.addAll(this.#input.meta());
    let pod = PodDef.make(this.ns(), this.#input.inputLoc(), sys.ObjUtil.coerce(this.#input.podName(), sys.Str.type$));
    pod.meta(meta);
    pod.index(this.#input.index());
    this.compiler().pod(pod);
    this.compiler().isSys(sys.ObjUtil.equals(pod.name(), "sys"));
    return;
  }

  initDepends() {
    const this$ = this;
    this.compiler().depends(sys.ObjUtil.coerce(this.#input.depends().map((d) => {
      return CDepend.make(d, null);
    }, CDepend.type$), sys.Type.find("compiler::CDepend[]")));
    return;
  }

  initFiles() {
    const this$ = this;
    if (this.#input.mode() !== CompilerInputMode.file()) {
      return;
    }
    ;
    this.compiler().srcFiles(this.findFiles(this.#input.srcFiles(), "fan"));
    this.compiler().resFiles(this.findFiles(this.#input.resFiles(), null));
    this.compiler().jsFiles(this.findFiles(this.#input.jsFiles(), "js"));
    this.compiler().jsPropsFiles(this.findFiles(this.#input.jsPropsFiles(), "props"));
    if ((this.compiler().srcFiles().isEmpty() && this.compiler().resFiles().isEmpty())) {
      throw this.err("No fan source files found", this.#input.inputLoc());
    }
    ;
    let map = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("sys::File"));
    this.compiler().srcFiles().each((file) => {
      if (map.get(file.name()) != null) {
        throw this$.err(sys.Str.plus("Cannot have source files with duplicate names: ", file.name()), Loc.makeFile(file));
      }
      ;
      map.set(file.name(), file);
      return;
    });
    this.log().info(sys.Str.plus(sys.Str.plus("FindSourceFiles [", sys.ObjUtil.coerce(this.compiler().srcFiles().size(), sys.Obj.type$.toNullable())), " files]"));
    return;
  }

  findFiles(uris,ext) {
    const this$ = this;
    let base = this.#input.baseDir();
    let acc = sys.List.make(sys.File.type$);
    ((this$) => { let $_u148 = uris; if ($_u148 == null) return null; return uris.each((uri) => {
      let f = base.plus(uri);
      if (!f.exists()) {
        throw this$.err("Invalid file or directory", Loc.makeFile(f));
      }
      ;
      if (f.isDir()) {
        f.list().each((kid) => {
          if (kid.isDir()) {
            return;
          }
          ;
          if ((ext == null || sys.ObjUtil.equals(kid.ext(), ext))) {
            acc.add(kid);
          }
          ;
          return;
        });
      }
      else {
        acc.add(f);
      }
      ;
      return;
    }); })(this);
    return acc;
  }

}

class DefaultCtor extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return DefaultCtor.type$; }

  static make(compiler) {
    const $self = new DefaultCtor();
    DefaultCtor.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    this.log().debug("DefaultCtor");
    this.walk(this.compiler(), VisitDepth.typeDef());
    return;
  }

  visitTypeDef(t) {
    const this$ = this;
    if ((t.isMixin() || t.isEnum() || t.isFacet())) {
      return;
    }
    ;
    let hasCtor = t.methodDefs().any((m) => {
      return m.isInstanceCtor();
    });
    if (hasCtor) {
      return;
    }
    ;
    let dup = t.slots().get("make");
    if (dup != null) {
      if (dup.parent() === t) {
        this.err(sys.Str.plus("Default constructor 'make' conflicts with slot at ", sys.ObjUtil.trap(sys.ObjUtil.trap(dup,"loc", sys.List.make(sys.Obj.type$.toNullable(), [])),"toLocStr", sys.List.make(sys.Obj.type$.toNullable(), []))), t.loc());
      }
      else {
        this.err(sys.Str.plus(sys.Str.plus("Default constructor 'make' conflicts with inherited slot '", dup.qname()), "'"), t.loc());
      }
      ;
      return;
    }
    ;
    DefaultCtor.addDefaultCtor(t, FConst.Public());
    return;
  }

  static addDefaultCtor(parent,flags) {
    let loc = parent.loc();
    let block = Block.make(loc);
    block.stmts().add(ReturnStmt.makeSynthetic(loc));
    let m = MethodDef.make(loc, parent);
    m.flags(sys.Int.or(flags, sys.Int.plus(FConst.Ctor(), FConst.Synthetic())));
    m.name("make");
    m.returns(sys.ObjUtil.coerce(parent.ns().voidType(), CType.type$));
    m.code(block);
    parent.addSlot(m);
    return m;
  }

}

class ResolveImports extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
    this.#resolved = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::CPod"));
    return;
  }

  typeof() { return ResolveImports.type$; }

  #resolved = null;

  resolved(it) {
    if (it === undefined) {
      return this.#resolved;
    }
    else {
      this.#resolved = it;
      return;
    }
  }

  static make(compiler) {
    const $self = new ResolveImports();
    ResolveImports.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    ;
    $self.#resolved.set($self.pod().name(), $self.pod());
    return;
  }

  run() {
    const this$ = this;
    this.log().debug("ResolveImports");
    this.units().each((unit) => {
      try {
        this$.resolveImports(unit);
      }
      catch ($_u149) {
        $_u149 = sys.Err.make($_u149);
        if ($_u149 instanceof CompilerErr) {
          let e = $_u149;
          ;
          this$.errReport(e);
        }
        else {
          throw $_u149;
        }
      }
      ;
      return;
    });
    this.bombIfErr();
    this.units().each((unit) => {
      try {
        this$.resolveImportedTypes(unit);
      }
      catch ($_u150) {
        $_u150 = sys.Err.make($_u150);
        if ($_u150 instanceof CompilerErr) {
          let e = $_u150;
          ;
          this$.errReport(e);
        }
        else {
          throw $_u150;
        }
      }
      ;
      return;
    });
    this.bombIfErr();
    if (this.compiler().input().isScript()) {
      this.#resolved.each((pod) => {
        if ((pod !== this$.compiler().pod() && sys.ObjUtil.compareNE(pod.name(), "sys"))) {
          this$.compiler().depends().add(CDepend.fromStr(sys.Str.plus(sys.Str.plus(sys.Str.plus("", pod.name()), " "), pod.version())));
        }
        ;
        return;
      });
    }
    ;
    return;
  }

  resolveImports(unit) {
    const this$ = this;
    let dups = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::Using"));
    unit.usings().each((u) => {
      let podName = u.podName();
      ResolveImports.checkUsingPod(this$, podName, u.loc());
      if ((u.typeName() == null && sys.ObjUtil.equals(u.podName(), this$.compiler().pod().name()))) {
        this$.err(sys.Str.plus(sys.Str.plus("Using '", u.podName()), "' is on pod being compiled"), u.loc());
      }
      ;
      let key = podName;
      if (u.typeName() != null) {
        key = sys.Str.plus(key, sys.Str.plus("::", u.typeName()));
      }
      ;
      if (dups.containsKey(key)) {
        this$.err(sys.Str.plus(sys.Str.plus("Duplicate using '", key), "'"), u.loc());
        return;
      }
      ;
      dups.set(key, u);
      u.resolvedPod(this$.#resolved.get(podName));
      if (u.resolvedPod() == null) {
        try {
          let pod = this$.ns().resolvePod(podName, u.loc());
          this$.#resolved.set(podName, sys.ObjUtil.coerce(((this$) => { let $_u151 = pod; u.resolvedPod($_u151); return $_u151; })(this$), CPod.type$));
        }
        catch ($_u152) {
          $_u152 = sys.Err.make($_u152);
          if ($_u152 instanceof CompilerErr) {
            let e = $_u152;
            ;
            this$.errReport(e);
            return;
          }
          else {
            throw $_u152;
          }
        }
        ;
      }
      ;
      if (u.typeName() != null) {
        u.resolvedType(u.resolvedPod().resolveType(sys.ObjUtil.coerce(u.typeName(), sys.Str.type$), false));
        if (u.resolvedType() == null) {
          this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Type not found in pod '", podName), "::"), u.typeName()), "'"), u.loc());
          return;
        }
        ;
      }
      ;
      return;
    });
    return;
  }

  resolveImportedTypes(unit) {
    const this$ = this;
    let types = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::CType[]"));
    this.addAll(types, this.compiler().pod().types());
    unit.usings().each((u) => {
      if (u.typeName() == null) {
        this$.addAll(types, u.resolvedPod().types());
      }
      ;
      return;
    });
    unit.usings().each((u) => {
      if (u.typeName() != null) {
        if (u.asName() == null) {
          types.set(sys.ObjUtil.coerce(u.typeName(), sys.Str.type$), sys.List.make(CType.type$.toNullable(), [u.resolvedType()]));
        }
        else {
          this$.remove(types, sys.ObjUtil.coerce(u.resolvedType(), CType.type$));
          types.set(sys.ObjUtil.coerce(u.asName(), sys.Str.type$), sys.List.make(CType.type$.toNullable(), [u.resolvedType()]));
        }
        ;
      }
      ;
      return;
    });
    unit.importedTypes(types);
    return;
  }

  addAll(types,toAdd) {
    const this$ = this;
    toAdd.each((t) => {
      let list = types.get(t.name());
      if (list == null) {
        types.set(t.name(), sys.ObjUtil.coerce((list = sys.List.make(CType.type$)), sys.Type.find("compiler::CType[]")));
      }
      ;
      list.add(t);
      return;
    });
    return;
  }

  remove(types,t) {
    let list = types.get(t.name());
    if (list != null) {
      for (let i = 0; sys.ObjUtil.compareLT(i, list.size()); i = sys.Int.increment(i)) {
        if (sys.ObjUtil.equals(list.get(i).qname(), t.qname())) {
          list.removeAt(i);
          break;
        }
        ;
      }
      ;
    }
    ;
    return;
  }

  static resolveQualified(cs,podName,typeName,loc) {
    if (sys.ObjUtil.equals(podName, cs.compiler().pod().name())) {
      let t = cs.pod().resolveType(typeName, false);
      if (t == null) {
        cs.err(sys.Str.plus(sys.Str.plus("Type '", typeName), "' not found within pod being compiled"), loc);
        return null;
      }
      ;
      return t;
    }
    ;
    let pod = ResolveImports.resolvePod(cs, podName, loc);
    if (pod == null) {
      return null;
    }
    ;
    let t = pod.resolveType(typeName, false);
    if (t == null) {
      cs.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Type '", typeName), "' not found in pod '"), podName), "'"), loc);
      return null;
    }
    ;
    return t;
  }

  static resolvePod(cs,podName,loc) {
    if (sys.ObjUtil.equals(cs.pod().name(), podName)) {
      return cs.pod();
    }
    ;
    let pod = null;
    try {
      (pod = cs.ns().resolvePod(podName, loc));
    }
    catch ($_u153) {
      $_u153 = sys.Err.make($_u153);
      if ($_u153 instanceof CompilerErr) {
        let e = $_u153;
        ;
        cs.errReport(e);
        return null;
      }
      else {
        throw $_u153;
      }
    }
    ;
    ResolveImports.checkUsingPod(cs, podName, loc);
    return pod;
  }

  static checkUsingPod(cs,podName,loc) {
    if (cs.compiler().input().isScript()) {
      return;
    }
    ;
    if (cs.ns().depends().containsKey(podName)) {
      return;
    }
    ;
    if (sys.ObjUtil.equals(cs.pod().name(), podName)) {
      return;
    }
    ;
    if (sys.Str.startsWith(podName, "[")) {
      return;
    }
    ;
    cs.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Using '", podName), "' which is not a declared dependency for '"), cs.pod().name()), "'"), loc);
    return;
  }

}

class Normalize extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Normalize.type$; }

  static make(compiler) {
    const $self = new Normalize();
    Normalize.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    this.log().debug("Normalize");
    this.walk(this.compiler(), VisitDepth.typeDef());
    this.bombIfErr();
    return;
  }

  visitTypeDef(t) {
    const this$ = this;
    let loc = t.loc();
    let iInit = Block.make(loc);
    let sInit = Block.make(loc);
    t.slotDefs().dup().each((s) => {
      if (sys.ObjUtil.is(s, FieldDef.type$)) {
        let f = sys.ObjUtil.coerce(s, FieldDef.type$);
        this$.normalizeField(f);
        if ((f.init() != null && !f.isAbstract())) {
          if (f.isStatic()) {
            sInit.add(Normalize.fieldInitStmt(f));
          }
          else {
            iInit.add(Normalize.fieldInitStmt(f));
          }
          ;
          f.walkInit(false);
        }
        ;
      }
      else {
        let m = sys.ObjUtil.coerce(s, MethodDef.type$);
        if (m.isStaticInit()) {
          this$.appendStaticInit(sInit, m);
        }
        else {
          this$.normalizeMethod(m, iInit, sInit);
        }
        ;
      }
      ;
      return;
    });
    if (!iInit.isEmpty()) {
      iInit.add(ReturnStmt.makeSynthetic(loc));
      let ii = MethodDef.makeInstanceInit(iInit.loc(), t, iInit);
      t.addSlot(ii);
      this.callInstanceInit(t, ii);
    }
    ;
    if (!sInit.isEmpty()) {
      sInit.add(ReturnStmt.makeSynthetic(loc));
      t.normalizeStaticInits(MethodDef.makeStaticInit(sInit.loc(), t, sInit));
    }
    ;
    return;
  }

  appendStaticInit(sInit,m) {
    let loc = m.loc();
    let cond = LiteralExpr.make(loc, ExprId.trueLiteral(), sys.ObjUtil.coerce(this.ns().boolType(), CType.type$), sys.ObjUtil.coerce(true, sys.Obj.type$.toNullable()));
    let ifStmt = IfStmt.make(loc, cond, sys.ObjUtil.coerce(m.code(), Block.type$));
    sInit.add(ifStmt);
    m.code(null);
    return;
  }

  normalizeMethod(m,iInit,sInit) {
    let code = m.code();
    if (code == null) {
      return;
    }
    ;
    if (!code.isExit()) {
      this.addImplicitReturn(m);
    }
    ;
    if (m.isInstanceCtor()) {
      this.insertSuperCtor(m);
    }
    ;
    if (m.isOnce()) {
      this.normalizeOnce(m, iInit, sInit);
    }
    ;
    return;
  }

  addImplicitReturn(m) {
    let code = m.code();
    let loc = code.loc();
    if ((sys.ObjUtil.equals(code.size(), 1) && !m.returns().isVoid() && sys.ObjUtil.equals(code.stmts().get(0).id(), StmtId.expr()))) {
      code.stmts().set(0, ReturnStmt.makeSynthetic(code.stmts().get(0).loc(), sys.ObjUtil.coerce(sys.ObjUtil.trap(code.stmts().get(0),"expr", sys.List.make(sys.Obj.type$.toNullable(), [])), Expr.type$.toNullable())));
      return;
    }
    ;
    code.add(ReturnStmt.makeSynthetic(loc));
    return;
  }

  insertSuperCtor(m) {
    const this$ = this;
    if (m.ctorChain() != null) {
      return;
    }
    ;
    let parent = m.parent();
    let base = parent.base();
    if (parent.isSynthetic()) {
      return;
    }
    ;
    if (parent.isMixin()) {
      return;
    }
    ;
    if (base.isObj()) {
      return;
    }
    ;
    let superCtors = base.instanceCtors();
    if (sys.ObjUtil.compareGT(superCtors.size(), 1)) {
      (superCtors = superCtors.findAll((ctor) => {
        return ctor.isVisibleTo(sys.ObjUtil.coerce(this$.curType(), CType.type$));
      }));
    }
    ;
    if (sys.ObjUtil.compareNE(superCtors.size(), 1)) {
      return;
    }
    ;
    let superCtor = superCtors.first();
    if (superCtor.isPrivate()) {
      return;
    }
    ;
    if ((superCtor.isInternal() && sys.ObjUtil.compareNE(base.pod(), parent.pod()))) {
      return;
    }
    ;
    if (!superCtor.params().isEmpty()) {
      return;
    }
    ;
    m.ctorChain(CallExpr.makeWithMethod(m.loc(), SuperExpr.make(m.loc()), sys.ObjUtil.coerce(superCtor, CMethod.type$)));
    m.ctorChain().isCtorChain(true);
    return;
  }

  normalizeOnce(m,iInit,sInit) {
    const this$ = this;
    let loc = m.loc();
    let isStatic = m.isStatic();
    if ((this.curType().isMixin() || m.isCtor() || m.isFieldAccessor())) {
      return;
    }
    ;
    if (m.returns().isVoid()) {
      this.err(sys.Str.plus(sys.Str.plus("Once method '", m.name()), "' cannot return Void"), loc);
    }
    ;
    if (!m.params().isEmpty()) {
      this.err(sys.Str.plus(sys.Str.plus("Once method '", m.name()), "' cannot have parameters"), loc);
    }
    ;
    if (m.returns().isForeign()) {
      this.err(sys.Str.plus(sys.Str.plus("Once method cannot be used with FFI type '", m.returns()), "'"), loc);
    }
    ;
    let fieldFlags = sys.Int.plus(sys.Int.plus(sys.Int.plus(FConst.Private(), FConst.Storage()), FConst.Synthetic()), FConst.Once());
    let methodFlags = sys.Int.plus(FConst.Private(), FConst.Synthetic());
    if (isStatic) {
      fieldFlags = sys.Int.plus(fieldFlags, FConst.Static());
      methodFlags = sys.Int.plus(methodFlags, FConst.Static());
    }
    ;
    let f = FieldDef.make(loc, sys.ObjUtil.coerce(this.curType(), TypeDef.type$));
    f.flags(fieldFlags);
    f.name(sys.Str.plus(m.name(), "\$Store"));
    f.type(this.ns().objType().toNullable());
    f.init(Expr.makeForLiteral(loc, this.ns(), "_once_"));
    this.curType().addSlot(f);
    if (isStatic) {
      sInit.add(Normalize.fieldInitStmt(f));
    }
    else {
      iInit.add(Normalize.fieldInitStmt(f));
    }
    ;
    let x = MethodDef.make(loc, sys.ObjUtil.coerce(this.curType(), TypeDef.type$));
    x.flags(methodFlags);
    x.name(sys.Str.plus(m.name(), "\$Once"));
    x.returns(m.returns());
    x.inheritedRet(null);
    x.paramDefs(m.paramDefs());
    x.vars(m.vars());
    x.usesCvars(m.usesCvars());
    x.code(m.code());
    this.curType().addSlot(x);
    this.curType().closures().each((c) => {
      if (c.enclosingSlot() === m) {
        c.enclosingSlot(x);
      }
      ;
      return;
    });
    m.code(Block.make(loc));
    let cond = BinaryExpr.make(f.makeAccessorExpr(loc, false), Token.same(), Expr.makeForLiteral(loc, this.ns(), "_once_"));
    let callTarget = ((this$) => { if (isStatic) return null; return ThisExpr.make(loc); })(this);
    let trueBlock = Block.make(loc);
    trueBlock.add(BinaryExpr.make(f.makeAccessorExpr(loc, false), Token.assign(), CallExpr.makeWithMethod(loc, callTarget, x)).toStmt());
    let ifStmt = IfStmt.make(loc, cond, trueBlock);
    m.code().add(ifStmt);
    let retStmt = ReturnStmt.makeSynthetic(loc);
    retStmt.expr(f.makeAccessorExpr(loc, false));
    m.code().add(retStmt);
    return;
  }

  callInstanceInit(t,ii) {
    const this$ = this;
    t.methodDefs().each((m) => {
      if (!m.isInstanceCtor()) {
        return;
      }
      ;
      if (t.isNative()) {
        return;
      }
      ;
      if ((m.ctorChain() != null && m.ctorChain().target().id() === ExprId.thisExpr())) {
        return;
      }
      ;
      let call = CallExpr.makeWithMethod(m.loc(), ThisExpr.make(m.loc()), ii);
      m.code().stmts().insert(0, call.toStmt());
      return;
    });
    return;
  }

  normalizeField(f) {
    let t = f.type();
    if (t.isThis()) {
      this.err("Cannot use This as field type", f.loc());
      return;
    }
    ;
    if (t.isVoid()) {
      this.err("Cannot use Void as field type", f.loc());
      return;
    }
    ;
    if (!t.isValid()) {
      this.err(sys.Str.plus(sys.Str.plus("Invalid type '", t), "'"), f.loc());
      return;
    }
    ;
    if (f.init() != null) {
      if (sys.ObjUtil.equals(f.init().id(), ExprId.listLiteral())) {
        this.inferFieldListType(f);
      }
      else {
        if (sys.ObjUtil.equals(f.init().id(), ExprId.mapLiteral())) {
          this.inferFieldMapType(f);
        }
        ;
      }
      ;
    }
    ;
    if ((f.concreteBase() != null && !f.isAbstract() && !f.isNative())) {
      if (!f.hasGet()) {
        this.genSyntheticOverrideGet(f);
      }
      ;
      if (!f.hasSet()) {
        this.genSyntheticOverrideSet(f);
      }
      ;
    }
    ;
    if (f.get() != null) {
      f.get().inheritedRet(f.inheritedRet());
    }
    ;
    return;
  }

  inferFieldListType(f) {
    let init = sys.ObjUtil.as(f.init(), ListLiteralExpr.type$);
    if (init.explicitType() != null) {
      return;
    }
    ;
    init.explicitType(sys.ObjUtil.as(f.type().toNonNullable(), ListType.type$));
    return;
  }

  inferFieldMapType(f) {
    let init = sys.ObjUtil.as(f.init(), MapLiteralExpr.type$);
    if (init.explicitType() != null) {
      return;
    }
    ;
    init.explicitType(sys.ObjUtil.as(f.type().toNonNullable(), MapType.type$));
    return;
  }

  genSyntheticOverrideGet(f) {
    let loc = f.loc();
    f.get().code().stmts().clear();
    f.get().code().add(ReturnStmt.makeSynthetic(loc, FieldExpr.make(loc, SuperExpr.make(loc), f.concreteBase())));
    return;
  }

  genSyntheticOverrideSet(f) {
    let loc = f.loc();
    let lhs = FieldExpr.make(loc, SuperExpr.make(loc), f.concreteBase());
    let rhs = UnknownVarExpr.make(loc, null, "it");
    let code = f.get().code();
    f.set().code().stmts().clear();
    f.set().code().add(BinaryExpr.makeAssign(lhs, rhs).toStmt());
    f.set().code().add(ReturnStmt.makeSynthetic(loc));
    return;
  }

  static fieldInitStmt(f) {
    let useAccessor = f.concreteBase() != null;
    let lhs = f.makeAccessorExpr(f.loc(), useAccessor);
    let rhs = f.init();
    return BinaryExpr.makeAssign(lhs, sys.ObjUtil.coerce(rhs, Expr.type$)).toStmt();
  }

}

class InitEnum extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return InitEnum.type$; }

  static make(compiler) {
    const $self = new InitEnum();
    InitEnum.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    this.log().debug("InitEnum");
    this.walk(this.compiler(), VisitDepth.typeDef());
    this.bombIfErr();
    return;
  }

  visitTypeDef(t) {
    const this$ = this;
    if (!t.isEnum()) {
      return;
    }
    ;
    try {
      this.addCtor();
      this.addFromStr();
      t.addFacet(this, sys.ObjUtil.coerce(this.ns().sysPod().resolveType("Serializable", true), CType.type$), sys.Map.__fromLiteral(["simple"], [sys.ObjUtil.coerce(true, sys.Bool.type$.toNullable())], sys.Type.find("sys::Str"), sys.Type.find("sys::Bool")));
      let fields = sys.List.make(FieldDef.type$);
      t.enumDefs().each((e) => {
        fields.add(this$.makeField(e));
        return;
      });
      fields.add(this.makeValsField());
      fields.each((f,i) => {
        t.addSlot(f, sys.ObjUtil.coerce(i, sys.Int.type$.toNullable()));
        return;
      });
    }
    catch ($_u155) {
      $_u155 = sys.Err.make($_u155);
      if ($_u155 instanceof CompilerErr) {
        let e = $_u155;
        ;
      }
      else {
        throw $_u155;
      }
    }
    ;
    return;
  }

  addCtor() {
    const this$ = this;
    let m = null;
    let ctors = this.curType().methodDefs().findAll((x) => {
      return x.isInstanceCtor();
    });
    ctors.each((ctor) => {
      if (sys.ObjUtil.equals(ctor.name(), "make")) {
        (m = ctor);
      }
      else {
        throw this$.err("Enum constructor must be named 'make'", ctor.loc());
      }
      ;
      return;
    });
    if (m != null) {
      if (!m.isPrivate()) {
        this.err("Enum constructor must be private", m.loc());
      }
      ;
      if (m.ctorChain() != null) {
        this.err("Enum constructor cannot call super constructor", m.loc());
      }
      ;
    }
    ;
    if (m == null) {
      (m = MethodDef.make(this.curType().loc(), sys.ObjUtil.coerce(this.curType(), TypeDef.type$)));
      m.name("make");
      m.flags(sys.Int.plus(sys.Int.plus(FConst.Ctor(), FConst.Private()), FConst.Synthetic()));
      m.returns(TypeRef.make(this.curType().loc(), sys.ObjUtil.coerce(this.ns().voidType(), CType.type$)));
      m.code(Block.make(this.curType().loc()));
      m.code().stmts().add(ReturnStmt.makeSynthetic(this.curType().loc()));
      this.curType().addSlot(sys.ObjUtil.coerce(m, CSlot.type$));
    }
    ;
    let loc = m.loc();
    m.ctorChain(CallExpr.make(loc, SuperExpr.make(loc), "make"));
    m.ctorChain().isCtorChain(true);
    m.ctorChain().args().add(UnknownVarExpr.make(loc, null, "\$ordinal"));
    m.ctorChain().args().add(UnknownVarExpr.make(loc, null, "\$name"));
    m.params().insert(0, ParamDef.make(loc, sys.ObjUtil.coerce(this.ns().intType(), CType.type$), "\$ordinal"));
    m.params().insert(1, ParamDef.make(loc, sys.ObjUtil.coerce(this.ns().strType(), CType.type$), "\$name"));
    return;
  }

  addFromStr() {
    let loc = this.curType().loc();
    let m = MethodDef.make(loc, sys.ObjUtil.coerce(this.curType(), TypeDef.type$));
    m.name("fromStr");
    m.flags(sys.Int.plus(sys.Int.plus(FConst.Public(), FConst.Static()), FConst.Ctor()));
    m.params().add(ParamDef.make(loc, sys.ObjUtil.coerce(this.ns().strType(), CType.type$), "name"));
    m.params().add(ParamDef.make(loc, sys.ObjUtil.coerce(this.ns().boolType(), CType.type$), "checked", LiteralExpr.make(loc, ExprId.trueLiteral(), sys.ObjUtil.coerce(this.ns().boolType(), CType.type$), sys.ObjUtil.coerce(true, sys.Obj.type$.toNullable()))));
    m.returns(TypeRef.make(loc, this.curType().toNullable()));
    m.code(Block.make(loc));
    m.docDef(DocDef.make(loc, sys.List.make(sys.Str.type$, [sys.Str.plus(sys.Str.plus("Return the ", this.curType().name()), " instance for the specified name.  If not a"), "valid name and checked is false return null, otherwise throw ParseErr."])));
    this.curType().addSlot(m);
    let doFromStr = CallExpr.make(loc, null, "doFromStr");
    doFromStr.args().add(LiteralExpr.make(loc, ExprId.typeLiteral(), sys.ObjUtil.coerce(this.ns().typeType(), CType.type$), this.curType()));
    doFromStr.args().add(UnknownVarExpr.make(loc, null, "name"));
    doFromStr.args().add(UnknownVarExpr.make(loc, null, "checked"));
    let cast = TypeCheckExpr.make(loc, ExprId.coerce(), doFromStr, this.curType().toNullable());
    m.code().stmts().add(ReturnStmt.makeSynthetic(loc, cast));
    return;
  }

  makeField(def) {
    let dup = this.curType().slot(def.name());
    if (dup != null) {
      if (dup.parent() === this.curType()) {
        throw this.err(sys.Str.plus(sys.Str.plus("Enum '", def.name()), "' conflicts with slot"), sys.ObjUtil.coerce(sys.ObjUtil.trap(dup,"loc", sys.List.make(sys.Obj.type$.toNullable(), [])), Loc.type$));
      }
      else {
        throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Enum '", def.name()), "' conflicts with inherited slot '"), dup.qname()), "'"), def.loc());
      }
      ;
    }
    ;
    let loc = def.loc();
    let init = CallExpr.make(loc, null, "make");
    init.args().add(LiteralExpr.make(loc, ExprId.intLiteral(), sys.ObjUtil.coerce(this.ns().intType(), CType.type$), sys.ObjUtil.coerce(def.ordinal(), sys.Obj.type$.toNullable())));
    init.args().add(LiteralExpr.make(loc, ExprId.strLiteral(), sys.ObjUtil.coerce(this.ns().strType(), CType.type$), def.name()));
    init.args().addAll(def.ctorArgs());
    let f = FieldDef.make(loc, sys.ObjUtil.coerce(this.curType(), TypeDef.type$));
    f.docDef(def.doc());
    f.facets(def.facets());
    f.flags(sys.Int.plus(sys.Int.plus(sys.Int.plus(sys.Int.plus(FConst.Public(), FConst.Static()), FConst.Const()), FConst.Storage()), FConst.Enum()));
    f.name(def.name());
    f.type(sys.ObjUtil.coerce(this.curType(), CType.type$));
    f.init(init);
    f.enumDef(def);
    return f;
  }

  makeValsField() {
    const this$ = this;
    let dup = this.curType().slot("vals");
    if (dup != null) {
      if (sys.ObjUtil.equals(dup.parent(), this.curType())) {
        throw this.err("Enum 'vals' conflicts with slot", sys.ObjUtil.coerce(sys.ObjUtil.trap(dup,"loc", sys.List.make(sys.Obj.type$.toNullable(), [])), Loc.type$));
      }
      else {
        throw this.err(sys.Str.plus(sys.Str.plus("Enum 'vals' conflicts with inherited slot '", dup.qname()), "'"), this.curType().loc());
      }
      ;
    }
    ;
    let loc = this.curType().loc();
    let listType = this.curType().toListOf();
    let init = ListLiteralExpr.make(loc, sys.ObjUtil.coerce(listType, ListType.type$.toNullable()));
    this.curType().enumDefs().each((e) => {
      let target = StaticTargetExpr.make(loc, sys.ObjUtil.coerce(this$.curType(), CType.type$));
      init.vals().add(UnknownVarExpr.make(loc, target, e.name()));
      return;
    });
    let f = FieldDef.make(loc, sys.ObjUtil.coerce(this.curType(), TypeDef.type$));
    f.flags(sys.Int.plus(sys.Int.plus(sys.Int.plus(FConst.Public(), FConst.Static()), FConst.Const()), FConst.Storage()));
    f.name("vals");
    f.type(listType);
    f.init(init);
    f.docDef(DocDef.make(loc, sys.List.make(sys.Str.type$, [sys.Str.plus(sys.Str.plus("List of ", this.curType().name()), " values indexed by ordinal")])));
    return f;
  }

}

class CheckErrors extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
    this.#protectedRegionDepth = 0;
    this.#finallyDepth = 0;
    return;
  }

  typeof() { return CheckErrors.type$; }

  #protectedRegionDepth = 0;

  // private field reflection only
  __protectedRegionDepth(it) { if (it === undefined) return this.#protectedRegionDepth; else this.#protectedRegionDepth = it; }

  #finallyDepth = 0;

  // private field reflection only
  __finallyDepth(it) { if (it === undefined) return this.#finallyDepth; else this.#finallyDepth = it; }

  #isSys = false;

  // private field reflection only
  __isSys(it) { if (it === undefined) return this.#isSys; else this.#isSys = it; }

  #coercer = null;

  // private field reflection only
  __coercer(it) { if (it === undefined) return this.#coercer; else this.#coercer = it; }

  static make(compiler) {
    const $self = new CheckErrors();
    CheckErrors.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    ;
    $self.#isSys = compiler.isSys();
    $self.#coercer = Coercer.make(compiler);
    return;
  }

  run() {
    this.log().debug("CheckErrors");
    this.checkPodDef(this.pod());
    this.walk(this.compiler(), VisitDepth.expr());
    this.bombIfErr();
    return;
  }

  checkPodDef(pod) {
    return;
  }

  visitTypeDef(t) {
    const this$ = this;
    this.checkTypeFlags(t);
    this.checkFacets(t.facets());
    this.checkAbstractSlots(t);
    this.checkConstType(t);
    if (CheckErrors.isRestrictedName(t.name())) {
      this.err(sys.Str.plus(sys.Str.plus("Type name '", t.name()), "' is restricted"), t.loc());
    }
    ;
    this.checkResConflicts(t);
    let foreign = t.foreignInheritance();
    if (foreign != null) {
      foreign.bridge().checkType(t);
    }
    ;
    if ((t.slotDef("typeof") != null && !this.#isSys)) {
      this.err("Cannot override Obj.typeof()", t.slotDef("typeof").loc());
    }
    ;
    if (t.base() != null) {
      this.checkBase(t, sys.ObjUtil.coerce(t.base(), CType.type$));
    }
    ;
    t.mixins().each((m) => {
      this$.checkMixin(t, m);
      return;
    });
    this.checkDefiniteAssign(t.staticInit());
    return;
  }

  static isRestrictedName(name) {
    return (sys.ObjUtil.equals(name, "pod") || sys.ObjUtil.equals(name, "index"));
  }

  checkResConflicts(t) {
    const this$ = this;
    ((this$) => { let $_u156 = this$.compiler().input().resFiles(); if ($_u156 == null) return null; return this$.compiler().input().resFiles().each((uri) => {
      if (sys.ObjUtil.equals(uri.path().first(), t.name())) {
        this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Resource `", uri), "` conflicts with type name '"), t.name()), "'"), t.loc());
      }
      ;
      return;
    }); })(this);
    return;
  }

  checkTypeFlags(t) {
    let flags = t.flags();
    let loc = t.loc();
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Ctor()), 0)) {
      this.err("Cannot use 'new' modifier on type", loc);
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Once()), 0)) {
      this.err("Cannot use 'once' modifier on type", loc);
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Override()), 0)) {
      this.err("Cannot use 'override' modifier on type", loc);
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Private()), 0)) {
      this.err("Cannot use 'private' modifier on type", loc);
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Protected()), 0)) {
      this.err("Cannot use 'protected' modifier on type", loc);
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Static()), 0)) {
      this.err("Cannot use 'static' modifier on type", loc);
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Virtual()), 0)) {
      this.err("Cannot use 'virtual' modifier on type", loc);
    }
    ;
    this.checkProtectionFlags(flags, loc);
    if ((sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0) && sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Final()), 0))) {
      this.err("Invalid combination of 'abstract' and 'final' modifiers", loc);
    }
    ;
    return;
  }

  checkAbstractSlots(t) {
    const this$ = this;
    if (t.isAbstract()) {
      return;
    }
    ;
    let errForDef = false;
    let closure = (slot) => {
      if (!slot.isAbstract()) {
        return;
      }
      ;
      if (slot.parent() === t) {
        if (!errForDef) {
          this$.err(sys.Str.plus(sys.Str.plus("Class '", t.name()), "' must be abstract since it contains abstract slots"), t.loc());
          (errForDef = true);
        }
        ;
      }
      else {
        this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Class '", t.name()), "' must be abstract since it inherits but doesn't override '"), slot.qname()), "'"), t.loc());
      }
      ;
      return;
    };
    if (this.compiler().input().isTest()) {
      t.slots().vals().sort().each(closure);
    }
    else {
      t.slots().each(closure);
    }
    ;
    return;
  }

  checkConstType(t) {
    const this$ = this;
    if (!t.isConst()) {
      return;
    }
    ;
    if ((t.base() != null && sys.ObjUtil.compareNE(t.base(), this.ns().objType()) && !t.base().isConst())) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Const type '", t.name()), "' cannot subclass non-const class '"), t.base().name()), "'"), t.loc());
    }
    ;
    t.fieldDefs().each((f) => {
      if ((!f.isConst() && !f.isStatic() && f.isStorage() && !this$.#isSys && !f.isOnce())) {
        this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Const type '", t.name()), "' cannot contain non-const field '"), f.name()), "'"), f.loc());
      }
      ;
      return;
    });
    return;
  }

  checkBase(t,base) {
    if ((t.isPublic() && !base.isPublic())) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Public type '", t.name()), "' cannot extend from internal class '"), base.name()), "'"), t.loc());
    }
    ;
    if ((!t.isConst() && base.isConst())) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Non-const type '", t.name()), "' cannot subclass const class '"), base.name()), "'"), t.loc());
    }
    ;
    return;
  }

  checkMixin(t,m) {
    if ((t.isPublic() && !m.isPublic())) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Public type '", t.name()), "' cannot implement internal mixin '"), m.name()), "'"), t.loc());
    }
    ;
    if ((!t.isConst() && m.isConst())) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Non-const type '", t.name()), "' cannot implement const mixin '"), m.name()), "'"), t.loc());
    }
    ;
    return;
  }

  visitFieldDef(f) {
    if (f.concreteBase() != null) {
      f.flags(sys.Int.and(f.flags(), sys.Int.not(FConst.Storage())));
    }
    ;
    this.checkFieldFlags(f);
    this.checkFacets(f.facets());
    if ((this.curType().isMixin() && !f.isAbstract() && !f.isStatic() && f.isStorage())) {
      this.err(sys.Str.plus(sys.Str.plus("Mixin field '", f.name()), "' must be abstract"), f.loc());
    }
    ;
    if ((f.isAbstract() && f.init() != null)) {
      this.err(sys.Str.plus(sys.Str.plus("Abstract field '", f.name()), "' cannot have initializer"), f.init().loc());
    }
    ;
    if ((f.isAbstract() && (f.hasGet() || f.hasSet()))) {
      this.err(sys.Str.plus(sys.Str.plus("Abstract field '", f.name()), "' cannot have getter or setter"), f.loc());
    }
    ;
    this.checkTypeProtection(f.type(), f.loc());
    if ((this.curType().isPublic() && (f.isPublic() || f.isProtected()) && !f.type().isPublic())) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Public field '", this.curType().name()), "."), f.name()), "' cannot use internal type '"), f.type()), "'"), f.loc());
    }
    ;
    return;
  }

  checkFieldFlags(f) {
    let flags = f.flags();
    let loc = f.loc();
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Ctor()), 0)) {
      this.err("Cannot use 'new' modifier on field", loc);
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Final()), 0)) {
      this.err("Cannot use 'final' modifier on field", loc);
    }
    ;
    if ((sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Once()), 0) && sys.ObjUtil.equals(sys.Int.and(flags, FConst.Synthetic()), 0))) {
      this.err("Cannot use 'once' modifier on field", loc);
    }
    ;
    this.checkProtectionFlags(flags, loc);
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Native()), 0)) {
      if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Const()), 0)) {
        this.err("Invalid combination of 'native' and 'const' modifiers", loc);
      }
      ;
      if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0)) {
        this.err("Invalid combination of 'native' and 'abstract' modifiers", loc);
      }
      ;
      if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Static()), 0)) {
        this.err("Invalid combination of 'native' and 'static' modifiers", loc);
      }
      ;
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Const()), 0)) {
      if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0)) {
        this.err("Invalid combination of 'const' and 'abstract' modifiers", loc);
      }
      else {
        if ((sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Virtual()), 0) && sys.ObjUtil.equals(sys.Int.and(flags, FConst.Override()), 0))) {
          this.err("Invalid combination of 'const' and 'virtual' modifiers", loc);
        }
        ;
      }
      ;
      if (!f.type().isConstFieldType()) {
        this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Const field '", f.name()), "' has non-const type '"), f.type()), "'"), loc);
      }
      ;
    }
    else {
      if ((sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Static()), 0) && sys.ObjUtil.equals(sys.Int.and(flags, FConst.Synthetic()), 0))) {
        this.err(sys.Str.plus(sys.Str.plus("Static field '", f.name()), "' must be const"), loc);
      }
      ;
    }
    ;
    if (f.setter() != null) {
      let fieldProtection = sys.Int.and(flags, sys.Int.not(Parser.ProtectionMask()));
      let setterProtection = sys.Int.and(f.set().flags(), sys.Int.not(Parser.ProtectionMask()));
      if (sys.ObjUtil.compareNE(fieldProtection, setterProtection)) {
        this.checkProtectionFlags(f.set().flags(), loc);
        if (sys.ObjUtil.compareNE(sys.Int.and(fieldProtection, FConst.Private()), 0)) {
          if (sys.ObjUtil.compareNE(sys.Int.and(setterProtection, FConst.Public()), 0)) {
            this.err("Setter cannot have wider visibility than the field", loc);
          }
          ;
          if (sys.ObjUtil.compareNE(sys.Int.and(setterProtection, FConst.Protected()), 0)) {
            this.err("Setter cannot have wider visibility than the field", loc);
          }
          ;
          if (sys.ObjUtil.compareNE(sys.Int.and(setterProtection, FConst.Internal()), 0)) {
            this.err("Setter cannot have wider visibility than the field", loc);
          }
          ;
        }
        else {
          if (sys.ObjUtil.compareNE(sys.Int.and(fieldProtection, FConst.Internal()), 0)) {
            if (sys.ObjUtil.compareNE(sys.Int.and(setterProtection, FConst.Public()), 0)) {
              this.err("Setter cannot have wider visibility than the field", loc);
            }
            ;
            if (sys.ObjUtil.compareNE(sys.Int.and(setterProtection, FConst.Protected()), 0)) {
              this.err("Setter cannot have wider visibility than the field", loc);
            }
            ;
          }
          else {
            if (sys.ObjUtil.compareNE(sys.Int.and(fieldProtection, FConst.Protected()), 0)) {
              if (sys.ObjUtil.compareNE(sys.Int.and(setterProtection, FConst.Public()), 0)) {
                this.err("Setter cannot have wider visibility than the field", loc);
              }
              ;
            }
            ;
          }
          ;
        }
        ;
      }
      ;
    }
    ;
    return;
  }

  visitMethodDef(m) {
    const this$ = this;
    this.checkMethodFlags(m);
    this.checkFacets(m.facets());
    this.checkParams(m);
    this.checkMethodReturn(m);
    if (m.isInstanceCtor()) {
      this.checkInstanceCtor(m);
    }
    ;
    if (m.hasFacet("sys::Operator")) {
      this.checkOperatorMethod(m);
    }
    ;
    if (!m.isAccessor()) {
      this.checkTypeProtection(m.returns(), m.loc());
      m.paramDefs().each((p) => {
        this$.checkTypeProtection(p.type(), p.loc());
        return;
      });
    }
    ;
    if ((!m.isAccessor() && this.curType().isPublic() && (m.isPublic() || m.isProtected()))) {
      if (!m.returns().isPublic()) {
        this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Public method '", this.curType().name()), "."), m.name()), "' cannot use internal type '"), m.returns()), "'"), m.loc());
      }
      ;
      m.paramDefs().each((p) => {
        if (!p.type().isPublic()) {
          this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Public method '", this$.curType().name()), "."), m.name()), "' cannot use internal type '"), p.type()), "'"), m.loc());
        }
        ;
        return;
      });
    }
    ;
    return;
  }

  checkMethodFlags(m) {
    if (m.isFieldAccessor()) {
      return;
    }
    ;
    let flags = m.flags();
    let loc = m.loc();
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Final()), 0)) {
      this.err("Cannot use 'final' modifier on method", loc);
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Const()), 0)) {
      this.err("Cannot use 'const' modifier on method", loc);
    }
    ;
    this.checkProtectionFlags(flags, loc);
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Ctor()), 0)) {
      if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0)) {
        this.err("Invalid combination of 'new' and 'abstract' modifiers", loc);
      }
      else {
        if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Override()), 0)) {
          this.err("Invalid combination of 'new' and 'override' modifiers", loc);
        }
        else {
          if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Virtual()), 0)) {
            this.err("Invalid combination of 'new' and 'virtual' modifiers", loc);
          }
          ;
        }
        ;
      }
      ;
      if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Once()), 0)) {
        this.err("Invalid combination of 'new' and 'once' modifiers", loc);
      }
      ;
      if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Native()), 0)) {
        this.err("Invalid combination of 'new' and 'native' modifiers", loc);
      }
      ;
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Static()), 0)) {
      if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0)) {
        this.err("Invalid combination of 'static' and 'abstract' modifiers", loc);
      }
      else {
        if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Override()), 0)) {
          this.err("Invalid combination of 'static' and 'override' modifiers", loc);
        }
        else {
          if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Virtual()), 0)) {
            this.err("Invalid combination of 'static' and 'virtual' modifiers", loc);
          }
          ;
        }
        ;
      }
      ;
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0)) {
      if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Native()), 0)) {
        this.err("Invalid combination of 'abstract' and 'native' modifiers", loc);
      }
      ;
      if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Once()), 0)) {
        this.err("Invalid combination of 'abstract' and 'once' modifiers", loc);
      }
      ;
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Once()), 0)) {
      if (this.curType().isMixin()) {
        this.err("Mixins cannot have once methods", m.loc());
      }
      ;
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Native()), 0)) {
      if (this.curType().isMixin()) {
        this.err("Mixins cannot have native methods", m.loc());
      }
      ;
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(m.flags(), FConst.Static()), 0)) {
      m.flags(sys.Int.or(flags, FConst.Const()));
    }
    ;
    return;
  }

  checkParams(m) {
    const this$ = this;
    let seenDef = false;
    m.paramDefs().each((p) => {
      this$.checkParam(p);
      if (seenDef) {
        if (p.def() == null) {
          this$.err(sys.Str.plus(sys.Str.plus("Parameter '", p.name()), "' must have default"), p.loc());
        }
        ;
      }
      else {
        (seenDef = p.def() != null);
      }
      ;
      return;
    });
    return;
  }

  checkParam(p) {
    const this$ = this;
    let t = p.type();
    if (t.isVoid()) {
      this.err("Cannot use Void as parameter type", p.loc());
      return;
    }
    ;
    if (t.isThis()) {
      this.err("Cannot use This as parameter type", p.loc());
      return;
    }
    ;
    let func = sys.ObjUtil.as(t.deref().toNonNullable(), FuncType.type$);
    if (func != null) {
      this.checkParamFuncType(p, sys.ObjUtil.coerce(func, FuncType.type$));
    }
    else {
      this.checkValidType(p.loc(), t);
    }
    ;
    if ((p.def() != null && !p.type().isGenericParameter())) {
      p.def(this.coerce(sys.ObjUtil.coerce(p.def(), Expr.type$), p.type(), () => {
        this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("'", p.def().toTypeStr()), "' is not assignable to '"), p.type()), "'"), p.def().loc());
        return;
      }));
    }
    ;
    return;
  }

  checkParamFuncType(param,t) {
    const this$ = this;
    if ((!t.returns().isVoid() && !t.returns().isValid())) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Invalid return type '", t.returns()), "' in func type of param '"), param.name()), "'"), param.loc());
    }
    ;
    t.params().each((p) => {
      if ((!p.isThis() && !p.isValid())) {
        this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Invalid param type '", p), "' in func type of param '"), param.name()), "'"), param.loc());
      }
      ;
      return;
    });
    return;
  }

  checkMethodReturn(m) {
    if (m.returns().isThis()) {
      if (m.isStatic()) {
        this.err("Cannot return This from static method", m.loc());
      }
      ;
      if (m.returns().isNullable()) {
        this.err("This type cannot be nullable", m.loc());
      }
      ;
    }
    ;
    if ((!m.returns().isThis() && !m.returns().isVoid())) {
      this.checkValidType(m.loc(), m.returns());
    }
    ;
    return;
  }

  checkInstanceCtor(m) {
    if (this.curType().isMixin()) {
      this.err("Mixins cannot have instance constructors", m.loc());
    }
    ;
    if ((m.ctorChain() == null && !this.compiler().isSys() && !this.curType().base().isObj() && !this.curType().isSynthetic() && !this.curType().isNative())) {
      this.err(sys.Str.plus(sys.Str.plus("Must call super class constructor in '", m.name()), "'"), m.loc());
    }
    ;
    if (((this$) => { let $_u157=((this$) => { let $_u158=m.ctorChain(); return ($_u158==null) ? null : $_u158.target(); })(this$); return ($_u157==null) ? null : $_u157.id(); })(this) !== ExprId.thisExpr()) {
      this.checkDefiniteAssign(m);
    }
    ;
    return;
  }

  checkDefiniteAssign(m) {
    const this$ = this;
    if (this.#isSys) {
      return;
    }
    ;
    if ((this.curType() != null && this.curType().isNative())) {
      return;
    }
    ;
    let isStaticInit = (m == null || m.isStatic());
    let fields = this.curType().fieldDefs().findAll((f) => {
      return (sys.ObjUtil.equals(f.isStatic(), isStaticInit) && !f.isAbstract() && !f.isNative() && f.isStorage() && (!f.isOverride() || f.concreteBase() == null) && !f.type().isNullable() && !f.type().isVal() && f.init() == null);
    });
    if (fields.isEmpty()) {
      return;
    }
    ;
    fields.each((f) => {
      let definite = (m != null && m.code().isDefiniteAssign((lhs) => {
        if (lhs.id() !== ExprId.field()) {
          return false;
        }
        ;
        let fe = sys.ObjUtil.coerce(lhs, FieldExpr.type$);
        if (sys.ObjUtil.compareNE(fe.field().qname(), f.qname())) {
          return false;
        }
        ;
        let ft = fe.target();
        if ((isStaticInit || ft == null)) {
          return true;
        }
        ;
        if ((ft.id() === ExprId.field() && sys.ObjUtil.equals(sys.ObjUtil.coerce(ft, FieldExpr.type$).field().name(), "\$this"))) {
          f.requiresNullCheck(true);
          return true;
        }
        ;
        return ft.id() === ExprId.thisExpr();
      }));
      if (definite) {
        return;
      }
      ;
      if ((m != null && m.isItBlockCtor())) {
        f.requiresNullCheck(true);
        return;
      }
      ;
      if (isStaticInit) {
        this$.err(sys.Str.plus(sys.Str.plus("Non-nullable field '", f.name()), "' must be assigned in static initializer"), f.loc());
      }
      else {
        this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Non-nullable field '", f.name()), "' must be assigned in constructor '"), m.name()), "'"), m.loc());
      }
      ;
      return;
    });
    return;
  }

  checkOperatorMethod(m) {
    let prefix = COperators.toPrefix(m.name());
    if (prefix == null) {
      this.err(sys.Str.plus(sys.Str.plus("Operator method '", m.name()), "' has invalid name"), m.loc());
      return;
    }
    ;
    let op = ShortcutOp.fromPrefix(sys.ObjUtil.coerce(prefix, sys.Str.type$));
    if ((sys.ObjUtil.equals(m.name(), "add") && !m.returns().isThis() && !this.#isSys)) {
      this.err(sys.Str.plus(sys.Str.plus("Operator method '", m.name()), "' must return This"), m.loc());
    }
    ;
    if ((m.returns().isVoid() && op !== ShortcutOp.set())) {
      this.err(sys.Str.plus(sys.Str.plus("Operator method '", m.name()), "' cannot return Void"), m.loc());
    }
    ;
    if ((sys.ObjUtil.compareNE(sys.Int.plus(m.params().size(), 1), op.degree()) && !sys.ObjUtil.coerce(((this$) => { let $_u159 = ((this$) => { let $_u160 = m.params().getSafe(sys.Int.minus(op.degree(), 1)); if ($_u160 == null) return null; return m.params().getSafe(sys.Int.minus(op.degree(), 1)).hasDefault(); })(this$); if ($_u159 != null) return $_u159; return sys.ObjUtil.coerce(false, sys.Bool.type$.toNullable()); })(this), sys.Bool.type$))) {
      this.err(sys.Str.plus(sys.Str.plus("Operator method '", m.name()), "' has wrong number of parameters"), m.loc());
    }
    ;
    return;
  }

  checkFacets(facets) {
    if (facets == null) {
      return;
    }
    ;
    for (let i = 0; sys.ObjUtil.compareLT(i, facets.size()); i = sys.Int.increment(i)) {
      let f = facets.get(i);
      this.checkFacet(f);
      for (let j = sys.Int.plus(i, 1); sys.ObjUtil.compareLT(j, facets.size()); j = sys.Int.increment(j)) {
        if (sys.ObjUtil.equals(f.type().qname(), facets.get(j).type().qname())) {
          this.err(sys.Str.plus(sys.Str.plus("Duplicate facet '", f.type()), "'"), f.loc());
        }
        ;
      }
      ;
    }
    ;
    return;
  }

  checkFacet(f) {
    const this$ = this;
    if (!f.type().fits(sys.ObjUtil.coerce(this.ns().facetType(), CType.type$))) {
      this.err(sys.Str.plus(sys.Str.plus("Not a facet type '", f.type()), "'"), f.loc());
    }
    ;
    f.names().each((name,i) => {
      let val = f.vals().get(i);
      let field = f.type().field(name);
      if (field == null) {
        this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Unknown facet field '", f.type()), "."), name), "'"), val.loc());
        return;
      }
      ;
      this$.checkDeprecated(sys.ObjUtil.coerce(field, sys.Obj.type$), val.loc());
      if (sys.ObjUtil.equals(val.id(), ExprId.nullLiteral())) {
        if (!field.type().isNullable()) {
          this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Cannot assign null to non-nullable facet field '", name), "': '"), field.type()), "'"), val.loc());
        }
        ;
      }
      else {
        if (!val.ctype().fits(field.type().inferredAs())) {
          this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Invalid type for facet field '", name), "': expected '"), field.type()), "' not '"), val.ctype()), "'"), val.loc());
        }
        ;
      }
      ;
      return;
    });
    return;
  }

  enterStmt(stmt) {
    if (sys.ObjUtil.equals(stmt.id(), StmtId.tryStmt())) {
      ((this$) => { let $_u161 = this$.#protectedRegionDepth;this$.#protectedRegionDepth = sys.Int.increment(this$.#protectedRegionDepth); return $_u161; })(this);
    }
    ;
    return;
  }

  exitStmt(stmt) {
    if (sys.ObjUtil.equals(stmt.id(), StmtId.tryStmt())) {
      ((this$) => { let $_u162 = this$.#protectedRegionDepth;this$.#protectedRegionDepth = sys.Int.decrement(this$.#protectedRegionDepth); return $_u162; })(this);
    }
    ;
    return;
  }

  enterFinally(stmt) {
    ((this$) => { let $_u163 = this$.#finallyDepth;this$.#finallyDepth = sys.Int.increment(this$.#finallyDepth); return $_u163; })(this);
    return;
  }

  exitFinally(stmt) {
    ((this$) => { let $_u164 = this$.#finallyDepth;this$.#finallyDepth = sys.Int.decrement(this$.#finallyDepth); return $_u164; })(this);
    return;
  }

  visitStmt(stmt) {
    let $_u165 = stmt.id();
    if (sys.ObjUtil.equals($_u165, StmtId.expr())) {
      this.checkExprStmt(sys.ObjUtil.coerce(stmt, ExprStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u165, StmtId.localDef())) {
      this.checkLocalDef(sys.ObjUtil.coerce(stmt, LocalDefStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u165, StmtId.ifStmt())) {
      this.checkIf(sys.ObjUtil.coerce(stmt, IfStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u165, StmtId.returnStmt())) {
      this.checkReturn(sys.ObjUtil.coerce(stmt, ReturnStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u165, StmtId.throwStmt())) {
      this.checkThrow(sys.ObjUtil.coerce(stmt, ThrowStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u165, StmtId.forStmt())) {
      this.checkFor(sys.ObjUtil.coerce(stmt, ForStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u165, StmtId.whileStmt())) {
      this.checkWhile(sys.ObjUtil.coerce(stmt, WhileStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u165, StmtId.breakStmt())) {
      this.checkBreak(sys.ObjUtil.coerce(stmt, BreakStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u165, StmtId.continueStmt())) {
      this.checkContinue(sys.ObjUtil.coerce(stmt, ContinueStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u165, StmtId.tryStmt())) {
      this.checkTry(sys.ObjUtil.coerce(stmt, TryStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u165, StmtId.switchStmt())) {
      this.checkSwitch(sys.ObjUtil.coerce(stmt, SwitchStmt.type$));
    }
    ;
    return null;
  }

  checkExprStmt(stmt) {
    if (!stmt.expr().isStmt()) {
      this.err("Not a statement", stmt.expr().loc());
    }
    ;
    return;
  }

  checkLocalDef(stmt) {
    let t = stmt.ctype();
    if (t.isVoid()) {
      this.err("Cannot use Void as local variable type", stmt.loc());
      return;
    }
    ;
    if (t.isThis()) {
      this.err("Cannot use This as local variable type", stmt.loc());
      return;
    }
    ;
    this.checkValidType(stmt.loc(), sys.ObjUtil.coerce(t, CType.type$));
    return;
  }

  checkIf(stmt) {
    const this$ = this;
    stmt.condition(this.coerce(stmt.condition(), sys.ObjUtil.coerce(this.ns().boolType(), CType.type$), () => {
      this$.err(sys.Str.plus(sys.Str.plus("If condition must be Bool, not '", stmt.condition().ctype()), "'"), stmt.condition().loc());
      return;
    }));
    return;
  }

  checkThrow(stmt) {
    const this$ = this;
    stmt.exception(this.coerce(stmt.exception(), sys.ObjUtil.coerce(this.ns().errType(), CType.type$), () => {
      this$.err(sys.Str.plus(sys.Str.plus("Must throw Err, not '", stmt.exception().ctype()), "'"), stmt.exception().loc());
      return;
    }));
    return;
  }

  checkThrowExpr(expr) {
    const this$ = this;
    expr.exception(this.coerce(expr.exception(), sys.ObjUtil.coerce(this.ns().errType(), CType.type$), () => {
      this$.err(sys.Str.plus(sys.Str.plus("Must throw Err, not '", expr.exception().ctype()), "'"), expr.exception().loc());
      return;
    }));
    return;
  }

  checkFor(stmt) {
    const this$ = this;
    if (stmt.condition() != null) {
      stmt.condition(this.coerce(sys.ObjUtil.coerce(stmt.condition(), Expr.type$), sys.ObjUtil.coerce(this.ns().boolType(), CType.type$), () => {
        this$.err(sys.Str.plus(sys.Str.plus("For condition must be Bool, not '", stmt.condition().ctype()), "'"), stmt.condition().loc());
        return;
      }));
    }
    ;
    return;
  }

  checkWhile(stmt) {
    const this$ = this;
    stmt.condition(this.coerce(stmt.condition(), sys.ObjUtil.coerce(this.ns().boolType(), CType.type$), () => {
      this$.err(sys.Str.plus(sys.Str.plus("While condition must be Bool, not '", stmt.condition().ctype()), "'"), stmt.condition().loc());
      return;
    }));
    return;
  }

  checkBreak(stmt) {
    if (stmt.loop() == null) {
      this.err("Break outside of loop (break is implicit in switch)", stmt.loc());
    }
    ;
    if (sys.ObjUtil.compareGT(this.#finallyDepth, 0)) {
      this.err("Cannot leave finally block", stmt.loc());
    }
    ;
    return;
  }

  checkContinue(stmt) {
    if (stmt.loop() == null) {
      this.err("Continue outside of loop", stmt.loc());
    }
    ;
    if (sys.ObjUtil.compareGT(this.#finallyDepth, 0)) {
      this.err("Cannot leave finally block", stmt.loc());
    }
    ;
    return;
  }

  checkReturn(stmt) {
    const this$ = this;
    let ret = this.curMethod().returns();
    if (stmt.expr() == null) {
      if (!ret.isVoid()) {
        this.err("Must return a value from non-Void method", stmt.loc());
      }
      ;
    }
    else {
      if (ret.isVoid()) {
      }
      else {
        if (ret.isThis()) {
          stmt.expr(this.coerce(sys.ObjUtil.coerce(stmt.expr(), Expr.type$), sys.ObjUtil.coerce(this.curType(), CType.type$), () => {
            this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Cannot return '", stmt.expr().toTypeStr()), "' as "), this$.curType()), " This"), stmt.expr().loc());
            return;
          }));
        }
        else {
          if ((this.curMethod().isCovariant() && this.needParameterizedCollectionCoerce(ret))) {
            (ret = this.curMethod().inheritedReturns());
          }
          ;
          stmt.expr(this.coerce(sys.ObjUtil.coerce(stmt.expr(), Expr.type$), ret, () => {
            this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Cannot return '", stmt.expr().toTypeStr()), "' as '"), ret), "'"), stmt.expr().loc());
            return;
          }));
        }
        ;
      }
      ;
    }
    ;
    if ((!stmt.isSynthetic() && this.curType().isClosure() && this.curType().closure().isItBlock())) {
      this.err("Cannot use return inside it-block", stmt.loc());
    }
    ;
    if (sys.ObjUtil.compareGT(this.#finallyDepth, 0)) {
      this.err("Cannot leave finally block", stmt.loc());
    }
    ;
    if ((stmt.expr() != null && sys.ObjUtil.compareGT(this.#protectedRegionDepth, 0) && !stmt.expr().ctype().isVoid())) {
      let v = this.curMethod().vars().find((v) => {
        return sys.ObjUtil.equals(v.name(), "\$return");
      });
      if (v == null) {
        (v = this.curMethod().addLocalVar(sys.ObjUtil.coerce(stmt.expr().ctype(), CType.type$), "\$return", null));
      }
      ;
      stmt.leaveVar(v);
    }
    ;
    return;
  }

  checkTry(stmt) {
    const this$ = this;
    if (stmt.block().isEmpty()) {
      this.err("Try block cannot be empty", stmt.loc());
    }
    ;
    let caught = sys.List.make(CType.type$);
    stmt.catches().each((c) => {
      let errType = c.errType();
      if (errType == null) {
        (errType = this$.ns().errType());
      }
      ;
      this$.checkTypeProtection(sys.ObjUtil.coerce(errType, CType.type$), c.loc());
      if (!errType.fits(sys.ObjUtil.coerce(this$.ns().errType(), CType.type$))) {
        this$.err(sys.Str.plus(sys.Str.plus("Must catch Err, not '", c.errType()), "'"), c.errType().loc());
      }
      else {
        if (errType.fitsAny(caught)) {
          this$.err(sys.Str.plus(sys.Str.plus("Already caught '", errType), "'"), c.loc());
        }
        ;
      }
      ;
      caught.add(sys.ObjUtil.coerce(errType, CType.type$));
      return;
    });
    return;
  }

  checkSwitch(stmt) {
    const this$ = this;
    let dups = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Int"), sys.Type.find("sys::Int"));
    stmt.cases().each((c) => {
      for (let i = 0; sys.ObjUtil.compareLT(i, c.cases().size()); i = sys.Int.increment(i)) {
        let expr = c.cases().get(i);
        this$.checkCompare(expr, stmt.condition());
        let literal = expr.asTableSwitchCase();
        if (literal != null) {
          if (dups.get(sys.ObjUtil.coerce(sys.ObjUtil.coerce(literal, sys.Int.type$), sys.Int.type$.toNullable())) == null) {
            dups.set(sys.ObjUtil.coerce(sys.ObjUtil.coerce(literal, sys.Int.type$), sys.Int.type$.toNullable()), sys.ObjUtil.coerce(sys.ObjUtil.coerce(literal, sys.Int.type$), sys.Int.type$.toNullable()));
          }
          else {
            this$.err("Duplicate case label", expr.loc());
          }
          ;
        }
        ;
      }
      ;
      return;
    });
    return;
  }

  visitExpr(expr) {
    let $_u166 = expr.id();
    if (sys.ObjUtil.equals($_u166, ExprId.typeLiteral())) {
      this.checkTypeLiteral(sys.ObjUtil.coerce(expr, LiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.slotLiteral())) {
      this.checkSlotLiteral(sys.ObjUtil.coerce(expr, SlotLiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.listLiteral())) {
      this.checkListLiteral(sys.ObjUtil.coerce(expr, ListLiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.mapLiteral())) {
      this.checkMapLiteral(sys.ObjUtil.coerce(expr, MapLiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.rangeLiteral())) {
      this.checkRangeLiteral(sys.ObjUtil.coerce(expr, RangeLiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.boolNot())) {
      this.checkBool(sys.ObjUtil.coerce(expr, UnaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.cmpNull()) || sys.ObjUtil.equals($_u166, ExprId.cmpNotNull())) {
      this.checkCompareNull(sys.ObjUtil.coerce(expr, UnaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.assign())) {
      this.checkAssign(sys.ObjUtil.coerce(expr, BinaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.elvis())) {
      this.checkElvis(sys.ObjUtil.coerce(expr, BinaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.boolOr()) || sys.ObjUtil.equals($_u166, ExprId.boolAnd())) {
      this.checkBools(sys.ObjUtil.coerce(expr, CondExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.same()) || sys.ObjUtil.equals($_u166, ExprId.notSame())) {
      this.checkSame(sys.ObjUtil.coerce(expr, BinaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.shortcut())) {
      this.checkShortcut(sys.ObjUtil.coerce(expr, ShortcutExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.call())) {
      this.checkCall(sys.ObjUtil.coerce(expr, CallExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.construction())) {
      this.checkConstruction(sys.ObjUtil.coerce(expr, CallExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.field())) {
      this.checkField(sys.ObjUtil.coerce(expr, FieldExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.thisExpr())) {
      this.checkThis(sys.ObjUtil.coerce(expr, ThisExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.superExpr())) {
      this.checkSuper(sys.ObjUtil.coerce(expr, SuperExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.isExpr()) || sys.ObjUtil.equals($_u166, ExprId.isnotExpr()) || sys.ObjUtil.equals($_u166, ExprId.asExpr()) || sys.ObjUtil.equals($_u166, ExprId.coerce())) {
      this.checkTypeCheck(sys.ObjUtil.coerce(expr, TypeCheckExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.ternary())) {
      this.checkTernary(sys.ObjUtil.coerce(expr, TernaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u166, ExprId.throwExpr())) {
      this.checkThrowExpr(sys.ObjUtil.coerce(expr, ThrowExpr.type$));
    }
    ;
    return expr;
  }

  checkTypeLiteral(expr) {
    this.checkTypeProtection(sys.ObjUtil.coerce(expr.val(), CType.type$), expr.loc());
    return;
  }

  checkSlotLiteral(expr) {
    this.checkSlotProtection(sys.ObjUtil.coerce(expr.slot(), CSlot.type$), expr.loc());
    return;
  }

  checkListLiteral(expr) {
    const this$ = this;
    let listType = sys.ObjUtil.coerce(expr.ctype(), ListType.type$);
    let valType = listType.v();
    expr.vals().each((val,i) => {
      expr.vals().set(i, this$.coerceBoxed(val, valType, () => {
        this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Invalid value type '", val.toTypeStr()), "' for list of '"), valType), "'"), val.loc());
        return;
      }));
      return;
    });
    return;
  }

  checkMapLiteral(expr) {
    const this$ = this;
    let mapType = sys.ObjUtil.coerce(expr.ctype(), MapType.type$);
    let keyType = mapType.k();
    let valType = mapType.v();
    expr.keys().each((key,i) => {
      expr.keys().set(i, this$.coerceBoxed(key, keyType, () => {
        this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Invalid key type '", key.toTypeStr()), "' for map type '"), mapType), "'"), key.loc());
        return;
      }));
      let val = expr.vals().get(i);
      expr.vals().set(i, this$.coerceBoxed(val, valType, () => {
        this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Invalid value type '", val.toTypeStr()), "' for map type '"), mapType), "'"), val.loc());
        return;
      }));
      return;
    });
    return;
  }

  checkRangeLiteral(range) {
    const this$ = this;
    range.start(this.coerce(range.start(), sys.ObjUtil.coerce(this.ns().intType(), CType.type$), () => {
      this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Range must be Int..Int, not '", range.start().ctype()), ".."), range.end().ctype()), "'"), range.loc());
      return;
    }));
    range.end(this.coerce(range.end(), sys.ObjUtil.coerce(this.ns().intType(), CType.type$), () => {
      this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Range must be Int..Int, not '", range.start().ctype()), ".."), range.end().ctype()), "'"), range.loc());
      return;
    }));
    return;
  }

  checkBool(expr) {
    const this$ = this;
    expr.operand(this.coerce(expr.operand(), sys.ObjUtil.coerce(this.ns().boolType(), CType.type$), () => {
      this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Cannot apply '", expr.opToken().symbol()), "' operator to '"), expr.operand().ctype()), "'"), expr.loc());
      return;
    }));
    return;
  }

  checkCompareNull(expr) {
    let t = expr.operand().ctype();
    if (t.isNullable()) {
      return;
    }
    ;
    if ((this.curMethod() != null && this.curMethod().isItBlockCtor())) {
      let field = sys.ObjUtil.as(expr.operand(), FieldExpr.type$);
      if ((field != null && field.target() != null && sys.ObjUtil.equals(field.target().id(), ExprId.thisExpr()) && !field.ctype().isVal())) {
        return;
      }
      ;
    }
    ;
    this.err(sys.Str.plus(sys.Str.plus("Comparison of non-nullable type '", t), "' to null"), expr.loc());
    return;
  }

  checkBools(expr) {
    const this$ = this;
    expr.operands().each((operand,i) => {
      expr.operands().set(i, this$.coerce(operand, sys.ObjUtil.coerce(this$.ns().boolType(), CType.type$), () => {
        this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Cannot apply '", expr.opToken().symbol()), "' operator to '"), operand.ctype()), "'"), operand.loc());
        return;
      }));
      return;
    });
    return;
  }

  checkSame(expr) {
    this.checkCompare(expr.lhs(), expr.rhs());
    if ((expr.lhs().ctype().isVal() || expr.rhs().ctype().isVal())) {
      this.err(sys.Str.plus(sys.Str.plus("Cannot use '", expr.opToken().symbol()), "' operator with value types"), expr.loc());
    }
    ;
    return;
  }

  checkCompare(lhs,rhs) {
    if ((!lhs.ctype().fits(sys.ObjUtil.coerce(rhs.ctype(), CType.type$)) && !rhs.ctype().fits(sys.ObjUtil.coerce(lhs.ctype(), CType.type$)))) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Incomparable types '", lhs.ctype()), "' and '"), rhs.ctype()), "'"), lhs.loc());
      return false;
    }
    ;
    if (lhs.ctype().isVoid()) {
      this.err("Cannot compare to Void type", lhs.loc());
      return false;
    }
    ;
    if (rhs.ctype().isVoid()) {
      this.err("Cannot compare to Void type", rhs.loc());
      return false;
    }
    ;
    return true;
  }

  checkAssign(expr) {
    const this$ = this;
    expr.rhs(this.coerce(expr.rhs(), sys.ObjUtil.coerce(expr.lhs().ctype(), CType.type$), () => {
      this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("'", expr.rhs().toTypeStr()), "' is not assignable to '"), expr.lhs().ctype()), "'"), expr.rhs().loc());
      return;
    }));
    if (!expr.lhs().isAssignable()) {
      this.err("Left hand side is not assignable", expr.lhs().loc());
    }
    ;
    if (expr.lhs().sameVarAs(expr.rhs())) {
      this.err("Self assignment", expr.lhs().loc());
    }
    ;
    if (expr.lhs().id() === ExprId.field()) {
      expr.rhs(sys.ObjUtil.coerce(this.checkAssignField(sys.ObjUtil.coerce(expr.lhs(), FieldExpr.type$), expr.rhs()), Expr.type$));
    }
    ;
    this.checkNoNullSafes(expr.lhs());
    if ((expr.leave() && expr.lhs().assignRequiresTempVar())) {
      expr.tempVar(this.curMethod().addLocalVar(sys.ObjUtil.coerce(expr.lhs().ctype(), CType.type$), null, null));
    }
    ;
    return;
  }

  checkElvis(expr) {
    const this$ = this;
    if (!expr.lhs().ctype().isNullable()) {
      this.err(sys.Str.plus(sys.Str.plus("Cannot use '?:' operator on non-nullable type '", expr.lhs().ctype()), "'"), expr.loc());
    }
    ;
    expr.rhs(this.coerce(expr.rhs(), sys.ObjUtil.coerce(expr.ctype(), CType.type$), () => {
      this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Cannot coerce '", expr.rhs().toTypeStr()), "' to '"), expr.ctype()), "'"), expr.rhs().loc());
      return;
    }));
    return;
  }

  checkNoNullSafes(x) {
    while (sys.ObjUtil.is(x, NameExpr.type$)) {
      let ne = sys.ObjUtil.coerce(x, NameExpr.type$);
      if (ne.isSafe()) {
        this.err("Null-safe operator on left hand side of assignment", x.loc());
      }
      ;
      (x = ne.target());
    }
    ;
    return;
  }

  checkShortcut(shortcut) {
    let $_u167 = shortcut.opToken();
    if (sys.ObjUtil.equals($_u167, Token.eq()) || sys.ObjUtil.equals($_u167, Token.notEq()) || sys.ObjUtil.equals($_u167, Token.gt()) || sys.ObjUtil.equals($_u167, Token.gtEq()) || sys.ObjUtil.equals($_u167, Token.lt()) || sys.ObjUtil.equals($_u167, Token.ltEq()) || sys.ObjUtil.equals($_u167, Token.cmp())) {
      if (!this.checkCompare(sys.ObjUtil.coerce(shortcut.target(), Expr.type$), sys.ObjUtil.coerce(shortcut.args().first(), Expr.type$))) {
        return;
      }
      ;
    }
    ;
    if (shortcut.isAssign()) {
      let lhs = shortcut.target();
      let ret = shortcut.method().returns();
      if (!lhs.isAssignable()) {
        this.err("Target is not assignable", lhs.loc());
      }
      ;
      if ((!ret.isThis() && !lhs.ctype().fits(ret))) {
        this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("'", ret), "' is not assignable to '"), lhs.ctype()), "'"), lhs.loc());
      }
      ;
      if (lhs.id() === ExprId.field()) {
        this.checkAssignField(sys.ObjUtil.coerce(lhs, FieldExpr.type$), shortcut.args().first());
      }
      ;
      this.checkNoNullSafes(lhs);
    }
    ;
    if ((shortcut.leave() && shortcut.isAssign() && shortcut.target().assignRequiresTempVar())) {
      shortcut.tempVar(this.curMethod().addLocalVar(sys.ObjUtil.coerce(shortcut.ctype(), CType.type$), null, null));
    }
    ;
    let indexedAssign = sys.ObjUtil.as(shortcut, IndexedAssignExpr.type$);
    if (indexedAssign != null) {
      let target = sys.ObjUtil.coerce(indexedAssign.target(), ShortcutExpr.type$);
      indexedAssign.scratchA(this.curMethod().addLocalVar(sys.ObjUtil.coerce(target.target().ctype(), CType.type$), null, null));
      indexedAssign.scratchB(this.curMethod().addLocalVar(sys.ObjUtil.coerce(target.args().get(0).ctype(), CType.type$), null, null));
    }
    ;
    if (!shortcut.isCompare()) {
      this.checkCall(shortcut);
    }
    ;
    return;
  }

  checkAssignField(lhs,rhs) {
    let field = sys.ObjUtil.coerce(lhs, FieldExpr.type$).field();
    if ((field.setter() != null && CheckErrors.slotProtectionErr(sys.ObjUtil.coerce(this.curType(), TypeDef.type$), sys.ObjUtil.coerce(field, CSlot.type$)) == null)) {
      this.checkSlotProtection(sys.ObjUtil.coerce(field.setter(), CSlot.type$), lhs.loc(), true);
    }
    ;
    if (!field.isConst()) {
      return rhs;
    }
    ;
    let inType = this.curType();
    let inMethod = this.curMethod();
    if (inType.isClosure()) {
      this.curType().closure().setsConst(true);
      (inType = inType.closure().enclosingType());
      (inMethod = ((this$) => { let $_u168 = sys.ObjUtil.as(this$.curType().closure().enclosingSlot(), MethodDef.type$); if ($_u168 != null) return $_u168; return this$.curMethod(); })(this));
    }
    ;
    if ((field.isStatic() && !inMethod.isStaticInit())) {
      this.err(sys.Str.plus(sys.Str.plus("Cannot set const static field '", field.name()), "' outside of static initializer"), lhs.loc());
      return rhs;
    }
    ;
    if (!(this.curType().isClosure() && this.curType().closure().isItBlock())) {
      if (sys.ObjUtil.compareNE(inType, field.parent())) {
        if ((!inType.fits(field.parent()) || !inMethod.isInstanceCtor())) {
          this.err(sys.Str.plus(sys.Str.plus("Cannot set const field '", field.qname()), "'"), lhs.loc());
          return rhs;
        }
        ;
      }
      ;
      if ((!field.isStatic() && !(inMethod.isInstanceInit() || inMethod.isInstanceCtor()))) {
        this.err(sys.Str.plus(sys.Str.plus("Cannot set const field '", field.name()), "' outside of constructor"), lhs.loc());
        return rhs;
      }
      ;
    }
    ;
    let ftype = field.type();
    if (ftype.isConst()) {
      return rhs;
    }
    else {
      return this.implicitToImmutable(ftype, sys.ObjUtil.coerce(rhs, Expr.type$));
    }
    ;
  }

  implicitToImmutable(fieldType,rhs) {
    if (sys.ObjUtil.equals(rhs.id(), ExprId.nullLiteral())) {
      return rhs;
    }
    ;
    let call = CallExpr.makeWithMethod(rhs.loc(), rhs, sys.ObjUtil.coerce(this.ns().objToImmutable(), CMethod.type$));
    call.isSafe(fieldType.isNullable());
    if (fieldType.toNonNullable().isObj()) {
      return call;
    }
    ;
    return TypeCheckExpr.coerce(call, fieldType);
  }

  checkConstruction(call) {
    if (!call.method().isCtor()) {
      this.warn(sys.Str.plus(sys.Str.plus("Using static method '", call.method().qname()), "' as constructor"), call.loc());
      if (sys.ObjUtil.compareNE(call.ctype().toNonNullable(), call.method().returns().toNonNullable())) {
        this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Construction method '", call.method().qname()), "' must return '"), call.ctype().name()), "'"), call.loc());
      }
      ;
      call.ctype(call.method().returns());
    }
    ;
    this.checkCall(call);
    return;
  }

  checkCall(call) {
    const this$ = this;
    let m = call.method();
    if (m == null) {
      this.err("Something wrong with method call?", call.loc());
      return;
    }
    ;
    let name = m.name();
    if ((call.target() != null && call.target().ctype().isVoid())) {
      this.err("Cannot call method on Void", call.loc());
      return;
    }
    ;
    this.checkSlotProtection(sys.ObjUtil.coerce(call.method(), CSlot.type$), call.loc());
    if (m.isForeign()) {
      if (!m.returns().isSupported()) {
        this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Method '", name), "' uses unsupported type '"), m.returns()), "'"), call.loc());
        return;
      }
      ;
      let unsupported = m.params().find((p) => {
        return !p.type().isSupported();
      });
      if (unsupported != null) {
        this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Method '", name), "' uses unsupported type '"), unsupported.type()), "'"), call.loc());
        return;
      }
      ;
    }
    ;
    if (!call.isDynamic()) {
      this.checkArgs(call);
    }
    else {
      call.args().each((arg,i) => {
        call.args().set(i, this$.box(call.args().get(i)));
        return;
      });
    }
    ;
    if ((m.isCtor() && !call.isCtorChain())) {
      if ((call.target() != null && call.target().id() !== ExprId.staticTarget() && !call.target().synthetic())) {
        this.err(sys.Str.plus(sys.Str.plus("Cannot call constructor '", name), "' on instance"), call.loc());
      }
      ;
      if ((m.parent().isAbstract() && !m.isStatic())) {
        this.err("Calling constructor on abstract class", call.loc());
      }
      ;
    }
    ;
    if (m.isStatic()) {
      if ((call.target() != null && call.target().id() !== ExprId.staticTarget())) {
        this.err(sys.Str.plus(sys.Str.plus("Cannot call static method '", name), "' on instance"), call.loc());
      }
      ;
    }
    ;
    if ((!m.isStatic() && !m.isCtor())) {
      if ((call.target() == null || call.target().id() === ExprId.staticTarget())) {
        this.err(sys.Str.plus(sys.Str.plus("Cannot call instance method '", name), "' in static context"), call.loc());
      }
      ;
    }
    ;
    if ((call.target() != null && call.target().id() === ExprId.superExpr())) {
      if (m.isAbstract()) {
        this.err(sys.Str.plus(sys.Str.plus("Cannot use super to call abstract method '", m.qname()), "'"), call.target().loc());
      }
      ;
      if ((sys.ObjUtil.compareNE(call.args().size(), m.params().size()) && sys.ObjUtil.equals(m.name(), this.curMethod().name()) && !m.isInstanceCtor())) {
        this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Must call super method '", m.qname()), "' with exactly "), sys.ObjUtil.coerce(m.params().size(), sys.Obj.type$.toNullable())), " arguments"), call.target().loc());
      }
      ;
    }
    ;
    if ((call.isSafe() && call.target() != null && !call.target().ctype().isNullable())) {
      this.err(sys.Str.plus(sys.Str.plus("Cannot use null-safe call on non-nullable type '", call.target().ctype()), "'"), call.target().loc());
    }
    ;
    if ((!call.isSafe() && sys.ObjUtil.is(call.target(), NameExpr.type$) && sys.ObjUtil.coerce(call.target(), NameExpr.type$).isSafe())) {
      this.err("Non-null safe call chained after null safe call", call.loc());
      return;
    }
    ;
    if ((call.target() != null && !call.isCompare() && !call.isSafe() && !call.method().isStatic())) {
      if ((call.target().ctype().isVal() || call.method().parent().isVal())) {
        if (sys.ObjUtil.equals(name, "with")) {
          return this.err("Cannot call 'Obj.with' on value type", call.target().loc());
        }
        ;
        call.target(this.coerce(sys.ObjUtil.coerce(call.target(), Expr.type$), call.method().parent(), () => {
          this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Cannot coerce '", call.target().ctype()), "' to '"), call.method().parent()), "'"), call.target().loc());
          return;
        }));
      }
      ;
    }
    ;
    if ((call.isCallOp() && !call.target().ctype().isFunc())) {
      this.err(sys.Str.plus(sys.Str.plus("Cannot use () call operator on non-func type '", call.target().ctype()), "'"), call.target().loc());
    }
    ;
    if ((call.isItAdd() && !call.method().hasFacet("sys::Operator"))) {
      this.err(sys.Str.plus("Missing Operator facet: ", call.method().qname()), call.loc());
    }
    ;
    return;
  }

  checkField(f) {
    let field = f.field();
    this.checkSlotProtection(sys.ObjUtil.coerce(field, CSlot.type$), f.loc());
    if (!field.type().isSupported()) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Field '", field.name()), "' has unsupported type '"), field.type()), "'"), f.loc());
      return;
    }
    ;
    if (field.isStatic()) {
      if ((f.target() != null && f.target().id() !== ExprId.staticTarget())) {
        this.err(sys.Str.plus(sys.Str.plus("Cannot access static field '", f.name()), "' on instance"), f.loc());
      }
      ;
    }
    else {
      if ((f.target() == null || f.target().id() === ExprId.staticTarget())) {
        this.err(sys.Str.plus(sys.Str.plus("Cannot access instance field '", f.name()), "' in static context"), f.loc());
      }
      ;
    }
    ;
    if ((f.target() != null && f.target().id() === ExprId.superExpr())) {
      if (field.isAbstract()) {
        this.err(sys.Str.plus(sys.Str.plus("Cannot use super to access abstract field '", field.qname()), "'"), f.target().loc());
      }
      ;
    }
    ;
    if ((f.isSafe() && f.target() != null && !f.target().ctype().isNullable())) {
      this.err(sys.Str.plus(sys.Str.plus("Cannot use null-safe access on non-nullable type '", f.target().ctype()), "'"), f.target().loc());
    }
    ;
    if ((!f.isSafe() && sys.ObjUtil.is(f.target(), NameExpr.type$) && sys.ObjUtil.coerce(f.target(), NameExpr.type$).isSafe())) {
      this.err("Non-null safe field access chained after null safe call", f.loc());
      return;
    }
    ;
    if (f.useAccessor()) {
      f.useAccessor(this.useFieldAccessor(sys.ObjUtil.coerce(field, CField.type$)));
      if ((this.curMethod() != null && (field.getter() === this.curMethod() || field.setter() === this.curMethod()) && (f.target() == null || sys.ObjUtil.equals(f.target().id(), ExprId.thisExpr())))) {
        this.err("Cannot use field accessor inside accessor itself - use '&' operator", f.loc());
      }
      ;
    }
    else {
      let enclosing = ((this$) => { if (this$.curType().isClosure()) return this$.curType().closure().enclosingType(); return this$.curType(); })(this);
      if ((!field.isConst() && sys.ObjUtil.compareNE(field.parent(), this.curType()) && sys.ObjUtil.compareNE(field.parent(), enclosing))) {
        this.err(sys.Str.plus(sys.Str.plus("Field storage for '", field.qname()), "' not accessible"), f.loc());
        return;
      }
      ;
      if (!field.isStorage()) {
        if ((sys.ObjUtil.is(field, FieldDef.type$) && sys.ObjUtil.coerce(field, FieldDef.type$).concreteBase() != null)) {
          this.err(sys.Str.plus(sys.Str.plus("Field storage of inherited field '", sys.ObjUtil.trap(sys.ObjUtil.trap(field,"concreteBase", sys.List.make(sys.Obj.type$.toNullable(), [])),"qname", sys.List.make(sys.Obj.type$.toNullable(), []))), "' not accessible (might try super)"), f.loc());
        }
        else {
          this.err(sys.Str.plus(sys.Str.plus("Invalid storage access of field '", field.qname()), "' which doesn't have storage"), f.loc());
        }
        ;
        return;
      }
      ;
      if ((enclosing.isMixin() && !this.curMethod().isSynthetic())) {
        this.err(sys.Str.plus(sys.Str.plus("Field storage not accessible in mixin '", field.qname()), "'"), f.loc());
        return;
      }
      ;
    }
    ;
    return;
  }

  useFieldAccessor(f) {
    if ((f.isConst() || f.getter() == null)) {
      return false;
    }
    ;
    let def = sys.ObjUtil.as(f, FieldDef.type$);
    if (def == null) {
      return true;
    }
    ;
    if ((def.isVirtual() || def.isOverride() || f.isNative())) {
      return true;
    }
    ;
    if ((!def.hasGet() && !def.hasSet())) {
      return false;
    }
    ;
    return true;
  }

  checkThis(expr) {
    if (this.inStatic()) {
      this.err("Cannot access 'this' in static context", expr.loc());
    }
    ;
    return;
  }

  checkSuper(expr) {
    if (this.inStatic()) {
      this.err("Cannot access 'super' in static context", expr.loc());
    }
    ;
    if (this.curType().isMixin()) {
      if (expr.explicitType() == null) {
        this.err("Must use named 'super' inside mixin", expr.loc());
      }
      else {
        if (!expr.explicitType().isMixin()) {
          this.err("Cannot use 'Obj.super' inside mixin (yeah I know - take it up with Sun)", expr.loc());
        }
        ;
      }
      ;
    }
    ;
    if (expr.explicitType() != null) {
      if (expr.explicitType().isClass()) {
        this.err(sys.Str.plus(sys.Str.plus("Cannot use named super on class type '", expr.explicitType()), "'"), expr.loc());
      }
      ;
      if (!this.curType().fits(sys.ObjUtil.coerce(expr.explicitType(), CType.type$))) {
        this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Named super '", expr.explicitType()), "' not a super class of '"), this.curType().name()), "'"), expr.loc());
      }
      ;
    }
    ;
    return;
  }

  checkTypeCheck(expr) {
    if (expr.synthetic()) {
      return;
    }
    ;
    this.checkTypeProtection(expr.check(), expr.loc());
    let check = expr.check();
    let target = expr.target().ctype();
    if ((!check.fits(sys.ObjUtil.coerce(target, CType.type$)) && !target.fits(check) && !check.isMixin() && !target.isMixin())) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Inconvertible types '", target), "' and '"), check), "'"), expr.loc());
    }
    ;
    if ((sys.ObjUtil.compareNE(expr.id(), ExprId.coerce()) && target.isVal())) {
      expr.target(this.box(expr.target()));
    }
    ;
    if ((expr.id() === ExprId.asExpr() && check.isNullable())) {
      this.err(sys.Str.plus(sys.Str.plus("Cannot use 'as' operator with nullable type '", check), "'"), expr.loc());
    }
    ;
    return;
  }

  checkTernary(expr) {
    const this$ = this;
    expr.condition(this.coerce(expr.condition(), sys.ObjUtil.coerce(this.ns().boolType(), CType.type$), () => {
      this$.err(sys.Str.plus(sys.Str.plus("Ternary condition must be Bool, not '", expr.condition().ctype()), "'"), expr.condition().loc());
      return;
    }));
    expr.trueExpr(this.coerce(expr.trueExpr(), sys.ObjUtil.coerce(expr.ctype(), CType.type$), () => {
      this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus("Ternary true expr '", expr.trueExpr().toTypeStr()), "' cannot be coerced to "), expr.ctype()), expr.trueExpr().loc());
      return;
    }));
    expr.falseExpr(this.coerce(expr.falseExpr(), sys.ObjUtil.coerce(expr.ctype(), CType.type$), () => {
      this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus("Ternary false expr '", expr.falseExpr().toTypeStr()), "' cannot be coerced to "), expr.ctype()), expr.falseExpr().loc());
      return;
    }));
    return;
  }

  checkArgs(call) {
    const this$ = this;
    let method = call.method();
    let base = ((this$) => { if (call.target() == null) return method.parent(); return call.target().ctype(); })(this);
    let name = call.name();
    let args = call.args();
    let newArgs = args.dup();
    let isErr = false;
    let params = method.params();
    let genericParams = ((this$) => { if (method.isParameterized()) return method.generic().params(); return null; })(this);
    let sig = sys.ObjUtil.as(method.parent(), FuncType.type$);
    if ((sig != null && sys.ObjUtil.equals(name, "call"))) {
      if (sys.ObjUtil.compareNE(sig.params().size(), args.size())) {
        (isErr = true);
      }
      else {
        sig.params().each((p,i) => {
          newArgs.set(i, this$.coerceBoxed(args.get(i), p, () => {
            (isErr = true);
            return;
          }));
          return;
        });
      }
      ;
    }
    else {
      if (sys.ObjUtil.compareLT(params.size(), args.size())) {
        (isErr = true);
      }
      else {
        params.each((p,i) => {
          if (sys.ObjUtil.compareGE(i, args.size())) {
            if (!p.hasDefault()) {
              (isErr = true);
            }
            ;
          }
          else {
            let pt = p.type().parameterizeThis(sys.ObjUtil.coerce(base, CType.type$));
            newArgs.set(i, this$.coerce(args.get(i), pt, () => {
              (isErr = sys.ObjUtil.compareNE(name, "compare"));
              return;
            }));
            if ((!isErr && genericParams != null && genericParams.get(i).type().isGenericParameter())) {
              newArgs.set(i, this$.box(newArgs.get(i)));
            }
            ;
          }
          ;
          return;
        });
      }
      ;
    }
    ;
    if (!isErr) {
      call.args(newArgs);
      return;
    }
    ;
    let msg = "Invalid args ";
    if (sig != null) {
      msg = sys.Str.plus(msg, sys.Str.plus(sys.Str.plus("|", sig.params().join(", ")), "|"));
    }
    else {
      msg = sys.Str.plus(msg, sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("", name), "("), params.join(", ", (p) => {
        return CheckErrors.paramTypeStr(sys.ObjUtil.coerce(base, CType.type$), p);
      })), ")"));
    }
    ;
    msg = sys.Str.plus(msg, sys.Str.plus(sys.Str.plus(", not (", args.join(", ", (e) => {
      return sys.Str.plus("", e.toTypeStr());
    })), ")"));
    this.err(msg, call.loc());
    return;
  }

  static paramTypeStr(base,param) {
    return param.type().parameterizeThis(base).inferredAs().signature();
  }

  checkValidType(loc,t) {
    if (!t.isValid()) {
      this.err(sys.Str.plus(sys.Str.plus("Invalid type '", t), "'"), loc);
    }
    ;
    return;
  }

  checkProtectionFlags(flags,loc) {
    let isPublic = sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Public()), 0);
    let isProtected = sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Protected()), 0);
    let isPrivate = sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Private()), 0);
    let isInternal = sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Internal()), 0);
    let isVirtual = sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Virtual()), 0);
    let isOverride = sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Override()), 0);
    if (isPublic) {
      if (isProtected) {
        this.err("Invalid combination of 'public' and 'protected' modifiers", loc);
      }
      ;
      if (isPrivate) {
        this.err("Invalid combination of 'public' and 'private' modifiers", loc);
      }
      ;
      if (isInternal) {
        this.err("Invalid combination of 'public' and 'internal' modifiers", loc);
      }
      ;
    }
    else {
      if (isProtected) {
        if (isPrivate) {
          this.err("Invalid combination of 'protected' and 'private' modifiers", loc);
        }
        ;
        if (isInternal) {
          this.err("Invalid combination of 'protected' and 'internal' modifiers", loc);
        }
        ;
      }
      else {
        if (isPrivate) {
          if (isInternal) {
            this.err("Invalid combination of 'private' and 'internal' modifiers", loc);
          }
          ;
          if ((isVirtual && !isOverride)) {
            this.err("Invalid combination of 'private' and 'virtual' modifiers", loc);
          }
          ;
        }
        ;
      }
      ;
    }
    ;
    return;
  }

  checkTypeProtection(t,loc) {
    const this$ = this;
    (t = t.toNonNullable());
    if (sys.ObjUtil.is(t, GenericType.type$)) {
      if (sys.ObjUtil.is(t, ListType.type$)) {
        let x = sys.ObjUtil.coerce(t, ListType.type$);
        this.checkTypeProtection(x.v(), loc);
      }
      else {
        if (sys.ObjUtil.is(t, MapType.type$)) {
          let x = sys.ObjUtil.coerce(t, MapType.type$);
          this.checkTypeProtection(x.k(), loc);
          this.checkTypeProtection(x.v(), loc);
        }
        else {
          let x = sys.ObjUtil.coerce(t, FuncType.type$);
          this.checkTypeProtection(x.returns(), loc);
          x.params().each((p) => {
            this$.checkTypeProtection(p, loc);
            return;
          });
        }
        ;
      }
      ;
    }
    else {
      if ((t.isInternal() && sys.ObjUtil.compareNE(t.pod(), this.curType().pod()))) {
        this.err(sys.Str.plus(sys.Str.plus("Internal type '", t), "' not accessible"), loc);
      }
      ;
    }
    ;
    this.checkDeprecated(t, loc);
    return;
  }

  checkSlotProtection(slot,loc,setter) {
    if (setter === undefined) setter = false;
    let errMsg = CheckErrors.slotProtectionErr(sys.ObjUtil.coerce(this.curType(), TypeDef.type$), slot, setter);
    if (errMsg != null) {
      this.err(sys.ObjUtil.coerce(errMsg, sys.Str.type$), loc);
    }
    ;
    this.checkDeprecated(slot, loc);
    return;
  }

  static isSlotVisible(curType,slot) {
    return CheckErrors.slotProtectionErr(curType, slot) == null;
  }

  static slotProtectionErr(curType,slot,setter) {
    if (setter === undefined) setter = false;
    let msg = ((this$) => { if (setter) return "setter of field"; return ((this$) => { if (sys.ObjUtil.is(slot, CMethod.type$)) return "method"; return "field"; })(this$); })(this);
    if (sys.ObjUtil.equals(curType, slot.parent())) {
      return null;
    }
    ;
    if (curType.isClosure()) {
      (curType = curType.closure().enclosingType());
    }
    ;
    let isInternal = (slot.isInternal() || (slot.parent().isInternal() && !slot.parent().isParameterized()));
    if ((slot.isPrivate() && sys.ObjUtil.compareNE(curType, slot.parent()))) {
      return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Private ", msg), " '"), slot.qname()), "' not accessible");
    }
    else {
      if ((slot.isProtected() && !curType.fits(slot.parent()) && sys.ObjUtil.compareNE(curType.pod(), slot.parent().pod()))) {
        return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Protected ", msg), " '"), slot.qname()), "' not accessible");
      }
      else {
        if ((isInternal && sys.ObjUtil.compareNE(curType.pod(), slot.parent().pod()))) {
          return sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Internal ", msg), " '"), slot.qname()), "' not accessible");
        }
        else {
          return null;
        }
        ;
      }
      ;
    }
    ;
  }

  checkDeprecated(target,loc) {
    let slot = sys.ObjUtil.as(target, CSlot.type$);
    if ((this.curMethod() != null && this.curMethod().isSynthetic())) {
      if (this.curMethod().isFieldAccessor()) {
        return;
      }
      ;
      if ((slot != null && sys.ObjUtil.equals(slot.parent(), this.curType()))) {
        return;
      }
      ;
    }
    ;
    if (this.curType().facet("sys::Deprecated") != null) {
      return;
    }
    ;
    let f = null;
    if (slot != null) {
      (f = slot.facet("sys::Deprecated"));
      if (f == null) {
        (target = slot.parent());
      }
      ;
    }
    ;
    if (f == null) {
      (f = sys.ObjUtil.coerce(target, CType.type$).facet("sys::Deprecated"));
    }
    ;
    if (f == null) {
      return;
    }
    ;
    let kind = ((this$) => { if (sys.ObjUtil.is(target, CType.type$)) return "type"; return "slot"; })(this);
    let qname = sys.ObjUtil.coerce(sys.ObjUtil.trap(target,"qname", sys.List.make(sys.Obj.type$.toNullable(), [])), sys.Str.type$);
    let msg = ((this$) => { let $_u175 = sys.ObjUtil.as(f.get("msg"), sys.Str.type$); if ($_u175 != null) return $_u175; return ""; })(this);
    if (!sys.Str.isEmpty(msg)) {
      this.warn(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Deprecated ", kind), " '"), qname), "' - "), msg), loc);
    }
    else {
      this.warn(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Deprecated ", kind), " '"), qname), "'"), loc);
    }
    ;
    return;
  }

  box(expr) {
    return this.#coercer.box(expr);
  }

  coerceBoxed(expr,expected,onErr) {
    return this.#coercer.coerceBoxed(expr, expected, onErr);
  }

  coerce(expr,expected,onErr) {
    return this.#coercer.coerce(expr, expected, onErr);
  }

}

class ResolveDepends extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ResolveDepends.type$; }

  #loc = null;

  loc(it) {
    if (it === undefined) {
      return this.#loc;
    }
    else {
      this.#loc = it;
      return;
    }
  }

  static make(compiler) {
    const $self = new ResolveDepends();
    ResolveDepends.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    $self.#loc = compiler.input().inputLoc();
    return;
  }

  run() {
    const this$ = this;
    this.log().debug("ResolveDepends");
    let input = this.compiler().input();
    let myName = input.podName();
    let isSys = sys.ObjUtil.equals(myName, "sys");
    if ((this.compiler().depends().isEmpty() && !isSys)) {
      this.compiler().depends().add(CDepend.fromStr("sys 0+"));
    }
    ;
    this.ns().depends(sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::CDepend")));
    this.compiler().depends().each((cdepend) => {
      this$.ns().depends().set(cdepend.name(), cdepend);
      cdepend.pod(this$.resolveDepend(cdepend.depend()));
      return;
    });
    if ((!this.ns().depends().containsKey("sys") && !isSys)) {
      this.err("All pods must have a dependency on 'sys'", this.#loc);
    }
    ;
    this.bombIfErr();
    this.ns().depends().each((myDepend) => {
      if (sys.ObjUtil.equals(myDepend.name(), myName)) {
        this$.err(sys.Str.plus(sys.Str.plus("Cyclic dependency on self '", myName), "'"), this$.#loc);
      }
      ;
      myDepend.pod().depends().each((podDepend) => {
        if (sys.ObjUtil.equals(podDepend.name(), myName)) {
          this$.err(sys.Str.plus(sys.Str.plus("Cyclic dependency on '", myDepend.name()), "'"), this$.#loc);
        }
        ;
        return;
      });
      return;
    });
    this.bombIfErr();
    return;
  }

  resolveDepend(depend) {
    let pod = null;
    try {
      (pod = this.ns().resolvePod(depend.name(), this.#loc));
    }
    catch ($_u176) {
      $_u176 = sys.Err.make($_u176);
      if ($_u176 instanceof CompilerErr) {
        let e = $_u176;
        ;
        this.err(sys.Str.plus(sys.Str.plus("Cannot resolve depend: pod '", depend.name()), "' not found"), this.#loc);
        return null;
      }
      else {
        throw $_u176;
      }
    }
    ;
    if (!depend.match(pod.version())) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Cannot resolve depend: '", pod.name()), " "), pod.version()), "' != '"), depend), "'"), this.#loc);
    }
    ;
    return pod;
  }

}

class ResolveExpr extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
    this.#stmtStack = sys.List.make(Stmt.type$);
    this.#blockStack = sys.List.make(Block.type$);
    this.#inClosure = false;
    return;
  }

  typeof() { return ResolveExpr.type$; }

  #stmtStack = null;

  stmtStack(it) {
    if (it === undefined) {
      return this.#stmtStack;
    }
    else {
      this.#stmtStack = it;
      return;
    }
  }

  #blockStack = null;

  blockStack(it) {
    if (it === undefined) {
      return this.#blockStack;
    }
    else {
      this.#blockStack = it;
      return;
    }
  }

  #inClosure = false;

  inClosure(it) {
    if (it === undefined) {
      return this.#inClosure;
    }
    else {
      this.#inClosure = it;
      return;
    }
  }

  #coercer = null;

  coercer(it) {
    if (it === undefined) {
      return this.#coercer;
    }
    else {
      this.#coercer = it;
      return;
    }
  }

  static make(compiler) {
    const $self = new ResolveExpr();
    ResolveExpr.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    ;
    $self.#coercer = Coercer.make(compiler);
    return;
  }

  run() {
    this.log().debug("ResolveExpr");
    this.walk(this.compiler(), VisitDepth.expr());
    this.bombIfErr();
    return;
  }

  enterMethodDef(m) {
    CompilerStep.prototype.enterMethodDef.call(this, m);
    this.#inClosure = (this.curType().isClosure() && this.curType().closure().doCall() === m);
    this.initMethodVars();
    return;
  }

  enterStmt(stmt) {
    this.#stmtStack.push(stmt);
    return;
  }

  visitStmt(stmt) {
    this.#stmtStack.pop();
    let $_u177 = stmt.id();
    if (sys.ObjUtil.equals($_u177, StmtId.expr())) {
      this.resolveExprStmt(sys.ObjUtil.coerce(stmt, ExprStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u177, StmtId.forStmt())) {
      this.resolveFor(sys.ObjUtil.coerce(stmt, ForStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u177, StmtId.breakStmt())) {
      this.resolveBreak(sys.ObjUtil.coerce(stmt, BreakStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u177, StmtId.continueStmt())) {
      this.resolveContinue(sys.ObjUtil.coerce(stmt, ContinueStmt.type$));
    }
    else if (sys.ObjUtil.equals($_u177, StmtId.localDef())) {
      this.resolveLocalVarDef(sys.ObjUtil.coerce(stmt, LocalDefStmt.type$));
    }
    ;
    return null;
  }

  resolveExprStmt(stmt) {
    stmt.expr(stmt.expr().noLeave());
    return;
  }

  resolveLocalVarDef(def) {
    if (def.ctype() == null) {
      def.ctype(def.init().ctype().inferredAs());
    }
    ;
    this.bindToMethodVar(def);
    if ((def.init() == null && !def.isCatchVar())) {
      def.init(LiteralExpr.makeDefaultLiteral(def.loc(), this.ns(), sys.ObjUtil.coerce(def.ctype(), CType.type$)));
    }
    ;
    if (def.init() != null) {
      def.init(BinaryExpr.makeAssign(LocalVarExpr.make(def.loc(), def.var()), sys.ObjUtil.coerce(def.init(), Expr.type$)));
    }
    ;
    return;
  }

  resolveFor(stmt) {
    if (stmt.update() != null) {
      stmt.update(stmt.update().noLeave());
    }
    ;
    return;
  }

  resolveBreak(stmt) {
    stmt.loop(this.findLoop());
    return;
  }

  resolveContinue(stmt) {
    stmt.loop(this.findLoop());
    return;
  }

  visitExpr(expr) {
    (expr = this.resolveExpr(expr));
    if (sys.ObjUtil.coerce(expr.ctype(), sys.Obj.type$.toNullable()) == null) {
      throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus("Expr type not resolved: ", expr.id()), ": "), expr), expr.loc());
    }
    ;
    if (expr.ctype().isGenericParameter()) {
      expr.ctype(expr.ctype().raw());
    }
    ;
    let assignTarget = sys.ObjUtil.as(expr.assignTarget(), LocalVarExpr.type$);
    if ((assignTarget != null && assignTarget.var() != null)) {
      assignTarget.var().reassigned();
    }
    ;
    return expr;
  }

  resolveExpr(expr) {
    let $_u178 = expr.id();
    if (sys.ObjUtil.equals($_u178, ExprId.localeLiteral())) {
      return this.resolveLocaleLiteral(sys.ObjUtil.coerce(expr, LocaleLiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.slotLiteral())) {
      return this.resolveSlotLiteral(sys.ObjUtil.coerce(expr, SlotLiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.listLiteral())) {
      return this.resolveList(sys.ObjUtil.coerce(expr, ListLiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.mapLiteral())) {
      return this.resolveMap(sys.ObjUtil.coerce(expr, MapLiteralExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.boolNot()) || sys.ObjUtil.equals($_u178, ExprId.cmpNull()) || sys.ObjUtil.equals($_u178, ExprId.cmpNotNull())) {
      expr.ctype(this.ns().boolType());
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.assign())) {
      return this.resolveAssign(sys.ObjUtil.coerce(expr, BinaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.elvis())) {
      this.resolveElvis(sys.ObjUtil.coerce(expr, BinaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.same()) || sys.ObjUtil.equals($_u178, ExprId.notSame()) || sys.ObjUtil.equals($_u178, ExprId.boolOr()) || sys.ObjUtil.equals($_u178, ExprId.boolAnd()) || sys.ObjUtil.equals($_u178, ExprId.isExpr())) {
      expr.ctype(this.ns().boolType());
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.isnotExpr())) {
      expr.ctype(this.ns().boolType());
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.asExpr())) {
      expr.ctype(sys.ObjUtil.coerce(expr, TypeCheckExpr.type$).check().toNullable());
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.call())) {
      return this.resolveCall(sys.ObjUtil.coerce(expr, CallExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.construction())) {
      return this.resolveConstruction(sys.ObjUtil.coerce(expr, CallExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.shortcut())) {
      return this.resolveShortcut(sys.ObjUtil.coerce(expr, ShortcutExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.thisExpr())) {
      return this.resolveThis(sys.ObjUtil.coerce(expr, ThisExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.superExpr())) {
      return this.resolveSuper(sys.ObjUtil.coerce(expr, SuperExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.itExpr())) {
      return this.resolveIt(sys.ObjUtil.coerce(expr, ItExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.unknownVar())) {
      return this.resolveVar(sys.ObjUtil.coerce(expr, UnknownVarExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.storage())) {
      return this.resolveStorage(sys.ObjUtil.coerce(expr, UnknownVarExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.coerce())) {
      expr.ctype(sys.ObjUtil.coerce(expr, TypeCheckExpr.type$).check());
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.ternary())) {
      this.resolveTernary(sys.ObjUtil.coerce(expr, TernaryExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.closure())) {
      this.resolveClosure(sys.ObjUtil.coerce(expr, ClosureExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.dsl())) {
      return this.resolveDsl(sys.ObjUtil.coerce(expr, DslExpr.type$));
    }
    else if (sys.ObjUtil.equals($_u178, ExprId.throwExpr())) {
      expr.ctype(this.ns().nothingType());
    }
    ;
    return expr;
  }

  resolveLocaleLiteral(expr) {
    let loc = expr.loc();
    if ((expr.podName() != null && expr.def() != null)) {
      this.err("Locale literal cannot specify both qualified pod and default value", loc);
    }
    ;
    let outputMode = this.compiler().input().output();
    if ((expr.podName() == null && sys.ObjUtil.compareNE(outputMode, CompilerOutputMode.podFile()))) {
      this.err("Scripts cannot define non-qualified locale literals", loc);
    }
    ;
    if (expr.def() != null) {
      this.compiler().localeDefs().add(expr);
    }
    ;
    let inType = this.curType();
    if (inType.isClosure()) {
      (inType = inType.closure().enclosingType());
    }
    ;
    let podTarget = ((this$) => { if (expr.podName() != null) return CallExpr.makeWithMethod(loc, null, sys.ObjUtil.coerce(this$.ns().podFind(), CMethod.type$), sys.List.make(LiteralExpr.type$, [LiteralExpr.makeStr(loc, this$.ns(), sys.ObjUtil.coerce(expr.podName(), sys.Str.type$))])); return CallExpr.makeWithMethod(loc, LiteralExpr.make(loc, ExprId.typeLiteral(), sys.ObjUtil.coerce(this$.ns().typeType(), CType.type$), inType), sys.ObjUtil.coerce(this$.ns().typePod(), CMethod.type$)); })(this);
    let args = sys.List.make(LiteralExpr.type$, [LiteralExpr.makeStr(loc, this.ns(), expr.key())]);
    if (expr.def() != null) {
      args.add(LiteralExpr.makeStr(loc, this.ns(), sys.ObjUtil.coerce(expr.def(), sys.Str.type$)));
    }
    ;
    return CallExpr.makeWithMethod(loc, podTarget, sys.ObjUtil.coerce(this.ns().podLocale(), CMethod.type$), args);
  }

  resolveSlotLiteral(expr) {
    let slot = expr.parent().slot(expr.name());
    if (slot == null) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Unknown slot literal '", expr.parent().signature()), "."), expr.name()), "'"), expr.loc());
      expr.ctype(this.ns().error());
      return expr;
    }
    ;
    expr.ctype(((this$) => { if (sys.ObjUtil.is(slot, CField.type$)) return this$.ns().fieldType(); return this$.ns().methodType(); })(this));
    expr.slot(slot);
    return expr;
  }

  resolveList(expr) {
    if (expr.explicitType() != null) {
      expr.ctype(expr.explicitType());
    }
    else {
      let v = Expr.commonType(this.ns(), expr.vals());
      expr.ctype(v.toListOf());
    }
    ;
    return expr;
  }

  resolveMap(expr) {
    if (expr.explicitType() != null) {
      expr.ctype(expr.explicitType());
    }
    else {
      let k = Expr.commonType(this.ns(), expr.keys()).toNonNullable();
      let v = Expr.commonType(this.ns(), expr.vals());
      expr.ctype(MapType.make(k, v));
    }
    ;
    return expr;
  }

  resolveThis(expr) {
    if (this.#inClosure) {
      let loc = expr.loc();
      let closure = this.curType().closure();
      if (closure.enclosingSlot().isStatic()) {
        expr.ctype(this.ns().error());
        this.err("Cannot access 'this' within closure of static context", loc);
        return expr;
      }
      ;
      return FieldExpr.make(loc, ThisExpr.make(loc), closure.outerThisField());
    }
    ;
    expr.ctype(this.curType());
    return expr;
  }

  resolveSuper(expr) {
    if (this.#inClosure) {
      this.err("Invalid use of 'super' within closure", expr.loc());
      expr.ctype(this.ns().error());
      return expr;
    }
    ;
    if (expr.explicitType() != null) {
      expr.ctype(expr.explicitType());
    }
    else {
      expr.ctype(this.curType().base());
    }
    ;
    return expr;
  }

  resolveIt(expr) {
    if ((this.curMethod() != null && this.curMethod().isFieldSetter())) {
      return LocalVarExpr.make(expr.loc(), this.curMethod().vars().first());
    }
    ;
    if ((!this.#inClosure || !this.curType().closure().isItBlock())) {
      this.err("Invalid use of 'it' outside of it-block", expr.loc());
      expr.ctype(this.ns().error());
      return expr;
    }
    ;
    expr.ctype(this.curType().closure().itType());
    return expr;
  }

  resolveAssign(expr) {
    if (expr.lhs().synthetic()) {
      if (expr.lhs().id() === ExprId.coerce()) {
        expr.lhs(sys.ObjUtil.coerce(expr.lhs(), TypeCheckExpr.type$).target());
      }
      else {
        if (expr.lhs().id() === ExprId.call()) {
          expr.lhs(sys.ObjUtil.coerce(sys.ObjUtil.coerce(expr.lhs(), CallExpr.type$).args().last(), Expr.type$));
        }
        else {
          throw sys.Err.make(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Unexpected LHS synthetic expr: ", expr), " ["), expr.loc().toLocStr()), "]"));
        }
        ;
      }
      ;
    }
    ;
    let shortcut = sys.ObjUtil.as(expr.lhs(), ShortcutExpr.type$);
    if ((shortcut != null && sys.ObjUtil.equals(shortcut.op(), ShortcutOp.get()))) {
      shortcut.op(ShortcutOp.set());
      shortcut.name("set");
      shortcut.args().add(expr.rhs());
      shortcut.method(null);
      return this.resolveCall(sys.ObjUtil.coerce(shortcut, CallExpr.type$));
    }
    ;
    let call = sys.ObjUtil.as(expr.lhs(), CallExpr.type$);
    if ((call != null && call.isDynamic())) {
      call.args().add(expr.rhs());
      return this.resolveCall(sys.ObjUtil.coerce(call, CallExpr.type$));
    }
    ;
    expr.ctype(expr.lhs().ctype());
    return expr;
  }

  resolveVar(var$) {
    if (var$.target() == null) {
      let binding = this.resolveLocal(sys.ObjUtil.coerce(var$.name(), sys.Str.type$), var$.loc());
      if (binding != null) {
        return LocalVarExpr.make(var$.loc(), binding);
      }
      ;
    }
    ;
    return CallResolver.make(this.compiler(), this.curType(), this.curMethod(), var$).resolve();
  }

  resolveStorage(var$) {
    let resolved = this.resolveVar(var$);
    if (resolved.id() === ExprId.localVar()) {
      let field = this.curType().field(sys.ObjUtil.coerce(var$.name(), sys.Str.type$));
      if (field != null) {
        (resolved = FieldExpr.make(var$.loc(), ThisExpr.make(var$.loc()), field));
      }
      ;
    }
    ;
    if (resolved.id() !== ExprId.field()) {
      if (resolved.ctype() !== this.ns().error()) {
        this.err("Invalid use of field storage operator '&'", var$.loc());
      }
      ;
      return resolved;
    }
    ;
    let f = sys.ObjUtil.as(resolved, FieldExpr.type$);
    f.useAccessor(false);
    if (sys.ObjUtil.is(f.field(), FieldDef.type$)) {
      let fd = sys.ObjUtil.coerce(f.field(), FieldDef.type$);
      fd.flags(sys.Int.or(fd.flags(), FConst.Storage()));
    }
    ;
    return sys.ObjUtil.coerce(f, Expr.type$);
  }

  resolveElvis(expr) {
    expr.ctype(CType.common(this.ns(), sys.List.make(CType.type$.toNullable(), [expr.lhs().ctype(), expr.rhs().ctype()])).toNullable());
    return expr;
  }

  resolveTernary(expr) {
    if (expr.trueExpr().id() === ExprId.nullLiteral()) {
      expr.ctype(expr.falseExpr().ctype().toNullable());
    }
    else {
      if (expr.falseExpr().id() === ExprId.nullLiteral()) {
        expr.ctype(expr.trueExpr().ctype().toNullable());
      }
      else {
        expr.ctype(CType.common(this.ns(), sys.List.make(CType.type$.toNullable(), [expr.trueExpr().ctype(), expr.falseExpr().ctype()])));
      }
      ;
    }
    ;
    return expr;
  }

  resolveCall(call) {
    if (call.isDynamic()) {
      return this.resolveTrapCall(call);
    }
    ;
    if ((call.isCtorChain() && this.curType().base().isForeign())) {
      return this.curType().base().bridge().resolveConstructorChain(call);
    }
    ;
    if (call.target() == null) {
      let binding = this.resolveLocal(sys.ObjUtil.coerce(call.name(), sys.Str.type$), call.loc());
      if (binding != null) {
        return this.resolveCallOnLocalVar(call, LocalVarExpr.make(call.loc(), binding));
      }
      ;
    }
    ;
    return CallResolver.make(this.compiler(), this.curType(), this.curMethod(), call).resolve();
  }

  resolveTrapCall(call) {
    const this$ = this;
    if (call.target().ctype() === this.ns().error()) {
      call.ctype(this.ns().error());
      return call;
    }
    ;
    call.method(call.target().ctype().method("trap"));
    call.ctype(call.method().returns());
    if (call.ctype().isObj()) {
      return call;
    }
    else {
      return sys.ObjUtil.coerce(sys.ObjUtil.with(TypeCheckExpr.coerce(call, sys.ObjUtil.coerce(call.ctype(), CType.type$)), (it) => {
        it.from(this$.ns().objType().toNullable());
        return;
      }), TypeCheckExpr.type$);
    }
    ;
  }

  resolveCallOnLocalVar(call,binding) {
    if ((call.noParens() && sys.ObjUtil.equals(call.args().size(), 1))) {
      let closure = sys.ObjUtil.as(call.args().last(), ClosureExpr.type$);
      if ((closure != null && closure.isItBlock())) {
        return closure.toWith(binding);
      }
      ;
    }
    ;
    if (sys.ObjUtil.compareGT(call.args().size(), 8)) {
      this.err("Tough luck - cannot use () operator with more than 8 arguments, use call(List)", call.loc());
      call.ctype(this.ns().error());
      return call;
    }
    ;
    let callMethod = binding.ctype().method("call");
    if (callMethod == null) {
      if (sys.ObjUtil.compareNE(binding.ctype(), this.ns().error())) {
        this.err(sys.Str.plus(sys.Str.plus("Cannot use () call operator on non-func type '", binding.ctype()), "'"), call.loc());
      }
      ;
      call.ctype(this.ns().error());
      return call;
    }
    ;
    (call = CallExpr.makeWithMethod(call.loc(), binding, sys.ObjUtil.coerce(callMethod, CMethod.type$), call.args()));
    call.isCallOp(true);
    return call;
  }

  resolveConstruction(call) {
    const this$ = this;
    let base = call.target().ctype();
    call.ctype(base);
    if (base.isForeign()) {
      return base.bridge().resolveConstruction(call);
    }
    ;
    let matches = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::CMethod"));
    this.findCtorMatches(matches, sys.ObjUtil.coerce(base, CType.type$), call.args());
    let itBlock = ((this$) => { let $_u181 = ((this$) => { let $_u182=sys.ObjUtil.as(call.args().last(), ClosureExpr.type$); return ($_u182==null) ? null : $_u182.isItBlock(); })(this$); if ($_u181 != null) return $_u181; return sys.ObjUtil.coerce(false, sys.Bool.type$.toNullable()); })(this);
    if (sys.ObjUtil.coerce(itBlock, sys.Bool.type$)) {
      this.findCtorMatches(matches, sys.ObjUtil.coerce(base, CType.type$), call.args().getRange(sys.Range.make(0, -2)));
    }
    ;
    if (matches.isEmpty()) {
      let args = call.args().join(", ", (arg) => {
        return arg.ctype().toStr();
      });
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("No constructor found: ", base.name()), "("), args), ")"), call.loc());
      return call;
    }
    ;
    if (sys.ObjUtil.compareGT(matches.size(), 1)) {
      let args = call.args().join(", ", (arg) => {
        return arg.ctype().toStr();
      });
      let names = matches.map((m) => {
        return m.name();
      }, sys.Obj.type$.toNullable()).vals().sort().join(", ");
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Ambiguous constructor: ", base.name()), "("), args), ") ["), names), "]"), call.loc());
      return call;
    }
    ;
    let match = matches.vals().first();
    call.method(match);
    call.ctype(((this$) => { if (match.isStatic()) return match.returns(); return base; })(this));
    return CallResolver.inferClosureTypeFromCall(this, call, sys.ObjUtil.coerce(base, CType.type$));
  }

  findCtorMatches(matches,base,args) {
    const this$ = this;
    base.slots().each((slot) => {
      if (!this$.isCtorMethod(slot)) {
        return;
      }
      ;
      if (!CheckErrors.isSlotVisible(sys.ObjUtil.coerce(this$.curType(), TypeDef.type$), slot)) {
        return;
      }
      ;
      if (sys.ObjUtil.compareNE(slot.parent(), base)) {
        return;
      }
      ;
      let ctor = sys.ObjUtil.coerce(slot, CMethod.type$);
      let params = ctor.params();
      if (sys.ObjUtil.compareLT(params.size(), args.size())) {
        return;
      }
      ;
      if ((sys.ObjUtil.compareGT(params.size(), args.size()) && !params.get(args.size()).hasDefault())) {
        return;
      }
      ;
      for (let i = 0; sys.ObjUtil.compareLT(i, args.size()); i = sys.Int.increment(i)) {
        if (!this$.#coercer.canCoerce(args.get(i), params.get(i).type())) {
          return;
        }
        ;
      }
      ;
      matches.set(ctor.name(), ctor);
      return;
    });
    return;
  }

  isCtorMethod(slot) {
    if (slot.isCtor()) {
      return true;
    }
    ;
    if (!sys.ObjUtil.is(slot, CMethod.type$)) {
      return false;
    }
    ;
    if ((slot.isStatic() && (sys.ObjUtil.equals(slot.name(), "make") || sys.ObjUtil.equals(slot.name(), "fromStr")))) {
      return true;
    }
    ;
    return false;
  }

  resolveShortcut(expr) {
    if ((expr.isAssign() && expr.target().id() === ExprId.shortcut())) {
      return this.resolveIndexedAssign(expr);
    }
    ;
    if (expr.isStrConcat()) {
      expr.ctype(this.ns().strType());
      expr.method(this.ns().strPlus());
      return ConstantFolder.make(this.compiler()).fold(expr);
    }
    ;
    if ((sys.ObjUtil.equals(expr.args().size(), 1) && expr.op().isOperator())) {
      let method = this.resolveBinaryOperator(expr);
      if (method == null) {
        expr.ctype(this.ns().error());
        return expr;
      }
      ;
      expr.method(method);
      expr.name(method.name());
    }
    ;
    let result = this.resolveCall(expr);
    if (result !== expr) {
      return result;
    }
    ;
    if ((expr.method() != null && expr.op().isOperator() && !expr.method().hasFacet("sys::Operator"))) {
      this.err(sys.Str.plus("Missing Operator facet: ", expr.method().qname()), expr.loc());
    }
    ;
    let $_u184 = expr.opToken();
    if (sys.ObjUtil.equals($_u184, Token.lt()) || sys.ObjUtil.equals($_u184, Token.ltEq()) || sys.ObjUtil.equals($_u184, Token.gt()) || sys.ObjUtil.equals($_u184, Token.gtEq())) {
      expr.ctype(this.ns().boolType());
    }
    ;
    return expr;
  }

  resolveBinaryOperator(expr) {
    const this$ = this;
    let op = expr.op();
    let lhs = expr.target().ctype();
    let rhs = expr.args().first();
    if ((lhs === this.ns().error() || rhs.ctype() === this.ns().error())) {
      return null;
    }
    ;
    let matches = lhs.operators().find(op.methodName());
    if (sys.ObjUtil.compareGT(matches.size(), 1)) {
      (matches = matches.findAll((m) => {
        if (sys.ObjUtil.compareNE(m.params().size(), 1)) {
          return false;
        }
        ;
        let paramType = m.params().first().type();
        return this$.#coercer.canCoerce(sys.ObjUtil.coerce(rhs, Expr.type$), paramType);
      }));
    }
    ;
    if (matches.isEmpty()) {
      this.err(sys.Str.plus("No operator method found: ", op.formatErr(sys.ObjUtil.coerce(lhs, CType.type$), sys.ObjUtil.coerce(rhs.ctype(), CType.type$))), expr.loc());
      return null;
    }
    ;
    if (sys.ObjUtil.equals(matches.size(), 1)) {
      return matches.first();
    }
    ;
    let names = matches.map((m) => {
      return m.name();
    }, sys.Str.type$).join(", ");
    this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Ambiguous operator method: ", op.formatErr(sys.ObjUtil.coerce(lhs, CType.type$), sys.ObjUtil.coerce(rhs.ctype(), CType.type$))), " ["), names), "]"), expr.loc());
    return null;
  }

  resolveIndexedAssign(orig) {
    if (orig.target().ctype() === this.ns().error()) {
      orig.ctype(this.ns().error());
      return orig;
    }
    ;
    if ((sys.ObjUtil.compareNE(orig.target().id(), ExprId.shortcut()) && sys.ObjUtil.trap(orig.target(),"op", sys.List.make(sys.Obj.type$.toNullable(), [])) === ShortcutOp.get())) {
      this.err("Expected indexed expression", orig.loc());
      return orig;
    }
    ;
    let expr = IndexedAssignExpr.makeFrom(orig);
    this.resolveCall(expr);
    let get$ = sys.ObjUtil.coerce(expr.target(), ShortcutExpr.type$).method();
    let set$ = get$.parent().method("set");
    if ((set$ == null || sys.ObjUtil.compareNE(set$.params().size(), 2) || set$.isStatic() || sys.ObjUtil.compareNE(set$.params().get(0).type().toNonNullable(), get$.params().get(0).type().toNonNullable()) || sys.ObjUtil.compareNE(set$.params().get(1).type().toNonNullable(), get$.returns().toNonNullable()))) {
      this.err(sys.Str.plus(sys.Str.plus("No matching 'set' method for '", get$.qname()), "'"), orig.loc());
    }
    else {
      expr.setMethod(set$);
    }
    ;
    return expr;
  }

  resolveClosure(expr) {
    const this$ = this;
    expr.enclosingVars(this.localsInScope());
    expr.doCall().paramDefs().each((p) => {
      if ((expr.enclosingVars().containsKey(p.name()) && sys.ObjUtil.compareNE(p.name(), "it"))) {
        this$.err(sys.Str.plus(sys.Str.plus("Closure parameter '", p.name()), "' is already defined in current block"), p.loc());
      }
      ;
      return;
    });
    return;
  }

  resolveDsl(expr) {
    let plugin = DslPlugin.find(this, expr.loc(), expr.anchorType());
    if (plugin == null) {
      expr.ctype(this.ns().error());
      return expr;
    }
    ;
    let origNumErrs = this.compiler().errs().size();
    expr.ctype(this.ns().error());
    try {
      let result = plugin.compile(expr);
      if (result === expr) {
        return result;
      }
      ;
      return result.walk(this);
    }
    catch ($_u185) {
      $_u185 = sys.Err.make($_u185);
      if ($_u185 instanceof CompilerErr) {
        let e = $_u185;
        ;
        if (sys.ObjUtil.equals(this.compiler().errs().size(), origNumErrs)) {
          this.errReport(e);
        }
        ;
        return expr;
      }
      else if ($_u185 instanceof sys.Err) {
        let e = $_u185;
        ;
        this.errReport(CompilerErr.make(sys.Str.plus(sys.Str.plus(sys.Str.plus("Internal error in DslPlugin '", sys.ObjUtil.typeof(plugin)), "': "), e), expr.loc(), e));
        e.trace();
        return expr;
      }
      else {
        throw $_u185;
      }
    }
    ;
  }

  initMethodVars() {
    const this$ = this;
    let m = this.curMethod();
    let reg = ((this$) => { if (m.isStatic()) return 0; return 1; })(this);
    m.paramDefs().each((p) => {
      let var$ = MethodVar.makeForParam(sys.ObjUtil.coerce(m, MethodDef.type$), ((this$) => { let $_u187 = reg;reg = sys.Int.increment(reg); return $_u187; })(this$), p, p.type().parameterizeThis(sys.ObjUtil.coerce(this$.curType(), CType.type$)));
      m.vars().add(var$);
      return;
    });
    return;
  }

  bindToMethodVar(def) {
    if (this.resolveLocal(def.name(), def.loc()) != null) {
      this.err(sys.Str.plus(sys.Str.plus("Variable '", def.name()), "' is already defined in current block"), def.loc());
    }
    ;
    def.var(this.curMethod().addLocalVarForDef(def, this.currentBlock()));
    return;
  }

  resolveLocal(name,loc) {
    const this$ = this;
    if (this.curMethod() == null) {
      return null;
    }
    ;
    let binding = this.curMethod().vars().find((var$) => {
      return (sys.ObjUtil.equals(var$.name(), name) && this$.isBlockInScope(var$.scope()));
    });
    if (binding != null) {
      return binding;
    }
    ;
    if (this.#inClosure) {
      let closure = this.curType().closure();
      (binding = closure.enclosingVars().get(name));
      if (binding != null) {
        binding.method().usesCvars(true);
        binding.usedInClosure(true);
        let shadow = this.curMethod().getOrAddShadowVar(sys.ObjUtil.coerce(binding, MethodVar.type$), this.currentBlock());
        let last = shadow;
        for (let p = closure.enclosingClosure(); p != null; (p = p.enclosingClosure())) {
          if (binding.method() === p.doCall()) {
            break;
          }
          ;
          let passThru = p.doCall().getOrAddShadowVar(sys.ObjUtil.coerce(binding, MethodVar.type$), p.doCall().code());
          last.shadows(passThru);
          (last = passThru);
        }
        ;
        return shadow;
      }
      ;
    }
    ;
    return null;
  }

  localsInScope() {
    const this$ = this;
    let acc = ((this$) => { if (this$.#inClosure) return this$.curType().closure().enclosingVars().dup(); return sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::MethodVar")); })(this);
    if (this.curMethod() == null) {
      return acc;
    }
    ;
    this.curMethod().vars().each((var$) => {
      if (this$.isBlockInScope(var$.scope())) {
        acc.set(var$.name(), var$);
      }
      ;
      return;
    });
    return acc;
  }

  currentBlock() {
    if (sys.ObjUtil.is(this.#stmtStack.peek(), ForStmt.type$)) {
      return sys.ObjUtil.coerce(sys.ObjUtil.coerce(this.#stmtStack.peek(), ForStmt.type$).block(), Block.type$);
    }
    else {
      return sys.ObjUtil.coerce(this.#blockStack.peek(), Block.type$);
    }
    ;
  }

  isBlockInScope(block) {
    const this$ = this;
    if (block == null) {
      return true;
    }
    ;
    if (sys.ObjUtil.is(this.#stmtStack.peek(), ForStmt.type$)) {
      if (sys.ObjUtil.coerce(this.#stmtStack.peek(), ForStmt.type$).block() === block) {
        return true;
      }
      ;
    }
    ;
    return this.#blockStack.any((b) => {
      return b === block;
    });
  }

  findLoop() {
    for (let i = sys.Int.minus(this.#stmtStack.size(), 1); sys.ObjUtil.compareGE(i, 0); i = sys.Int.decrement(i)) {
      let stmt = this.#stmtStack.get(i);
      if (stmt.id() === StmtId.whileStmt()) {
        return stmt;
      }
      ;
      if (stmt.id() === StmtId.forStmt()) {
        return stmt;
      }
      ;
    }
    ;
    return null;
  }

  enterBlock(block) {
    this.#blockStack.push(block);
    return;
  }

  exitBlock(block) {
    this.#blockStack.pop();
    return;
  }

}

class ClosureVars extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ClosureVars.type$; }

  static #syntheticFieldFlags = undefined;

  static syntheticFieldFlags() {
    if (ClosureVars.#syntheticFieldFlags === undefined) {
      ClosureVars.static$init();
      if (ClosureVars.#syntheticFieldFlags === undefined) ClosureVars.#syntheticFieldFlags = 0;
    }
    return ClosureVars.#syntheticFieldFlags;
  }

  static make(compiler) {
    const $self = new ClosureVars();
    ClosureVars.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    const this$ = this;
    this.types().each((t) => {
      this$.scanType(t);
      return;
    });
    this.compiler().closures().each((c) => {
      this$.processClosure(c);
      return;
    });
    return;
  }

  scanType(t) {
    const this$ = this;
    t.methodDefs().each((m) => {
      if (m.usesCvars()) {
        this$.processMethod(m);
      }
      ;
      return;
    });
    return;
  }

  processMethod(method) {
    if (!this.wrapNonFinalVars(method)) {
      return;
    }
    ;
    this.walkMethod(method);
    this.fixWrappedParams(method);
    return;
  }

  wrapNonFinalVars(m) {
    const this$ = this;
    let wrapped = false;
    m.vars().each((var$,i) => {
      if (!var$.usedInClosure()) {
        return;
      }
      ;
      if (!var$.isReassigned()) {
        return;
      }
      ;
      let wrapField = ClosureVars.genWrapper(this$, var$.ctype());
      if (var$.isParam()) {
        let w = m.addLocalVar(wrapField.parent(), sys.Str.plus(var$.name(), "\$Wrapper"), m.code());
        w.wrapField(wrapField);
        var$.paramWrapper(w);
      }
      else {
        if (var$.wrapField() != null) {
          throw sys.Err.make();
        }
        ;
        var$.wrapField(wrapField);
        var$.ctype(wrapField.parent());
      }
      ;
      (wrapped = true);
      return;
    });
    return wrapped;
  }

  walkMethod(method) {
    method.code().walk(this, VisitDepth.expr());
    return;
  }

  visitStmt(stmt) {
    if ((stmt.id() === StmtId.localDef() && sys.ObjUtil.coerce(stmt, LocalDefStmt.type$).var().isWrapped())) {
      return this.fixLocalDef(sys.ObjUtil.coerce(stmt, LocalDefStmt.type$));
    }
    ;
    return null;
  }

  visitExpr(expr) {
    let $_u189 = expr.id();
    if (sys.ObjUtil.equals($_u189, ExprId.localVar())) {
      return this.fixWrappedVar(sys.ObjUtil.coerce(expr, LocalVarExpr.type$));
    }
    ;
    return expr;
  }

  fixLocalDef(stmt) {
    let init = null;
    if (stmt.init() == null) {
      (init = LiteralExpr.makeNull(stmt.loc(), this.ns()));
    }
    else {
      (init = sys.ObjUtil.coerce(stmt.init(), BinaryExpr.type$).rhs());
    }
    ;
    stmt.init(this.initWrapper(stmt.loc(), sys.ObjUtil.coerce(stmt.var(), MethodVar.type$), sys.ObjUtil.coerce(init, Expr.type$)));
    stmt.ctype(stmt.init().ctype());
    return null;
  }

  initWrapper(loc,var$,init) {
    let wrapCtor = var$.wrapField().parent().method("make");
    let lhs = LocalVarExpr.makeNoUnwrap(loc, var$);
    let rhs = CallExpr.makeWithMethod(loc, null, sys.ObjUtil.coerce(wrapCtor, CMethod.type$), sys.List.make(Expr.type$, [init]));
    return BinaryExpr.makeAssign(lhs, rhs);
  }

  fixWrappedVar(local) {
    let var$ = local.var();
    if (var$.paramWrapper() != null) {
      (var$ = var$.paramWrapper());
    }
    ;
    if ((!var$.isWrapped() || !local.unwrap())) {
      return local;
    }
    ;
    let loc = local.loc();
    return ClosureVars.fieldExpr(loc, LocalVarExpr.makeNoUnwrap(loc, sys.ObjUtil.coerce(var$, MethodVar.type$)), sys.ObjUtil.coerce(var$.wrapField(), CField.type$));
  }

  fixWrappedParams(method) {
    const this$ = this;
    method.vars().each((var$) => {
      if (var$.paramWrapper() == null) {
        return;
      }
      ;
      let loc = method.loc();
      let initWrap = this$.initWrapper(loc, sys.ObjUtil.coerce(var$.paramWrapper(), MethodVar.type$), LocalVarExpr.makeNoUnwrap(loc, var$));
      method.code().stmts().insert(0, LocalDefStmt.makeAssign(sys.ObjUtil.coerce(initWrap, BinaryExpr.type$)));
      return;
    });
    return;
  }

  processClosure(closure) {
    const this$ = this;
    let shadowed = closure.doCall().vars().findAll((var$) => {
      return var$.shadows() != null;
    });
    shadowed.each((var$,i) => {
      this$.addVarToClosure(closure, var$, sys.Str.plus(sys.Str.plus(var$.name(), "\$"), sys.ObjUtil.coerce(i, sys.Obj.type$.toNullable())));
      return;
    });
    let walkExprTree = shadowed.any((var$) => {
      return var$.isWrapped();
    });
    if (walkExprTree) {
      closure.doCall().code().walkExpr((expr) => {
        return ((this$) => { if (expr.id() === ExprId.localVar()) return this$.fixWrappedVar(sys.ObjUtil.coerce(expr, LocalVarExpr.type$)); return expr; })(this$);
      });
    }
    ;
    return;
  }

  addVarToClosure(closure,var$,name) {
    if (var$.shadows().paramWrapper() != null) {
      var$.shadows(var$.shadows().paramWrapper());
    }
    ;
    if (var$.shadows().isWrapped()) {
      var$.ctype(var$.shadows().ctype());
      var$.wrapField(var$.shadows().wrapField());
    }
    ;
    let loc = closure.loc();
    let field = ClosureVars.addToClosure(closure, name, LocalVarExpr.makeNoUnwrap(loc, sys.ObjUtil.coerce(var$.shadows(), MethodVar.type$)), sys.Str.plus("wrapper for ", sys.Str.toCode(var$.name())));
    let init = BinaryExpr.makeAssign(LocalVarExpr.makeNoUnwrap(loc, var$), ClosureVars.fieldExpr(loc, ThisExpr.make(loc), field));
    closure.doCall().code().stmts().insert(0, LocalDefStmt.makeAssign(init));
    return;
  }

  static makeOuterThisField(closure) {
    let loc = closure.loc();
    let subArg = null;
    if (closure.enclosingClosure() != null) {
      let outerThis = closure.enclosingClosure().outerThisField();
      (subArg = ClosureVars.fieldExpr(loc, ThisExpr.make(loc), outerThis));
    }
    else {
      (subArg = ThisExpr.make(loc, closure.enclosingType()));
    }
    ;
    return ClosureVars.addToClosure(closure, "\$this", sys.ObjUtil.coerce(subArg, Expr.type$), "implicit this");
  }

  static addToClosure(closure,name,subtituteArg,info) {
    let loc = closure.loc();
    let thisType = closure.enclosingType();
    let implType = closure.cls();
    let ctype = subtituteArg.ctype();
    let field = FieldDef.make(loc, sys.ObjUtil.coerce(implType, TypeDef.type$));
    field.name(name);
    field.flags(ClosureVars.syntheticFieldFlags());
    field.type(sys.ObjUtil.coerce(ctype, CType.type$));
    field.closureInfo(info);
    implType.addSlot(field);
    closure.substitute().args().add(subtituteArg);
    let ctor = implType.methodDef("make");
    let pvar = ctor.addParamVar(sys.ObjUtil.coerce(ctype, CType.type$), name);
    let assign = BinaryExpr.makeAssign(ClosureVars.fieldExpr(loc, ThisExpr.make(loc), field), LocalVarExpr.makeNoUnwrap(loc, pvar));
    ctor.code().stmts().insert(0, assign.toStmt());
    return field;
  }

  static genWrapper(cs,ctype) {
    let podName = ((this$) => { if (sys.ObjUtil.compareNE(ctype.pod().name(), "sys")) return sys.Str.plus("\$", ClosureVars.toSafe(ctype.pod().name())); return ""; })(this);
    let name = sys.Str.plus(sys.Str.plus(sys.Str.plus("Wrap", podName), "\$"), ClosureVars.toSafe(ctype.name()));
    if ((cs.compiler().input().wrapperPerParameterizedCollectionType() && ctype.isParameterized())) {
      if (ctype.isList()) {
        let list = sys.ObjUtil.coerce(ctype.toNonNullable(), ListType.type$);
        name = sys.Str.plus(name, sys.Str.plus("\$", ClosureVars.toSafe(list.v().name())));
      }
      else {
        if (ctype.isMap()) {
          let map = sys.ObjUtil.coerce(ctype.toNonNullable(), MapType.type$);
          name = sys.Str.plus(name, sys.Str.plus(sys.Str.plus(sys.Str.plus("\$", ClosureVars.toSafe(map.k().name())), "\$"), ClosureVars.toSafe(map.v().name())));
        }
        ;
      }
      ;
    }
    ;
    if (ctype.isNullable()) {
      name = sys.Str.plus(name, "\$n");
    }
    ;
    let existing = cs.compiler().wrappers().get(name);
    if (existing != null) {
      return sys.ObjUtil.coerce(existing, CField.type$);
    }
    ;
    let loc = Loc.make("synthetic");
    let w = TypeDef.make(cs.ns(), loc, cs.syntheticsUnit(), name);
    w.flags(sys.Int.plus(FConst.Internal(), FConst.Synthetic()));
    w.base(cs.ns().objType());
    cs.addTypeDef(w);
    let f = FieldDef.make(loc, w);
    f.name("val");
    f.type(ctype);
    f.flags(ClosureVars.syntheticFieldFlags());
    w.addSlot(f);
    let ctor = MethodDef.make(loc, w);
    ctor.flags(sys.Int.plus(sys.Int.plus(FConst.Ctor(), FConst.Internal()), FConst.Synthetic()));
    ctor.name("make");
    ctor.returns(sys.ObjUtil.coerce(cs.ns().voidType(), CType.type$));
    let param = ParamDef.make(loc, ctype, "v");
    let pvar = MethodVar.makeForParam(ctor, 1, param, param.type());
    ctor.params().add(param);
    ctor.vars().add(pvar);
    ctor.code(Block.make(loc));
    let lhs = ClosureVars.fieldExpr(loc, ThisExpr.make(loc, w), f);
    let rhs = LocalVarExpr.make(loc, pvar);
    ctor.code().add(BinaryExpr.makeAssign(lhs, rhs).toStmt());
    ctor.code().add(ReturnStmt.makeSynthetic(loc));
    w.addSlot(ctor);
    cs.compiler().wrappers().set(name, f);
    return f;
  }

  static toSafe(n) {
    const this$ = this;
    if (sys.Str.isAlphaNum(n)) {
      return n;
    }
    ;
    let s = sys.StrBuf.make();
    sys.Str.each(n, (ch) => {
      if (sys.Int.isAlphaNum(ch)) {
        s.addChar(ch);
      }
      else {
        s.addChar(95);
      }
      ;
      return;
    });
    return s.toStr();
  }

  static fieldExpr(loc,target,field) {
    return FieldExpr.make(loc, target, field, false);
  }

  static static$init() {
    ClosureVars.#syntheticFieldFlags = sys.Int.plus(sys.Int.plus(FConst.Internal(), FConst.Storage()), FConst.Synthetic());
    return;
  }

}

class Parse extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Parse.type$; }

  static make(compiler) {
    const $self = new Parse();
    Parse.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    const this$ = this;
    this.log().debug("Parse");
    let types = sys.List.make(TypeDef.type$);
    let closures = sys.List.make(ClosureExpr.type$);
    this.units().each((unit) => {
      let p = Parser.make(this$.compiler(), unit, closures);
      p.parse();
      types.addAll(unit.types());
      return;
    });
    this.bombIfErr();
    this.compiler().types(types);
    this.compiler().closures(closures);
    return;
  }

}

class CheckInheritance extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return CheckInheritance.type$; }

  static make(compiler) {
    const $self = new CheckInheritance();
    CheckInheritance.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    this.log().debug("CheckInheritance");
    this.walk(this.compiler(), VisitDepth.typeDef());
    this.bombIfErr();
    return;
  }

  visitTypeDef(t) {
    const this$ = this;
    if (!this.checkOutOfOrder(t)) {
      return;
    }
    ;
    this.checkExtends(t, t.base());
    t.mixins().each((m) => {
      this$.checkMixin(t, m);
      return;
    });
    return;
  }

  checkOutOfOrder(t) {
    const this$ = this;
    if (!t.baseSpecified()) {
      let cls = t.mixins().find((x) => {
        return !x.isMixin();
      });
      if (cls != null) {
        this.err(sys.Str.plus(sys.Str.plus("Invalid inheritance order, ensure class '", cls), "' comes first before mixins"), t.loc());
        return false;
      }
      ;
    }
    ;
    return true;
  }

  checkExtends(t,base) {
    if ((base == null && sys.ObjUtil.equals(t.qname(), "sys::Obj"))) {
      return;
    }
    ;
    if ((t.isMixin() && t.baseSpecified())) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Mixin '", t.name()), "' cannot extend class '"), base), "'"), t.loc());
    }
    ;
    if ((t.isEnum() && t.baseSpecified())) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Enum '", t.name()), "' cannot extend class '"), base), "'"), t.loc());
    }
    ;
    if ((t.isFacet() && t.baseSpecified())) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Facet '", t.name()), "' cannot extend class '"), base), "'"), t.loc());
    }
    ;
    if (base.isMixin()) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Class '", t.name()), "' cannot extend mixin '"), base), "'"), t.loc());
    }
    ;
    if (base.isParameterized()) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Class '", t.name()), "' cannot extend parameterized type '"), base), "'"), t.loc());
    }
    ;
    if (base.isFinal()) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Class '", t.name()), "' cannot extend final class '"), base), "'"), t.loc());
    }
    ;
    if ((base.isInternal() && sys.ObjUtil.compareNE(t.pod(), base.pod()))) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Class '", t.name()), "' cannot access internal scoped class '"), base), "'"), t.loc());
    }
    ;
    return;
  }

  checkMixin(t,m) {
    if (!m.isMixin()) {
      if (t.isMixin()) {
        this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Mixin '", t.name()), "' cannot extend class '"), m), "'"), t.loc());
      }
      else {
        this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Class '", t.name()), "' cannot mixin class '"), m), "'"), t.loc());
      }
      ;
    }
    ;
    if ((m.isInternal() && sys.ObjUtil.compareNE(t.pod(), m.pod()))) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Type '", t.name()), "' cannot access internal scoped mixin '"), m), "'"), t.loc());
    }
    ;
    return;
  }

}

class Tokenize extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Tokenize.type$; }

  #input = null;

  input(it) {
    if (it === undefined) {
      return this.#input;
    }
    else {
      this.#input = it;
      return;
    }
  }

  static make(compiler) {
    const $self = new Tokenize();
    Tokenize.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    $self.#input = compiler.input();
    return;
  }

  run() {
    this.log().debug("Tokenize");
    let $_u192 = this.#input.mode();
    if (sys.ObjUtil.equals($_u192, CompilerInputMode.str())) {
      this.runStrMode();
    }
    else if (sys.ObjUtil.equals($_u192, CompilerInputMode.file())) {
      this.runFileMode();
    }
    else {
      throw sys.UnsupportedErr.make();
    }
    ;
    return;
  }

  runStrMode() {
    this.tokenize(sys.ObjUtil.coerce(this.#input.srcStrLoc(), Loc.type$), sys.ObjUtil.coerce(this.#input.srcStr(), sys.Str.type$));
    return;
  }

  runFileMode() {
    const this$ = this;
    this.compiler().srcFiles().each((file) => {
      let loc = Loc.makeFile(file);
      try {
        let src = file.readAllStr();
        this$.tokenize(loc, src);
      }
      catch ($_u193) {
        $_u193 = sys.Err.make($_u193);
        if ($_u193 instanceof CompilerErr) {
          let err = $_u193;
          ;
          throw err;
        }
        else if ($_u193 instanceof sys.Err) {
          let e = $_u193;
          ;
          if (file.exists()) {
            throw this$.err(sys.Str.plus("Cannot read source file: ", e), loc);
          }
          else {
            throw this$.err("Source file not found", loc);
          }
          ;
        }
        else {
          throw $_u193;
        }
      }
      ;
      return;
    });
    return;
  }

  tokenize(loc,src) {
    let unit = CompilationUnit.make(loc, this.pod());
    let tokenizer = Tokenizer.make(this.compiler(), loc, src, this.#input.includeDoc());
    unit.tokens(tokenizer.tokenize());
    this.pod().units().add(unit);
    return unit;
  }

}

class LoadPod extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return LoadPod.type$; }

  static make(compiler) {
    const $self = new LoadPod();
    LoadPod.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    throw sys.UnsupportedErr.make();
  }

  load() {
    let buf = sys.Buf.make(4096);
    let fpod = this.compiler().fpod();
    fpod.zip(sys.Zip.write(buf.out()));
    fpod.write();
    fpod.zip().close();
    return sys.Pod.load(sys.ObjUtil.coerce(buf.flip(), sys.Buf.type$.toNullable()).in());
  }

}

class Inherit extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Inherit.type$; }

  static make(compiler) {
    const $self = new Inherit();
    Inherit.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    this.log().debug("Inherit");
    this.walk(this.compiler(), VisitDepth.typeDef());
    return;
  }

  visitTypeDef(t) {
    const this$ = this;
    this.inheritType(t, t.base());
    t.mixins().each((m) => {
      this$.inheritType(t, m);
      return;
    });
    t.slotDefs().each((slot) => {
      if ((slot.isOverride() && !slot.overridden() && !slot.isAccessor())) {
        this$.err(sys.Str.plus(sys.Str.plus("Override of unknown virtual slot '", slot.name()), "'"), slot.loc());
      }
      ;
      return;
    });
    return;
  }

  inheritType(t,parent) {
    const this$ = this;
    if (parent == null) {
      if (sys.ObjUtil.equals(t.qname(), "sys::Obj")) {
        return;
      }
      else {
        throw this.err("Illegal state", t.loc());
      }
      ;
    }
    ;
    let closure = (s) => {
      if ((parent.isMixin() && s.parent().isObj())) {
        return;
      }
      ;
      try {
        this$.inheritSlot(t, s);
      }
      catch ($_u194) {
        $_u194 = sys.Err.make($_u194);
        if ($_u194 instanceof CompilerErr) {
          let e = $_u194;
          ;
        }
        else {
          throw $_u194;
        }
      }
      ;
      return;
    };
    if (this.compiler().input().isTest()) {
      parent.slots().vals().sort((a,b) => {
        return sys.ObjUtil.compare(a.name(), b.name());
      }).each(closure);
    }
    else {
      parent.slots().each(closure);
    }
    ;
    return;
  }

  inheritSlot(t,newSlot) {
    if ((newSlot.isCtor() || newSlot.isPrivate() || (newSlot.isInternal() && sys.ObjUtil.compareNE(newSlot.parent().pod(), t.pod())))) {
      return;
    }
    ;
    let name = newSlot.name();
    let oldSlot = t.slot(name);
    if (oldSlot == null) {
      t.addSlot(newSlot);
      return;
    }
    ;
    if (newSlot === oldSlot) {
      return;
    }
    ;
    if ((oldSlot.parent() === t && !newSlot.isCtor())) {
      let slotDef = sys.ObjUtil.coerce(oldSlot, SlotDef.type$);
      slotDef.overridden(true);
      this.checkOverride(t, newSlot, slotDef);
      return;
    }
    ;
    if (!this.matchingSignatures(sys.ObjUtil.coerce(oldSlot, CSlot.type$), newSlot)) {
      throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Inherited slots have conflicting signatures '", oldSlot.qname()), "' and '"), newSlot.qname()), "'"), t.loc());
    }
    ;
    let keep = this.keep(sys.ObjUtil.coerce(oldSlot, CSlot.type$), newSlot);
    if (keep != null) {
      if (keep === newSlot) {
        t.replaceSlot(sys.ObjUtil.coerce(oldSlot, CSlot.type$), newSlot);
      }
      ;
      return;
    }
    ;
    if ((oldSlot.isVirtual() && newSlot.isVirtual())) {
      throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Must override ambiguous inheritance '", oldSlot.qname()), "' and '"), newSlot.qname()), "'"), t.loc());
    }
    ;
    throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Inheritance conflict '", oldSlot.qname()), "' and '"), newSlot.qname()), "'"), t.loc());
  }

  matchingSignatures(a,b) {
    let fa = sys.ObjUtil.as(a, CField.type$);
    let fb = sys.ObjUtil.as(b, CField.type$);
    let ma = sys.ObjUtil.as(a, CMethod.type$);
    let mb = sys.ObjUtil.as(b, CMethod.type$);
    if ((fa != null && fb != null)) {
      return sys.ObjUtil.equals(fa.type(), fb.type());
    }
    ;
    if ((ma != null && mb != null)) {
      return (sys.ObjUtil.equals(ma.returns(), mb.returns()) && sys.ObjUtil.equals(ma.inheritedReturns(), mb.inheritedReturns()) && ma.hasSameParams(sys.ObjUtil.coerce(mb, CMethod.type$)));
    }
    ;
    if ((fa != null && mb != null)) {
      return (sys.ObjUtil.equals(fa.type(), mb.returns()) && sys.ObjUtil.equals(fa.type(), mb.inheritedReturns()) && sys.ObjUtil.equals(mb.params().size(), 0));
    }
    ;
    if ((ma != null && fb != null)) {
      return (sys.ObjUtil.equals(ma.returns(), fb.type()) && sys.ObjUtil.equals(ma.inheritedReturns(), fb.type()) && sys.ObjUtil.equals(ma.params().size(), 0));
    }
    ;
    return false;
  }

  keep(a,b) {
    if ((a.isAbstract() && !b.isAbstract())) {
      return b;
    }
    ;
    if ((!a.isAbstract() && b.isAbstract())) {
      return a;
    }
    ;
    if (a.parent().fits(b.parent())) {
      return a;
    }
    ;
    if (b.parent().fits(a.parent())) {
      return b;
    }
    ;
    return null;
  }

  checkOverride(t,base,def) {
    let loc = def.loc();
    if (!base.isVirtual()) {
      throw this.err(sys.Str.plus(sys.Str.plus("Cannot override non-virtual slot '", base.qname()), "'"), loc);
    }
    ;
    if (!def.isOverride()) {
      throw this.err(sys.Str.plus(sys.Str.plus("Must specify override keyword to override '", base.qname()), "'"), loc);
    }
    ;
    if (this.isOverrideProtectionErr(base, def)) {
      throw this.err(sys.Str.plus(sys.Str.plus("Override narrows protection scope of '", base.qname()), "'"), loc);
    }
    ;
    if (base.isForeign()) {
      base.bridge().checkOverride(t, base, def);
    }
    ;
    if ((sys.ObjUtil.is(base, CMethod.type$) && sys.ObjUtil.is(def, MethodDef.type$))) {
      this.checkMethodMethodOverride(t, sys.ObjUtil.coerce(base, CMethod.type$), sys.ObjUtil.coerce(def, MethodDef.type$));
      return;
    }
    ;
    if ((sys.ObjUtil.is(base, CMethod.type$) && sys.ObjUtil.is(def, FieldDef.type$))) {
      this.checkMethodFieldOverride(t, sys.ObjUtil.coerce(base, CMethod.type$), sys.ObjUtil.coerce(def, FieldDef.type$));
      return;
    }
    ;
    if ((sys.ObjUtil.is(base, CField.type$) && sys.ObjUtil.is(def, FieldDef.type$))) {
      this.checkFieldFieldOverride(t, sys.ObjUtil.coerce(base, CField.type$), sys.ObjUtil.coerce(def, FieldDef.type$));
      return;
    }
    ;
    throw this.err(sys.Str.plus(sys.Str.plus("Invalid slot override of '", base.qname()), "'"), def.loc());
  }

  isOverrideProtectionErr(base,def) {
    if (def.isPublic()) {
      return false;
    }
    ;
    if (def.isProtected()) {
      return (base.isPublic() || base.isInternal());
    }
    ;
    if (def.isInternal()) {
      return (base.isPublic() || base.isProtected());
    }
    ;
    return true;
  }

  checkMethodMethodOverride(t,base,def) {
    const this$ = this;
    let loc = def.loc();
    let defRet = def.returns();
    let baseRet = base.returns();
    if (baseRet.isThis()) {
      if (!defRet.isThis()) {
        throw this.err(sys.Str.plus(sys.Str.plus("Return in override of '", base.qname()), "' must be This"), loc);
      }
      ;
    }
    else {
      if (sys.ObjUtil.compareNE(defRet, baseRet)) {
        if ((!defRet.fits(baseRet) || (defRet.isVoid() && !baseRet.isVoid()) || sys.ObjUtil.compareNE(defRet.isNullable(), baseRet.isNullable()))) {
          throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Return type mismatch in override of '", base.qname()), "' - '"), baseRet.inferredAs()), "' != '"), defRet), "'"), loc);
        }
        ;
        if ((defRet.isVal() || baseRet.isVal())) {
          throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Cannot use covariance with value types '", base.qname()), "' - '"), baseRet), "' != '"), defRet), "'"), loc);
        }
        ;
      }
      ;
      if ((def.inheritedRet() != null && sys.ObjUtil.compareNE(def.inheritedRet(), base.inheritedReturns()))) {
        throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Conflicting covariant returns: '", def.inheritedRet()), "' and '"), base.inheritedReturns()), "'"), loc);
      }
      ;
    }
    ;
    def.inheritedRet(base.inheritedReturns());
    if (!base.hasSameParams(def)) {
      throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Parameter mismatch in override of '", base.qname()), "' - '"), base.nameAndParamTypesToStr()), "' != '"), def.nameAndParamTypesToStr()), "'"), loc);
    }
    ;
    base.params().each((b,i) => {
      let d = def.params().get(i);
      if (sys.ObjUtil.equals(b.hasDefault(), d.hasDefault())) {
        return;
      }
      ;
      if (d.hasDefault()) {
        throw this$.err(sys.Str.plus(sys.Str.plus("Parameter '", d.name()), "' must not have default to match overridden method"), loc);
      }
      else {
        throw this$.err(sys.Str.plus(sys.Str.plus("Parameter '", d.name()), "' must have default to match overridden method"), loc);
      }
      ;
    });
    return;
  }

  checkMethodFieldOverride(t,base,def) {
    let loc = def.loc();
    let ft = def.type();
    let rt = base.returns();
    if (sys.ObjUtil.compareNE(ft, rt)) {
      if ((!ft.fits(rt) || sys.ObjUtil.compareNE(ft.isNullable(), rt.isNullable()))) {
        throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Type mismatch in override of '", base.qname()), "' - '"), rt), "' != '"), ft), "'"), loc);
      }
      ;
      if ((ft.isVal() || rt.isVal())) {
        throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Cannot use covariance with value types '", base.qname()), "' - '"), rt), "' != '"), ft), "'"), loc);
      }
      ;
    }
    ;
    if (def.isStatic()) {
      throw this.err(sys.Str.plus(sys.Str.plus("Cannot override virtual method with static field '", def.name()), "'"), loc);
    }
    ;
    if (!base.params().isEmpty()) {
      throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Field '", def.name()), "' cannot override method with params '"), base.qname()), "'"), loc);
    }
    ;
    def.inheritedRet(base.inheritedReturns());
    return;
  }

  checkFieldFieldOverride(t,base,def) {
    let loc = def.loc();
    if (sys.ObjUtil.compareNE(base.type(), def.type())) {
      throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Type mismatch in override of '", base.qname()), "' - '"), base.type()), "' != '"), def.type()), "'"), loc);
    }
    ;
    if (!base.isAbstract()) {
      def.concreteBase(base);
    }
    ;
    if (def.isConst()) {
      throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Const field '", def.name()), "' cannot override field '"), base.qname()), "'"), loc);
    }
    ;
    return;
  }

}

class InitFacet extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return InitFacet.type$; }

  #ctors = null;

  ctors(it) {
    if (it === undefined) {
      return this.#ctors;
    }
    else {
      this.#ctors = it;
      return;
    }
  }

  #fields = null;

  fields(it) {
    if (it === undefined) {
      return this.#fields;
    }
    else {
      this.#fields = it;
      return;
    }
  }

  static make(compiler) {
    const $self = new InitFacet();
    InitFacet.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    this.log().debug("InitFacet");
    this.walk(this.compiler(), VisitDepth.typeDef());
    this.bombIfErr();
    return;
  }

  visitTypeDef(t) {
    if (!t.isFacet()) {
      return;
    }
    ;
    try {
      this.initCurType();
      this.errorChecks();
      if (this.#fields.isEmpty()) {
        this.initSingleton();
      }
      else {
        this.initStruct();
      }
      ;
    }
    catch ($_u195) {
      $_u195 = sys.Err.make($_u195);
      if ($_u195 instanceof CompilerErr) {
        let e = $_u195;
        ;
      }
      else {
        throw $_u195;
      }
    }
    ;
    return;
  }

  initCurType() {
    this.#ctors = this.curType().ctorDefs();
    this.#fields = this.curType().instanceFieldDefs();
    return;
  }

  errorChecks() {
    if (!this.#ctors.isEmpty()) {
      throw this.err("Facet cannot declare constructors", this.#ctors.first().loc());
    }
    ;
    return;
  }

  initSingleton() {
    let loc = this.curType().loc();
    let m = MethodDef.make(loc, sys.ObjUtil.coerce(this.curType(), TypeDef.type$));
    m.name("make");
    m.flags(sys.Int.plus(sys.Int.plus(FConst.Ctor(), FConst.Private()), FConst.Synthetic()));
    m.returns(TypeRef.make(loc, sys.ObjUtil.coerce(this.ns().voidType(), CType.type$)));
    m.code(Block.make(loc));
    m.code().stmts().add(ReturnStmt.makeSynthetic(loc));
    this.curType().addSlot(m);
    let f = FieldDef.make(loc, sys.ObjUtil.coerce(this.curType(), TypeDef.type$));
    f.docDef(DocDef.make(loc, sys.List.make(sys.Str.type$, [sys.Str.plus(sys.Str.plus("Singleton for ", this.curType().name()), " facet.")])));
    f.flags(sys.Int.plus(sys.Int.plus(sys.Int.plus(FConst.Public(), FConst.Static()), FConst.Const()), FConst.Storage()));
    f.name("defVal");
    f.type(sys.ObjUtil.coerce(this.curType(), CType.type$));
    f.init(CallExpr.make(loc, null, "make"));
    this.curType().addSlot(f);
    return;
  }

  initStruct() {
    let loc = this.curType().loc();
    let call = CallExpr.makeWithMethod(loc, UnknownVarExpr.make(loc, null, "f"), sys.ObjUtil.coerce(this.ns().funcCall(), CMethod.type$), sys.List.make(ThisExpr.type$, [ThisExpr.make(loc)]));
    call.isSafe(true);
    let m = MethodDef.make(loc, sys.ObjUtil.coerce(this.curType(), TypeDef.type$));
    m.name("make");
    m.flags(sys.Int.plus(sys.Int.plus(FConst.Ctor(), FConst.Public()), FConst.Synthetic()));
    m.returns(TypeRef.make(loc, sys.ObjUtil.coerce(this.ns().voidType(), CType.type$)));
    m.params().add(ParamDef.make(loc, FuncType.makeItBlock(sys.ObjUtil.coerce(this.curType(), CType.type$)).toNullable(), "f", LiteralExpr.makeNull(loc, this.ns())));
    m.code(Block.make(loc));
    m.code().stmts().add(call.toStmt());
    m.code().stmts().add(ReturnStmt.makeSynthetic(loc));
    this.curType().addSlot(m);
    this.curType().addFacet(this, sys.ObjUtil.coerce(this.ns().sysPod().resolveType("Serializable", true), CType.type$));
    return;
  }

}

class LocaleProps extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return LocaleProps.type$; }

  static make(compiler) {
    const $self = new LocaleProps();
    LocaleProps.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    const this$ = this;
    this.log().debug("LocaleProps");
    if (this.compiler().localeDefs().isEmpty()) {
      return;
    }
    ;
    let dups = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::LocaleLiteralExpr"));
    this.compiler().localeDefs().each((expr) => {
      let dup = dups.get(expr.key());
      if (dup != null) {
        this$.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Duplicate locale literal defaults for key '", expr.key()), "' ["), dup.loc().toLocStr()), "]"), expr.loc());
      }
      ;
      dups.set(expr.key(), expr);
      return;
    });
    let baseProps = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("sys::Str"));
    let baseUri = this.compiler().input().baseDir().uri();
    let baseFileStr = "";
    let defFile = this.compiler().resFiles().find((f) => {
      return sys.ObjUtil.equals(f.uri().relTo(baseUri), sys.Uri.fromStr("locale/en.props"));
    });
    if (defFile != null) {
      try {
        (baseProps = defFile.readProps());
        (baseFileStr = defFile.readAllStr());
      }
      catch ($_u196) {
        $_u196 = sys.Err.make($_u196);
        if ($_u196 instanceof sys.Err) {
          let e = $_u196;
          ;
          throw this.err(sys.Str.plus("Cannot load base locale/en.props: ", e), Loc.makeFile(defFile));
        }
        else {
          throw $_u196;
        }
      }
      ;
    }
    ;
    baseProps.each((v,k) => {
      let dup = dups.get(k);
      if (dup != null) {
        this$.err(sys.Str.plus(sys.Str.plus("Duplicate locale literal defaults for key '", k), "' [locale/en.props]"), dup.loc());
      }
      ;
      return;
    });
    let buf = sys.StrBuf.make();
    if (!sys.Str.isEmpty(baseFileStr)) {
      buf.add(baseFileStr);
      buf.add("\n\n");
    }
    ;
    buf.add("// locale literal defaults\n");
    this.compiler().localeDefs().each((expr) => {
      buf.add(expr.key()).add("=").add(expr.def()).add("\n");
      return;
    });
    this.compiler().localeProps(buf.toStr());
    this.bombIfErr();
    return;
  }

}

class GenerateOutput extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return GenerateOutput.type$; }

  static make(compiler) {
    const $self = new GenerateOutput();
    GenerateOutput.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    let output = CompilerOutput.make();
    output.mode(this.compiler().input().output());
    let $_u197 = output.mode();
    if (sys.ObjUtil.equals($_u197, CompilerOutputMode.transientPod())) {
      output.transientPod(LoadPod.make(this.compiler()).load());
    }
    else if (sys.ObjUtil.equals($_u197, CompilerOutputMode.podFile())) {
      output.podFile(WritePod.make(this.compiler()).write());
    }
    else if (sys.ObjUtil.equals($_u197, CompilerOutputMode.js())) {
      output.js(this.compiler().js());
      output.cjs(this.compiler().cjs());
      output.esm(this.compiler().esm());
    }
    else {
      throw this.err(sys.Str.plus(sys.Str.plus("Unknown output type: '", output.mode()), "'"), null);
    }
    ;
    this.compiler().output(output);
    return;
  }

}

class CheckParamDefs extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return CheckParamDefs.type$; }

  #name = null;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #used = false;

  used(it) {
    if (it === undefined) {
      return this.#used;
    }
    else {
      this.#used = it;
      return;
    }
  }

  static make(compiler) {
    const $self = new CheckParamDefs();
    CheckParamDefs.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    this.walk(this.compiler(), VisitDepth.slotDef());
    return;
  }

  visitMethodDef(m) {
    const this$ = this;
    let params = m.paramDefs();
    let num = params.size();
    if ((sys.ObjUtil.equals(num, 0) || params.get(-1).def() == null)) {
      return;
    }
    ;
    sys.Int.times(num, (i) => {
      if (params.get(i).def() == null) {
        return;
      }
      ;
      let used = this$.usedInSuccDef(params, i);
      if (used != null) {
        if (sys.ObjUtil.equals(used, params.get(i))) {
          this$.err(sys.Str.plus(sys.Str.plus("Param default '", used.name()), "' cannot access itself"), used.loc());
          return;
        }
        ;
        let param = params.get(i);
        let var$ = m.vars().get(i);
        let loc = param.loc();
        if (!sys.ObjUtil.equals(param.name(), var$.name())) {
          throw this$.err("invalid state", loc);
        }
        ;
        param.def(BinaryExpr.makeAssign(LocalVarExpr.make(loc, var$), sys.ObjUtil.coerce(param.def(), Expr.type$), true));
      }
      ;
      return;
    });
    return;
  }

  usedInSuccDef(params,index) {
    this.#name = params.get(index).name();
    for (let i = index; sys.ObjUtil.compareLT(i, params.size()); i = sys.Int.increment(i)) {
      this.#used = false;
      params.get(i).def().walk(this);
      if (this.#used) {
        return params.get(i);
      }
      ;
    }
    ;
    return null;
  }

  visitExpr(expr) {
    if (expr.id() === ExprId.localVar()) {
      let local = sys.ObjUtil.coerce(expr, LocalVarExpr.type$);
      if (sys.ObjUtil.equals(this.#name, local.var().name())) {
        this.#used = true;
      }
      ;
    }
    ;
    return expr;
  }

}

class Assemble extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Assemble.type$; }

  static make(compiler) {
    const $self = new Assemble();
    Assemble.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    if (this.compiler().input().output() === CompilerOutputMode.js()) {
      return;
    }
    ;
    this.log().debug("Assemble");
    this.compiler().fpod(Assembler.make(this.compiler()).assemblePod());
    this.bombIfErr();
    if (this.compiler().input().fcodeDump()) {
      this.compiler().fpod().dump(sys.ObjUtil.coerce(this.log().out(), sys.OutStream.type$));
    }
    ;
    return;
  }

}

class InitClosures extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return InitClosures.type$; }

  #closure = null;

  closure(it) {
    if (it === undefined) {
      return this.#closure;
    }
    else {
      this.#closure = it;
      return;
    }
  }

  #loc = null;

  loc(it) {
    if (it === undefined) {
      return this.#loc;
    }
    else {
      this.#loc = it;
      return;
    }
  }

  #signature = null;

  signature(it) {
    if (it === undefined) {
      return this.#signature;
    }
    else {
      this.#signature = it;
      return;
    }
  }

  #enclosingType = null;

  enclosingType(it) {
    if (it === undefined) {
      return this.#enclosingType;
    }
    else {
      this.#enclosingType = it;
      return;
    }
  }

  #cls = null;

  cls(it) {
    if (it === undefined) {
      return this.#cls;
    }
    else {
      this.#cls = it;
      return;
    }
  }

  #ctor = null;

  ctor(it) {
    if (it === undefined) {
      return this.#ctor;
    }
    else {
      this.#ctor = it;
      return;
    }
  }

  #doCall = null;

  doCall(it) {
    if (it === undefined) {
      return this.#doCall;
    }
    else {
      this.#doCall = it;
      return;
    }
  }

  static make(compiler) {
    const $self = new InitClosures();
    InitClosures.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    const this$ = this;
    this.log().debug("Closures");
    this.compiler().closures().each((c) => {
      this$.process(c);
      return;
    });
    return;
  }

  process(c) {
    this.setup(c);
    this.genClass();
    this.genCtor();
    this.genDoCall();
    this.genCall();
    this.substitute();
    return;
  }

  setup(c) {
    this.#closure = c;
    this.#loc = c.loc();
    this.#signature = c.signature();
    this.#enclosingType = c.enclosingType();
    return;
  }

  genClass() {
    this.#cls = TypeDef.make(this.ns(), sys.ObjUtil.coerce(this.#loc, Loc.type$), this.#closure.enclosingType().unit(), this.#closure.name());
    this.#cls.flags(sys.Int.plus(sys.Int.plus(FConst.Internal(), FConst.Final()), FConst.Synthetic()));
    this.#cls.base(this.#closure.signature());
    this.#cls.closure(this.#closure);
    this.#closure.cls(this.#cls);
    this.addTypeDef(sys.ObjUtil.coerce(this.#cls, TypeDef.type$));
    return;
  }

  genCtor() {
    let code = Block.make(sys.ObjUtil.coerce(this.#loc, Loc.type$));
    code.stmts().add(ReturnStmt.makeSynthetic(sys.ObjUtil.coerce(this.#loc, Loc.type$)));
    this.#ctor = MethodDef.make(sys.ObjUtil.coerce(this.#loc, Loc.type$), sys.ObjUtil.coerce(this.#cls, TypeDef.type$));
    this.#ctor.flags(sys.Int.plus(sys.Int.plus(FConst.Internal(), FConst.Ctor()), FConst.Synthetic()));
    this.#ctor.name("make");
    this.#ctor.returns(sys.ObjUtil.coerce(this.ns().voidType(), CType.type$));
    this.#ctor.code(code);
    this.#cls.addSlot(sys.ObjUtil.coerce(this.#ctor, CSlot.type$));
    return;
  }

  genDoCall() {
    this.#doCall = MethodDef.make(sys.ObjUtil.coerce(this.#loc, Loc.type$), sys.ObjUtil.coerce(this.#cls, TypeDef.type$));
    this.#doCall.name("doCall");
    this.#doCall.flags(sys.Int.plus(FConst.Internal(), FConst.Synthetic()));
    this.#doCall.code(this.#closure.code());
    this.#doCall.returns(this.#signature.returns());
    this.#doCall.paramDefs(this.#signature.toParamDefs(sys.ObjUtil.coerce(this.#loc, Loc.type$)));
    this.#closure.doCall(this.#doCall);
    this.#cls.addSlot(sys.ObjUtil.coerce(this.#doCall, CSlot.type$));
    return;
  }

  genCall() {
    let c = CallExpr.makeWithMethod(sys.ObjUtil.coerce(this.#loc, Loc.type$), ThisExpr.make(sys.ObjUtil.coerce(this.#loc, Loc.type$)), sys.ObjUtil.coerce(this.#doCall, CMethod.type$));
    InitClosures.genMethodCall(this.compiler(), sys.ObjUtil.coerce(this.#loc, Loc.type$), sys.ObjUtil.coerce(this.#cls, TypeDef.type$), sys.ObjUtil.coerce(this.#signature, FuncType.type$), c, false);
    return;
  }

  static genMethodCall(compiler,loc,parent,signature,c,firstAsTarget) {
    const this$ = this;
    let ns = compiler.ns();
    let m = MethodDef.make(loc, parent);
    m.flags(sys.Int.plus(sys.Int.plus(FConst.Public(), FConst.Override()), FConst.Synthetic()));
    m.returns(ns.objType().toNullable());
    m.code(Block.make(loc));
    let paramCount = signature.params().size();
    let useListArgs = sys.ObjUtil.compareGT(paramCount, 8);
    if (useListArgs) {
      m.name("callList");
      let p = ParamDef.make(loc, ns.objType().toListOf(), "list");
      m.params().add(p);
    }
    else {
      m.name("call");
      sys.Int.times(paramCount, (i) => {
        let p = ParamDef.make(loc, ns.objType().toNullable(), sys.Str.plus("p", sys.ObjUtil.coerce(i, sys.Obj.type$.toNullable())));
        m.paramDefs().add(p);
        return;
      });
    }
    ;
    sys.Int.times(paramCount, (i) => {
      let arg = null;
      if (useListArgs) {
        let listGet = CallExpr.makeWithMethod(loc, UnknownVarExpr.make(loc, null, "list"), sys.ObjUtil.coerce(ns.objType().toListOf().method("get"), CMethod.type$));
        listGet.args().add(LiteralExpr.make(loc, ExprId.intLiteral(), sys.ObjUtil.coerce(ns.intType(), CType.type$), sys.ObjUtil.coerce(i, sys.Obj.type$.toNullable())));
        (arg = listGet);
      }
      else {
        (arg = UnknownVarExpr.make(loc, null, sys.Str.plus("p", sys.ObjUtil.coerce(i, sys.Obj.type$.toNullable()))));
      }
      ;
      (arg = TypeCheckExpr.coerce(sys.ObjUtil.coerce(arg, Expr.type$), signature.params().get(i)));
      if ((firstAsTarget && sys.ObjUtil.equals(i, 0))) {
        c.target(arg);
      }
      else {
        c.args().add(sys.ObjUtil.coerce(arg, Expr.type$));
      }
      ;
      return;
    });
    if (signature.returns().isVoid()) {
      m.code().add(c.toStmt());
      m.code().add(ReturnStmt.makeSynthetic(loc, LiteralExpr.makeNull(loc, sys.ObjUtil.coerce(ns, CNamespace.type$))));
    }
    else {
      m.code().add(ReturnStmt.makeSynthetic(loc, c));
    }
    ;
    parent.addSlot(m);
    if (parent.isClosure()) {
      parent.closure().call(m);
    }
    ;
    return m;
  }

  substitute() {
    this.#closure.code(null);
    this.#closure.substitute(CallExpr.makeWithMethod(sys.ObjUtil.coerce(this.#loc, Loc.type$), null, sys.ObjUtil.coerce(this.#ctor, CMethod.type$)));
    return;
  }

}

class ScanForUsingsAndTypes extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ScanForUsingsAndTypes.type$; }

  static make(compiler) {
    const $self = new ScanForUsingsAndTypes();
    ScanForUsingsAndTypes.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    const this$ = this;
    this.log().debug("ScanForUsingsAndTypes");
    let allTypes = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::TypeDef"));
    this.units().each((unit) => {
      UsingAndTypeScanner.make(this$.compiler(), unit, allTypes).parse();
      return;
    });
    this.bombIfErr();
    this.pod().typeDefs(allTypes);
    return;
  }

}

class UsingAndTypeScanner extends CompilerSupport {
  constructor() {
    super();
    const this$ = this;
    this.#isSys = false;
    return;
  }

  typeof() { return UsingAndTypeScanner.type$; }

  #unit = null;

  // private field reflection only
  __unit(it) { if (it === undefined) return this.#unit; else this.#unit = it; }

  #tokens = null;

  // private field reflection only
  __tokens(it) { if (it === undefined) return this.#tokens; else this.#tokens = it; }

  #pos = 0;

  // private field reflection only
  __pos(it) { if (it === undefined) return this.#pos; else this.#pos = it; }

  #isSys = false;

  // private field reflection only
  __isSys(it) { if (it === undefined) return this.#isSys; else this.#isSys = it; }

  #allTypes = null;

  // private field reflection only
  __allTypes(it) { if (it === undefined) return this.#allTypes; else this.#allTypes = it; }

  static make(compiler,unit,allTypes) {
    const $self = new UsingAndTypeScanner();
    UsingAndTypeScanner.make$($self,compiler,unit,allTypes);
    return $self;
  }

  static make$($self,compiler,unit,allTypes) {
    CompilerSupport.make$($self, compiler);
    ;
    $self.#unit = unit;
    $self.#tokens = sys.ObjUtil.coerce(unit.tokens(), sys.Type.find("compiler::TokenVal[]"));
    $self.#pos = 0;
    $self.#allTypes = allTypes;
    $self.#isSys = compiler.isSys();
    return;
  }

  parse() {
    const this$ = this;
    if (!this.#isSys) {
      this.#unit.usings().add(sys.ObjUtil.coerce(sys.ObjUtil.with(Using.make(this.#unit.loc()), (it) => {
        it.podName("sys");
        return;
      }), Using.type$));
    }
    ;
    let inClassHeader = false;
    while (true) {
      let tok = this.consume();
      if (tok.kind() === Token.eof()) {
        break;
      }
      ;
      let $_u198 = tok.kind();
      if (sys.ObjUtil.equals($_u198, Token.usingKeyword())) {
        this.parseUsing(tok);
      }
      else if (sys.ObjUtil.equals($_u198, Token.lbrace())) {
        (inClassHeader = false);
      }
      else if (sys.ObjUtil.equals($_u198, Token.classKeyword()) || sys.ObjUtil.equals($_u198, Token.mixinKeyword())) {
        if (!inClassHeader) {
          (inClassHeader = true);
          this.parseType(tok);
        }
        ;
      }
      ;
    }
    ;
    return;
  }

  parseUsing(tok) {
    let u = Using.make(tok);
    if (this.curt() === Token.strLiteral()) {
      u.podName(sys.ObjUtil.coerce(this.consume(Token.strLiteral()).val(), sys.Str.type$));
    }
    else {
      u.podName("");
      if (this.curt() === Token.lbracket()) {
        this.consume();
        u.podName(sys.Str.plus(sys.Str.plus("[", this.consumeId()), "]"));
        this.consume(Token.rbracket());
      }
      ;
      u.podName(sys.Str.plus(u.podName(), this.consumeId()));
      while (this.curt() === Token.dot()) {
        this.consume();
        u.podName(sys.Str.plus(u.podName(), sys.Str.plus(".", this.consumeId())));
      }
      ;
    }
    ;
    if (this.curt() === Token.doubleColon()) {
      this.consume();
      u.typeName(this.consumeId());
      while (this.curt() === Token.dollar()) {
        this.consume();
        u.typeName(sys.Str.plus(u.typeName(), "\$"));
        if (sys.ObjUtil.equals(this.curt(), Token.identifier())) {
          u.typeName(sys.Str.plus(u.typeName(), this.consumeId()));
        }
        ;
      }
      ;
      if (this.curt() === Token.asKeyword()) {
        this.consume();
        u.asName(this.consumeId());
      }
      ;
    }
    ;
    this.#unit.usings().add(u);
    return;
  }

  parseType(tok) {
    let name = this.consumeId();
    let typeDef = TypeDef.make(this.ns(), tok, this.#unit, name);
    this.#unit.types().add(typeDef);
    if (tok.kind() === Token.mixinKeyword()) {
      typeDef.flags(sys.Int.or(typeDef.flags(), FConst.Mixin()));
    }
    ;
    if (this.#allTypes.containsKey(name)) {
      this.err(sys.Str.plus(sys.Str.plus("Duplicate type name '", name), "'"), typeDef.loc());
    }
    else {
      this.#allTypes.set(name, typeDef);
    }
    ;
    return;
  }

  consumeId() {
    let id = this.consume();
    if (sys.ObjUtil.compareNE(id.kind(), Token.identifier())) {
      this.err("Expected identifier", id);
      return "";
    }
    ;
    return sys.ObjUtil.coerce(id.val(), sys.Str.type$);
  }

  verify(expected) {
    if (this.curt() !== expected) {
      this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Expected '", expected.symbol()), "', not '"), this.#tokens.get(this.#pos)), "'"), this.#tokens.get(this.#pos));
    }
    ;
    return;
  }

  consume(expected) {
    if (expected === undefined) expected = null;
    if (expected != null) {
      this.verify(sys.ObjUtil.coerce(expected, Token.type$));
    }
    ;
    return this.#tokens.get(((this$) => { let $_u199 = this$.#pos;this$.#pos = sys.Int.increment(this$.#pos); return $_u199; })(this));
  }

  curt() {
    return this.#tokens.get(this.#pos).kind();
  }

}

class ClosureToImmutable extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ClosureToImmutable.type$; }

  static make(compiler) {
    const $self = new ClosureToImmutable();
    ClosureToImmutable.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    const this$ = this;
    this.compiler().closures().each((c) => {
      this$.process(c);
      return;
    });
    return;
  }

  process(closure) {
    let cls = closure.cls();
    if (this.isAlwaysImmutable(sys.ObjUtil.coerce(cls, TypeDef.type$))) {
      this.genIsImmutable(sys.ObjUtil.coerce(cls, TypeDef.type$), LiteralExpr.makeTrue(cls.loc(), this.ns()));
      this.setAllFieldsConst(sys.ObjUtil.coerce(cls, TypeDef.type$));
      return;
    }
    ;
    let never = this.isNeverImmutable(sys.ObjUtil.coerce(cls, TypeDef.type$));
    if (never != null) {
      this.genToImmutableErr(sys.ObjUtil.coerce(cls, TypeDef.type$), sys.ObjUtil.coerce(never, sys.Str.type$));
      return;
    }
    ;
    this.genToImmutable(sys.ObjUtil.coerce(cls, TypeDef.type$));
    return;
  }

  isAlwaysImmutable(cls) {
    const this$ = this;
    return cls.fieldDefs().all((f) => {
      return f.type().isConst();
    });
  }

  isNeverImmutable(cls) {
    const this$ = this;
    let field = cls.fieldDefs().find((f) => {
      return !f.type().isConstFieldType();
    });
    if (field == null) {
      return null;
    }
    ;
    return sys.Str.plus("Closure field not const: ", ((this$) => { let $_u200 = field.closureInfo(); if ($_u200 != null) return $_u200; return field.name(); })(this));
  }

  setAllFieldsConst(cls) {
    const this$ = this;
    cls.fieldDefs().each((f) => {
      f.flags(sys.Int.or(f.flags(), FConst.Const()));
      return;
    });
    return;
  }

  genIsImmutable(cls,result) {
    let loc = cls.loc();
    let m = MethodDef.make(loc, cls);
    m.flags(sys.Int.plus(sys.Int.plus(FConst.Public(), FConst.Synthetic()), FConst.Override()));
    m.name("isImmutable");
    m.returns(sys.ObjUtil.coerce(this.ns().boolType(), CType.type$));
    m.code(Block.make(loc));
    m.code().stmts().add(ReturnStmt.makeSynthetic(loc, result));
    cls.addSlot(m);
    return;
  }

  genToImmutableErr(cls,msg) {
    let loc = cls.loc();
    let m = this.stubToImmutable(cls);
    let ctor = CallExpr.makeWithMethod(loc, null, sys.ObjUtil.coerce(this.ns().notImmutableErrMake(), CMethod.type$), sys.List.make(LiteralExpr.type$, [LiteralExpr.makeStr(loc, this.ns(), msg)]));
    m.code().add(ThrowStmt.make(loc, ctor));
    return;
  }

  genToImmutable(cls) {
    const this$ = this;
    let loc = cls.loc();
    let immutableField = FieldDef.make(loc, cls);
    immutableField.name("immutable");
    immutableField.type(sys.ObjUtil.coerce(this.ns().boolType(), CType.type$));
    immutableField.flags(sys.Int.plus(sys.Int.plus(FConst.Private(), FConst.Storage()), FConst.Synthetic()));
    cls.addSlot(immutableField);
    this.genIsImmutable(cls, FieldExpr.make(loc, ThisExpr.make(loc, cls), immutableField, false));
    let m = this.stubToImmutable(cls);
    let ctor = cls.method("make");
    let args = sys.List.make(Expr.type$);
    ctor.params().each((param) => {
      let field = cls.field(param.name());
      if (field == null) {
        throw sys.Err.make(sys.Str.plus("Closure param missing matched field ", param.name()));
      }
      ;
      let fieldGet = FieldExpr.make(loc, ThisExpr.make(loc, cls), field, false);
      if (field.type().isConst()) {
        args.add(fieldGet);
      }
      else {
        let call = CallExpr.makeWithMethod(loc, fieldGet, sys.ObjUtil.coerce(this$.ns().objToImmutable(), CMethod.type$));
        args.add(TypeCheckExpr.coerce(call, field.type()));
      }
      ;
      return;
    });
    let makeCall = CallExpr.makeWithMethod(loc, null, sys.ObjUtil.coerce(ctor, CMethod.type$), args);
    let temp = m.addLocalVar(cls, "temp", m.code());
    let init = BinaryExpr.makeAssign(LocalVarExpr.make(loc, temp), makeCall);
    m.code().add(LocalDefStmt.makeAssign(init));
    m.code().add(BinaryExpr.makeAssign(FieldExpr.make(loc, LocalVarExpr.make(loc, temp), immutableField, false), LiteralExpr.makeTrue(loc, this.ns())).toStmt());
    m.code().add(ReturnStmt.makeSynthetic(loc, LocalVarExpr.make(loc, temp)));
    return;
  }

  stubToImmutable(cls) {
    let loc = cls.loc();
    let m = MethodDef.make(loc, cls);
    m.flags(sys.Int.plus(sys.Int.plus(FConst.Public(), FConst.Synthetic()), FConst.Override()));
    m.name("toImmutable");
    m.returns(sys.ObjUtil.coerce(this.ns().objType(), CType.type$));
    m.code(Block.make(loc));
    cls.addSlot(m);
    return m;
  }

}

class ConstChecks extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ConstChecks.type$; }

  #curCtor = null;

  curCtor(it) {
    if (it === undefined) {
      return this.#curCtor;
    }
    else {
      this.#curCtor = it;
      return;
    }
  }

  #fieldCheck = null;

  fieldCheck(it) {
    if (it === undefined) {
      return this.#fieldCheck;
    }
    else {
      this.#fieldCheck = it;
      return;
    }
  }

  static make(compiler) {
    const $self = new ConstChecks();
    ConstChecks.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    return;
  }

  run() {
    const this$ = this;
    this.log().debug("ConstChecks");
    this.compiler().closures().each((c) => {
      this$.processClosure(c);
      return;
    });
    this.types().each((t) => {
      if (t.isNative()) {
        return;
      }
      ;
      this$.curType(t);
      this$.#fieldCheck = this$.genFieldCheck(t);
      t.ctorDefs().each((ctor) => {
        this$.processCtor(ctor);
        return;
      });
      return;
    });
    return;
  }

  processClosure(c) {
    if ((!c.isItBlock() || !c.setsConst())) {
      return;
    }
    ;
    let loc = c.loc();
    let check = CallExpr.makeWithMethod(loc, ThisExpr.make(loc), sys.ObjUtil.coerce(this.ns().funcCheckInCtor(), CMethod.type$), sys.List.make(ItExpr.type$, [ItExpr.make(loc)]));
    check.noLeave();
    c.doCall().code().stmts().insert(0, check.toStmt());
    return;
  }

  processCtor(ctor) {
    if (ctor.isStatic()) {
      return;
    }
    ;
    this.#curCtor = ctor;
    if (ctor.isItBlockCtor()) {
      let loc = ctor.loc();
      let enter = CallExpr.makeWithMethod(loc, LocalVarExpr.make(loc, this.itBlockVar()), sys.ObjUtil.coerce(this.ns().funcEnterCtor(), CMethod.type$), sys.List.make(ThisExpr.type$, [ThisExpr.make(loc)]));
      enter.isSafe(true);
      enter.noLeave();
      ctor.code().stmts().insert(0, enter.toStmt());
    }
    ;
    if ((ctor.isItBlockCtor() || this.#fieldCheck != null)) {
      ctor.code().walk(this, VisitDepth.stmt());
    }
    ;
    return;
  }

  visitStmt(stmt) {
    if (stmt.id() !== StmtId.returnStmt()) {
      return null;
    }
    ;
    let loc = stmt.loc();
    let result = sys.List.make(Stmt.type$);
    if (this.#curCtor.isItBlockCtor()) {
      let exit1 = CallExpr.makeWithMethod(loc, LocalVarExpr.make(loc, this.itBlockVar()), sys.ObjUtil.coerce(this.ns().funcExitCtor(), CMethod.type$));
      exit1.isSafe(true);
      exit1.noLeave();
      result.add(exit1.toStmt());
    }
    ;
    if (this.#fieldCheck != null) {
      let exit2 = CallExpr.makeWithMethod(loc, ThisExpr.make(loc), sys.ObjUtil.coerce(this.#fieldCheck, CMethod.type$));
      exit2.noLeave();
      result.add(exit2.toStmt());
    }
    ;
    return result.add(stmt);
  }

  itBlockVar() {
    return this.#curCtor.vars().get(sys.Int.minus(this.#curCtor.params().size(), 1));
  }

  genFieldCheck(t) {
    const this$ = this;
    let checkedFields = t.fieldDefs().findAll((f) => {
      return f.requiresNullCheck();
    });
    if (checkedFields.isEmpty()) {
      return null;
    }
    ;
    let loc = t.loc();
    let block = Block.make(loc);
    checkedFields.each((f) => {
      let condExpr = UnaryExpr.make(loc, ExprId.cmpNull(), Token.same(), FieldExpr.make(loc, ThisExpr.make(loc), f, false));
      let throwExpr = ThrowStmt.make(loc, CallExpr.makeWithMethod(loc, null, sys.ObjUtil.coerce(this$.ns().fieldNotSetErrMake(), CMethod.type$), sys.List.make(LiteralExpr.type$, [LiteralExpr.makeStr(loc, this$.ns(), f.qname())])));
      let trueBlock = Block.make(loc);
      trueBlock.stmts().add(throwExpr);
      block.stmts().add(IfStmt.make(loc, condExpr, trueBlock));
      return;
    });
    block.stmts().add(ReturnStmt.makeSynthetic(loc));
    let m = MethodDef.make(loc, t);
    m.flags(sys.Int.or(FConst.Private(), FConst.Synthetic()));
    m.name(sys.Str.plus("checkFields\$", t.name()));
    m.returns(sys.ObjUtil.coerce(this.ns().voidType(), CType.type$));
    m.code(block);
    t.addSlot(m);
    return m;
  }

}

class WritePod extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
    this.#noFacets = sys.List.make(FacetDef.type$).ro();
    return;
  }

  typeof() { return WritePod.type$; }

  #loc = null;

  // private field reflection only
  __loc(it) { if (it === undefined) return this.#loc; else this.#loc = it; }

  #noFacets = null;

  // private field reflection only
  __noFacets(it) { if (it === undefined) return this.#noFacets; else this.#noFacets = it; }

  static make(compiler) {
    const $self = new WritePod();
    WritePod.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    ;
    return;
  }

  run() {
    throw sys.UnsupportedErr.make();
  }

  write() {
    const this$ = this;
    let dir = this.compiler().input().outDir();
    let fpod = this.compiler().fpod();
    let podName = fpod.name();
    let podFile = dir.plus(sys.Str.toUri(sys.Str.plus(sys.Str.plus("", podName), ".pod")));
    this.#loc = Loc.makeFile(podFile);
    this.log().info(sys.Str.plus(sys.Str.plus("WritePod [", podFile.toStr()), "]"));
    dir.create();
    let zip = null;
    try {
      (zip = sys.Zip.write(podFile.out()));
      fpod.write(sys.ObjUtil.coerce(zip, sys.Zip.type$));
      if (this.compiler().js() != null) {
        this.writeStr(sys.ObjUtil.coerce(zip, sys.Zip.type$), sys.Str.toUri(sys.Str.plus(sys.Str.plus("", podName), ".js")), sys.ObjUtil.coerce(this.compiler().js(), sys.Str.type$));
        if (this.compiler().jsSourceMap() != null) {
          this.writeStr(sys.ObjUtil.coerce(zip, sys.Zip.type$), sys.Str.toUri(sys.Str.plus(sys.Str.plus("", podName), ".js.map")), sys.ObjUtil.coerce(this.compiler().jsSourceMap(), sys.Str.type$));
        }
        ;
      }
      ;
      if (this.compiler().cjs() != null) {
        this.writeStr(sys.ObjUtil.coerce(zip, sys.Zip.type$), sys.Str.toUri(sys.Str.plus(sys.Str.plus("js/", podName), ".js")), sys.ObjUtil.coerce(this.compiler().cjs(), sys.Str.type$));
        if (this.compiler().cjsSourceMap() != null) {
          this.writeStr(sys.ObjUtil.coerce(zip, sys.Zip.type$), sys.Str.toUri(sys.Str.plus(sys.Str.plus("js/", podName), ".js.map")), sys.ObjUtil.coerce(this.compiler().cjsSourceMap(), sys.Str.type$));
        }
        ;
      }
      ;
      if (this.compiler().esm() != null) {
        this.writeStr(sys.ObjUtil.coerce(zip, sys.Zip.type$), sys.Str.toUri(sys.Str.plus(sys.Str.plus("js/", podName), ".mjs")), sys.ObjUtil.coerce(this.compiler().esm(), sys.Str.type$));
        if (this.compiler().tsDecl() != null) {
          this.writeStr(sys.ObjUtil.coerce(zip, sys.Zip.type$), sys.Str.toUri(sys.Str.plus(sys.Str.plus("js/", podName), ".d.ts")), sys.ObjUtil.coerce(this.compiler().tsDecl(), sys.Str.type$));
        }
        ;
      }
      ;
      if (this.compiler().localeProps() != null) {
        this.writeStr(sys.ObjUtil.coerce(zip, sys.Zip.type$), sys.Uri.fromStr("locale/en.props"), sys.ObjUtil.coerce(this.compiler().localeProps(), sys.Str.type$));
      }
      ;
      if (this.compiler().resFiles() != null) {
        this.compiler().resFiles().each((file) => {
          this$.writeRes(sys.ObjUtil.coerce(zip, sys.Zip.type$), file);
          return;
        });
      }
      ;
      if (this.compiler().input().includeDoc()) {
        this.writeDocs(sys.ObjUtil.coerce(zip, sys.Zip.type$));
      }
      ;
      if (this.compiler().input().includeSrc()) {
        this.compiler().srcFiles().each((file) => {
          this$.writeSrc(sys.ObjUtil.coerce(zip, sys.Zip.type$), file);
          return;
        });
      }
      ;
    }
    catch ($_u201) {
      $_u201 = sys.Err.make($_u201);
      if ($_u201 instanceof CompilerErr) {
        let e = $_u201;
        ;
        throw e;
      }
      else if ($_u201 instanceof sys.Err) {
        let e = $_u201;
        ;
        e.trace();
        throw this.errReport(CompilerErr.make("Cannot write", this.#loc, e));
      }
      else {
        throw $_u201;
      }
    }
    ;
    if (zip != null) {
      zip.close();
    }
    ;
    return podFile;
  }

  writeStr(zip,path,content) {
    try {
      zip.writeNext(path, sys.DateTime.now()).print(content).close();
    }
    catch ($_u202) {
      $_u202 = sys.Err.make($_u202);
      if ($_u202 instanceof sys.Err) {
        let e = $_u202;
        ;
        throw this.errReport(CompilerErr.make(sys.Str.plus(sys.Str.plus("Cannot write resource '", path), "'"), this.#loc, e));
      }
      else {
        throw $_u202;
      }
    }
    ;
    return;
  }

  writeRes(zip,file,path) {
    if (path === undefined) path = null;
    let input = this.compiler().input();
    if (path == null) {
      (path = file.uri());
      (path = path.relTo(input.baseDir().uri()));
    }
    ;
    if (sys.ObjUtil.equals(file.name(), ".DS_Store")) {
      return;
    }
    ;
    if ((sys.ObjUtil.equals(path, sys.Uri.fromStr("locale/en.props")) && this.compiler().localeProps() != null)) {
      return;
    }
    ;
    if (sys.ObjUtil.equals(file.ext(), "jar")) {
      this.writeResZip(zip, file);
      return;
    }
    ;
    try {
      let out = zip.writeNext(sys.ObjUtil.coerce(path, sys.Uri.type$), sys.ObjUtil.coerce(file.modified(), sys.DateTime.type$));
      file.in().pipe(out);
      out.close();
    }
    catch ($_u203) {
      $_u203 = sys.Err.make($_u203);
      if ($_u203 instanceof sys.Err) {
        let e = $_u203;
        ;
        if (!file.isDir()) {
          throw this.errReport(CompilerErr.make(sys.Str.plus(sys.Str.plus(sys.Str.plus("Cannot write resource file '", path), "': "), e), this.#loc, e));
        }
        ;
      }
      else {
        throw $_u203;
      }
    }
    ;
    return;
  }

  writeResZip(zip,resFile) {
    const this$ = this;
    let resZip = null;
    try {
      (resZip = sys.Zip.open(resFile));
    }
    catch ($_u204) {
      $_u204 = sys.Err.make($_u204);
      if ($_u204 instanceof sys.Err) {
        let e = $_u204;
        ;
        this.errReport(CompilerErr.make("Cannot open resource file as zip: 'f'", this.#loc, e));
      }
      else {
        throw $_u204;
      }
    }
    ;
    try {
      resZip.contents().each((c) => {
        this$.writeRes(zip, c, c.uri());
        return;
      });
    }
    finally {
      resZip.close();
    }
    ;
    return;
  }

  writeSrc(zip,file) {
    this.writeRes(zip, file, sys.Str.toUri(sys.Str.plus("src/", file.name())));
    return;
  }

  writeDocs(zip) {
    const this$ = this;
    this.writePodDocs(zip);
    this.compiler().types().each((type) => {
      if (type.isDocumented()) {
        this$.writeApiDoc(zip, type);
      }
      ;
      return;
    });
    return;
  }

  writePodDocs(zip) {
    const this$ = this;
    let files = this.compiler().input().baseDir().list().findAll((f) => {
      return sys.ObjUtil.equals(f.ext(), "fandoc");
    });
    files.each((f) => {
      this$.writeRes(zip, f, sys.Str.toUri(sys.Str.plus("doc/", f.name())));
      return;
    });
    return;
  }

  writeApiDoc(zip,t) {
    try {
      let out = zip.writeNext(sys.Str.toUri(sys.Str.plus(sys.Str.plus("doc/", t.name()), ".apidoc")));
      ApiDocWriter.make(out).writeType(t).close();
    }
    catch ($_u205) {
      $_u205 = sys.Err.make($_u205);
      if ($_u205 instanceof sys.Err) {
        let e = $_u205;
        ;
        throw this.errReport(CompilerErr.make(sys.Str.plus(sys.Str.plus("Cannot write fandoc '", t.name()), "'"), t.loc(), e));
      }
      else {
        throw $_u205;
      }
    }
    ;
    return;
  }

}

class OrderByInheritance extends CompilerStep {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return OrderByInheritance.type$; }

  #processing = null;

  processing(it) {
    if (it === undefined) {
      return this.#processing;
    }
    else {
      this.#processing = it;
      return;
    }
  }

  #todo = null;

  todo(it) {
    if (it === undefined) {
      return this.#todo;
    }
    else {
      this.#todo = it;
      return;
    }
  }

  #ordered = null;

  ordered(it) {
    if (it === undefined) {
      return this.#ordered;
    }
    else {
      this.#ordered = it;
      return;
    }
  }

  static make(compiler) {
    const $self = new OrderByInheritance();
    OrderByInheritance.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerStep.make$($self, compiler);
    $self.#ordered = sys.List.make(TypeDef.type$);
    $self.#processing = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::TypeDef"));
    $self.#todo = sys.Map.__fromLiteral([], [], sys.Type.find("sys::Str"), sys.Type.find("compiler::TypeDef"));
    return;
  }

  run() {
    const this$ = this;
    this.log().debug("OrderByInheritance");
    this.#ordered.capacity(this.types().size());
    this.types().each((t) => {
      this$.#todo.set(t.qname(), t);
      return;
    });
    this.bombIfErr();
    this.types().each((t) => {
      this$.process(t);
      return;
    });
    this.bombIfErr();
    if (sys.ObjUtil.compareNE(this.#ordered.size(), this.compiler().types().size())) {
      throw sys.Err.make("Internal error");
    }
    ;
    this.compiler().types(this.#ordered);
    return;
  }

  process(t) {
    const this$ = this;
    let def = this.#todo.get(t.qname());
    if (def == null) {
      return;
    }
    ;
    if (this.#processing.containsKey(def.qname())) {
      this.err(sys.Str.plus(sys.Str.plus("Cyclic inheritance for '", def.name()), "'"), def.loc());
      return;
    }
    ;
    this.#processing.set(def.qname(), sys.ObjUtil.coerce(def, TypeDef.type$));
    if (def.base() != null) {
      this.process(sys.ObjUtil.coerce(def.base(), CType.type$));
    }
    ;
    def.mixins().each((m) => {
      this$.process(m);
      return;
    });
    this.#processing.remove(def.qname());
    this.#todo.remove(def.qname());
    this.#ordered.add(sys.ObjUtil.coerce(def, TypeDef.type$));
    return;
  }

}

class ConstantFolder extends CompilerSupport {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ConstantFolder.type$; }

  static #never = undefined;

  static never() {
    if (ConstantFolder.#never === undefined) {
      ConstantFolder.static$init();
      if (ConstantFolder.#never === undefined) ConstantFolder.#never = null;
    }
    return ConstantFolder.#never;
  }

  static make(compiler) {
    const $self = new ConstantFolder();
    ConstantFolder.make$($self,compiler);
    return $self;
  }

  static make$($self,compiler) {
    CompilerSupport.make$($self, compiler);
    return;
  }

  fold(call) {
    let name = call.name();
    if (ConstantFolder.never().containsKey(sys.ObjUtil.coerce(name, sys.Str.type$))) {
      return call;
    }
    ;
    let target = this.exprToConst(call.target());
    if (target == null) {
      return call;
    }
    ;
    let args = null;
    for (let i = 0; sys.ObjUtil.compareLT(i, call.args().size()); i = sys.Int.increment(i)) {
      let arg = this.exprToConst(call.args().get(i));
      if (arg == null) {
        return call;
      }
      ;
      if (sys.ObjUtil.equals(i, 0)) {
        (args = sys.List.make(sys.Obj.type$.toNullable()));
      }
      ;
      args.add(sys.ObjUtil.coerce(arg, sys.Obj.type$));
    }
    ;
    let method = sys.ObjUtil.typeof(target).method(sys.ObjUtil.coerce(name, sys.Str.type$), false);
    if ((method == null || method.isStatic())) {
      return call;
    }
    ;
    if (sys.ObjUtil.compareNE(call.args().size(), method.params().size())) {
      if ((sys.ObjUtil.compareGE(call.args().size(), method.params().size()) || !method.params().get(call.args().size()).hasDefault())) {
        return call;
      }
      ;
    }
    ;
    let result = null;
    try {
      (result = method.callOn(target, args));
    }
    catch ($_u206) {
      return call;
    }
    ;
    return this.constToExpr(call, result);
  }

  exprToConst(expr) {
    if (expr == null) {
      return null;
    }
    ;
    let $_u207 = expr.id();
    if (sys.ObjUtil.equals($_u207, ExprId.intLiteral())) {
      return sys.ObjUtil.coerce(expr, LiteralExpr.type$).val();
    }
    else if (sys.ObjUtil.equals($_u207, ExprId.floatLiteral())) {
      return sys.ObjUtil.coerce(expr, LiteralExpr.type$).val();
    }
    else if (sys.ObjUtil.equals($_u207, ExprId.decimalLiteral())) {
      return sys.ObjUtil.coerce(expr, LiteralExpr.type$).val();
    }
    else if (sys.ObjUtil.equals($_u207, ExprId.strLiteral())) {
      return sys.ObjUtil.coerce(expr, LiteralExpr.type$).val();
    }
    else if (sys.ObjUtil.equals($_u207, ExprId.durationLiteral())) {
      return sys.ObjUtil.coerce(expr, LiteralExpr.type$).val();
    }
    else {
      return null;
    }
    ;
  }

  constToExpr(orig,val) {
    if (val == null) {
      return LiteralExpr.make(orig.loc(), ExprId.nullLiteral(), this.ns().objType().toNullable(), null);
    }
    ;
    if (sys.ObjUtil.is(val, sys.Int.type$)) {
      return LiteralExpr.make(orig.loc(), ExprId.intLiteral(), sys.ObjUtil.coerce(this.ns().intType(), CType.type$), val);
    }
    ;
    if (sys.ObjUtil.is(val, sys.Float.type$)) {
      return LiteralExpr.make(orig.loc(), ExprId.floatLiteral(), sys.ObjUtil.coerce(this.ns().floatType(), CType.type$), val);
    }
    ;
    if (sys.ObjUtil.is(val, sys.Decimal.type$)) {
      return LiteralExpr.make(orig.loc(), ExprId.decimalLiteral(), sys.ObjUtil.coerce(this.ns().decimalType(), CType.type$), val);
    }
    ;
    if (sys.ObjUtil.is(val, sys.Str.type$)) {
      return LiteralExpr.make(orig.loc(), ExprId.strLiteral(), sys.ObjUtil.coerce(this.ns().strType(), CType.type$), val);
    }
    ;
    if (sys.ObjUtil.is(val, sys.Duration.type$)) {
      return LiteralExpr.make(orig.loc(), ExprId.durationLiteral(), sys.ObjUtil.coerce(this.ns().durationType(), CType.type$), val);
    }
    ;
    return orig;
  }

  static static$init() {
    ConstantFolder.#never = sys.ObjUtil.coerce(sys.ObjUtil.toImmutable(sys.Map.__fromLiteral(["compare","equal","hash","intern","toLocale","fromLocale"], [sys.ObjUtil.coerce(1, sys.Int.type$.toNullable()),sys.ObjUtil.coerce(1, sys.Int.type$.toNullable()),sys.ObjUtil.coerce(1, sys.Int.type$.toNullable()),sys.ObjUtil.coerce(1, sys.Int.type$.toNullable()),sys.ObjUtil.coerce(1, sys.Int.type$.toNullable()),sys.ObjUtil.coerce(1, sys.Int.type$.toNullable())], sys.Type.find("sys::Str"), sys.Type.find("sys::Int"))), sys.Type.find("[sys::Str:sys::Int]"));
    return;
  }

}

class ApiDocWriter extends sys.Obj {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return ApiDocWriter.type$; }

  static #typePrefix = undefined;

  static typePrefix() {
    if (ApiDocWriter.#typePrefix === undefined) {
      ApiDocWriter.static$init();
      if (ApiDocWriter.#typePrefix === undefined) ApiDocWriter.#typePrefix = null;
    }
    return ApiDocWriter.#typePrefix;
  }

  static #slotPrefix = undefined;

  static slotPrefix() {
    if (ApiDocWriter.#slotPrefix === undefined) {
      ApiDocWriter.static$init();
      if (ApiDocWriter.#slotPrefix === undefined) ApiDocWriter.#slotPrefix = null;
    }
    return ApiDocWriter.#slotPrefix;
  }

  static #protectionMask = undefined;

  static protectionMask() {
    if (ApiDocWriter.#protectionMask === undefined) {
      ApiDocWriter.static$init();
      if (ApiDocWriter.#protectionMask === undefined) ApiDocWriter.#protectionMask = 0;
    }
    return ApiDocWriter.#protectionMask;
  }

  #out = null;

  out(it) {
    if (it === undefined) {
      return this.#out;
    }
    else {
      this.#out = it;
      return;
    }
  }

  static make(out) {
    const $self = new ApiDocWriter();
    ApiDocWriter.make$($self,out);
    return $self;
  }

  static make$($self,out) {
    $self.#out = out;
    return;
  }

  close() {
    return this.#out.close();
  }

  writeType(t) {
    const this$ = this;
    this.w(ApiDocWriter.typePrefix()).w(t.name()).w("\n");
    this.writeAttr("base", this.encodeBase(t));
    this.writeAttr("mixins", this.encodeMixins(t));
    this.writeAttr("flags", ApiDocWriter.encodeFlags(t.flags()));
    this.writeAttr("loc", this.encodeLoc(t, true));
    this.writeFacets(t.facets());
    this.writeDoc(t);
    t.slotDefs().each((slot) => {
      if (slot.isDocumented()) {
        this$.writeSlot(slot);
      }
      ;
      return;
    });
    return this;
  }

  writeSlot(s) {
    if (sys.ObjUtil.is(s, FieldDef.type$)) {
      this.writeFieldStart(sys.ObjUtil.coerce(s, FieldDef.type$));
    }
    else {
      this.writeMethodStart(sys.ObjUtil.coerce(s, MethodDef.type$));
    }
    ;
    this.writeAttr("flags", ApiDocWriter.encodeFlags(s.flags()));
    this.writeAttr("loc", this.encodeLoc(s, false));
    this.writeFacets(s.facets());
    this.writeDoc(s);
    return;
  }

  writeFieldStart(f) {
    this.w(ApiDocWriter.slotPrefix()).w(f.name()).w(" ").w(f.type().signature());
    if (f.init() != null) {
      this.w(":=").w(ApiDocWriter.encodeExpr(sys.ObjUtil.coerce(f.init(), Expr.type$)));
    }
    ;
    this.w("\n");
    if ((f.setter() != null && sys.ObjUtil.compareNE(sys.Int.and(f.flags(), ApiDocWriter.protectionMask()), sys.Int.and(f.setter().flags(), ApiDocWriter.protectionMask())))) {
      this.writeAttr("set", ApiDocWriter.encodeFlags(sys.Int.and(f.setter().flags(), ApiDocWriter.protectionMask())));
    }
    ;
    return;
  }

  writeMethodStart(m) {
    const this$ = this;
    this.w(ApiDocWriter.slotPrefix()).w(m.name()).w("(\n");
    m.paramDefs().each((p,i) => {
      this$.w(p.name()).w(" ").w(p.type().signature());
      if (p.def() != null) {
        this$.w(":=").w(ApiDocWriter.encodeExpr(sys.ObjUtil.coerce(p.def(), Expr.type$)));
      }
      ;
      this$.w("\n");
      return;
    });
    this.w(") ").w(m.returns().signature()).w("\n");
    return;
  }

  writeAttr(name,val) {
    if (val == null) {
      return;
    }
    ;
    this.w(name).w("=").w(sys.ObjUtil.toStr(val)).w("\n");
    return;
  }

  writeFacets(facets) {
    const this$ = this;
    if (facets == null) {
      return;
    }
    ;
    facets.each((facet) => {
      this$.writeFacet(facet);
      return;
    });
    return;
  }

  writeFacet(facet) {
    const this$ = this;
    this.w("@").w(facet.type().signature());
    if (!facet.names().isEmpty()) {
      this.w("{\n");
      facet.names().each((name,i) => {
        this$.w(name).w("=").w(ApiDocWriter.encodeExpr(facet.vals().get(i)));
        this$.w("\n");
        return;
      });
      this.w("}");
    }
    ;
    this.w("\n");
    return;
  }

  writeDoc(node) {
    const this$ = this;
    this.w("\n");
    if (node.doc() == null) {
      return;
    }
    ;
    node.docDef().lines().each((line) => {
      if (sys.Str.startsWith(line, ApiDocWriter.slotPrefix())) {
        this$.w(sys.Str.getRange(ApiDocWriter.slotPrefix(), sys.Range.make(0, 0)));
      }
      ;
      this$.w(line).w("\n");
      return;
    });
    this.w("\n");
    return;
  }

  w(x) {
    this.#out.print(x);
    return this;
  }

  encodeBase(t) {
    if (t.isMixin()) {
      return null;
    }
    ;
    let s = sys.StrBuf.make();
    let b = t.base();
    while (b != null) {
      s.join(b.signature());
      (b = b.base());
    }
    ;
    return ((this$) => { if (s.isEmpty()) return null; return s.toStr(); })(this);
  }

  encodeMixins(t) {
    const this$ = this;
    if (t.mixins().isEmpty()) {
      return null;
    }
    ;
    let s = sys.StrBuf.make();
    t.mixins().each((m) => {
      if (!m.isNoDoc()) {
        s.join(m.signature());
      }
      ;
      return;
    });
    return ((this$) => { if (s.isEmpty()) return null; return s.toStr(); })(this);
  }

  encodeLoc(n,includeFile) {
    let s = sys.StrBuf.make();
    if (includeFile) {
      s.add(n.loc().filename());
    }
    ;
    if (n.loc().line() != null) {
      s.add(":").add(sys.ObjUtil.coerce(n.loc().line(), sys.Obj.type$.toNullable()));
      if ((n.docDef() != null && n.docDef().loc().line() != null)) {
        s.add("/").add(sys.ObjUtil.coerce(n.docDef().loc().line(), sys.Obj.type$.toNullable()));
      }
      ;
    }
    ;
    return ((this$) => { if (s.isEmpty()) return null; return s.toStr(); })(this);
  }

  static encodeExpr(expr) {
    return sys.ObjUtil.coerce(((this$) => { let $_u211 = expr.toDocStr(); if ($_u211 != null) return $_u211; return "..."; })(this), sys.Str.type$);
  }

  static encodeFlags(flags) {
    let s = sys.StrBuf.make();
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Abstract()), 0)) {
      s.join("abstract");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Const()), 0)) {
      s.join("const");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Enum()), 0)) {
      s.join("enum");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Facet()), 0)) {
      s.join("facet");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Final()), 0)) {
      s.join("final");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Internal()), 0)) {
      s.join("internal");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Mixin()), 0)) {
      s.join("mixin");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Native()), 0)) {
      s.join("native");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Override()), 0)) {
      s.join("override");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Private()), 0)) {
      s.join("private");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Protected()), 0)) {
      s.join("protected");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Public()), 0)) {
      s.join("public");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Static()), 0)) {
      s.join("static");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Synthetic()), 0)) {
      s.join("synthetic");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Virtual()), 0)) {
      s.join("virtual");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Ctor()), 0)) {
      s.join("new");
    }
    ;
    if (sys.ObjUtil.compareNE(sys.Int.and(flags, FConst.Once()), 0)) {
      s.join("once");
    }
    ;
    return s.toStr();
  }

  static static$init() {
    ApiDocWriter.#typePrefix = "== ";
    ApiDocWriter.#slotPrefix = "-- ";
    ApiDocWriter.#protectionMask = sys.Int.or(sys.Int.or(sys.Int.or(FConst.Public(), FConst.Protected()), FConst.Private()), FConst.Internal());
    return;
  }

}

class Coercer extends CompilerSupport {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return Coercer.type$; }

  static make(c) {
    const $self = new Coercer();
    Coercer.make$($self,c);
    return $self;
  }

  static make$($self,c) {
    CompilerSupport.make$($self, c);
    return;
  }

  box(expr) {
    if (expr.ctype().isVal()) {
      return TypeCheckExpr.coerce(expr, expr.ctype().toNullable());
    }
    else {
      return expr;
    }
    ;
  }

  coerceBoxed(expr,expected,onErr) {
    return this.box(this.coerce(expr, expected, onErr));
  }

  canCoerce(expr,expected) {
    const this$ = this;
    let ok = true;
    this.coerce(expr, expected, () => {
      (ok = false);
      return;
    });
    return ok;
  }

  coerce(expr,expected,onErr) {
    if (expected.isForeign()) {
      return expected.bridge().coerce(expr, expected, onErr);
    }
    ;
    if (expr.ctype().isForeign()) {
      return expr.ctype().bridge().coerce(expr, expected, onErr);
    }
    ;
    return this.doCoerce(expr, expected, onErr);
  }

  doCoerce(expr,expected,onErr) {
    let actual = sys.ObjUtil.coerce(expr.ctype(), CType.type$);
    if (sys.ObjUtil.coerce(actual, sys.Obj.type$.toNullable()) == null) {
      throw sys.NullErr.make(sys.Str.plus("null ctype: ", expr));
    }
    ;
    if (sys.ObjUtil.equals(actual, expected)) {
      let call = expr.asCall();
      if ((call != null && call.method().isCovariant())) {
        return this.coerceInheritedReturns(sys.ObjUtil.coerce(call, CallExpr.type$), expected);
      }
      ;
      return expr;
    }
    ;
    if (actual.isNothing()) {
      return expr;
    }
    ;
    if ((actual.isVoid() || expected.isVoid())) {
      sys.Func.call(onErr);
      return expr;
    }
    ;
    if (expr.isAlwaysNullable()) {
      if (!expected.isNullable()) {
        sys.Func.call(onErr);
        return expr;
      }
      ;
      if (expr.id() === ExprId.nullLiteral()) {
        return expr;
      }
      ;
    }
    ;
    if (actual.fits(expected)) {
      if (this.forceCoerce(actual, expected)) {
        return TypeCheckExpr.coerce(expr, expected);
      }
      else {
        return expr;
      }
      ;
    }
    ;
    if ((actual.isFunc() && expected.isFunc())) {
      if (this.isFuncAutoCoerce(actual, expected)) {
        return TypeCheckExpr.coerce(expr, expected);
      }
      ;
    }
    else {
      if (expected.fits(actual)) {
        return TypeCheckExpr.coerce(expr, expected);
      }
      ;
    }
    ;
    sys.Func.call(onErr);
    return expr;
  }

  coerceInheritedReturns(call,expected) {
    if (this.forceParameterizedCollectionCoerce(call.method().inheritedReturns(), expected)) {
      return TypeCheckExpr.coerce(call, expected);
    }
    else {
      return call;
    }
    ;
  }

  isFuncAutoCoerce(actualType,expectedType) {
    const this$ = this;
    if ((!actualType.isFunc() || !expectedType.isFunc())) {
      return false;
    }
    ;
    let actual = sys.ObjUtil.as(actualType.toNonNullable(), FuncType.type$);
    let expected = sys.ObjUtil.as(expectedType.toNonNullable(), FuncType.type$);
    if ((actual == null || expected == null)) {
      return true;
    }
    ;
    if (sys.ObjUtil.compareGT(actual.arity(), expected.arity())) {
      return false;
    }
    ;
    if (!this.isFuncAutoCoerceMatch(actual.returns(), expected.returns())) {
      return false;
    }
    ;
    return actual.params().all((actualParam,i) => {
      let expectedParam = expected.params().get(i);
      return this$.isFuncAutoCoerceMatch(actualParam, expectedParam);
    });
    return true;
  }

  isFuncAutoCoerceMatch(actual,expected) {
    if (actual.fits(expected)) {
      return true;
    }
    ;
    if (expected.fits(actual)) {
      return true;
    }
    ;
    if (this.isFuncAutoCoerce(actual, expected)) {
      return true;
    }
    ;
    return false;
  }

  forceCoerce(from$,to) {
    if ((from$.isVal() || to.isVal())) {
      return true;
    }
    ;
    if (this.forceParameterizedCollectionCoerce(from$, to)) {
      return true;
    }
    ;
    if (!to.isNullable()) {
      return from$.isNullable();
    }
    ;
    return false;
  }

  forceParameterizedCollectionCoerce(from$,to) {
    return (this.needParameterizedCollectionCoerce(from$) || this.needParameterizedCollectionCoerce(to));
  }

}

class CallResolver extends CompilerSupport {
  constructor() {
    super();
    const this$ = this;
  }

  typeof() { return CallResolver.type$; }

  #curType = null;

  curType(it) {
    if (it === undefined) {
      return this.#curType;
    }
    else {
      this.#curType = it;
      return;
    }
  }

  #curMethod = null;

  curMethod(it) {
    if (it === undefined) {
      return this.#curMethod;
    }
    else {
      this.#curMethod = it;
      return;
    }
  }

  #expr = null;

  expr(it) {
    if (it === undefined) {
      return this.#expr;
    }
    else {
      this.#expr = it;
      return;
    }
  }

  #loc = null;

  loc(it) {
    if (it === undefined) {
      return this.#loc;
    }
    else {
      this.#loc = it;
      return;
    }
  }

  #target = null;

  target(it) {
    if (it === undefined) {
      return this.#target;
    }
    else {
      this.#target = it;
      return;
    }
  }

  #name = null;

  name(it) {
    if (it === undefined) {
      return this.#name;
    }
    else {
      this.#name = it;
      return;
    }
  }

  #isItAdd = false;

  isItAdd(it) {
    if (it === undefined) {
      return this.#isItAdd;
    }
    else {
      this.#isItAdd = it;
      return;
    }
  }

  #isVar = false;

  isVar(it) {
    if (it === undefined) {
      return this.#isVar;
    }
    else {
      this.#isVar = it;
      return;
    }
  }

  #isFuncFieldCall = false;

  isFuncFieldCall(it) {
    if (it === undefined) {
      return this.#isFuncFieldCall;
    }
    else {
      this.#isFuncFieldCall = it;
      return;
    }
  }

  #args = null;

  args(it) {
    if (it === undefined) {
      return this.#args;
    }
    else {
      this.#args = it;
      return;
    }
  }

  #base = null;

  base(it) {
    if (it === undefined) {
      return this.#base;
    }
    else {
      this.#base = it;
      return;
    }
  }

  #baseIt = null;

  baseIt(it) {
    if (it === undefined) {
      return this.#baseIt;
    }
    else {
      this.#baseIt = it;
      return;
    }
  }

  #found = null;

  found(it) {
    if (it === undefined) {
      return this.#found;
    }
    else {
      this.#found = it;
      return;
    }
  }

  #foundOnIt = false;

  foundOnIt(it) {
    if (it === undefined) {
      return this.#foundOnIt;
    }
    else {
      this.#foundOnIt = it;
      return;
    }
  }

  #result = null;

  result(it) {
    if (it === undefined) {
      return this.#result;
    }
    else {
      this.#result = it;
      return;
    }
  }

  static make(compiler,curType,curMethod,expr) {
    const $self = new CallResolver();
    CallResolver.make$($self,compiler,curType,curMethod,expr);
    return $self;
  }

  static make$($self,compiler,curType,curMethod,expr) {
    CompilerSupport.make$($self, compiler);
    $self.#curType = curType;
    $self.#curMethod = curMethod;
    $self.#expr = expr;
    $self.#loc = expr.loc();
    $self.#target = expr.target();
    $self.#name = sys.ObjUtil.coerce(expr.name(), sys.Str.type$);
    let call = sys.ObjUtil.as(expr, CallExpr.type$);
    if (call != null) {
      $self.#isVar = false;
      $self.#isItAdd = call.isItAdd();
      $self.#args = call.args();
      $self.#found = call.method();
    }
    else {
      $self.#isVar = true;
      $self.#args = sys.List.make(Expr.type$);
    }
    ;
    return;
  }

  resolve() {
    try {
      if (this.isStaticLiteral()) {
        return sys.ObjUtil.coerce(this.#result, Expr.type$);
      }
      ;
      this.resolveBase();
      this.find();
      if (this.#result != null) {
        return sys.ObjUtil.coerce(this.#result, Expr.type$);
      }
      ;
      this.insertImplicitThisOrIt();
      this.resolveToExpr();
      this.inferClosureType();
      this.resolveForeign();
      this.constantFolding();
      this.castForThisType();
      this.safeToNullable();
      this.ffiCoercion();
      return sys.ObjUtil.coerce(this.#result, Expr.type$);
    }
    catch ($_u212) {
      $_u212 = sys.Err.make($_u212);
      if ($_u212 instanceof CompilerErr) {
        let err = $_u212;
        ;
        this.#expr.ctype(this.ns().error());
        return this.#expr;
      }
      else {
        throw $_u212;
      }
    }
    ;
  }

  isStaticLiteral() {
    const this$ = this;
    if ((this.#target == null && this.#isVar)) {
      let stypes = this.#curType.unit().importedTypes().get(this.#name);
      if ((stypes != null && sys.ObjUtil.compareGT(stypes.size(), 1))) {
        stypes.exclude((t) => {
          return (t.isInternal() && sys.ObjUtil.compareNE(t.pod().name(), this$.compiler().pod().name()));
        });
      }
      ;
      if ((stypes != null && !stypes.isEmpty())) {
        if (sys.ObjUtil.compareGT(stypes.size(), 1)) {
          throw this.err(sys.Str.plus("Ambiguous type: ", stypes.join(", ")), this.#loc);
        }
        else {
          this.#result = StaticTargetExpr.make(this.#loc, sys.ObjUtil.coerce(stypes.first(), CType.type$));
        }
        ;
        return true;
      }
      ;
    }
    ;
    return false;
  }

  resolveBase() {
    if (this.#target == null) {
      if (this.#curType.isClosure()) {
        this.#base = this.#curType.closure().enclosingType();
        if (this.#curType.closure().isItBlock()) {
          this.#baseIt = this.#curType.closure().itType();
        }
        ;
      }
      else {
        this.#base = this.#curType;
      }
      ;
    }
    else {
      this.#base = this.#target.ctype();
    }
    ;
    if (this.#base === this.ns().error()) {
      throw CompilerErr.make("ignore", this.#loc, null);
    }
    ;
    if (this.#base == null) {
      throw this.err("Internal error", this.#loc);
    }
    ;
    return;
  }

  find() {
    if (this.#found != null) {
      return;
    }
    ;
    this.#found = this.findOn(sys.ObjUtil.coerce(this.#base, CType.type$));
    if (this.#baseIt != null) {
      let foundIt = this.findOn(sys.ObjUtil.coerce(this.#baseIt, CType.type$));
      if (this.isAmbiguous(this.#found, foundIt)) {
        if (!CheckErrors.isSlotVisible(sys.ObjUtil.coerce(this.#curType, TypeDef.type$), sys.ObjUtil.coerce(foundIt, CSlot.type$))) {
          (foundIt = null);
        }
        else {
          throw this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Ambiguous slot '", this.#name), "' on both 'this' ("), this.#base), ") and 'it' ("), this.#baseIt), ")"), this.#loc);
        }
        ;
      }
      ;
      if (foundIt != null) {
        this.#found = foundIt;
        this.#foundOnIt = true;
      }
      ;
    }
    ;
    if (this.#found == null) {
      if (this.#isVar) {
        if (this.#target == null) {
          throw this.err(sys.Str.plus(sys.Str.plus("Unknown variable '", this.#name), "'"), this.#loc);
        }
        else {
          throw this.err(sys.Str.plus(sys.Str.plus("Unknown slot '", this.errSig()), "'"), this.#loc);
        }
        ;
      }
      else {
        let ct = sys.ObjUtil.as(this.#target, CallExpr.type$);
        if ((ct != null && ct.isItAdd())) {
          throw this.err(sys.Str.plus(sys.Str.plus("'", ct.method().qname()), "' must return This"), this.#loc);
        }
        else {
          if (this.#isItAdd) {
            throw this.err(sys.Str.plus(sys.Str.plus("No comma operator method found: '", this.errSig()), "'"), this.#loc);
          }
          else {
            throw this.err(sys.Str.plus(sys.Str.plus("Unknown method '", this.errSig()), "'"), this.#loc);
          }
          ;
        }
        ;
      }
      ;
    }
    ;
    return;
  }

  findOn(base) {
    if (base === this.ns().error()) {
      return null;
    }
    ;
    let found = null;
    try {
      (found = base.slot(this.#name));
    }
    catch ($_u213) {
      $_u213 = sys.Err.make($_u213);
      if ($_u213 instanceof sys.Err) {
        let e = $_u213;
        ;
        this.err(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus(sys.Str.plus("Cannot resolve ", this.#name), " on "), base), "; "), e.msg()), this.#loc);
      }
      else {
        throw $_u213;
      }
    }
    ;
    if (found == null) {
      return null;
    }
    ;
    if (found.isForeign()) {
      (found = found.parent().bridge().resolveSlotAccess(base, this.#name, this.#isVar));
    }
    ;
    if ((sys.ObjUtil.is(found, CField.type$) && !this.#isVar)) {
      let field = sys.ObjUtil.coerce(found, CField.type$);
      if (field.type().isFunc()) {
        this.#isFuncFieldCall = true;
      }
      else {
        throw this.err(sys.Str.plus(sys.Str.plus("Expected method, not field '", this.errSig()), "'"), this.#loc);
      }
      ;
    }
    ;
    return found;
  }

  isAmbiguous(onBase,onIt) {
    if ((onBase == null || onIt == null)) {
      return false;
    }
    ;
    if ((sys.ObjUtil.equals(onBase.qname(), onIt.qname()) && onBase.isStatic())) {
      return false;
    }
    ;
    if ((!onBase.isStatic() && !onIt.isStatic() && this.#curType.closure().enclosingSlot().isStatic())) {
      return false;
    }
    ;
    return true;
  }

  errSig() {
    return sys.Str.plus(sys.Str.plus(sys.Str.plus("", this.#base.qname()), "."), this.#name);
  }

  insertImplicitThisOrIt() {
    if (this.#target != null) {
      return;
    }
    ;
    if ((this.#found.isStatic() || this.#found.isCtor())) {
      return;
    }
    ;
    if (this.#curMethod.isStatic()) {
      return;
    }
    ;
    if (this.#foundOnIt) {
      this.#target = ItExpr.make(this.#loc, this.#baseIt);
    }
    else {
      if (this.#curType.isClosure()) {
        let closure = this.#curType.closure();
        if (!closure.enclosingSlot().isStatic()) {
          this.#target = FieldExpr.make(this.#loc, ThisExpr.make(this.#loc, closure.enclosingType()), closure.outerThisField());
        }
        ;
      }
      else {
        this.#target = ThisExpr.make(this.#loc, this.#curType);
      }
      ;
    }
    ;
    return;
  }

  resolveToExpr() {
    if (sys.ObjUtil.is(this.#found, CField.type$)) {
      this.#result = this.resolveToFieldExpr();
      if (this.#isFuncFieldCall) {
        let callMethod = sys.ObjUtil.coerce(this.#found, CField.type$).type().method("call");
        this.#result = CallExpr.makeWithMethod(this.#loc, this.#result, sys.ObjUtil.coerce(callMethod, CMethod.type$), this.#args);
      }
      ;
    }
    else {
      this.#result = this.resolveToCallExpr();
    }
    ;
    return;
  }

  resolveToCallExpr() {
    let method = sys.ObjUtil.coerce(this.#found, CMethod.type$);
    let call = sys.ObjUtil.as(this.#expr, CallExpr.type$);
    if (call == null) {
      (call = CallExpr.make(this.#loc));
      call.name(this.#name);
      call.args(this.#args);
    }
    ;
    call.target(this.#target);
    call.isSafe(this.#expr.isSafe());
    call.noParens(this.#isVar);
    call.method(method);
    if (method.isInstanceCtor()) {
      call.ctype(method.parent());
    }
    else {
      call.ctype(method.returns());
    }
    ;
    return sys.ObjUtil.coerce(call, CallExpr.type$);
  }

  resolveToFieldExpr() {
    let f = sys.ObjUtil.coerce(this.#found, CField.type$);
    let field = FieldExpr.make(this.#loc);
    field.target(this.#target);
    field.name(this.#name);
    field.field(f);
    field.ctype(f.type());
    field.isSafe(this.#expr.isSafe());
    return field;
  }

  inferClosureType() {
    if (sys.ObjUtil.is(this.#result, CallExpr.type$)) {
      let base = ((this$) => { if (this$.#foundOnIt) return this$.#baseIt; return this$.#base; })(this);
      this.#result = CallResolver.inferClosureTypeFromCall(this, sys.ObjUtil.coerce(this.#result, CallExpr.type$), sys.ObjUtil.coerce(base, CType.type$));
    }
    ;
    return;
  }

  static inferClosureTypeFromCall(support,call,base) {
    let c = sys.ObjUtil.as(call.args().last(), ClosureExpr.type$);
    if (c == null) {
      return call;
    }
    ;
    let m = call.method();
    let lastParam = sys.ObjUtil.as(((this$) => { let $_u215 = ((this$) => { let $_u216 = ((this$) => { let $_u217 = m.params().last(); if ($_u217 == null) return null; return m.params().last().type(); })(this$); if ($_u216 == null) return null; return ((this$) => { let $_u218 = m.params().last(); if ($_u218 == null) return null; return m.params().last().type(); })(this$).deref(); })(this$); if ($_u215 == null) return null; return ((this$) => { let $_u219 = ((this$) => { let $_u220 = m.params().last(); if ($_u220 == null) return null; return m.params().last().type(); })(this$); if ($_u219 == null) return null; return ((this$) => { let $_u221 = m.params().last(); if ($_u221 == null) return null; return m.params().last().type(); })(this$).deref(); })(this$).toNonNullable(); })(this), FuncType.type$);
    if ((lastParam != null && sys.ObjUtil.equals(call.args().size(), m.params().size()) && sys.ObjUtil.compareLE(c.signature().params().size(), lastParam.params().size()))) {
      if (sys.ObjUtil.equals(call.method().name(), "with")) {
        (lastParam = FuncType.makeItBlock(base));
      }
      else {
        (lastParam = lastParam.parameterizeThis(base));
      }
      ;
      c.setInferredSignature(sys.ObjUtil.coerce(lastParam, FuncType.type$));
      return call;
    }
    ;
    if (c.isItBlock()) {
      if (call.ctype().isThis()) {
        call.ctype(base);
      }
      ;
      if (call.ctype().isVoid()) {
        support.err("Cannot apply it-block to Void expr", call.loc());
        return call;
      }
      ;
      call.args().removeAt(-1);
      return c.toWith(call);
    }
    ;
    return call;
  }

  resolveForeign() {
    let bridge = this.#found.usesBridge();
    if ((bridge != null && sys.ObjUtil.is(this.#result, CallExpr.type$))) {
      this.#result = bridge.resolveCall(sys.ObjUtil.coerce(this.#result, CallExpr.type$));
    }
    ;
    return;
  }

  constantFolding() {
    let call = sys.ObjUtil.as(this.#result, CallExpr.type$);
    if (call == null) {
      return;
    }
    ;
    if ((this.#curType != null && sys.ObjUtil.equals(this.compiler().pod().name(), "testSys"))) {
      return;
    }
    ;
    this.#result = ConstantFolder.make(this.compiler()).fold(sys.ObjUtil.coerce(call, CallExpr.type$));
    return;
  }

  castForThisType() {
    const this$ = this;
    if (!this.#result.ctype().isThis()) {
      return;
    }
    ;
    let method = sys.ObjUtil.as(this.#found, CMethod.type$);
    if (method == null) {
      return;
    }
    ;
    let base = ((this$) => { if (this$.#foundOnIt) return this$.#baseIt; return this$.#base; })(this);
    this.#result.ctype(base);
    if (sys.ObjUtil.compareNE(method.inheritedReturns(), base)) {
      this.#result = sys.ObjUtil.coerce(sys.ObjUtil.with(TypeCheckExpr.coerce(sys.ObjUtil.coerce(this.#result, Expr.type$), sys.ObjUtil.coerce(base, CType.type$)), (it) => {
        it.from(method.inheritedReturns());
        return;
      }), TypeCheckExpr.type$);
    }
    ;
    return;
  }

  safeToNullable() {
    if (this.#expr.isSafe()) {
      this.#result.ctype(this.#result.ctype().toNullable());
    }
    ;
    return;
  }

  ffiCoercion() {
    const this$ = this;
    if (this.#result.ctype().isForeign()) {
      let foreign = this.#result.ctype();
      let inferred = foreign.inferredAs();
      if (foreign !== inferred) {
        this.#result = foreign.bridge().coerce(sys.ObjUtil.coerce(this.#result, Expr.type$), inferred, () => {
          throw this$.err("Cannot coerce call return to Fantom type", this$.#loc);
        });
      }
      ;
    }
    ;
    return;
  }

}

const p = sys.Pod.add$('compiler');
const xp = sys.Param.noParams$();
let m;
CompilerErr.type$ = p.at$('CompilerErr','sys::Err',[],{},8194,CompilerErr);
CompilerLog.type$ = p.at$('CompilerLog','sys::Obj',[],{},8192,CompilerLog);
Loc.type$ = p.at$('Loc','sys::Obj',[],{},8192,Loc);
Compiler.type$ = p.at$('Compiler','sys::Obj',[],{},8192,Compiler);
CompilerSupport.type$ = p.at$('CompilerSupport','sys::Obj',[],{},8192,CompilerSupport);
Visitor.type$ = p.am$('Visitor','sys::Obj',[],{},8449,Visitor);
CompilerStep.type$ = p.at$('CompilerStep','compiler::CompilerSupport',['compiler::Visitor'],{},8193,CompilerStep);
CompilerInput.type$ = p.at$('CompilerInput','sys::Obj',[],{},8192,CompilerInput);
CompilerInputMode.type$ = p.at$('CompilerInputMode','sys::Enum',[],{'sys::Serializable':"sys::Serializable{simple=true;}"},8234,CompilerInputMode);
SuppressedErr.type$ = p.at$('SuppressedErr','sys::Err',[],{},130,SuppressedErr);
CompilerOutput.type$ = p.at$('CompilerOutput','sys::Obj',[],{},8192,CompilerOutput);
CompilerOutputMode.type$ = p.at$('CompilerOutputMode','sys::Enum',[],{'sys::Serializable':"sys::Serializable{simple=true;}"},8234,CompilerOutputMode);
Main.type$ = p.at$('Main','sys::Obj',[],{},8192,Main);
AttrAsm.type$ = p.at$('AttrAsm','compiler::CompilerSupport',[],{},8192,AttrAsm);
FConst.type$ = p.am$('FConst','sys::Obj',[],{},8449,FConst);
Assembler.type$ = p.at$('Assembler','compiler::CompilerSupport',['compiler::FConst'],{},8192,Assembler);
CodeAsm.type$ = p.at$('CodeAsm','compiler::CompilerSupport',[],{},8192,CodeAsm);
Loop.type$ = p.at$('Loop','sys::Obj',[],{},8192,Loop);
ProtectedRegion.type$ = p.at$('ProtectedRegion','sys::Obj',[],{},8192,ProtectedRegion);
Cond.type$ = p.at$('Cond','sys::Obj',[],{},8192,Cond);
Node.type$ = p.at$('Node','sys::Obj',[],{},8193,Node);
CNode.type$ = p.am$('CNode','sys::Obj',[],{},8449,CNode);
DefNode.type$ = p.at$('DefNode','compiler::Node',['compiler::CNode'],{},8193,DefNode);
CDoc.type$ = p.am$('CDoc','sys::Obj',[],{},8449,CDoc);
DocDef.type$ = p.at$('DocDef','compiler::Node',['compiler::CDoc'],{},8192,DocDef);
Block.type$ = p.at$('Block','compiler::Node',[],{},8192,Block);
Expr.type$ = p.at$('Expr','compiler::Node',[],{},8193,Expr);
LiteralExpr.type$ = p.at$('LiteralExpr','compiler::Expr',[],{},8192,LiteralExpr);
LocaleLiteralExpr.type$ = p.at$('LocaleLiteralExpr','compiler::Expr',[],{},8192,LocaleLiteralExpr);
SlotLiteralExpr.type$ = p.at$('SlotLiteralExpr','compiler::Expr',[],{},8192,SlotLiteralExpr);
RangeLiteralExpr.type$ = p.at$('RangeLiteralExpr','compiler::Expr',[],{},8192,RangeLiteralExpr);
ListLiteralExpr.type$ = p.at$('ListLiteralExpr','compiler::Expr',[],{},8192,ListLiteralExpr);
MapLiteralExpr.type$ = p.at$('MapLiteralExpr','compiler::Expr',[],{},8192,MapLiteralExpr);
UnaryExpr.type$ = p.at$('UnaryExpr','compiler::Expr',[],{},8192,UnaryExpr);
BinaryExpr.type$ = p.at$('BinaryExpr','compiler::Expr',[],{},8192,BinaryExpr);
CondExpr.type$ = p.at$('CondExpr','compiler::Expr',[],{},8192,CondExpr);
NameExpr.type$ = p.at$('NameExpr','compiler::Expr',[],{},8193,NameExpr);
UnknownVarExpr.type$ = p.at$('UnknownVarExpr','compiler::NameExpr',[],{},8192,UnknownVarExpr);
CallExpr.type$ = p.at$('CallExpr','compiler::NameExpr',[],{},8192,CallExpr);
ShortcutExpr.type$ = p.at$('ShortcutExpr','compiler::CallExpr',[],{},8192,ShortcutExpr);
IndexedAssignExpr.type$ = p.at$('IndexedAssignExpr','compiler::ShortcutExpr',[],{},8192,IndexedAssignExpr);
FieldExpr.type$ = p.at$('FieldExpr','compiler::NameExpr',[],{},8192,FieldExpr);
LocalVarExpr.type$ = p.at$('LocalVarExpr','compiler::Expr',[],{},8192,LocalVarExpr);
ThisExpr.type$ = p.at$('ThisExpr','compiler::LocalVarExpr',[],{},8192,ThisExpr);
SuperExpr.type$ = p.at$('SuperExpr','compiler::LocalVarExpr',[],{},8192,SuperExpr);
ItExpr.type$ = p.at$('ItExpr','compiler::LocalVarExpr',[],{},8192,ItExpr);
StaticTargetExpr.type$ = p.at$('StaticTargetExpr','compiler::Expr',[],{},8192,StaticTargetExpr);
TypeCheckExpr.type$ = p.at$('TypeCheckExpr','compiler::Expr',[],{},8192,TypeCheckExpr);
TernaryExpr.type$ = p.at$('TernaryExpr','compiler::Expr',[],{},8192,TernaryExpr);
ComplexLiteral.type$ = p.at$('ComplexLiteral','compiler::Expr',[],{},8192,ComplexLiteral);
ClosureExpr.type$ = p.at$('ClosureExpr','compiler::Expr',[],{},8192,ClosureExpr);
DslExpr.type$ = p.at$('DslExpr','compiler::Expr',[],{},8192,DslExpr);
ThrowExpr.type$ = p.at$('ThrowExpr','compiler::Expr',[],{},8192,ThrowExpr);
ExprId.type$ = p.at$('ExprId','sys::Enum',[],{'sys::Serializable':"sys::Serializable{simple=true;}"},8234,ExprId);
ShortcutOp.type$ = p.at$('ShortcutOp','sys::Enum',[],{'sys::Serializable':"sys::Serializable{simple=true;}"},8234,ShortcutOp);
CompilationUnit.type$ = p.at$('CompilationUnit','compiler::Node',[],{},8192,CompilationUnit);
CFacet.type$ = p.am$('CFacet','sys::Obj',[],{},8449,CFacet);
FacetDef.type$ = p.at$('FacetDef','compiler::Node',['compiler::CFacet'],{},8192,FacetDef);
Stmt.type$ = p.at$('Stmt','compiler::Node',[],{},8193,Stmt);
NopStmt.type$ = p.at$('NopStmt','compiler::Stmt',[],{},8192,NopStmt);
ExprStmt.type$ = p.at$('ExprStmt','compiler::Stmt',[],{},8192,ExprStmt);
LocalDefStmt.type$ = p.at$('LocalDefStmt','compiler::Stmt',[],{},8192,LocalDefStmt);
IfStmt.type$ = p.at$('IfStmt','compiler::Stmt',[],{},8192,IfStmt);
ReturnStmt.type$ = p.at$('ReturnStmt','compiler::Stmt',[],{},8192,ReturnStmt);
ThrowStmt.type$ = p.at$('ThrowStmt','compiler::Stmt',[],{},8192,ThrowStmt);
ForStmt.type$ = p.at$('ForStmt','compiler::Stmt',[],{},8192,ForStmt);
WhileStmt.type$ = p.at$('WhileStmt','compiler::Stmt',[],{},8192,WhileStmt);
BreakStmt.type$ = p.at$('BreakStmt','compiler::Stmt',[],{},8192,BreakStmt);
ContinueStmt.type$ = p.at$('ContinueStmt','compiler::Stmt',[],{},8192,ContinueStmt);
TryStmt.type$ = p.at$('TryStmt','compiler::Stmt',[],{},8192,TryStmt);
Catch.type$ = p.at$('Catch','compiler::Node',[],{},8192,Catch);
SwitchStmt.type$ = p.at$('SwitchStmt','compiler::Stmt',[],{},8192,SwitchStmt);
Case.type$ = p.at$('Case','compiler::Node',[],{},8192,Case);
StmtId.type$ = p.at$('StmtId','sys::Enum',[],{'sys::Serializable':"sys::Serializable{simple=true;}"},8234,StmtId);
CType.type$ = p.am$('CType','sys::Obj',['compiler::CNode'],{},8449,CType);
TypeDef.type$ = p.at$('TypeDef','compiler::DefNode',['compiler::CType'],{},8192,TypeDef);
Using.type$ = p.at$('Using','compiler::Node',[],{},8192,Using);
CSlot.type$ = p.am$('CSlot','sys::Obj',['compiler::CNode'],{},8449,CSlot);
SlotDef.type$ = p.at$('SlotDef','compiler::DefNode',['compiler::CSlot'],{},8193,SlotDef);
VisitDepth.type$ = p.at$('VisitDepth','sys::Enum',[],{'sys::Serializable':"sys::Serializable{simple=true;}"},8234,VisitDepth);
ExprVisitor.type$ = p.at$('ExprVisitor','sys::Obj',['compiler::Visitor'],{},128,ExprVisitor);
CField.type$ = p.am$('CField','sys::Obj',['compiler::CSlot'],{},8449,CField);
FieldDef.type$ = p.at$('FieldDef','compiler::SlotDef',['compiler::CField'],{},8192,FieldDef);
CPod.type$ = p.am$('CPod','sys::Obj',[],{},8449,CPod);
PodDef.type$ = p.at$('PodDef','compiler::Node',['compiler::CPod'],{},8192,PodDef);
CParam.type$ = p.am$('CParam','sys::Obj',[],{},8449,CParam);
ParamDef.type$ = p.at$('ParamDef','compiler::Node',['compiler::CParam'],{},8192,ParamDef);
CMethod.type$ = p.am$('CMethod','sys::Obj',['compiler::CSlot'],{},8449,CMethod);
MethodDef.type$ = p.at$('MethodDef','compiler::SlotDef',['compiler::CMethod'],{},8192,MethodDef);
TypeRef.type$ = p.at$('TypeRef','compiler::Node',['compiler::CType'],{},8192,TypeRef);
AstWriter.type$ = p.at$('AstWriter','sys::Obj',[],{},8192,AstWriter);
MethodVar.type$ = p.at$('MethodVar','sys::Obj',[],{},8192,MethodVar);
EnumDef.type$ = p.at$('EnumDef','compiler::Node',[],{},8192,EnumDef);
DslPlugin.type$ = p.at$('DslPlugin','compiler::CompilerSupport',[],{},8193,DslPlugin);
RegexDslPlugin.type$ = p.at$('RegexDslPlugin','compiler::DslPlugin',[],{},8192,RegexDslPlugin);
StrDslPlugin.type$ = p.at$('StrDslPlugin','compiler::DslPlugin',[],{},8192,StrDslPlugin);
FUtil.type$ = p.at$('FUtil','sys::Obj',['compiler::FConst'],{},8192,FUtil);
FPod.type$ = p.at$('FPod','sys::Obj',['compiler::CPod','compiler::FConst'],{},8224,FPod);
FTypeRef.type$ = p.at$('FTypeRef','sys::Obj',[],{},8194,FTypeRef);
FFieldRef.type$ = p.at$('FFieldRef','sys::Obj',[],{},8194,FFieldRef);
FMethodRef.type$ = p.at$('FMethodRef','sys::Obj',[],{},8194,FMethodRef);
FSlot.type$ = p.at$('FSlot','sys::Obj',['compiler::CSlot','compiler::FConst'],{},8193,FSlot);
FField.type$ = p.at$('FField','compiler::FSlot',['compiler::CField'],{},8192,FField);
FPrinter.type$ = p.at$('FPrinter','sys::Obj',['compiler::FConst'],{},8192,FPrinter);
FMethod.type$ = p.at$('FMethod','compiler::FSlot',['compiler::CMethod'],{},8192,FMethod);
FMethodVar.type$ = p.at$('FMethodVar','sys::Obj',['compiler::FConst','compiler::CParam'],{},8192,FMethodVar);
FAttr.type$ = p.at$('FAttr','sys::Obj',['compiler::FConst'],{},8192,FAttr);
FFacet.type$ = p.at$('FFacet','sys::Obj',['compiler::CFacet'],{},8192,FFacet);
FCodePrinter.type$ = p.at$('FCodePrinter','sys::Obj',['compiler::FConst'],{},8192,FCodePrinter);
FOp.type$ = p.at$('FOp','sys::Enum',[],{'sys::Serializable':"sys::Serializable{simple=true;}"},8234,FOp);
FOpArg.type$ = p.at$('FOpArg','sys::Enum',[],{'sys::Serializable':"sys::Serializable{simple=true;}"},8234,FOpArg);
FTable.type$ = p.at$('FTable','sys::Obj',[],{},8192,FTable);
CNamespace.type$ = p.at$('CNamespace','sys::Obj',[],{},8193,CNamespace);
FPodNamespace.type$ = p.at$('FPodNamespace','compiler::CNamespace',[],{},8192,FPodNamespace);
Fanp.type$ = p.at$('Fanp','sys::Obj',[],{},8192,Fanp);
FType.type$ = p.at$('FType','sys::Obj',['compiler::CType'],{},8192,FType);
COperators.type$ = p.at$('COperators','sys::Obj',[],{},8192,COperators);
ReflectSlot.type$ = p.at$('ReflectSlot','sys::Obj',['compiler::CSlot'],{},8193,ReflectSlot);
ReflectField.type$ = p.at$('ReflectField','compiler::ReflectSlot',['compiler::CField'],{},8192,ReflectField);
ReflectMethod.type$ = p.at$('ReflectMethod','compiler::ReflectSlot',['compiler::CMethod'],{},8192,ReflectMethod);
ReflectParam.type$ = p.at$('ReflectParam','sys::Obj',['compiler::CParam'],{},8192,ReflectParam);
ReflectFacet.type$ = p.at$('ReflectFacet','sys::Obj',['compiler::CFacet'],{},8192,ReflectFacet);
ReflectPod.type$ = p.at$('ReflectPod','sys::Obj',['compiler::CPod'],{},8192,ReflectPod);
ReflectType.type$ = p.at$('ReflectType','sys::Obj',['compiler::CType'],{},8192,ReflectType);
MarkerFacet.type$ = p.at$('MarkerFacet','sys::Obj',['compiler::CFacet'],{},8194,MarkerFacet);
ReflectNamespace.type$ = p.at$('ReflectNamespace','compiler::CNamespace',[],{},8192,ReflectNamespace);
CBridge.type$ = p.at$('CBridge','compiler::CompilerSupport',[],{},8193,CBridge);
NullableType.type$ = p.at$('NullableType','sys::Obj',['compiler::CType'],{},8192,NullableType);
MDoc.type$ = p.at$('MDoc','sys::Obj',['compiler::CDoc'],{},130,MDoc);
GenericType.type$ = p.at$('GenericType','sys::Obj',['compiler::CType'],{},8193,GenericType);
ListType.type$ = p.at$('ListType','compiler::GenericType',[],{},8192,ListType);
MapType.type$ = p.at$('MapType','compiler::GenericType',[],{},8192,MapType);
FuncType.type$ = p.at$('FuncType','compiler::GenericType',[],{},8192,FuncType);
GenericParameterType.type$ = p.at$('GenericParameterType','sys::Obj',['compiler::CType'],{},8192,GenericParameterType);
ParameterizedField.type$ = p.at$('ParameterizedField','sys::Obj',['compiler::CField'],{},8192,ParameterizedField);
ParameterizedMethod.type$ = p.at$('ParameterizedMethod','sys::Obj',['compiler::CMethod'],{},8192,ParameterizedMethod);
ParameterizedMethodParam.type$ = p.at$('ParameterizedMethodParam','sys::Obj',['compiler::CParam'],{},8192,ParameterizedMethodParam);
MockSlot.type$ = p.at$('MockSlot','sys::Obj',['compiler::CSlot'],{},8193,MockSlot);
MockField.type$ = p.at$('MockField','compiler::MockSlot',['compiler::CField'],{},8192,MockField);
MockMethod.type$ = p.at$('MockMethod','compiler::MockSlot',['compiler::CMethod'],{},8192,MockMethod);
MockParam.type$ = p.at$('MockParam','sys::Obj',['compiler::CParam'],{},8192,MockParam);
TypeParser.type$ = p.at$('TypeParser','sys::Obj',[],{},8192,TypeParser);
CDepend.type$ = p.at$('CDepend','sys::Obj',[],{},8192,CDepend);
Tokenizer.type$ = p.at$('Tokenizer','compiler::CompilerSupport',[],{},8192,Tokenizer);
Quoted.type$ = p.at$('Quoted','sys::Enum',[],{'sys::Serializable':"sys::Serializable{simple=true;}"},170,Quoted);
TokenVal.type$ = p.at$('TokenVal','compiler::Loc',[],{},8192,TokenVal);
TokenValDsl.type$ = p.at$('TokenValDsl','compiler::TokenVal',[],{},8192,TokenValDsl);
Parser.type$ = p.at$('Parser','compiler::CompilerSupport',[],{},8192,Parser);
Token.type$ = p.at$('Token','sys::Enum',[],{'sys::Serializable':"sys::Serializable{simple=true;}"},8234,Token);
CompileJs.type$ = p.at$('CompileJs','compiler::CompilerStep',[],{},8192,CompileJs);
InitInput.type$ = p.at$('InitInput','compiler::CompilerStep',[],{},8192,InitInput);
DefaultCtor.type$ = p.at$('DefaultCtor','compiler::CompilerStep',[],{},8192,DefaultCtor);
ResolveImports.type$ = p.at$('ResolveImports','compiler::CompilerStep',[],{},8192,ResolveImports);
Normalize.type$ = p.at$('Normalize','compiler::CompilerStep',[],{},8192,Normalize);
InitEnum.type$ = p.at$('InitEnum','compiler::CompilerStep',[],{},8192,InitEnum);
CheckErrors.type$ = p.at$('CheckErrors','compiler::CompilerStep',[],{},8192,CheckErrors);
ResolveDepends.type$ = p.at$('ResolveDepends','compiler::CompilerStep',[],{},8192,ResolveDepends);
ResolveExpr.type$ = p.at$('ResolveExpr','compiler::CompilerStep',[],{},8192,ResolveExpr);
ClosureVars.type$ = p.at$('ClosureVars','compiler::CompilerStep',[],{},8192,ClosureVars);
Parse.type$ = p.at$('Parse','compiler::CompilerStep',[],{},8192,Parse);
CheckInheritance.type$ = p.at$('CheckInheritance','compiler::CompilerStep',[],{},8192,CheckInheritance);
Tokenize.type$ = p.at$('Tokenize','compiler::CompilerStep',[],{},8192,Tokenize);
LoadPod.type$ = p.at$('LoadPod','compiler::CompilerStep',[],{},8192,LoadPod);
Inherit.type$ = p.at$('Inherit','compiler::CompilerStep',[],{},8192,Inherit);
InitFacet.type$ = p.at$('InitFacet','compiler::CompilerStep',[],{},8192,InitFacet);
LocaleProps.type$ = p.at$('LocaleProps','compiler::CompilerStep',[],{},8192,LocaleProps);
GenerateOutput.type$ = p.at$('GenerateOutput','compiler::CompilerStep',[],{},8192,GenerateOutput);
CheckParamDefs.type$ = p.at$('CheckParamDefs','compiler::CompilerStep',[],{},8192,CheckParamDefs);
Assemble.type$ = p.at$('Assemble','compiler::CompilerStep',[],{},8192,Assemble);
InitClosures.type$ = p.at$('InitClosures','compiler::CompilerStep',[],{},8192,InitClosures);
ScanForUsingsAndTypes.type$ = p.at$('ScanForUsingsAndTypes','compiler::CompilerStep',[],{},8192,ScanForUsingsAndTypes);
UsingAndTypeScanner.type$ = p.at$('UsingAndTypeScanner','compiler::CompilerSupport',[],{},8192,UsingAndTypeScanner);
ClosureToImmutable.type$ = p.at$('ClosureToImmutable','compiler::CompilerStep',[],{},8192,ClosureToImmutable);
ConstChecks.type$ = p.at$('ConstChecks','compiler::CompilerStep',[],{},8192,ConstChecks);
WritePod.type$ = p.at$('WritePod','compiler::CompilerStep',[],{},8192,WritePod);
OrderByInheritance.type$ = p.at$('OrderByInheritance','compiler::CompilerStep',[],{},8192,OrderByInheritance);
ConstantFolder.type$ = p.at$('ConstantFolder','compiler::CompilerSupport',[],{},8192,ConstantFolder);
ApiDocWriter.type$ = p.at$('ApiDocWriter','sys::Obj',[],{},8192,ApiDocWriter);
Coercer.type$ = p.at$('Coercer','compiler::CompilerSupport',[],{},8192,Coercer);
CallResolver.type$ = p.at$('CallResolver','compiler::CompilerSupport',[],{},8192,CallResolver);
CompilerErr.type$.af$('level',73730,'sys::LogLevel',{}).af$('file',73730,'sys::Str?',{}).af$('line',73730,'sys::Int?',{}).af$('col',73730,'sys::Int?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('msg','sys::Str',false),new sys.Param('loc','compiler::Loc?',false),new sys.Param('cause','sys::Err?',true),new sys.Param('level','sys::LogLevel',true)]),{}).am$('loc',8192,'compiler::Loc',xp,{}).am$('isErr',8192,'sys::Bool',xp,{}).am$('isWarn',8192,'sys::Bool',xp,{});
CompilerLog.type$.af$('level',73728,'sys::LogLevel',{}).af$('indentation',73728,'sys::Int',{}).af$('out',73728,'sys::OutStream?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',true)]),{}).am$('isDebug',8192,'sys::Bool',xp,{}).am$('indent',8192,'sys::Void',xp,{}).am$('unindent',8192,'sys::Void',xp,{}).am$('err',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('msg','sys::Str',false),new sys.Param('err','sys::Err?',true)]),{}).am$('warn',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('msg','sys::Str',false),new sys.Param('err','sys::Err?',true)]),{}).am$('info',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('msg','sys::Str',false),new sys.Param('err','sys::Err?',true)]),{}).am$('debug',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('msg','sys::Str',false),new sys.Param('err','sys::Err?',true)]),{}).am$('log',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('rec','sys::LogRec',false)]),{}).am$('compilerErr',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('err','compiler::CompilerErr',false)]),{}).am$('print',8192,'compiler::CompilerLog',sys.List.make(sys.Param.type$,[new sys.Param('s','sys::Obj?',false)]),{}).am$('printLine',8192,'compiler::CompilerLog',sys.List.make(sys.Param.type$,[new sys.Param('s','sys::Obj?',true)]),{});
Loc.type$.af$('file',73728,'sys::Str',{}).af$('line',73728,'sys::Int?',{}).af$('col',73728,'sys::Int?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('file','sys::Str',false),new sys.Param('line','sys::Int?',true),new sys.Param('col','sys::Int?',true)]),{}).am$('makeFile',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('file','sys::File?',false),new sys.Param('line','sys::Int?',true),new sys.Param('col','sys::Int?',true)]),{}).am$('makeUninit',8196,'sys::Void',xp,{}).am$('filename',8192,'sys::Str?',xp,{}).am$('fileUri',8192,'sys::Str?',xp,{}).am$('hash',271360,'sys::Int',xp,{}).am$('equals',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('that','sys::Obj?',false)]),{}).am$('compare',271360,'sys::Int',sys.List.make(sys.Param.type$,[new sys.Param('that','sys::Obj',false)]),{}).am$('toStr',271360,'sys::Str',xp,{}).am$('toLocStr',8192,'sys::Str',xp,{});
Compiler.type$.af$('input',73728,'compiler::CompilerInput',{}).af$('log',73728,'compiler::CompilerLog',{}).af$('errs',73728,'compiler::CompilerErr[]',{}).af$('warns',73728,'compiler::CompilerErr[]',{}).af$('depends',73728,'compiler::CDepend[]',{}).af$('ns',73728,'compiler::CNamespace?',{}).af$('pod',73728,'compiler::PodDef?',{}).af$('isSys',73728,'sys::Bool',{}).af$('srcFiles',73728,'sys::File[]?',{}).af$('resFiles',73728,'sys::File[]?',{}).af$('jsFiles',73728,'sys::File[]?',{}).af$('jsPropsFiles',73728,'sys::File[]?',{}).af$('types',73728,'compiler::TypeDef[]?',{}).af$('closures',73728,'compiler::ClosureExpr[]?',{}).af$('wrappers',73728,'[sys::Str:compiler::CField]',{}).af$('jsPod',73728,'sys::Obj?',{}).af$('js',73728,'sys::Str?',{}).af$('jsSourceMap',73728,'sys::Str?',{}).af$('cjs',73728,'sys::Str?',{}).af$('cjsSourceMap',73728,'sys::Str?',{}).af$('esm',73728,'sys::Str?',{}).af$('tsDecl',73728,'sys::Str?',{}).af$('localeDefs',73728,'compiler::LocaleLiteralExpr[]',{}).af$('localeProps',73728,'sys::Str?',{}).af$('fpod',73728,'compiler::FPod?',{}).af$('output',73728,'compiler::CompilerOutput?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('input','compiler::CompilerInput',false)]),{}).am$('compile',270336,'compiler::CompilerOutput',xp,{}).am$('frontend',270336,'sys::Void',xp,{}).am$('backend',270336,'sys::Void',xp,{}).am$('cleanup',2048,'sys::Void',xp,{});
CompilerSupport.type$.af$('c',67584,'compiler::Compiler',{}).af$('suppressErr',73728,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('compiler',270336,'compiler::Compiler',xp,{}).am$('ns',8192,'compiler::CNamespace',xp,{}).am$('pod',8192,'compiler::PodDef',xp,{}).am$('units',8192,'compiler::CompilationUnit[]',xp,{}).am$('syntheticsUnit',8192,'compiler::CompilationUnit',xp,{}).am$('types',8192,'compiler::TypeDef[]',xp,{}).am$('log',8192,'compiler::CompilerLog',xp,{}).am$('addTypeDef',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('removeTypeDef',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('needParameterizedCollectionCoerce',8192,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::CType',false)]),{}).am$('err',270336,'compiler::CompilerErr',sys.List.make(sys.Param.type$,[new sys.Param('msg','sys::Str',false),new sys.Param('loc','compiler::Loc?',true)]),{}).am$('warn',270336,'compiler::CompilerErr',sys.List.make(sys.Param.type$,[new sys.Param('msg','sys::Str',false),new sys.Param('loc','compiler::Loc?',true)]),{}).am$('errReport',8192,'compiler::CompilerErr',sys.List.make(sys.Param.type$,[new sys.Param('e','compiler::CompilerErr',false)]),{}).am$('bombIfErr',8192,'sys::Void',xp,{});
Visitor.type$.am$('walk',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('c','compiler::Compiler',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('enterUnit',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('unit','compiler::CompilationUnit',false)]),{}).am$('exitUnit',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('unit','compiler::CompilationUnit',false)]),{}).am$('enterTypeDef',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::TypeDef',false)]),{}).am$('exitTypeDef',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::TypeDef',false)]),{}).am$('visitTypeDef',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::TypeDef',false)]),{}).am$('enterFieldDef',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::FieldDef',false)]),{}).am$('exitFieldDef',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::FieldDef',false)]),{}).am$('visitFieldDef',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::FieldDef',false)]),{}).am$('enterMethodDef',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::MethodDef',false)]),{}).am$('exitMethodDef',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::MethodDef',false)]),{}).am$('visitMethodDef',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::MethodDef',false)]),{}).am$('enterBlock',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('block','compiler::Block',false)]),{}).am$('exitBlock',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('block','compiler::Block',false)]),{}).am$('visitBlock',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('block','compiler::Block',false)]),{}).am$('enterStmt',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::Stmt',false)]),{}).am$('exitStmt',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::Stmt',false)]),{}).am$('visitStmt',270336,'compiler::Stmt[]?',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::Stmt',false)]),{}).am$('enterFinally',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::TryStmt',false)]),{}).am$('exitFinally',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::TryStmt',false)]),{}).am$('visitExpr',270336,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false)]),{});
CompilerStep.type$.af$('curType',73728,'compiler::TypeDef?',{}).af$('curMethod',73728,'compiler::MethodDef?',{}).af$('curUnit',73728,'compiler::CompilationUnit?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',270337,'sys::Void',xp,{}).am$('inStatic',8192,'sys::Bool',xp,{}).am$('enterUnit',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('unit','compiler::CompilationUnit',false)]),{}).am$('exitUnit',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('unit','compiler::CompilationUnit',false)]),{}).am$('enterTypeDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::TypeDef',false)]),{}).am$('exitTypeDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::TypeDef',false)]),{}).am$('enterMethodDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::MethodDef',false)]),{}).am$('exitMethodDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::MethodDef',false)]),{});
CompilerInput.type$.af$('inputLoc',73728,'compiler::Loc',{}).af$('podName',73728,'sys::Str?',{}).af$('isScript',73728,'sys::Bool',{}).af$('forceJs',73728,'sys::Bool',{}).af$('jsReflectClosures',73728,'sys::Bool',{}).af$('version',73728,'sys::Version?',{}).af$('summary',73728,'sys::Str?',{}).af$('depends',73728,'sys::Depend[]',{}).af$('ns',73728,'compiler::CNamespace',{}).af$('meta',73728,'[sys::Str:sys::Str]',{}).af$('index',73728,'[sys::Str:sys::Obj]',{}).af$('output',73728,'compiler::CompilerOutputMode?',{}).af$('log',73728,'compiler::CompilerLog',{}).af$('outDir',73728,'sys::File',{}).af$('includeDoc',73728,'sys::Bool',{}).af$('includeSrc',73728,'sys::Bool',{}).af$('isTest',73728,'sys::Bool',{}).af$('fcodeDump',73728,'sys::Bool',{}).af$('mode',73728,'compiler::CompilerInputMode?',{}).af$('docTests',73728,'sys::Bool',{}).af$('wrapperPerParameterizedCollectionType',73728,'sys::Bool',{}).af$('coerceParameterizedCollectionTypes',73728,'sys::Bool',{}).af$('baseDir',73728,'sys::File?',{}).af$('srcFiles',73728,'sys::Uri[]?',{}).af$('resFiles',73728,'sys::Uri[]?',{}).af$('jsFiles',73728,'sys::Uri[]?',{}).af$('jsPropsFiles',73728,'sys::Uri[]?',{}).af$('srcStr',73728,'sys::Str?',{}).af$('srcStrLoc',73728,'compiler::Loc?',{}).am$('validate',128,'sys::Void',xp,{}).am$('validateReqField',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('field','sys::Str',false)]),{}).am$('make',139268,'sys::Void',xp,{});
CompilerInputMode.type$.af$('file',106506,'compiler::CompilerInputMode',{}).af$('str',106506,'compiler::CompilerInputMode',{}).af$('vals',106498,'compiler::CompilerInputMode[]',{}).am$('make',133124,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('$ordinal','sys::Int',false),new sys.Param('$name','sys::Str',false)]),{}).am$('fromStr',40966,'compiler::CompilerInputMode?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('checked','sys::Bool',true)]),{}).am$('static$init',165890,'sys::Void',xp,{});
SuppressedErr.type$.am$('make',8196,'sys::Void',xp,{});
CompilerOutput.type$.af$('mode',73728,'compiler::CompilerOutputMode?',{}).af$('transientPod',73728,'sys::Pod?',{}).af$('podFile',73728,'sys::File?',{}).af$('js',73728,'sys::Str?',{}).af$('cjs',73728,'sys::Str?',{}).af$('esm',73728,'sys::Str?',{}).am$('make',139268,'sys::Void',xp,{});
CompilerOutputMode.type$.af$('transientPod',106506,'compiler::CompilerOutputMode',{}).af$('podFile',106506,'compiler::CompilerOutputMode',{}).af$('js',106506,'compiler::CompilerOutputMode',{}).af$('vals',106498,'compiler::CompilerOutputMode[]',{}).am$('make',133124,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('$ordinal','sys::Int',false),new sys.Param('$name','sys::Str',false)]),{}).am$('fromStr',40966,'compiler::CompilerOutputMode?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('checked','sys::Bool',true)]),{}).am$('static$init',165890,'sys::Void',xp,{});
Main.type$.am$('compileScript',40962,'sys::Pod',sys.List.make(sys.Param.type$,[new sys.Param('podName','sys::Str',false),new sys.Param('file','sys::File',false),new sys.Param('options','[sys::Str:sys::Obj]?',true)]),{}).am$('compileScriptToJs',40962,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('podName','sys::Str',false),new sys.Param('file','sys::File',false),new sys.Param('options','[sys::Str:sys::Obj]?',true)]),{}).am$('make',139268,'sys::Void',xp,{});
AttrAsm.type$.af$('fpod',73728,'compiler::FPod',{}).af$('attrs',73728,'compiler::FAttr[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false),new sys.Param('fpod','compiler::FPod',false)]),{}).am$('sourceFile',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('source','sys::Str?',false)]),{}).am$('lineNumber',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('line','sys::Int?',false)]),{}).am$('facets',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('facets','compiler::FacetDef[]?',false)]),{}).am$('enumOrdinal',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('ordinal','sys::Int',false)]),{}).am$('utf',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('data','sys::Str',false)]),{}).am$('u2',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('data','sys::Int',false)]),{}).am$('add',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('data','sys::Buf',false)]),{});
FConst.type$.af$('FCodeVersion',106498,'sys::Str',{}).af$('Abstract',106498,'sys::Int',{}).af$('Const',106498,'sys::Int',{}).af$('Ctor',106498,'sys::Int',{}).af$('Enum',106498,'sys::Int',{}).af$('Facet',106498,'sys::Int',{}).af$('Final',106498,'sys::Int',{}).af$('Getter',106498,'sys::Int',{}).af$('Internal',106498,'sys::Int',{}).af$('Mixin',106498,'sys::Int',{}).af$('Native',106498,'sys::Int',{}).af$('Override',106498,'sys::Int',{}).af$('Private',106498,'sys::Int',{}).af$('Protected',106498,'sys::Int',{}).af$('Public',106498,'sys::Int',{}).af$('Setter',106498,'sys::Int',{}).af$('Static',106498,'sys::Int',{}).af$('Storage',106498,'sys::Int',{}).af$('Synthetic',106498,'sys::Int',{}).af$('Virtual',106498,'sys::Int',{}).af$('Once',106498,'sys::Int',{}).af$('FlagsMask',106498,'sys::Int',{}).af$('Param',106498,'sys::Int',{}).af$('ErrTableAttr',106498,'sys::Str',{}).af$('FacetsAttr',106498,'sys::Str',{}).af$('LineNumberAttr',106498,'sys::Str',{}).af$('LineNumbersAttr',106498,'sys::Str',{}).af$('SourceFileAttr',106498,'sys::Str',{}).af$('ParamDefaultAttr',106498,'sys::Str',{}).af$('EnumOrdinalAttr',106498,'sys::Str',{}).am$('static$init',165890,'sys::Void',xp,{});
Assembler.type$.af$('fpod',67584,'compiler::FPod?',{}).af$('omitCode',67584,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('assemblePod',8192,'compiler::FPod',xp,{}).am$('assemblePodNoCode',8192,'compiler::FPod',xp,{}).am$('assembleMeta',2048,'[sys::Str:sys::Str]',xp,{}).am$('assembleType',2048,'compiler::FType',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::TypeDef',false)]),{}).am$('assembleField',8192,'compiler::FField',sys.List.make(sys.Param.type$,[new sys.Param('fparent','compiler::FType',false),new sys.Param('def','compiler::FieldDef',false)]),{}).am$('assembleMethod',8192,'compiler::FMethod',sys.List.make(sys.Param.type$,[new sys.Param('fparent','compiler::FType',false),new sys.Param('def','compiler::MethodDef',false)]),{}).am$('typeRef',8192,'sys::Int',sys.List.make(sys.Param.type$,[new sys.Param('type','compiler::CType',false)]),{}).am$('name',8192,'sys::Int',sys.List.make(sys.Param.type$,[new sys.Param('val','sys::Str',false)]),{}).am$('assembleCode',2048,'sys::Buf?',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::MethodDef',false),new sys.Param('attrs','compiler::AttrAsm',false)]),{}).am$('assembleExpr',2048,'sys::Buf?',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr?',false)]),{});
CodeAsm.type$.af$('loc',73728,'compiler::Loc',{}).af$('fpod',73728,'compiler::FPod',{}).af$('curMethod',73728,'compiler::MethodDef?',{}).af$('code',73728,'sys::Buf',{}).af$('errTable',73728,'sys::Buf',{}).af$('errCount',73728,'sys::Int',{}).af$('lines',73728,'sys::Buf',{}).af$('lineCount',73728,'sys::Int',{}).af$('lastLine',73728,'sys::Int',{}).af$('lastOffset',73728,'sys::Int',{}).af$('loopStack',73728,'compiler::Loop[]',{}).af$('protectedRegions',73728,'compiler::ProtectedRegion[]?',{}).af$('leavesToReturn',73728,'sys::Int[]?',{}).af$('returnLocal',73728,'compiler::MethodVar?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false),new sys.Param('loc','compiler::Loc',false),new sys.Param('fpod','compiler::FPod',false),new sys.Param('curMethod','compiler::MethodDef?',false)]),{}).am$('block',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('block','compiler::Block',false)]),{}).am$('stmt',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::Stmt',false)]),{}).am$('ifStmt',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::IfStmt',false)]),{}).am$('returnStmt',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::ReturnStmt',false)]),{}).am$('throwStmt',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::ThrowStmt',false)]),{}).am$('throwOp',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('exception','compiler::Expr',false)]),{}).am$('localVarDefStmt',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::LocalDefStmt',false)]),{}).am$('whileStmt',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::WhileStmt',false)]),{}).am$('forStmt',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::ForStmt',false)]),{}).am$('breakOrContinueStmt',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::Stmt',false)]),{}).am$('switchStmt',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::SwitchStmt',false)]),{}).am$('computeTableRange',2048,'sys::Int[]?',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::SwitchStmt',false)]),{}).am$('tableSwitchStmt',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::SwitchStmt',false),new sys.Param('min','sys::Int',false),new sys.Param('max','sys::Int',false)]),{}).am$('equalsSwitchStmt',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::SwitchStmt',false)]),{}).am$('switchBlock',2048,'sys::Int',sys.List.make(sys.Param.type$,[new sys.Param('block','compiler::Block?',false),new sys.Param('popType','compiler::CType?',true)]),{}).am$('inProtectedRegion',2048,'sys::Bool',xp,{}).am$('tryStmt',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::TryStmt',false)]),{}).am$('tryCatch',2048,'sys::Int',sys.List.make(sys.Param.type$,[new sys.Param('c','compiler::Catch',false),new sys.Param('start','sys::Int',false),new sys.Param('end','sys::Int',false),new sys.Param('region','compiler::ProtectedRegion',false)]),{}).am$('addToErrTable',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('start','sys::Int',false),new sys.Param('end','sys::Int',false),new sys.Param('handler','sys::Int',false),new sys.Param('errType','compiler::CType?',false)]),{}).am$('expr',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false)]),{}).am$('nullLiteral',2048,'sys::Void',xp,{}).am$('boolLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::LiteralExpr',false)]),{}).am$('intLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::LiteralExpr',false)]),{}).am$('floatLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::LiteralExpr',false)]),{}).am$('decimalLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::LiteralExpr',false)]),{}).am$('strLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::LiteralExpr',false)]),{}).am$('durationLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::LiteralExpr',false)]),{}).am$('uriLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::LiteralExpr',false)]),{}).am$('typeLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::LiteralExpr',false)]),{}).am$('slotLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::SlotLiteralExpr',false)]),{}).am$('rangeLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('r','compiler::RangeLiteralExpr',false)]),{}).am$('listLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('list','compiler::ListLiteralExpr',false)]),{}).am$('mapLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('map','compiler::MapLiteralExpr',false)]),{}).am$('not',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('unary','compiler::UnaryExpr',false)]),{}).am$('cmpNull',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('unary','compiler::UnaryExpr',false)]),{}).am$('cmpNotNull',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('unary','compiler::UnaryExpr',false)]),{}).am$('same',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('binary','compiler::BinaryExpr',false)]),{}).am$('notSame',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('binary','compiler::BinaryExpr',false)]),{}).am$('cond',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::CondExpr',false),new sys.Param('cond','compiler::Cond',false)]),{}).am$('or',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::CondExpr',false),new sys.Param('cond','compiler::Cond?',false)]),{}).am$('and',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::CondExpr',false),new sys.Param('cond','compiler::Cond?',false)]),{}).am$('condEnd',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('cond','compiler::Cond',false)]),{}).am$('isExpr',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('tc','compiler::TypeCheckExpr',false)]),{}).am$('isnotExpr',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('tc','compiler::TypeCheckExpr',false)]),{}).am$('asExpr',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('tc','compiler::TypeCheckExpr',false)]),{}).am$('coerce',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('tc','compiler::TypeCheckExpr',false)]),{}).am$('coerceOp',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('from','compiler::CType',false),new sys.Param('to','compiler::CType',false)]),{}).am$('elvis',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('binary','compiler::BinaryExpr',false)]),{}).am$('ternary',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('ternary','compiler::TernaryExpr',false)]),{}).am$('closure',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('c','compiler::ClosureExpr',false)]),{}).am$('assign',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::BinaryExpr',false)]),{}).am$('loadLocalVar',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('var','compiler::LocalVarExpr',false)]),{}).am$('storeLocalVar',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('var','compiler::LocalVarExpr',false)]),{}).am$('assignLocalVar',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('assign','compiler::BinaryExpr',false)]),{}).am$('loadField',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('fexpr','compiler::FieldExpr',false),new sys.Param('dupTarget','sys::Bool',true)]),{}).am$('assignField',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('assign','compiler::BinaryExpr',false)]),{}).am$('storeField',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('fexpr','compiler::FieldExpr',false)]),{}).am$('call',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false),new sys.Param('leave','sys::Bool',true)]),{}).am$('dynamicCall',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false)]),{}).am$('invokeCall',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false),new sys.Param('leave','sys::Bool',true)]),{}).am$('shortcut',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::ShortcutExpr',false)]),{}).am$('compareOp',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('lhs','sys::Obj',false),new sys.Param('opCode','compiler::FOp',false),new sys.Param('rhs','compiler::Expr',false)]),{}).am$('shortcutAssign',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('c','compiler::ShortcutExpr',false)]),{}).am$('addStr',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::ShortcutExpr',false),new sys.Param('topLevel','sys::Bool',false)]),{}).am$('isEmptyStrLiteral',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false)]),{}).am$('opType',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('opcode','compiler::FOp',false),new sys.Param('arg','compiler::CType',false)]),{}).am$('op',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('op','compiler::FOp',false),new sys.Param('arg','sys::Int?',true)]),{}).am$('mark',2048,'sys::Int',xp,{}).am$('jump',2048,'sys::Int',sys.List.make(sys.Param.type$,[new sys.Param('op','compiler::FOp',false),new sys.Param('mark','sys::Int',true)]),{}).am$('backpatch',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('pos','sys::Int',false),new sys.Param('mark','sys::Int',true)]),{}).am$('finishCode',8192,'sys::Buf',xp,{}).am$('finishErrTable',8192,'sys::Buf',xp,{}).am$('finishLines',8192,'sys::Buf',xp,{}).am$('line',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false)]),{});
Loop.type$.af$('stmt',73728,'compiler::Stmt',{}).af$('breaks',73728,'sys::Int[]',{}).af$('continues',73728,'sys::Int[]',{}).af$('protectedRegions',73728,'compiler::ProtectedRegion[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::Stmt',false)]),{});
ProtectedRegion.type$.af$('hasFinally',73728,'sys::Bool',{}).af$('jumpFinallys',73728,'sys::Int[]?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::TryStmt',false)]),{});
Cond.type$.af$('jumpTrues',73728,'sys::Int[]',{}).af$('jumpFalses',73728,'sys::Int[]',{}).am$('make',139268,'sys::Void',xp,{});
Node.type$.af$('loc',73728,'compiler::Loc',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false)]),{}).am$('dump',8192,'sys::Void',xp,{}).am$('print',270337,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
CNode.type$.am$('ns',270337,'compiler::CNamespace',xp,{}).am$('doc',270337,'compiler::CDoc?',xp,{}).am$('facet',270337,'compiler::CFacet?',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('hasFacet',8192,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('isNoDoc',8192,'sys::Bool',xp,{});
DefNode.type$.af$('docDef',73728,'compiler::DocDef?',{}).af$('flags',73728,'sys::Int',{}).af$('facets',73728,'compiler::FacetDef[]?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false)]),{}).am$('doc',271360,'compiler::CDoc?',xp,{}).am$('walkFacets',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('facet',271360,'compiler::CFacet?',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('addFacet',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('support','compiler::CompilerSupport',false),new sys.Param('type','compiler::CType',false),new sys.Param('vals','[sys::Str:sys::Obj]?',true)]),{}).am$('printFacets',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{}).am$('isDocumented',128,'sys::Bool',xp,{});
CDoc.type$.am$('fromStr',40966,'compiler::CDoc?',sys.List.make(sys.Param.type$,[new sys.Param('s','sys::Str?',false)]),{}).am$('text',270337,'sys::Str',xp,{});
DocDef.type$.af$('lines',73728,'sys::Str[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('lines','sys::Str[]',false)]),{}).am$('text',271360,'sys::Str',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
Block.type$.af$('stmts',73728,'compiler::Stmt[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false)]),{}).am$('isEmpty',8192,'sys::Bool',xp,{}).am$('size',8192,'sys::Int',xp,{}).am$('isExit',8192,'sys::Bool',xp,{}).am$('isDefiniteAssign',8192,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('add',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::Stmt',false)]),{}).am$('addAll',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmts','compiler::Stmt[]',false)]),{}).am$('walkExpr',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('closure','|compiler::Expr->compiler::Expr|',false)]),{}).am$('walk',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{}).am$('printOpt',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false),new sys.Param('braces','sys::Bool',true)]),{});
Expr.type$.af$('id',73730,'compiler::ExprId',{}).af$('ctype',73728,'compiler::CType?',{}).af$('leave',73728,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('id','compiler::ExprId',false)]),{}).am$('asTableSwitchCase',270336,'sys::Int?',xp,{}).am$('toTypeStr',8192,'sys::Str',xp,{}).am$('assignTarget',270336,'sys::Obj?',xp,{}).am$('isAssignable',270336,'sys::Bool',xp,{}).am$('isCond',270336,'sys::Bool',xp,{}).am$('isStmt',270336,'sys::Bool',xp,{}).am$('synthetic',270336,'sys::Bool',xp,{}).am$('isDefiniteAssign',270336,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('isAlwaysNullable',270336,'sys::Bool',xp,{}).am$('asCall',270336,'compiler::CallExpr?',xp,{}).am$('assignRequiresTempVar',270336,'sys::Bool',xp,{}).am$('sameVarAs',270336,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('that','compiler::Expr',false)]),{}).am$('ctypes',40962,'compiler::CType[]',sys.List.make(sys.Param.type$,[new sys.Param('exprs','compiler::Expr[]',false)]),{}).am$('commonType',40962,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::CNamespace',false),new sys.Param('exprs','compiler::Expr[]',false)]),{}).am$('toStmt',8192,'compiler::ExprStmt',xp,{}).am$('serialize',270336,'sys::Str',xp,{}).am$('makeForLiteral',40962,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ns','compiler::CNamespace',false),new sys.Param('val','sys::Obj',false)]),{}).am$('noLeave',8192,'compiler::Expr',xp,{}).am$('toDocStr',8192,'sys::Str?',xp,{}).am$('walk',8192,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('walkChildren',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('walkExpr',40962,'compiler::Expr?',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('expr','compiler::Expr?',false)]),{}).am$('walkExprs',40962,'compiler::Expr[]',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('exprs','compiler::Expr?[]',false)]),{}).am$('toStr',271361,'sys::Str',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
LiteralExpr.type$.af$('val',73728,'sys::Obj?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('id','compiler::ExprId',false),new sys.Param('ctype','compiler::CType',false),new sys.Param('val','sys::Obj?',false)]),{}).am$('makeNull',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ns','compiler::CNamespace',false)]),{}).am$('makeTrue',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ns','compiler::CNamespace',false)]),{}).am$('makeFalse',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ns','compiler::CNamespace',false)]),{}).am$('makeStr',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ns','compiler::CNamespace',false),new sys.Param('val','sys::Str',false)]),{}).am$('makeDefaultLiteral',40962,'compiler::LiteralExpr',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ns','compiler::CNamespace',false),new sys.Param('ctype','compiler::CType',false)]),{}).am$('isAlwaysNullable',271360,'sys::Bool',xp,{}).am$('asTableSwitchCase',271360,'sys::Int?',xp,{}).am$('serialize',271360,'sys::Str',xp,{}).am$('toStr',271360,'sys::Str',xp,{});
LocaleLiteralExpr.type$.af$('pattern',73728,'sys::Str',{}).af$('key',73728,'sys::Str',{}).af$('podName',73728,'sys::Str?',{}).af$('def',73728,'sys::Str?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('pattern','sys::Str',false)]),{}).am$('toStr',271360,'sys::Str',xp,{});
SlotLiteralExpr.type$.af$('parent',73728,'compiler::CType',{}).af$('name',73728,'sys::Str',{}).af$('slot',73728,'compiler::CSlot?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('parent','compiler::CType',false),new sys.Param('name','sys::Str',false)]),{}).am$('serialize',271360,'sys::Str',xp,{}).am$('toStr',271360,'sys::Str',xp,{});
RangeLiteralExpr.type$.af$('start',73728,'compiler::Expr',{}).af$('end',73728,'compiler::Expr',{}).af$('exclusive',73728,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ctype','compiler::CType',false),new sys.Param('start','compiler::Expr',false),new sys.Param('end','compiler::Expr',false),new sys.Param('exclusive','sys::Bool',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('toStr',271360,'sys::Str',xp,{});
ListLiteralExpr.type$.af$('explicitType',73728,'compiler::ListType?',{}).af$('vals',73728,'compiler::Expr[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('explicitType','compiler::ListType?',true)]),{}).am$('makeFor',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ctype','compiler::CType',false),new sys.Param('vals','compiler::Expr[]',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('serialize',271360,'sys::Str',xp,{}).am$('toStr',271360,'sys::Str',xp,{}).am$('format',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Str|',false)]),{});
MapLiteralExpr.type$.af$('explicitType',73728,'compiler::MapType?',{}).af$('keys',73728,'compiler::Expr[]',{}).af$('vals',73728,'compiler::Expr[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('explicitType','compiler::MapType?',true)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('serialize',271360,'sys::Str',xp,{}).am$('toStr',271360,'sys::Str',xp,{}).am$('format',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Str|',false)]),{});
UnaryExpr.type$.af$('opToken',73728,'compiler::Token',{}).af$('operand',73728,'compiler::Expr',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('id','compiler::ExprId',false),new sys.Param('opToken','compiler::Token',false),new sys.Param('operand','compiler::Expr',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('toStr',271360,'sys::Str',xp,{});
BinaryExpr.type$.af$('opToken',73728,'compiler::Token',{}).af$('lhs',73728,'compiler::Expr',{}).af$('rhs',73728,'compiler::Expr',{}).af$('tempVar',73728,'compiler::MethodVar?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('lhs','compiler::Expr',false),new sys.Param('opToken','compiler::Token',false),new sys.Param('rhs','compiler::Expr',false)]),{}).am$('makeAssign',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('lhs','compiler::Expr',false),new sys.Param('rhs','compiler::Expr',false),new sys.Param('leave','sys::Bool',true)]),{}).am$('assignTarget',271360,'sys::Obj?',xp,{}).am$('isStmt',271360,'sys::Bool',xp,{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('serialize',271360,'sys::Str',xp,{}).am$('toStr',271360,'sys::Str',xp,{});
CondExpr.type$.af$('opToken',73728,'compiler::Token',{}).af$('operands',73728,'compiler::Expr[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('first','compiler::Expr',false),new sys.Param('opToken','compiler::Token',false)]),{}).am$('isCond',271360,'sys::Bool',xp,{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('toStr',271360,'sys::Str',xp,{});
NameExpr.type$.af$('target',73728,'compiler::Expr?',{}).af$('name',73728,'sys::Str?',{}).af$('isSafe',73728,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('id','compiler::ExprId',false),new sys.Param('target','compiler::Expr?',false),new sys.Param('name','sys::Str?',false)]),{}).am$('isAlwaysNullable',271360,'sys::Bool',xp,{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('toStr',271360,'sys::Str',xp,{});
UnknownVarExpr.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('target','compiler::Expr?',false),new sys.Param('name','sys::Str',false),new sys.Param('id','compiler::ExprId',true)]),{});
CallExpr.type$.af$('args',73728,'compiler::Expr[]',{}).af$('isDynamic',73728,'sys::Bool',{}).af$('isCtorChain',73728,'sys::Bool',{}).af$('noParens',73728,'sys::Bool',{}).af$('isCallOp',73728,'sys::Bool',{}).af$('isItAdd',73728,'sys::Bool',{}).af$('method',73728,'compiler::CMethod?',{}).af$('synthetic',336896,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('target','compiler::Expr?',true),new sys.Param('name','sys::Str?',true),new sys.Param('id','compiler::ExprId',true)]),{}).am$('makeWithMethod',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('target','compiler::Expr?',false),new sys.Param('method','compiler::CMethod',false),new sys.Param('args','compiler::Expr[]?',true)]),{}).am$('toStr',271360,'sys::Str',xp,{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('isStmt',271360,'sys::Bool',xp,{}).am$('asCall',271360,'compiler::CallExpr?',xp,{}).am$('isCompare',270336,'sys::Bool',xp,{}).am$('targetx',8192,'compiler::Expr',xp,{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('serialize',271360,'sys::Str',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{}).am$('toCallStr',2048,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('isToStr','sys::Bool',false)]),{});
ShortcutExpr.type$.af$('op',73728,'compiler::ShortcutOp',{}).af$('opToken',73728,'compiler::Token',{}).af$('isPostfixLeave',73728,'sys::Bool',{}).af$('tempVar',73728,'compiler::MethodVar?',{}).am$('makeUnary',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('opToken','compiler::Token',false),new sys.Param('operand','compiler::Expr',false)]),{}).am$('makeBinary',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('lhs','compiler::Expr',false),new sys.Param('opToken','compiler::Token',false),new sys.Param('rhs','compiler::Expr',false)]),{}).am$('makeGet',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('target','compiler::Expr',false),new sys.Param('index','compiler::Expr',false)]),{}).am$('makeFrom',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('from','compiler::ShortcutExpr',false)]),{}).am$('assignRequiresTempVar',271360,'sys::Bool',xp,{}).am$('assignTarget',271360,'sys::Obj?',xp,{}).am$('isAssignable',271360,'sys::Bool',xp,{}).am$('isCompare',271360,'sys::Bool',xp,{}).am$('isStmt',271360,'sys::Bool',xp,{}).am$('isAssign',8192,'sys::Bool',xp,{}).am$('isStrConcat',8192,'sys::Bool',xp,{}).am$('toStr',271360,'sys::Str',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
IndexedAssignExpr.type$.af$('scratchA',73728,'compiler::MethodVar?',{}).af$('scratchB',73728,'compiler::MethodVar?',{}).af$('setMethod',73728,'compiler::CMethod?',{}).am$('makeFrom',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('from','compiler::ShortcutExpr',false)]),{});
FieldExpr.type$.af$('field',73728,'compiler::CField?',{}).af$('useAccessor',73728,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('target','compiler::Expr?',true),new sys.Param('field','compiler::CField?',true),new sys.Param('useAccessor','sys::Bool',true)]),{}).am$('isAssignable',271360,'sys::Bool',xp,{}).am$('assignRequiresTempVar',271360,'sys::Bool',xp,{}).am$('sameVarAs',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('that','compiler::Expr',false)]),{}).am$('asTableSwitchCase',271360,'sys::Int?',xp,{}).am$('serialize',271360,'sys::Str',xp,{}).am$('toStr',271360,'sys::Str',xp,{});
LocalVarExpr.type$.af$('var',73728,'compiler::MethodVar?',{}).af$('unwrap',73728,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('var','compiler::MethodVar?',false),new sys.Param('id','compiler::ExprId',true)]),{}).am$('makeNoUnwrap',40962,'compiler::LocalVarExpr',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('var','compiler::MethodVar',false)]),{}).am$('isAssignable',271360,'sys::Bool',xp,{}).am$('assignRequiresTempVar',271360,'sys::Bool',xp,{}).am$('sameVarAs',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('that','compiler::Expr',false)]),{}).am$('register',270336,'sys::Int',xp,{}).am$('name',270336,'sys::Str',xp,{}).am$('toStr',271360,'sys::Str',xp,{});
ThisExpr.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ctype','compiler::CType?',true)]),{}).am$('isAssignable',271360,'sys::Bool',xp,{}).am$('register',271360,'sys::Int',xp,{}).am$('toStr',271360,'sys::Str',xp,{});
SuperExpr.type$.af$('explicitType',73728,'compiler::CType?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('explicitType','compiler::CType?',true)]),{}).am$('isAssignable',271360,'sys::Bool',xp,{}).am$('register',271360,'sys::Int',xp,{}).am$('toStr',271360,'sys::Str',xp,{});
ItExpr.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ctype','compiler::CType?',true)]),{}).am$('isAssignable',271360,'sys::Bool',xp,{}).am$('register',271360,'sys::Int',xp,{}).am$('name',271360,'sys::Str',xp,{}).am$('toStr',271360,'sys::Str',xp,{});
StaticTargetExpr.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ctype','compiler::CType',false)]),{}).am$('sameVarAs',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('that','compiler::Expr',false)]),{}).am$('toStr',271360,'sys::Str',xp,{});
TypeCheckExpr.type$.af$('from',73728,'compiler::CType?',{}).af$('target',73728,'compiler::Expr',{}).af$('check',73728,'compiler::CType',{}).af$('synthetic',336896,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('id','compiler::ExprId',false),new sys.Param('target','compiler::Expr',false),new sys.Param('check','compiler::CType',false)]),{}).am$('coerce',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('target','compiler::Expr',false),new sys.Param('to','compiler::CType',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('isStmt',271360,'sys::Bool',xp,{}).am$('isAlwaysNullable',271360,'sys::Bool',xp,{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('serialize',271360,'sys::Str',xp,{}).am$('opStr',8192,'sys::Str',xp,{}).am$('toStr',271360,'sys::Str',xp,{});
TernaryExpr.type$.af$('condition',73728,'compiler::Expr',{}).af$('trueExpr',73728,'compiler::Expr',{}).af$('falseExpr',73728,'compiler::Expr',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('condition','compiler::Expr',false),new sys.Param('trueExpr','compiler::Expr',false),new sys.Param('falseExpr','compiler::Expr',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('toStr',271360,'sys::Str',xp,{});
ComplexLiteral.type$.af$('names',73728,'sys::Str[]',{}).af$('vals',73728,'compiler::Expr[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ctype','compiler::CType',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('toStr',271360,'sys::Str',xp,{}).am$('serialize',271360,'sys::Str',xp,{}).am$('doToStr',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Str|',false)]),{});
ClosureExpr.type$.af$('enclosingType',73728,'compiler::TypeDef',{}).af$('enclosingSlot',73728,'compiler::SlotDef',{}).af$('enclosingClosure',73728,'compiler::ClosureExpr?',{}).af$('signature',73728,'compiler::FuncType',{}).af$('code',73728,'compiler::Block?',{}).af$('name',73728,'sys::Str',{}).af$('isItBlock',73728,'sys::Bool',{}).af$('substitute',73728,'compiler::CallExpr?',{}).af$('cls',73728,'compiler::TypeDef?',{}).af$('call',73728,'compiler::MethodDef?',{}).af$('doCall',73728,'compiler::MethodDef?',{}).af$('enclosingVars',73728,'[sys::Str:compiler::MethodVar]?',{}).af$('setsConst',73728,'sys::Bool',{}).af$('itType',73728,'compiler::CType?',{}).af$('outerThisField$Store',722944,'sys::Obj?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('enclosingType','compiler::TypeDef',false),new sys.Param('enclosingSlot','compiler::SlotDef',false),new sys.Param('enclosingClosure','compiler::ClosureExpr?',false),new sys.Param('signature','compiler::FuncType',false),new sys.Param('name','sys::Str',false)]),{}).am$('outerThisField',532480,'compiler::CField',xp,{}).am$('toStr',271360,'sys::Str',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('toWith',8192,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('target','compiler::Expr',false)]),{}).am$('setInferredSignature',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::FuncType',false)]),{}).am$('collapseExprAndReturn',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false)]),{}).am$('outerThisField$Once',133120,'compiler::CField',xp,{});
DslExpr.type$.af$('anchorType',73728,'compiler::CType',{}).af$('src',73728,'sys::Str',{}).af$('srcLoc',73728,'compiler::Loc',{}).af$('leadingTabs',73728,'sys::Int',{}).af$('leadingSpaces',73728,'sys::Int',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('anchorType','compiler::CType',false),new sys.Param('srcLoc','compiler::Loc',false),new sys.Param('src','sys::Str',false)]),{}).am$('toStr',271360,'sys::Str',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
ThrowExpr.type$.af$('exception',73728,'compiler::Expr',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('exception','compiler::Expr',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('toStr',271360,'sys::Str',xp,{});
ExprId.type$.af$('nullLiteral',106506,'compiler::ExprId',{}).af$('trueLiteral',106506,'compiler::ExprId',{}).af$('falseLiteral',106506,'compiler::ExprId',{}).af$('intLiteral',106506,'compiler::ExprId',{}).af$('floatLiteral',106506,'compiler::ExprId',{}).af$('decimalLiteral',106506,'compiler::ExprId',{}).af$('strLiteral',106506,'compiler::ExprId',{}).af$('durationLiteral',106506,'compiler::ExprId',{}).af$('uriLiteral',106506,'compiler::ExprId',{}).af$('typeLiteral',106506,'compiler::ExprId',{}).af$('localeLiteral',106506,'compiler::ExprId',{}).af$('slotLiteral',106506,'compiler::ExprId',{}).af$('rangeLiteral',106506,'compiler::ExprId',{}).af$('listLiteral',106506,'compiler::ExprId',{}).af$('mapLiteral',106506,'compiler::ExprId',{}).af$('boolNot',106506,'compiler::ExprId',{}).af$('cmpNull',106506,'compiler::ExprId',{}).af$('cmpNotNull',106506,'compiler::ExprId',{}).af$('elvis',106506,'compiler::ExprId',{}).af$('assign',106506,'compiler::ExprId',{}).af$('same',106506,'compiler::ExprId',{}).af$('notSame',106506,'compiler::ExprId',{}).af$('boolOr',106506,'compiler::ExprId',{}).af$('boolAnd',106506,'compiler::ExprId',{}).af$('isExpr',106506,'compiler::ExprId',{}).af$('isnotExpr',106506,'compiler::ExprId',{}).af$('asExpr',106506,'compiler::ExprId',{}).af$('coerce',106506,'compiler::ExprId',{}).af$('call',106506,'compiler::ExprId',{}).af$('construction',106506,'compiler::ExprId',{}).af$('shortcut',106506,'compiler::ExprId',{}).af$('field',106506,'compiler::ExprId',{}).af$('localVar',106506,'compiler::ExprId',{}).af$('thisExpr',106506,'compiler::ExprId',{}).af$('superExpr',106506,'compiler::ExprId',{}).af$('itExpr',106506,'compiler::ExprId',{}).af$('staticTarget',106506,'compiler::ExprId',{}).af$('unknownVar',106506,'compiler::ExprId',{}).af$('storage',106506,'compiler::ExprId',{}).af$('ternary',106506,'compiler::ExprId',{}).af$('complexLiteral',106506,'compiler::ExprId',{}).af$('closure',106506,'compiler::ExprId',{}).af$('dsl',106506,'compiler::ExprId',{}).af$('throwExpr',106506,'compiler::ExprId',{}).af$('vals',106498,'compiler::ExprId[]',{}).am$('make',133124,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('$ordinal','sys::Int',false),new sys.Param('$name','sys::Str',false)]),{}).am$('fromStr',40966,'compiler::ExprId?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('checked','sys::Bool',true)]),{}).am$('static$init',165890,'sys::Void',xp,{});
ShortcutOp.type$.af$('plus',106506,'compiler::ShortcutOp',{}).af$('minus',106506,'compiler::ShortcutOp',{}).af$('mult',106506,'compiler::ShortcutOp',{}).af$('div',106506,'compiler::ShortcutOp',{}).af$('mod',106506,'compiler::ShortcutOp',{}).af$('negate',106506,'compiler::ShortcutOp',{}).af$('increment',106506,'compiler::ShortcutOp',{}).af$('decrement',106506,'compiler::ShortcutOp',{}).af$('eq',106506,'compiler::ShortcutOp',{}).af$('cmp',106506,'compiler::ShortcutOp',{}).af$('get',106506,'compiler::ShortcutOp',{}).af$('set',106506,'compiler::ShortcutOp',{}).af$('add',106506,'compiler::ShortcutOp',{}).af$('vals',106498,'compiler::ShortcutOp[]',{}).af$('prefixes',100354,'[sys::Str:compiler::ShortcutOp]',{}).af$('degree',73730,'sys::Int',{}).af$('methodName',73730,'sys::Str',{}).af$('isOperator',73730,'sys::Bool',{}).af$('symbol',73730,'sys::Str',{}).am$('make',2052,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('$ordinal','sys::Int',false),new sys.Param('$name','sys::Str',false),new sys.Param('degree','sys::Int',false),new sys.Param('symbol','sys::Str',false),new sys.Param('methodName','sys::Str?',true)]),{}).am$('fromPrefix',40962,'compiler::ShortcutOp?',sys.List.make(sys.Param.type$,[new sys.Param('prefix','sys::Str',false)]),{}).am$('formatErr',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('lhs','compiler::CType',false),new sys.Param('rhs','compiler::CType',false)]),{}).am$('fromStr',40966,'compiler::ShortcutOp?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('checked','sys::Bool',true)]),{}).am$('static$init',165890,'sys::Void',xp,{});
CompilationUnit.type$.af$('pod',73728,'compiler::CPod',{}).af$('tokens',73728,'compiler::TokenVal[]?',{}).af$('usings',73728,'compiler::Using[]',{}).af$('types',73728,'compiler::TypeDef[]',{}).af$('importedTypes',73728,'[sys::Str:compiler::CType[]]?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('pod','compiler::CPod',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{}).am$('toStr',271360,'sys::Str',xp,{});
CFacet.type$.am$('qname',270337,'sys::Str',xp,{}).am$('get',270337,'sys::Obj?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{});
FacetDef.type$.af$('type',73728,'compiler::CType',{}).af$('names',73728,'sys::Str[]',{}).af$('vals',73728,'compiler::Expr[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('type','compiler::CType',false)]),{}).am$('qname',271360,'sys::Str',xp,{}).am$('get',271360,'sys::Obj?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('serialize',8192,'sys::Str',xp,{}).am$('walk',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false)]),{}).am$('toStr',271360,'sys::Str',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
Stmt.type$.af$('id',73730,'compiler::StmtId',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('id','compiler::StmtId',false)]),{}).am$('isExit',270337,'sys::Bool',xp,{}).am$('isDefiniteAssign',270337,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('isOnceFieldInit',270336,'sys::Bool',xp,{}).am$('walk',8192,'compiler::Stmt[]?',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('walkChildren',270336,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('walkExpr',40962,'compiler::Expr?',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false),new sys.Param('expr','compiler::Expr?',false)]),{});
NopStmt.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false)]),{}).am$('isExit',271360,'sys::Bool',xp,{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
ExprStmt.type$.af$('expr',73728,'compiler::Expr',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false)]),{}).am$('isExit',271360,'sys::Bool',xp,{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('isOnceFieldInit',271360,'sys::Bool',xp,{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('toStr',271360,'sys::Str',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{}).am$('printOpt',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false),new sys.Param('nl','sys::Bool',true)]),{});
LocalDefStmt.type$.af$('ctype',73728,'compiler::CType?',{}).af$('name',73728,'sys::Str',{}).af$('init',73728,'compiler::Expr?',{}).af$('isCatchVar',73728,'sys::Bool',{}).af$('var',73728,'compiler::MethodVar?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ctype','compiler::CType?',true),new sys.Param('name','sys::Str',true)]),{}).am$('makeAssign',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('init','compiler::BinaryExpr',false)]),{}).am$('isExit',271360,'sys::Bool',xp,{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('makeCatchVar',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('c','compiler::Catch',false)]),{}).am$('toStr',271360,'sys::Str',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{}).am$('printOpt',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false),new sys.Param('nl','sys::Bool',true)]),{}).am$('initVal',8192,'compiler::Expr?',xp,{});
IfStmt.type$.af$('condition',73728,'compiler::Expr',{}).af$('trueBlock',73728,'compiler::Block',{}).af$('falseBlock',73728,'compiler::Block?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('condition','compiler::Expr',false),new sys.Param('trueBlock','compiler::Block',false)]),{}).am$('isExit',271360,'sys::Bool',xp,{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
ReturnStmt.type$.af$('expr',73728,'compiler::Expr?',{}).af$('leaveVar',73728,'compiler::MethodVar?',{}).af$('isSynthetic',73728,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('expr','compiler::Expr?',true)]),{}).am$('makeSynthetic',40962,'compiler::ReturnStmt',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('expr','compiler::Expr?',true)]),{}).am$('isExit',271360,'sys::Bool',xp,{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('toStr',271360,'sys::Str',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
ThrowStmt.type$.af$('exception',73728,'compiler::Expr',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('exception','compiler::Expr',false)]),{}).am$('isExit',271360,'sys::Bool',xp,{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
ForStmt.type$.af$('init',73728,'compiler::Stmt?',{}).af$('condition',73728,'compiler::Expr?',{}).af$('update',73728,'compiler::Expr?',{}).af$('block',73728,'compiler::Block?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false)]),{}).am$('isExit',271360,'sys::Bool',xp,{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
WhileStmt.type$.af$('condition',73728,'compiler::Expr',{}).af$('block',73728,'compiler::Block',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('condition','compiler::Expr',false),new sys.Param('block','compiler::Block',false)]),{}).am$('isExit',271360,'sys::Bool',xp,{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
BreakStmt.type$.af$('loop',73728,'compiler::Stmt?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false)]),{}).am$('isExit',271360,'sys::Bool',xp,{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
ContinueStmt.type$.af$('loop',73728,'compiler::Stmt?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false)]),{}).am$('isExit',271360,'sys::Bool',xp,{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
TryStmt.type$.af$('exception',73728,'compiler::Expr?',{}).af$('block',73728,'compiler::Block?',{}).af$('catches',73728,'compiler::Catch[]',{}).af$('finallyBlock',73728,'compiler::Block?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false)]),{}).am$('isExit',271360,'sys::Bool',xp,{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
Catch.type$.af$('errType',73728,'compiler::TypeRef?',{}).af$('errVariable',73728,'sys::Str?',{}).af$('block',73728,'compiler::Block?',{}).af$('start',73728,'sys::Int',{}).af$('end',73728,'sys::Int',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false)]),{}).am$('isDefiniteAssign',8192,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
SwitchStmt.type$.af$('condition',73728,'compiler::Expr',{}).af$('cases',73728,'compiler::Case[]',{}).af$('defaultBlock',73728,'compiler::Block?',{}).af$('isTableswitch',73728,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('condition','compiler::Expr',false)]),{}).am$('isDefiniteAssign',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','|compiler::Expr->sys::Bool|',false)]),{}).am$('isExit',271360,'sys::Bool',xp,{}).am$('walkChildren',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
Case.type$.af$('cases',73728,'compiler::Expr[]',{}).af$('block',73728,'compiler::Block?',{}).af$('startOffset',73728,'sys::Int',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false)]),{}).am$('walk',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
StmtId.type$.af$('nop',106506,'compiler::StmtId',{}).af$('expr',106506,'compiler::StmtId',{}).af$('localDef',106506,'compiler::StmtId',{}).af$('ifStmt',106506,'compiler::StmtId',{}).af$('returnStmt',106506,'compiler::StmtId',{}).af$('throwStmt',106506,'compiler::StmtId',{}).af$('forStmt',106506,'compiler::StmtId',{}).af$('whileStmt',106506,'compiler::StmtId',{}).af$('breakStmt',106506,'compiler::StmtId',{}).af$('continueStmt',106506,'compiler::StmtId',{}).af$('tryStmt',106506,'compiler::StmtId',{}).af$('switchStmt',106506,'compiler::StmtId',{}).af$('vals',106498,'compiler::StmtId[]',{}).am$('make',133124,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('$ordinal','sys::Int',false),new sys.Param('$name','sys::Str',false)]),{}).am$('fromStr',40966,'compiler::StmtId?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('checked','sys::Bool',true)]),{}).am$('static$init',165890,'sys::Void',xp,{});
CType.type$.am$('pod',270337,'compiler::CPod',xp,{}).am$('name',270337,'sys::Str',xp,{}).am$('qname',270337,'sys::Str',xp,{}).am$('signature',270337,'sys::Str',xp,{}).am$('toStr',9216,'sys::Str',xp,{}).am$('deref',270336,'compiler::CType',xp,{}).am$('isVal',270337,'sys::Bool',xp,{}).am$('isValType',40962,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('isNullable',270337,'sys::Bool',xp,{}).am$('toNullable',270337,'compiler::CType',xp,{}).am$('toNonNullable',270336,'compiler::CType',xp,{}).am$('isForeign',270336,'sys::Bool',xp,{}).am$('foreignInheritance',8192,'compiler::CType?',xp,{}).am$('bridge',8192,'compiler::CBridge?',xp,{}).am$('inferredAs',270336,'compiler::CType',xp,{}).am$('isSupported',270336,'sys::Bool',xp,{}).am$('isGeneric',270337,'sys::Bool',xp,{}).am$('isParameterized',270337,'sys::Bool',xp,{}).am$('isGenericParameter',270337,'sys::Bool',xp,{}).am$('toListOf',270337,'compiler::CType',xp,{}).am$('raw',8192,'compiler::CType',xp,{}).am$('parameterizeThis',270336,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('thisType','compiler::CType',false)]),{}).am$('base',270337,'compiler::CType?',xp,{}).am$('mixins',270337,'compiler::CType[]',xp,{}).am$('hash',271360,'sys::Int',xp,{}).am$('equals',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('t','sys::Obj?',false)]),{}).am$('fits',270336,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::CType',false)]),{}).am$('fitsAny',8192,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('types','compiler::CType[]',false)]),{}).am$('common',40962,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::CNamespace',false),new sys.Param('types','compiler::CType[]',false)]),{}).am$('commonClass',34818,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::CNamespace',false),new sys.Param('types','compiler::CType[]',false)]),{}).am$('commonMixin',34818,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::CNamespace',false),new sys.Param('types','compiler::CType[]',false)]),{}).am$('commonParameterized',34818,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::CNamespace',false),new sys.Param('types','compiler::CType[]',false)]),{}).am$('commonList',34818,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::CNamespace',false),new sys.Param('types','compiler::ListType[]',false)]),{}).am$('commonExact',34818,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::CNamespace',false),new sys.Param('types','compiler::CType[]',false),new sys.Param('fallback','compiler::CType',false)]),{}).am$('flags',270337,'sys::Int',xp,{}).am$('isClass',8192,'sys::Bool',xp,{}).am$('isMixin',8192,'sys::Bool',xp,{}).am$('isEnum',8192,'sys::Bool',xp,{}).am$('isFacet',8192,'sys::Bool',xp,{}).am$('isAbstract',8192,'sys::Bool',xp,{}).am$('isConst',8192,'sys::Bool',xp,{}).am$('isFinal',8192,'sys::Bool',xp,{}).am$('isPublic',8192,'sys::Bool',xp,{}).am$('isInternal',8192,'sys::Bool',xp,{}).am$('isSynthetic',8192,'sys::Bool',xp,{}).am$('isNative',8192,'sys::Bool',xp,{}).am$('isObj',8192,'sys::Bool',xp,{}).am$('isBool',8192,'sys::Bool',xp,{}).am$('isInt',8192,'sys::Bool',xp,{}).am$('isFloat',8192,'sys::Bool',xp,{}).am$('isDecimal',8192,'sys::Bool',xp,{}).am$('isRange',8192,'sys::Bool',xp,{}).am$('isStr',8192,'sys::Bool',xp,{}).am$('isNum',8192,'sys::Bool',xp,{}).am$('isThis',8192,'sys::Bool',xp,{}).am$('isType',8192,'sys::Bool',xp,{}).am$('isVoid',8192,'sys::Bool',xp,{}).am$('isBuf',8192,'sys::Bool',xp,{}).am$('isList',8192,'sys::Bool',xp,{}).am$('isMap',8192,'sys::Bool',xp,{}).am$('isFunc',8192,'sys::Bool',xp,{}).am$('isNothing',8192,'sys::Bool',xp,{}).am$('isValid',270336,'sys::Bool',xp,{}).am$('isConstFieldType',8192,'sys::Bool',xp,{}).am$('slots',270337,'[sys::Str:compiler::CSlot]',xp,{}).am$('hasSlot',8192,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('slot',270336,'compiler::CSlot?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('field',270336,'compiler::CField?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('method',270336,'compiler::CMethod?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('fields',8192,'compiler::CField[]',xp,{}).am$('methods',8192,'compiler::CMethod[]',xp,{}).am$('ctors',8192,'compiler::CMethod[]',xp,{}).am$('instanceCtors',8192,'compiler::CMethod[]',xp,{}).am$('operators',270337,'compiler::COperators',xp,{});
TypeDef.type$.af$('ns',336896,'compiler::CNamespace',{}).af$('unit',73728,'compiler::CompilationUnit',{}).af$('podDef',73728,'compiler::PodDef',{}).af$('pod',336896,'compiler::CPod',{}).af$('name',336898,'sys::Str',{}).af$('qname',336898,'sys::Str',{}).af$('isVal',336898,'sys::Bool',{}).af$('baseSpecified',73728,'sys::Bool',{}).af$('base',336896,'compiler::CType?',{}).af$('mixins',336896,'compiler::CType[]',{}).af$('enumDefs',73728,'compiler::EnumDef[]',{}).af$('closures',73728,'compiler::ClosureExpr[]',{}).af$('closure',73728,'compiler::ClosureExpr?',{}).af$('slotMap',67584,'[sys::Str:compiler::CSlot]',{}).af$('slotDefMap',67584,'[sys::Str:compiler::SlotDef]',{}).af$('slotDefList',67584,'compiler::SlotDef[]',{}).af$('indexedFacets',73728,'compiler::FacetDef[]?',{}).af$('toNullable$Store',722944,'sys::Obj?',{}).af$('toListOf$Store',722944,'sys::Obj?',{}).af$('hasNativePeer$Store',722944,'sys::Obj?',{}).af$('hasItBlockCtor$Store',722944,'sys::Obj?',{}).af$('operators$Store',722944,'sys::Obj?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::CNamespace',false),new sys.Param('loc','compiler::Loc',false),new sys.Param('unit','compiler::CompilationUnit',false),new sys.Param('name','sys::Str',false),new sys.Param('flags','sys::Int',true)]),{}).am$('signature',271360,'sys::Str',xp,{}).am$('isNullable',271360,'sys::Bool',xp,{}).am$('toNullable',795648,'compiler::CType',xp,{}).am$('isGeneric',271360,'sys::Bool',xp,{}).am$('isParameterized',271360,'sys::Bool',xp,{}).am$('isGenericParameter',271360,'sys::Bool',xp,{}).am$('toListOf',795648,'compiler::CType',xp,{}).am$('isClosure',8192,'sys::Bool',xp,{}).am$('hasNativePeer',532480,'sys::Bool',xp,{}).am$('hasItBlockCtor',532480,'sys::Bool',xp,{}).am$('slots',271360,'[sys::Str:compiler::CSlot]',xp,{}).am$('operators',795648,'compiler::COperators',xp,{}).am$('addSlot',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('s','compiler::CSlot',false),new sys.Param('slotDefIndex','sys::Int?',true)]),{}).am$('replaceSlot',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('oldSlot','compiler::CSlot',false),new sys.Param('newSlot','compiler::CSlot',false)]),{}).am$('staticInit',8192,'compiler::MethodDef?',xp,{}).am$('normalizeStaticInits',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false)]),{}).am$('hasSlotDef',8192,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('slotDef',8192,'compiler::SlotDef?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('fieldDef',8192,'compiler::FieldDef?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('methodDef',8192,'compiler::MethodDef?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('slotDefs',8192,'compiler::SlotDef[]',xp,{}).am$('fieldDefs',8192,'compiler::FieldDef[]',xp,{}).am$('staticFieldDefs',8192,'compiler::FieldDef[]',xp,{}).am$('instanceFieldDefs',8192,'compiler::FieldDef[]',xp,{}).am$('methodDefs',8192,'compiler::MethodDef[]',xp,{}).am$('ctorDefs',8192,'compiler::MethodDef[]',xp,{}).am$('enumDef',8192,'compiler::EnumDef?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('walk',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{}).am$('toNullable$Once',133120,'compiler::CType',xp,{}).am$('toListOf$Once',133120,'compiler::CType',xp,{}).am$('hasNativePeer$Once',133120,'sys::Bool',xp,{}).am$('hasItBlockCtor$Once',133120,'sys::Bool',xp,{}).am$('operators$Once',133120,'compiler::COperators',xp,{});
Using.type$.af$('podName',73728,'sys::Str',{}).af$('typeName',73728,'sys::Str?',{}).af$('asName',73728,'sys::Str?',{}).af$('resolvedPod',73728,'compiler::CPod?',{}).af$('resolvedType',73728,'compiler::CType?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false)]),{}).am$('isPod',8192,'sys::Bool',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{}).am$('toStr',271360,'sys::Str',xp,{});
CSlot.type$.am$('ns',271360,'compiler::CNamespace',xp,{}).am$('parent',270337,'compiler::CType',xp,{}).am$('name',270337,'sys::Str',xp,{}).am$('qname',270337,'sys::Str',xp,{}).am$('signature',270337,'sys::Str',xp,{}).am$('flags',270337,'sys::Int',xp,{}).am$('toStr',9216,'sys::Str',xp,{}).am$('isAbstract',8192,'sys::Bool',xp,{}).am$('isAccessor',8192,'sys::Bool',xp,{}).am$('isConst',8192,'sys::Bool',xp,{}).am$('isCtor',8192,'sys::Bool',xp,{}).am$('isEnum',8192,'sys::Bool',xp,{}).am$('isGetter',8192,'sys::Bool',xp,{}).am$('isInternal',8192,'sys::Bool',xp,{}).am$('isNative',8192,'sys::Bool',xp,{}).am$('isOverride',8192,'sys::Bool',xp,{}).am$('isPrivate',8192,'sys::Bool',xp,{}).am$('isProtected',8192,'sys::Bool',xp,{}).am$('isPublic',8192,'sys::Bool',xp,{}).am$('isSetter',8192,'sys::Bool',xp,{}).am$('isStatic',8192,'sys::Bool',xp,{}).am$('isStorage',8192,'sys::Bool',xp,{}).am$('isSynthetic',8192,'sys::Bool',xp,{}).am$('isVirtual',8192,'sys::Bool',xp,{}).am$('isOnce',8192,'sys::Bool',xp,{}).am$('isInstanceCtor',8192,'sys::Bool',xp,{}).am$('isStaticCtor',8192,'sys::Bool',xp,{}).am$('isForeign',270336,'sys::Bool',xp,{}).am$('usesForeign',8192,'sys::Bool',xp,{}).am$('bridge',270336,'compiler::CBridge?',xp,{}).am$('usesBridge',270337,'compiler::CBridge?',xp,{}).am$('isVisibleTo',8192,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('curType','compiler::CType',false)]),{});
SlotDef.type$.af$('parentDef',73728,'compiler::TypeDef',{}).af$('name',336896,'sys::Str',{}).af$('overridden',73728,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('parentDef','compiler::TypeDef',false)]),{}).am$('ns',271360,'compiler::CNamespace',xp,{}).am$('parent',271360,'compiler::CType',xp,{}).am$('qname',271360,'sys::Str',xp,{}).am$('walk',270337,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{});
VisitDepth.type$.af$('typeDef',106506,'compiler::VisitDepth',{}).af$('slotDef',106506,'compiler::VisitDepth',{}).af$('stmt',106506,'compiler::VisitDepth',{}).af$('expr',106506,'compiler::VisitDepth',{}).af$('vals',106498,'compiler::VisitDepth[]',{}).am$('make',133124,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('$ordinal','sys::Int',false),new sys.Param('$name','sys::Str',false)]),{}).am$('fromStr',40966,'compiler::VisitDepth?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('checked','sys::Bool',true)]),{}).am$('static$init',165890,'sys::Void',xp,{});
ExprVisitor.type$.af$('func',73728,'|compiler::Expr->compiler::Expr|',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('func','|compiler::Expr->compiler::Expr|',false)]),{}).am$('visitExpr',271360,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false)]),{});
CField.type$.am$('fieldType',8192,'compiler::CType',xp,{'sys::Deprecated':""}).am$('type',270337,'compiler::CType',xp,{}).am$('getter',270337,'compiler::CMethod?',xp,{}).am$('setter',270337,'compiler::CMethod?',xp,{}).am$('inheritedReturns',270337,'compiler::CType',xp,{}).am$('isCovariant',8192,'sys::Bool',xp,{}).am$('isGeneric',8192,'sys::Bool',xp,{}).am$('isParameterized',270336,'sys::Bool',xp,{}).am$('usesBridge',271360,'compiler::CBridge?',xp,{});
FieldDef.type$.af$('type',336896,'compiler::CType',{}).af$('field',73728,'sys::Field?',{}).af$('init',73728,'compiler::Expr?',{}).af$('walkInit',73728,'sys::Bool',{}).af$('get',73728,'compiler::MethodDef?',{}).af$('set',73728,'compiler::MethodDef?',{}).af$('concreteBase',73728,'compiler::CField?',{}).af$('inheritedRet',73728,'compiler::CType?',{}).af$('requiresNullCheck',73728,'sys::Bool',{}).af$('enumDef',73728,'compiler::EnumDef?',{}).af$('closureInfo',73728,'sys::Str?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('parent','compiler::TypeDef',false),new sys.Param('name','sys::Str',true),new sys.Param('flags','sys::Int',true)]),{}).am$('hasGet',8192,'sys::Bool',xp,{}).am$('hasSet',8192,'sys::Bool',xp,{}).am$('makeAccessorExpr',8192,'compiler::FieldExpr',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('useAccessor','sys::Bool',false)]),{}).am$('signature',271360,'sys::Str',xp,{}).am$('getter',271360,'compiler::CMethod?',xp,{}).am$('setter',271360,'compiler::CMethod?',xp,{}).am$('inheritedReturns',271360,'compiler::CType',xp,{}).am$('walk',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
CPod.type$.am$('ns',270337,'compiler::CNamespace',xp,{}).am$('name',270337,'sys::Str',xp,{}).am$('version',270337,'sys::Version',xp,{}).am$('depends',270337,'compiler::CDepend[]',xp,{}).am$('types',270337,'compiler::CType[]',xp,{}).am$('file',270337,'sys::File',xp,{}).am$('meta',270337,'[sys::Str:sys::Str]',xp,{}).am$('hasJs',8192,'sys::Bool',xp,{}).am$('resolveType',270337,'compiler::CType?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('checked','sys::Bool',false)]),{}).am$('isForeign',270336,'sys::Bool',xp,{}).am$('bridge',270336,'compiler::CBridge?',xp,{}).am$('hash',271360,'sys::Int',xp,{}).am$('equals',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('t','sys::Obj?',false)]),{}).am$('toStr',9216,'sys::Str',xp,{});
PodDef.type$.af$('ns',336896,'compiler::CNamespace',{}).af$('name',336898,'sys::Str',{}).af$('meta',336896,'[sys::Str:sys::Str]',{}).af$('index',73728,'[sys::Str:sys::Obj]',{}).af$('units',73728,'compiler::CompilationUnit[]',{}).af$('typeDefs',73728,'[sys::Str:compiler::TypeDef]?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::CNamespace',false),new sys.Param('loc','compiler::Loc',false),new sys.Param('name','sys::Str',false)]),{}).am$('version',271360,'sys::Version',xp,{}).am$('depends',271360,'compiler::CDepend[]',xp,{}).am$('file',271360,'sys::File',xp,{}).am$('resolveType',271360,'compiler::CType?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('checked','sys::Bool',false)]),{}).am$('types',271360,'compiler::CType[]',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
CParam.type$.am$('name',270337,'sys::Str',xp,{}).am$('type',270337,'compiler::CType',xp,{}).am$('hasDefault',270337,'sys::Bool',xp,{}).am$('paramType',8192,'compiler::CType',xp,{'sys::Deprecated':""});
ParamDef.type$.af$('type',336896,'compiler::CType',{}).af$('name',336896,'sys::Str',{}).af$('def',73728,'compiler::Expr?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('type','compiler::CType',false),new sys.Param('name','sys::Str',false),new sys.Param('def','compiler::Expr?',true)]),{}).am$('hasDefault',271360,'sys::Bool',xp,{}).am$('isAssign',8192,'sys::Bool',xp,{}).am$('toStr',271360,'sys::Str',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
CMethod.type$.am$('returns',270337,'compiler::CType',xp,{}).am$('returnType',8192,'compiler::CType',xp,{'sys::Deprecated':""}).am$('params',270337,'compiler::CParam[]',xp,{}).am$('inheritedReturns',270337,'compiler::CType',xp,{}).am$('inheritedReturnType',8192,'compiler::CType',xp,{'sys::Deprecated':""}).am$('isCovariant',8192,'sys::Bool',xp,{}).am$('usesBridge',271360,'compiler::CBridge?',xp,{}).am$('isGeneric',270336,'sys::Bool',xp,{}).am$('isParameterized',270336,'sys::Bool',xp,{}).am$('generic',270336,'compiler::CMethod?',xp,{}).am$('calcGeneric',40962,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::CMethod',false)]),{}).am$('nameAndParamTypesToStr',8192,'sys::Str',xp,{}).am$('hasSameParams',8192,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('that','compiler::CMethod',false)]),{});
MethodDef.type$.af$('transpileTempCount',67584,'sys::Int',{}).af$('returns',336896,'compiler::CType',{}).af$('ret',8192,'compiler::CType',{'sys::Deprecated':""}).af$('inheritedRet',73728,'compiler::CType?',{}).af$('paramDefs',73728,'compiler::ParamDef[]',{}).af$('code',73728,'compiler::Block?',{}).af$('ctorChain',73728,'compiler::CallExpr?',{}).af$('vars',73728,'compiler::MethodVar[]',{}).af$('accessorFor',73728,'compiler::FieldDef?',{}).af$('usesCvars',73728,'sys::Bool',{}).am$('makeStaticInit',40962,'compiler::MethodDef',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('parent','compiler::TypeDef',false),new sys.Param('block','compiler::Block?',false)]),{}).am$('makeInstanceInit',40962,'compiler::MethodDef',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('parent','compiler::TypeDef',false),new sys.Param('block','compiler::Block?',false)]),{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('parent','compiler::TypeDef',false),new sys.Param('name','sys::Str',true),new sys.Param('flags','sys::Int',true)]),{}).am$('isStaticInit',8192,'sys::Bool',xp,{}).am$('isNameStaticInit',40962,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('isInstanceInit',8192,'sys::Bool',xp,{}).am$('isNameInstanceInit',40962,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('isFieldAccessor',8192,'sys::Bool',xp,{}).am$('isFieldSetter',8192,'sys::Bool',xp,{}).am$('isItBlockCtor',8192,'sys::Bool',xp,{}).am$('addLocalVarForDef',8192,'compiler::MethodVar',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::LocalDefStmt',false),new sys.Param('scope','compiler::Block?',false)]),{}).am$('addLocalVar',8192,'compiler::MethodVar',sys.List.make(sys.Param.type$,[new sys.Param('ctype','compiler::CType',false),new sys.Param('name','sys::Str?',false),new sys.Param('scope','compiler::Block?',false)]),{}).am$('getOrAddShadowVar',8192,'compiler::MethodVar',sys.List.make(sys.Param.type$,[new sys.Param('binding','compiler::MethodVar',false),new sys.Param('scope','compiler::Block?',false)]),{}).am$('addParamVar',8192,'compiler::MethodVar',sys.List.make(sys.Param.type$,[new sys.Param('ctype','compiler::CType',false),new sys.Param('name','sys::Str',false)]),{}).am$('transpileTempVar',8192,'sys::Str',xp,{}).am$('signature',271360,'sys::Str',xp,{}).am$('inheritedReturns',271360,'compiler::CType',xp,{}).am$('params',271360,'compiler::CParam[]',xp,{}).am$('walk',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::Visitor',false),new sys.Param('depth','compiler::VisitDepth',false)]),{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
TypeRef.type$.af$('t',73728,'compiler::CType',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('t','compiler::CType',false)]),{}).am$('ns',271360,'compiler::CNamespace',xp,{}).am$('doc',271360,'compiler::CDoc?',xp,{}).am$('pod',271360,'compiler::CPod',xp,{}).am$('name',271360,'sys::Str',xp,{}).am$('qname',271360,'sys::Str',xp,{}).am$('signature',271360,'sys::Str',xp,{}).am$('deref',271360,'compiler::CType',xp,{}).am$('isForeign',271360,'sys::Bool',xp,{}).am$('flags',271360,'sys::Int',xp,{}).am$('facet',271360,'compiler::CFacet?',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('base',271360,'compiler::CType?',xp,{}).am$('mixins',271360,'compiler::CType[]',xp,{}).am$('fits',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('that','compiler::CType',false)]),{}).am$('isValid',271360,'sys::Bool',xp,{}).am$('isVal',271360,'sys::Bool',xp,{}).am$('isNullable',271360,'sys::Bool',xp,{}).am$('toNullable',271360,'compiler::CType',xp,{}).am$('toNonNullable',271360,'compiler::CType',xp,{}).am$('inferredAs',271360,'compiler::CType',xp,{}).am$('isGeneric',271360,'sys::Bool',xp,{}).am$('isParameterized',271360,'sys::Bool',xp,{}).am$('isGenericParameter',271360,'sys::Bool',xp,{}).am$('parameterizeThis',271360,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('thisType','compiler::CType',false)]),{}).am$('toListOf',271360,'compiler::CType',xp,{}).am$('slot',271360,'compiler::CSlot?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('field',271360,'compiler::CField?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('method',271360,'compiler::CMethod?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('slots',271360,'[sys::Str:compiler::CSlot]',xp,{}).am$('operators',271360,'compiler::COperators',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
AstWriter.type$.af$('out',73728,'sys::OutStream',{}).af$('indentation',73728,'sys::Int',{}).af$('needIndent',73728,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',true)]),{}).am$('w',8192,'compiler::AstWriter',sys.List.make(sys.Param.type$,[new sys.Param('o','sys::Obj',false)]),{}).am$('nl',8192,'compiler::AstWriter',xp,{}).am$('indent',8192,'compiler::AstWriter',xp,{}).am$('unindent',8192,'compiler::AstWriter',xp,{}).am$('flags',8192,'compiler::AstWriter',sys.List.make(sys.Param.type$,[new sys.Param('flags','sys::Int',false)]),{}).am$('flagsToStr',40962,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('flags','sys::Int',false)]),{});
MethodVar.type$.af$('method',73728,'compiler::MethodDef',{}).af$('register',73728,'sys::Int',{}).af$('ctype',73728,'compiler::CType',{}).af$('name',73728,'sys::Str',{}).af$('flags',73728,'sys::Int',{}).af$('isCatchVar',73728,'sys::Bool',{}).af$('scope',73728,'compiler::Block?',{}).af$('paramDef',73728,'compiler::ParamDef?',{}).af$('usedInClosure',73728,'sys::Bool',{}).af$('shadows',73728,'compiler::MethodVar?',{}).af$('wrapField',73728,'compiler::CField?',{}).af$('isReassigned',73728,'sys::Bool',{}).af$('paramWrapper',73728,'compiler::MethodVar?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('method','compiler::MethodDef',false),new sys.Param('register','sys::Int',false),new sys.Param('ctype','compiler::CType',false),new sys.Param('name','sys::Str',false),new sys.Param('flags','sys::Int',true),new sys.Param('scope','compiler::Block?',true)]),{}).am$('makeForParam',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('method','compiler::MethodDef',false),new sys.Param('register','sys::Int',false),new sys.Param('p','compiler::ParamDef',false),new sys.Param('paramType','compiler::CType',false)]),{}).am$('isParam',8192,'sys::Bool',xp,{}).am$('isWrapped',8192,'sys::Bool',xp,{}).am$('toStr',271360,'sys::Str',xp,{}).am$('reassigned',8192,'sys::Void',xp,{});
EnumDef.type$.af$('doc',73728,'compiler::DocDef?',{}).af$('facets',73728,'compiler::FacetDef[]?',{}).af$('ordinal',73728,'sys::Int',{}).af$('name',73728,'sys::Str',{}).af$('ctorArgs',73728,'compiler::Expr[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('doc','compiler::DocDef?',false),new sys.Param('facets','compiler::FacetDef[]?',false),new sys.Param('name','sys::Str',false),new sys.Param('ordinal','sys::Int',false)]),{}).am$('toStr',271360,'sys::Str',xp,{}).am$('print',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','compiler::AstWriter',false)]),{});
DslPlugin.type$.am$('find',40962,'compiler::DslPlugin?',sys.List.make(sys.Param.type$,[new sys.Param('c','compiler::CompilerSupport',false),new sys.Param('loc','compiler::Loc',false),new sys.Param('anchorType','compiler::CType',false)]),{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('c','compiler::Compiler',false)]),{}).am$('compile',270337,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('dsl','compiler::DslExpr',false)]),{}).am$('normalizeSrc',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('dsl','compiler::DslExpr',false)]),{});
RegexDslPlugin.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('c','compiler::Compiler',false)]),{}).am$('compile',271360,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('dsl','compiler::DslExpr',false)]),{});
StrDslPlugin.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('c','compiler::Compiler',false)]),{}).am$('compile',271360,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('dsl','compiler::DslExpr',false)]),{});
FUtil.type$.am$('writeBuf',40962,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream?',false),new sys.Param('buf','sys::Buf?',false)]),{}).am$('readBuf',40962,'sys::Buf?',sys.List.make(sys.Param.type$,[new sys.Param('in','sys::InStream',false)]),{}).am$('writeAttrs',40962,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',false),new sys.Param('fattrs','compiler::FAttr[]?',false)]),{}).am$('readAttrs',40962,'compiler::FAttr[]',sys.List.make(sys.Param.type$,[new sys.Param('in','sys::InStream',false)]),{}).am$('writeDoc',40962,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',false),new sys.Param('key','sys::Str',false),new sys.Param('doc','sys::Str?',false)]),{}).am$('make',139268,'sys::Void',xp,{});
FPod.type$.af$('ns',336896,'compiler::CNamespace',{}).af$('name',336896,'sys::Str',{}).af$('version',336896,'sys::Version',{}).af$('depends',336896,'compiler::CDepend[]',{}).af$('meta',336896,'[sys::Str:sys::Str]',{}).af$('index',73728,'[sys::Str:sys::Obj]',{}).af$('zip',73728,'sys::Zip?',{}).af$('ftypes',73728,'compiler::FType[]?',{}).af$('names',73728,'compiler::FTable',{}).af$('typeRefs',73728,'compiler::FTable',{}).af$('fieldRefs',73728,'compiler::FTable',{}).af$('methodRefs',73728,'compiler::FTable',{}).af$('ints',73728,'compiler::FTable',{}).af$('floats',73728,'compiler::FTable',{}).af$('decimals',73728,'compiler::FTable',{}).af$('strs',73728,'compiler::FTable',{}).af$('durations',73728,'compiler::FTable',{}).af$('uris',73728,'compiler::FTable',{}).af$('ftypesByName',73728,'[sys::Str:compiler::FType]?',{}).af$('onFile',73728,'|sys::Uri->sys::OutStream|?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::CNamespace',false),new sys.Param('podName','sys::Str',false),new sys.Param('zip','sys::Zip?',false)]),{}).am$('file',271360,'sys::File',xp,{}).am$('resolveType',271360,'compiler::CType?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('checked','sys::Bool',false)]),{}).am$('types',271360,'compiler::CType[]',xp,{}).am$('toType',8192,'compiler::CType?',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('resolveTypes',8192,'compiler::CType[]',sys.List.make(sys.Param.type$,[new sys.Param('indexes','sys::Int[]',false)]),{}).am$('n',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('typeRef',8192,'compiler::FTypeRef',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('fieldRef',8192,'compiler::FFieldRef',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('methodRef',8192,'compiler::FMethodRef',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('integer',8192,'sys::Int',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('float',8192,'sys::Float',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('decimal',8192,'sys::Decimal',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('str',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('duration',8192,'sys::Duration',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('uri',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('typeRefStr',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('fieldRefStr',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('methodRefStr',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('addName',8192,'sys::Int',sys.List.make(sys.Param.type$,[new sys.Param('val','sys::Str',false)]),{}).am$('addTypeRef',8192,'sys::Int',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::CType',false)]),{}).am$('addFieldRef',8192,'sys::Int',sys.List.make(sys.Param.type$,[new sys.Param('field','compiler::CField',false)]),{}).am$('addMethodRef',8192,'sys::Int',sys.List.make(sys.Param.type$,[new sys.Param('method','compiler::CMethod',false),new sys.Param('argCount','sys::Int?',true)]),{}).am$('dump',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',true)]),{}).am$('read',8192,'sys::Void',xp,{}).am$('readFully',8192,'sys::Void',xp,{}).am$('write',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('zip','sys::Zip',true)]),{}).am$('doWrite',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('onFile','|sys::Uri->sys::OutStream|',false)]),{}).am$('writeMeta',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('uri','sys::Uri',false)]),{}).am$('writeIndex',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('uri','sys::Uri',false)]),{}).am$('prop',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',false),new sys.Param('n','sys::Str',false),new sys.Param('v','sys::Str',false)]),{}).am$('in',8192,'sys::InStream?',sys.List.make(sys.Param.type$,[new sys.Param('uri','sys::Uri',false)]),{}).am$('out',8192,'sys::OutStream',sys.List.make(sys.Param.type$,[new sys.Param('uri','sys::Uri',false)]),{});
FTypeRef.type$.af$('podName',73730,'sys::Int',{}).af$('typeName',73730,'sys::Int',{}).af$('sig',73730,'sys::Str',{}).af$('hashcode',73730,'sys::Int',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('podName','sys::Int',false),new sys.Param('typeName','sys::Int',false),new sys.Param('sig','sys::Str',false)]),{}).am$('hash',271360,'sys::Int',xp,{}).am$('equals',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('obj','sys::Obj?',false)]),{}).am$('isGenericInstance',8192,'sys::Bool',xp,{}).am$('signature',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false)]),{}).am$('format',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false)]),{}).am$('write',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',false)]),{}).am$('read',40962,'compiler::FTypeRef',sys.List.make(sys.Param.type$,[new sys.Param('in','sys::InStream',false)]),{});
FFieldRef.type$.af$('parent',73730,'sys::Int',{}).af$('name',73730,'sys::Int',{}).af$('typeRef',73730,'sys::Int',{}).af$('hashcode',73730,'sys::Int',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('parent','sys::Int',false),new sys.Param('name','sys::Int',false),new sys.Param('typeRef','sys::Int',false)]),{}).am$('hash',271360,'sys::Int',xp,{}).am$('equals',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('obj','sys::Obj?',false)]),{}).am$('format',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false)]),{}).am$('write',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',false)]),{}).am$('read',40962,'compiler::FFieldRef',sys.List.make(sys.Param.type$,[new sys.Param('in','sys::InStream',false)]),{});
FMethodRef.type$.af$('parent',73730,'sys::Int',{}).af$('name',73730,'sys::Int',{}).af$('ret',73730,'sys::Int',{}).af$('params',73730,'sys::Int[]',{}).af$('hashcode',73730,'sys::Int',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('parent','sys::Int',false),new sys.Param('name','sys::Int',false),new sys.Param('ret','sys::Int',false),new sys.Param('params','sys::Int[]',false)]),{}).am$('hash',271360,'sys::Int',xp,{}).am$('equals',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('obj','sys::Obj?',false)]),{}).am$('format',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false)]),{}).am$('write',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',false)]),{}).am$('read',40962,'compiler::FMethodRef',sys.List.make(sys.Param.type$,[new sys.Param('in','sys::InStream',false)]),{});
FSlot.type$.af$('fparent',73728,'compiler::FType',{}).af$('flags',336896,'sys::Int',{}).af$('nameIndex',73728,'sys::Int',{}).af$('fattrs',73728,'compiler::FAttr[]?',{}).af$('ffacets',73728,'compiler::FFacet[]?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('fparent','compiler::FType',false)]),{}).am$('pod',8192,'compiler::FPod',xp,{}).am$('parent',271360,'compiler::CType',xp,{}).am$('name',271360,'sys::Str',xp,{}).am$('qname',271360,'sys::Str',xp,{}).am$('doc',271360,'compiler::CDoc?',xp,{}).am$('attr',8192,'compiler::FAttr?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('facet',271360,'compiler::CFacet?',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('writeCommon',4096,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',false)]),{}).am$('readCommon',4096,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('in','sys::InStream',false)]),{});
FField.type$.af$('typeRef',73728,'sys::Int',{}).af$('getter',336896,'compiler::CMethod?',{}).af$('setter',336896,'compiler::CMethod?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('fparent','compiler::FType',false)]),{}).am$('write',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',false)]),{}).am$('read',8192,'sys::This',sys.List.make(sys.Param.type$,[new sys.Param('in','sys::InStream',false)]),{}).am$('signature',271360,'sys::Str',xp,{}).am$('type',271360,'compiler::CType',xp,{}).am$('inheritedReturns',271360,'compiler::CType',xp,{});
FPrinter.type$.af$('pod',73728,'compiler::FPod',{}).af$('out',73728,'sys::OutStream',{}).af$('showIndex',73728,'sys::Bool',{}).af$('showCode',73728,'sys::Bool',{}).af$('showLines',73728,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false),new sys.Param('out','sys::OutStream',true)]),{}).am$('all',8192,'sys::Void',xp,{}).am$('tables',8192,'sys::Void',xp,{}).am$('table',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('title','sys::Str',false),new sys.Param('table','compiler::FTable',false)]),{}).am$('ftypes',8192,'sys::Void',xp,{}).am$('ftype',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::FType',false)]),{}).am$('slot',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('s','compiler::FSlot',false)]),{}).am$('field',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FField',false)]),{}).am$('method',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::FMethod',false)]),{}).am$('code',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('code','sys::Buf',false)]),{}).am$('attrs',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('attrs','compiler::FAttr[]?',false)]),{}).am$('attr',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('attr','compiler::FAttr',false)]),{}).am$('sourceFileAttr',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('attr','compiler::FAttr',false)]),{}).am$('lineNumberAttr',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('attr','compiler::FAttr',false)]),{}).am$('facetsAttr',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('attr','compiler::FAttr',false)]),{}).am$('enumOrdinalAttr',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('attr','compiler::FAttr',false)]),{}).am$('errTableAttr',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('attr','compiler::FAttr',false)]),{}).am$('lineNumbersAttr',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('attr','compiler::FAttr',false)]),{}).am$('typeRef',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('i','sys::Int',false)]),{}).am$('name',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('i','sys::Int',false)]),{}).am$('flags',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('flags','sys::Int',false)]),{}).am$('index',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('print',8192,'compiler::FPrinter',sys.List.make(sys.Param.type$,[new sys.Param('obj','sys::Obj',false)]),{}).am$('printLine',8192,'compiler::FPrinter',sys.List.make(sys.Param.type$,[new sys.Param('obj','sys::Obj',true)]),{});
FMethod.type$.af$('ret',73728,'sys::Int',{}).af$('inheritedRet',73728,'sys::Int',{}).af$('vars',73728,'compiler::FMethodVar[]?',{}).af$('paramCount',73728,'sys::Int',{}).af$('localCount',73728,'sys::Int',{}).af$('code',73728,'sys::Buf?',{}).af$('maxStack',73728,'sys::Int',{}).af$('isGeneric$Store',722944,'sys::Obj?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('fparent','compiler::FType',false)]),{}).am$('fparams',8192,'compiler::FMethodVar[]',xp,{}).am$('returns',271360,'compiler::CType',xp,{}).am$('params',271360,'compiler::CParam[]',xp,{}).am$('signature',271360,'sys::Str',xp,{}).am$('isGeneric',795648,'sys::Bool',xp,{}).am$('inheritedReturns',271360,'compiler::CType',xp,{}).am$('write',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',false)]),{}).am$('read',8192,'sys::This',sys.List.make(sys.Param.type$,[new sys.Param('in','sys::InStream',false)]),{}).am$('dump',8192,'sys::Void',xp,{}).am$('isGeneric$Once',133120,'sys::Bool',xp,{});
FMethodVar.type$.af$('fmethod',73728,'compiler::FMethod',{}).af$('nameIndex',73728,'sys::Int',{}).af$('typeRef',73728,'sys::Int',{}).af$('flags',73728,'sys::Int',{}).af$('defNameIndex',73728,'sys::Int',{}).af$('def',73728,'sys::Buf?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('fmethod','compiler::FMethod',false)]),{}).am$('name',271360,'sys::Str',xp,{}).am$('type',271360,'compiler::CType',xp,{}).am$('hasDefault',271360,'sys::Bool',xp,{}).am$('toStr',271360,'sys::Str',xp,{}).am$('isParam',8192,'sys::Bool',xp,{}).am$('write',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',false)]),{}).am$('read',8192,'compiler::FMethodVar',sys.List.make(sys.Param.type$,[new sys.Param('in','sys::InStream',false)]),{}).am$('fpod',8192,'compiler::FPod',xp,{});
FAttr.type$.af$('name',73728,'sys::Int',{}).af$('data',73728,'sys::Buf?',{}).am$('utf',8192,'sys::Str',xp,{}).am$('u2',8192,'sys::Int',xp,{}).am$('write',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',false)]),{}).am$('read',8192,'compiler::FAttr',sys.List.make(sys.Param.type$,[new sys.Param('in','sys::InStream',false)]),{}).am$('make',139268,'sys::Void',xp,{});
FFacet.type$.af$('qname',336898,'sys::Str',{}).af$('val',73730,'sys::Str',{}).am$('decode',40962,'compiler::FFacet[]',sys.List.make(sys.Param.type$,[new sys.Param('fpod','compiler::FPod',false),new sys.Param('attr','compiler::FAttr?',false)]),{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('qn','sys::Str',false),new sys.Param('v','sys::Str',false)]),{}).am$('toStr',271360,'sys::Str',xp,{}).am$('get',271360,'sys::Obj?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{});
FCodePrinter.type$.af$('pod',73728,'compiler::FPod',{}).af$('out',73728,'sys::OutStream',{}).af$('showIndex',73728,'sys::Bool',{}).af$('buf',73728,'sys::Buf?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false),new sys.Param('out','sys::OutStream',true)]),{}).am$('code',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('buf','sys::Buf',false)]),{}).am$('op',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('op','compiler::FOp',false)]),{}).am$('printSwitch',8192,'sys::Void',xp,{}).am$('index',8192,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{}).am$('print',8192,'compiler::FCodePrinter',sys.List.make(sys.Param.type$,[new sys.Param('obj','sys::Obj',true)]),{}).am$('printLine',8192,'compiler::FCodePrinter',sys.List.make(sys.Param.type$,[new sys.Param('obj','sys::Obj',true)]),{});
FOp.type$.af$('Nop',106506,'compiler::FOp',{}).af$('LoadNull',106506,'compiler::FOp',{}).af$('LoadFalse',106506,'compiler::FOp',{}).af$('LoadTrue',106506,'compiler::FOp',{}).af$('LoadInt',106506,'compiler::FOp',{}).af$('LoadFloat',106506,'compiler::FOp',{}).af$('LoadDecimal',106506,'compiler::FOp',{}).af$('LoadStr',106506,'compiler::FOp',{}).af$('LoadDuration',106506,'compiler::FOp',{}).af$('LoadType',106506,'compiler::FOp',{}).af$('LoadUri',106506,'compiler::FOp',{}).af$('LoadVar',106506,'compiler::FOp',{}).af$('StoreVar',106506,'compiler::FOp',{}).af$('LoadInstance',106506,'compiler::FOp',{}).af$('StoreInstance',106506,'compiler::FOp',{}).af$('LoadStatic',106506,'compiler::FOp',{}).af$('StoreStatic',106506,'compiler::FOp',{}).af$('LoadMixinStatic',106506,'compiler::FOp',{}).af$('StoreMixinStatic',106506,'compiler::FOp',{}).af$('CallNew',106506,'compiler::FOp',{}).af$('CallCtor',106506,'compiler::FOp',{}).af$('CallStatic',106506,'compiler::FOp',{}).af$('CallVirtual',106506,'compiler::FOp',{}).af$('CallNonVirtual',106506,'compiler::FOp',{}).af$('CallMixinStatic',106506,'compiler::FOp',{}).af$('CallMixinVirtual',106506,'compiler::FOp',{}).af$('CallMixinNonVirtual',106506,'compiler::FOp',{}).af$('Jump',106506,'compiler::FOp',{}).af$('JumpTrue',106506,'compiler::FOp',{}).af$('JumpFalse',106506,'compiler::FOp',{}).af$('CmpEQ',106506,'compiler::FOp',{}).af$('CmpNE',106506,'compiler::FOp',{}).af$('Cmp',106506,'compiler::FOp',{}).af$('CmpLE',106506,'compiler::FOp',{}).af$('CmpLT',106506,'compiler::FOp',{}).af$('CmpGT',106506,'compiler::FOp',{}).af$('CmpGE',106506,'compiler::FOp',{}).af$('CmpSame',106506,'compiler::FOp',{}).af$('CmpNotSame',106506,'compiler::FOp',{}).af$('CmpNull',106506,'compiler::FOp',{}).af$('CmpNotNull',106506,'compiler::FOp',{}).af$('Return',106506,'compiler::FOp',{}).af$('Pop',106506,'compiler::FOp',{}).af$('Dup',106506,'compiler::FOp',{}).af$('Is',106506,'compiler::FOp',{}).af$('As',106506,'compiler::FOp',{}).af$('Coerce',106506,'compiler::FOp',{}).af$('Switch',106506,'compiler::FOp',{}).af$('Throw',106506,'compiler::FOp',{}).af$('Leave',106506,'compiler::FOp',{}).af$('JumpFinally',106506,'compiler::FOp',{}).af$('CatchAllStart',106506,'compiler::FOp',{}).af$('CatchErrStart',106506,'compiler::FOp',{}).af$('CatchEnd',106506,'compiler::FOp',{}).af$('FinallyStart',106506,'compiler::FOp',{}).af$('FinallyEnd',106506,'compiler::FOp',{}).af$('vals',106498,'compiler::FOp[]',{}).af$('arg',73730,'compiler::FOpArg',{}).am$('make',2052,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('$ordinal','sys::Int',false),new sys.Param('$name','sys::Str',false),new sys.Param('arg','compiler::FOpArg',true)]),{}).am$('fromStr',40966,'compiler::FOp?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('checked','sys::Bool',true)]),{}).am$('static$init',165890,'sys::Void',xp,{});
FOpArg.type$.af$('None',106506,'compiler::FOpArg',{}).af$('Int',106506,'compiler::FOpArg',{}).af$('Float',106506,'compiler::FOpArg',{}).af$('Decimal',106506,'compiler::FOpArg',{}).af$('Str',106506,'compiler::FOpArg',{}).af$('Duration',106506,'compiler::FOpArg',{}).af$('Uri',106506,'compiler::FOpArg',{}).af$('Register',106506,'compiler::FOpArg',{}).af$('TypeRef',106506,'compiler::FOpArg',{}).af$('FieldRef',106506,'compiler::FOpArg',{}).af$('MethodRef',106506,'compiler::FOpArg',{}).af$('Jump',106506,'compiler::FOpArg',{}).af$('TypePair',106506,'compiler::FOpArg',{}).af$('vals',106498,'compiler::FOpArg[]',{}).am$('make',133124,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('$ordinal','sys::Int',false),new sys.Param('$name','sys::Str',false)]),{}).am$('fromStr',40966,'compiler::FOpArg?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('checked','sys::Bool',true)]),{}).am$('static$init',165890,'sys::Void',xp,{});
FTable.type$.af$('pod',73728,'compiler::FPod',{}).af$('table',73728,'sys::Obj[]',{}).af$('reverse',73728,'[sys::Obj:sys::Int]',{}).af$('writer',73728,'|sys::OutStream,sys::Obj->sys::Void|',{}).af$('reader',73728,'|sys::InStream->sys::Obj|',{}).am$('makeStrs',40962,'compiler::FTable',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false)]),{}).am$('makeTypeRefs',40962,'compiler::FTable',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false)]),{}).am$('makeFieldRefs',40962,'compiler::FTable',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false)]),{}).am$('makeMethodRefs',40962,'compiler::FTable',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false)]),{}).am$('makeInts',40962,'compiler::FTable',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false)]),{}).am$('makeFloats',40962,'compiler::FTable',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false)]),{}).am$('makeDecimals',40962,'compiler::FTable',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false)]),{}).am$('makeDurations',40962,'compiler::FTable',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false)]),{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false),new sys.Param('writer','|sys::OutStream,sys::Obj->sys::Void|',false),new sys.Param('reader','|sys::InStream->sys::Obj|',false)]),{}).am$('isEmpty',8192,'sys::Bool',xp,{}).am$('get',8192,'sys::Obj',sys.List.make(sys.Param.type$,[new sys.Param('index','sys::Int',false)]),{'sys::Operator':""}).am$('add',8192,'sys::Int',sys.List.make(sys.Param.type$,[new sys.Param('val','sys::Obj',false)]),{}).am$('read',8192,'compiler::FTable',sys.List.make(sys.Param.type$,[new sys.Param('in','sys::InStream?',false)]),{}).am$('write',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',false)]),{});
CNamespace.type$.af$('bridgeCache',67584,'[sys::Str:compiler::CBridge]',{}).af$('podCache',67584,'[sys::Str:compiler::CPod]',{}).af$('typeCache',65664,'[sys::Str:compiler::CType]',{}).af$('c',65664,'compiler::Compiler?',{}).af$('depends',73728,'[sys::Str:compiler::CDepend]?',{}).af$('sysPod',73728,'compiler::CPod?',{}).af$('genericParams',73728,'[sys::Str:compiler::CType]?',{}).af$('error',73728,'compiler::CType?',{}).af$('nothingType',73728,'compiler::CType?',{}).af$('itBlockType',73728,'compiler::FuncType?',{}).af$('objType',73728,'compiler::CType?',{}).af$('boolType',73728,'compiler::CType?',{}).af$('enumType',73728,'compiler::CType?',{}).af$('facetType',73728,'compiler::CType?',{}).af$('intType',73728,'compiler::CType?',{}).af$('floatType',73728,'compiler::CType?',{}).af$('decimalType',73728,'compiler::CType?',{}).af$('strType',73728,'compiler::CType?',{}).af$('strBufType',73728,'compiler::CType?',{}).af$('durationType',73728,'compiler::CType?',{}).af$('listType',73728,'compiler::CType?',{}).af$('mapType',73728,'compiler::CType?',{}).af$('funcType',73728,'compiler::CType?',{}).af$('errType',73728,'compiler::CType?',{}).af$('podType',73728,'compiler::CType?',{}).af$('typeType',73728,'compiler::CType?',{}).af$('slotType',73728,'compiler::CType?',{}).af$('fieldType',73728,'compiler::CType?',{}).af$('methodType',73728,'compiler::CType?',{}).af$('rangeType',73728,'compiler::CType?',{}).af$('testType',73728,'compiler::CType?',{}).af$('uriType',73728,'compiler::CType?',{}).af$('voidType',73728,'compiler::CType?',{}).af$('fieldNotSetErrType',73728,'compiler::CType?',{}).af$('notImmutableErrType',73728,'compiler::CType?',{}).af$('objTrap',73728,'compiler::CMethod?',{}).af$('objWith',73728,'compiler::CMethod?',{}).af$('objToImmutable',73728,'compiler::CMethod?',{}).af$('boolNot',73728,'compiler::CMethod?',{}).af$('intIncrement',73728,'compiler::CMethod?',{}).af$('intDecrement',73728,'compiler::CMethod?',{}).af$('intPlus',73728,'compiler::CMethod?',{}).af$('floatPlus',73728,'compiler::CMethod?',{}).af$('floatMinus',73728,'compiler::CMethod?',{}).af$('strPlus',73728,'compiler::CMethod?',{}).af$('strBufMake',73728,'compiler::CMethod?',{}).af$('strBufAdd',73728,'compiler::CMethod?',{}).af$('strBufToStr',73728,'compiler::CMethod?',{}).af$('listMake',73728,'compiler::CMethod?',{}).af$('listMakeObj',73728,'compiler::CMethod?',{}).af$('listAdd',73728,'compiler::CMethod?',{}).af$('mapMake',73728,'compiler::CMethod?',{}).af$('mapSet',73728,'compiler::CMethod?',{}).af$('enumOrdinal',73728,'compiler::CMethod?',{}).af$('funcBind',73728,'compiler::CMethod?',{}).af$('rangeMakeInclusive',73728,'compiler::CMethod?',{}).af$('rangeMakeExclusive',73728,'compiler::CMethod?',{}).af$('slotFindMethod',73728,'compiler::CMethod?',{}).af$('slotFindFunc',73728,'compiler::CMethod?',{}).af$('podFind',73728,'compiler::CMethod?',{}).af$('podLocale',73728,'compiler::CMethod?',{}).af$('typePod',73728,'compiler::CMethod?',{}).af$('typeField',73728,'compiler::CMethod?',{}).af$('typeMethod',73728,'compiler::CMethod?',{}).af$('funcEnterCtor',73728,'compiler::CMethod?',{}).af$('funcExitCtor',73728,'compiler::CMethod?',{}).af$('funcCheckInCtor',73728,'compiler::CMethod?',{}).af$('funcCall',73728,'compiler::CMethod?',{}).af$('fieldNotSetErrMake',73728,'compiler::CMethod?',{}).af$('notImmutableErrMake',73728,'compiler::CMethod?',{}).am$('init',4096,'sys::Void',xp,{}).am$('genericParam',2048,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('sysType',2048,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('sysMethod',2048,'compiler::CMethod',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::CType',false),new sys.Param('name','sys::Str',false)]),{}).am$('cleanup',8192,'sys::Void',xp,{}).am$('resolveBridge',2048,'compiler::CBridge',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('loc','compiler::Loc?',false)]),{}).am$('findBridge',266240,'compiler::CBridge',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false),new sys.Param('name','sys::Str',false),new sys.Param('loc','compiler::Loc?',false)]),{}).am$('resolvePod',8192,'compiler::CPod',sys.List.make(sys.Param.type$,[new sys.Param('podName','sys::Str',false),new sys.Param('loc','compiler::Loc?',false)]),{}).am$('findPod',266241,'compiler::CPod?',sys.List.make(sys.Param.type$,[new sys.Param('podName','sys::Str',false)]),{}).am$('resolveType',8192,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('sig','sys::Str',false)]),{}).am$('resolveSlot',8192,'compiler::CSlot',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('genericParameter',8192,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('id','sys::Str',false)]),{}).am$('compiler',128,'compiler::Compiler',xp,{}).am$('flattenAndOrderByDepends',8192,'compiler::CPod[]',sys.List.make(sys.Param.type$,[new sys.Param('pods','compiler::CPod[]',false)]),{}).am$('flattenDepends',8192,'compiler::CPod[]',sys.List.make(sys.Param.type$,[new sys.Param('pods','compiler::CPod[]',false)]),{}).am$('doFlattenDepends',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('acc','[sys::Str:compiler::CPod]',false),new sys.Param('pod','compiler::CPod',false)]),{}).am$('orderByDepends',8192,'compiler::CPod[]',sys.List.make(sys.Param.type$,[new sys.Param('pods','compiler::CPod[]',false)]),{}).am$('noDependsInLeft',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('left','compiler::CPod[]',false),new sys.Param('p','compiler::CPod',false)]),{}).am$('make',139268,'sys::Void',xp,{});
FPodNamespace.type$.af$('dir',73730,'sys::File?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('dir','sys::File?',false)]),{}).am$('findPod',267264,'compiler::FPod?',sys.List.make(sys.Param.type$,[new sys.Param('podName','sys::Str',false)]),{});
Fanp.type$.af$('compiler',73728,'compiler::Compiler?',{}).af$('showTables',73728,'sys::Bool',{}).af$('showCode',73728,'sys::Bool',{}).af$('showLines',73728,'sys::Bool',{}).af$('showIndex',73728,'sys::Bool',{}).af$('file',73728,'sys::Str?',{}).am$('execute',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('target','sys::Str',false)]),{}).am$('executeFile',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('target','sys::Str?',false)]),{}).am$('printPod',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('pod','sys::Pod',false)]),{}).am$('printType',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','sys::Type',false)]),{}).am$('printSlot',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('slot','sys::Slot',false)]),{}).am$('printer',8192,'compiler::FPrinter',sys.List.make(sys.Param.type$,[new sys.Param('pod','sys::Pod',false)]),{}).am$('fpod',8192,'compiler::FPod',sys.List.make(sys.Param.type$,[new sys.Param('podName','sys::Str',false)]),{}).am$('ftype',8192,'compiler::FType',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::FPod',false),new sys.Param('typeName','sys::Str',false)]),{}).am$('fslot',8192,'compiler::FSlot',sys.List.make(sys.Param.type$,[new sys.Param('ftype','compiler::FType',false),new sys.Param('slotName','sys::Str',false)]),{}).am$('run',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('args','sys::Str[]',false)]),{}).am$('help',8192,'sys::Void',xp,{}).am$('main',40962,'sys::Void',xp,{}).am$('make',139268,'sys::Void',xp,{});
FType.type$.af$('base',336896,'compiler::CType?',{}).af$('slotsCached',67584,'[sys::Str:compiler::CSlot]?',{}).af$('flags',336896,'sys::Int',{}).af$('hollow',73728,'sys::Bool',{}).af$('fpod',73728,'compiler::FPod',{}).af$('self',73728,'sys::Int',{}).af$('fbase',73728,'sys::Int',{}).af$('fmixins',73728,'sys::Int[]?',{}).af$('ffields',73728,'compiler::FField[]?',{}).af$('fmethods',73728,'compiler::FMethod[]?',{}).af$('fattrs',73728,'compiler::FAttr[]?',{}).af$('ffacets',73728,'compiler::FFacet[]?',{}).af$('name$Store',722944,'sys::Obj?',{}).af$('qname$Store',722944,'sys::Obj?',{}).af$('mixins$Store',722944,'sys::Obj?',{}).af$('isVal$Store',722944,'sys::Obj?',{}).af$('operators$Store',722944,'sys::Obj?',{}).af$('toNullable$Store',722944,'sys::Obj?',{}).af$('toListOf$Store',722944,'sys::Obj?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('fpod','compiler::FPod',false)]),{}).am$('ns',271360,'compiler::CNamespace',xp,{}).am$('pod',271360,'compiler::FPod',xp,{}).am$('name',795648,'sys::Str',xp,{}).am$('qname',795648,'sys::Str',xp,{}).am$('signature',271360,'sys::Str',xp,{}).am$('doc',271360,'compiler::CDoc?',xp,{}).am$('attr',8192,'compiler::FAttr?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('mixins',795648,'compiler::CType[]',xp,{}).am$('isVal',795648,'sys::Bool',xp,{}).am$('facet',271360,'compiler::CFacet?',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('slots',271360,'[sys::Str:compiler::CSlot]',xp,{}).am$('operators',795648,'compiler::COperators',xp,{}).am$('isNullable',271360,'sys::Bool',xp,{}).am$('toNullable',795648,'compiler::CType',xp,{}).am$('isGeneric',271360,'sys::Bool',xp,{}).am$('isParameterized',271360,'sys::Bool',xp,{}).am$('isGenericParameter',271360,'sys::Bool',xp,{}).am$('toListOf',795648,'compiler::CType',xp,{}).am$('reflect',2048,'sys::Void',xp,{}).am$('inherit',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::CType',false)]),{}).am$('writeMeta',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',false)]),{}).am$('readMeta',8192,'sys::This',sys.List.make(sys.Param.type$,[new sys.Param('in','sys::InStream',false)]),{}).am$('uri',8192,'sys::Uri',xp,{}).am$('write',8192,'sys::Void',xp,{}).am$('read',8192,'sys::Void',xp,{}).am$('name$Once',133120,'sys::Str',xp,{}).am$('qname$Once',133120,'sys::Str',xp,{}).am$('mixins$Once',133120,'compiler::CType[]',xp,{}).am$('isVal$Once',133120,'sys::Bool',xp,{}).am$('operators$Once',133120,'compiler::COperators',xp,{}).am$('toNullable$Once',133120,'compiler::CType',xp,{}).am$('toListOf$Once',133120,'compiler::CType',xp,{});
COperators.type$.af$('prefixes',100354,'sys::Str[]',{}).af$('exacts',100354,'[sys::Str:sys::Str]',{}).af$('parent',73728,'compiler::CType',{}).af$('byPrefix',67584,'[sys::Str:compiler::CMethod[]]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('parent','compiler::CType',false)]),{}).am$('toPrefix',40962,'sys::Str?',sys.List.make(sys.Param.type$,[new sys.Param('methodName','sys::Str',false)]),{}).am$('find',8192,'compiler::CMethod[]',sys.List.make(sys.Param.type$,[new sys.Param('prefix','sys::Str',false)]),{}).am$('static$init',165890,'sys::Void',xp,{});
ReflectSlot.type$.af$('flags$Store',722944,'sys::Obj?',{}).am$('doc',271360,'compiler::CDoc?',xp,{}).am$('name',271360,'sys::Str',xp,{}).am$('qname',271360,'sys::Str',xp,{}).am$('signature',271360,'sys::Str',xp,{}).am$('flags',795648,'sys::Int',xp,{}).am$('slot',270337,'sys::Slot',xp,{}).am$('facet',271360,'compiler::CFacet?',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('make',139268,'sys::Void',xp,{}).am$('flags$Once',133120,'sys::Int',xp,{});
ReflectField.type$.af$('ns',336896,'compiler::ReflectNamespace',{}).af$('parent',336896,'compiler::CType',{}).af$('type',336896,'compiler::CType',{}).af$('getter',336896,'compiler::CMethod?',{}).af$('setter',336896,'compiler::CMethod?',{}).af$('f',73728,'sys::Field',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::ReflectNamespace',false),new sys.Param('parent','compiler::CType',false),new sys.Param('f','sys::Field',false)]),{}).am$('slot',271360,'sys::Slot',xp,{}).am$('inheritedReturns',271360,'compiler::CType',xp,{});
ReflectMethod.type$.af$('ns',336896,'compiler::ReflectNamespace',{}).af$('parent',336896,'compiler::CType',{}).af$('returns',336896,'compiler::CType',{}).af$('params',336896,'compiler::CParam[]',{}).af$('isGeneric',336896,'sys::Bool',{}).af$('m',73728,'sys::Method',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::ReflectNamespace',false),new sys.Param('parent','compiler::CType',false),new sys.Param('m','sys::Method',false)]),{}).am$('slot',271360,'sys::Slot',xp,{}).am$('inheritedReturns',271360,'compiler::CType',xp,{});
ReflectParam.type$.af$('type',336896,'compiler::CType',{}).af$('p',73728,'sys::Param',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::ReflectNamespace',false),new sys.Param('p','sys::Param',false)]),{}).am$('name',271360,'sys::Str',xp,{}).am$('hasDefault',271360,'sys::Bool',xp,{});
ReflectFacet.type$.af$('f',73728,'sys::Facet',{}).am$('map',40962,'compiler::ReflectFacet?',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::ReflectNamespace',false),new sys.Param('f','sys::Facet?',false)]),{}).am$('make',2052,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','sys::Facet',false)]),{}).am$('qname',271360,'sys::Str',xp,{}).am$('get',271360,'sys::Obj?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('toStr',271360,'sys::Str',xp,{});
ReflectPod.type$.af$('ns',336896,'compiler::ReflectNamespace',{}).af$('pod',73728,'sys::Pod',{}).af$('typeMap',67584,'[sys::Str:compiler::ReflectType]',{}).af$('loadedAllTypes',67584,'sys::Bool',{}).af$('depends$Store',722944,'sys::Obj?',{}).af$('file$Store',722944,'sys::Obj?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::ReflectNamespace',false),new sys.Param('pod','sys::Pod',false)]),{}).am$('name',271360,'sys::Str',xp,{}).am$('version',271360,'sys::Version',xp,{}).am$('depends',795648,'compiler::CDepend[]',xp,{}).am$('file',795648,'sys::File',xp,{}).am$('meta',271360,'[sys::Str:sys::Str]',xp,{}).am$('types',271360,'compiler::CType[]',xp,{}).am$('resolveType',271360,'compiler::ReflectType?',sys.List.make(sys.Param.type$,[new sys.Param('typeName','sys::Str',false),new sys.Param('checked','sys::Bool',false)]),{}).am$('depends$Once',133120,'compiler::CDepend[]',xp,{}).am$('file$Once',133120,'sys::File',xp,{});
ReflectType.type$.af$('isVal',336898,'sys::Bool',{}).af$('t',73730,'sys::Type',{}).af$('pod',336896,'compiler::ReflectPod',{}).af$('base',336896,'compiler::CType?',{}).af$('mixins',336896,'compiler::CType[]',{}).af$('slotMap',67584,'[sys::Str:compiler::CSlot]',{}).af$('slotsLoaded',67584,'sys::Bool',{}).af$('toNullable$Store',722944,'sys::Obj?',{}).af$('toListOf$Store',722944,'sys::Obj?',{}).af$('operators$Store',722944,'sys::Obj?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::ReflectNamespace',false),new sys.Param('t','sys::Type',false)]),{}).am$('ns',271360,'compiler::ReflectNamespace',xp,{}).am$('doc',271360,'compiler::CDoc?',xp,{}).am$('name',271360,'sys::Str',xp,{}).am$('qname',271360,'sys::Str',xp,{}).am$('signature',271360,'sys::Str',xp,{}).am$('flags',271360,'sys::Int',xp,{}).am$('isNullable',271360,'sys::Bool',xp,{}).am$('toNullable',795648,'compiler::CType',xp,{}).am$('isGeneric',271360,'sys::Bool',xp,{}).am$('isParameterized',271360,'sys::Bool',xp,{}).am$('isGenericParameter',271360,'sys::Bool',xp,{}).am$('toListOf',795648,'compiler::CType',xp,{}).am$('facet',271360,'compiler::CFacet?',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('slots',271360,'[sys::Str:compiler::CSlot]',xp,{}).am$('operators',795648,'compiler::COperators',xp,{}).am$('slot',271360,'compiler::CSlot?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('toNullable$Once',133120,'compiler::CType',xp,{}).am$('toListOf$Once',133120,'compiler::CType',xp,{}).am$('operators$Once',133120,'compiler::COperators',xp,{});
MarkerFacet.type$.af$('qname',336898,'sys::Str',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('get',271360,'sys::Obj?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{});
ReflectNamespace.type$.af$('pods',67584,'[sys::Str:compiler::ReflectPod]',{}).am$('make',8196,'sys::Void',xp,{}).am$('findPod',267264,'compiler::ReflectPod?',sys.List.make(sys.Param.type$,[new sys.Param('podName','sys::Str',false)]),{}).am$('importPod',8192,'compiler::ReflectPod',sys.List.make(sys.Param.type$,[new sys.Param('pod','sys::Pod',false)]),{}).am$('importType',8192,'compiler::CType?',sys.List.make(sys.Param.type$,[new sys.Param('t','sys::Type?',false)]),{}).am$('importTypes',8192,'compiler::CType[]',sys.List.make(sys.Param.type$,[new sys.Param('t','sys::Type[]',false)]),{}).am$('importSlot',8192,'compiler::CSlot',sys.List.make(sys.Param.type$,[new sys.Param('slot','sys::Slot',false)]),{}).am$('importField',8192,'compiler::CField',sys.List.make(sys.Param.type$,[new sys.Param('f','sys::Field',false)]),{}).am$('importMethod',8192,'compiler::CMethod',sys.List.make(sys.Param.type$,[new sys.Param('m','sys::Method',false)]),{});
CBridge.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('c','compiler::Compiler',false)]),{}).am$('resolvePod',270337,'compiler::CPod',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('loc','compiler::Loc?',false)]),{}).am$('coerce',270336,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false),new sys.Param('expected','compiler::CType',false),new sys.Param('onErr','|->sys::Void|',false)]),{}).am$('resolveConstruction',270337,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false)]),{}).am$('resolveConstructorChain',270337,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false)]),{}).am$('resolveSlotAccess',270337,'compiler::CSlot?',sys.List.make(sys.Param.type$,[new sys.Param('base','compiler::CType',false),new sys.Param('name','sys::Str',false),new sys.Param('noParens','sys::Bool',false)]),{}).am$('resolveCall',270337,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false)]),{}).am$('checkOverride',270337,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false),new sys.Param('base','compiler::CSlot',false),new sys.Param('def','compiler::SlotDef',false)]),{}).am$('checkType',270337,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::TypeDef',false)]),{}).am$('cleanup',270336,'sys::Void',xp,{});
NullableType.type$.af$('signature',336896,'sys::Str',{}).af$('root',73728,'compiler::CType',{}).af$('toListOf$Store',722944,'sys::Obj?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('root','compiler::CType',false)]),{}).am$('ns',271360,'compiler::CNamespace',xp,{}).am$('doc',271360,'compiler::CDoc?',xp,{}).am$('pod',271360,'compiler::CPod',xp,{}).am$('name',271360,'sys::Str',xp,{}).am$('qname',271360,'sys::Str',xp,{}).am$('flags',271360,'sys::Int',xp,{}).am$('isVal',271360,'sys::Bool',xp,{}).am$('facet',271360,'compiler::CFacet?',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('isNullable',271360,'sys::Bool',xp,{}).am$('toNullable',271360,'compiler::CType',xp,{}).am$('toNonNullable',271360,'compiler::CType',xp,{}).am$('isGeneric',271360,'sys::Bool',xp,{}).am$('isParameterized',271360,'sys::Bool',xp,{}).am$('isGenericParameter',271360,'sys::Bool',xp,{}).am$('parameterizeThis',271360,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('thisType','compiler::CType',false)]),{}).am$('isForeign',271360,'sys::Bool',xp,{}).am$('isSupported',271360,'sys::Bool',xp,{}).am$('inferredAs',271360,'compiler::CType',xp,{}).am$('toListOf',795648,'compiler::CType',xp,{}).am$('base',271360,'compiler::CType?',xp,{}).am$('mixins',271360,'compiler::CType[]',xp,{}).am$('fits',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::CType',false)]),{}).am$('slots',271360,'[sys::Str:compiler::CSlot]',xp,{}).am$('operators',271360,'compiler::COperators',xp,{}).am$('isValid',271360,'sys::Bool',xp,{}).am$('toListOf$Once',133120,'compiler::CType',xp,{});
MDoc.type$.af$('text',336898,'sys::Str',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('text','sys::Str',false)]),{});
GenericType.type$.af$('base',336896,'compiler::CType?',{}).af$('toNullable$Store',722944,'sys::Obj?',{}).af$('toListOf$Store',722944,'sys::Obj?',{}).af$('slots$Store',722944,'sys::Obj?',{}).af$('operators$Store',722944,'sys::Obj?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('base','compiler::CType',false)]),{}).am$('ns',271360,'compiler::CNamespace',xp,{}).am$('doc',271360,'compiler::CDoc?',xp,{}).am$('pod',271360,'compiler::CPod',xp,{}).am$('name',271360,'sys::Str',xp,{}).am$('qname',271360,'sys::Str',xp,{}).am$('isVal',271360,'sys::Bool',xp,{}).am$('isNullable',271360,'sys::Bool',xp,{}).am$('toNullable',795648,'compiler::CType',xp,{}).am$('isGeneric',271360,'sys::Bool',xp,{}).am$('isParameterized',271360,'sys::Bool',xp,{}).am$('toListOf',795648,'compiler::CType',xp,{}).am$('mixins',271360,'compiler::CType[]',xp,{}).am$('facet',271360,'compiler::CFacet?',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('slots',795648,'[sys::Str:compiler::CSlot]',xp,{}).am$('operators',795648,'compiler::COperators',xp,{}).am$('parameterizeSlots',2048,'[sys::Str:compiler::CSlot]',xp,{}).am$('parameterizeSlot',2048,'compiler::CSlot',sys.List.make(sys.Param.type$,[new sys.Param('slot','compiler::CSlot',false)]),{}).am$('parameterize',128,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::CType',false)]),{}).am$('parameterizeListType',2048,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::ListType',false)]),{}).am$('parameterizeMapType',2048,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::MapType',false)]),{}).am$('parameterizeFuncType',2048,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::FuncType',false)]),{}).am$('doParameterize',270337,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('ch','sys::Int',false)]),{}).am$('toNullable$Once',133120,'compiler::CType',xp,{}).am$('toListOf$Once',133120,'compiler::CType',xp,{}).am$('slots$Once',133120,'[sys::Str:compiler::CSlot]',xp,{}).am$('operators$Once',133120,'compiler::COperators',xp,{});
ListType.type$.af$('v',73728,'compiler::CType',{}).af$('signature',336898,'sys::Str',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('v','compiler::CType',false)]),{}).am$('isGenericParameter',271360,'sys::Bool',xp,{}).am$('flags',271360,'sys::Int',xp,{}).am$('fits',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::CType',false)]),{}).am$('doParameterize',271360,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('ch','sys::Int',false)]),{}).am$('isValid',271360,'sys::Bool',xp,{});
MapType.type$.af$('k',73728,'compiler::CType',{}).af$('v',73728,'compiler::CType',{}).af$('signature',336898,'sys::Str',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('k','compiler::CType',false),new sys.Param('v','compiler::CType',false)]),{}).am$('flags',271360,'sys::Int',xp,{}).am$('isGenericParameter',271360,'sys::Bool',xp,{}).am$('fits',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::CType',false)]),{}).am$('doParameterize',271360,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('ch','sys::Int',false)]),{}).am$('isValid',271360,'sys::Bool',xp,{});
FuncType.type$.af$('params',73728,'compiler::CType[]',{}).af$('names',73728,'sys::Str[]',{}).af$('returns',73728,'compiler::CType',{}).af$('unnamed',73728,'sys::Bool',{}).af$('signature',336898,'sys::Str',{}).af$('isGenericParameter',336898,'sys::Bool',{}).af$('inferredSignature',73728,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('params','compiler::CType[]',false),new sys.Param('names','sys::Str[]',false),new sys.Param('returns','compiler::CType',false)]),{}).am$('makeItBlock',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('itType','compiler::CType',false)]),{}).am$('flags',271360,'sys::Int',xp,{}).am$('fits',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::CType',false)]),{}).am$('arity',8192,'sys::Int',xp,{}).am$('toArity',8192,'compiler::FuncType',sys.List.make(sys.Param.type$,[new sys.Param('num','sys::Int',false)]),{}).am$('mostSpecific',8192,'compiler::FuncType',sys.List.make(sys.Param.type$,[new sys.Param('b','compiler::FuncType',false)]),{}).am$('toMostSpecific',40962,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('a','compiler::CType',false),new sys.Param('b','compiler::CType',false)]),{}).am$('toParamDefs',8192,'compiler::ParamDef[]',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false)]),{}).am$('doParameterize',271360,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('ch','sys::Int',false)]),{}).am$('usesThis',8192,'sys::Bool',xp,{}).am$('parameterizeThis',271360,'compiler::FuncType',sys.List.make(sys.Param.type$,[new sys.Param('thisType','compiler::CType',false)]),{}).am$('isValid',271360,'sys::Bool',xp,{}).am$('ret',8192,'compiler::CType',xp,{'sys::Deprecated':""});
GenericParameterType.type$.af$('ns',336896,'compiler::CNamespace',{}).af$('name',336896,'sys::Str',{}).af$('qname',336896,'sys::Str',{}).af$('toNullable$Store',722944,'sys::Obj?',{}).af$('toListOf$Store',722944,'sys::Obj?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::CNamespace',false),new sys.Param('name','sys::Str',false)]),{}).am$('pod',271360,'compiler::CPod',xp,{}).am$('doc',271360,'compiler::CDoc?',xp,{}).am$('signature',271360,'sys::Str',xp,{}).am$('flags',271360,'sys::Int',xp,{}).am$('isVal',271360,'sys::Bool',xp,{}).am$('isNullable',271360,'sys::Bool',xp,{}).am$('toNullable',795648,'compiler::CType',xp,{}).am$('isGeneric',271360,'sys::Bool',xp,{}).am$('isParameterized',271360,'sys::Bool',xp,{}).am$('isGenericParameter',271360,'sys::Bool',xp,{}).am$('toListOf',795648,'compiler::CType',xp,{}).am$('base',271360,'compiler::CType?',xp,{}).am$('mixins',271360,'compiler::CType[]',xp,{}).am$('facet',271360,'compiler::CFacet?',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('slots',271360,'[sys::Str:compiler::CSlot]',xp,{}).am$('operators',271360,'compiler::COperators',xp,{}).am$('toNullable$Once',133120,'compiler::CType',xp,{}).am$('toListOf$Once',133120,'compiler::CType',xp,{});
ParameterizedField.type$.af$('type',336896,'compiler::CType',{}).af$('getter',336896,'compiler::CMethod?',{}).af$('setter',336896,'compiler::CMethod?',{}).af$('parent',336896,'compiler::CType',{}).af$('generic',73728,'compiler::CField',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('parent','compiler::GenericType',false),new sys.Param('generic','compiler::CField',false)]),{}).am$('doc',271360,'compiler::CDoc?',xp,{}).am$('name',271360,'sys::Str',xp,{}).am$('qname',271360,'sys::Str',xp,{}).am$('signature',271360,'sys::Str',xp,{}).am$('flags',271360,'sys::Int',xp,{}).am$('facet',271360,'compiler::CFacet?',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('inheritedReturns',271360,'compiler::CType',xp,{}).am$('isParameterized',271360,'sys::Bool',xp,{});
ParameterizedMethod.type$.af$('parent',336896,'compiler::CType',{}).af$('signature',336896,'sys::Str',{}).af$('generic',336896,'compiler::CMethod?',{}).af$('returns',336896,'compiler::CType',{}).af$('params',336896,'compiler::CParam[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('parent','compiler::GenericType',false),new sys.Param('generic','compiler::CMethod',false)]),{}).am$('doc',271360,'compiler::CDoc?',xp,{}).am$('name',271360,'sys::Str',xp,{}).am$('qname',271360,'sys::Str',xp,{}).am$('flags',271360,'sys::Int',xp,{}).am$('facet',271360,'compiler::CFacet?',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('isParameterized',271360,'sys::Bool',xp,{}).am$('inheritedReturns',271360,'compiler::CType',xp,{});
ParameterizedMethodParam.type$.af$('type',336896,'compiler::CType',{}).af$('generic',73728,'compiler::CParam',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('parent','compiler::GenericType',false),new sys.Param('generic','compiler::CParam',false)]),{}).am$('name',271360,'sys::Str',xp,{}).am$('hasDefault',271360,'sys::Bool',xp,{}).am$('toStr',271360,'sys::Str',xp,{});
MockSlot.type$.af$('parent',336896,'compiler::CType',{}).af$('name',336896,'sys::Str',{}).af$('flags',336896,'sys::Int',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('parent','compiler::CType',false),new sys.Param('name','sys::Str',false),new sys.Param('flags','sys::Int',false)]),{}).am$('qname',271360,'sys::Str',xp,{}).am$('signature',271360,'sys::Str',xp,{}).am$('doc',271360,'compiler::CDoc?',xp,{}).am$('facet',271360,'compiler::CFacet?',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{});
MockField.type$.af$('type',336896,'compiler::CType',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('parent','compiler::CType',false),new sys.Param('name','sys::Str',false),new sys.Param('flags','sys::Int',false),new sys.Param('of','compiler::CType',false)]),{}).am$('getter',271360,'compiler::CMethod?',xp,{}).am$('setter',271360,'compiler::CMethod?',xp,{}).am$('inheritedReturns',271360,'compiler::CType',xp,{});
MockMethod.type$.af$('returns',336896,'compiler::CType',{}).af$('params',336896,'compiler::CParam[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('parent','compiler::CType',false),new sys.Param('name','sys::Str',false),new sys.Param('flags','sys::Int',false),new sys.Param('ret','compiler::CType',false),new sys.Param('params','compiler::CType[]',false)]),{}).am$('inheritedReturns',271360,'compiler::CType',xp,{});
MockParam.type$.af$('name',336896,'sys::Str',{}).af$('type',336896,'compiler::CType',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('of','compiler::CType',false)]),{}).am$('hasDefault',271360,'sys::Bool',xp,{});
TypeParser.type$.af$('ns',67584,'compiler::CNamespace',{}).af$('sig',67584,'sys::Str',{}).af$('len',67584,'sys::Int',{}).af$('pos',67584,'sys::Int',{}).af$('cur',67584,'sys::Int',{}).af$('peek',67584,'sys::Int',{}).am$('resolve',40962,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::CNamespace',false),new sys.Param('sig','sys::Str',false)]),{}).am$('make',2052,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('ns','compiler::CNamespace',false),new sys.Param('sig','sys::Str',false)]),{}).am$('loadTop',2048,'compiler::CType',xp,{}).am$('loadAny',2048,'compiler::CType',xp,{}).am$('loadMap',2048,'compiler::CType',xp,{}).am$('loadFunc',2048,'compiler::CType',xp,{}).am$('loadFFI',2048,'compiler::CType',xp,{}).am$('loadBasic',2048,'compiler::CType',xp,{}).am$('consume',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expected','sys::Int?',true)]),{}).am$('isIdChar',34818,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('ch','sys::Int',false)]),{}).am$('err',2048,'sys::ArgErr',xp,{});
CDepend.type$.af$('depend',73730,'sys::Depend',{}).af$('pod',73728,'compiler::CPod?',{}).am$('fromStr',40962,'compiler::CDepend',sys.List.make(sys.Param.type$,[new sys.Param('s','sys::Str',false)]),{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('d','sys::Depend',false),new sys.Param('p','compiler::CPod?',false)]),{}).am$('makeList',40962,'compiler::CDepend[]',sys.List.make(sys.Param.type$,[new sys.Param('d','sys::Depend[]',false)]),{}).am$('name',8192,'sys::Str',xp,{}).am$('toStr',271360,'sys::Str',xp,{});
Tokenizer.type$.af$('buf',67584,'sys::Str',{}).af$('pos',67584,'sys::Int',{}).af$('isDoc',67584,'sys::Bool',{}).af$('filename',67584,'sys::Str',{}).af$('line',67584,'sys::Int',{}).af$('col',67584,'sys::Int',{}).af$('curLine',67584,'sys::Int',{}).af$('cur',67584,'sys::Int',{}).af$('peek',67584,'sys::Int',{}).af$('lastLine',67584,'sys::Int',{}).af$('posOfLine',67584,'sys::Int',{}).af$('tokens',67584,'compiler::TokenVal[]',{}).af$('inStrLiteral',67584,'sys::Bool',{}).af$('whitespace',67584,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false),new sys.Param('loc','compiler::Loc',false),new sys.Param('buf','sys::Str',false),new sys.Param('isDoc','sys::Bool',false)]),{}).am$('tokenize',8192,'compiler::TokenVal[]',xp,{}).am$('next',8192,'compiler::TokenVal?',xp,{}).am$('find',2048,'compiler::TokenVal?',xp,{}).am$('word',2048,'compiler::TokenVal',xp,{}).am$('isIdentifierStart',34818,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('c','sys::Int',false)]),{}).am$('number',2048,'compiler::TokenVal',xp,{}).am$('hexInt',2048,'compiler::TokenVal',xp,{}).am$('binaryInt',2048,'compiler::TokenVal',xp,{}).am$('quoted',2048,'compiler::TokenVal?',sys.List.make(sys.Param.type$,[new sys.Param('q','compiler::Quoted',false)]),{}).am$('skipStrWs',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('openLine','sys::Int',false),new sys.Param('openPos','sys::Int',false)]),{}).am$('interpolation',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('line','sys::Int',false),new sys.Param('col','sys::Int',false),new sys.Param('s','sys::Str',false),new sys.Param('q','compiler::Quoted',false)]),{}).am$('endOfQuoted',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('q','compiler::Quoted',false)]),{}).am$('makeVirtualToken',2048,'compiler::TokenVal',sys.List.make(sys.Param.type$,[new sys.Param('line','sys::Int',false),new sys.Param('col','sys::Int',false),new sys.Param('kind','compiler::Token',false),new sys.Param('value','sys::Obj?',true)]),{}).am$('ch',2048,'compiler::TokenVal',xp,{}).am$('escape',8192,'sys::Int',xp,{}).am$('dsl',2048,'compiler::TokenVal',xp,{}).am$('skipCommentSL',2048,'compiler::TokenVal?',xp,{}).am$('skipCommentML',2048,'compiler::TokenVal?',xp,{}).am$('docComment',2048,'compiler::TokenVal?',xp,{}).am$('symbol',2048,'compiler::TokenVal',xp,{}).am$('err',271360,'compiler::CompilerErr',sys.List.make(sys.Param.type$,[new sys.Param('msg','sys::Str',false),new sys.Param('loc','compiler::Loc?',true)]),{}).am$('peekPeek',2048,'sys::Int',xp,{}).am$('consume',2048,'sys::Void',xp,{});
Quoted.type$.af$('normal',106506,'compiler::Quoted',{}).af$('triple',106506,'compiler::Quoted',{}).af$('uri',106506,'compiler::Quoted',{}).af$('vals',106498,'compiler::Quoted[]',{}).af$('toStr',336898,'sys::Str',{}).af$('multiLine',73730,'sys::Bool',{}).am$('isUri',8192,'sys::Bool',xp,{}).am$('isTriple',8192,'sys::Bool',xp,{}).am$('make',2052,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('$ordinal','sys::Int',false),new sys.Param('$name','sys::Str',false),new sys.Param('s','sys::Str',false),new sys.Param('ml','sys::Bool',false)]),{}).am$('fromStr',40966,'compiler::Quoted?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('checked','sys::Bool',true)]),{}).am$('static$init',165890,'sys::Void',xp,{});
TokenVal.type$.af$('kind',73728,'compiler::Token',{}).af$('val',73728,'sys::Obj?',{}).af$('newline',73728,'sys::Bool',{}).af$('whitespace',73728,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('kind','compiler::Token',false),new sys.Param('val','sys::Obj?',true)]),{}).am$('hash',271360,'sys::Int',xp,{}).am$('equals',271360,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('obj','sys::Obj?',false)]),{}).am$('toStr',271360,'sys::Str',xp,{}).am$('toCode',8192,'sys::Str',xp,{}).am$('isCallOpenParen',8192,'sys::Bool',xp,{}).am$('isIndexOpenBracket',8192,'sys::Bool',xp,{});
TokenValDsl.type$.af$('leadingTabs',73728,'sys::Int',{}).af$('leadingSpaces',73728,'sys::Int',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('kind','compiler::Token',false),new sys.Param('src','sys::Str',false),new sys.Param('tabs','sys::Int',false),new sys.Param('spaces','sys::Int',false)]),{});
Parser.type$.af$('ProtectionMask',106498,'sys::Int',{}).af$('unit',67584,'compiler::CompilationUnit',{}).af$('tokens',67584,'compiler::TokenVal[]',{}).af$('numTokens',67584,'sys::Int',{}).af$('pos',67584,'sys::Int',{}).af$('cur',67584,'compiler::TokenVal?',{}).af$('curt',67584,'compiler::Token?',{}).af$('peek',67584,'compiler::TokenVal?',{}).af$('peekt',67584,'compiler::Token?',{}).af$('inFieldInit',67584,'sys::Bool',{}).af$('curType',67584,'compiler::TypeDef?',{}).af$('curSlot',67584,'compiler::SlotDef?',{}).af$('curClosure',67584,'compiler::ClosureExpr?',{}).af$('closureCount',67584,'sys::Int?',{}).af$('closures',67584,'compiler::ClosureExpr[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false),new sys.Param('unit','compiler::CompilationUnit',false),new sys.Param('closures','compiler::ClosureExpr[]',false)]),{}).am$('parse',8192,'sys::Void',xp,{}).am$('usings',2048,'sys::Void',xp,{}).am$('skipUsing',2048,'sys::Void',xp,{}).am$('typeDef',8192,'sys::Void',xp,{}).am$('inheritType',2048,'compiler::CType',xp,{}).am$('flags',2048,'sys::Int',sys.List.make(sys.Param.type$,[new sys.Param('normalize','sys::Bool',true)]),{}).am$('enumDefs',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::TypeDef',false)]),{}).am$('enumDef',2048,'compiler::EnumDef',sys.List.make(sys.Param.type$,[new sys.Param('ordinal','sys::Int',false)]),{}).am$('slotDef',2048,'compiler::SlotDef',sys.List.make(sys.Param.type$,[new sys.Param('parent','compiler::TypeDef',false),new sys.Param('doc','compiler::DocDef?',false)]),{}).am$('fieldDef',2048,'compiler::FieldDef',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('parent','compiler::TypeDef',false),new sys.Param('doc','compiler::DocDef?',false),new sys.Param('facets','compiler::FacetDef[]?',false),new sys.Param('flags','sys::Int',false),new sys.Param('type','compiler::TypeRef?',false),new sys.Param('name','sys::Str',false)]),{}).am$('defGetAndSet',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{}).am$('defGet',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{}).am$('defSet',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{}).am$('genSyntheticGet',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{}).am$('genSyntheticSet',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{}).am$('getOrSet',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{}).am$('methodDef',2048,'compiler::MethodDef',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('parent','compiler::TypeDef',false),new sys.Param('doc','compiler::DocDef?',false),new sys.Param('facets','compiler::FacetDef[]?',false),new sys.Param('flags','sys::Int',false),new sys.Param('ret','compiler::TypeRef',false),new sys.Param('name','sys::Str',false)]),{}).am$('paramDef',2048,'compiler::ParamDef',xp,{}).am$('ctorChain',2048,'compiler::CallExpr',sys.List.make(sys.Param.type$,[new sys.Param('method','compiler::MethodDef',false)]),{}).am$('facets',2048,'compiler::FacetDef[]?',xp,{}).am$('block',2048,'compiler::Block',xp,{}).am$('stmtOrBlock',2048,'compiler::Block',xp,{}).am$('stmt',2048,'compiler::Stmt',xp,{}).am$('exprOrLocalDefStmt',2048,'compiler::Stmt',sys.List.make(sys.Param.type$,[new sys.Param('isEndOfStmt','sys::Bool',false)]),{}).am$('itAdd',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('e','compiler::Expr',false)]),{}).am$('localDefStmt',2048,'compiler::LocalDefStmt',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('localType','compiler::CType?',false),new sys.Param('isEndOfStmt','sys::Bool',false)]),{}).am$('ifStmt',2048,'compiler::IfStmt',xp,{}).am$('returnStmt',2048,'compiler::ReturnStmt',xp,{}).am$('throwStmt',2048,'compiler::ThrowStmt',xp,{}).am$('whileStmt',2048,'compiler::WhileStmt',xp,{}).am$('forStmt',2048,'compiler::ForStmt',xp,{}).am$('breakStmt',2048,'compiler::BreakStmt',xp,{}).am$('continueStmt',2048,'compiler::ContinueStmt',xp,{}).am$('tryStmt',2048,'compiler::TryStmt',xp,{}).am$('tryCatch',2048,'compiler::Catch',xp,{}).am$('switchStmt',2048,'compiler::SwitchStmt',xp,{}).am$('switchBlock',2048,'compiler::Block',xp,{}).am$('expr',2048,'compiler::Expr',xp,{}).am$('assignExpr',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr?',true)]),{}).am$('ifExpr',2048,'compiler::Expr',xp,{}).am$('ifExprBody',2048,'compiler::Expr',xp,{}).am$('condOrExpr',2048,'compiler::Expr',xp,{}).am$('condAndExpr',2048,'compiler::Expr',xp,{}).am$('equalityExpr',2048,'compiler::Expr',xp,{}).am$('relationalExpr',2048,'compiler::Expr',xp,{}).am$('rangeExpr',2048,'compiler::Expr',xp,{}).am$('addExpr',2048,'compiler::Expr',xp,{}).am$('multExpr',2048,'compiler::Expr',xp,{}).am$('parenExpr',2048,'compiler::Expr',xp,{}).am$('unaryExpr',2048,'compiler::Expr',xp,{}).am$('termExpr',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('target','compiler::Expr?',true)]),{}).am$('termBaseExpr',2048,'compiler::Expr',xp,{}).am$('typeBaseExpr',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ctype','compiler::CType',false)]),{}).am$('termChainExpr',2048,'compiler::Expr?',sys.List.make(sys.Param.type$,[new sys.Param('target','compiler::Expr',false)]),{}).am$('idExpr',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('target','compiler::Expr?',false),new sys.Param('dynamicCall','sys::Bool',false),new sys.Param('safeCall','sys::Bool',false)]),{}).am$('callExpr',2048,'compiler::CallExpr',sys.List.make(sys.Param.type$,[new sys.Param('target','compiler::Expr?',false)]),{}).am$('callArgs',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false),new sys.Param('closureOk','sys::Bool',true)]),{}).am$('callOp',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('target','compiler::Expr',false)]),{}).am$('indexExpr',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('target','compiler::Expr',false)]),{}).am$('collectionLiteralExpr',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('explicitType','compiler::CType?',false)]),{}).am$('listLiteralExpr',2048,'compiler::ListLiteralExpr',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('explicitType','compiler::CType?',false),new sys.Param('first','compiler::Expr?',false)]),{}).am$('mapLiteralExpr',2048,'compiler::MapLiteralExpr',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('explicitType','compiler::CType?',false),new sys.Param('first','compiler::Expr?',false)]),{}).am$('tryClosure',2048,'compiler::ClosureExpr?',xp,{}).am$('tryItBlock',2048,'compiler::ClosureExpr?',xp,{}).am$('closure',2048,'compiler::ClosureExpr',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('funcType','compiler::FuncType',false)]),{}).am$('complexLiteral',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('ctype','compiler::CType',false)]),{}).am$('typeRef',2048,'compiler::TypeRef',xp,{}).am$('tryType',2048,'compiler::CType?',xp,{}).am$('ctype',2048,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('isTypeRef','sys::Bool',true)]),{}).am$('simpleType',2048,'compiler::CType',xp,{}).am$('funcType',2048,'compiler::CType',sys.List.make(sys.Param.type$,[new sys.Param('isTypeRef','sys::Bool',false)]),{}).am$('funcTypeFormal',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('isTypeRef','sys::Bool',false),new sys.Param('params','compiler::CType[]',false),new sys.Param('names','sys::Str[]',false),new sys.Param('unnamed','sys::Bool[]',false)]),{}).am$('doc',2048,'compiler::DocDef?',xp,{}).am$('err',271360,'compiler::CompilerErr',sys.List.make(sys.Param.type$,[new sys.Param('msg','sys::Str',false),new sys.Param('loc','compiler::Loc?',true)]),{}).am$('consumeId',2048,'sys::Str',xp,{}).am$('verify',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('kind','compiler::Token',false)]),{}).am$('consume',2048,'compiler::TokenVal',sys.List.make(sys.Param.type$,[new sys.Param('kind','compiler::Token?',true)]),{}).am$('endOfStmt',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('errMsg','sys::Str?',true)]),{}).am$('reset',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('pos','sys::Int',false)]),{}).am$('static$init',165890,'sys::Void',xp,{});
Token.type$.af$('identifier',106506,'compiler::Token',{}).af$('strLiteral',106506,'compiler::Token',{}).af$('intLiteral',106506,'compiler::Token',{}).af$('floatLiteral',106506,'compiler::Token',{}).af$('decimalLiteral',106506,'compiler::Token',{}).af$('durationLiteral',106506,'compiler::Token',{}).af$('uriLiteral',106506,'compiler::Token',{}).af$('dsl',106506,'compiler::Token',{}).af$('localeLiteral',106506,'compiler::Token',{}).af$('dot',106506,'compiler::Token',{}).af$('semicolon',106506,'compiler::Token',{}).af$('comma',106506,'compiler::Token',{}).af$('colon',106506,'compiler::Token',{}).af$('doubleColon',106506,'compiler::Token',{}).af$('plus',106506,'compiler::Token',{}).af$('minus',106506,'compiler::Token',{}).af$('star',106506,'compiler::Token',{}).af$('slash',106506,'compiler::Token',{}).af$('percent',106506,'compiler::Token',{}).af$('pound',106506,'compiler::Token',{}).af$('increment',106506,'compiler::Token',{}).af$('decrement',106506,'compiler::Token',{}).af$('bang',106506,'compiler::Token',{}).af$('question',106506,'compiler::Token',{}).af$('tilde',106506,'compiler::Token',{}).af$('pipe',106506,'compiler::Token',{}).af$('amp',106506,'compiler::Token',{}).af$('caret',106506,'compiler::Token',{}).af$('at',106506,'compiler::Token',{}).af$('doublePipe',106506,'compiler::Token',{}).af$('doubleAmp',106506,'compiler::Token',{}).af$('same',106506,'compiler::Token',{}).af$('notSame',106506,'compiler::Token',{}).af$('eq',106506,'compiler::Token',{}).af$('notEq',106506,'compiler::Token',{}).af$('cmp',106506,'compiler::Token',{}).af$('lt',106506,'compiler::Token',{}).af$('ltEq',106506,'compiler::Token',{}).af$('gt',106506,'compiler::Token',{}).af$('gtEq',106506,'compiler::Token',{}).af$('lbrace',106506,'compiler::Token',{}).af$('rbrace',106506,'compiler::Token',{}).af$('lparen',106506,'compiler::Token',{}).af$('rparen',106506,'compiler::Token',{}).af$('lbracket',106506,'compiler::Token',{}).af$('rbracket',106506,'compiler::Token',{}).af$('dotDot',106506,'compiler::Token',{}).af$('dotDotLt',106506,'compiler::Token',{}).af$('defAssign',106506,'compiler::Token',{}).af$('assign',106506,'compiler::Token',{}).af$('assignPlus',106506,'compiler::Token',{}).af$('assignMinus',106506,'compiler::Token',{}).af$('assignStar',106506,'compiler::Token',{}).af$('assignSlash',106506,'compiler::Token',{}).af$('assignPercent',106506,'compiler::Token',{}).af$('arrow',106506,'compiler::Token',{}).af$('elvis',106506,'compiler::Token',{}).af$('safeDot',106506,'compiler::Token',{}).af$('safeArrow',106506,'compiler::Token',{}).af$('docComment',106506,'compiler::Token',{}).af$('dollar',106506,'compiler::Token',{}).af$('lparenSynthetic',106506,'compiler::Token',{}).af$('abstractKeyword',106506,'compiler::Token',{}).af$('asKeyword',106506,'compiler::Token',{}).af$('assertKeyword',106506,'compiler::Token',{}).af$('breakKeyword',106506,'compiler::Token',{}).af$('caseKeyword',106506,'compiler::Token',{}).af$('catchKeyword',106506,'compiler::Token',{}).af$('classKeyword',106506,'compiler::Token',{}).af$('constKeyword',106506,'compiler::Token',{}).af$('continueKeyword',106506,'compiler::Token',{}).af$('defaultKeyword',106506,'compiler::Token',{}).af$('doKeyword',106506,'compiler::Token',{}).af$('elseKeyword',106506,'compiler::Token',{}).af$('falseKeyword',106506,'compiler::Token',{}).af$('finalKeyword',106506,'compiler::Token',{}).af$('finallyKeyword',106506,'compiler::Token',{}).af$('forKeyword',106506,'compiler::Token',{}).af$('foreachKeyword',106506,'compiler::Token',{}).af$('ifKeyword',106506,'compiler::Token',{}).af$('internalKeyword',106506,'compiler::Token',{}).af$('isKeyword',106506,'compiler::Token',{}).af$('isnotKeyword',106506,'compiler::Token',{}).af$('itKeyword',106506,'compiler::Token',{}).af$('mixinKeyword',106506,'compiler::Token',{}).af$('nativeKeyword',106506,'compiler::Token',{}).af$('newKeyword',106506,'compiler::Token',{}).af$('nullKeyword',106506,'compiler::Token',{}).af$('onceKeyword',106506,'compiler::Token',{}).af$('overrideKeyword',106506,'compiler::Token',{}).af$('privateKeyword',106506,'compiler::Token',{}).af$('protectedKeyword',106506,'compiler::Token',{}).af$('publicKeyword',106506,'compiler::Token',{}).af$('readonlyKeyword',106506,'compiler::Token',{}).af$('returnKeyword',106506,'compiler::Token',{}).af$('staticKeyword',106506,'compiler::Token',{}).af$('superKeyword',106506,'compiler::Token',{}).af$('switchKeyword',106506,'compiler::Token',{}).af$('thisKeyword',106506,'compiler::Token',{}).af$('throwKeyword',106506,'compiler::Token',{}).af$('trueKeyword',106506,'compiler::Token',{}).af$('tryKeyword',106506,'compiler::Token',{}).af$('usingKeyword',106506,'compiler::Token',{}).af$('virtualKeyword',106506,'compiler::Token',{}).af$('volatileKeyword',106506,'compiler::Token',{}).af$('voidKeyword',106506,'compiler::Token',{}).af$('whileKeyword',106506,'compiler::Token',{}).af$('eof',106506,'compiler::Token',{}).af$('vals',106498,'compiler::Token[]',{}).af$('keywords',106498,'[sys::Str:compiler::Token]',{}).af$('symbol',73730,'sys::Str',{}).af$('keyword',73730,'sys::Bool',{}).af$('isAssign',73730,'sys::Bool',{}).am$('make',2052,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('$ordinal','sys::Int',false),new sys.Param('$name','sys::Str',false),new sys.Param('symbol','sys::Str?',true)]),{}).am$('toExprId',8192,'compiler::ExprId',xp,{}).am$('toShortcutOp',8192,'compiler::ShortcutOp',sys.List.make(sys.Param.type$,[new sys.Param('degree','sys::Int',false)]),{}).am$('isProtectionKeyword',8192,'sys::Bool',xp,{}).am$('isIncrementOrDecrement',8192,'sys::Bool',xp,{}).am$('toStr',271360,'sys::Str',xp,{}).am$('main',40962,'sys::Void',xp,{}).am$('fromStr',40966,'compiler::Token?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('checked','sys::Bool',true)]),{}).am$('static$init',165890,'sys::Void',xp,{});
CompileJs.type$.af$('hasJs',67586,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('compile',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('qname','sys::Str',false)]),{}).am$('needCompileEs',8192,'sys::Bool',xp,{}).am$('needCompileJs',8192,'sys::Bool',xp,{});
InitInput.type$.af$('loc',67584,'compiler::Loc',{}).af$('input',67584,'compiler::CompilerInput',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('validateInput',2048,'sys::Void',xp,{}).am$('validatePodName',2048,'sys::Void',xp,{}).am$('isValidPodName',40962,'sys::Str?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{'sys::NoDoc':""}).am$('initNamespace',2048,'sys::Void',xp,{}).am$('initPod',2048,'sys::Void',xp,{}).am$('initDepends',2048,'sys::Void',xp,{}).am$('initFiles',2048,'sys::Void',xp,{}).am$('findFiles',2048,'sys::File[]',sys.List.make(sys.Param.type$,[new sys.Param('uris','sys::Uri[]?',false),new sys.Param('ext','sys::Str?',false)]),{});
DefaultCtor.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('visitTypeDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('addDefaultCtor',40962,'compiler::MethodDef',sys.List.make(sys.Param.type$,[new sys.Param('parent','compiler::TypeDef',false),new sys.Param('flags','sys::Int',false)]),{});
ResolveImports.type$.af$('resolved',73728,'[sys::Str:compiler::CPod]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('resolveImports',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('unit','compiler::CompilationUnit',false)]),{}).am$('resolveImportedTypes',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('unit','compiler::CompilationUnit',false)]),{}).am$('addAll',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('types','[sys::Str:compiler::CType[]]',false),new sys.Param('toAdd','compiler::CType[]',false)]),{}).am$('remove',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('types','[sys::Str:compiler::CType[]]',false),new sys.Param('t','compiler::CType',false)]),{}).am$('resolveQualified',40962,'compiler::CType?',sys.List.make(sys.Param.type$,[new sys.Param('cs','compiler::CompilerSupport',false),new sys.Param('podName','sys::Str',false),new sys.Param('typeName','sys::Str',false),new sys.Param('loc','compiler::Loc',false)]),{}).am$('resolvePod',40962,'compiler::CPod?',sys.List.make(sys.Param.type$,[new sys.Param('cs','compiler::CompilerSupport',false),new sys.Param('podName','sys::Str',false),new sys.Param('loc','compiler::Loc',false)]),{}).am$('checkUsingPod',34818,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('cs','compiler::CompilerSupport',false),new sys.Param('podName','sys::Str',false),new sys.Param('loc','compiler::Loc',false)]),{});
Normalize.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('visitTypeDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('appendStaticInit',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('sInit','compiler::Block',false),new sys.Param('m','compiler::MethodDef',false)]),{}).am$('normalizeMethod',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false),new sys.Param('iInit','compiler::Block',false),new sys.Param('sInit','compiler::Block',false)]),{}).am$('addImplicitReturn',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false)]),{}).am$('insertSuperCtor',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false)]),{}).am$('normalizeOnce',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false),new sys.Param('iInit','compiler::Block',false),new sys.Param('sInit','compiler::Block',false)]),{}).am$('callInstanceInit',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false),new sys.Param('ii','compiler::MethodDef',false)]),{}).am$('normalizeField',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{}).am$('inferFieldListType',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{}).am$('inferFieldMapType',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{}).am$('genSyntheticOverrideGet',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{}).am$('genSyntheticOverrideSet',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{}).am$('fieldInitStmt',34818,'compiler::ExprStmt',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{});
InitEnum.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('visitTypeDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('addCtor',8192,'sys::Void',xp,{}).am$('addFromStr',8192,'sys::Void',xp,{}).am$('makeField',8192,'compiler::FieldDef',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::EnumDef',false)]),{}).am$('makeValsField',8192,'compiler::FieldDef',xp,{});
CheckErrors.type$.af$('protectedRegionDepth',67584,'sys::Int',{}).af$('finallyDepth',67584,'sys::Int',{}).af$('isSys',67584,'sys::Bool',{}).af$('coercer',67584,'compiler::Coercer',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('checkPodDef',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('pod','compiler::PodDef',false)]),{}).am$('visitTypeDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('isRestrictedName',40962,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false)]),{}).am$('checkResConflicts',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('checkTypeFlags',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('checkAbstractSlots',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('checkConstType',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('checkBase',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false),new sys.Param('base','compiler::CType',false)]),{}).am$('checkMixin',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false),new sys.Param('m','compiler::CType',false)]),{}).am$('visitFieldDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{}).am$('checkFieldFlags',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{}).am$('visitMethodDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false)]),{}).am$('checkMethodFlags',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false)]),{}).am$('checkParams',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false)]),{}).am$('checkParam',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('p','compiler::ParamDef',false)]),{}).am$('checkParamFuncType',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('param','compiler::ParamDef',false),new sys.Param('t','compiler::FuncType',false)]),{}).am$('checkMethodReturn',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false)]),{}).am$('checkInstanceCtor',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false)]),{}).am$('checkDefiniteAssign',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef?',false)]),{}).am$('checkOperatorMethod',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false)]),{}).am$('checkFacets',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('facets','compiler::FacetDef[]?',false)]),{}).am$('checkFacet',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FacetDef',false)]),{}).am$('enterStmt',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::Stmt',false)]),{}).am$('exitStmt',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::Stmt',false)]),{}).am$('enterFinally',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::TryStmt',false)]),{}).am$('exitFinally',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::TryStmt',false)]),{}).am$('visitStmt',271360,'compiler::Stmt[]?',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::Stmt',false)]),{}).am$('checkExprStmt',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::ExprStmt',false)]),{}).am$('checkLocalDef',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::LocalDefStmt',false)]),{}).am$('checkIf',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::IfStmt',false)]),{}).am$('checkThrow',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::ThrowStmt',false)]),{}).am$('checkThrowExpr',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::ThrowExpr',false)]),{}).am$('checkFor',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::ForStmt',false)]),{}).am$('checkWhile',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::WhileStmt',false)]),{}).am$('checkBreak',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::BreakStmt',false)]),{}).am$('checkContinue',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::ContinueStmt',false)]),{}).am$('checkReturn',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::ReturnStmt',false)]),{}).am$('checkTry',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::TryStmt',false)]),{}).am$('checkSwitch',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::SwitchStmt',false)]),{}).am$('visitExpr',271360,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false)]),{}).am$('checkTypeLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::LiteralExpr',false)]),{}).am$('checkSlotLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::SlotLiteralExpr',false)]),{}).am$('checkListLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::ListLiteralExpr',false)]),{}).am$('checkMapLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::MapLiteralExpr',false)]),{}).am$('checkRangeLiteral',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('range','compiler::RangeLiteralExpr',false)]),{}).am$('checkBool',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::UnaryExpr',false)]),{}).am$('checkCompareNull',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::UnaryExpr',false)]),{}).am$('checkBools',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::CondExpr',false)]),{}).am$('checkSame',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::BinaryExpr',false)]),{}).am$('checkCompare',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('lhs','compiler::Expr',false),new sys.Param('rhs','compiler::Expr',false)]),{}).am$('checkAssign',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::BinaryExpr',false)]),{}).am$('checkElvis',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::BinaryExpr',false)]),{}).am$('checkNoNullSafes',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('x','compiler::Expr?',false)]),{}).am$('checkShortcut',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('shortcut','compiler::ShortcutExpr',false)]),{}).am$('checkAssignField',2048,'compiler::Expr?',sys.List.make(sys.Param.type$,[new sys.Param('lhs','compiler::FieldExpr',false),new sys.Param('rhs','compiler::Expr?',false)]),{}).am$('implicitToImmutable',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('fieldType','compiler::CType',false),new sys.Param('rhs','compiler::Expr',false)]),{}).am$('checkConstruction',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false)]),{}).am$('checkCall',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false)]),{}).am$('checkField',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldExpr',false)]),{}).am$('useFieldAccessor',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::CField',false)]),{}).am$('checkThis',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::ThisExpr',false)]),{}).am$('checkSuper',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::SuperExpr',false)]),{}).am$('checkTypeCheck',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::TypeCheckExpr',false)]),{}).am$('checkTernary',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::TernaryExpr',false)]),{}).am$('checkArgs',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false)]),{}).am$('paramTypeStr',32898,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('base','compiler::CType',false),new sys.Param('param','compiler::CParam',false)]),{}).am$('checkValidType',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('t','compiler::CType',false)]),{}).am$('checkProtectionFlags',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('flags','sys::Int',false),new sys.Param('loc','compiler::Loc',false)]),{}).am$('checkTypeProtection',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::CType',false),new sys.Param('loc','compiler::Loc',false)]),{}).am$('checkSlotProtection',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('slot','compiler::CSlot',false),new sys.Param('loc','compiler::Loc',false),new sys.Param('setter','sys::Bool',true)]),{}).am$('isSlotVisible',40962,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('curType','compiler::TypeDef',false),new sys.Param('slot','compiler::CSlot',false)]),{}).am$('slotProtectionErr',34818,'sys::Str?',sys.List.make(sys.Param.type$,[new sys.Param('curType','compiler::TypeDef',false),new sys.Param('slot','compiler::CSlot',false),new sys.Param('setter','sys::Bool',true)]),{}).am$('checkDeprecated',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('target','sys::Obj',false),new sys.Param('loc','compiler::Loc',false)]),{}).am$('box',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false)]),{}).am$('coerceBoxed',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false),new sys.Param('expected','compiler::CType',false),new sys.Param('onErr','|->sys::Void|',false)]),{}).am$('coerce',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false),new sys.Param('expected','compiler::CType',false),new sys.Param('onErr','|->sys::Void|',false)]),{});
ResolveDepends.type$.af$('loc',73728,'compiler::Loc',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('resolveDepend',2048,'compiler::CPod?',sys.List.make(sys.Param.type$,[new sys.Param('depend','sys::Depend',false)]),{});
ResolveExpr.type$.af$('stmtStack',73728,'compiler::Stmt[]',{}).af$('blockStack',73728,'compiler::Block[]',{}).af$('inClosure',73728,'sys::Bool',{}).af$('coercer',73728,'compiler::Coercer',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('enterMethodDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false)]),{}).am$('enterStmt',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::Stmt',false)]),{}).am$('visitStmt',271360,'compiler::Stmt[]?',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::Stmt',false)]),{}).am$('resolveExprStmt',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::ExprStmt',false)]),{}).am$('resolveLocalVarDef',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::LocalDefStmt',false)]),{}).am$('resolveFor',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::ForStmt',false)]),{}).am$('resolveBreak',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::BreakStmt',false)]),{}).am$('resolveContinue',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::ContinueStmt',false)]),{}).am$('visitExpr',271360,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false)]),{}).am$('resolveExpr',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false)]),{}).am$('resolveLocaleLiteral',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::LocaleLiteralExpr',false)]),{}).am$('resolveSlotLiteral',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::SlotLiteralExpr',false)]),{}).am$('resolveList',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::ListLiteralExpr',false)]),{}).am$('resolveMap',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::MapLiteralExpr',false)]),{}).am$('resolveThis',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::ThisExpr',false)]),{}).am$('resolveSuper',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::SuperExpr',false)]),{}).am$('resolveIt',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::ItExpr',false)]),{}).am$('resolveAssign',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::BinaryExpr',false)]),{}).am$('resolveVar',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('var','compiler::UnknownVarExpr',false)]),{}).am$('resolveStorage',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('var','compiler::UnknownVarExpr',false)]),{}).am$('resolveElvis',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::BinaryExpr',false)]),{}).am$('resolveTernary',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::TernaryExpr',false)]),{}).am$('resolveCall',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false)]),{}).am$('resolveTrapCall',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false)]),{}).am$('resolveCallOnLocalVar',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false),new sys.Param('binding','compiler::LocalVarExpr',false)]),{}).am$('resolveConstruction',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false)]),{}).am$('findCtorMatches',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('matches','[sys::Str:compiler::CMethod]',false),new sys.Param('base','compiler::CType',false),new sys.Param('args','compiler::Expr[]',false)]),{}).am$('isCtorMethod',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('slot','compiler::CSlot',false)]),{}).am$('resolveShortcut',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::ShortcutExpr',false)]),{}).am$('resolveBinaryOperator',2048,'compiler::CMethod?',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::ShortcutExpr',false)]),{}).am$('resolveIndexedAssign',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('orig','compiler::ShortcutExpr',false)]),{}).am$('resolveClosure',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::ClosureExpr',false)]),{}).am$('resolveDsl',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::DslExpr',false)]),{}).am$('initMethodVars',2048,'sys::Void',xp,{}).am$('bindToMethodVar',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('def','compiler::LocalDefStmt',false)]),{}).am$('resolveLocal',2048,'compiler::MethodVar?',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('loc','compiler::Loc',false)]),{}).am$('localsInScope',2048,'[sys::Str:compiler::MethodVar]',xp,{}).am$('currentBlock',2048,'compiler::Block',xp,{}).am$('isBlockInScope',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('block','compiler::Block?',false)]),{}).am$('findLoop',2048,'compiler::Stmt?',xp,{}).am$('enterBlock',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('block','compiler::Block',false)]),{}).am$('exitBlock',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('block','compiler::Block',false)]),{});
ClosureVars.type$.af$('syntheticFieldFlags',100354,'sys::Int',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('scanType',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('processMethod',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('method','compiler::MethodDef',false)]),{}).am$('wrapNonFinalVars',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false)]),{}).am$('walkMethod',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('method','compiler::MethodDef',false)]),{}).am$('visitStmt',271360,'compiler::Stmt[]?',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::Stmt',false)]),{}).am$('visitExpr',271360,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false)]),{}).am$('fixLocalDef',2048,'compiler::Stmt[]?',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::LocalDefStmt',false)]),{}).am$('initWrapper',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('var','compiler::MethodVar',false),new sys.Param('init','compiler::Expr',false)]),{}).am$('fixWrappedVar',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('local','compiler::LocalVarExpr',false)]),{}).am$('fixWrappedParams',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('method','compiler::MethodDef',false)]),{}).am$('processClosure',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('closure','compiler::ClosureExpr',false)]),{}).am$('addVarToClosure',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('closure','compiler::ClosureExpr',false),new sys.Param('var','compiler::MethodVar',false),new sys.Param('name','sys::Str',false)]),{}).am$('makeOuterThisField',40962,'compiler::CField',sys.List.make(sys.Param.type$,[new sys.Param('closure','compiler::ClosureExpr',false)]),{}).am$('addToClosure',34818,'compiler::FieldDef',sys.List.make(sys.Param.type$,[new sys.Param('closure','compiler::ClosureExpr',false),new sys.Param('name','sys::Str',false),new sys.Param('subtituteArg','compiler::Expr',false),new sys.Param('info','sys::Str',false)]),{}).am$('genWrapper',40962,'compiler::CField',sys.List.make(sys.Param.type$,[new sys.Param('cs','compiler::CompilerSupport',false),new sys.Param('ctype','compiler::CType',false)]),{}).am$('toSafe',34818,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('n','sys::Str',false)]),{}).am$('fieldExpr',34818,'compiler::FieldExpr',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('target','compiler::Expr',false),new sys.Param('field','compiler::CField',false)]),{}).am$('static$init',165890,'sys::Void',xp,{});
Parse.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{});
CheckInheritance.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('visitTypeDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('checkOutOfOrder',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('checkExtends',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false),new sys.Param('base','compiler::CType?',false)]),{}).am$('checkMixin',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false),new sys.Param('m','compiler::CType',false)]),{});
Tokenize.type$.af$('input',73728,'compiler::CompilerInput',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('runStrMode',2048,'sys::Void',xp,{}).am$('runFileMode',2048,'sys::Void',xp,{}).am$('tokenize',8192,'compiler::CompilationUnit',sys.List.make(sys.Param.type$,[new sys.Param('loc','compiler::Loc',false),new sys.Param('src','sys::Str',false)]),{});
LoadPod.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('load',8192,'sys::Pod',xp,{});
Inherit.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('visitTypeDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('inheritType',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false),new sys.Param('parent','compiler::CType?',false)]),{}).am$('inheritSlot',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false),new sys.Param('newSlot','compiler::CSlot',false)]),{}).am$('matchingSignatures',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('a','compiler::CSlot',false),new sys.Param('b','compiler::CSlot',false)]),{}).am$('keep',2048,'compiler::CSlot?',sys.List.make(sys.Param.type$,[new sys.Param('a','compiler::CSlot',false),new sys.Param('b','compiler::CSlot',false)]),{}).am$('checkOverride',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false),new sys.Param('base','compiler::CSlot',false),new sys.Param('def','compiler::SlotDef',false)]),{}).am$('isOverrideProtectionErr',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('base','compiler::CSlot',false),new sys.Param('def','compiler::SlotDef',false)]),{}).am$('checkMethodMethodOverride',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false),new sys.Param('base','compiler::CMethod',false),new sys.Param('def','compiler::MethodDef',false)]),{}).am$('checkMethodFieldOverride',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false),new sys.Param('base','compiler::CMethod',false),new sys.Param('def','compiler::FieldDef',false)]),{}).am$('checkFieldFieldOverride',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false),new sys.Param('base','compiler::CField',false),new sys.Param('def','compiler::FieldDef',false)]),{});
InitFacet.type$.af$('ctors',73728,'compiler::MethodDef[]?',{}).af$('fields',73728,'compiler::FieldDef[]?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('visitTypeDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('initCurType',2048,'sys::Void',xp,{}).am$('errorChecks',2048,'sys::Void',xp,{}).am$('initSingleton',2048,'sys::Void',xp,{}).am$('initStruct',2048,'sys::Void',xp,{});
LocaleProps.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{});
GenerateOutput.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{});
CheckParamDefs.type$.af$('name',73728,'sys::Str?',{}).af$('used',73728,'sys::Bool',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('visitMethodDef',271360,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false)]),{}).am$('usedInSuccDef',8192,'compiler::ParamDef?',sys.List.make(sys.Param.type$,[new sys.Param('params','compiler::ParamDef[]',false),new sys.Param('index','sys::Int',false)]),{}).am$('visitExpr',271360,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false)]),{});
Assemble.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{});
InitClosures.type$.af$('closure',73728,'compiler::ClosureExpr?',{}).af$('loc',73728,'compiler::Loc?',{}).af$('signature',73728,'compiler::FuncType?',{}).af$('enclosingType',73728,'compiler::TypeDef?',{}).af$('cls',73728,'compiler::TypeDef?',{}).af$('ctor',73728,'compiler::MethodDef?',{}).af$('doCall',73728,'compiler::MethodDef?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('process',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('c','compiler::ClosureExpr',false)]),{}).am$('setup',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('c','compiler::ClosureExpr',false)]),{}).am$('genClass',2048,'sys::Void',xp,{}).am$('genCtor',2048,'sys::Void',xp,{}).am$('genDoCall',2048,'sys::Void',xp,{}).am$('genCall',2048,'sys::Void',xp,{}).am$('genMethodCall',40962,'compiler::MethodDef',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false),new sys.Param('loc','compiler::Loc',false),new sys.Param('parent','compiler::TypeDef',false),new sys.Param('signature','compiler::FuncType',false),new sys.Param('c','compiler::CallExpr',false),new sys.Param('firstAsTarget','sys::Bool',false)]),{}).am$('substitute',2048,'sys::Void',xp,{});
ScanForUsingsAndTypes.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{});
UsingAndTypeScanner.type$.af$('unit',67584,'compiler::CompilationUnit',{}).af$('tokens',67584,'compiler::TokenVal[]',{}).af$('pos',67584,'sys::Int',{}).af$('isSys',67584,'sys::Bool',{}).af$('allTypes',67584,'[sys::Str:compiler::TypeDef]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false),new sys.Param('unit','compiler::CompilationUnit',false),new sys.Param('allTypes','[sys::Str:compiler::TypeDef]',false)]),{}).am$('parse',8192,'sys::Void',xp,{}).am$('parseUsing',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('tok','compiler::TokenVal',false)]),{}).am$('parseType',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('tok','compiler::TokenVal',false)]),{}).am$('consumeId',2048,'sys::Str',xp,{}).am$('verify',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('expected','compiler::Token',false)]),{}).am$('consume',2048,'compiler::TokenVal',sys.List.make(sys.Param.type$,[new sys.Param('expected','compiler::Token?',true)]),{}).am$('curt',2048,'compiler::Token',xp,{});
ClosureToImmutable.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('process',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('closure','compiler::ClosureExpr',false)]),{}).am$('isAlwaysImmutable',8192,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('cls','compiler::TypeDef',false)]),{}).am$('isNeverImmutable',8192,'sys::Str?',sys.List.make(sys.Param.type$,[new sys.Param('cls','compiler::TypeDef',false)]),{}).am$('setAllFieldsConst',8192,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('cls','compiler::TypeDef',false)]),{}).am$('genIsImmutable',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('cls','compiler::TypeDef',false),new sys.Param('result','compiler::Expr',false)]),{}).am$('genToImmutableErr',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('cls','compiler::TypeDef',false),new sys.Param('msg','sys::Str',false)]),{}).am$('genToImmutable',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('cls','compiler::TypeDef',false)]),{}).am$('stubToImmutable',2048,'compiler::MethodDef',sys.List.make(sys.Param.type$,[new sys.Param('cls','compiler::TypeDef',false)]),{});
ConstChecks.type$.af$('curCtor',73728,'compiler::MethodDef?',{}).af$('fieldCheck',73728,'compiler::MethodDef?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('processClosure',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('c','compiler::ClosureExpr',false)]),{}).am$('processCtor',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('ctor','compiler::MethodDef',false)]),{}).am$('visitStmt',271360,'compiler::Stmt[]?',sys.List.make(sys.Param.type$,[new sys.Param('stmt','compiler::Stmt',false)]),{}).am$('itBlockVar',2048,'compiler::MethodVar',xp,{}).am$('genFieldCheck',2048,'compiler::MethodDef?',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{});
WritePod.type$.af$('loc',67584,'compiler::Loc?',{}).af$('noFacets',67584,'compiler::FacetDef[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('write',8192,'sys::File',xp,{}).am$('writeStr',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('zip','sys::Zip',false),new sys.Param('path','sys::Uri',false),new sys.Param('content','sys::Str',false)]),{}).am$('writeRes',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('zip','sys::Zip',false),new sys.Param('file','sys::File',false),new sys.Param('path','sys::Uri?',true)]),{}).am$('writeResZip',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('zip','sys::Zip',false),new sys.Param('resFile','sys::File',false)]),{}).am$('writeSrc',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('zip','sys::Zip',false),new sys.Param('file','sys::File',false)]),{}).am$('writeDocs',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('zip','sys::Zip',false)]),{}).am$('writePodDocs',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('zip','sys::Zip',false)]),{}).am$('writeApiDoc',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('zip','sys::Zip',false),new sys.Param('t','compiler::TypeDef',false)]),{});
OrderByInheritance.type$.af$('processing',73728,'[sys::Str:compiler::TypeDef]',{}).af$('todo',73728,'[sys::Str:compiler::TypeDef]',{}).af$('ordered',73728,'compiler::TypeDef[]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('run',271360,'sys::Void',xp,{}).am$('process',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::CType',false)]),{});
ConstantFolder.type$.af$('never',100354,'[sys::Str:sys::Int]',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false)]),{}).am$('fold',8192,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false)]),{}).am$('exprToConst',2048,'sys::Obj?',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr?',false)]),{}).am$('constToExpr',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('orig','compiler::Expr',false),new sys.Param('val','sys::Obj?',false)]),{}).am$('static$init',165890,'sys::Void',xp,{});
ApiDocWriter.type$.af$('typePrefix',100354,'sys::Str',{}).af$('slotPrefix',100354,'sys::Str',{}).af$('protectionMask',100354,'sys::Int',{}).af$('out',73728,'sys::OutStream',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('out','sys::OutStream',false)]),{}).am$('close',8192,'sys::Bool',xp,{}).am$('writeType',8192,'sys::This',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('writeSlot',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('s','compiler::SlotDef',false)]),{}).am$('writeFieldStart',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('f','compiler::FieldDef',false)]),{}).am$('writeMethodStart',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('m','compiler::MethodDef',false)]),{}).am$('writeAttr',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('name','sys::Str',false),new sys.Param('val','sys::Obj?',false)]),{}).am$('writeFacets',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('facets','compiler::FacetDef[]?',false)]),{}).am$('writeFacet',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('facet','compiler::FacetDef',false)]),{}).am$('writeDoc',2048,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('node','compiler::DefNode',false)]),{}).am$('w',8192,'sys::This',sys.List.make(sys.Param.type$,[new sys.Param('x','sys::Str',false)]),{}).am$('encodeBase',2048,'sys::Str?',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('encodeMixins',2048,'sys::Str?',sys.List.make(sys.Param.type$,[new sys.Param('t','compiler::TypeDef',false)]),{}).am$('encodeLoc',2048,'sys::Str?',sys.List.make(sys.Param.type$,[new sys.Param('n','compiler::DefNode',false),new sys.Param('includeFile','sys::Bool',false)]),{}).am$('encodeExpr',34818,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false)]),{}).am$('encodeFlags',34818,'sys::Str',sys.List.make(sys.Param.type$,[new sys.Param('flags','sys::Int',false)]),{}).am$('static$init',165890,'sys::Void',xp,{});
Coercer.type$.am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('c','compiler::Compiler',false)]),{}).am$('box',8192,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false)]),{}).am$('coerceBoxed',8192,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false),new sys.Param('expected','compiler::CType',false),new sys.Param('onErr','|->sys::Void|',false)]),{}).am$('canCoerce',8192,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false),new sys.Param('expected','compiler::CType',false)]),{}).am$('coerce',8192,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false),new sys.Param('expected','compiler::CType',false),new sys.Param('onErr','|->sys::Void|',false)]),{}).am$('doCoerce',8192,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('expr','compiler::Expr',false),new sys.Param('expected','compiler::CType',false),new sys.Param('onErr','|->sys::Void|',false)]),{}).am$('coerceInheritedReturns',2048,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('call','compiler::CallExpr',false),new sys.Param('expected','compiler::CType',false)]),{}).am$('isFuncAutoCoerce',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('actualType','compiler::CType',false),new sys.Param('expectedType','compiler::CType',false)]),{}).am$('isFuncAutoCoerceMatch',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('actual','compiler::CType',false),new sys.Param('expected','compiler::CType',false)]),{}).am$('forceCoerce',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('from','compiler::CType',false),new sys.Param('to','compiler::CType',false)]),{}).am$('forceParameterizedCollectionCoerce',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('from','compiler::CType',false),new sys.Param('to','compiler::CType',false)]),{});
CallResolver.type$.af$('curType',73728,'compiler::TypeDef?',{}).af$('curMethod',73728,'compiler::MethodDef?',{}).af$('expr',73728,'compiler::NameExpr',{}).af$('loc',73728,'compiler::Loc',{}).af$('target',73728,'compiler::Expr?',{}).af$('name',73728,'sys::Str',{}).af$('isItAdd',73728,'sys::Bool',{}).af$('isVar',73728,'sys::Bool',{}).af$('isFuncFieldCall',73728,'sys::Bool',{}).af$('args',73728,'compiler::Expr[]',{}).af$('base',73728,'compiler::CType?',{}).af$('baseIt',73728,'compiler::CType?',{}).af$('found',73728,'compiler::CSlot?',{}).af$('foundOnIt',73728,'sys::Bool',{}).af$('result',73728,'compiler::Expr?',{}).am$('make',8196,'sys::Void',sys.List.make(sys.Param.type$,[new sys.Param('compiler','compiler::Compiler',false),new sys.Param('curType','compiler::TypeDef?',false),new sys.Param('curMethod','compiler::MethodDef?',false),new sys.Param('expr','compiler::NameExpr',false)]),{}).am$('resolve',8192,'compiler::Expr',xp,{}).am$('isStaticLiteral',8192,'sys::Bool',xp,{}).am$('resolveBase',8192,'sys::Void',xp,{}).am$('find',8192,'sys::Void',xp,{}).am$('findOn',2048,'compiler::CSlot?',sys.List.make(sys.Param.type$,[new sys.Param('base','compiler::CType',false)]),{}).am$('isAmbiguous',2048,'sys::Bool',sys.List.make(sys.Param.type$,[new sys.Param('onBase','compiler::CSlot?',false),new sys.Param('onIt','compiler::CSlot?',false)]),{}).am$('errSig',2048,'sys::Str',xp,{}).am$('insertImplicitThisOrIt',2048,'sys::Void',xp,{}).am$('resolveToExpr',2048,'sys::Void',xp,{}).am$('resolveToCallExpr',2048,'compiler::CallExpr',xp,{}).am$('resolveToFieldExpr',2048,'compiler::FieldExpr',xp,{}).am$('inferClosureType',2048,'sys::Void',xp,{}).am$('inferClosureTypeFromCall',40962,'compiler::Expr',sys.List.make(sys.Param.type$,[new sys.Param('support','compiler::CompilerSupport',false),new sys.Param('call','compiler::CallExpr',false),new sys.Param('base','compiler::CType',false)]),{}).am$('resolveForeign',2048,'sys::Void',xp,{}).am$('constantFolding',2048,'sys::Void',xp,{}).am$('castForThisType',2048,'sys::Void',xp,{}).am$('safeToNullable',2048,'sys::Void',xp,{}).am$('ffiCoercion',2048,'sys::Void',xp,{});

m=sys.Map.make(sys.Str.type$,sys.Str.type$);
m.set("pod.name", "compiler");
m.set("pod.version", "1.0.83");
m.set("pod.depends", "sys 1.0");
m.set("pod.summary", "Fantom compiler");
m.set("pod.isScript", "false");
m.set("fcode.version", "1.0.51");
m.set("build.host", "");
m.set("build.user", "");
m.set("build.ts", "");
m.set("build.tsKey", "");
m.set("build.compiler", "1.0.83");
m.set("build.platform", "");
m.set("pod.docSrc", "true");
m.set("license.name", "Academic Free License 3.0");
m.set("org.name", "Fantom");
m.set("pod.native.dotnet", "false");
m.set("proj.name", "Fantom Core");
m.set("proj.uri", "https://fantom.org/");
m.set("pod.docApi", "true");
m.set("org.uri", "https://fantom.org/");
m.set("pod.native.java", "false");
m.set("vcs.uri", "https://github.com/fantom-lang/fantom");
m.set("pod.native.jni", "false");
m.set("vcs.name", "Git");
m.set("pod.native.js", "false");
p.__meta(m);



// cjs exports begin
export {
  CompilerErr,
  CompilerLog,
  Loc,
  Compiler,
  CompilerSupport,
  Visitor,
  CompilerStep,
  CompilerInput,
  CompilerInputMode,
  CompilerOutput,
  CompilerOutputMode,
  Main,
  AttrAsm,
  FConst,
  Assembler,
  CodeAsm,
  Loop,
  ProtectedRegion,
  Cond,
  Node,
  CNode,
  DefNode,
  CDoc,
  DocDef,
  Block,
  Expr,
  LiteralExpr,
  LocaleLiteralExpr,
  SlotLiteralExpr,
  RangeLiteralExpr,
  ListLiteralExpr,
  MapLiteralExpr,
  UnaryExpr,
  BinaryExpr,
  CondExpr,
  NameExpr,
  UnknownVarExpr,
  CallExpr,
  ShortcutExpr,
  IndexedAssignExpr,
  FieldExpr,
  LocalVarExpr,
  ThisExpr,
  SuperExpr,
  ItExpr,
  StaticTargetExpr,
  TypeCheckExpr,
  TernaryExpr,
  ComplexLiteral,
  ClosureExpr,
  DslExpr,
  ThrowExpr,
  ExprId,
  ShortcutOp,
  CompilationUnit,
  CFacet,
  FacetDef,
  Stmt,
  NopStmt,
  ExprStmt,
  LocalDefStmt,
  IfStmt,
  ReturnStmt,
  ThrowStmt,
  ForStmt,
  WhileStmt,
  BreakStmt,
  ContinueStmt,
  TryStmt,
  Catch,
  SwitchStmt,
  Case,
  StmtId,
  CType,
  TypeDef,
  Using,
  CSlot,
  SlotDef,
  VisitDepth,
  CField,
  FieldDef,
  CPod,
  PodDef,
  CParam,
  ParamDef,
  CMethod,
  MethodDef,
  TypeRef,
  AstWriter,
  MethodVar,
  EnumDef,
  DslPlugin,
  RegexDslPlugin,
  StrDslPlugin,
  FUtil,
  FPod,
  FTypeRef,
  FFieldRef,
  FMethodRef,
  FSlot,
  FField,
  FPrinter,
  FMethod,
  FMethodVar,
  FAttr,
  FFacet,
  FCodePrinter,
  FOp,
  FOpArg,
  FTable,
  CNamespace,
  FPodNamespace,
  Fanp,
  FType,
  COperators,
  ReflectSlot,
  ReflectField,
  ReflectMethod,
  ReflectParam,
  ReflectFacet,
  ReflectPod,
  ReflectType,
  MarkerFacet,
  ReflectNamespace,
  CBridge,
  NullableType,
  GenericType,
  ListType,
  MapType,
  FuncType,
  GenericParameterType,
  ParameterizedField,
  ParameterizedMethod,
  ParameterizedMethodParam,
  MockSlot,
  MockField,
  MockMethod,
  MockParam,
  TypeParser,
  CDepend,
  Tokenizer,
  TokenVal,
  TokenValDsl,
  Parser,
  Token,
  CompileJs,
  InitInput,
  DefaultCtor,
  ResolveImports,
  Normalize,
  InitEnum,
  CheckErrors,
  ResolveDepends,
  ResolveExpr,
  ClosureVars,
  Parse,
  CheckInheritance,
  Tokenize,
  LoadPod,
  Inherit,
  InitFacet,
  LocaleProps,
  GenerateOutput,
  CheckParamDefs,
  Assemble,
  InitClosures,
  ScanForUsingsAndTypes,
  UsingAndTypeScanner,
  ClosureToImmutable,
  ConstChecks,
  WritePod,
  OrderByInheritance,
  ConstantFolder,
  ApiDocWriter,
  Coercer,
  CallResolver,
};
